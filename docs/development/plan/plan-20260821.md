# UP-01 发布签名与自动升级链--客户端与发布 CI 侧计划（2026-08-21）

> 规范来源: [`docs/development/internal/release-signing-auto-upgrade.md`](../internal/release-signing-auto-upgrade.md)（设计讨论稿，含 D1–D10 冻结决策）
> 模板版本: v2.1（按 `plan-template.md` 成稿；本计划为新建计划，整份符合 v2.1）
> 姊妹交付线: `libra-backend` 仓库 `cf` 分支（Backend Workers 侧：key ceremony、publish API / broker / renew / emergency / Admin UI）。该仓当前没有可引用的 `docs/development/plan/plan-20260822.md`；跨仓契约以本文的 DEP 登记和设计稿为准。
> Long-track ID: `plan-long.md` **UP-01**

## 文档职责

本文把 UP-01 设计稿中落在 **libra 仓库** 的范围拆成可执行任务卡：客户端 upgrade 模块的最后缺口（trust table、单调 generation floor）、`release.yml` 的 workflow 卫生与 publish 调用、`install.sh` / `install.ps1` 的 fail-closed 验签、契约测试补齐。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `src/internal/upgrade/`（trusted_keys、state floor、transition 不变式测试）
- `.github/workflows/release.yml`（workflow 卫生、digest 汇总、`request-stable-manifest` job、environment 划分）
- `install.sh` / `install.ps1`（预置公钥、manifest 验签下载、fail-closed 收敛）
- `tests/upgrade_publish_contract_test.rs` 等测试目标与 `tests/INDEX.md`
- 密钥 ceremony 的公开半边（公钥入库；私钥侧在 libra-backend 计划）

### 非目标

- Backend publish / broker / renew / emergency / Admin UI--由 `libra-backend` 姊妹计划承接（`docs/development/internal/release-signing-auto-upgrade.md` §5–§8）。
- signing Worker 拆分、外部 KMS/HSM、SLSA attestation（设计稿 §10.1 第 9/10 条，未补冻）。
- standby key 预置（设计稿 §7 第 5 条，仅「建议补冻」；若补冻须先改设计稿再改本计划）。
- bucket lock 配置（Cloudflare 控制台动作，登记在上线检查清单，非本仓库代码）。

### 成功定义

- 客户端 `PRODUCTION_TRUSTED_KEYS` 非空版本 **0.22.1**（D9）发布；当前已到达的 `0.22.0` 基线只能由 A1-02 的下一次 patch 发布消费；upgrade state 持久化单调 generation floor。
- `release.yml` 无 pipe-to-shell、action 全部钉 SHA、四平台 digest artifact 强制产出、Windows 补 `.sha256` 与无后缀副本；所有上传对象携带可由 Backend `HEAD` 读取的 SHA-256 checksum。
- tag 发布经 OIDC 调用 Backend publish 成功后 release 绿；未签或签名失败 release 红、客户端 fail-closed 不升级。
- `install.sh` / `install.ps1` 预置公钥并对 stable manifest 验签后下载，验签失败 fail-closed。
- 所有卡 `Lifecycle=done` 且 `Acceptance=complete`，计划完成门全绿。

## 事实基线

> 锚点核对日期：2026-09-01（前次 2026-08-31）。开工时仍须按 ER-02 刷新。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 客户端 trust table | active 公钥已随 **`v0.22.1`**（`fc9c203`）发布进 `PRODUCTION_TRUSTED_KEYS` 与两安装器；D 组证据按前滚口径锚定 `v0.22.2` 绿 run | `src/internal/upgrade/trusted_keys.rs`；修订历史 2026-09-01 行 |
| generation floor | A1-01 源码已持久化单调 floor，并在验签中合并 manifest / 编译期 / persisted 三源；尚未随 A1-01 的 C/D patch（顺延后 `0.22.6`）发布 | `src/internal/upgrade/state.rs`；`src/internal/upgrade/flow.rs`；A1-01 |
| manifest 契约 | Ed25519 envelope、`SIGNATURE_DOMAIN_PREFIX`、四平台 matrix、URL 绑定 | `src/internal/upgrade/manifest.rs` |
| 契约测试 | envelope / matrix / `paused`·`revoked_versions` 验签已钉死；无 transition 不变式测试 | `tests/upgrade_publish_contract_test.rs` |
| 测试 feature 门 | `upgrade_auto_test` / `upgrade_publish_contract_test` 均带 `required-features = ["test-upgrade"]`，默认 feature 集不含；CI 权威口径含 `--features test-upgrade` + `LIBRA_TEST=1` + `--test-threads=1` | `Cargo.toml:543-551`；`.github/workflows/base.yml:439-449` |
| 发布 CI | tag `v*` -> 四平台 build -> rclone（钉版本+SHA-256 校验的 archive）上传 R2 -> 上传 install 脚本；A1-03 卫生已生效并经 `v0.22.2` 真实 run 验证；无签名、无 stable manifest（A1-04/A1-09 未做） | `.github/workflows/release.yml`；run `33453752700` |
| pipe-to-shell / 钉 SHA | 已消除：workflow 零 pipe-to-shell，全部 action 钉 40 位 SHA（A1-03，随 `v0.22.1`/`v0.22.2` 发布） | `.github/workflows/release.yml`；A1-03 守卫命令 |
| digest artifact | 四平台 per-leg digest artifact 强制（无 `continue-on-error`）；Windows 产出 `.sha256` 与无后缀副本，均经 `v0.22.2` run 上传且 CDN HEAD 200 | `.github/workflows/release.yml`；修订历史 2026-09-01 行 |
| install.sh 现状 | 仅 sha256 校验，可跳过；release API 不可达默认 fail-fast，`LIBRA_ALLOW_FALLBACK=1` 回退未签名 `DEFAULT_VERSION`；A1-02 公钥已内嵌但为休眠常量（验签流程 A1-05 承接） | `install.sh`；设计稿 §4 |
| install.ps1 现状 | 无 release API 流程，静态 `$DefaultVersion` 直接下载，完全无校验；A1-02 公钥已内嵌但为休眠常量 | `install.ps1`；设计稿 §4 |
| 版本面 | 五处版本面当前均为 `0.22.2`；tag `v0.22.1`（A1-02 D9 C 组）与 `v0.22.2`（A1-03 rclone env 前滚修复）均已推送。D9 的 `0.22.0 -> 0.22.1` gate 已由 A1-02 按 ADR-UP01-02 消费；下一发布槽位（合并后 `0.22.6`）归 A1-01 | `Cargo.toml:3`；`tests/compat/version_surface_sync.rs`；`libra tag -l`；修订历史 2026-09-01 行 |
| Backend | origin/cf @ `baf869a` 已绑定 `R2_ARTIFACTS`->`artifacts`；无 release 写入代码路径 | `libra-backend` origin/cf |
| 设计稿 | 18 轮 review 修订后的冻结决策 D1–D10 | `docs/development/internal/release-signing-auto-upgrade.md` |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01（已解决，2026-09-01） | trust root 已随 `v0.22.1` 发布（客户端 + 两安装器同一定值）；D 组绿 run 经 `v0.22.2` 前滚取得 | 已闭合 | `fc9c203`；run `33453752700`；修订历史 2026-09-01 行 | A1-02 已完成 |
| GAP-02（本地已解决，待发布） | 已实现持久化 generation floor；尚未随 A1-01 C/D patch（顺延后 `0.22.6`）进入已发布客户端 | 在发布前，已发布客户端（≤ 最新已 tag 版本 `v0.22.2`）仍不能抵御 manifest floor 回退 | `src/internal/upgrade/state.rs`；A1-01 | A1-01 |
| GAP-03（已解决，2026-09-01） | `v0.22.1` 首次真实 run 暴露 rclone env 冲突并前滚修复；`v0.22.2` run 成功，供应链加固已在 GitHub runner 生效 | 已闭合 | run `33417381720`（失败）；run `33453752700`（成功）；`96ddb7c` | A1-03 已完成 |
| GAP-04（已解决，2026-09-01） | Windows `.sha256`、无后缀副本与四平台强制 digest artifact 经 `v0.22.2` 真实 run 上传，CDN HEAD 200 | 已闭合 | `curl -fsSI` 三对象 200（2026-09-01T02:09Z）；修订历史 2026-09-01 行 | A1-03 已完成 |
| GAP-05 | 无 publish 调用（OIDC） | stable manifest 永不签发 | `.github/workflows/release.yml` | A1-04 |
| GAP-06 | install 脚本不验签 manifest | bootstrap 信任锚缺失 | `install.sh` / `install.ps1` | A1-05 |
| GAP-07 | 无 transition 不变式测试 | publish/renew/emergency 变换无守卫 | `tests/upgrade_publish_contract_test.rs` | A1-06 |
| GAP-08（已解决） | plan-long 的 UP-01 A.1–A.12 全文已失（文档债） | 历史规格只剩摘要；不得复制形成第二事实源 | `plan-long.md`；设计稿 §10 | A0-01 已在 plan-long 登记「不回填，设计稿为事实源」 |
| GAP-09（已解决） | 原 `0.20.x -> 0.22.0` 的版本可达性缺口 | 两次 minor 跃迁已由其它已完成计划发布，不能再次执行 | `Cargo.toml:3`；`plan-20260824.md` DF-08 交付记录 | A1-08a/A1-08b 退役；A1-02 成为唯一允许消费 `0.22.1` 槽位的卡 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | UP-01 长期条目 | A0-01 同步状态并链接本计划；A.1–A.12 文档债以设计稿为事实源外链登记 |
| `docs/development/internal/release-signing-auto-upgrade.md` | 设计契约（D1–D10） | 本计划不得偏离；偏离须先改设计稿再改计划 |
| `libra-backend` `cf` 交付线 | 姊妹交付线（Backend 侧） | 跨仓依赖经 DEP-01/DEP-02/DEP-05 / 跨仓交付 DEP 登记（见依赖登记表）；ceremony 公钥定值以 backend B1-00 为唯一生产者。当前无可引用的 backend `plan-20260822.md`，不得误链本仓 LR-02/LR-03 计划 |
| `docs/auto-upgrade.md` | 客户端用户面契约 | A1-01/A1-02 同步其口径 |
| `docs/development/plan/plan-20260818.md` 等近期计划 | 无写集交集 | 不触碰 |
| `docs/development/plan/plan-20260729.md`（CT-01） | 已完成，版本面竞争不再活跃 | 保留 ER-12 的全仓单一发布者约束；A1-02 开始 C 组前复核没有其它已 bump 未发布的工作流，若版本已超过 `0.22.0` 则停止并走 D9 重裁 ADR |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | UP-01 已列为下一执行任务（plan-long） | 无 |
| 可行性 | 客户端契约已 code-complete，剩余为增量 | 无 |
| 任务卡粒度 | A1-03、A1-04、A1-09 保持三个独立轴；A1-08a/A1-08b 的纯版本跳变已被当前基线吸收而退役 | A1-09 先于 A1-04 发布，避免广域 R2 凭据参与首签；A1-06 改为消费 Backend 向量，避免在客户端复制状态机 |
| 依赖与顺序 | A1-02 仅依赖 ceremony DEP-02，且必须是 `0.22.0` 后的下一张发版卡；A1-09 在 A1-04 前完成，A1-05 在 A1-04 前完成；A1-07 依赖首签 DEP-05 | 已登记 |
| 完整性 | 测试/文档/回滚逐卡登记 | 无 |
| 安全性 | 私钥永不入本仓库（D2）；install fail-closed | ADR-UP01-02/03 |
| 功能正确性 | Backend 是 publish/renew/emergency transition 的唯一实现；客户端只消费 Backend 生成且签名的契约向量并验签 | A1-06 / DEP-07 |
| 接口兼容 | 客户端契约不改（设计稿 §3） | ADR-UP01-01 |
| 数据流与控制流 | floor 持久化为单调不降 | A1-01 |
| 性能与容量 | install 脚本新增一次 manifest 拉取（< 64 KiB），无热路径影响 | 无 |
| 可靠性与容错 | install 验签失败 fail-closed；release publish 失败整次红 | A1-04/A1-05 |
| 可维护性 | 设计稿为单一事实源 | 无 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 -> 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-02 | v0.22.9 macOS 兼容性热修（收口后跟进） | 用户在 macOS 实测安装被拒：BSD grep 对 `{n,m}` 重复上界限 255,A1-05 全 payload 文法的 `{1,256}`/`{0,1024}` 触发 `maximum repetition exceeds 255` 后 fail-closed（行为正确、兼容性缺陷,GNU grep 平台不受影响）。修复:URL 界收 `{1,255}`（精确 URL 绑定本就钉死）、撤销列表改无括号无界类（逐条目校验 + 1 MiB 上限仍在）、ps1 同步;冒烟 harness 增静态守卫（任一安装器出现 >255 重复界即红）。`v0.22.9` run `33525792911` 8/8 全绿,CDN/manifest（rev 3）线上验证,用户三平台（linux-amd64/linux-arm64/darwin-arm64）实测通过 | 无拆卡 | install.sh/install.ps1、run.sh 守卫、版本面（0.22.9） |
| 2026-09-01 | 发布列车放行与 D10 首签（执行期） | 用户完成管理动作：cf 分支自动部署 Worker（探测确认 publish/upload-credential 422 活跃、manifest 404 过渡态）、`LIBRA_R2_S3_*` secrets 写入、四个长期 `R2_*` GitHub secrets 删除（GC-UP01-1，Claude 侧被权限门拦截由用户执行）。**发布执行**：`v0.22.6` = 被消耗槽位（attempt1 缺 S3 secrets 503 `broker_unconfigured`;attempt2 两 linux 腿工件已入不可变前缀后 Windows 编译红——`attributes.rs` 用 nightly-only `windows_by_handle`，base.yml 不编 Windows 故 CI 从未暴露）；ER-10 前滚 `v0.22.7`：改用 `GetFileInformationByHandle`（windows-sys 直调，msvc 目标 type-check 过）+ release.yml broker 调用 5xx 有界重试（每次换新 OIDC token，jti 一次性）——四平台构建/上传、homebrew、`request-stable-manifest` 全绿，**D10 首签落地**（`libra-release-1` 签名 manifest 200，openssl 独立验签通过，生产 install.sh 真实走签名通道装出 `libra 0.22.7` = A1-07 端到端）；唯一红步 CDN 版本核验系 `printf|grep -Fq` pipefail SIGPIPE 假阴性（install.sh 超管道缓冲后必现），herestring 修复后 ER-10 前滚 `v0.22.8` | A1-01/A1-04/A1-09: -> done；A1-05/A1-06/A1-07: D 组闭合 | 版本面（0.22.8）、`release.yml`、`src/utils/attributes.rs`、DEP-05、完成判据 |
| 2026-09-01 | 收口三门与前滚修复（执行期） | fmt/clippy 零告警；`source .env.test && cargo test --all`（-j8，--no-fail-fast）225 个套件仅 1 失败且已归因 DEFER-05（预存在存储锁饿死，写集零交集）；门中另暴露并前滚修复预存在缺陷：`agent_bridge_read_methods_test::diff_get_without_a_repository_fails_closed_on_scope` 依赖进程 cwd 不在任何 Libra 仓库内（CI checkout 无 `.libra` 掩盖了逃逸）——测试加 `ChangeDirGuard` + `#[serial(cwd)]` 停靠空 tempdir，SERIAL_MANIFEST/REGISTRY 补行、`compat_serial_registry` 18/18 全绿；install-smoke 双 harness（24 sh / 22 ps1，含便携 pwsh 7.4.6）在门内实际执行 | 无拆卡 | 完成判据、DEFER-05/06、C-T3/C-T4 评审行 |
| 2026-09-01 | origin/main 合流与槽位顺延（执行期） | 并发工作流已把版本面 bump 至 `0.22.5`（未 tag：远端最新 tag 仍为 `v0.22.2`）并合入 LFS FastCDC、OL-01、CI 修复等 PR；本计划全部提交经 merge `cfd916e` 与其合流并推送 origin/main。冲突仅版本面五处 + 安装器信任块，取 theirs 版本号 + ours 内容；合并后聚焦测试、八场景冒烟、fmt/clippy 全绿。**槽位顺延：A1-01 的 C 组 = 合并基线 patch+1 = `0.22.6`**（`0.22.3`-`0.22.5` 由并发工作流的 bump 占用）。**发布门（执行期事实）**：`release.yml` 已 broker-only，Worker 部署 + R2 S3 Secrets + GitHub environments 就绪前任何新 tag 必 fail-closed；A1-01 及后续所有 C 组统一排在该门之后，首个绿 run 兼作 A1-09 的 D 组上传证据。 | 无拆卡 | 版本面、A1-01/A1-06/A1-05/A1-09/A1-04 槽位、发布窗口执行记录 |
| 2026-09-01 | A1-09/A1-04 workflow 实现（执行期） | `release.yml` 重构：rclone 与 `secrets.R2_*` 全部移除，上传改为 Backend credential broker 的逐对象 presigned PUT（不可变资产签入 `If-None-Match: *`；桶根安装脚本可覆写、不签该头——broker 侧按 environment 差异化）；两 upload job 划入 `release-artifacts`/`release-installers` environments；新增 `request-stable-manifest` job（`release-publish` + `aud=libra-release-publish`，四平台 digest+size artifact 强制汇总）。compat guard `release_rclone_env_guard` 从「rclone env 卫生」改为「零 rclone/零长期凭据」断言（A1-03 卡的 rclone 钉版本守卫被本次收口取代，历史记录保留）。**用户侧待办（权限门拦截）**：Worker 部署、R2 S3 Secrets、GitHub environments 创建、长期 `R2_*` secrets 删除。 | A1-09: blocked -> in-progress；A1-04: pending -> in-progress | `release.yml`、`tests/compat/release_rclone_env_guard.rs`、`tests/compat/README.md`、A1-03/A1-09/A1-04 |
| 2026-09-01 | backend B1-02/B1-04/B1-05 交付（执行期） | libra-backend `cf` 落地：`bed79d0`（B1-00 smoke 实现收编）、`a165e1c`（B1-02 transition engine + UP-01 contract vectors，DEP-07 交付）、`0939076`（B1-04 publish 端点 + B1-05 credential broker + D7 renew cron，42 个 release-signing 单测含 §5.3 OIDC 拒绝矩阵与 miniflare R2+D1 端到端 publish/renew）。A1-06 客户端消费测试随 libra `8b37320` 落地（11 passed）。DEP-01/DEP-03 转为「代码已交付，待生产部署与 D 组联调」；A1-06 blocked -> in-progress/complete（C 组待 `0.22.4`）。 | A1-06: blocked -> in-progress | DEP-01、DEP-03、DEP-07、A1-06、A1-09 |
| 2026-09-01 | A1-02/A1-03 C/D 收口（执行期） | ① A1-02 C 组已执行：`fc9c203` = `v0.22.1`（D9，版本面五处 0.22.0 -> 0.22.1，trust root + 两安装器公钥 + smoke fixture 随发布进入渠道）；其首次真实 tag run **失败**（run `33417381720`：workflow 顶层 `RCLONE_VERSION` 被 rclone 解析为其 `--version` 选项，ARM64 leg 在任何 R2 写入前失败、matrix fail-fast 取消其余 leg，未发布任何资产；设计稿变更记录 #23）。② 前滚修复以 `v0.22.2` 发布（`96ddb7c`：workflow 自有变量改 `LIBRA_RCLONE_*` + 新 compat guard `tests/compat/release_rclone_env_guard.rs`，版本面五处 0.22.2）；其 run `33453752700` **成功**，四平台资产、Windows `.sha256` 与无后缀副本 `libra-windows-amd64` 均 CDN HEAD 200（2026-09-01T02:09Z 实测）。A1-02/A1-03 的 D 组证据按 ER-10 前滚口径锚定 v0.22.2 绿 run（v0.22.1 run 不重跑：修复必须换新 tag，见设计稿 #23）。③ 该前滚修复消费了 `0.22.2` 槽位，发布窗口顺序相应顺延：A1-01 的 C 组 = `0.22.3`。④ A1-07 按 DEP-05 登记的失败策略由 pending 置 `blocked`（首签未发生，`stable/manifest-v1.json` 实测 404）。 | A1-02: in-progress -> done；A1-03: in-progress -> done；A1-07: pending -> blocked | 事实基线、GAP-01..04、A1-02、A1-03、A1-07、发布窗口顺序、完成判据 |
| 2026-08-31 | A1-02 DEP-02 ceremony proof 验收 | 用户升级 Worker 容量后，默认 `libra-backend` 成功部署版本 `d64e1887-a990-4236-8539-143c9f524a49`；`*/15` cron 写入公开 smoke proof，15:45:58Z 后 URL 为 200。fixture 由已部署 proof 固定，客户端用 production trust root 验通独立 smoke domain，且同一签名在 manifest domain / manifest parser 均拒绝 | A1-02: blocked -> in-progress | DEP-02、A1-02、设计稿变更记录 #21 |
| 2026-08-31 | A1-02 生产部署尝试 | 经已登录 Wrangler 对默认 `libra-backend` 执行 `pnpm deploy:cf`；构建成功，但 Cloudflare 在创建版本前以 `10027` 拒绝当前 Worker（超过免费层 3 MiB 脚本限制，构建总模块约 17.3 MiB）。无新生产版本、无 R2 smoke object；A1-02 置 `blocked`，恢复条件为把可部署 Worker 体积收敛到账户限额或在不降低安全边界的前提下提供合规部署容量 | A1-02: in-progress -> blocked | DEP-02、A1-02、风险登记 |
| 2026-08-31 | 执行期 DEP 可用性复核 | 当前 sibling backend checkout 仅含 release-signing smoke 实现；未发现 `tests/data/up01-*.json` / B1-02 transition vector，亦未发现 `/api/internal/release/upload-credential` broker 路由。按既有 DEP-07/DEP-03 失败策略，将 A1-06、A1-09 明确置为 `blocked`，禁止以客户端 fixture、状态机或长期 R2 凭据绕过前置 | A1-06/A1-09: pending -> blocked | DEP-03、DEP-07、Phase 1/2、风险登记 |
| 2026-08-31 | Claude Code C-R10（A1-03 P2-1/2/3/4/5） | 统一 `upload-artifact` 为同一 SHA-pinned v5；Windows rclone 下载加 120 秒上限；Windows 两份发布副本均与 build 源文件 hash 比对；rclone 版本/四平台 SHA 提升到 workflow 顶层单一 env；test-upgrade release guard 覆盖 Windows | 无拆卡 | A1-03 `release.yml`；任务证据 |
| 2026-08-31 | Claude Code C-R8（P0-1、P1-1、P2-1） | ① Phase A 改为只保留内存 candidate；Phase B 持锁后先 CAS baseline 再 stage/probe/txn，startup recovery 同锁，消除并发 install/failure-backoff 覆盖已推进 anti-rollback state 的 P0；② state storage 改用 `InstallDir` fd-relative no-follow read/write，`0600` 在临时文件创建时设定，消除 path I/O 与 rename 后 chmod 窗口的 P1；③ revision 0 已有 digest 时也拒绝 fork，并补 stale-backoff、symlink storage 与 revision-0 回归用例 | 无拆卡 | A1-01 `state.rs`、`orchestrator.rs`、`txn.rs`、任务证据 |
| 2026-08-31 | Claude Code C-R5（P1-1、P2-1/2/3） | 删除不构成 D9 前提、且与 `plan-20260824` 环境折算口径不一致的 `release.yml` run 成功断言，改为 annotated-tag 基线；修正 `test-upgrade` 两个源码锚点；A1-07 按 `release` 类型把粒度摘要和审计表分母从 `/8` 改为 `/12` | 无拆卡 | 事实基线、A1-07 粒度行、粒度审计表、评审记录 |
| 2026-08-31 | 执行期安全修订 | ① A0-01 完成：`plan-long` 以本计划和真实 `libra-backend` `cf` 交付线为事实源，登记 A.1–A.12 不回填、以设计稿为唯一事实源，并删除对不存在 backend `plan-20260822` 的误链；② DEP-02/A1-02 将 public smoke 改为独立 domain key-pair proof。禁止 client-valid manifest smoke，防有 `stable/` 写权限者重放成为错误 stable | 无拆卡 | plan-long、关系表、DEP-02、A0-01、A1-02、设计稿 §7.2 |
| 2026-08-31 | 现状复核（本计划自审） | ① 刷新版本基线为已发布 `0.22.0`，退役已无可执行性的 A1-08a/A1-08b，并把 D9 保护改为 A1-02 必须消费下一 patch `0.22.1`；② 重排首签链：A1-09 的 broker/去长期凭据和 A1-05 的安装器验签均在 A1-04 首签前完成，A1-09 禁止回退长期 R2 凭据；③ A1-06 改为 Backend transition 向量的客户端消费测试，避免双实现；④ 补 A1-03 的各平台固定 rclone 校验与 A1-05 的 EN/zh 安装文档及无运行时测试公钥覆盖要求；⑤ 当前 Libra checkout 无 `.git` 自动发现上下文，故发布一律用 `libra tag` + `libra push origin`，D 组 GitHub Actions 证据用显式 `gh api` 仓库路径（`origin` 本身已复核为 GitHub SSH remote） | A1-08a/A1-08b -> 退役历史卡；A1-06/A1-09 重定范围 | 事实基线、ADR-UP01-02/05、ER-UP01-1/4、实施顺序、DEP/REL、Phase、A1-02/03/04/05/06/07/09、粒度审计、测试/追溯/风险/完成判据 |
| 2026-08-22 | 自审 | 成稿 | N/A | N/A |
| 2026-08-22 | Codex R1（P1-1、P2-1） | 新增 ADR-UP01-04：DEP-03 降级为 informational，A1-04 去除 DEP-03 硬依赖（跨仓闭环消除）；A1-05 验证改为 `tests/data/install-smoke/` 可复现自动化冒烟（五场景 fixture + run.sh/run.ps1） | 无拆分（A1-04/A1-05 卡内修订） | 依赖登记表 DEP-03、A1-04/A1-05 的 Dependencies/Verification/Granularity、粒度审计表、ADR 集 |
| 2026-08-22 | Claude Code C-R4（P1-4、P2-1/2/4/6/7/8/9） | ① REL-02 改完整表登记（成员/唯一发布点/窗口规则含进入条件=DEP-02 就绪、7 天超时上限与超时动作/失败回滚顺序/理由），A1-08b 补 DEP-02 前置，A1-08b/A1-02 Release boundary 补 REL-02 引用，DEP-02 失败策略重写（删「A1-05 先合入发版」冲突表述、加等待上限），A1-02 卡内补 D9 重裁逃生路径（P1-4）；② 实施顺序补 `DEP-07 -> A1-06`、`A1-05 -> A1-07` 边（P2-1）；③ `A1-01 -> A1-06` 序列化边反转（解 floor 卡跨仓传递阻断）（P2-7）；④ 钉 SHA 守卫真正改 allowlist 逐行断言（P2-4）；⑤ DEP-01 行 A1-08 引用同步 08a/08b（P2-6）；⑥ A1-05/A1-06 计数口径重算（顿号并列逐条计）（P2-8）；⑦ REL 编号保留说明（P2-9）；backend 侧 C-R4 findings 同批修复 | 无拆分 | REL-02 表、依赖登记表、实施顺序、A1-01/A1-02/A1-05/A1-06/A1-08b、并发声明、粒度审计表 |
| 2026-08-22 | Claude Code C-R3（P1-5/P1-6、P2-1/2/3/5/8/10） | ① 新增 DEP-07（backend B1-02 fixture 移交），A1-06 跨实现验签 AC 改二态判定（真 fixture 未到即 blocked，不得本地冒充）（P1-5）；② 新增 REL-02 排他发布窗口（A1-08b -> A1-02，0.22.1 槽位排他，CT-01 让位例外），A1-02 补收口断言与 D9 重裁逃生路径（P1-6）；③ 钉 SHA 守卫改真 allowlist 正则（P2-1）；④ 测试矩阵同步六场景（P2-2）；⑤ A1-03 C/D coverage job 名修正（P2-3）；⑥ A1-08a/b release 卡分母改 12、ER-04 强制门移出 VER 计数（P2-5）；⑦ Phase/M1 版本轨道序列跨 Phase 表述修正（P2-8）；⑧ DEP-04 outgoing 配对登记（P2-10）；backend 侧 C-R3 findings 已同批修复 | 无拆分 | 依赖登记表、A1-02/A1-03/A1-06/A1-08a/A1-08b、测试矩阵、粒度审计表、Phase 1/M1、发布分组 |
| 2026-08-22 | Claude Code C-R2（P1-7/P1-8、P2-1/2/3） | ① A1-08 拆为 A1-08a/A1-08b 两张 release 卡（各一跳 minor、各一次发布切片），删除 EX-01（不在 waiver 白名单，minor 通道由 ADR-UP01-05 直接论证）（P1-7）；② A1-05 定义 manifest 404 过渡态（签名链未启用，与验签失败严格区分，显式确认路径），冒烟扩为六场景，风险表登记 A1-05 发布至 B1-09 首签窗口（P1-8）；③ A1-05 冒烟公钥覆盖机制文档化（P2-1）；④ A1-06 fixture 依赖收敛为登记式表述（P2-2）；⑤ A1-03 钉 SHA 守卫改 allowlist 正则（P2-3）；backend 侧全部 C-R2 findings 已在同批落地（见 backend 计划修订历史） | A1-08 -> A1-08a, A1-08b | ADR-UP01-02/05、EX 表、实施顺序、发布窗口顺序、A1-02/A1-03/A1-05/A1-06、粒度审计表、风险登记、完成判据 |
| 2026-08-22 | Claude Code C-R1 补充修复（P1-2/P1-5/P1-6，backend 侧主体） | backend B1-00~B1-03 发版边界改为 independent/本仓 ADR-UP01-B3 口径（P1-2）；jti 一次性消费表归属 B1-03 承接（P1-5）；跨实现验签 fixture `tests/data/up01-envelope-fixture.json` 双侧引用（P1-6，libra 侧 A1-06 消费） | 无拆分 | backend 计划 B1-00~B1-03、本计划 A1-06 |
| 2026-08-22 | Claude Code R1（P0-1、P1-1/3/4/7/8、P2-1/2/3/4/6/7/8/9/10） | ① 新增 A1-08 版本跃迁卡（0.20.x -> 0.21.0 -> 0.22.0，ADR-UP01-05 + EX-01），A1-02 依赖 A1-08（P0-1）；② 全部 test-upgrade 命令改 CI 权威口径（P1-1）；③ A1-04 拆出 A1-09「upload jobs 接入 broker 短期凭据」（P1-3）；④ 新增 DEP-05（backend B1-09 首签），A1-07 引用（P1-4）；⑤ DEP-02 对象钉死 backend#B1-00（P1-7）；⑥ 发布窗口顺序与依赖边一致化（P1-8）；⑦ A1-02 并入两安装器公钥预置（D9 括号语义，P2-2）；⑧ A1-03 补 D 组登记、A1-05 冒烟包 cargo `#[test]` 入 CI（P2-1/7）；⑨ 并发声明修正、新增「字段全局默认与例外」、CT-01 版本面冲突登记（P2-3/9/10）；⑩ 完成判据 web/worker 表述修正（P2-8）；零命中守卫改模板多行范式（P2-11） | A1-04 -> A1-04, A1-09；A1-02 扩轴（D9 括号）；新增 A1-08 | 实施顺序、依赖登记表、发布窗口顺序、粒度审计表、测试矩阵、ADR 集、waiver 表、完成判据 |

## 已决议设计决策

设计稿 D1–D10 直接构成本计划的 ADR 集，引用而非复制；下列为本计划新增的裁决。

### ADR-UP01-01: 客户端契约零改动

- **Status:** Accepted（继承设计稿 §3）
- **Context:** 客户端 fail-closed 合同已实现并被契约测试钉死。
- **Decision:** 本计划所有卡不得修改 `src/internal/upgrade/manifest.rs` 的 URL 语法、envelope 结构、验签流程；只做增量（trust table 填值、state floor 字段）。
- **Alternatives considered:** 顺带重构 upgrade 模块--拒绝（多轴）。
- **Consequences:** 发布侧必须满足既有契约。
- **Revisit when:** 设计稿修订。

### ADR-UP01-02: trust table 非空版本绑死 0.22.1（D9）

- **Status:** Accepted（继承 D9；2026-08-31 现状复核修订）
- **Context:** 首个 trust 客户端版本已冻结为 `0.22.1`；仓库和五处版本面已在其它计划中到达且发布 `0.22.0`，两张旧 minor 跃迁卡已无可执行性。
- **Decision:** A1-02 在 DEP-02 ceremony 就绪后必须成为**当前 `0.22.0` 的下一张发版卡**，并按其自身 `patch + 1` 发布 `0.22.1`。在 A1-02 的 C 组完成前，任何其它计划或本计划独立卡不得消费 `0.22.1` 槽位；若开工复核发现版本已不是 `0.22.0`，停止执行并先重裁 D9。
- **Alternatives considered:** 重跑 A1-08a/A1-08b--不采纳（会倒退或重复已发布版本）；修订 D9 为「当值 patch 版本」--不采纳（D9 已冻结）。
- **Consequences:** A1-05 依赖 A1-02 的公钥定值；其余卡可做不发版的准备和本地验收，但其 C 组发布必须排在 A1-02 之后。
- **Revisit when:** D9 修订。

### ADR-UP01-03: install 脚本 fail-closed 契约统一

- **Status:** Accepted（设计稿 §10.1 第 3 条建议稿）
- **Context:** 脚本从同一可写 CDN 分发，内嵌公钥不能防整体替换；bootstrap 信任锚须带外（GitHub repo 公布哈希）。
- **Decision:** A1-05 落地后：manifest 拉取/验签失败即 fail-closed（未签名回退仅 `LIBRA_ALLOW_FALLBACK=1` 显式 opt-in 保留语义、PowerShell 对齐）；`-v` 指定历史版本与自定义 mirror 为显式 opt-in 的未验签路径。
- **Alternatives considered:** 保持 PowerShell 静态版本默认--拒绝（无校验面）。
- **Consequences:** 需同步 README 与安装文档的信任假设说明。
- **Revisit when:** 设计稿 §10.1 第 3 条补冻。

### ADR-UP01-04: release.yml ↔ broker 调用契约以设计稿为唯一事实源

- **Status:** Accepted（消除跨仓双向依赖闭环，Codex R1 P1-1）
- **Context:** `A1-04`（workflow 调用方）与 backend `B1-05`（broker 实现）若互为前置会形成跨仓闭环，计划不可执行。
- **Decision:** 调用契约（`aud`、三类 `environment`、请求体字段、presigned PUT 头、对象键白名单）以设计稿 §5.2/§5.3 的冻结文本为唯一事实源（版本以设计稿 git commit 锚定）；本计划与 backend 计划各自**消费**该契约、互不构成硬依赖。backend 交付的 broker 端点（原 DEP-03）对 A1-04 为 **informational**。A1-09（upload jobs 真实接入 broker）**硬依赖** DEP-03：该卡的范围本身即「接入已上线的 broker」，端点未上线时置 `blocked`。
- **Alternatives considered:** 把契约抽为独立合同文档--拒绝（设计稿 §5.2 已是冻结契约，再抄一份即两个事实源）。
- **Consequences:** A1-04 用合成 OIDC JWT fixture 即可完整验收；真实 broker 联调证据归 A1-09 的 D 组/上线检查清单。
- **Revisit when:** 设计稿 §5.2 修订时双方同批对齐。

### ADR-UP01-06: 安装器验签原语选型（预检 P1-4）

- **Context:** A1-05 要求两个安装器默认路径对 stable manifest 做 Ed25519 验签、失败 fail-closed。设计稿 §10.1 第 3 条将「install.sh 验签选型（openssl / 其它）」列为待讨论项，尚未冻结；PowerShell 侧无原生 Ed25519（CNG/.NET BCL 均不支持），POSIX 侧 `openssl pkeyutl -verify` 需 OpenSSL 1.1.1+，「验签器不可用」是与「验签失败」不同的第三态。
- **Decision:** 冻结为：POSIX `install.sh` 走 `openssl pkeyutl`（运行前版本探测，OpenSSL < 1.1.1 判为「验签器不可用」）；PowerShell `install.ps1` 嵌入随脚本分发的最小独立 verifier（自包含、无模块依赖）；「验签器不可用」-> 与 manifest 404 同档的显式确认过渡路径（提示 + 用户确认/`LIBRA_ALLOW_FALLBACK=1`），不静默跳过、不直接 fail-closed 到不可用。本决策回写设计稿 §10.1 补冻（本计划开工前同步设计讨论稿变更记录）。
- **Alternatives:** BouncyCastle 模块依赖（需网络拉取，bootstrap 阶段不可接受）；降级为纯 opt-in 未验签（削弱 D9 首个信任版本语义，弃）。
- **Consequences:** A1-05 的 AC 与冒烟增「验签器不可用」场景（第七场景）；scope 维持 M（verifier 以最小独立脚本随安装器分发，计入本卡 prod-files）。

### ADR-UP01-05: 历史版本跃迁记录（0.21.0 / 0.22.0）

- **Status:** Superseded（2026-08-31）
- **Context:** 原计划在 `0.20.3` 基线下用 A1-08a/A1-08b 到达 `0.22.0`。实际版本面已由其它计划在 `v0.22.0` 交付；该 minor 的发布理由是那个计划的公开协议变更，而不是尚未实施的 UP-01 activation。
- **Decision:** 保留本 ADR 与 A1-08a/A1-08b 的历史记录以解释旧 review finding，但它们不再是本计划的可执行任务或 D9 依据。活跃规则唯一见 ADR-UP01-02。
- **Alternatives considered:** 重新归因或重发两个 minor--不采纳（会伪造历史并破坏 semver/发布不变量）。
- **Consequences:** `tests/compat/version_surface_sync` 继续只校验版本面一致性；本计划不再提出 minor bump。
- **Revisit when:** D9 修订或仓库版本发生意外漂移。

## 全局工程约束

继承 `plan-template.md` 的 GC-01 至 GC-12 全文（不在本计划重复），并额外强调：

- **GC-UP01-1（D2 边界）:** 任何卡不得在 `.github/workflows/**`、repo secrets、Environment secrets 引入签名私钥或 R2 parent 凭据。当前 `secrets.R2_*` 仅是 A1-09 前的已知过渡事实；**A1-04 不得在 A1-09 删除它们并取得 D 组上传证据前发布**，因此第一份 signed stable manifest 不会由持有全桶长期凭据的 Action 触发。A1-09 完成后不得以任何回滚/补偿路径恢复这些凭据。
- **GC-UP01-2（占位符语义）:** `{tag}` 含 `v` 前缀（如 `v0.20.3`）；对象键 `libra/releases/{tag}/…`。契约用例防 `vv` 双前缀回归（设计稿第 16 条变更）。

## 执行检查必备需求（强制）

继承 `plan-template.md` ER-01 至 ER-12 全文（含 ER-04 四组门、ER-07 Libra 工作流与提交签名、ER-08 每卡 `patch + 1` + 版本面五处 + tag 发布、ER-12 串行发布），不在此复制。补充：

- **ER-UP01-1:** 涉及 `release.yml` 的卡，A 组门按「仓库配置与 CI 展开规则」从 `.github/workflows/release.yml` 现场提取受影响 job；OIDC 换 token、rclone 上传、tag push 触发等 action-only / 远端语义归 D 组，登记 workflow、job、触发事件与 ref（`release.yml` = `push` `v*` tag）。
- **ER-UP01-2:** 涉及私钥 ceremony 的动作（公钥定值、Secret 写入）只登记 sanitized 证据（ER-11）；公钥 32 字节 hex 可入文档，私钥材料任何形式不得入库、入日志、入聊天。
- **ER-UP01-3（feature 门并入命令）:** 凡引用 `upgrade_auto_test` / `upgrade_publish_contract_test` 的 Verification 一律用 CI 权威口径（`Cargo.toml:543-551` 的 `required-features` + `.github/workflows/base.yml:439-449`）：`source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test <target> -- --test-threads=1`；`cargo test --all` 不覆盖这两个 target，相关卡的守卫以本口径为准。
- **ER-UP01-4（tag 与远端证据）：** 当前 `origin` 已复核为 GitHub SSH remote，但 Libra checkout 没有 `.git` 自动发现上下文；禁止把隐式 `gh run` 或 `gh release create` 当作可执行发布证据。每张 release 卡完成 C 组后用 `libra tag "v<version>"` 与 `libra push origin "v<version>"` 触发 `release.yml`；D 组以显式仓库 API 查询 run，例如 `gh api 'repos/libra-tools/libra/actions/workflows/release.yml/runs?branch=v<version>&event=push&per_page=1' --jq '.workflow_runs[0] | select(.conclusion == "success")'`，并保存返回的 run URL/ID。若 GitHub 镜像或该 ref 不可查询，卡停 `blocked`，不得用无证据的本地 tag 或 `gh release` 替代。

## 字段全局默认与例外

- **Release boundary 默认:** independent（每卡完整 C 组：`patch + 1` + 版本面五处 + `libra tag` / `libra push origin`）
- **Task type 默认:** implementation
- **Rollback mode 默认:** revert
- **Migration and rollback 默认:** N/A（无 schema 迁移）
- **Security and privacy 默认:** 继承 GC-07、GC-11、GC-UP01-1/2
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 按 GC-05 同步 EN + zh（无命令面变化时为设计稿变更记录 + README）

**默认覆盖**（非默认值，无需审批）：

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| A0-01 | Release boundary | no-release（audit 卡，产物随 A1-01 发布进入渠道） |
| A1-01 | Rollback mode | forward-only（state 字段已写入不回收） |
| A1-02 | Rollback mode | forward-only（已发布公钥不可撤回） |
| A1-09 | Rollback mode | forward-only（broker 故障时 release fail-closed，禁止恢复长期 R2 凭据） |

**规则 waiver（`EX-*`）**：

| 例外 ID | 任务（或 `ALL/<作用域>`） | 豁免项 | 理由与补偿措施 | Approver | Review round | 证据 | 有效期 |
|---|---|---|---|---|---|---|---|

## 实施顺序

- `A0-01 -> A1-01`
- `A0-01 -> A1-03`
- `A0-01 -> A1-06`
- `DEP-07 -> A1-06`（跨实现 fixture 移交，Claude C-R4 P2-1 补边）
- `DEP-02 -> A1-02`（ceremony 公钥定值，backend B1-00 产出）
- **D9 release gate:** A1-02 must be the first C-group release from the `0.22.0` baseline and therefore publish `0.22.1`; a version other than `0.22.0` at that point is a hard stop requiring a D9 ADR revision.
- `A1-02 -> A1-05`
- `A1-03 -> A1-09`（先收口上传凭据与对象 checksum，再允许 publish job 触发首签）
- `DEP-03 -> A1-09`（broker 端点已上线，硬前置）
- `A1-09 -> A1-04`（首签前已删除长期 R2 凭据并取得真实上传证据）
- `A1-05 -> A1-04`（首签前安装器默认路径已完成验签收敛）
- `DEP-01 -> A1-04`（Backend publish 端点可用）
- `A1-04 -> A1-07`（A1-04 的 tag run 触发首签；A1-07 做后续端到端发布验证）
- `DEP-05 -> A1-07`（backend B1-09 首份 signed stable 已签发）

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | incoming | 跨计划 | `libra-backend` `cf` 交付线 B1-04（publish API） | libra-backend 计划发布者 | `POST /api/internal/release/publish` 经 OIDC 验证可用（staging 或生产，可对测试 tag 演练） | libra-backend origin/cf 分支 + 部署 URL + 核对日期 | 超时未交付则 A1-04/A1-07 置 `blocked`，其余卡不受影响继续推进 |
| DEP-02 | incoming | 审批（人工仪式） | `libra-backend` `cf` 交付线 B1-00（ceremony：公钥 32 字节 + `key_id` + `not_before`/`not_after`/`generation` 定值文档） | libra-backend 计划发布者（操作员 Eli 执行） | 公钥定值文档、与该公钥匹配的**已签名公共 key-pair smoke proof**（均 sanitized）已移交，且 backend 侧 Secret 配对验证通过。proof 固定使用独立 domain `libra-release-key-smoke-v1\0`（末尾一个 NUL byte），不得是 client-valid manifest envelope | 2026-08-31：Secret binding 已由 Wrangler 列出；部署版本 `d64e1887-a990-4236-8539-143c9f524a49`；公开 smoke URL 200（`Cache-Control: no-store`）；`tests/data/up01-production-key-smoke.json` 经 production trust root 在独立 domain 验通 | 已完成；后续 A1-02 C/D 仍受 D9 `0.22.0 -> 0.22.1` gate 约束 |
| DEP-04（outgoing 配对说明） | outgoing | 跨计划 | 本计划 A1-04/A1-02 交付给 backend `#B1-09` 消费（backend DEP-05 的对偶：release.yml publish job 已发布 + 0.22.1 客户端 + `artifacts` 桶四平台矩阵） | 本计划发布者 | A1-04/A1-02 发布完成（按 ER-UP01-4 查询的 GitHub Actions run 为绿 + R2 对象可 HEAD） | run URL/ID + 对象列表 | backend 未接手不影响本计划验收 |
| DEP-07 | incoming | 跨计划 | `libra-backend` `cf` 交付线 B1-02（跨实现契约向量：signed envelope 及 publish/renew/emergency 的 `accepted`/`rejected` transition vectors，测试公钥签名、payload digest 与预期结果登记） | libra-backend 计划发布者 | vectors 移交至 `tests/data/up01-*.json`；每个 accepted envelope 可被客户端验通，rejected vector 的原因与 backend B1-02 测试证据一致 | 已交付（2026-09-01）：backend `cf` `a165e1c`+`be09b13`（transition engine + 18 vectors 含 u64/2^53 边界 + 逐字节再生成守卫），移交 `tests/data/up01-transition-vectors-v1.json`（libra `8b37320`+`294ce0a`，客户端钉全文件 sha256 与信任根常量） | 已满足；A1-06 已解除 blocked |
| DEP-03 | incoming | 跨计划 | `libra-backend` `cf` 交付线 B1-05（credential broker 端点） | libra-backend 计划发布者 | broker 端点生产可用：测试 tag 可获得短期限定凭据，上传对象的 SHA-256 checksum 可被 Backend `HEAD` 读取并与请求/`.sha256` 三方核对 | backend 部署 URL + staging 上传演练记录 | A1-09 置 `blocked` 待端点上线；A1-04/A1-07 随其安全前置一并 `blocked`。不得恢复长期 R2 凭据，失败只允许修复 broker 后以前滚新 tag 重试 |
| DEP-05 | incoming | 跨计划 | `libra-backend` `cf` 交付线 B1-09（D10 首份 signed stable manifest 签发） | libra-backend 计划发布者 | `https://download.libra.tools/libra/releases/stable/manifest-v1.json` HEAD 200 且验签通过（首签经真实 tag run OIDC，其 tag run 依赖本计划 A1-04 已发布） | CDN HEAD 证据 + backend D 组记录 | 未完成则 A1-07 置 `blocked`；其余卡不受影响。注：A1-04 与 B1-09 的先后关系是**同一首签事件的两侧**（A1-04 的任一后续 tag run 即 B1-09 的首签触发器），由 backend 发布者驱动、本计划只做验收，不构成跨仓闭环 |

### 发布分组与并发窗口

默认每卡独立发布（G-07），没有活跃 `REL-*` 多卡窗口；已退役的 REL-02 仅保留在修订历史中。D9 的单卡 release gate 见 ADR-UP01-02 与「实施顺序」。

**并发声明:** A0-01 / A1-01 / A1-03 / A1-06 的实现写集互不相交，可并发；A1-02 与 A1-05 写集同含 `install.sh`/`install.ps1`，已有 `A1-02 -> A1-05` 顺序边，**不得并发**；A1-03 / A1-09 / A1-04 写集同含 `release.yml`，按 `A1-03 -> A1-09 -> A1-04` 串行。即使实现可并行，全部 C 组仍受 ER-12 单一发布者约束。

**发布者:** 单一 Agent（Eli 指派的当值 Agent），按 ER-12 串行执行 C 组与 `libra tag` / `libra push origin`；A1-02 开始 C 组前复核版本仍为 `0.22.0`，并确认无其它已 bump 未发布工作流。

**发布窗口顺序:** A0-01（no-release）-> **A1-02（0.22.1，D9）** -> A1-01 -> A1-06 -> A1-03 -> A1-09 -> A1-05 -> A1-04（首签）-> A1-07（首签后的端到端验证）。任一在 A1-02 前的卡可以做本地实现/验收，但不得执行 C 组版本 bump 或 tag 发布。
**执行期实际消费（2026-09-01）：** `0.22.1` = A1-02（按序）；`0.22.2` = A1-03 的 rclone env 前滚修复（ER-10 forward-fix，插队于 A1-01/A1-06 之前——修复破损 release run 优先于常规窗口顺序，A1-03 的 D 组随之闭合）；合并顺延后自 `0.22.6` 起回归窗口顺序：A1-01 -> A1-06 -> A1-09/A1-05 -> A1-04 -> A1-07（全部排在 Worker 部署门之后）。

## Phase 0: 基线冻结与文档债收口

**目标:** 冻结事实基线、补 plan-long 的 UP-01 外链，确认设计稿锚点仍有效。

**进入条件:** 设计稿入库（已满足，commit `3b5d9631`）。

**退出条件:** A0-01 验收通过。

## Phase 1: 客户端增量与 CI 卫生

**目标:** 在不消费 `0.22.1` 槽位前完成本地准备；ceremony 就绪后首先发布 A1-02（D9），再交付单调 floor、workflow 卫生与客户端契约向量测试。

**进入条件:** Phase 0 完成。

**退出条件:** A1-02 / A1-01 / A1-03 完成；A1-06 完成或按 DEP-07 的失败策略登记为 `blocked`；A1-09 的 DEP-03 前置已核对，未就绪则 Phase 2 停在 `blocked`。

## Phase 2: 签名链接入与安装器验签

**目标:** 先收口上传 broker 与对象 checksum，再完成安装器验签、`request-stable-manifest` job 和首签后的端到端发布验证。

**进入条件:** Phase 1 完成 + DEP-01 / DEP-02 / DEP-03 交付。

**退出条件:** A1-09 / A1-05 / A1-04 / A1-07 完成；D10 首份 signed stable 由 Backend 侧计划收口（DEP-05）。

## 任务卡

### Task A0-01: UP-01 基线冻结与 plan-long 同步

**Task type:** audit

**Lifecycle / Acceptance:** done / complete

**Description:** 核对设计稿现有修订后的锚点与当前代码一致（release.yml、upgrade 模块、libra-backend cf），在 `plan-long.md` UP-01 条目下登记本计划、`libra-backend` `cf` 交付线及 A.1–A.12 文档债处置（外链设计稿为事实源）。当前不存在可引用的 backend `plan-20260822.md`；不得继续把本仓 LR-02/LR-03 计划误标为 UP-01 backend 姊妹计划。

**Out of scope:** 任何代码改动（如需改动即按 ER-03 重分类）；A.1–A.12 全文回填（「永久非目标，理由：设计稿已含全部有效内容，回填为重复事实源」）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 设计稿入库于 `3b5d9631` | `libra log` |
| plan-long UP-01 仅剩摘要 | `plan-long.md` |

**Acceptance criteria:**

- [x] plan-long UP-01 条目含本计划与 backend `cf` 交付线，状态改为「实施中」
- [x] 文档债登记一行：A.1–A.12 以设计稿为事实源外链
- [x] 本卡的写集仅含本计划与 `plan-long.md`，无生产代码或配置改动

**Verification:**

- [x] `rg -n "plan-20260821.md.*UP-01|libra-backend.*cf" docs/development/plan/plan-long.md`
- [x] `rg -n "A\\.1.*A\\.12|release-signing-auto-upgrade\\.md" docs/development/plan/plan-long.md`
- [x] `libra diff -- docs/development/plan/plan-long.md`（本卡写集核对）

**Dependencies:** 无

**Deliverables:** `docs/development/plan/plan-20260821.md`（真实 backend 交付线与 DEP 引用修正）；`docs/development/plan/plan-long.md`（UP-01 条目更新）

**Implementation write set:** `docs/development/plan/plan-20260821.md`; `docs/development/plan/plan-long.md`

**Release write set:** N/A

**Files likely touched:** `docs/development/plan/plan-20260821.md`; `docs/development/plan/plan-long.md`

**Rollback mode:** revert

**Estimated scope:** S

**Version increment:** N/A

**Release boundary:** no-release（产物随 A1-01 的发布进入用户渠道）

**C/D coverage from:** A1-01

**Granularity:** `type=audit; axis=UP-01 计划索引同步; recovery=revert plan-long 单文件改动; complete=yes; self-contained=yes; AC=3/20; VER=3/20; landing=0; prod-files=0; scope=S; deps=none; writeset=no-overlap; release=no-release; split-from=N/A; exception=N/A`

### Task A1-01: upgrade state 持久化单调 generation floor

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（实现与 A/B 本地验证完成、C-T1 十轮 PASS；C/D 组 2026-09-01 闭合：`v0.22.6` 为被消耗槽位（Windows 编译红，预存在 `windows_by_handle`，前滚见修订历史），实际发布 = `v0.22.7` run `33512672400`（四平台构建上传全绿）+ `v0.22.8` 全绿收口）

**Description:** 在 upgrade state（`src/internal/upgrade/state.rs`）持久化「已见过的最高 `min_key_generation`」单调不降 floor；验签与执行路径取 `max(manifest.min_key_generation, 编译期 floor, 持久化 floor)`。这是设计稿 §7 第 4 条 (d) 的客户端配套（密钥泄漏 runbook 的生效前提），唯一行为轴 = floor 的持久化与单调合并。

**Out of scope:** trust table 填值（A1-02 承接）；`MIN_TRUSTED_KEY_GENERATION` 编译期常量变更（rotation 仪式时另立卡）；manifest 契约改动（永久非目标，ADR-UP01-01）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| state 已持久化 `generation_floor`，旧 state 缺字段默认 0 | `src/internal/upgrade/state.rs`；state round-trip/legacy tests |
| 验签把 persisted floor 加入 key 选择；双签优先接受满足 floor 的 key | `src/internal/upgrade/flow.rs`；`upgrade_auto_test` |
| state 读写经验证过的 install-dir fd-relative no-follow 原子操作，`0600` 在 rename 前设定；symlink/corrupt state fail-closed | `src/internal/upgrade/state.rs`；state storage tests |
| Phase-B install commit 与 crash recovery 在主升级锁下序列化，长 Phase-A 后先比对 baseline 且安装前二次复核 floors；已接受的单调 floors 另经专用 `.libra-upgrade-floors.json` 侧文件（自带微锁：非阻塞快路径 + 内核排队阻塞锁的有界等待，永不无锁写）持久化，`read_state` 合并侧文件——CAS 失配、锁竞争、进程退出在正常并发下都不丢弃已验证的 floor（唯一登记例外：微锁被外部停滞的持有者占满有界等待时该次不持久化、显式报错并由下次检查再推导） | `src/internal/upgrade/orchestrator.rs`、`src/internal/upgrade/txn.rs`、`src/internal/upgrade/state.rs`（`record_acceptance_floors`）；orchestrator/state unit tests |
| 泄漏 runbook 依赖该前提 | 设计稿 §7 第 4 条 (d) |

**Acceptance criteria:**

- [x] state 持久化 `generation_floor`，只升不降（旧 state 缺字段时迁移默认 0）
- [x] 升级判定使用三源 max；manifest 低于持久化 floor 时拒绝并给出稳定错误信息
- [x] 崩溃/半写后 state 读取 fail-closed 或安全重建，无 panic；state 文件拒绝 symlink 且原子写无 chmod-after-rename 窗口
- [x] `docs/auto-upgrade.md` 同步 floor 语义（EN 既有文件）

**Verification:**

- [x] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --lib upgrade -- --test-threads=1`（119 passed；含 floor、contended-merge、并发写者序列化、revision-0 fork、fd-relative state storage、skip/backoff CAS、C-T1 修复的 acceptance-floor 合并/失败保留用例）
- [x] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test upgrade_auto_test -- --test-threads=1`（11 passed，含 effective-floor 稳定错误回归；ER-UP01-3）
- [x] `LIBRA_SKIP_WEB_BUILD=1 cargo +nightly fmt --all --check`
- [x] `LIBRA_SKIP_WEB_BUILD=1 cargo clippy --all-targets --all-features -- -D warnings`

**Dependencies:** A0-01

**Deliverables:** N/A

**Implementation write set:** `src/internal/upgrade/state.rs`、`src/internal/upgrade/flow.rs`、`src/internal/upgrade/orchestrator.rs`、`src/internal/upgrade/txn.rs`、`docs/auto-upgrade.md`、`src/internal/upgrade/` 内 `#[cfg(test)]`

**Release write set:** Inherited

**Files likely touched:** 上述写集 + 版本面五处 + `Cargo.lock`

**Docs and compatibility impact:** `docs/auto-upgrade.md`；无 CLI/错误码新表面（若新增用户可见错误信息则同步 `docs/error-codes.md`）

**Rollback mode:** forward-only（已写入用户 state 的 floor 字段不回收；revert 代码后旧客户端忽略未知字段，自洽）

**Migration and rollback:** state schema 增字段向前兼容；恢复验证 `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test upgrade_auto_test -- --test-threads=1`

**Security and privacy:** 继承 GC-07；floor 单调不降即 anti-rollback 的一部分

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=generation floor 持久化与单调合并; recovery=forward-only（floor 数据保留，代码 revert 后旧读方忽略字段）; complete=yes; self-contained=yes; AC=4/8; VER=4/8; landing=2; prod-files=4; scope=S; deps=A0-01; writeset=no-overlap（A1-06 只消费 Backend contract vectors，不再写 src/internal/upgrade/）; release=independent; split-from=N/A; exception=N/A`

### Task A1-02: 信任锚定值--PRODUCTION_TRUSTED_KEYS 与两安装器公钥预置（D9 完整语义，绑 0.22.1）

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（2026-09-01：C 组已以 `fc9c203` = `v0.22.1` 执行，版本面五处 0.22.0 -> 0.22.1；首次 tag run `33417381720` 失败于 A1-03 范围的 rclone env 冲突、未发布资产，D 组按 ER-10 前滚口径锚定 `v0.22.2` 绿 run `33453752700` + 四平台/`.sha256`/无后缀副本 CDN HEAD 200——trust root 内容在两个 tag 间逐字节相同）

**Description:** 将 ceremony（DEP-02，backend B1-00 产出）公钥（32 字节 raw + `key_id` + 窗口 + `generation`）同时落三处：`src/internal/upgrade/trusted_keys.rs` 的 `PRODUCTION_TRUSTED_KEYS`、`install.sh` 预置公钥、`install.ps1` 预置公钥（D9 冻结文本「客户端 0.22.1 首次携带非空 `PRODUCTION_TRUSTED_KEYS`（**及 install.sh 预置公钥**）」的完整括号语义--信任锚定值是同一行为轴，拆开即造成 0.22.1 的安装器不含公钥的窗口）。附单元/契约测试与 ceremony 记录的 sanitized 文档。唯一行为轴 = 生产信任锚从空到非空（客户端 + 两个安装器同一定值）。

**Out of scope:** 安装器的 manifest 验签/下载流程改造（A1-05 承接，仅消费本卡定值）；standby key（「尚未排期，重启条件：设计稿 §7 第 5 条补冻」）；Backend 侧 Secret 写入（backend B1-00 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| trust table 与两安装器 public trust root 已配置并随 `v0.22.1`（`fc9c203`）发布 | `src/internal/upgrade/trusted_keys.rs`；`install.sh`；`install.ps1`；C/D 组证据行 |
| ceremony 公开半边已登记，且由单元测试逐字节钉住四处落点 | 设计稿变更记录 #20；`trusted_keys` 单元测试 |
| D9 冻结 0.22.1（含 install.sh 预置公钥） | 设计稿 §1 D9、§7.1 |
| install 脚本已预置同一公钥（验签流程仍由 A1-05 承接） | `install.sh` / `install.ps1` |
| DEP-02 public smoke proof 已生成并验签 | 2026-08-31 `https://download.libra.tools/libra/releases/smoke/ed25519-key-pair-v1.json` 返回 200、`Cache-Control: no-store`；首次 cron 写入后固化为 `tests/data/up01-production-key-smoke.json`，`trusted_keys` 单元测试用 production key 验通独立 domain 且拒绝 manifest domain 重放 |
| Worker Secret 与部署门的实测状态 | `wrangler secret list` 已列出 `LIBRA_RELEASE_ED25519_SEED`；用户升级容量后 `pnpm deploy:cf` 成功，生产版本 `d64e1887-a990-4236-8539-143c9f524a49`，cron `*/15 * * * *` 已启用 |
| C/D 组证据（2026-09-01） | C：`fc9c203` = `v0.22.1`（五处版本面 0.22.0 -> 0.22.1）。D（ER-UP01-4 显式 `gh api` 查询）：`v0.22.1` run `33417381720` conclusion=failure（rclone env 冲突，A1-03 范围，未发布资产）；前滚 `v0.22.2`（`96ddb7c`，trust root 逐字节不变）run `33453752700` conclusion=success；`libra-linux-amd64` / `libra-windows-amd64` / `libra-windows-amd64.sha256` 于 `download.libra.tools/libra/releases/v0.22.2/` HEAD 200（2026-09-01T02:09Z） |

**Acceptance criteria:**

- [x] `PRODUCTION_TRUSTED_KEYS` 含 ceremony 公钥，字段与 `TrustedKey` 结构一致
- [x] `install.sh` 与 `install.ps1` 内嵌同一公钥（与 `PRODUCTION_TRUSTED_KEYS` 定值逐字节一致，测试或守卫断言）
- [x] 单元测试钉死 key 条目数、`key_id`、窗口常量；消费 DEP-02 移交的已签名公共 key-pair smoke proof，并用该公钥直接验证 `libra-release-key-smoke-v1\0 || payload`（末尾一个 NUL byte）的签名通过，且断言该 proof 不能通过 manifest domain（私钥不入库）
- [x] `docs/development/internal/release-signing-auto-upgrade.md` 变更记录登记一行（公钥定值；不含私钥或 Secret）
- [x] 开始 C 组时仓库版本**恰为** 0.22.0，本卡 `patch + 1` 后发布版本即 0.22.1（`fc9c203` 五处版本面 0.22.0 -> 0.22.1，tag `v0.22.1` 已推送；D9 gate 按 ADR-UP01-02 精确满足）

**Verification:**

- [x] `source .env.test && cargo test --lib trusted_keys`
- [x] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test upgrade_publish_contract_test -- --test-threads=1`（ER-UP01-3）
- [x] `bash -n install.sh`（公钥注入后语法门）
- [x] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --lib trusted_keys`（公钥定值与 ceremony 记录逐字节一致；无私钥材料；三处落点一致性一并断言）
- [x] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --lib trusted_keys`（已部署 public smoke fixture 的独立-domain 验签与 manifest-domain 重放拒绝）

**Dependencies:** DEP-02（ceremony 公钥定值与已签名公共 key-pair smoke proof，backend B1-00）

**Deliverables:** N/A

**Implementation write set:** `src/internal/upgrade/trusted_keys.rs`（含 `#[cfg(test)]`）、`install.sh`（仅公钥常量段）、`install.ps1`（仅公钥常量段）、`tests/data/up01-production-key-smoke.json`（DEP-02 公共 key-pair smoke proof fixture）、设计稿变更记录行

**Release write set:** Inherited

**Files likely touched:** 上述写集 + 版本面五处 + `Cargo.lock`

**Rollback mode:** forward-only（已发布客户端与安装器携带公钥不可撤回；回退仅在下一版本清空/换钥并按 rotation 流程）

**Migration and rollback:** 无 schema 迁移；恢复 = 后续版本修订 trust table 与安装器常量

**Security and privacy:** 公钥公开可入库；ceremony 证据按 ER-UP01-2 sanitized

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent（ADR-UP01-02 D9 release gate）

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=生产信任锚定值（客户端+两安装器同一定值）; recovery=forward-only（发布不可撤，靠后续版本与 rotation）; complete=yes; self-contained=yes; AC=4/8; VER=4/8; landing=2; prod-files=4; scope=M; deps=DEP-02; writeset=序列化于 A1-05（同含 install.sh/install.ps1，依赖边 A1-02 -> A1-05）; release=independent; split-from=N/A; exception=N/A`

### Task A1-03: release.yml workflow 卫生与产物完整性

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（2026-09-01：静态守卫全部通过；`v0.22.1` 首次真实 run `33417381720` 失败于本卡卫生工作自身的 `RCLONE_VERSION` env 与 rclone 选项解析冲突——前滚修复随 `v0.22.2`（`96ddb7c`，`LIBRA_RCLONE_*` 重命名 + 新 compat guard `tests/compat/release_rclone_env_guard.rs`，已注册 `Cargo.toml` `[[test]]`/`tests/INDEX.md`/`tests/compat/README.md`）发布；D 组：run `33453752700` 成功，Windows `.sha256` 与无后缀副本 `libra/releases/v0.22.2/libra-windows-amd64` CDN HEAD 200，满足本卡 C/D coverage 判据）

**Description:** 落实设计稿 §8.2 第 7–9 项（签名链前提）：第三方 action 钉完整 commit SHA；移除 linux leg pipe-to-shell（改用 `upload-install-scripts` 已有的 rclone zip + `sha256sum -c` 模式，`brew`/`choco` 同理替换为钉死版本校验安装）；Windows leg 补 `.sha256` 产出与 artifact；额外上传无后缀副本 `libra-windows-amd64`；四平台 digest per-leg artifact 改为强制（去 `continue-on-error`）。唯一行为轴 = release.yml 产物的可校验完整性与 workflow 供应链卫生。

**Out of scope:** OIDC publish job（A1-04 承接）；upload jobs 的 broker/environment 与对象 checksum 接入（A1-09 承接）；bucket lock（Cloudflare 控制台动作，上线检查清单项）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 全部第三方 action 已钉 40 位 commit SHA；upload jobs 显式最小 `contents: read` 且不含 `id-token` | `.github/workflows/release.yml` |
| Linux、macOS、Windows 均下载同一 rclone 版本的匹配 archive 并先验固定 SHA-256；无 brew/choco/installer-script 路径 | `.github/workflows/release.yml` |
| Windows 产出 `.exe`、逐字节相同的 `libra-windows-amd64` 与 `libra-windows-amd64.sha256`，并上传 fourth per-leg digest artifact | `.github/workflows/release.yml` |
| Unix 三 leg 的 digest artifact 上传已去掉 `continue-on-error` | `.github/workflows/release.yml` |

**Acceptance criteria:**

- [x] workflow 内无 `curl … | bash/sh` 管道；第三方 action 全部为完整 40 位 SHA 引用
- [x] Linux、macOS、Windows 均不用未版本锁定的 `brew install rclone` / `choco install rclone` / installer script；每个平台下载同一明确版本的 rclone archive，并在执行前按该平台 pinned SHA-256 校验
- [x] Windows leg 产出并上传 `libra-windows-amd64.sha256` artifact；Unix 三 leg artifact 上传 `if-no-files-found: error` 且无 `continue-on-error`
- [x] Windows 上传无后缀副本 `libra/releases/{tag}/libra-windows-amd64`（与 `.exe` 逐字节相同）
- [x] 各 job `permissions` 最小化（upload jobs 不新增 `id-token`）

**Verification:**

- [x] 零命中守卫（模板多行范式）：

  ```bash
  if rg -n "curl [^|]*\| *(sudo )?(ba)?sh" .github/workflows/release.yml; then
    echo "FAIL: forbidden pattern found"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- [x] 零命中守卫（不允许绕开 pinned archive 路径）：`if rg -n '(^|[[:space:];|])(brew|choco)[[:space:]]+install[[:space:]]+rclone|rclone\.org/install\.sh' .github/workflows/release.yml; then echo 'FAIL: unpinned rclone installer'; exit 1; else rc=$?; [ "$rc" -eq 1 ] || exit "$rc"; fi`
- [x] 逐平台 allowlist 断言：release.yml 对 linux/darwin/windows 各有一个 rclone archive URL、一个 64 位 SHA-256 和对应校验步骤（Linux `sha256sum -c`、macOS `shasum -a 256 -c`、Windows `Get-FileHash` 严格比较）；每个 runner 只允许使用该已验证 binary

- [x] 零命中守卫（模板多行范式）：

  ```bash
  uses_lines="$(rg -n "^\s*uses:" .github/workflows/release.yml)" || { rc=$?; [ "$rc" -ne 1 ] && exit "$rc"; uses_lines=""; }
  if printf '%s\n' "$uses_lines" | rg -v "@[0-9a-f]{40}\b"; then
    echo "FAIL: forbidden pattern found"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- [x] 静态断言（D 组前置）：`rg -n "libra-windows-amd64" .github/workflows/release.yml` 命中无后缀副本上传 step 与 `.sha256` 产出 step（锚点判定：命中数 ≥ 2）
- [x] `awk '/^jobs:/{in_jobs=1; next} in_jobs && /^[^[:space:]]/{exit} in_jobs && /^  [A-Za-z0-9_-]+:/{print FNR ":" $0}' .github/workflows/release.yml`（受影响 job 清单核对）
- [x] YAML 语法门：`python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"`

**Dependencies:** A0-01

**Deliverables:** N/A

**Implementation write set:** `.github/workflows/release.yml`

**Release write set:** Inherited

**Files likely touched:** `.github/workflows/release.yml` + 版本面五处 + `Cargo.lock`

**Docs and compatibility impact:** Inherited（无用户命令变化；设计稿 §8.2 现状描述随实施在变更记录登记）

**Rollback mode:** revert（workflow 文件单点 revert；已按新 workflow 发出的产物不受影响）

**Migration and rollback:** N/A

**Security and privacy:** 本卡即供应链加固本体（设计稿签名链前提）

**Performance budget:** N/A

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self（D 组：本卡按 ER-UP01-4 通过 `libra tag "v<version>"` + `libra push origin "v<version>"` 触发的真实 run 中，Windows `.sha256` artifact 与无后缀副本 `libra/releases/{tag}/libra-windows-amd64` 在 `artifacts` 桶可 HEAD--workflow `release.yml`、job `build-and-upload`/`upload-install-scripts`、触发 `push` `v*` tag；判据 = ER-UP01-4 的显式 `gh api` 查询为绿 + R2/CDN HEAD 200）

**Granularity:** `type=implementation; axis=release.yml 产物完整性与供应链卫生; recovery=revert workflow 单文件; complete=yes; self-contained=yes; AC=5/8; VER=7/8; landing=1; prod-files=1; scope=S; deps=A0-01; writeset=序列化于 A1-09（同文件，依赖边 A1-03 -> A1-09 -> A1-04）; release=independent; split-from=N/A; exception=N/A`

### Task A1-04: release.yml 接入 OIDC publish（request-stable-manifest job）

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（2026-09-01：`request-stable-manifest` job 已实现——`needs: build-and-upload`、`environment: release-publish` + `id-token: write`、`concurrency: libra-release-manifest / cancel-in-progress: false`、四平台 digest artifact 强制下载（新增 `.size` 随 `.sha256` 入 artifact 以提供 publish body 的 size 字段）、`aud=libra-release-publish` OIDC、按 §5.2 第 4 步组装 body、非 2xx 整次红；后随 `v0.22.7` 补 5xx 有界重试（每次换新 OIDC token）。**C/D 组闭合 + D10 首签落地**：`v0.22.7` run `33512672400` 的 `request-stable-manifest` success——`stable/manifest-v1.json` HTTP 200、签名者 `libra-release-1`、payload canonical（version 0.22.7、control_revision 1、90 天有效期），openssl 生产公钥独立验签通过；C-T3 五轮 PASS）

**Description:** 新增 `request-stable-manifest` job（`needs: build-and-upload`；`permissions: id-token: write`；`environment: release-publish`；`concurrency: libra-release-manifest, cancel-in-progress: false`）：汇总四平台 digest artifact -> 请求 `aud=libra-release-publish` 的 OIDC JWT -> `POST /api/internal/release/publish`（body 按 §5.2 第 4 步：`version`/`tag`/`artifacts[]`/`run_id`/`run_attempt`/`workflow_sha`/`commit_sha`）；非 2xx 整次 release 红。本卡**只做 publish job**；upload jobs 的 environment 划分与 broker 接入由 A1-09 承接（Claude R1 P1-3 拆分：避免凭据链半吊子中间态）。唯一行为轴 = release 流程接入 Backend 签名链（publish 调用）。

**Out of scope:** upload jobs 的 broker/短期凭据接入与 `environment: release-artifacts`/`release-installers` 划分（A1-09 承接）；broker 端点实现（backend 计划 B1-05）；长期 R2 凭据删除（A1-09 收口，GC-UP01-1）；presigned PUT 客户端改造（backend 形态定案后随 A1-09 评估）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 现无 publish job | `.github/workflows/release.yml` |
| publish 请求体契约 | 设计稿 §5.2 第 4 步 |
| environment 派生 scope | 设计稿 §5.2 第 2 步 |

**Acceptance criteria:**

- [ ] `request-stable-manifest` job 存在且 `needs` 四平台构建；非 2xx 使 release 失败（未签则客户端不升级）
- [ ] 请求体字段与 token claims 绑定（`run_id`/`run_attempt`/`workflow_sha`/`commit_sha`），URL/host 从 env 取，无私钥/长期凭据
- [ ] 本 job `environment: release-publish` 与 `aud=libra-release-publish` 组合正确（§5.3 第 5 步联合钉死表）
- [ ] `concurrency` 串行化组配置正确且 `cancel-in-progress: false`
- [ ] 四平台 digest artifact 下载步骤无 `continue-on-error`，缺任一即 job 失败（预检 P2-6）
- [ ] 首次允许该 job 触发 production publish 前，A1-09 已删除 `secrets.R2_*` 并取得 broker 上传 D 组证据，A1-05 已完成其代码/测试与 C 组（其依赖首签 manifest 的 D 组正常路径冒烟可在本卡后补齐）；否则 job 保持未发布，不得用持全桶长期凭据的 tag run 触发 D10

**Verification:**

- [ ] `awk` job 清单核对（同 A1-03 命令，确认新 job 与 environment/permissions/concurrency 字段）
- [ ] `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`
- [ ] 零命中守卫（模板多行范式）：

  ```bash
  if rg -n "LIBRA_RELEASE.*SIGNING|ED25519.*SECRET" .github/workflows/release.yml; then
    echo "FAIL: forbidden pattern found"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- [ ] 手工证据（sanitized）：DEP-01 未交付期间，以「workflow lint + 合成请求体对照设计稿 §5.2 逐字段核对清单」替代；DEP-01 交付后补真实联调（staging 或生产对测试 tag 触发一次；以 ER-UP01-4 的 `libra tag` / `libra push origin` 与显式 `gh api` run 证据，加 publish 调用日志 2xx，归 D 组）

**Dependencies:** A1-09（上传凭据已收口）；A1-05（代码/测试与 C 组已完成；其 signed-manifest D 组正常路径由本卡首签后补齐）；DEP-01（publish API）

**Deliverables:** N/A

**Implementation write set:** `.github/workflows/release.yml`

**Release write set:** Inherited

**Files likely touched:** `.github/workflows/release.yml` + 版本面五处 + `Cargo.lock`

**Docs and compatibility impact:** 设计稿 §8.2 现状段随实施更新变更记录；README 发布段落如提及 release 流程则同步

**Rollback mode:** revert（revert workflow 后回到无签名发布；已签发 manifest 由 backend 侧管理）

**Migration and rollback:** N/A；A1-09 已在本卡前置完成长期凭据收口，禁止把该过渡重新带回发布链

**Security and privacy:** 私钥零接触（D2）；OIDC token 只在本 job 内换取并即时使用，不入 artifact/log（ER-11）

**Performance budget:** 新增一次 API 调用与 JWKS 验证（Backend 侧），非本仓热路径

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self（D 组：`release.yml` 由 ER-UP01-4 的 tag push 触发，publish job 绿）

**Granularity:** `type=implementation; axis=release 流程接入 Backend 签名链（publish 调用）; recovery=revert workflow 单文件（已签 manifest 归 backend 管）; complete=yes; self-contained=yes; AC=6/8; VER=4/8; landing=1; prod-files=1; scope=S; deps=A1-09,A1-05,DEP-01; writeset=序列化于 A1-09（同文件，依赖边 A1-09 -> A1-04）; release=independent; split-from=N/A; exception=N/A`

### Task A1-05: install.sh / install.ps1 fail-closed 验签流程

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（2026-09-01：实现随 `2ea10cc` 落地，八场景 sh 冒烟全绿、`install_smoke_test` 进 CI、EN/zh 文档同步；C-T4 第 1/2/3/4 轮 18 项 P1 修复后 sh 冒烟扩至 **24 场景**、ps1 扩至 **22 场景**——新增 expired/paused/revoked/stale-replay/tampered-payload/zero-size/min_key_generation/key-window/noncanonical/bad-calendar/huge-min-key/trailing-artifact/pretty-envelope/undersized/multiline-payload/huge-semver 策略分支;补齐 §7 密钥代际与有效窗校验、覆盖整个 payload 的 canonical 结构文法(数字位宽上界 + 尾锚,杜绝嵌套/尾随字段欺骗与 shell 整型溢出)、闰年感知日历校验、1 MiB manifest 上限、(0,128 MiB] 工件体积界、按签名 size 有界的流式下载(sh curl/wget 均管道 head -c 硬截断 + curl --max-filesize 提前拒绝;ps1 HttpClient 流式 + Content-Length 预检,验证字节不落盘)、空白归一化的信封解析(pretty-printed 信封可验),trusted_keys 单测同步断言两安装器的代际与窗口常量——**pwsh 缺口已关闭**：本机以便携 pwsh 7.4.6（linux-arm64 tarball）首跑 `run.ps1` 全绿，顺带修复 harness 四个潜在缺陷（DocRoot 未建、`$home` 只读、ConciseView 折行断言、hashtable `+` 重键）；本卡 C/D 组随 `v0.22.7`/`v0.22.8` 闭合：安装脚本经 broker 上传至桶根、CDN 生效，生产 install.sh 对 LIVE 首签 manifest 完整验签并装出 `libra 0.22.7`（正常路径 D 组冒烟，C-T4 五轮 PASS））

**Description:** 两个安装器的默认路径改为：拉取 `stable/manifest-v1.json` -> Ed25519 验签（domain prefix + envelope，公钥为 A1-02 已预置的定值）-> 按 manifest 的 URL/sha256/size 下载校验；验签或校验失败 fail-closed。**过渡态定义（Claude C-R2 P1-8）：manifest 404 = 签名链未启用，与验签失败严格区分--打印「自动升级签名链尚未启用（stable manifest 不存在）」提示并回退到与 `LIBRA_ALLOW_FALLBACK=1` 相同的显式确认未验签路径（要求用户显式确认或设置该变量），不得静默跳过校验。**PowerShell 迁移到同一契约（release API 流程 + 验签 + 404 过渡态对齐）；`-v` 历史版本与自定义 mirror 收敛为显式 opt-in 未验签路径；POSIX 的 `LIBRA_ALLOW_FALLBACK=1` 语义保留并文档化（ADR-UP01-03）。唯一行为轴 = 安装器 bootstrap 的验签下载流程。发布时序约束：本卡发布到 B1-09 首签之间的窗口内，官方安装路径进入上述 404 过渡态（见风险登记「A1-05 发布至首签窗口」行）。

**Out of scope:** 公钥预置（A1-02 已完成，本卡只消费）；bootstrap 带外信任锚（GitHub repo 公布脚本哈希，登记为上线检查清单项与设计稿 §10.1 第 3 条待补冻项）；上传对象 checksum（A1-09 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| install.sh 仅 sha256 可跳过 | `install.sh` |
| install.ps1 无校验 | `install.ps1` |
| 验签契约 | 设计稿 §7.3 第 6 步 |

**Acceptance criteria:**

- [x] manifest 验签失败 / sha256 或 size 不符 -> 非零退出且不落盘半成品；POSIX 显式回退仅 `LIBRA_ALLOW_FALLBACK=1`（冒烟场景 2/3/4）
- [x] manifest 404（签名链未启用）-> 走显式确认的过渡路径（提示 + 用户确认/环境变量），与验签失败的非零退出严格区分（冒烟场景 5/6）
- [x] 验签器不可用（openssl Ed25519 端到端能力探测失败）-> 显式确认过渡路径（ADR-UP01-06 第三态，不静默跳过；冒烟场景 7/8。PowerShell 侧 verifier 随脚本分发故无此态）
- [x] `-v` 指定版本与自定义 mirror 走显式 opt-in 未验签路径并有醒目告警
- [x] production installer **没有** `LIBRA_INSTALL_PUBLIC_KEY_B64` 或任何运行时公钥覆盖入口；测试只在临时复制的脚本上替换明确 key marker（harness 全局断言 + 原文件逐字节比对）
- [x] `README.md` / `README.zh-CN.md` 与 `docs/installation.md` / `docs/installation.zh-CN.md` 同步信任假设与回退语义；`docs/auto-upgrade.md` 同步默认 stable 路径
- [x] 冒烟门进入 CI：`tests/install_smoke_test.rs`（cargo `#[test]`，`std::process::Command` 调 `tests/data/install-smoke/run.sh`，注册 `tests/INDEX.md`；tests/ 根下自动发现，无需 `[[test]]`）

**Verification:**

- [x] `bash -n install.sh`
- [x] 可复现自动化冒烟（`tests/data/install-smoke/`：24 场景——正常 / 篡改签名 / 篡改 payload / sha256 不符 / size 不符 / expired / paused / revoked / stale-replay 反重放地板 / zero-size / min_key_generation 超代际 / 超位宽数字 / 密钥有效窗越界 / 非 canonical 序列化 / 尾随伪工件行 / 非法日历日期 / pretty 信封可验 / 超量响应截断 / 多行 payload 走私 / 超位宽 SemVer 分量 / manifest 404 ± fallback / 验签器不可用 ± fallback；每场景断言退出码、是否落盘与关键输出；harness 复制安装器、只改明确 marker、断言原文件逐字节未改且零 runtime key-override 入口）: `bash tests/data/install-smoke/run.sh`（2026-09-01 全绿 `all 24 scenarios passed`）
- [x] `source .env.test && cargo test --test install_smoke_test`（冒烟门的 CI 载体；2026-09-01 2 passed——sh 与 ps1 场景均实际执行）
- [x] PowerShell 对应场景：`pwsh -File tests/data/install-smoke/run.ps1`（2026-09-01 便携 pwsh 7.4.6 linux-arm64 全绿 `all 22 scenarios passed`，含纯 PowerShell Ed25519 验签器的真实签名验证；缺口关闭）
- [x] `source .env.test && cargo test --test compat_version_surface_sync`（版本面仍五处一致；2026-09-01 2 passed）
- [x] `rg -n "manifest-v1" install.sh install.ps1`（锚点定位用，非判据）

**Dependencies:** A1-02（公钥定值已预置）

**Deliverables:** N/A

**Implementation write set:** `install.sh`、`install.ps1`、`tests/data/install-smoke/`（fixture 与 run.sh/run.ps1）、`tests/install_smoke_test.rs`、`tests/INDEX.md`、`README.md`、`README.zh-CN.md`、`docs/installation.md`、`docs/installation.zh-CN.md`、`docs/auto-upgrade.md`

**Release write set:** Inherited（`install.sh`/`install.ps1` 兼属版本面五处）

**Files likely touched:** 上述写集 + `Cargo.toml`/`web/package.json`/`worker/package.json` + `Cargo.lock`

**Docs and compatibility impact:** `README.md` / `README.zh-CN.md`、`docs/installation.md` / `docs/installation.zh-CN.md`、`docs/auto-upgrade.md`；COMPATIBILITY.md 若列安装行为则同步

**Rollback mode:** revert（脚本为静态分发文件，revert 后旧脚本继续可用；已下载验签安装不受影响）

**Migration and rollback:** N/A

**Security and privacy:** ADR-UP01-03；脚本整体替换威胁如实文档化（带外锚登记）

**Performance budget:** 新增一次 manifest HTTPS 拉取（≤ 64 KiB）与四平台之一下载，无累积成本

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self（D 组：脚本 CDN 分发可用；A1-04/DEP-05 后再以 signed stable manifest 对 CDN 脚本跑正常路径冒烟）

**Granularity:** `type=implementation; axis=安装器 bootstrap 验签流程; recovery=revert 脚本与 fixture 文件; complete=yes; self-contained=yes; AC=7/8; VER=6/8; landing=2; prod-files=2; scope=M; deps=A1-02; writeset=序列化于 A1-02（同含 install.sh/install.ps1，依赖边 A1-02 -> A1-05）; release=independent; split-from=N/A; exception=N/A`

### Task A1-06: Backend transition 契约向量的客户端消费测试

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（2026-09-01：DEP-07 已由 libra-backend `cf` `a165e1c`（B1-02）交付并移交 `tests/data/up01-transition-vectors-v1.json`，客户端消费测试随 `8b37320` 落地、`upgrade_publish_contract_test` 12 passed（C-T2 四轮 PASS 后含双侧 sibling 比对）；C/D 组随 `v0.22.7`/`v0.22.8` 发布闭合——首个真实签名 manifest 即由同一 transition 引擎产出且客户端验签通过）

**Description:** Backend 是 publish/renew/emergency state transition 的唯一实现。本卡不在客户端写同位 transition 函数；而是在 `tests/upgrade_publish_contract_test.rs` 消费 DEP-07 移交的 version-controlled contract vectors：Backend B1-02 的 tests 对每个 vector 断言 renew Skip（>60d）、完整窗口前置拒绝、publish version 回退拒绝、`new==current` 幂等、`paused`/`revoked_versions` 逐字节继承、`control_revision` 首份为 1 且后续递增；本卡只断言 accepted vector 的 signed envelope 可由 `verify_envelope_bytes` 用对应测试公钥验通、其 client-visible payload 与 vector 预期一致，rejected vector 没有可被接受的 envelope。唯一行为轴 = client 对 Backend transition 输出与跨实现签名的契约守卫。

**Out of scope:** Backend transition 状态机、OIDC/CAS/outbox 测试（backend 计划承接）；在 `src/internal/upgrade/` 添加 reference model 或 production transition 函数；`KeyWindowMismatch` 错误文案改写（永久非目标，Display 已钉死）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 现有契约测试范围 | `tests/upgrade_publish_contract_test.rs` |
| §10 测试预期清单 | 设计稿 §10 |
| Backend transition 的唯一实现与向量生产者 | libra-backend `cf` 交付线 B1-02（DEP-07） |
| 2026-08-31 sibling checkout 复核 | 未找到 `tests/data/up01-*.json`、`up01-envelope-fixture.json` 或 B1-02 transition vector；`DEP-07` 的移交 commit / evidence digest 尚未提供 |

**Acceptance criteria:**

- [x] DEP-07 的 vector schema 对每一条明确 operation、pre-state、expected result 与 Backend B1-02 证据 digest；Backend tests 覆盖 renew Skip（>60d）、窗口前置拒绝、publish version 回退、`new==current`、控制字段逐字节继承、`control_revision` 首份为 1 / 后续 +1（backend `tests/unit/release-manifest-transitions.test.ts` 11 用例 + `up01-contract-vectors.test.ts` 三重守卫：committed==regenerated 逐字节、accepted envelope 验签、digest 绑定）
- [x] client test 对每条 `accepted` vector 验证 signed envelope、`key_id`、payload digest 与 client-visible payload；对 `rejected` vector 断言它没有可被 client 接受的 envelope（`up01_accepted_vectors_verify_and_match_result_payload` / `up01_rejected_vectors_carry_no_acceptable_envelope`）
- [x] 占位符契约用例：`tag=v0.20.3` -> 对象键 `libra/releases/v0.20.3/libra-linux-amd64`（防 `vv`；`up01_anti_vv_placeholder_contract`）
- [x] 跨实现验签（二态判定）：DEP-07 真 vectors 已到位（backend `a165e1c` 移交，含 `vectors_digest_sha256` 绑定）-> `tests/data/up01-transition-vectors-v1.json` 经 `verify_envelope_bytes` 验通（测试公钥 + key_id 与 vector 内一致）
- [x] 新 target 若有则 `tests/INDEX.md` 同步：无新 target（沿用 `upgrade_publish_contract_test`），INDEX 无需变更

**Verification:**

- [x] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test upgrade_publish_contract_test -- --test-threads=1`（11 passed，2026-09-01；ER-UP01-3）
- [x] `rg -n "upgrade_publish_contract_test" tests/INDEX.md`（锚点定位用，非判据）

**Dependencies:** A0-01；DEP-07（跨实现 contract vectors 移交；未交付时本卡停 `blocked`，不得以客户端复制状态机替代）

**Deliverables:** N/A

**Implementation write set:** `tests/upgrade_publish_contract_test.rs`、`tests/data/up01-*.json`（DEP-07 vectors）、`tests/INDEX.md`（如新 target）

**Release write set:** Inherited

**Files likely touched:** 上述写集 + 版本面五处 + `Cargo.lock`

**Docs and compatibility impact:** Inherited（纯测试卡，无用户可见变化）

**Rollback mode:** revert

**Migration and rollback:** N/A

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=Backend transition 输出的客户端契约与跨实现验签; recovery=revert 测试/fixture 文件; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=0; scope=S; deps=A0-01,DEP-07; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

### Task A1-07: 首签后的端到端发布验证（D10 客户端半边）

**Task type:** release

**Lifecycle / Acceptance:** done / complete（2026-09-01：DEP-05 随 `v0.22.7` 首签交付。端到端验证：① `stable/manifest-v1.json` HTTP 200、Ed25519 签名以生产公钥 openssl 独立验签通过、payload canonical 且 artifacts 四平台齐备；② 生产 install.sh（未改动、信任 `libra-release-1`）在隔离 HOME 真实走签名通道——manifest 拉取 -> 验签 -> 按签名行下载 linux-arm64 -> 强制 sha256+size 比对 -> 安装,`libra --version` 输出 `libra 0.22.7`）

**Description:** A1-04 的真实 tag run 是 D10 首签触发器。本卡在 DEP-05 已证明那次首签后，执行**后续**真实 `v*` tag 发布验收：核对四平台对象 + `.sha256` + 无后缀 Windows 副本在 `artifacts` 桶、对象 checksum 三方核对、publish 2xx、signed stable manifest 可从 `download.libra.tools` 拉取并经本仓客户端（0.22.1+）验签升级成功。产出上线检查清单证据（数字 ID、bucket lock smoke 引用、purge 证据引用--动作本体在 backend 计划）。唯一行为轴 = 首签后的发布链端到端验证。

**Out of scope:** 首份 manifest 的签发动作本体（backend 计划 B1-09 承接，本卡是客户端/CI 半边的验证）；CDN 缓存规则配置（Cloudflare 控制台）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| D10 绑定实现上线时 | 设计稿 §1 D10、§7.1 |

**Acceptance criteria:**

- [ ] release run 全绿且 publish job 2xx（ER-UP01-4 的显式 `gh api` run 证据）
- [ ] `stable/manifest-v1.json` HEAD 200 且带短 `Cache-Control`（curl -I 证据；对象存在性依赖 DEP-05 = backend B1-09 已首签）
- [ ] Backend 记录 `head.checksums`、Action 声明 digest 与 `.sha256` 对象三方一致；缺失任一项的 tag run 被拒签
- [ ] 测试环境客户端验签 + 升级事务成功（marker/txn 断言，sanitized 日志）
- [ ] 上线检查清单条目（数字 ID、过渡凭据状态、bucket lock smoke 引用、purge 引用）登记在计划或设计稿变更记录

**Verification:**

- [ ] `libra tag "v<version>"` + `libra push origin "v<version>"`，再以 ER-UP01-4 的显式 `gh api` 查询取得成功 run URL/ID（D 组证据）
- [ ] `curl -fsSI https://download.libra.tools/libra/releases/stable/manifest-v1.json | head -20`
- [ ] 手工证据：客户端 `libra upgrade`（或等价命令）成功升级的 sanitized 输出

**Dependencies:** A1-04；A1-05；A1-09；DEP-01；DEP-05（backend B1-09 首签完成）

**Deliverables:** N/A

**Implementation write set:** 无生产文件（证据与文档登记；若需微调 workflow 修复则按 ER-10 走 FIX 卡）

**Release write set:** Inherited

**Files likely touched:** `docs/development/internal/release-signing-auto-upgrade.md`（变更记录一行）

**Docs and compatibility impact:** 设计稿变更记录；`docs/auto-upgrade.md` 如需声明启用状态则同步

**Rollback mode:** immutable-release（已发布 artifact 与已签 manifest 不可撤回；问题以前滚新版本处理）

**Migration and rollback:** 前滚：新 patch 版本 + 新 publish；emergency 由 backend Admin 面

**Security and privacy:** 证据 sanitized（ER-11）

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=release; axis=首签后的发布链端到端验证; recovery=immutable-release（前滚新版本）; complete=yes; self-contained=yes; AC=5/12; VER=3/12; landing=0; prod-files=1; scope=S; deps=A1-04,A1-05,A1-09,DEP-01,DEP-05; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

### 退役历史卡：A1-08a / A1-08b

这两张纯版本跃迁卡在 `0.20.3` 基线下曾用于到达 `0.22.0`。截至 2026-08-31，五处版本面和已完成的其它计划已经交付 `v0.22.0`；重跑会倒退或重复已发布版本，故它们不再是任务卡、不参与生命周期/完成判据/粒度审计或发布 DAG。历史 rationale 与此前 review 记录保留在 ADR-UP01-05 及修订历史；现行 D9 保护唯一以 ADR-UP01-02 为准。

### Task A1-09: upload jobs 接入 credential broker 短期凭据

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete（2026-09-01：workflow 侧实现完成——两 upload job 已声明 `release-artifacts`/`release-installers` environment + `id-token: write`，rclone 与 `secrets.R2_*` **全部移除**（compat guard `release_rclone_env_guard` 改为零 rclone/零长期凭据断言），上传改为 broker 逐对象 presigned PUT（`If-None-Match: *` 仅签入不可变 per-tag 资产；桶根安装脚本按设计可覆写不签该头）+ `x-amz-checksum-sha256` + `Content-Length`。DEP-03 代码已交付（backend `cf`），**D 组待**：① Worker 生产部署；② Cloudflare R2 S3 凭据入 Workers Secrets（`LIBRA_R2_S3_ACCESS_KEY_ID`/`_SECRET_ACCESS_KEY`）；③ GitHub environments `release-artifacts`/`release-installers`/`release-publish` 创建；④ GitHub 长期 `R2_*` secrets 删除——①-④ 已全部完成（①cf 分支自动部署、②用户提供 R2 S3 令牌并入 Workers Secrets、③environments 首 run 自动创建、④用户删除四个长期 `R2_*` secrets，GC-UP01-1 永不恢复）。**D 组闭合**：`v0.22.7` run `33512672400` 四平台 build-and-upload 全绿——短期 presigned PUT 全链路（credential broker 换发 -> 逐对象上传 -> If-None-Match 不可变约束），run 全程零长期凭据）

**Description:** 把 `build-and-upload` 与 `upload-install-scripts` 两个 job 从长期 `secrets.R2_*` 凭据切换为 broker 短期凭据（自 A1-04 拆出，Claude R1 P1-3）：各 job 加 `environment: release-artifacts` / `release-installers`（`id-token: write` 仅用于向 broker 换凭据）；上传前 `aud=libra-release-upload` 换 OIDC JWT -> `POST /api/internal/release/upload-credential` -> 用签发的 STS/presigned PUT 上传。每个发布 artifact（binary、无后缀 Windows 副本、`.sha256`、installer script）必须携带可由 Backend `HEAD` 读取的 SHA-256 checksum；STS 路径也不得例外，presigned PUT 另须将 `If-None-Match: *`、checksum、`Content-Length` 签入。删除 workflow 中全部 `secrets.R2_*` 引用并在上线清单记录 GitHub secrets 已删除。调用契约按设计稿 §5.2/§5.3 第 11 步冻结文本；本卡完成后才允许 A1-04 触发首签。唯一行为轴 = 上传凭据和对象完整性收口。

**Out of scope:** broker 端点实现（backend B1-05）；publish job（A1-04 后续承接）。无论 backend 选 STS 还是 presigned PUT，checksum 上传是本卡必做项，不可延后。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| upload jobs 持全桶长期凭据 | `.github/workflows/release.yml`（`secrets.R2_*`）；设计稿 §4.1 |
| broker 调用契约 | 设计稿 §5.2 broker 段 |
| 2026-08-31 sibling checkout 复核 | 只发现 release-signing smoke 实现；未发现 `/api/internal/release/upload-credential` 路由、broker handler 或 staging/production upload rehearsal 证据，因此 DEP-03 未满足 |

**Acceptance criteria:**

- [ ] 两 upload job 分别声明 `environment: release-artifacts` / `release-installers`，凭据经 broker 换取，workflow 无 `secrets.R2_*` 引用
- [ ] 上传走 STS（put-only、前缀/对象精确、分钟级 TTL、每对象 `x-amz-checksum-sha256`）或 presigned PUT（`If-None-Match: *` + checksum + Content-Length 签入），与 backend B1-05 实际选型一致
- [ ] 真实 tag run 经 broker 上传成功（四平台对象、`.sha256`、无后缀 Windows 副本、installer scripts 全部就位），Backend `HEAD` checksum、Action 声明 digest 与 `.sha256` 内容三方一致
- [ ] 上线检查清单登记「长期 R2 secrets 已从 GitHub 删除」收口项证据

**Verification:**

- [ ] 零命中守卫（模板多行范式）：

  ```bash
  if rg -n "secrets\.R2_" .github/workflows/release.yml; then
    echo "FAIL: forbidden pattern found"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- [ ] `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`
- [ ] 上传命令/请求 allowlist：每个二进制对象显式设置 SHA-256 checksum；若使用 presigned PUT，逐对象请求必须同时带 `If-None-Match: *`、`x-amz-checksum-sha256`、`Content-Length`，缺任一项使 job 失败
- [ ] `libra tag "v<version>"` + `libra push origin "v<version>"`，再以 ER-UP01-4 的显式 `gh api` 查询确认 run 内上传 job 绿（D 组，真实 broker 联调）

**Dependencies:** A1-03（workflow 供应链卫生完成，同文件串行）；DEP-03（broker 端点上线与 checksum HEAD 核对，硬前置）

**Deliverables:** N/A

**Implementation write set:** `.github/workflows/release.yml`

**Release write set:** Inherited

**Files likely touched:** `.github/workflows/release.yml` + 版本面五处 + `Cargo.lock`

**Docs and compatibility impact:** 设计稿 §8.2 凭据过渡和 §5.3 第 11 步 checksum 目标态随实施更新变更记录（长期凭据删除）

**Rollback mode:** forward-only（broker 或 checksum 校验故障使 release fail-closed；修复 broker/workflow 后以前滚新 tag 重试，绝不恢复长期 R2 凭据）

**Migration and rollback:** 凭据切换无数据迁移；恢复路径 = 修复 broker、轮换已失效短期凭据并以新 tag 重试，不存在恢复长期凭据配置的路径

**Security and privacy:** 上传凭据分钟级 TTL；OIDC token 即取即用（ER-11）；`id-token: write` 仅 upload jobs

**Performance budget:** 每上传 job 一次 broker 调用（每 tag ≤ 3 次），无热路径

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self（D 组：真实 release run 经 broker 上传成功，且 Backend 记录的 `head.checksums`、Action digest、`.sha256` 内容三方一致）

**Granularity:** `type=implementation; axis=上传凭据与对象 checksum 收口; recovery=forward-only（broker 故障 fail-closed，修复后新 tag）; complete=yes; self-contained=yes; AC=4/8; VER=4/8; landing=1; prod-files=1; scope=M; deps=A1-03,DEP-03; writeset=序列化于 A1-03/A1-04（同文件，依赖边 A1-03 -> A1-09 -> A1-04）; release=independent; split-from=A1-04; exception=N/A`

#### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A0-01 | audit | 计划索引同步 | revert plan-long | yes | yes | 3/20 | 3/20 | 0/0 | S | none | no-overlap | no-release | N/A | N/A |
| A1-01 | implementation | generation floor | forward-only | yes | yes | 4/8 | 4/8 | 2/4 | S | A0-01 | no-overlap | independent | N/A | N/A |
| A1-02 | implementation | 信任锚定值（客户端+两安装器） | forward-only | yes | yes | 4/8 | 4/8 | 2/4 | M | DEP-02 | 序列化于 A1-05 | independent | N/A | N/A |
| A1-03 | implementation | CI 产物完整/卫生 | revert | yes | yes | 5/8 | 7/8 | 1/1 | S | A0-01 | 序列化于 A1-09 | independent | N/A | N/A |
| A1-04 | implementation | OIDC publish 接入 | revert | yes | yes | 6/8 | 4/8 | 1/1 | S | A1-09,A1-05,DEP-01 | 序列化于 A1-09 | independent | N/A | N/A |
| A1-05 | implementation | 安装器验签流程 | revert | yes | yes | 7/8 | 6/8 | 2/2 | M | A1-02 | 序列化于 A1-02 | independent | N/A | N/A |
| A1-06 | implementation | Backend transition 输出契约+跨实现验签 | revert | yes | yes | 5/8 | 2/8 | 1/0 | S | A0-01,DEP-07 | no-overlap | independent | N/A | N/A |
| A1-07 | release | 首签后的端到端发布验证 | immutable-release | yes | yes | 5/12 | 3/12 | 0/1 | S | A1-04,A1-05,A1-09,DEP-01,DEP-05 | no-overlap | independent | N/A | N/A |
| A1-09 | implementation | 上传凭据+对象 checksum 收口 | forward-only | yes | yes | 4/8 | 4/8 | 1/1 | M | A1-03,DEP-03 | 序列化于 A1-03/A1-04 | independent | A1-04 | N/A |

## 测试矩阵

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | trust table 定值、state floor 单调 | `source .env.test && cargo test --lib upgrade` |
| 集成 | manifest 契约、Backend transition contract vectors、占位符契约、跨实现验签 | `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test upgrade_publish_contract_test -- --test-threads=1` |
| 集成 | upgrade 自动流程（floor 并入后） | `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test upgrade_auto_test -- --test-threads=1` |
| 集成 | install 冒烟七场景（CI 载体） | `source .env.test && cargo test --test install_smoke_test` |
| 兼容 | 版本面五处一致 | `source .env.test && cargo test --test compat_version_surface_sync` |
| 安全 | install 篡改 fail-closed、零 runtime 公钥覆盖、workflow 零 pipe-to-shell/钉 SHA/零长期凭据/对象 checksum 守卫 | 本卡 Verification 内命令 |
| 迁移 | state 增字段向后兼容 | `source .env.test && LIBRA_SKIP_WEB_BUILD=1 LIBRA_TEST=1 cargo test --features test-upgrade --test upgrade_auto_test -- --test-threads=1` |
| live/gated | 真实 tag 发布 + publish 2xx + broker 上传 + checksum 三方一致 + CDN 可获取 | ER-UP01-4 的 `libra tag` / `libra push origin` + 显式 `gh api`（D 组） |

注：`upgrade_auto_test` / `upgrade_publish_contract_test` 带 `required-features = ["test-upgrade"]`，`cargo test --all` 不覆盖（ER-UP01-3）。

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| A0-01 | 设计稿 §10 文档债 | `plan-long.md` | plan-long 索引 | N/A |
| A1-01 | 设计稿 §7 第 4 条 (d) | `src/internal/upgrade/state.rs` | `docs/auto-upgrade.md` | `upgrade_auto_test` |
| A1-02 | D9；设计稿 §7.2 | `src/internal/upgrade/trusted_keys.rs` + `install.sh`/`install.ps1` 公钥段 | 设计稿变更记录 | `upgrade_publish_contract_test` |
| A1-03 | 设计稿 §8.2 第 7–9 项 | `.github/workflows/release.yml` | 设计稿变更记录 | 本卡守卫命令 |
| A1-04 | 设计稿 §5.2 | `.github/workflows/release.yml` | 设计稿变更记录 | D 组 ER-UP01-4 run 证据 |
| A1-05 | 设计稿 §4、§10.1 第 3 条 | `install.sh` / `install.ps1` | README + installation EN/zh | `install_smoke_test` |
| A1-06 | 设计稿 §10；backend B1-02 contract vectors | `tests/upgrade_publish_contract_test.rs` + vectors | `tests/INDEX.md` | `upgrade_publish_contract_test` |
| A1-07 | D10、§7.1 | 证据/文档登记 | 设计稿变更记录 | D 组证据 |
| A1-09 | 设计稿 §4.1/§5.2 broker、§5.3 第 11 步、§8.2 第 8–9 项 | `.github/workflows/release.yml` | 设计稿变更记录（凭据/checksum 收口） | D 组 ER-UP01-4 run 证据 |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | A0-01 完成，基线冻结 | commit/test/doc | N/A |
| M1 | Phase 1 全部卡 done；A1-02 已作为 0.22.0 后的第一张发布卡交付 0.22.1 | version/review + D9 evidence | 各卡 revert / forward-only |
| M2 | Phase 2 全部卡 complete（含 D 组；A1-09 已先收口凭据/checksum，D10 首签由 DEP-05 闭环） | ER-UP01-4 tag/run 证据 + CDN + checksum 三方证据 | immutable-release / forward-only，前滚 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| publish 非 2xx（artifact 已上 R2） | 无 signed manifest，客户端不升级（正确语义） | 修复后重跑或新 tag 重发 | 发布未签名 envelope |
| install 验签失败 | 用户安装中止 | 公告 + 修复后重发脚本 | 回退默认未签名下载 |
| state floor 半写 | 字段缺失按默认 0 读 | 下一 tick 重写 | panic / floor 降级 |
| broker 或 checksum 故障（A1-09 后） | 上传 job / publish 失败，未签 stable 不更新 | 修复 broker/workflow 后以新 tag 重试 | 恢复长期 R2 凭据、旁路凭据或缺 checksum 签名 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| DEP-01/DEP-02/DEP-03 延期 | A1-02 的 D9 发版或首签链阻塞 | 允许不发版的本地准备；任何非 A1-02 的 C 组不得抢占 0.22.1，A1-04/A1-07 等待 A1-09 与后端前置 | A1-02/A1-04/A1-09 |
| Worker 部署体积限额 | 在容量不足时会阻断 smoke handler 部署；2026-08-31 首次 `10027` 已由用户升级容量解除并成功部署 | 保留失败记录；后续部署若再触发限额，禁止复制私钥到替代 Worker、跳过 smoke proof 或宣称 Secret 已配对 | A1-02/DEP-02 |
| DEP-07 未移交 | A1-06 无法验证 Backend transition 输出及跨实现验签 | A1-06 保持 `blocked`；只接受 Backend B1-02 版本控制的 vectors、测试公钥、证据 digest 与移交 commit；不得在客户端自造替代物 | A1-06 |
| checksum 上传或三方核对失败 | 签名前完整性闸门拒绝，stable 不更新 | A1-09/A1-07 D 组以 `HEAD` checksum、Action digest、`.sha256` 逐一取证；修复后新 tag 重试 | A1-09/A1-07 |
| broker 未上线或故障 | Action 无法上传，但不会再有全桶长期凭据 | A1-09 是 A1-04 硬前置；release fail-closed，修复后新 tag 重试 | A1-09 |
| install 脚本被整体替换 | bootstrap 失效 | 带外锚登记（ADR-UP01-03） | A1-05 |
| A1-05 发布至 B1-09 首签窗口：官方安装路径进入 manifest 404 过渡态 | 新用户安装需显式确认未验签路径（不静默降级） | 404 过渡态独立定义（与验签失败区分）+ 404 TTL cache rule 前置动作登记上线检查清单；窗口期公告 | A1-05/B1-09 |
| 验签器不可用（旧 OpenSSL / verifier 缺失） | 安装路径第三态 | ADR-UP01-06：显式确认过渡路径，冒烟第七场景钉死 | A1-05 |
| D9 0.22.1 槽位被其它发布先占用 | 首个非空 trust table 不再是冻结版本 | A1-02 开始 C 组前断言版本恰为 0.22.0；不满足即停工重裁 D9，不得静默换版本 | A1-02 / 发布者 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| install 拉取 manifest | 一次 HTTPS GET ≤ 64 KiB | 无累积 | CDN TTL 60s | curl -I |
| 客户端升级检查（既有） | 不变 | 不变 | 15min/1h 冷却 | `upgrade_auto_test` |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md`：N/A（无 Git 兼容面变化；install 行为变化在 README 说明）
- [ ] `docs/commands/<cmd>.md`：N/A（无命令新增/变更）
- [ ] `docs/commands/zh-CN/<cmd>.md`：N/A
- [ ] `docs/development/commands/<cmd>.md`：N/A
- [ ] `docs/error-codes.md`：A1-01 若新增错误码则同步
- [ ] `tests/INDEX.md`：A1-05（`install_smoke_test`）、A1-06（如新 target）同步
- [ ] `Cargo.toml` `[[test]]`：A1-05 若 `install_smoke_test` 需显式注册则同步
- [ ] `README.md` / `README.zh-CN.md`、`docs/installation.md` / `docs/installation.zh-CN.md`、`docs/auto-upgrade.md`：A1-05 同步安装器信任与回退语义
- [ ] `plan-long.md`：A0-01 同步

## 评审记录

Result 只允许 `PASS` 或 `FAIL`。`FAIL` 必须列出 P0/P1 条目并在下一轮复审关闭；P2 可由具名责任人书面接受为 residual risk，但不改变本轮 `FAIL` 记录（ER-05）。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R1 | 两份计划全文（gpt-5.3-codex-spark, read-only） | FAIL | P1-1 跨仓 DEP-03 双向闭环（A1-04 ↔ B1-05 互为前置） | P2-1 A1-05 手工冒烟 -> 已修复（自动化 install-smoke 门）；P2-2 backend 清单可执行化 -> backend 计划 R1 记录处置 | 修订历史 2026-08-22 行；ADR-UP01-04 |
| C-R1（claude-code, read-only） | 两份计划全文 | FAIL -> 已修复 | P0-1 版本 0.22.1 不可达 -> 已修复（A1-08 + ADR-UP01-05 + EX-01）；P1-1 test-upgrade 命令 -> 已修复（ER-UP01-3 全量替换）；P1-3 A1-04 双轴 -> 已拆出 A1-09；P1-4 首签跨仓依赖 -> 已修复（DEP-05 双侧登记）；P1-7 ceremony 双重登记 -> 已修复（DEP-02 钉死 backend#B1-00）；P1-8 发布顺序矛盾 -> 已修复；P1-2/5/6 backend 侧 -> 已修复（见 backend 计划修订历史） | P2-1/2/3/4/6/7/8/9/10 已修复；P2-5 计数失真已重算 | 本修订历史 2026-08-22 Claude R1 行；/tmp/claude-r1-result.txt |
| C-R2（claude-code, read-only） | 两份计划全文 | FAIL -> 已修复 | P0-1 姊妹计划未修订 -> 已修复（backend 本轮全部落地）；P1-7 A1-08 双切片/EX-01 越权 -> 拆卡 A1-08a/A1-08b + 删 EX-01；P1-8 A1-05 首签前 404 窗口 -> 过渡态定义 + 风险登记 | P2-1/2/3/4 已修复（公钥覆盖、fixture 依赖表述、守卫 allowlist、release note 说明）；P2-6 语义已写明 | /tmp/claude-r2-result.txt |
| C-R3（claude-code, read-only） | 两份计划全文 | FAIL -> 已修复 | P1-5 A1-06 跨仓前置未登记 -> DEP-07 + 二态 AC；P1-6 0.22.1 槽位无排他保护 -> REL-02 排他窗口 + CT-01 让位例外 + 收口断言 | P2-1/2/3/5/8/10 已修复 | /tmp/claude-r3-result.txt |
| C-R4（claude-code, read-only） | 两份计划全文 | FAIL -> 已修复 | P1-4 REL-02 无边界/回滚/登记 -> 完整表登记+DEP-02 前置+逃生路径；backend P1-1/2/3/5 同批修复 | P2-1/2/4/5/6/7/8/9 已修复 | /tmp/claude-r4-result.txt |
| C-R5（claude-code, read-only） | 本计划及其本地源码锚点 | FAIL -> 已修复 | P1-1：D9 基线把 run 成功作为证据，但与 sibling plan 的 non-GitHub 环境折算口径不一致 -> 改为 annotated-tag 基线，run 仅按 ER-UP01-4 作未来 D 组证据 | P2-1/2：`test-upgrade` 的 Cargo/base.yml 行号过期；P2-3：A1-07 `release` 卡误用 `/8` 分母，均已修复 | Claude Code 2026-08-31 read-only C-R5 输出；本修订历史 C-R5 行 |
| C-R6（claude-code, read-only） | 本计划及其本地源码锚点（含 C-R5 修复） | PASS | 无 | 无 | Claude Code 2026-08-31 read-only C-R6 输出 |
| C-R7（claude-code, read-only） | A0-01、`plan-long.md` UP-01 同步、设计稿 smoke-proof contract | PASS | 无（前序 A0 复审的 false completion、NUL domain、状态摘要与 VER 计数均已修复） | 无 | Claude Code 2026-08-31 read-only A0 final review，`/tmp/claude-a0-review.txt` |
| C-R8（claude-code, read-only） | A1-01 state/flow/orchestrator/txn 与用户文档 | FAIL -> 已修复 | P0-1：install commit 与 failure-backoff 可覆盖并发推进的 anti-rollback state；P1-1：state path I/O 绕过 `InstallDir` fd-relative no-follow 且有 rename 后 chmod 窗口 | P2-1：revision 0 的 control fork guard；均已修复并加回归覆盖 | Claude Code 2026-08-31 read-only C-R8 输出 |
| C-R9（claude-code, read-only） | A1-01 C-R8 修复及相关 `lock.rs` 依赖 | PASS | 无 | 无 | Claude Code 2026-08-31 read-only C-R9 输出 |
| C-R10（claude-code, read-only） | A1-03 `release.yml` 与设计/计划记录 | PASS | 无 | P2-1 action major 一致性、P2-2 Windows download timeout、P2-3 source-hash copy verification、P2-4 rclone constants 去重、P2-5 Windows guard coverage；均已在 C-R11 前修复 | Claude Code 2026-08-31 read-only C-R10 输出 |
| C-R11（claude-code, read-only） | A1-03 C-R10 P2 修复及文档 | PASS | 无 | 无 | Claude Code 2026-08-31 read-only C-R11 输出 |
| C-R12（claude-code, read-only） | A1-02 trust root、公开 key-pair smoke proof、Worker scheduler 与计划记录 | PASS | 无 | 无 | Claude Code 2026-08-31 read-only C-R12 输出；独立 domain 防 manifest 重放、无 HTTP signing oracle、R2 create-once、Secret/公钥边界与 fixture 一致性均已核对 |
| C-T2（codex gpt-5.6-luna, read-only, 4 轮） | B1-02 transition engine + 18 contract vectors + A1-06 客户端消费（两仓） | FAIL x3 -> **PASS**（第 4 轮） | P1 均修复：TS Number vs Rust u64 语义分歧（改 BigInt + 边界向量）、移交件未认证（客户端钉全文件 sha256 + 信任根常量）、resume 不变式覆盖不足（双侧全字段断言）、跨仓单侧再生成漂移（双向 sibling-checkout 字节比对测试 + 双 pin） | 无 | codex session 01a05b51；提交 be09b13/38fa3fc/d35fdea（backend）、294ce0a/f057e6e/f3526ca（libra） |
| C-T1（codex gpt-5.6-luna, read-only, 10 轮） | A1-01 落地提交 + A0-01/簿记 + v0.22.1/v0.22.2 C/D 记录 | FAIL x9 -> **PASS**（第 10 轮） | 逐轮 P1 均修复：failed-install/rollback/recovery 丢 floor、有效 floor 错误码、下载失败/超时丢 acceptance、txn 前窗口、Prepared 清理、revision-0 digest、CAS 失配/锁竞争丢 floor、无界阻塞、进程退出窗口、无锁 lost-update、NB 探测饿死——终态：floors 侧文件 + 微锁 + 内核排队有界等待，永不无锁写 | 第 10 轮 P2（措辞限定外部停滞例外）已修 | codex session 01a05ad0；提交 244f177/07884bf/43b02ff/4058604/c830a49/5b05b45/4d53293/1abf290/07b3ae7 |
| C-T4（codex gpt-5.6-luna, read-only, 5 轮） | A1-05 两安装器签名通道 + install-smoke（对照设计 §4/§7/§9 与卡内 AC） | FAIL x4 -> **PASS**（第 5 轮，零残留 P0/P1/P2） | 逐轮 18 项 P1 全修复：无状态反重放地板、canonical X.Y.Z/撤销条目/URL 精确绑定、UTC-Z 强制 + 闰年日历校验、set -e 下 wget 404 三态、策略分支冒烟缺失、ps1 临时文件 TOCTOU（独占句柄 + 内存哈希 + 原子落位）、§7 密钥代际/有效窗、零体积工件、sed 嵌套字段欺骗 -> 全 payload 结构文法（数字位宽上界 + 尾锚）、pretty 信封拒收 -> 空白归一化信封解析、无界下载 -> 双分支 head -c 硬截断 + ps1 HttpClient 流式、行导向 grep 多行走私 -> 单行可打印 ASCII 门、semver 分量溢出 -> 九位上界 | 第 2/3/4 轮 P2 同批修复：ps1 大小写敏感比较、1 MiB manifest 上限、INDEX.md 行、trusted_keys 交叉断言代际/窗口常量、冒烟补 8 个新分支 | codex session 01a05ba5；提交 f1af267/d6639a3/94ab8c1/f0892f1；冒烟终态 24（sh）/22（ps1）全绿，pwsh 缺口以便携 pwsh 7.4.6 关闭 |
| C-T3（codex gpt-5.6-luna, read-only, 5 轮） | B1-04/B1-05 broker+publish+renew 端点与 A1-09/A1-04 workflow（对照设计 §5.1–§5.4/§6.1/§8.2） | FAIL x4 -> **PASS**（第 5 轮） | 逐轮 P1 均修复：presign 头对未排序、per-leg environment 缺失、installer Cache-Control 未签名、replay 未绑定请求体、audit 晚于 applied 标记、孤儿 attempt 无回收、renew 异常无耐久审计、同 run 重试被 15 分钟租约卡死（终态 reconcileOwnPending 无租约同 run 修复 + aged reconciler 处理外来孤儿）；第 4 轮 P2 同批修复：幂等分支补 applied-audit 行核验（instr digest）、artifacts 形状在任何 R2 访问前强制恰好 4 平台、DDL 移入 jti 消费闭包（签名+claim 全过后才触碰 D1）、attempt 行存 {sub, gh actor, jti} 归因 JSON | 第 5 轮 PASS 附带两条备注已随手关闭（39f6303）：reconcileOwnPending 尊重 markAttempt 条件结果并复查行状态；sub/actor 提前到 jti 消费前校验 | codex session 01a05b6d；提交 e1c7742/b789125/1dcd6d3/39f6303（backend）、113aa78（libra workflow） |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-02 | standby key 预置 | 设计稿仅「建议补冻」 | 设计稿 §7 第 5 条补冻 | 本计划修订 |
| DEFER-03 | bootstrap 带外信任锚（脚本哈希公布） | 超出仓库代码范围 | 设计稿 §10.1 第 3 条补冻 | 上线检查清单 + 文档 |
| DEFER-04 | SLSA provenance/attestation | 深层供应链加固 | workflow 卫生落地后 | 设计稿 §10.1 第 10 条 |
| DEFER-05 | `gc_ten_thousand_objects_within_budget` 在本机确定性超时（预存在，非 UP-01） | 收口全量门（2026-09-01）暴露：万级 blob 保存下，后台索引任务与前台 `persist_index_repair_marker` 对同一 generation flock 以 10ms NB 轮询竞争，前台 2s 饿死（`src/utils/client_storage.rs` INDEX_REPAIR_LOCK_WAIT_TIMEOUT）。基线证据：测试与代码在合并基线 `cfd916e` 即存在，UP-01 全部提交写集与 `client_storage.rs`/`save_object` 零交集；空载隔离复跑仍失败（确定性，非负载抖动）；同签名 LBR-INTERNAL-001 在本会话早期无关 `libra commit` 时亦出现过。属并发 tiered-storage 工作流辖区（协作协议：不越权收编） | 存储侧工作流修复 flock 公平性/批处理持锁窗口 | 并发工作流（mega）/独立 fix 卡 |
| DEFER-06 | `sh tests/SERIAL_CONVERT.sh` 报 `mega_fastcdc_http_interop` 不在冻结 manifest（预存在漂移，非 UP-01） | 并发工作流合入 `tests/media_fastcdc_test.rs` 未同步冻结 manifest；实际 CI 门 `compat_serial_registry` 18/18 全绿，转换器为手工维护工具 | 存储侧工作流补 manifest 行 | 并发工作流（mega） |

## 完成判据

- [x] 所有任务卡满足 `G-*`；粒度审计表已核对，无 L/XL、无碎片卡、写集冲突已消解（A1-08a/A1-08b 已因实际 `0.22.0` 基线退役，ADR-UP01-05 仅保留历史说明）。
- [x] 所有非延后任务 `Lifecycle=done` 且 `Acceptance=complete`；无 `blocked` 卡残留（A1-07 随 DEP-05 首签解除）；DEFER-02/03/04 为原登记延后项，DEFER-05/06 为收口门暴露的并发工作流辖区预存在缺陷。
- [x] 所有 Verification 已运行并记录（2026-09-01；A1-05 的 pwsh 场景以便携 pwsh 7.4.6 补齐）。
- [x] 计划完成门（2026-09-01）：`cargo +nightly fmt --all --check` 零差异、`cargo clippy --all-targets --all-features -- -D warnings` 零告警、`source .env.test && cargo test --all`（-j8 --no-fail-fast）225 套件——唯一失败为 DEFER-05 归因的预存在存储锁饿死（写集零交集、基线 `cfd916e` 即存在、空载隔离复现）；`upgrade_auto_test` 11 passed、`upgrade_publish_contract_test` 12 passed、`install_smoke_test` 2 passed（24 sh + 22 ps1 场景实际执行）单独记录。本计划不改 `web/`、`worker/` 源码。
- [x] docs/compat/error-code/test-index 更新完成（按各卡；含 `tests/INDEX.md` install_smoke 行、`tests/compat/README.md` release_rclone_env_guard 行）。
- [x] migration/rollback 验证完成（A1-01 state 字段；其余 revert/forward-only/immutable-release 已记录）。
- [x] 评审最终 PASS，P0/P1 全部关闭（C-R6/C-R7/C-R9/C-R10/C-R11/C-R12 + codex gpt-5.6-luna C-T1 十轮/C-T2 四轮/C-T3 五轮/C-T4 五轮，见评审记录）。
- [x] 发布要求：A1-02 = `v0.22.1` D9 首发；后续 `v0.22.2`（A1-03 前滚）、`v0.22.6`（被消耗槽位）、`v0.22.7`（A1-01 主发布 + D10 首签 + A1-07 端到端）、`v0.22.8`（CDN 核验前滚收全绿）；版本面五处一致（compat_version_surface_sync 每次发布前通过）、tag run 证据 = run `33512672400`（v0.22.7，publish/构建绿）+ run `33519196299`（v0.22.8，8/8 job 全绿零重跑，线上 manifest 0.22.8 / control_revision 2 验签通过）；签名 manifest 与安装器 sha256/size 三方一致由 A1-07 端到端安装验证。
- [x] 修订历史已记录全部规范性变更。
- [x] `plan-long.md` UP-01 状态已同步（A0-01；收口终态由本判据段记录）。
