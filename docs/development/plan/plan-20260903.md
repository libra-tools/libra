# Merge 能力对齐 Git 计划（2026-09-03）

## 文档职责

本文解决 `libra merge` 与 `git merge` 在算法层与内容层的能力差距，目标是：`libra merge` 主路径能力（rename 检测、多 merge-base recursive 合并、D/F 冲突、增量树合并、merge driver、冲突精化、octopus、策略框架、mergetool、签名与消息面）与 Git 对齐；完成后与 Git 的剩余差距**仅为**（a）永久非目标的 subtree / submodule / gitlink（ADR-MG-01），以及（b）「非目标与延后项」中逐条登记的有意差异（DEFER-01 ~ DEFER-14）。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `libra merge` 命令全表面：`src/command/merge.rs`、`src/internal/merge_base.rs`、`src/command/rename_detect.rs`（复用）、`src/command/rerere.rs`、`src/internal/pseudo_ref.rs`、新增 `mergetool` 命令。
- 受影响的状态面：`<worktree gitdir>/merge-state.json`、pseudo-ref 投影（`MERGE_HEAD`/`ORIG_HEAD`/`AUTO_MERGE`）、冲突 index stage 1/2/3。
- 复用方同步：`cherry-pick` / `revert`（已复用 merge 内容合并）、`rebase`（收敛到统一三路引擎）。
- 测试：`tests/command/` 各模块（统一编译进 `command_test` 单 binary，见事实基线「测试」行）、`tests/compat/*`；文档：`docs/commands/merge*.md` EN+zh、`docs/development/commands/merge.md`、`COMPATIBILITY.md`、`docs/error-codes.md`、`tests/INDEX.md`。

### 非目标

- **subtree 合并（`-s subtree` / `-X subtree`）、submodule/gitlink 合并：永久非目标**（产品定位 monorepo 客户端，见 ADR-MG-01）。gitlink 条目由「静默跳过」改为「裁决触及即 fail-closed」，仍不实现合并。
- `merge-tree --write-tree`（独立命令，不是 `git merge` 表面）——DEFER-01。
- `-X diff-algorithm=<patience|histogram|minimal>`（diffy 内核单一算法）——DEFER-02。
- mergetool 全量 25+ 外部工具描述文件——DEFER-03。
- 合并签名 tag 的 `mergetag` header 嵌入与 throwaway tag 强制 no-ff——DEFER-04。
- `merge.rs` 模块拆分（预计超 5000 行后的 `src/internal/merge/` 归位）——DEFER-05。
- 目录 rename 推断与 rename/rename 叠加的其余角落案例（MG-07 AC4 采样组合之外）——DEFER-07。
- cherry-pick 的 rename 接入；外部 `git-merge-<name>` 策略命令发现——DEFER-06。
- `merge.<name>.recursive` 低层 driver 的递归调用语义——DEFER-08。
- `-X find-renames[=<score>]` / `rename-threshold` flag 别名——DEFER-09。
- 显式 `-s octopus` 策略名——DEFER-10。
- `mergetool.keepBackup` 之外的 `mergetool.*` 配置面——DEFER-11。
- 外部 GPG keyring / SSH 签名（merge 签名仅 vault，与 commit 现状一致）——DEFER-12。
- merge 默认打开编辑器（保持 Libra「不自动开编辑器」现状；Git 默认 `--edit`）——DEFER-13。
- `merge.suppressDest` 等 fmt_merge_msg 衍生配置——DEFER-14。
- `merge-file` 的 `-L`/`--union`/`--marker-size` 补齐（独立命令表面，不在本计划）。

### 成功定义

- 日常重构场景（一侧 rename + 对侧修改）自动合并成功，不再退化为 modify/delete + add 两条独立记录；criss-cross 历史经 recursive 虚拟祖先合并，结果与 Git 一致。
- D/F 冲突、rename/rename、rename/delete、rename/add 冲突被显式检测并按 Git 语义呈现（stage 1/2/3 + 人读提示）。
- merge driver（gitattributes `merge` 属性、内建 text/binary/union、外部 driver）、`-X ignore-space-*`、`merge.conflictStyle=zdiff3`、冲突精化与 Git 行为一致。
- octopus 多分支合并、`-s ort/recursive/resolve`、多 `-s` 回退选择、`mergetool`、`-S/--gpg-sign`、`--signoff`、`--quit`、`--edit` 可用。
- 大仓库下 merge base 计算与树合并不再全量扫描（复杂度界写入「性能与容量摘要」并有 benchmark 证据）。
- `COMPATIBILITY.md` merge 行更新为「主路径与 Git 对齐；剩余差异 = 永久非目标（subtree/submodule/gitlink）+ DEFER-01~14 登记项」——**不**宣称「仅三项例外」，DEFER 项在兼容矩阵中逐条可见。
- 全量收口门全绿；Codex review 最终 PASS。

## 事实基线

> 所有行号和源码锚点必须在开工当天刷新。过期锚点只能作为历史线索。核对日期：2026-09-03。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 代码入口 | `MergeArgs`（单 `Option<String>` 分支参数） | `src/command/merge.rs:99`（struct 声明）、`:101`（`branch` 字段） |
| 代码入口 | `MergeStrategy` 枚举仅 `Ours`；`MergeFavor` 仅 `ours/theirs` | `src/command/merge.rs:82`、`src/command/merge.rs:90` |
| 代码入口 | merge base 取单个 LCA（BFS 交集 + 剔除严格祖先） | `src/command/merge.rs:2829`（`merge_base_commits`，MG-02 前为 `lca_commit` 只取首个）、`src/internal/merge_base.rs:323`（`merge_bases`） |
| 代码入口 | tree 拍平为 `HashMap<PathBuf, MergeTreeEntry>`，**静默过滤 gitlink** | `src/command/merge.rs:2666`（`commit_tree_items`） |
| 代码入口 | rebase 同样静默丢弃 gitlink（平行实现的拍平） | `src/command/rebase.rs:3842` 附近（2026-09-03 核对，开工时刷新精确行） |
| 代码入口 | 路径级三路决策 | `src/command/merge.rs:3045`（`merge_tree_items`）、`src/command/merge.rs:2791`（`resolve_three_way`） |
| 代码入口 | 冲突类型仅 BothChanged / OursModifiedTheirsDeleted / TheirsModifiedOursDeleted | `src/command/merge.rs:2752`（`ConflictKind`） |
| 代码入口 | 内容级合并用 `diffy`（`MergeOptions`，diff3 内核） | `src/command/merge.rs:2883`（`try_merge_blob_contents`）、`Cargo.toml:123`（`diffy = "0.4.2"`） |
| 代码入口 | 行级冲突渲染（Git 风格 marker、长度防撞、diff3 风格） | `src/command/merge.rs:3256`（`render_line_level_conflict`）、`src/command/merge.rs:2117`（`conflict_style_from_config`） |
| 代码入口 | 空白归一化引擎（diff 的 `-w`/`-b`/`--ignore-space-at-eol` 实现） | `src/command/diff.rs:5930-5960`（`normalize_ignore_all_space` 等） |
| 数据/状态 | `MergeState` JSON sidecar（atomic write + fsync + owner_scope） | `src/command/merge.rs:395` |
| 数据/状态 | pseudo-ref 单一事实源：`pub enum PseudoRef` 仅六变体、`pub const ALL: [PseudoRef; 6]`，无 `AUTO_MERGE`；compat 契约要求全部 DECLARED ref 在 rev-parse 不可解析，且用 `_HEAD` 后缀启发式提取名称集合（`AUTO_MERGE` 不匹配该启发式） | `src/internal/pseudo_ref.rs:46`、`:70`；`tests/compat/pseudo_ref_surface.rs:38`（声明对齐断言）、`:113`（`_HEAD` 启发式）、`:135`（全量拒绝断言） |
| 代码入口 | 对象指定符解析 seam：`util::resolve_object_spec_typed`（定义于 `src/utils/util.rs:2124`）被 rev-parse / cat-file / update-ref 等直接调用——pseudo-ref 解析接线必须为**调用方 opt-in**，否则 update-ref 的 `<oldvalue>` CAS 会意外接受 tree OID | `src/utils/util.rs:2124`、`src/command/rev_parse.rs:639`（另 `:431`、`:802`）、`src/command/cat_file.rs:890`、`src/command/update_ref.rs:452`（`resolve_old_value` 起始，其 `resolve_object_spec_typed` 调用在 `:461`；`:389` 起的 `resolve_new_value` 反向有 commit 类型检查）；diff 目标解析 `src/command/mod.rs:555`（`get_target_commit`，只产 commit） |
| 数据/状态 | 冲突 index stage 1/2/3 写入 | `src/command/merge.rs:2156`（`write_conflicted_merge_state`） |
| 数据/状态 | gitlink 在 index 转换处报错但 tree 拍平处被过滤（防线不一致） | `src/command/merge.rs:3495` |
| 代码入口 | rename 检测独立模块（spanhash、Git 0..60000 标尺、exact/basename/inexact 配对、预算控制） | `src/command/rename_detect.rs:253`（`match_pairs`）；调用方仅 `src/command/diff.rs:4104`、`src/command/status.rs:2375`、`:2459`；merge.rs 零引用（2026-09-03 grep 实证） |
| 代码入口 | rebase 平行三路实现（不复用 merge） | `src/command/rebase.rs:4649`（`resolve_three_way` 定义）、`:4756`（`replay_commit_with_conflict_detection` 定义）、`:4823`（`resolve_three_way` 调用点） |
| 代码入口 | cherry-pick 复用 merge 内容合并；revert 无 favor 路径直接调 `diffy::merge_bytes` | `src/command/cherry_pick.rs:1624`、`src/command/revert.rs:960-963` |
| 数据/状态 | rerere：`conflict_id` = 全文件 SHA-256；`auto_update` 为 `flag \|\| config` 二态，无法表达显式关闭；cherry-pick 把 rerere_autoupdate 布尔持久化进 sequencer opts 并 round-trip | `src/command/rerere.rs:414-419`、`:126-158`；`src/command/cherry_pick.rs:286-348`；`COMPATIBILITY.md:174` |
| 用户命令 | `Commands::Merge` 挂载；`Commands` 枚举无 `Mergetool` 变体（2026-09-03 核对） | `src/cli.rs:613` |
| 用户命令 | commit-graph maintenance 已能写出含 generation 的 v1 文件（**只有 writer，无 reader**） | `COMPATIBILITY.md:196` |
| 错误码 | 稳定错误码权威 | `src/utils/error.rs`、`docs/error-codes.md` |
| 文档 | merge 行 `partial`（octopus/策略等缺口已登记；rename 缺口由 GAP-06 独立证据支撑，COMPATIBILITY.md:150 尚未登记 rename 字样） | `COMPATIBILITY.md:150`；merge-base `:151`；merge-file `:152`；rerere `:174` |
| 测试 | `tests/command/*.rs` 是 `command_test` **单 binary** 的模块（`tests/command_test.rs` 声明 `mod command`，`tests/command/mod.rs:382-404` 挂载各命令模块）——**不存在** `--test merge_test` 这类独立 target；正确调用形式为 `cargo test --test command_test command::merge_test::<filter>`。merge 集成测试 2284 行 + merge.rs 内联单测 | `tests/command_test.rs:1-8`、`tests/command/mod.rs:382-404`、`tests/command/merge_test.rs`、`src/command/merge.rs:3517-3987` |
| 外部参照 | git@`3cb9185f65410273787f74333cc027d2ea5daada`（2026-09-02 tip，2026-09-03 核对） | `/Volumes/Data/competition/git/git`：`builtin/merge.c`、`merge-ort.c`、`merge-ll.c`、`xdiff/xmerge.c`、`commit-reach.c`、`diffcore-rename.c`、`rerere.c`、`git-merge-octopus.sh`、`git-mergetool.sh` |
| 外部参照 | release.yml job 集合（2026-09-03 现场读取）：`update-homebrew-tap` / `verify-homebrew-formula` 失败降级 warning + `exit 0`（best-effort）；`request-stable-manifest` 为 `set -euo pipefail` 硬失败（可作判据）；workflow 仅由 `v*` tag push 触发 | `.github/workflows/release.yml`：`build-and-upload:15`、`upload-install-scripts:323`、`update-homebrew-tap:493`（降级逻辑 `:530-556`）、`verify-homebrew-formula:641`、`request-stable-manifest:690`（硬失败 `:732`）、触发 `on.push.tags: v*`（`:3`） |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | gitlink 被 merge/rebase 的 tree 拍平静默丢弃 | 含 gitlink 的外部仓库合并结果静默错误 | `merge.rs:2666`、`rebase.rs:3842` vs `merge.rs:3495` | MG-00 |
| GAP-02 | merge base 为 O(全历史) BFS 双遍历 + 交集集合运算，无染色结构与有界 frontier | 大历史下 merge 启动慢 | `merge_base.rs:72` | MG-01 |
| GAP-03 | 多 merge-base（criss-cross）只取单一 base，无 recursive 虚拟祖先 | 虚报冲突、与 Git 结果不一致 | `merge.rs:2829`（`merge_base_commits`，MG-02 前为 `lca_commit`）；git `merge-ort.c:5313` | MG-02 |
| GAP-04 | 树合并全量拍平，无目录剪枝/trivial 快速路径 | monorepo 规模性能瓶颈 | `merge.rs` `fn commit_tree_split`（符号为准；MG-03 收口时位于 `:3181`，原基线 `:2666`）；git `merge-ort.c:1259` | MG-03 |
| GAP-05 | 无 D/F（目录/文件）冲突处理 | 静默写出"半合"状态 | `src/command/merge.rs` `fn merge_tree_items`（符号为准；开工核对：计划成稿时的 `:3045` 已随 MG-02/MG-03 编辑漂移）；git `merge-ort.c:4100-4198` | MG-04 |
| GAP-06 | merge 不做 rename 检测（模块存在但零调用） | rename+修改退化为 modify/delete+add | `rename_detect.rs:253`；merge.rs 零引用 | MG-05 |
| GAP-07 | 无 rename 路径级冲突类型（rename/rename、rename/delete、rename/add） | 冲突语义缺失 | `merge.rs:2752`；git `merge-ort.c:2394` | MG-06 |
| GAP-08 | 无目录级 rename 推断 | 整目录改名后另一侧新增文件不落位 | git `merge-ort.c:2467-2912` | MG-07 |
| GAP-09 | 无 merge driver（gitattributes `merge` 属性、binary/union 内建 driver） | 二进制/union 场景只能整文件冲突 | `merge.rs:2883`；git `merge-ll.c:361-390` | MG-08 |
| GAP-10 | 无外部 merge driver（`merge.<name>.driver`） | 用户自定义合并不可接入 | git `merge-ll.c:191` | MG-09 |
| GAP-11 | 无冲突精化（zealous refine）、无 zdiff3、冲突标记行尾按平台而非按文件 | 冲突块虚大、跨平台假冲突 | `merge.rs:3256`；git `xdiff/xmerge.c:363` | MG-10 |
| GAP-12 | 无 `-X ignore-space-*` / renormalize | 行尾/空白差异制造假冲突 | git `merge-ort.c:5551-5608` | MG-11 |
| GAP-13 | 不支持 octopus 多分支合并 | 功能缺口 | `merge.rs:101`（单分支参数）；`COMPATIBILITY.md:150` | MG-12 |
| GAP-14 | 无策略框架：`-s` 仅 ours，无 ort/recursive/resolve 别名与多策略回退 | `-s recursive` 解析期即拒绝 | `merge.rs:82`；git `builtin/merge.c:101` | MG-13 |
| GAP-15 | 无 mergetool | 冲突人工解决无工具集成 | git `git-mergetool.sh` | MG-14 |
| GAP-16 | merge 提交永不签名、无 `--signoff` | 与 commit/cherry-pick/revert 的签名能力不一致 | `COMPATIBILITY.md:150`（`--no-gpg-sign` no-op） | MG-15、MG-16 |
| GAP-17 | 状态机缺 `--quit` | 无法只清理状态保留冲突现场 | git `builtin/merge.c:1417-1470` | MG-17 |
| GAP-18 | 无 `--edit`/`-F`/`--into-name`/`--cleanup`，`--no-edit` 为 no-op | 消息面缺口 | `COMPATIBILITY.md:150` | MG-18 |
| GAP-19 | rebase 三路合并为平行实现，不共享 rename/driver 能力 | 重复实现、行为漂移 | `rebase.rs:4649` | MG-19 |
| GAP-20 | rerere 整文件字节级匹配，无 per-hunk 规范化 | resolution 复用率低 | `rerere.rs:414-419`；git `rerere.c:340` | MG-20 |
| GAP-21 | 无 `AUTO_MERGE` pseudo-ref 投影；pseudo-ref 未接入对象解析 seam；compat 契约的 `_HEAD` 启发式与全量拒绝断言尚不兼容新 ref | 无法消费「自动合并结果树」 | `pseudo_ref.rs:46`、`:70`；`pseudo_ref_surface.rs:38/:113/:135`；`util.rs:2124`；git `merge-ort.c:4960` | MG-21 |
| GAP-22 | rerere autoupdate 为 `flag \|\| config` 二态：`--no-rerere-autoupdate` 是 no-op（无法显式关闭），正向 flag 缺失，且 override 不跨冲突/续作持久化 | 当次覆盖配置不可用、续作后退回配置值 | `rerere.rs:126-158`；`cherry_pick.rs:286-348`；`COMPATIBILITY.md:150` | MG-22 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | 待核对是否有 merge 相关长期能力编号 | 开工时核对；有则在任务卡 `Dependencies` 引用，无则登记 N/A |
| `docs/development/commands/merge.md` | merge 开发文档事实源 | 每张卡按 GC-05 同步 |
| `docs/development/commands/_compatibility.md` | 兼容决策源（octopus 等 deferred 记录） | 随对应卡更新 |
| git@`3cb9185f6`（本地副本） | 算法语义外部参照（DEP-01） | pin revision 引用，不跟踪 main |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | merge 是 VCS 核心命令，rename/criss-cross 缺失直接影响 monorepo 日常重构合并 | 无 |
| 可行性 | 23 张卡均为 S/M，单轴可交付；rename_detect 模块可直接复用 | 无 |
| 任务卡粒度 | 无 L/XL；无碎片卡；全部独立发布 | R1 后 MG-17 拆三卡；R5 后 16 张门族卡登记 EX-01（R7 增至 18 张：MG-03/MG-15 补登；R13 增至 20 张：MG-02/MG-19 补登；R16 增至 21 张：MG-01 补登；G-03 门族型验收豁免） |
| 依赖与顺序 | DAG 无环；merge.rs 单文件热点经串行链（写集串行边）消解 I–I 冲突 | R1 后串行关系全部落为显式依赖边 |
| 完整性 | 每卡含测试 + 文档 + 兼容同步；收口全量门已写入完成判据 | R2 后 Verification 命令修正为真实 target；R3 后 tag 推送顺序写入契约 |
| 安全性 | gitlink fail-closed（MG-00）；外部 driver / mergetool 子进程注入面（MG-09/MG-14）有专项约束 | R1 后 MG-09 sq_quote；R2 后 gitlink 两档语义；R5 后 MG-21 pseudo-ref 解析改调用方 opt-in（update-ref 不受影响） |
| 功能正确性 | criss-cross/rename 冲突语义以 git@pin 为参照逐条对齐 | R1~R3 三轮修正 MG-08 `merge.default` 至与 git 源码一致（未命中一律回退 text） |
| 接口兼容 | 新增 flag 均为 Git 既有表面；merge-state.json schema 演进要求旧 state 可读 | 无 |
| 数据流与控制流 | 状态写入沿用 atomic sidecar + 崩溃安全顺序（GC-08） | R3 后 MG-22 override 持久化到 sequencer 状态；R4 后补新进程续作验收 |
| 性能与容量 | MG-01/MG-03 有复杂度界与 benchmark 断言 | R1 后 MG-01 移除无 reader 的 generation 承诺 |
| 可靠性与容错 | 冲突状态恢复路径沿用现有 `--abort`/`--continue` 框架 | R4 后 MG-21 的 AUTO_MERGE 生命周期修正为「仅 merge 进行中可观察」 |
| 可维护性 | MG-19 收敛 rebase 平行实现，消除双写 | 无 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-03 | 成稿 | 初版 | — | — |
| 2026-09-03 | Codex R1（FAIL，11 项 P1，逐项映射见 Codex review log R1 行） | ① MG-17 拆为 MG-17+MG-21+MG-22；② 串行窗口落为显式依赖边；③ Rollback 默认改 `immutable-release`；④ 计划级 D 组契约；⑤ cargo 单 filter；⑥ MG-01 移除 generation AC；⑦ MG-08 `merge.default`；⑧ MG-09 sq_quote；⑨ MG-16/18 解环；⑩ AC 谓词重计；⑪ T-1 修正 | MG-17 → MG-17, MG-21, MG-22 | 实施顺序、发布窗口顺序、粒度审计表、追溯表、里程碑、测试矩阵、Codex review log |
| 2026-09-03 | Codex R2（FAIL，8 项 P1，逐项映射见 R2 行） | ① Verification 改 `command_test` 模块路径；② AC 二次按谓词重写；③ MG-08 `merge.default` 仅属性未设置时生效；④ D 组排除 homebrew best-effort；⑤ ADR-MG-01 两档语义+MG-00 写集扩三消费者；⑥ MG-21 增 tree-ish 消费设计；⑦ MG-22 三态 override+T-1；⑧ MG-08 写集加消费者接线 | MG-00 / MG-08 / MG-21 / MG-22 范围修订 | 事实基线、ADR-MG-01、D 组契约、全部任务卡 Verification、粒度审计表、追溯表、测试矩阵、Codex review log |
| 2026-09-03 | Codex R3（FAIL，7 项 P1，逐项映射见 R3 行） | ① MG-08 非法 `merge.default` 改「未命中回退 text」；② MG-21 接入共享对象解析 seam；③ MG-22 override 持久化到 sequencer 状态；④ MG-08 三消费者具名测试+文档；⑤ AC 三次重写；⑥ D 组加回 `request-stable-manifest`；⑦ tag 推送顺序写入契约 | MG-08 / MG-21 / MG-22 范围修订 | 事实基线、D 组契约、ER-08 说明、多卡 AC/Verification/写集、粒度审计表、追溯表、Codex review log |
| 2026-09-03 | Codex R4（FAIL，5 项 P1，逐项映射见 R4 行） | ① MG-21 写集具名 `src/utils/util.rs:2124`；② MG-08 merge-file 具名测试；③ MG-22 补新进程+旧 JSON 验收；④ AC 三次原子化重写；⑤ MG-21 AUTO_MERGE 生命周期修正为「仅 merge 进行中可观察」 | MG-21 范围修订 | 事实基线、粒度审计表、全部任务卡 AC/Granularity、MG-08/MG-21/MG-22 Verification、Codex review log |
| 2026-09-03 | Codex R5（FAIL，4 项 P1，逐项映射见 R5 行） | ① 16 张门族卡登记 EX-01（G-03 门族型验收豁免）：每卡新增「判据规范（非计数正文）」门族块，逐门挂具名测试命令，分子如实写 `n/8@EX-01`；② MG-21 公共边界闭合：pseudo-ref 解析改调用方 opt-in（ADR-MG-06），compat 测试名称提取从 `_HEAD` 启发式改显式集合、拆 resolve/reject 两组；③ 全卡 prod-files 改为具名生产文件真实计数（MG-09 写集具名 `src/utils/atomic_write.rs`、MG-11 具名 `src/command/diff.rs`）；④ 补 `## 文档职责` 标题 | 无拆卡；MG-21 范围修订；16 卡登记 EX-01 | 文档职责、字段全局默认与例外（waiver 表）、粒度审计表、16 张卡的判据规范块与 Granularity 行、MG-09/MG-11 写集、MG-21 全卡、Codex review log |
| 2026-09-03 | Codex R6（FAIL，2 项 P1，逐项映射见 R6 行） | ① EX-01 准入补全：Approver 改为具名审批人 Eli Ma <eli@patch.sh>（libra 配置身份）；MG-07 补 G11（10→11 门）、MG-08 补 G15（14→15 门）、MG-09 补 G19（18→19 门）；② MG-21 写集补 `docs/commands/{rev-parse,cat-file,diff}.md` EN+zh 与开发文档，门族补 G14（13→14 门） | 无拆卡 | waiver 表、MG-07/MG-08/MG-09/MG-21 判据规范块与 Granularity 行、粒度审计表、MG-21 写集、Codex review log |
| 2026-09-03 | Codex R7（FAIL，4 项 P1 + 1 P2，逐项映射见 R7 行） | ① MG-21 G14 改由 `compat_pseudo_ref_surface` 同文件 (new) 文档契约断言承载，写集补三命令开发文档；② 非 EX-01 卡 AC 重算：MG-03（→10 门）、MG-15（→10 门）登记入 EX-01（门族卡 16→18 张），MG-15/16/17 scope 由 S 改 M（各含一处公开 flag 变更，G-04）；③ Docs 默认改为「按本卡公开表面逐份列明」，MG-19 写集补 `docs/commands/rebase.md` EN+zh 并加文档 AC（AC 5→6）；④ MG-08 属性读取锚点补 `src/utils/attributes.rs:267`；⑤（P2）Approver 删除与 git config 不一致的「libra 配置身份」断言 | 无拆卡；MG-03/MG-15 登记 EX-01 | waiver 表、MG-03/MG-15 判据规范块与 Granularity 行、粒度审计表、Docs 默认行、MG-19 写集与 AC、MG-08 证据锚点、Codex review log |
| 2026-09-03 | R8 双评审（Claude Code + Codex 均 FAIL，4 项 P1 + 4 项 P2，逐项映射见 review log R8 行） | ① EX-01 waiver Review round 补 R7（MG-03/MG-15 补登溯源）；② MG-15 增 `--lib merge::signing` 守卫（VER 3→4）、MG-00 G7 补 T-1 承载标注；③ Docs 默认逐卡落实：MG-00/MG-08/MG-10/MG-14/MG-15/MG-16/MG-18/MG-20/MG-22 写集补消费者 EN+zh 与开发文档，默认覆盖表删除已被默认覆盖的 MG-08/MG-22 行，收口清单扩容；④ 非 EX-01 卡 AC 原子化：MG-01（→6）、MG-02（→8）、MG-16（→5）、MG-17（→5）、MG-19（→8），分子同步 Granularity 与审计表；P2：MG-09/MG-21 Verification 注释补 G19/G14、MG-03 拍平路径生命周期写明、评审结论表与追溯表同步、锚点刷新（diff.rs:4104、status.rs:2375/:2459、pseudo_ref_surface.rs:38/:135、cherry_pick_test.rs:2065）、review log 证据迁移持久目录 | 无拆卡 | waiver 表、MG-00/MG-15 门族承载、MG-01/02/16/17/19 AC 与 Granularity、粒度审计表、默认覆盖表、九张卡写集、MG-09/MG-21 Verification、MG-03 写集、追溯表、收口清单、事实基线锚点、review log |
| 2026-09-03 | R9 双评审（Claude Code + Codex 均 PASS；Claude Code 余 1 项 P2） | MG-21 Current evidence `pseudo_ref_surface.rs:131`→`:135`（与事实基线锚点刷新对齐，事实基线 R8 已改、MG-21 卡内漏改）；R7/R8 全部 P1/P2 经双评审逐项确认关闭 | 无拆卡 | MG-21 Current evidence、review log |
| 2026-09-03 | R10 双评审（Codex FAIL 1 项 P1；Claude Code PASS + 2 项 P2） | Codex P1：ADR-MG-04 与 MG-02 的虚拟祖先保留模型矛盾→改为「一次性对象 + GC 回收预期化 + `--restart` 重算恢复」契约（ADR-MG-04 Consequences、MG-02 Description/AC7/Verification、故障恢复矩阵 recursive 行）；Claude Code P2：事实基线 revert.rs 锚点 `:960-963`、`_HEAD` 启发式锚点 `:113`（四处同步） | 无拆卡 | ADR-MG-04、MG-02、故障恢复矩阵、事实基线、GAP-21、MG-21 证据、追溯表、review log |
| 2026-09-03 | R11 双评审（Codex FAIL 2 项 P1 + 1 项 P2；Claude Code PASS + 2 项 P2） | Codex P1-①：ADR-MG-04 声称 `--continue` 重算虚拟祖先不可执行（`run_merge_continue` 只加载 state/index 收尾，`merge.rs:2291-2356`）→恢复路径改为仅 `--restart` 重算并在 ADR 写明 `--continue` 不重算；P1-②：MG-07 排除的目录 rename × rename/rename 角落案例违反「剩余差距仅为 DEFER-01~06」成功定义→登记 DEFER-07（非目标 bullet + DEFER 表 + 成功定义/COMPATIBILITY 收口口径改 DEFER-01~07 + MG-07 Out of scope 引用）；P2：R10 记录的「AC5」更正为 AC7。Claude Code P2：MG-14 卡内补 `Docs and compatibility impact` 字段行（与默认覆盖表登记对齐）；非目标 bullet 补 DEFER-05 条目 | 无拆卡 | ADR-MG-04、MG-07 Out of scope、非目标清单、DEFER 表、成功定义、COMPATIBILITY 收口行、MG-14 卡内字段、review log 与修订历史更正 |
| 2026-09-03 | R12 双评审（Codex FAIL 3 项 P1 + 1 项 P2；Claude Code PASS + 2 项 P2） | Codex P1：① MG-20 写集补三消费者 EN+zh 与开发文档（收口清单/追溯表同步）；② MG-02 共享文件 `internal/merge_base.rs`（仅消费）移出写集，landing/prod-files 2→1（Estimated scope/Granularity/审计表同步），trigger=none 合规；③ MG-09 共享文件 `utils/atomic_write.rs` 移出写集（临时文件实现收敛于 merge.rs，共享 helper 只读复用；若开工确需扩展则按 ER-02 刷新并改 T-1），landing/prod-files 2→1 同步；P2：R10/R11 行 Result 复合值改 FAIL、分歧移 Scope 列。Claude Code P2：事实基线 merge 行转述删「rename」、非目标 DEFER-06 bullet 补「cherry-pick 的 rename 接入」 | 无拆卡 | MG-20 写集、MG-02/MG-09 写集与计数、审计表、收口清单、追溯表、review log、事实基线、非目标清单 |
| 2026-09-03 | R13 双评审（Codex FAIL 2 项 P1；Claude Code PASS 零条目） | Codex P1-①：MG-01/02/19 AC 仍压缩多谓词→原子化：MG-01 拆至 8 门（仍非豁免）；MG-02 拆至 10 门、MG-19 拆至 10 门并登记 EX-01（门族卡 18→20 张，waiver/粒度规则/完成判据/评审结论同步），MG-19 增 (new) `rebase_driver`/`rebase_recursive`/`rebase_refine` 与文档契约门（VER 4→5），修订历史登记移为卡内过程性义务；P1-②：MG-09 追溯表删除 `atomic_write.rs` 落点并补开发文档列 | 无拆卡；MG-02/MG-19 登记 EX-01 | MG-01/02/19 AC 与 Granularity、审计表、waiver 表、粒度规则段、完成判据、评审结论表、追溯表、review log |
| 2026-09-03 | R14 双评审（Codex FAIL 2 项 P1；Claude Code PASS + 2 项 P2） | Codex P1-①：MG-14 `mergetool.<tool>.cmd` 契约与 Git（shell 求值 + 四变量 + trustExitCode/.path）矛盾→按 Git 信任模型重写（门族 13→15、AC 9、Security 信任边界同步）；P1-②：MG-19 G1/G2 零命中守卫→拆两条完整 `rg` 守卫命令（含 `>1` 分支），VER 5→6。Claude Code P2：MG-08 evidence 改 `diff_driver_for_path`（diff.rs:2728/:2734）与 `check_attr.rs:177`；release.yml 硬失败行锚点 `:732`（两处） | 无拆卡 | MG-14 全卡、MG-19 Verification/Granularity/审计表、事实基线、D 组契约、review log |
| 2026-09-03 | R15 双评审（Codex FAIL 2 项 P1；Claude Code PASS + 1 项 P2） | Codex P1-①：MG-14 trustExitCode=false 语义改 Git 精确语义（mtime + 未变更询问；trustExitCode=true 仅退出码）；P1-②：登记 DEFER-08~11（`merge.<name>.recursive` 修正误述、`-X find-renames`、`-s octopus`、mergetool 其余配置），成功定义/收口口径改 DEFER-01~11。Claude Code P2：MG-17 T-1 保守判定补区别理由 | 无拆卡 | MG-14 AC/门族、MG-09/11/13/14 Out of scope、非目标清单、DEFER 表、成功定义、收口行、MG-17 trigger、review log |
| 2026-09-03 | R16 双评审（Codex FAIL 4 项 P1 + 1 项 P2；Claude Code PASS + 1 项 P2） | Codex P1-①：MG-01 删除 generation 依赖的提前退出承诺（`commit-reach.c:194`），改保守全量染色终止，性能判据改写；P1-②：MG-01 逐调用面拆分并入 EX-01（11 门、VER 7、门族卡 20→21 张）；P1-③：MG-14 补「mtime 未变 + EOF/非交互 = 未解决」AC 与门 G8（门族 16、AC 11）；P1-④：登记 DEFER-12/13/14（外部 GPG/SSH 签名、默认不开编辑器、merge.suppressDest），口径改 DEFER-01~14；P2：mergetool.adoc 路径补 `Documentation/` 前缀。Claude Code P2：`update_ref.rs:401` 引用更正为 `:452`/`:461`（四处） | 无拆卡；MG-01 登记 EX-01 | MG-01 全卡与性能摘要、MG-14 AC/门族、MG-15/18 Out of scope、非目标清单、DEFER 表、成功定义、收口行、waiver 表、粒度规则段、完成判据、评审结论表、事实基线/ADR-MG-06/MG-21/追溯表锚点、review log |
| 2026-09-03 | R17 双评审（Codex FAIL 4 项 P1；Claude Code PASS + 2 项 P2） | Codex P1-①：MG-17 AC1 的 AUTO_MERGE 清理归还 MG-21 独占；P1-②：MG-01 移除 `log A...B` 调用面（log.rs 为独立实现），门族 11→10、VER 7→6、T-1 理由同步；P1-③：D 组判据 ① 改为 run ID/headSha 捕获 + `gh run watch` + `gh run view --json jobs` 逐 job conclusion 判定；P1-④：review log Evidence 绝对路径改归档标识（ER-11）。Claude Code P2：MergeArgs 锚点补 `:101`、rebase.rs 锚点精确化（:4649/:4756/:4823） | 无拆卡 | MG-17 AC/Out of scope、MG-01 Description/AC/门族/Verification/T-1/Granularity/审计表、D 组契约、review log 与 preamble、事实基线锚点 |
| 2026-09-03 | R18 评审（Claude Code 终审 PASS 零条目；Codex 服务不可用取消） | Claude Code 终审零条目（R8~R17 全部条目闭环核实、DAG/计数交叉核对、43 处锚点核对）；Codex R18 因服务连续 404 不可用，经委托人 Eli Ma 指示取消——Codex 最终 PASS 留待服务恢复后补做，本文档内容无变更（本行仅登记评审状态） | 无拆卡 | review log |

## 已决议设计决策

实现时若需偏离本节，必须先修改计划并说明原因，不得在代码中静默改语义。

### ADR-MG-01: subtree/submodule/gitlink 永久非目标；gitlink「裁决触及才 fail-closed」

- **Status:** Accepted
- **Context:** Libra 定位 monorepo 客户端，submodule/subtree 不是目标工作流；当前 merge 与 rebase 的 tree 拍平（`merge.rs:2666`、`rebase.rs:3842`）静默丢弃 gitlink，参与合并时会产生错误结果。但「任一输入 tree 含 gitlink 即拒绝」又会误伤大量无害场景（gitlink 子树三方一致时没有任何合并决策要做），且与剪枝优化（MG-03）冲突。
- **Decision:** 不实现 subtree 合并与 gitlink 合并。语义分两档：① 三路合并**裁决触及**的 gitlink 条目（任一侧与 base 不一致、或需要写入合并结果的路径）→ fail-closed，报可操作错误（路径 + 「Libra 不支持 submodule」）；② 三树一致（含被剪枝）的 gitlink 子树 → 原样 pass-through（内容逐字节来自输入，不产生合并决策，不构成错误结果）。merge/rebase/cherry-pick 共用同一校验 helper。
- **Alternatives considered:** 维持静默过滤（正确性风险，拒绝）；任一输入含 gitlink 即全拒（误伤无害场景、与剪枝冲突，拒绝）；实现 gitlink 可达性合并（超出产品定位，拒绝）。
- **Consequences:** 纯 Libra 工作流无影响；外部含 submodule 仓库的合并在「submodule 指针有分歧」时被拒绝而非出错结果；pass-through 语义是未来若支持 gitlink 时的兼容路径。
- **Revisit when:** 产品定位变化、决定支持 submodule 时。

### ADR-MG-02: 内容合并继续以 diffy 为行级内核，高级语义在其上自建

- **Status:** Accepted
- **Context:** 冲突精化、whitespace-ignore、union、driver 分派在 Git 由 xdiff/ll-merge 承载；Libra 已用 diffy 0.4.2 且有大量既有测试钉住标记格式。
- **Decision:** 保留 diffy 作为行级 diff3 内核；merge driver 分派、renormalize、whitespace 归一化、冲突精化（对 diffy 输出的冲突块再跑 diff 缩小）作为 Libra 自建层置于内核之上。`-X diff-algorithm=*` 因 diffy 单一算法而 DEFER。
- **Alternatives considered:** 移植/绑定 xdiff（工作量大、收益不覆盖成本，拒绝）；换 similar crate（同样无 zealous refine，拒绝）。
- **Consequences:** 精化后冲突块与 Git 字节级一致不作硬性承诺，判据为「语义一致（同真冲突、同自动合并结果）+ 既有格式测试不回归」。
- **Revisit when:** diffy 停止维护或出现必须字节级对齐的需求。

### ADR-MG-03: rename 检测按侧进行（base→ours、base→theirs 两轮），复用 rename_detect

- **Status:** Accepted
- **Context:** Git ort 的 rename 是 per-side 检测后处理路径级冲突（`merge-ort.c:3429`）；Libra 现有 `rename_detect::match_pairs` 接受删除/新增快照配对，语义吻合。
- **Decision:** merge 对 base→ours 与 base→theirs 各跑一次 `rename_detect::match_pairs`；三路匹配键由「同一路径三元组」扩展为「经 rename 表重定向的三元组」；rename 冲突类型按 Git 分类落地。目录级 rename 推断作为独立后续卡（MG-07）。
- **Alternatives considered:** 引入 diffcore-rename 式全量相似度矩阵新实现（重复造轮子，拒绝）。
- **Consequences:** `merge_tree_items()`/`resolve_three_way()` 需要重构以支持路径重定向；rename 检测的预算控制（`RenameDetectConfig`）沿用 status/diff 的默认值并允许 `merge.renameLimit` 类配置。
- **Revisit when:** rename 检测成为 merge 热路径性能瓶颈（届时评估 cached_pairs 类缓存）。

### ADR-MG-04: 保持 Libra 全内存合并 + sidecar 状态架构，不引入 Git 的 index 中间态

- **Status:** Accepted
- **Context:** Git ort 先全内存合并再 `merge_switch_to_result()` 一次性写回；Libra 现有架构同构（内存决策 → 写 index stage/工作区/sidecar）。
- **Decision:** 新能力（recursive 虚拟祖先、octopus、rename）全部在该架构内实现；虚拟祖先只作为内存中的合成 tree/commit，不落对象库之外的中间态；recursive 合并产生的虚拟 base 不写入 `merge-state.json`（崩溃恢复时由 target/base 重算，与 `--restart` 现有语义一致）。
- **Alternatives considered:** 移植 Git 的 AUTO_MERGE + unpack_trees 体系（与 Libra 存储模型不匹配，拒绝；AUTO_MERGE 的**投影**由 MG-21 在本架构内实现，仅 merge 进行中可观察）。
- **Consequences:** recursive 虚拟祖先的 tree/合成 commit 对象作为一次性 loose object 写入对象库；因不写入 `merge-state.json`，它们不被 `merge_state_gc_oids`（`src/command/merge.rs:367` 起的语义 OID 收集）引用，`maintenance gc` 可将其回收——这是预期行为而非缺陷：中断恢复不依赖虚拟对象存活，恢复路径仅为 `--restart` 由 target/base 重算（`--continue` 不做虚拟祖先重算，其只加载既有 state/index 收尾，MG-02 故障注入用例验证「GC 回收 → `--restart` 重算恢复」）。
- **Revisit when:** 出现嵌套 depth 过大导致的性能或栈深度问题。

### ADR-MG-05: octopus 只做树级无冲突合并，与 git-merge-octopus.sh 语义一致

- **Status:** Accepted
- **Context:** Git 的 octopus 策略在非最后一个 head 出现需手工解决的冲突时整体失败（"Should not be doing an octopus"）。
- **Decision:** Libra octopus 同样只接受全自动可合并情形：逐 head 三方合并，任一 head 产生内容级冲突即整体拒绝（不留下半成品状态）；`--no-ff` 语义与多父提交（parents = HEAD + 全部 remote heads）与 Git 一致。
- **Alternatives considered:** 允许 octopus 冲突进入人工解决（Git 自身也不支持，拒绝）。
- **Consequences:** 需要 `merge_bases` 的 octopus 变体（`get_octopus_merge_bases` 等价物）；`MergeArgs` 分支参数从 `Option<String>` 改为 `Vec<String>`，单分支行为完全不变。
- **Revisit when:** N/A（与 Git 对齐即终态）。

### ADR-MG-06: pseudo-ref 在共享对象解析 seam 中以「调用方 opt-in」接入

- **Status:** Accepted
- **Context:** `util::resolve_object_spec_typed`（`src/utils/util.rs:2124`）被 rev-parse、cat-file、update-ref 等多个调用方共享；update-ref 的 `<oldvalue>` CAS 操作数不做 commit 类型检查（`update_ref.rs:452` `resolve_old_value`，其 doc comment 明示 "deliberately no commit type check"，resolver 调用在 `:461`；对比 `:389` 起的 `resolve_new_value` 在 `:421` 有 commit 类型检查）。若在 seam 中无条件启用 pseudo-ref 解析，update-ref 会意外接受 AUTO_MERGE 的 tree OID，改变既有拒绝语义。
- **Decision:** seam 的 pseudo-ref 解析为**调用方 opt-in**（显式能力参数/开关）：只有 rev-parse、cat-file、diff 三个本计划声明的表面启用 AUTO_MERGE 解析；未 opt-in 的调用方（update-ref 及其余全部现有调用方）行为零变化，对 pseudo-ref 名称继续保持拒绝。
- **Alternatives considered:** seam 无条件启用（update-ref 语义被破坏，拒绝）；在三个表面各自实现 pseudo-ref 解析（违反 GC-02 单一事实源，拒绝）。
- **Consequences:** 既有六个 pseudo-ref 的「rev-parse 不可解析」契约中，AUTO_MERGE 成为**唯一例外**（仅 merge 进行中、仅 opt-in 表面可解析）；compat 测试必须拆 resolve/reject 两组并把名称提取从 `_HEAD` 启发式改为显式集合。
- **Revisit when:** 出现新的需要消费 pseudo-ref 的表面（逐一评审 opt-in）。

## 全局工程约束

以下约束对本文所有任务生效（继承 plan-template v2.2 GC-01 ~ GC-12，全文不再复制）：

- **GC-01 现状核实前置** … **GC-12 精确暂存**：按 `docs/development/plan/plan-template.md` v2.2「全局工程约束」逐条生效。
- **GC-MG-13（计划级）merge.rs 单一持有者：** merge.rs 是全部算法卡的公共热点。任何卡在修改 merge.rs 期间，其它卡不得开工 merge.rs 相关改动；由「实施顺序」的串行链保证（G-10 I–I 冲突以顺序边消解，不做并发豁免）。
- **GC-MG-14（计划级）merge-state.json schema 演进：** 新增字段必须 serde 可选 + 默认值，旧版 state 文件可读（既有 `MergeState` 旧 schema 兼容单测模式延续）；禁止破坏性格式变更。
- **GC-MG-15（计划级）Git 语义判据：** 凡声明「与 Git 一致」的行为，判据用例必须能从 pin 的 git@`3cb9185f6` 对应文件/函数指出语义出处；无法实跑对照 git 二进制的场景，以源码语义 + 既有文档为准并在用例注释注明出处。

## 执行检查必备需求（强制）

plan-template v2.2 的 **ER-01 ~ ER-13 全文对本计划生效**，要点登记如下（权威文本以模板为准）：

- ER-01 开工前 `libra status --short --branch` 安全检查；ER-02 先核对后实现；ER-03 粒度门禁逐字段核对 `Granularity` 行。
- ER-04 每卡验收门 A（表面 focused）+ B（类型）+ C（发布收口）+ D（远端后置）四组累加；`Lifecycle` / `Acceptance` 双状态字段按模板取值，唯一转移路径 A/B → `locally-accepted` → ER-05 PASS → C 覆盖 →（无 D）`complete`。
- ER-05 Codex review 闭环：P0/P1 必须关闭至 PASS。
- ER-06 文档与兼容同步（EN+zh、开发文档、COMPATIBILITY.md、错误码、测试索引）。
- ER-07 Libra-native 工作流 + 签名预检（先读 `commit.gpgSign` 再 `vault.signing`）+ 提交后 `libra cat-file -p HEAD | rg -q '^gpgsig'` 校验。
- ER-08 每卡 `patch + 1`（patch 位无上限，只动 patch 位，**不进位 minor**——本计划全程停在 `0.22.x` 线）、版本面五处同步、`Cargo.lock` 只由工具链刷新。

  **⚠️ 并发前置（计划级强制）：本仓库同时被多个 Session 修改，版本号不得按开工时的基线推算。** 每张卡在 bump 之前必须先同步远端，再现场取值：

  1. `libra fetch origin`，把其它 Session 已推的提交取回本地。
  2. 将本卡工作**merge** 到最新 `origin/main` 之上。**禁止用 `libra rebase` 做这一步**：本工作树是 partial checkout（约 170 个 tracked 文件在磁盘上为空），rebase 会按工作区重建树并把它们判为删除，推送即等于从 origin 删文件。若需线性化，只能走「`reset --mixed <upstream>` → 仅 `add` 本卡文件 → 推前做 tree 校验」的安全路径。
  3. 同步完成后**重新读取 `Cargo.toml` 的 `version`**，以该实际值 `patch + 1` 作为本卡版本；**允许跳号**（其它 Session 已占用的 patch 位直接跳过），禁止沿用开工时推算出的序列。
  4. `libra tag` 之前预检该 tag 在远端不存在（`libra ls-remote --tags origin` 或等价手段）；版本号被其它 Session 占用会导致 tag 冲突或 `release.yml` 跑在错误的 commit 上。
  5. 提交按 GC-12 精确暂存：并发 WIP 会让 `libra diff --cached` 不可靠，以 `libra status` 为准，`libra restore --staged .` 清空暂存区后只 `add` 本卡写集内的文件，再 `commit -s -m`（**不用 `-a`**，避免把其它 Session 的改动或带外删除一并提交）。

  **发布触发顺序（计划级明确，各卡 C 组第 ⑨ 步按此执行）：** 上述并发前置 1–5 完成后 → `libra tag "v${VERSION}"` → `libra push origin "v${VERSION}"`（release.yml 仅由 `v*` tag push 触发，见 release.yml:3）→ `gh release create "v${VERSION}" --generate-notes --verify-tag`（tag 已在远端存在，`--verify-tag` 不会中止）→ 进入 D 组取证。

- ER-09 push 失败策略；ER-10 内部服务错误有界重试（默认 ≤5 次 / ≤30 分钟）；ER-11 证据卫生；ER-12 并发边界与串行发布（本计划发布者唯一，见「发布分组与并发窗口」）。
- ER-13 测试分层：执行阶段 focused；命中 T-1 ~ T-6 的卡跑全量（逐卡 `Full-suite trigger` 登记）；全部卡完成后跑全量收口门并修复暴露的全部 Bug。

**D 组统一登记（计划级契约，全部 `independent` 卡适用；模板 ER-04 D 组要求的逐卡登记由本条统一承载，各卡 `C/D coverage from: self` 即引用本契约）：**

- **Workflow / 触发：** `.github/workflows/release.yml`（`on.push.tags: v*`），由 ER-08 的 `libra push origin "v${VERSION}"` 触发。
- **硬判据 job（2026-09-03 现场读取，开工时重新核对）：** `build-and-upload`（release.yml:15）、`upload-install-scripts`（:323）、`request-stable-manifest`（:690，`set -euo pipefail` 硬失败 job，见 :732）。
- **判据：** ① `gh run list --workflow=release.yml --branch "v<version>" --json databaseId,headSha --jq '.[0]'` 取得本卡 tag 的 run，记录并核对其 `headSha` 与本卡 tag 指向一致——**并发发布下这是硬判据**：多个 Session 交替推 tag 时，`gh run list` 的首条 run 可能属于其它卡，`headSha` 不匹配即必须按 tag 重新定位 run，不得就近取用；`gh run watch "<run_id>" --exit-status` 等待完成；`gh run view "<run_id>" --json jobs --jq '.jobs[] | select(.name == "build-and-upload" or .name == "upload-install-scripts" or .name == "request-stable-manifest") | {name, conclusion}'` 要求上述三个硬判据 job 的 `conclusion` 均为 `success`（run/ID/job 逐项可复现，ER-04 D 组口径）；② `download.libra.tools` 对应版本 artifact 可获取（CDN 抽查，sanitized 证据）；③ stable manifest 响应中的版本号/摘要与本卡 `v<version>` 一致（客户端可升级的直接证据，sanitized 记录）。
- **排除项（不进入完成条件）：** `update-homebrew-tap`、`verify-homebrew-formula` 为 best-effort——其失败路径降级为 `::warning::` 并 `exit 0`（release.yml:530-556），job 绿色不构成「已更新/可安装」证据，故不作为 D 组判据；若发布方需要 Homebrew 渠道确认，须以 tap 仓库 formula 的实际提交内容与本地安装版本为独立人工证据，不阻塞本计划的 `complete` 流转。
- **状态流转：** 取得 ①②③ 前卡停留 `remote-pending`，不得报 `complete`；D 组失败一律前滚修复（ER-04 D 组口径）。

## 实施顺序

依赖边格式：`A -> B` 表示 A 必须先于 B。本计划 merge.rs 写集高度相交（GC-MG-13），全部卡以**串行链**排序（I–I 冲突的顺序边，G-10），逻辑依赖另列；两者合并后的 DAG 无环。

**串行链（写集串行边）：**

- `MG-00 -> MG-01 -> MG-02 -> MG-03 -> MG-04 -> MG-05 -> MG-06 -> MG-07 -> MG-08 -> MG-09 -> MG-10 -> MG-11 -> MG-12 -> MG-13 -> MG-14 -> MG-15 -> MG-16 -> MG-17 -> MG-18 -> MG-21 -> MG-22 -> MG-19 -> MG-20`

**逻辑依赖边（除链上边外的新增边）：**

- `MG-00 -> MG-02`（gitlink fail-closed 的输入校验先于 recursive 落地，避免虚拟祖先路径绕过校验）
- `MG-01 -> MG-02`（recursive 需要完整 merge base 集合）
- `MG-00 -> MG-03`（MG-03 的剪枝路径必须与 MG-00 的「裁决触及」语义一致）
- `MG-05 -> MG-06`（rename 冲突类型依赖 per-side rename 检测）
- `MG-05 -> MG-07`、`MG-06 -> MG-07`（目录 rename 推断依赖 per-side 检测与路径级冲突分类）
- `MG-08 -> MG-09`（外部 driver 依赖 driver 分派框架）
- `MG-02 -> MG-09`（外部 driver 的 `%S/%X/%Y` 标签随 recursive depth 变化）
- `MG-01 -> MG-12`（octopus 需要 merge base 集合的 octopus 变体）
- `MG-02 -> MG-13`、`MG-05 -> MG-13`、`MG-12 -> MG-13`（`-s resolve` 降级分界 = 关闭 recursive 与 rename；多 head 默认路由依赖 octopus 落地）
- `MG-12 -> MG-15`（octopus 多父提交纳入签名路径）
- `MG-16 -> MG-18`（`--signoff` 与 `--cleanup`/编辑器的组合顺序验收在 MG-18）
- `MG-02 -> MG-19`、`MG-05 -> MG-19`、`MG-08 -> MG-19`、`MG-10 -> MG-19`（rebase 收敛依赖统一引擎具备 recursive / rename / driver / 精化能力）

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | incoming | 外部参照 | git@`3cb9185f65410273787f74333cc027d2ea5daada`（本地 `/Volumes/Data/competition/git/git`） | 本计划 | 算法语义参照：对应文件/函数可读、revision 固定 | 2026-09-03 核对 `rev-parse HEAD` | 本地副本不可用时以 `COMPATIBILITY.md` 与既有开发文档记录语义为准，并在用例注释标注「参照缺失」 |

### 发布分组与并发窗口

默认每张卡独立发布（G-07），无 `REL-*` 登记。

**并发声明:** 全串行。实现写集以 merge.rs 为中心高度相交（GC-MG-13），串行链已在「实施顺序」落为顺序边；不声明并发窗口。

**发布者:** 单一发布者（执行本计划的 Agent/人），负责全部卡的 C 组 bump/构建/安装/提交/branch 推送/tag 推送/`gh release create` 与 D 组远端证据跟踪（ER-12）。

**发布窗口顺序:** 与串行链一致：`MG-00 → MG-01 → MG-02 → MG-03 → MG-04 → MG-05 → MG-06 → MG-07 → MG-08 → MG-09 → MG-10 → MG-11 → MG-12 → MG-13 → MG-14 → MG-15 → MG-16 → MG-17 → MG-18 → MG-21 → MG-22 → MG-19 → MG-20`。同一时刻只允许一张卡处于「已 bump 未完成推送 / 未触发 release」状态。

### Phase 0: 安全默认值与基线冻结

**目标:** gitlink fail-closed 落地；确认基线锚点与 `plan-long.md` 关系。

**进入条件:** 计划成稿、Codex review 结论为 PASS。

**退出条件:** MG-00 完成（`Acceptance=complete`）。

### Phase 1: 算法内核

**目标:** merge base 升级、recursive 虚拟祖先、增量树合并、D/F 冲突、rename 三卡。

**进入条件:** Phase 0 退出。

**退出条件:** MG-01 ~ MG-07 完成；rename+修改、criss-cross、D/F 场景与 Git 语义一致。

### Phase 2: 内容层

**目标:** merge driver（内建 + 外部）、冲突精化 + zdiff3、whitespace/renormalize。

**进入条件:** Phase 1 退出。

**退出条件:** MG-08 ~ MG-11 完成。

### Phase 3: 功能面

**目标:** octopus、策略框架、mergetool、签名/signoff、状态面（--quit / AUTO_MERGE / rerere flag）、消息面。

**进入条件:** Phase 2 退出。

**退出条件:** MG-12 ~ MG-18、MG-21、MG-22 完成；`COMPATIBILITY.md` merge 行缺口描述收敛到「永久非目标 + DEFER 登记项」。

### Phase 4: 收敛与收口

**目标:** rebase 收敛统一引擎、rerere 规范化、全量收口门。

**进入条件:** Phase 3 退出。

**退出条件:** MG-19、MG-20 完成；全量收口门全绿；Codex review 最终 PASS。

## 任务卡

任务 ID 前缀 `MG-`。编号被引用后不重排；拆分出的新卡在所属 Phase 末尾追加新编号，编号顺序 ≠ 执行顺序（MG-21、MG-22 即 R1 后自 MG-17 拆出，排在 Phase 3 末尾）。

### 任务卡粒度规则（强制）

plan-template v2.2 的 **G-01 ~ G-11**（含「推荐拆分维度」「粒度反模式速查」）逐条生效，全文不再复制。每卡的 `Granularity` 摘要行是机器可核对判据，ER-03 开工前逐字段核对。**AC 计数口径（计划级强调）：** `Acceptance criteria` 是每卡的头条判据（原子化、一个条件一个结果）；21 张卡的完整验收是同一恢复轴上的 Git 语义机械门族，逐门计数超过 G-03 上限，按模板白名单登记 **EX-01（G-03 门族型验收豁免）**——这些卡在卡内「判据规范（非计数正文，EX-01 门族）」块逐门枚举全部验收门（每门挂具名测试命令，失败即整卡不达标），`Granularity` 与审计表的 AC 分子如实写门族总数 `n/8@EX-01`。

#### 字段全局默认与例外

计划在本节声明字段的全局默认值后，任务卡中取默认值的字段整行省略：

- **Release boundary 默认:** `independent`（每张卡独立发布切片）
- **Task type 默认:** `implementation`
- **Rollback mode 默认:** `immutable-release`——每张卡独立完成并发布后，artifact 已上传 Cloudflare R2，不可撤回。**统一降级指引：** 恢复动作 = 前滚修复并发布新 patch 版本（ER-08 流程），用户侧可安装前一版本（`install.sh` 指定版本号）；**兼容窗口** = merge-state.json 旧 schema 可读（GC-MG-14）保证跨版本状态兼容，无任何卡引入不可逆数据迁移。各卡不再逐条重复本指引。
- **Migration and rollback 默认:** N/A（无 schema 迁移；merge-state.json 演进按 GC-MG-14 向后兼容）
- **Security and privacy 默认:** 继承 GC-07、GC-11
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 按 GC-05 同步**本卡公开表面实际触达的**命令文档 EN+zh（各卡在写集中逐份列明实际文档，禁止只挂 merge 文档）、相关 `docs/development/commands/*.md`、`COMPATIBILITY.md`；新增/变更测试模块按 GC-06 评估 `tests/INDEX.md` 对应行描述是否失真（`tests/compat/*` 新文件另需 `Cargo.toml` `[[test]]` + `tests/compat/README.md`）
- **C/D coverage from 默认:** `self`（C 组自行执行；D 组适用「执行检查必备需求」中的 D 组统一登记契约）
- **Version increment 默认:** `patch`
- **Deliverables 默认:** N/A（代码卡）

**默认覆盖**（不是例外，只是取了非默认值，无需审批）：

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| MG-09 | `Security and privacy` | 外部 driver 是子进程边界：sq_quote 契约、临时文件、退出码语义有专项 AC |
| MG-01 / MG-03 | `Performance budget` | 有复杂度界与 benchmark 断言（见卡内） |
| MG-14 | `Security and privacy` | mergetool 调外部进程，工具路径与临时文件有专项 AC |
| MG-14 | `Docs and compatibility impact` | 新增命令：`docs/commands/mergetool.md` EN+zh 新建 + `docs/development/commands/mergetool.md` 新建 + `COMPATIBILITY.md` 新增 mergetool 行 + `tests/command/mod.rs` 注册测试模块（`tests/INDEX.md` 的 `command_test` 行若描述失真则更新） |

**规则 waiver（`EX-*`，需具名审批）**：可豁免规则为模板白名单三项之一；本计划登记一项：

| 例外 ID | 任务（或 `ALL/<作用域>`） | 豁免项 | 理由与补偿措施 | Approver | Review round | 证据 | 有效期 |
|---|---|---|---|---|---|---|---|
| EX-01 | `ALL/MG-* 中 21 张门族卡`（MG-00、MG-01、MG-02、MG-03、MG-04、MG-05、MG-06、MG-07、MG-08、MG-09、MG-10、MG-11、MG-12、MG-13、MG-14、MG-15、MG-18、MG-19、MG-20、MG-21、MG-22） | G-03 条目上限（**门族型验收**） | 这些卡的验收本质是同一恢复轴上的 Git 语义机械门族（属性分支、flag 矩阵、冲突类别、占位符契约、消费者继承面），逐门计数必然超过 8；按门拆卡会违反 G-01/G-02（同一行为轴的实现/测试/文档不得拆散）与「碎片卡」反模式。准入条件落实：① 每门均为具名测试命令承载（门族块逐门标注承载命令），失败即整卡不达标；② 门族清单在卡内「判据规范（非计数正文，EX-01 门族）」块逐条枚举；③ 分子如实写 `n/8@EX-01`；④ 门族增减时同批更新本行与粒度审计表 | Eli Ma <eli@patch.sh>（本计划委托人，2026-09-03 会话指令按 plan-template 成稿并迭代至评审 PASS，采用模板白名单机制） | R5、R7、R13、R16（MG-03/MG-15 于 R7、MG-02/MG-19 于 R13、MG-01 于 R16 补登，同批更新本行） | 各卡判据规范块 + 粒度审计表 | 本计划内 |

#### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| MG-00 | implementation | gitlink fail-closed | 前滚修复 | yes | yes | 9/8@EX-01 | 3/8 | 2/9 | M | none | 序列化于链首 | independent | N/A | EX-01 |
| MG-01 | implementation | merge base 算法 | 前滚修复 | yes | yes | 10/8@EX-01 | 7/8 | 1/1 | M | MG-00 | 序列化于 MG-00 | independent | N/A | EX-01 |
| MG-02 | implementation | recursive 虚拟祖先 | 前滚修复 | yes | yes | 10/8@EX-01 | 4/8 | 1/2 | M | MG-00, MG-01 | 序列化于 MG-01 | independent | N/A | EX-01 |
| MG-03 | implementation | 增量树合并 | 前滚修复 | yes | yes | 10/8@EX-01 | 4/8 | 1/1 | M | MG-00, MG-02 | 序列化于 MG-02 | independent | N/A | EX-01 |
| MG-04 | implementation | D/F 冲突 | 前滚修复 | yes | yes | 11/8@EX-01 | 4/8 | 1/1 | M | MG-03 | 序列化于 MG-03 | independent | N/A | EX-01 |
| MG-05 | implementation | per-side rename 合并 | 前滚修复 | yes | yes | 10/8@EX-01 | 4/8 | 1/2 | M | MG-04 | 序列化于 MG-04 | independent | N/A | EX-01 |
| MG-06 | implementation | rename 冲突类型 | 前滚修复 | yes | yes | 13/8@EX-01 | 4/8 | 1/1 | M | MG-05 | 序列化于 MG-05 | independent | N/A | EX-01 |
| MG-07 | implementation | 目录 rename 推断 | 前滚修复 | yes | yes | 11/8@EX-01 | 3/8 | 1/1 | M | MG-05, MG-06 | 序列化于 MG-06 | independent | N/A | EX-01 |
| MG-08 | implementation | merge driver 分派（内建） | 前滚修复 | yes | yes | 15/8@EX-01 | 6/8 | 1/4 | M | MG-07 | 序列化于 MG-07 | independent | N/A | EX-01 |
| MG-09 | implementation | 外部 merge driver | 前滚修复 | yes | yes | 19/8@EX-01 | 4/8 | 1/1 | M | MG-02, MG-08 | 序列化于 MG-08 | independent | N/A | EX-01 |
| MG-10 | implementation | 冲突精化 + zdiff3 | 前滚修复 | yes | yes | 10/8@EX-01 | 5/8 | 1/3 | M | MG-09 | 序列化于 MG-09 | independent | N/A | EX-01 |
| MG-11 | implementation | whitespace/renormalize | 前滚修复 | yes | yes | 10/8@EX-01 | 4/8 | 1/2 | M | MG-10 | 序列化于 MG-10 | independent | N/A | EX-01 |
| MG-12 | implementation | octopus 合并 | 前滚修复 | yes | yes | 13/8@EX-01 | 4/8 | 2/2 | M | MG-01, MG-11 | 序列化于 MG-11 | independent | N/A | EX-01 |
| MG-13 | implementation | 策略框架与回退 | 前滚修复 | yes | yes | 14/8@EX-01 | 4/8 | 1/1 | M | MG-02, MG-05, MG-12 | 序列化于 MG-12 | independent | N/A | EX-01 |
| MG-14 | implementation | mergetool | 前滚修复 | yes | yes | 16/8@EX-01 | 5/8 | 2/3 | M | MG-13 | 序列化于 MG-13 | independent | N/A | EX-01 |
| MG-15 | implementation | merge 提交签名 | 前滚修复 | yes | yes | 10/8@EX-01 | 4/8 | 1/1 | M | MG-12, MG-14 | 序列化于 MG-14 | independent | N/A | EX-01 |
| MG-16 | implementation | --signoff | 前滚修复 | yes | yes | 5/8 | 2/8 | 1/1 | M | MG-15 | 序列化于 MG-15 | independent | N/A | N/A |
| MG-17 | implementation | --quit | 前滚修复 | yes | yes | 5/8 | 2/8 | 1/1 | M | MG-16 | 序列化于 MG-16 | independent | N/A | N/A |
| MG-18 | implementation | 消息面 parity | 前滚修复 | yes | yes | 10/8@EX-01 | 4/8 | 1/1 | M | MG-16, MG-17 | 序列化于 MG-17 | independent | N/A | EX-01 |
| MG-21 | implementation | AUTO_MERGE 投影 | 前滚修复 | yes | yes | 14/8@EX-01 | 5/8 | 3/7 | M | MG-18 | 序列化于 MG-18 | independent | MG-17 | EX-01 |
| MG-22 | implementation | rerere autoupdate 三态 | 前滚修复 | yes | yes | 11/8@EX-01 | 4/8 | 1/4 | M | MG-21 | 序列化于 MG-21 | independent | MG-17 | EX-01 |
| MG-19 | implementation | rebase 引擎收敛 | 前滚修复 | yes | yes | 10/8@EX-01 | 6/8 | 1/2 | M | MG-02, MG-05, MG-08, MG-10, MG-22 | 序列化于 MG-22 | independent | N/A | EX-01 |
| MG-20 | implementation | rerere 规范化 | 前滚修复 | yes | yes | 10/8@EX-01 | 4/8 | 1/1 | M | MG-19 | 序列化于 MG-19 | independent | N/A | EX-01 |

#### Verification 判定口径

- 零命中守卫按 plan-template v2.2「Verification 判定口径」的 `rg` 退出码模板执行（区分 `1` 零命中与 `>1` 失败）。
- 新增 test fn 标 `(new)`；所属测试模块必须已存在或在同卡注册（`tests/command/mod.rs` 挂 `mod`；`tests/compat/*` 新文件还需 `Cargo.toml` `[[test]]`）并按 GC-06 评估 `tests/INDEX.md`。
- `cargo test --all` 不作为单卡判据行（ER-13：由触发条件或收口门覆盖）。
- **cargo 单 filter 规则：** `cargo test` 每次调用只接受一个 test-name filter；本计划所有 Verification 命令均为单 filter 形式。
- **测试 target 事实（2026-09-03 核对）：** `tests/command/*.rs` 全部编译进 `command_test` 单 binary，调用形式为 `cargo test --test command_test command::<module>::<filter>`；`--lib <filter>` 用于 `src/**` 内联单测；`tests/compat/*` 是在 `Cargo.toml` 注册的独立 target。计划命令中的 compat target 名开工时按 `Cargo.toml` `[[test]]` 刷新。
- **EX-01 门族块的「承载」标注：** 每门标注的 `<filter>` 即该门断言所在的 test fn；执行该 fn 所属的 Verification 命令即覆盖该门，门失败 = 该测试失败 = 整卡不达标。
- Git 语义出处注释（GC-MG-15）不构成独立验证门。

### Task MG-00: gitlink 输入 fail-closed（裁决触及语义）

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `released` / `complete`（A/B 组全绿 → Codex R10 `PASS` → `locally-accepted` → C 组 `v0.22.11` 发布完成 → D 组三项证据取得：① release.yml run `33858000213`（`headSha=611002a`，与 tag 一致）三个硬判据 job 全 `success`——`build-and-upload` 四个矩阵腿（darwin-arm64 / windows-amd64 / linux-arm64 / linux-amd64）、`upload-install-scripts`、`request-stable-manifest`；② CDN 可获取：`https://download.libra.tools/libra/releases/v0.22.11/libra-darwin-arm64` 返回 200，且 `install.sh` 已服务 `DEFAULT_VERSION="v0.22.11"`；③ stable manifest（`/libra/releases/stable/manifest-v1.json`）解出 `channel=stable, version=0.22.11, published_at=2026-09-04T10:03:13Z`，四个平台 artifact 齐全，与本卡 tag 一致。发布时本地 `main` 与 `origin/main` 已分叉：本地另有 6 个本会话之外的既有未推送提交（`605a10f`/`36f249f`/`7321885`/`8c226ec`/`dcd563f`/`7245929`），`origin/main` 另有 2 个（`b800de7`/`b21e4ef`）；经委托人 2026-09-04 明确裁决「合并 origin/main 后整体推送」，故本卡的发布切片附带发布了那 6 个提交，登记在此以免与 ER-08 的单卡切片口径混淆。）

**Description:** 按 ADR-MG-01 的两档语义落地 gitlink 安全默认值：三路合并**裁决触及**的 gitlink 条目 fail-closed；三树一致的 gitlink 子树原样 pass-through。merge、rebase、cherry-pick 三个消费者共用同一校验 helper，消除 `merge.rs:2666` 与 `rebase.rs:3842` 两处静默丢弃。本卡唯一行为轴：危险输入的安全默认值。

**Out of scope:** gitlink 合并/可达性分析——「永久非目标，理由 ADR-MG-01」。submodule **工作树内容**的管理同属该边界，其三项残留（`restore` 写入已检出 submodule 目录**之下**、`reset --hard` 的空占位目录清理、`restore --ours/--theirs` 在缺失 stage 时的删除）经 R9 逐条评审确认先于本卡存在且未被本卡变得可达，登记在 `_compatibility.md` D24 的「已登记残留」，不在本卡修。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| merge 拍平静默跳过 `TreeItemMode::Commit` | `src/command/merge.rs:2666`（2026-09-04 开工核对：`commit_tree_items`，一致） |
| rebase 拍平同样静默丢弃 gitlink | `src/command/rebase.rs:3842`（2026-09-04 开工核对：`collect_tree_items_and_paths`，行号一致） |
| index 转换处已有 "gitlink entries are not supported by merge" 报错（防线不一致） | `src/command/merge.rs:3495`（2026-09-04 开工核对：`tree_item_mode_to_index_mode`，一致） |
| cherry-pick 经 `TreeExt::get_plain_items` 丢弃 gitlink（并向 stderr 打红字警告） | `src/command/cherry_pick.rs:1590`（`diff_trees`）→ `src/utils/object_ext.rs:81` |
| **开工实测（2026-09-04）**：`reset::rebuild_index_from_tree_typed` 对 gitlink 调 `Blob::load` → panic（submodule 的 commit 对象不在本仓库对象库） | `src/command/reset.rs:1708` |
| **开工实测（2026-09-04）**：`status::changes_to_be_staged_split_*` 把 gitlink 索引条目当普通文件比对——路径缺失判 deleted、目录存在则整条 status 以 `LBR-IO-001`（"Is a directory"）硬失败；该判定喂给 `switch::ensure_clean_status`，因此**含 submodule 的仓库连 `switch`/`merge`/`rebase` 都无法起步** | `src/command/status.rs:6579`、`:6640`；`src/command/switch.rs:1161` |

**Acceptance criteria:**

- [ ] 裁决触及 gitlink 条目时，在任何 worktree/index 写入前 fail-closed，错误消息含 gitlink 路径与「Libra 不支持 submodule」说明。
- [ ] 三树一致（含被剪枝）的 gitlink 子树原样传递，不触发拒绝。
- [ ] 三个消费者（merge/rebase/cherry-pick）的三路输入经同一校验 helper 进出（GC-02），`rebase.rs` 的静默丢弃同步消除。
- [ ] 无 gitlink 场景行为零变化（既有 `command::merge_test` / `command::rebase_test` 全绿）。
- [ ] 新稳定错误码（或语义相符的既有码复用）同步 `docs/error-codes.md` 与 Display pin 测试。

**判据规范（非计数正文，EX-01 门族，共 9 门）：**

- G1 裁决触及 gitlink 时在任何写入前 fail-closed — 承载 `merge_gitlink`
- G2 错误消息含 gitlink 路径 — 承载 `merge_gitlink`
- G3 错误消息含「Libra 不支持 submodule」说明 — 承载 `merge_gitlink`
- G4 三树一致 gitlink 子树 pass-through — 承载 `merge_gitlink`
- G5 校验 helper 单一事实源（三消费者同一入口） — 承载 `merge_gitlink` + `merge::` 单测
- G6 `rebase.rs` 静默丢弃消除（rebase 触及 gitlink 同样拒绝） — 承载 `merge_gitlink`（rebase 场景）
- G7 merge 无 gitlink 场景零变化 — 承载 `command::merge_test::` 回归（T-1 全量）
- G8 rebase 无 gitlink 场景零变化 — 承载 `command::rebase_test::` 回归（T-1 全量）
- G9 错误码同步 + Display pin — 承载 `compat_error_codes_doc_sync`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_gitlink`（(new) 门 G1-G6，共 25 个 `merge_gitlink_*` 用例：三消费者各一组「裁决拒绝 + 三侧一致放行」；四个写入边界（merge 冲突后 `--continue`、rebase 冲突重放、`merge --autostash` 前置拒绝、`reset --hard` 携带指针且 submodule 目录已检出）；两个**假拒绝**反例（fast-forward rebase 与 `cherry-pick --ff` 遇到「已移动」的指针必须放行）；`pull --rebase --autostash` 端到端前置拒绝（真实 git remote 造出携带 gitlink 的历史，验证 `pull` 在自己的 autostash push **之前**拒绝、脏改动原样保留、stash list 为空）；以及四个工作树过渡用例——指针路径上是**未跟踪普通文件**时必须在 HEAD 移动前拒绝并保住该文件、路径上是**已检出的 submodule 目录**时放行且目录不动、fast-forward **丢弃**指针时若目录非空则在 ref 移动前拒绝（保住目录且 HEAD 不动）、若只剩空占位则放行且 HEAD/索引/树三者一致）；以及三个 R5 补强用例——`restore --source` 不得删除指针路径上的未跟踪文件、洁净度闸门放行「不存在／目录」两种形态但仍拦住普通文件、多 pick 序列里靠后的 `EmptyCommit` 不得让靠前 pick 的分歧指针漏过前置 gate）；以及四个 restore 方向用例——索引那一格本身是 gitlink 时，落在该路径上的文件既不得被 `restore` 删除、也不得被 `reset --hard` 删除；source 丢弃该指针时 `restore` 必须拒绝而非删掉用户内容；`--overlay` 下 source 有目标的替换同样要拦）；以及 fast-forward **rebase** 同样必须在 ref 移动前拒绝（与 merge 对称）
- [ ] `source .env.test && cargo test --lib merge::`（merge.rs 内联单测回归 + (new) `ensure_gitlinks_not_arbitrated_*` / `split_gitlink_entries_*`，承载 G5）
- [ ] `source .env.test && cargo test --test compat_error_codes_doc_sync`（`docs/error-codes.md` 同步守卫，承载 G9；target 名 2026-09-04 按 `Cargo.toml:267` 核对，保持不变）

**Full-suite trigger:** `T-1`（开工核对后按 ER-02 重判：**不再**由「新增稳定错误码」触发——AC5 走既有码复用分支；改由 `src/command/status.rs` 的跨命令共享判定函数 `changes_to_be_staged_split_*` 触发，其消费者包含 `switch`/`merge`/`rebase`/`commit`/`stash` 等全部洁净度闸门。G7/G8 的「无 gitlink 场景零变化」由该全量回归承载）

**Dependencies:** 无（串行链首）

**Implementation write set:** `src/command/merge.rs`、`src/command/rebase.rs`、`src/command/cherry_pick.rs`（三消费者的校验接入）、`src/command/pull.rs`（`pull` / `pull --rebase` 的继承面：错误映射 + 前置 gate 入口）、`src/command/reset.rs`、`src/command/restore.rs`、`src/command/clone.rs`（新 `RestoreError` 变体的穷尽匹配）、`src/command/status.rs`、`src/utils/worktree.rs`、`docs/commands/pull.md`、`docs/commands/zh-CN/pull.md`、`docs/commands/restore.md`、`docs/commands/zh-CN/restore.md`、`docs/development/commands/restore.md`、`docs/error-codes.md`、`tests/command/merge_test.rs`、`COMPATIBILITY.md`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/commands/rebase.md`、`docs/commands/zh-CN/rebase.md`、`docs/commands/cherry-pick.md`、`docs/commands/zh-CN/cherry-pick.md`（rebase/cherry-pick 消费者的 fail-closed 继承面）、`docs/development/commands/merge.md`、`docs/development/commands/rebase.md`、`docs/development/commands/cherry-pick.md`、`docs/development/commands/_compatibility.md`（D24 拒绝项登记）

**开工核对后的写集偏离（ER-02，2026-09-04）：**

- **移出** `src/utils/error.rs`：AC5 的「语义相符的既有码复用」分支成立——`StableErrorCode::Unsupported`（`LBR-UNSUPPORTED-001`，「Operation declined because the requested mode is intentionally unsupported」）正是 ADR-MG-01 的永久非目标语义，无需新增枚举变体；因此本卡不命中 `Full-suite trigger` 的 T-1「新增稳定错误码」条件（G7/G8 的全量回归仍按门族要求执行）。`docs/error-codes.md` 仍在写集：该行的 category 列由 `repo` 订正为 `internal`（与 `src/utils/error.rs:2370` 的 pin 测试一致，属既存文档失真），并补记 gitlink 用例。
- **移出** `tests/command/rebase_test.rs`：rebase / cherry-pick 消费者的拒绝与放行判据与 merge 同源（同一 helper、同一措辞），集中在 `command::merge_test::merge_gitlink*` 一处更能证明 G5 的单一事实源；rebase 侧的索引登记行为另由 `--lib rebase::` 内联单测承载。
- **加入** `src/command/reset.rs`：merge/rebase 的索引重建都经 `reset::rebuild_index_from_tree`，其 gitlink 分支对 submodule 的 commit 对象调 `Blob::load` 会 panic（见 Current evidence）。pass-through 语义要求该路径存活，故改为以 size 0 直接登记指针。
- **`src/command/status.rs` 的跳过范围（R5 收窄，R6 修正错误分支）**：只跳过 Libra 预期的两种形态——路径**不存在**（未 materialize，Libra 的常态；仅 `NotFound`/`NotADirectory`）与路径是**目录**（用户自行检出的 submodule）。路径上是普通文件/符号链接时仍按 modified 报出，否则 `reset --hard` 等把新 gitlink 写进索引后，那个文件会变成「被 submodule 遮住」的隐形状态；其余 `symlink_metadata` 错误落回原有处理（报 deleted），不再被当成「未 materialize」而误判为干净。
- **`reset --hard` 的删除集合（R6）**：`tracked_paths_from_index` 过滤掉 `160000` 条目。Libra 从不 materialize submodule 工作树，因此该路径上的任何东西都不是它写的、也不该由它删除——无论是用户的 submodule 检出还是随手留下的文件（`cherry-pick --ff` 经 `reset_hard` 复用同一条路径）。`tracked_paths_from_commit` 走 `get_plain_items`，本就跳过 gitlink。
- **加入** `src/command/status.rs`：见 Current evidence 的开工实测——不修此处，「三树一致 pass-through」在真实 merge 路径上不可达（`ensure_clean_status` 先于本卡的 gate 拒绝），AC2/G4 无法验收。改动面为 `changes_to_be_staged_split_*` 两处循环跳过 gitlink 索引条目。
- **G1「任何写入前」的实现位置（R1 评审后修订）**：三个消费者的**引擎内** gate 都晚于各自入口的首次写入，因此每个消费者另加一道**前置 gate**：① merge 在 `run_merge_for_pull_with_options` 内、`--autostash`（stash 对象 + sidecar + 工作树 reset）之前，复用与引擎同一组 `merge_is_up_to_date` / `merge_is_fast_forward` 判定，只对真正会裁决的三路合并生效（GC-02）；② rebase 在 `prepare_rebase_aux` 之前；③ cherry-pick 在首个 pick 之前、以及 `resume_picks` 写 sequencer 行之前。②③ 是多步重放：每步的 ours 侧要等前一步落地才存在，无法逐步前置，故改用保守的**全序列一致**判据 `merge::ensure_gitlinks_uniform_across_inputs`（所有输入树对同一 gitlink 路径必须记录相同 oid），宁可提前整体拒绝也不半途停下。
- **加入** `docs/development/commands/_compatibility.md`：ADR-MG-01 的永久非目标需按该文件的规则分配 D 编号（新增 D24），`docs/error-codes.md` 的 `LBR-UNSUPPORTED-001` 行也指向此处。
- **加入** `src/command/restore.rs`（R4 评审后补，R5 修订）：索引承载 gitlink 之后，`restore` 的目录过渡 preflight 会把已检出的 submodule 目录判为待替换（`will_replace` 因 `index.tracked()` 成立），于是 fast-forward 在**移动 HEAD 之后**才以 `LBR-IO-002` 失败、refs 与索引/工作树不一致。R4 时曾改为在 restore 内跳过 gitlink，但那会推翻 restore 既有且**刻意**的契约（空占位目录清除、非空目录在改动任何其它路径前拒绝，`command::restore_test::test_restore_{removes_empty_materialized_gitlink_directory,refuses_nonempty_gitlink_before_mutating_other_paths}` 钉住）——已回退。最终改动新增只读入口 `restore::preflight_worktree_restore_to_commit`，由 `apply_fast_forward_merge` 在 ref 更新**之前**调用，把 fast-forward 变回全有全无。restore 自身另补一条缺口（R5，R6/R7/R8 逐轮收紧）：`160000` 路径上的**两个方向**都会毁掉 Libra 没写过的东西——materialize 指针时 `restore_target_to_file_typed` 会 `remove_file` 掉恰好落在那里的普通文件再建目录；source 丢弃或替换该指针时又会删除/覆盖那里的内容。合为一条规则，由共用 helper `ensure_submodule_path_is_replaceable` 判据、以 `RestoreError::SubmodulePathNotOwned`（`LBR-CONFLICT-002`）在任何写入前拒绝：只有索引把该路径记为**普通 tracked blob**（内容可从对象库取回）、或工作树那一格是目录（空占位可删，非空由既有 `NonEmptyWorktreeDirectory` 拦）才放行；未跟踪文件、以及索引里那一格**本身就是 gitlink** 的情形都拒绝（`Index::tracked` 不看 mode，只用它会把后者误判为安全）。触发条件为「目标是 gitlink」或「索引是 gitlink 且该 restore 会动它」——`--overlay` 只抑制「source 缺失即删除」，source 有目标的替换仍要拦。source/index 与 `--ours`/`--theirs` 两条预检共用该 helper；`--merge` 渲染的是冲突标记**文件**、不建目录占位，不适用。`src/command/clone.rs` 因该 error 枚举穷尽匹配同步一行映射。
- **rebase fast-forward 的顺序（R9 补）**：`run_rebase_start` 的快进分支同样先动 ref/索引再 materialize 工作树，而本卡让 gitlink 仓库第一次能走到那里（改动前 `rebuild_index_from_tree` 直接拒绝 gitlink），因此与 merge 同样在 ref 更新前调用 `restore::preflight_worktree_restore_to_commit`。
- **加入** `src/utils/worktree.rs`（R3 评审后补）：`untracked_overwrite_path` 是 merge / rebase / cherry-pick / switch 共用的「未跟踪文件会被覆盖」判定（`reset --hard` 不走它——它本就不做未跟踪覆盖检查）。Libra 从不 materialize submodule 工作树，所以 `160000` 索引条目不可能覆盖任何东西；不排除它，用户**自行检出**的 submodule 目录里的文件（对 Libra 是 untracked）就会与该 gitlink 路径判定冲突，让含 submodule 的仓库连 `reset --hard` 都跑不完。改在单一事实源处收口，而非在三个调用点各写一遍（GC-02）。
- **加入** `src/command/pull.rs` 与 `docs/commands/pull.md` EN+zh（R2 评审后补）：`pull` 复用 merge 引擎（需要 `PullMergeError::GitlinkUnsupported` 的错误映射），`pull --rebase` 走 `rebase::run_rebase_for_pull` 这条**独立入口**且自带 autostash，必须同样在写入前挂前置 gate；两条路径都是本卡语义的用户可见继承面，按 Docs 默认逐份列明。

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Rollback mode:** `immutable-release`（降级指引见字段全局默认）

**Estimated scope:** `M`（2 落点：`src/command/`、`src/utils/`；9 生产文件：merge.rs、rebase.rs、cherry_pick.rs、pull.rs、reset.rs、restore.rs、clone.rs、status.rs、utils/worktree.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=gitlink fail-closed; recovery=前滚修复并发布新 patch（immutable-release）; complete=yes; self-contained=yes; AC=9/8@EX-01; VER=3/8; landing=2; prod-files=9; scope=M; deps=none; writeset=序列化于链首; release=independent; split-from=N/A; exception=EX-01`

### Task MG-01: merge base 染色算法升级

**Task type:** `implementation`

**Lifecycle / Acceptance:** `released` / `complete`（A/B 组全绿；Codex R4 `PASS`，R1-R3 共 9 项 P1 逐项关闭；C 组 `v0.22.12` 发布完成 → D 组三项证据取得：① release.yml run `33869917757`（`headSha=65ed48d`，与 tag `v0.22.12` 一致）三个硬判据 job 全 `success`——`build-and-upload` 四个矩阵腿（darwin-arm64 / windows-amd64 / linux-arm64 / linux-amd64）、`upload-install-scripts`、`request-stable-manifest`；② CDN 可获取：`https://download.libra.tools/libra/releases/v0.22.12/libra-darwin-arm64` 返回 200，且 `install.sh` 已服务 `DEFAULT_VERSION="v0.22.12"`；③ stable manifest（`/libra/releases/stable/manifest-v1.json`）解出 `channel=stable, version=0.22.12, control_revision=6, published_at=2026-09-04T12:24:14.000Z`，四个平台 artifact 齐全，与本卡 tag 一致。）

**Description:** 将 `internal/merge_base.rs` 的全量 BFS 交集算法升级为 Git `paint_down_to_common` 同构的染色算法：按 committer date 排序的优先级队列向下染色，双侧命中即标记 RESULT。**终止判据为保守全量染色**：不实现依赖 generation 排序的提前返回（Git 的 side-exhaustion / single-result 早退以 generation 拓扑序为前提，`commit-reach.c:194` 注释；本仓库当前只有 commit-graph writer、无 reader，见事实基线），性能收益来自单次双染色遍历 + RESULT 标记替代「两次全量遍历 + 交集集合运算 + 单独剔除严格祖先」。卡内写明相对旧 BFS 的复杂度分析（过程性义务，不计 AC）。既有调用方面：merge、merge-base 命令、`diff A...B`、rebase（清单在此登记）；`log A...B` 当前为独立实现（`log.rs` 自行求可达集交集，未消费 `internal::merge_base`），不属本卡调用面，其收敛随后续 log 计划登记。本卡唯一行为轴：merge base 计算性能。

**Out of scope:** recursive 虚拟祖先（由 MG-02 承接）；commit-graph generation 的消费与依赖 generation 的提前退出优化——「尚未排期，重启条件：commit-graph reader 基础设施落地（当前仓库只有 writer，见事实基线）且 profiling 证明保守终止不足」。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 现算法为 BFS 求全祖先交集再剔除严格祖先 | 开工前 `src/internal/merge_base.rs:72`（`merge_bases`）；模块开工时无内联测试模块。**改写后锚点**：`CommitSource` trait `:76`、`paint_down_to_common` `:195`、`remove_redundant` `:289`、`merge_bases` `:323`（R4 评审后按最终代码刷新；行号随后续卡改动会漂移，符号名是稳定锚） |
| Git 参照：`paint_down_to_common` 染色 + 优先队列（其提前退出依赖 generation 拓扑序，`commit-reach.c:194`；本卡不做，见 Out of scope） | git@`3cb9185f6` `commit-reach.c:187` |

**Acceptance criteria:**

- [ ] 新实现为冲突染色（paint-down）结构。
- [ ] 优先队列按 committer date 排序，终止判据保守（无 generation 依赖的提前返回）。
- [ ] skewed/乱序 committer date 输入的结果与旧实现一致（回归）。
- [ ] merge 调用面结果与旧实现一致。
- [ ] merge-base 命令调用面结果与旧实现一致。
- [ ] `diff A...B` 调用面结果与旧实现一致。
- [ ] rebase 调用面结果与旧实现一致。
- [ ] 对拍用例覆盖 criss-cross 多 base 集合。
- [ ] 合成历史 benchmark 数值断言通过（规模与阈值见 Performance budget）。
- [ ] 单次调用的驻留状态显著小于旧实现（frontier 有界 + 驻留对比断言）。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 冲突染色（paint-down）结构 — 承载 `merge_base::` 单测
- G2 committer-date 优先队列 + 保守终止 — 承载 `merge_base::` 单测
- G3 skewed/乱序日期回归 — 承载 `merge_base::` 单测
- G4 merge 调用面一致 — 承载 `command::merge_test::` 回归
- G5 merge-base 命令调用面一致 — 承载 `command::merge_base_test::` 回归
- G6 `diff A...B` 调用面一致 — 承载 `command::diff_test::` 回归（模块名开工时刷新）
- G7 rebase 调用面一致 — 承载 `command::rebase_test::` 回归
- G8 对拍覆盖 criss-cross 多 base 集合 — 承载 `merge_base::` 单测
- G9 benchmark 数值断言 — 承载 `merge_base::bench_merge_base_scaling`
- G10 frontier 有界 + 驻留对比 — 承载 `merge_base::` 单测（`paint_down_keeps_the_frontier_bounded`、`paint_down_frontier_tracks_width_not_depth`、`paint_down_holds_one_map_where_the_replaced_walk_held_several`）

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_base_test::`（门 G5：既有用例回归）
- [ ] `source .env.test && cargo test --lib merge_base::`（(new) 门 G1-G3、G8、G10：以**被替换的 BFS-交集实现为 oracle 逐例对拍** + 保守终止/skewed 与全等日期/frontier 单测；frontier 门由 `paint_down_keeps_the_frontier_bounded` 与 `paint_down_frontier_tracks_width_not_depth` 两例承载——后者证明队列规模只随历史**宽度**变化、与**深度**无关）
- [ ] `source .env.test && cargo test --lib merge_base::bench_merge_base_scaling`（(new) 门 G9 性能断言）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（门 G4 merge 路径回归）
- [ ] `source .env.test && cargo test --test command_test command::diff_test::`（门 G6 diff 回归；模块名 2026-09-04 核对为 `command::diff_test`，保持不变）
- [ ] `source .env.test && cargo test --test command_test command::rebase_test::`（门 G7 rebase 回归）
- [ ] `source .env.test && cargo test --test compat_mail_am_basic`（第五个调用面：开工 grep 实证 `src/command/am.rs:391` 也消费本模块的 `is_ancestor`（`am --abort`/`--skip` 的「分支已前进则拒绝硬重置」判定），卡内原调用面清单未列。该目标是**已存在**的 `[[test]]`（`Cargo.toml:430`），其 `--abort`/`--skip` 用例即走该判定——R1 评审指出「`rebase_test` 覆盖 `am`」的说法不成立，改挂真实承载命令）

**Full-suite trigger:** `T-1: GC-02 单一事实源（merge_base 被 merge/rebase/diff 共享；log 当前为独立实现、未消费 internal::merge_base，不在此列）`

**Dependencies:** MG-00（串行链前驱）

**Implementation write set:** `src/internal/merge_base.rs`、`COMPATIBILITY.md`、`docs/commands/merge-base.md`、`docs/commands/zh-CN/merge-base.md`、`docs/development/commands/merge-base.md`

**开工核对后的写集偏离（ER-02，2026-09-04）：**

- **极大化过滤也一并线性化（R2 评审后）**：`remove_redundant` 原本对**每个**候选各做一次全祖先遍历（候选集大时仍是二次），改为**一次多源遍历**——用全部候选的 parents 作种子，凡走到的候选即被支配；DAG 无环，故候选不可能被自己的 parents 走到，无需按源归属。单候选直接返回、不走图。
- **移出** `src/command/merge_base.rs`：octopus 变体（`get_octopus_merge_bases` 等价物）属 MG-12，本卡不动 CLI；`merge_bases`/`merge_base`/`is_ancestor` 三个公开签名与返回排序全部不变，调用面零改动。
- **移出** `tests/command/merge_base_test.rs`、`tests/command/merge_test.rs`：本卡不新增集成用例——G4-G7 是**既有**用例的回归门，新增判据（对拍、skewed 日期、frontier、benchmark）都要求注入合成图，只能落在模块内联单测（否则 10^4 提交要真写 10^4 个对象）。
- **移出** `docs/development/commands/merge.md`、**加入** `docs/development/commands/merge-base.md`：算法事实源在 merge-base 的开发文档（其「设计方案」逐条描述旧算法，且「还未实现的功能」里那条 O(common×E) 性能项正是本卡消除的），merge 开发文档不描述 merge base 算法。

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Performance budget:** ≥10^4 提交合成历史，merge base 计算优于旧 BFS（单次双染色遍历 + RESULT 标记替代两次全量遍历 + 交集集合运算 + 单独剔除严格祖先；提前退出为 commit-graph reader 落地后的后续优化，不进本卡判据）；单次调用 frontier 有界。

**AC10 措辞订正（R1 评审后，ER-02）：** 原文「单次调用**无全历史内存驻留**」比算法实际提供的更强，且与 Git 的 `paint_down_to_common` 结构矛盾——commit 一旦被访问就必须留在 paint map 里，因为后来的另一条路径仍可能给它加 flag。真实且可断言的是**对比性**改善：新实现持有**一份** paint map，而被替换的实现同时持有两份完整祖先集合 + 其交集，再加上「每个候选一份」的 visited 集合。AC10/G10 据此改写，并由 `paint_down_holds_one_map_where_the_replaced_walk_held_several` 实测断言（paint map ≤ 每 commit 一项，且不足旧实现驻留的一半）；队列侧的 frontier 有界性另由两例承载。

**判据形式（开工后按 ER-02 明确）：** 数值断言用**图读取次数**而非 wall-clock——wall-clock 在负载下会 flake，且 `cargo test --all` 本身就是并发负载。`bench_merge_base_scaling` 断言染色的读取数 ≤ `3 × (commits + edges)`——上界由算法本身给出（每个 commit 至多按它取到的 3 种 paint 值各入队一次，每次出队读自身 + **每个 parent 一次**），因此是**边敏感**的，不能只按 commit 数计（R2 评审指出这一点）。同一测试做三次测量：12,000 提交线性主干、R1 评审给出的反例（一侧 tip 日期高于整条主干、另一侧低于全部，优先队列先抽干一侧再看另一侧）、以及一个 500 路 octopus fan-out（边数主导 commit 数、且候选集为全部 500 个叶子），三者同受该上界约束，并在 200/400 两个小规模上实测被替换实现的读取数**超过 3 倍增长**（证实其对共享历史是超线性的，而非假设）。被替换实现在 10^4 规模上是分钟级，故不在该规模实跑——它作为 oracle 只在小规模逐例对拍。

**Estimated scope:** `M`（1 落点：`src/internal/`；1 生产文件：internal/merge_base.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=merge base 算法性能; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=7/8; landing=1; prod-files=1; scope=M; deps=MG-00; writeset=序列化于 MG-00; release=independent; split-from=N/A; exception=EX-01`

### Task MG-02: recursive 虚拟祖先（多 merge-base 合并）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `released` / `complete`（A 组（Codex R6 修订后复跑）：`--lib merge::recursive` 19/19、`--lib merge::` 39/39、`command::merge_test::` 90/90（含 12 个 `merge_crisscross*` 新用例）、`command::maintenance_test::` 40/40、`command::pull_test::` 30/30、`compat_ledger_schema` 43/43、`compat_matrix_alignment` 9/9、`compat_command_docs_examples_section` 1/1、`compat_version_surface_sync` 2/2；B 组：`cargo +nightly fmt --all --check` 与 `cargo clippy --all-targets --all-features -- -D warnings` 零输出；ER-05：Codex R7 `PASS`（R1–R6 共 15 项 P1 逐项关闭，1 项判定为分发层既有行为登记残留）；C 组 `v0.22.13` 发布完成（commit `5edba1d`，main 以合并提交 `e501b41` 推送——origin 另有一条只改 `plan-20260901.md` 的文档提交 `34727ce`，与本卡 16 个文件无交集，按「合并 origin/main 后推送」处理；`gh release create` 当时被自动模式分类器拒绝，非 D 组判据；2026-09-05 分类器放行后已补建 v0.22.11/12/13 三个 GitHub release）→ D 组三项证据取得：① release.yml run `33898257470`（`headSha=5edba1d`，与 tag `v0.22.13` 一致）三个硬判据 job 全 `success`——`build-and-upload` 四个矩阵腿（darwin-arm64 / windows-amd64 / linux-arm64 / linux-amd64）、`upload-install-scripts`、`request-stable-manifest`；② CDN 可获取：`https://download.libra.tools/libra/releases/v0.22.13/libra-darwin-arm64` 返回 200，且 `install.sh` 已服务 `DEFAULT_VERSION="v0.22.13"`；③ stable manifest（`/libra/releases/stable/manifest-v1.json`）解出 `channel=stable, version=0.22.13, control_revision=7, published_at=2026-09-04T17:43:05.000Z`，四个平台 artifact 齐全，与本卡 tag 一致。）

**Description:** criss-cross 等多 merge-base 场景下，按 Git recursive 语义（`merge-ort.c:5313`）递归合并所有 merge base 生成虚拟祖先，再以虚拟祖先为 base 做最终三路合并；虚拟祖先的 tree/合成 commit 作为一次性 loose object 落对象库（不写入 merge-state.json、不纳入 GC root，可被 gc 回收，恢复走 `--restart` 重算，ADR-MG-04）。本卡唯一行为轴：多 base 合并语义。

**折叠内部的判定按 git@`3cb9185f6` 逐条对齐**（开工后按 Codex R1 修订，实现细节见 `docs/development/commands/merge.md`）：`-X ours/theirs` 在 `call_depth > 0` 关闭；change/delete 取 base；两侧类型不同或同为符号链接取 base 条目（无 base 即该路径在祖先中不存在，`merge-ort.c` `handle_content_merge`）；任一侧二进制（`ll_xdl_merge` 判据：`buffer_is_binary` 前 8000 字节含 NUL，或输入超过 `MAX_XDIFF_SIZE` 1023 MiB）取 base **内容**、无 base 取**空 blob**（`merge-ll.c` `ll_binary_merge` 在 `virtual_ancestor` 下 steal `orig`）；其余按 `merge.conflictStyle` 渲染带标记的内容，标记宽度 `7 + 2 × depth`（`extra_marker_size = call_depth * 2`），标签用 Git 的 `Temporary merge branch 1/2`；mode 取 `handle_content_merge` 的规则。**与 Git 的五项有意差异**（用户文档 EN+zh、`COMPATIBILITY.md`、开发文档同步登记）：折叠顺序固定 hex 升序、递归深度上限 20、同层 base 数上限 32（Codex R3：折叠工作量随宽度平方增长，`internal::merge_base` 仅暴露逐次 API 且本卡只消费不改，故以宽度上限给出确定性工作量界）、多 base 时 `merge.conflictStyle` 提前解析、合成 commit 的 parents 为「全部已折叠真实 base」（可达性与 Git 链式虚拟提交等价）。

**Out of scope:** 虚拟祖先内的 rename/driver 增强（随 MG-05/MG-08 自然继承，非本卡验收）；`merge-base --all` 输出变化（无变化，MG-01 已固定集合语义）；CLI 分发层在任何命令前的仓库级例行维护（`src/cli.rs` 仓库 preflight：schema 自动升级、object-index 修复、`maintenance.lock`）不纳入 `--dry-run` 零写入契约（Codex R4 判定为分发层既有行为，先于本卡且 `src/cli.rs` 为 T-1 表面；已在 `docs/development/commands/merge.md` 登记为残留，用户文档写明契约范围）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| merge 只取单个 base（hex 最小者，MG-02 前的 `lca_commit`） | 现为 `src/command/merge.rs:2829`（`merge_base_commits`，返回全集） |
| Git 参照：call_depth 递归 + "Temporary merge branch" 标签 + `make_virtual_commit` | git@`3cb9185f6` `merge-ort.c:5313`、`merge-ort.c:5429` |

**Acceptance criteria:**

- [ ] 多 merge base 时递归两两折叠为单一虚拟祖先。
- [ ] 折叠顺序确定（按 base hex 排序）。
- [ ] 递归深度有上限。
- [ ] 超限时报显式错误（禁止栈溢出）。
- [ ] 嵌套冲突 marker 长度随 depth 加长（对齐 Git `conflict_marker_size + depth` 规则）。
- [ ] criss-cross 构造用例的最终合并结果与 Git 语义一致（用例注释标注 git 出处，GC-MG-15）。
- [ ] 虚拟 tree/commit 为一次性对象：不写入 merge-state.json、不纳入 GC root。
- [ ] 合并中断后 `maintenance gc` 回收不产生悬空引用错误（故障注入用例）。
- [ ] gc 后 `--restart` 重算虚拟祖先恢复合并（故障注入用例）。
- [ ] merge-state.json schema 演进向后兼容（GC-MG-14）：recursive 不新增破坏字段。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 递归两两折叠为单一虚拟祖先 — 承载 `merge::recursive` 单测
- G2 折叠顺序按 base hex 确定 — 承载 `merge::recursive` 单测
- G3 递归深度上限 — 承载 `merge::recursive` 单测
- G4 超限报显式错误（禁止栈溢出） — 承载 `merge::recursive` 单测
- G5 marker 长度随 depth 加长 — 承载 `merge::recursive` 单测
- G6 criss-cross 结果与 Git 语义一致 — 承载 `merge_crisscross`
- G7 虚拟对象一次性（不写入 merge-state.json、不纳入 GC root） — 承载 `merge_crisscross`（state 字段断言场景）
- G8 gc 回收不产生悬空引用错误 — 承载 `merge_crisscross`（gc 场景）+ `command::maintenance_test::`
- G9 gc 后 `--restart` 重算恢复 — 承载 `merge_crisscross`（--restart 场景）
- G10 merge-state 旧 schema 可读 — 承载 `merge_crisscross`（旧 schema 场景）

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_crisscross`（(new) criss-cross 端到端：无冲突自动合并 / 冲突路径 / 单 base 零变化 / `--allow-unrelated-histories` 零变化 / `--restart` 重算覆盖 recursive / gc 回收虚拟祖先后 `--restart` 恢复（按 object id 逐个断言「被回收」与「被重算回来」）/ merge-state 旧 schema 可读 / `--dry-run` 零对象写入 / **三 merge base**（两步折叠 + `--restart` 重算一致）/ **嵌套标记宽于外层**（`merge.conflictStyle=diff3` 下同一文件内同时可见 9 字符嵌套标记与更宽的外层标记）——场景矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib merge::recursive`（(new) 折叠顺序、生产入口 `fold_merge_bases` 的 depth 上限、marker 宽度、change/delete 与二进制/符号链接/混合类型回退、Git 二进制判据、非 UTF-8 文本逐字节保真、clean 折叠内容的可寻址性、state 不记录虚拟 base）
- [ ] `source .env.test && cargo test --test command_test command::maintenance_test::`（GC 回归：中断后 gc 无悬空引用/解析错误，虚拟祖先回收不破坏既有 GC 语义；模块名开工时按 tests/command/mod.rs 刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-00（gitlink 校验先行）、MG-01（merge base 集合接口 + 串行链前驱）

**Implementation write set:** `src/command/merge.rs`（多 base 接口自 MG-01 已就绪，本卡仅消费不修改 `src/internal/merge_base.rs`，故该共享文件不入写集）、`src/command/pull.rs`（**ER-02 写集偏离，开工后记录**：`PullMergeError` 新增 `VirtualAncestorTooDeep` 变体使 `pull::map_merge_error_to_cli` 的 match 非穷尽，必须补一条错误映射臂——纯映射、无逻辑，落点仍是 `src/command/`，`prod-files` 因此 1→2，`landing` 仍为 1，`scope` 仍为 `M`；未引入任何 T-1 表面，`Full-suite trigger` 保持 `none`）、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/commands/pull.md`、`docs/commands/zh-CN/pull.md`（**ER-02 写集偏离，Codex R2 判定**：pull 的 merge 阶段经共享 wrapper 继承递归虚拟祖先与 `VirtualAncestorTooDeep`→`LBR-UNSUPPORTED-001`，两份用户文档的「single-head three-way」表述与错误表须同步；仅文档，不改计数）、`docs/development/commands/merge.md`、`COMPATIBILITY.md`（merge 行 + pull 行的继承说明）

**开工核对后的写集内修复（ER-02，Codex R2 判定，同在 `src/command/merge.rs`）：** `--dry-run` 原本仍会执行 `run_merge_for_pull_with_options` 顶部的陈旧 autostash sidecar 回收（提升入 stash list 并删文件）与合并后的 autostash finalize，两者都是写入、与既有「零写入」契约矛盾；该缺口先于本卡存在，但本卡的 `--dry-run` 零写入断言与用户文档措辞直接依赖该契约，故在写集内修复：`dry_run` 下跳过陈旧 sidecar 快照与 `resolve_pending_autostash`，`autostash` 结果为 `None`。回归：`merge_crisscross_dry_run_leaves_a_stale_autostash_sidecar_untouched`（sidecar 字节不变、stash list 与对象数不变）。

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点 `src/command/`；2 生产文件：merge.rs + pull.rs 的错误映射臂，见写集的 ER-02 偏离记录）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=recursive 虚拟祖先; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=4/8; landing=1; prod-files=2; scope=M; deps=MG-00,MG-01; writeset=序列化于 MG-01; release=independent; split-from=N/A; exception=EX-01`

### Task MG-03: 增量树合并（目录剪枝 + trivial 快速路径）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `released` / `complete`（A 组（Codex R11 修订后复跑）：`--lib merge::tree` 8/8、`--lib merge::` 47/47、`command::merge_test::` 106/106（默认增量路径）与 106/106（`LIBRA_TEST_MERGE_TREE_WALK=flat` 拍平路径复跑），含 (new) `merge_tree_walk_conflicted_dry_run_matches_the_real_conflict_path_under_a_dangling_replace` / `merge_tree_walk_preserves_a_mode_only_change_on_both_walks` / `merge_tree_walk_preserves_a_symlink_change_on_both_walks` / `merge_tree_walk_preserves_an_added_executable_on_both_walks`（G6–G8 的 CLI 承载，双 walk）/ `merge_tree_walk_dry_run_follows_nested_replacements_like_the_checkout` / `merge_tree_walk_dry_run_matches_the_real_verdict_on_a_missing_shared_tree` / `merge_gitlink_nested_changed_pointer_inside_a_pruned_subtree_is_refused` / `merge_gitlink_nested_identical_pointer_inside_a_pruned_subtree_passes_through`（G9/G10 的 CLI 承载）/ `merge_tree_walk_flat_switch_matches_the_incremental_default` / `merge_tree_walk_default_is_incremental_and_reads_only_changed_paths` / `merge_tree_walk_refuses_untracked_ancestor_of_an_adopted_subtree` / `merge_tree_walk_rechecks_hook_created_files_under_adopted_subtrees` / `merge_tree_walk_refuses_an_added_subtree_with_a_missing_nested_tree` / `merge_tree_walk_root_tree_replacement_is_honoured_identically_by_both_walks` / `merge_tree_walk_missing_shared_tree_fails_before_head_moves` / `merge_tree_walk_conflict_stages_follow_the_replaced_root_on_both_walks`；`command::merge_test::merge_gitlink*` 全部经剪枝路径；`command::pull_test::` 30/30；B 组：`cargo +nightly fmt --all --check` 与 `cargo clippy --all-targets --all-features -- -D warnings` 零输出。ER-05：Codex R12 `PASS`（R1–R11 共 17 项 P1 逐项关闭，其中 2 项判定为本卡外既有残留登记——CLI 分发层不在 `--dry-run` 契约内的例行维护、merge 检出写入器不应用可执行位/符号链接目标）；C 组 `v0.22.14` 发布完成（commit `0d32b76`，fast-forward 推送；`gh release create v0.22.14` 已建）→ D 组三项证据取得（2026-09-05）：① release.yml run `33931417437`（`headSha=0d32b76`，与 tag `v0.22.14` 一致）全部 8 个 job `success`——硬判据 `build-and-upload` 四个矩阵腿（darwin-arm64 / windows-amd64 / linux-arm64 / linux-amd64）、`upload-install-scripts`、`request-stable-manifest`，另 `update-homebrew-tap` / `verify-homebrew-formula` 亦 `success`；② CDN 可获取：`https://download.libra.tools/libra/releases/v0.22.14/libra-darwin-arm64`、`libra-linux-amd64`、`libra-windows-amd64.exe` 均返回 200，且 `install.sh` 已服务 `DEFAULT_VERSION="v0.22.14"`；③ stable manifest（`/libra/releases/stable/manifest-v1.json`，schema 1，1 个签名）解出 `channel=stable, version=0.22.14, control_revision=8, published_at=2026-09-05T00:37:01.000Z`，4 个平台 artifact，与本卡 tag 一致。）

**开工核对后的写集偏离与语义精化（ER-02）：** ① 事实基线锚点 `merge.rs:2666` 已漂移——拍平入口现为 `commit_tree_split`（`src/command/merge.rs`，经 `src/utils/object_ext.rs:111` `TreeExt::get_plain_items_with_mode`）；② G1/G2 的「不读取」按可实现语义精化为：三侧一致的子树**零读取**；「一侧等于 base」的子树不作为整体读取，只在另一侧不同的路径上逐层打开一次（供 `files_changed` 计数与 ADR-MG-01 隐藏 gitlink 扫描合成的**一次**剪枝 diff）——因为 JSON `files_changed` 与 MG-00 的裁决判定都需要知道采纳子树相对 ours 改了什么，而这只能靠沿差异路径比对得到；benchmark 断言按此界写；③ 多 base（MG-02 递归虚拟祖先）保留拍平路径：构造虚拟祖先时已读全三侧，本卡不改；④ 冲突路径需逐文件索引与工作树写入，采纳子树在此展开（只读被采纳侧）；干净路径原样写入采纳子树；⑤ 「trivial 快速路径」在本引擎中即「一侧等于 base 的目录/文件按 id 直接采纳、不做内容合并」，已含于剪枝判定，不另设开关；⑥（Codex R1）ADR-MG-01 裁决改为**独立只读 gate 先行**（`incremental_gitlink_gate`）：与拍平规则逐点等价、覆盖全部快捷臂、先于任何 blob 落盘，`preflight_merge_gitlinks` 单/无 base 同样改用；⑦ G3 精度为「逐叶判定相同」，原样采纳的子树内空 tree 得以保留（Git 同义）登记为已知差异；⑧ 生产入口证据 seam `LIBRA_TEST_MERGE_TREE_STATS` 供端到端断言默认路径与读取数；⑩（Codex R8/R9）`--dry-run` 以只读探测读携带子树的 tree，且以**检出的视角**（应用嵌套 `refs/replace`）读取，使预演结论与真实合并一致（拍平预演本就读遍）；未跟踪碰撞检查只展开结果引入的子树；stats seam 在引擎出口写出、计读取尝试；⑪（Codex R9）G6–G8 补 CLI 承载（mode-only / symlink / 新增可执行，双 walk）；Performance budget 改为可度量的界。⑨（Codex R3→R5）不做「读遍采纳子树」的写前校验（那会把读取量退回仓库规模）：以「walk 未打开的 tree 必已被 HEAD 引用」不变量替代（`unopened_trees_are_heads_own` 钉住）；**可用性**由干净路径的写入顺序保证——检出（本就要读遍结果树）提前到 commit/HEAD 之前，缺失对象在 HEAD/索引/工作树/merge-state 任何一项写入前失败（此前落盘的仅是内容寻址的结果 tree 与自动合并 blob，失败时无引用、可被 gc 回收，与拍平路径同时点写出的对象相同），崩溃窗口变为「结果已暂存、HEAD 仍旧」（文档写明，Git 同序）；`refs/replace` 口径与拍平路径逐点对齐（根 tree 经 `replace::resolve`，嵌套 tree 用 `load_object_raw`，冲突 stage 亦用解析后的根）；stats seam 统计 preflight 与引擎两遍的总读取；冲突路径展开全部携带子树（O(全树)，与拍平相同）如实写明。

**Description:** 树合并从「三棵树全量拍平为 HashMap」升级为按目录递归的三路遍历：一侧与 base 相同的子树整体剪枝直接采纳（Git `collect_merge_info_callback` 的延迟遍历语义），三方一致的目录不展开；另加 trivial 快速路径（仅一侧变化的文件集合可判定时跳过内容级合并）。本卡唯一行为轴：树合并性能（行为不变重构 + 复杂度下降）。

**Out of scope:** rename 检测接入（由 MG-05 承接）；遍历顺序输出的人读消息文案变化（允许变化但非目标，既有测试钉住的文案不回归）；merge 检出写入器不应用可执行位 / 不重写符号链接目标（tree 与索引正确、`checkout` 正确；先于本卡存在且两种 walk 相同——G6–G8 的 CLI 承载据此断言 tree/index mode 与双 walk 工作树一致，残留登记于 `docs/development/commands/merge.md`，属检出写入器轴，另行立卡）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 全量拍平：`commit_tree_split`/`commit_tree_items` + `get_plain_items_with_mode` | `src/command/merge.rs` `fn commit_tree_split`（符号为准；本卡收口时位于 `:3181`，开工核对：原 `:2666` 已漂移，行号随本卡编辑继续漂移） |
| Git 参照：`traverse_trees` + filemask/dirmask + `possible_trivial_merges` | git@`3cb9185f6` `merge-ort.c:1259`、`tree-walk.c` |

**Acceptance criteria:**

- [ ] 一侧等于 base 的子树不读取其内部 blob/tree 对象（对象读取计数断言）。
- [ ] 新遍历路径与拍平路径的 resolution 集合对拍一致（对拍中可用 env 开关强制旧路径；生产默认新路径）。
- [ ] 合成大树 benchmark 数值断言通过（规模与阈值见 Performance budget）。
- [ ] mode/symlink/executable 变化的既有行为不回归。
- [ ] gitlink 两档语义（ADR-MG-01）在剪枝路径成立：裁决触及的 gitlink fail-closed，三树一致的 gitlink 子树剪枝传递不触发。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 剪枝子树不读取其内部 blob 对象 — 承载 `merge::tree` 单测
- G2 剪枝子树不读取其内部 tree 对象 — 承载 `merge::tree` 单测
- G3 新旧路径 resolution 集合对拍一致 — 承载 `merge::tree` 单测
- G4 对拍可用 env 开关强制旧路径 — 承载 `merge::tree` 单测
- G5 合成大树 benchmark 数值断言 — 承载 `merge::tree` 单测
- G6 mode 变化既有行为不回归 — 承载 `command::merge_test::` 回归
- G7 symlink 既有行为不回归 — 承载 `command::merge_test::` 回归
- G8 executable 既有行为不回归 — 承载 `command::merge_test::` 回归
- G9 裁决触及的 gitlink fail-closed — 承载 `merge_gitlink`（剪枝路径场景）
- G10 三树一致 gitlink 子树剪枝传递不触发 — 承载 `merge_gitlink`（剪枝路径场景）

**Verification:**

- [ ] `source .env.test && cargo test --lib merge::tree`（(new) 门 G1-G5 单测）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_gitlink`（MG-00 用例在剪枝路径回归）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）
- [ ] `source .env.test && cargo test --lib merge::`（内联单测回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-00（gitlink 两档语义）、MG-02（串行链前驱）

**Implementation write set:** `src/command/merge.rs`（遍历与拍平并存一版本：拍平路径保留承载对拍门 G3/G4 的旧路径实现，其下线随 DEFER-05 模块拆分登记，不在本卡删除）、`tests/command/merge_test.rs`、`docs/development/commands/merge.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Performance budget:** ≥10^5 文件 / 1% 变化的合成树：合并判定的 tree 读取数 ≤ Σ(改动路径深度 × 不同侧数)，preflight gate 与引擎各一遍（各自缓存）——即 O(改动路径 × 深度 × 侧数) × 2，不再 O(全树)（开工后按 Codex R9 精确化；检出与冲突路径仍为 O(全树)）；benchmark 断言见 Verification。

**Estimated scope:** `M`（1 落点 `src/command/`；1 生产文件：merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=树合并性能; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-00,MG-02; writeset=序列化于 MG-02; release=independent; split-from=N/A; exception=EX-01`

### Task MG-04: D/F（目录/文件）冲突处理

**Task type:** `implementation`

**Lifecycle / Acceptance:** `released` / `remote-pending`（A 组（Codex R10 修订后复跑，`--test-threads=4`——本机高并发下 `command_test` 会在 serial lane 上死锁，属既有 flake 类）：`--lib merge::` 57/57（含 (new) `merge::df` 8/8：`unique_df_path` 后缀、占用名含祖先目录与已删除输入、`directory_is_in_the_way` 三形态判据、携带子树按需读取、modify/delete 换落点、`-X` 偏袒下仍冲突、4000 候选线性、折叠标签与占用；`merge::tree` 9/9 含 (new) `an_empty_subtree_is_in_the_way_only_when_the_base_had_nothing_there`）、`command::merge_test::` 141/141（默认增量路径）与 141/141（`LIBRA_TEST_MERGE_TREE_WALK=flat` 拍平路径复跑），含 (new) `merge_df_conflict_file_on_ours_is_moved_to_head_suffix`（G1/G3/G5/G6，两行 Git 消息 + 原样内容）/ `merge_df_conflict_file_on_theirs_is_moved_to_branch_suffix`（G2/G4/G6）/ `merge_df_conflict_added_file_is_moved_with_only_its_own_stage`/ `merge_df_conflict_abort_restores_the_file_and_removes_the_moved_copy`（G7/G8）/ `merge_df_conflict_occupied_name_and_nested_directory_restart_and_abort`/ `merge_df_conflict_deleted_input_path_still_occupies_its_name`（双 walk）/ `merge_df_conflict_empty_subtree_follows_gits_base_presence_rule`（双形态双 walk，git 实测判据）/ `merge_add_add_conflict_over_an_empty_base_directory_has_no_stage_1`（双 walk）/ `merge_empty_directory_turning_into_a_file_counts_one_change`（双 walk）/ `merge_df_conflict_strategy_option_keeps_the_modify_delete_conflict`（`-X ours`/`-X theirs`）/ `merge_df_conflict_tracked_symlink_gives_way_to_a_directory`（双 walk，落点是真符号链接）/ `merge_dangling_tracked_symlink_gives_way_to_a_directory_cleanly`（双 walk，干净路径）/ `merge_df_conflict_refused_at_the_moved_name_announces_nothing` / `merge_refuses_to_replace_a_directory_holding_ignored_content`（双 walk）/ `merge_refuses_to_unlink_a_tracked_file_through_an_ignored_symlinked_directory`（双 walk）/ `merge_refuses_to_write_through_an_ignored_symlinked_directory`（双 walk）/ `merge_refuses_a_hook_planted_ignored_symlink_before_head_moves`（双 walk × 两个 hook 检查点）/ `merge_df_conflict_paths_are_reported_in_path_order` / `merge_df_conflict_json_keeps_stdout_machine_clean` / `merge_df_conflict_continue_after_staging_the_moved_file` / `merge_df_conflict_is_identical_on_the_flat_walk`（双引擎 `--dry-run` 摘要逐字段对拍）/ `merge_df_conflict_is_not_raised_when_the_directory_merges_to_nothing` / `merge_df_unchanged_file_replaced_by_a_directory_merges_cleanly` / `merge_dry_run_reports_a_df_conflict_with_its_kind`（G9/G10）/ `merge_dry_run_reports_content_conflict_kind` / `merge_crisscross_df_collision_inside_the_fold_moves_the_file_like_git`（折叠内部 + 仅 root 有过的临时分支名占位，ER-02 ⑦/㉕）；G11 回归 = 上述 141/141 双 walk；`command::pull_test::` 31/31（含 (new) `test_pull_df_conflict_json_keeps_stdout_clean`）；共用工作树写入器与 gitlink 面的 `command::rebase_test::` 70/70、`command::cherry_pick_test::` 54/54、`command::worktree_isolation_test::` 119/119；compat 守卫（ledger schema 43 / matrix alignment 9 / subface labels 4 / pull flags 5 / docs examples 1 / error codes 2 / version surface 2 / serial registry 18）全绿；B 组：`cargo +nightly fmt --all --check` 与 `cargo clippy --all-targets --all-features -- -D warnings` 零输出；COMPATIBILITY 保持 383 行。ER-05：**Codex R11 `PASS`**（R1–R10 共 32 项 P1/P2 逐项关闭：R1 5、R2 6、R3 6+5、R4 3（其一经 git 实测判定为有意一致）、R5 2+2、R6 1、R7 2、R8 1+1、R9 1、R10 1，另经本地 6 lens × 3 refuter 对抗式预审补修 7 项）；C 组 `v0.22.15` 发布进行中 → 等 D 组远端证据）

**开工核对后的写集偏离与语义精化（ER-02）：** ① 事实基线锚点 `merge.rs:3045` 已漂移，且 MG-03 之后存在**两条**引擎（拍平 `merge_tree_items` 与增量 `incremental_merge_walk`）——D/F 判定做成两者共用的后处理 `resolve_df_conflicts`（拍平引擎按组件序单趟扫描出候选，增量引擎在 walk 的目录/文件分支登记候选），flat walk 对拍门随之成立；② 「目录挡路」按 Git 精确化：只有目录内容（已合并或冲突）在结果里**存活**时才移位，目录合并为空则文件留原地（`merge-ort.c:4100-4118`「directory no longer in the way」），两侧都删的文件不进候选（`filemask == 1`）；③ 落点名按 Git `unique_path`：`<path>~<branch>`，分支名 `/`→`_`，被本次合并任一路径占用时追加 `_0`、`_1`……；分支标签我方 `HEAD`、对方为被合并 ref 名（与冲突标记标签同源）；④ `--abort` 清理规则：`restore_pre_merge_state` 删除恢复后索引不跟踪的冲突路径（移位文件由此清理），不新增状态字段；⑤ JSON 为**加法**变更：`--dry-run` 摘要新增 `conflict_kinds`（`{path, kind, original_path?}`，空则不序列化，真实合并永不带），`conflicted_paths` 列落点名——merge-state schema 不变；⑥ 写集偏离：`pull` 经 `run_merge_for_pull_with_options` 共用引擎，`docs/commands/pull.md` EN+zh 与 COMPATIBILITY `pull` 行同步登记继承（同 MG-02 的先例）；⑦ 折叠内部（MG-02 虚拟祖先）同样存在 D/F——`merge_virtual_items` 末尾 `relocate_virtual_df_files` 按 Git `call_depth > 0` 语义把文件移到 `<path>~Temporary merge branch 1|2`、不登记冲突；否则虚拟祖先 tree 会在同一名字下带 blob 与子树并让外层合并误判（本卡发现并修复，属同一行为轴）；⑧ 与 Git 的差异登记两条（见 `docs/development/commands/merge.md`「D/F 冲突」）：未跟踪同名文件由 Libra 的写前检查拒绝整个合并（Git 在检出阶段报错），rename 引起的 D/F 不在本卡；⑨ 按 `git merge` 实测（git@`3cb9185f6`）把「文件侧修改过」精确化为 **modify/delete 冲突换落点**（种类不变、`foo~HEAD` 带 stage 1+2，`--dry-run` 报 `modify-delete`），「仅一侧新增」才是纯 `file-directory`（仅自己一侧 stage），「一侧未动」是干净删除无提示；⑩ 本卡实测暴露三处既有缺陷：`checkout` 不能在文件/目录间切换同一路径（`LBR-IO-001` / `LBR-REPO-003`）、已跟踪文件被目录替换后 `status`/`add -A` 失败（`LBR-REPO-002 Is a directory`）、merge 冲突写入器在 D/F 场景 `LBR-IO-002`——第三处由本卡修复（落点搬迁 + 冲突路径「先删后写」+ `write_workdir_file` 空目录接管），前两处属 checkout/status 轴登记于开发文档、夹具以 `root` 中转分支 + `libra rm` 绕行；⑪（Codex R1）`unique_path` 的占用集含祖先目录（`occupied_names`，已跟踪 `foo~HEAD/bar` ⇒ `foo~HEAD_0`，git 实测）；⑫（Codex R1）换落点的 modify/delete 同时打印 Git 的 modify/delete 行并把文件**原样**留在落点（此前套 Libra 标记，与「Version HEAD … left in tree」自相矛盾）；⑬（Codex R1）空目录挡路的两引擎一致：拍平引擎与折叠改用 merge 自有 flattener 保留空子树标记（只服务 D/F 判定，结果仍只含叶子），git 实测人工空 tree 仍搬迁；⑭（Codex R1）`announce_df_conflicts` 仅人读输出，`--json`/`--machine` 的真实合并/pull stdout 保持机器可读；⑮（Codex R1）`--abort`/`--restart` 清理腾空目录（`prune_empty_parents` / `remove_empty_dir_tree`），嵌套 `foo/a/` 不再挡住文件 `foo` 回位；⑯（Codex R2）落点决策统一提前到 `resolve_df_conflicts`（结果仍列出全部目录条目时；modify/delete 换落点也在此处变成 `FileDirectory { modify_delete: true }`），每条引擎只算一次 placements 并随 `MergeConflictInput` 传给写入器——增量引擎展开采纳子树后丢失空目录标记不再影响判定；⑰（Codex R2）占用集含**全部输入路径**（含两侧都删掉的）与祖先目录（`df_occupied_names` / `incremental_df_occupancy`），git 实测仅 base 有过的 `foo~HEAD` ⇒ `foo~HEAD_0`；⑱（Codex R2）空目录标记永远不进 stage（stage 取自 `FileDirectory` 自带条目，候选的 `base_file` 过滤标记）；⑲（Codex R2）工作树写入绝不穿过符号链接（`refuse_symlink_components[_for]` 写前预检 + 目标处符号链接被替换 + `remove_empty_dir_tree` 不跟随），被忽略的符号链接对未跟踪扫描不可见是既有事实；⑳（Codex R2）`-X ours/theirs` 下目录之下的 modify/delete 仍是冲突（git 实测），无目录的普通 modify/delete 沿用既有 `-X` 裁决并登记为先于本卡的策略选项轴差异；㉑（Codex R2）pull 文档 EN/zh 与 COMPATIBILITY pull 行改为「人读输出才打印该行」；㉒（Codex R3）冲突路径的删除集只取**当前索引跟踪**且结果不含的路径（不再并入 base/ours/theirs 的历史路径——重新出现的未跟踪 `foo~HEAD` 不是合并的东西）；㉓（Codex R3）符号链接预检改为 `refuse_symlink_traversal(writes, removals)`：写入与删除的路径祖先都检查，唯一放行的是**本次合并自己要删除的已跟踪符号链接**（`foo` 链接让位给 `foo/` 目录——删除先于写入），拍平引擎在两处 hook 复检点也重跑（hook 可植入被忽略的链接），增量引擎靠检出先于 HEAD 的顺序天然覆盖；㉔（Codex R3）普通冲突的回退 stage 查找过滤空目录标记（base 空目录 + 两侧各自新增 `foo` = add/add，无 stage 1）；㉕（Codex R3）折叠内部的搬迁占用集同样含三侧输入（仅 root 有过、被两条 base 都删掉的 `foo~Temporary merge branch N` ⇒ `_0`），交叉合并用例双标签覆盖；㉖（Codex R3）后处理用一次建好的 modify/delete 槽位表，候选数随树线性时不再二次方（4000 候选单测 + 时间预算）；㉗（Codex R3 P2）增量引擎在后处理搬走保留文件时修正 `files_changed`（ours 持有 +1 / theirs 新增 −1）、`count_item_map_changes` 把空目录标记视为不存在（空目录变文件计 1 个新增），双 walk 的 `--dry-run` 摘要逐字段对拍；㉘（本地对抗式预审，6 lens × 3 refuter）**「空目录挡路」的判据按 `git merge` 实测纠正**：只含空 tree 的目录仅在 merge base 于该路径**什么都没有**时才挡路（Git 对全新目录延迟遍历、整棵原样采纳），base 已有该路径时文件留在原地（普通 modify/delete）——判据抽成 `directory_is_in_the_way`（文件条目 / 空 tree + `base_present` / 携带子树用 `subtree_holds_a_file` 回调），`DfCandidate.base_present` 由两引擎各自提供，虚拟祖先不再剥离空目录标记；R1 的「空目录一律挡路」结论与其用例随之作废、改为双向双 walk 用例 `merge_df_conflict_empty_subtree_follows_gits_base_presence_rule`；㉙（预审）搬迁文件按原**类型**落盘（现由 `write_workdir_entry`：`120000` 落成真符号链接、`100755` 显式 `0755`；R7 后它是唯一工作树写入口），此前符号链接被写成含目标文本的普通文件；㉚（预审）冲突公告移到冲突状态**写成之后**——预检（未跟踪碰撞 / 符号链接 / 目录接管）可能拒绝合并，Git 在拒绝时什么都不打印；㉛（预审）删除与存在性判断改用 `symlink_metadata`（不跟随）：**悬空**的已跟踪符号链接此前不会被删除，随后写它下面的路径以 `File exists` 失败，而拍平引擎已写 commit 并移动 HEAD（HEAD/索引/工作树三者不一致）；㉜（预审）目录→文件的接管改为写前预检（`directory_content_outside`）：除本次合并自己要删的已跟踪文件外，目录里有任何东西（含被忽略的构建产物）即在改动前拒绝并指名挡路者；gitlink 路径不参与该预检（submodule 的检出不是合并的东西）；㉝（预审）冲突列表在算落点前按路径排序，消息 / `conflicted_paths` / `Would conflict in:` 与 Git 的排序输出一致；㉞（预审）落点占用集只在真的存在搬迁时才计算（`df_occupied_names_if_needed`），祖先符号链接检查带记忆化；㊱（Codex R4）搬迁的符号链接目标按原始字节写出（`OsStr::from_bytes`，非 UTF-8 目标此前在状态已写出后失败）、落点上的**空目录**由 `clear_write_target` 接管（符号链接分支此前 `remove_file` 目录报 `EISDIR`），双 walk 用例 `merge_df_conflict_moves_a_non_utf8_symlink_over_an_empty_directory`；㊺（Codex R10）符号链接写入分支同样先 `clear_ancestor_files`（此前只有文件分支做，写 `foo/link` 会以 `Not a directory` 失败），用例扩展为 theirs 同时含普通文件与符号链接；㊹（Codex R9）挡在要创建目录位置上的**被忽略**普通文件先被清掉再 `create_dir_all`（git 实测替换成目录；此前写入中途失败，拍平引擎此时 HEAD 已移动），(new) `merge_replaces_an_ignored_file_standing_where_a_directory_must_go`（双 walk，断言合并完成且 status 干净）；㊸（Codex R8）「不挡路」时清掉留在结果里的空子树条目（否则同一名字下会既有 blob 又有目录，且两引擎分叉），增量与折叠两处；㊷（Codex R7）R5 的「写前 unlink」让**类型与权限**不能再从磁盘继承：符号链接条目退化成含目标文本的普通文件、`100755` 退化成 umask 默认 → 全部工作树写入统一走 `write_workdir_entry(mode)`（`120000` 建链接、`100755` 显式 `0755`、其余 `0644`），(new) `merge_preserves_symlinks_and_executable_bits_when_it_writes`（双 walk）；**MG-03 登记的「检出写入器不应用可执行位/符号链接目标」残留由此消除**，开发文档同步改写；㊶（Codex R6）`flat_items_with_empty_dirs` 改为可失败（`Tree::try_load` + `TreeLoad`），`commit_gitlink_entries` 同走该路径——嵌套 tree 缺失时此前经 `TreeExt::load` 直接 panic（`fatal: CLI thread panicked`），现返回 `LBR-REPO-002` 并在 HEAD 未动前拒绝，(new) `merge_flat_walk_reports_a_missing_nested_tree_instead_of_panicking`；㊳（Codex R5）`--abort` 清理移位文件的存在性判断改用 `symlink_metadata`（`is_file()` 跟随链接，悬空/指向目录的移位符号链接会被永久遗留）；㊴（Codex R5，git 实测）工作树写入对已存在普通文件先 `remove_file` 再写（替换目录项），否则原地截断会破坏仓库外的硬链接别名并保留过期权限；㊵（Codex R5 P2）rustdoc 与开发文档的锚点 `paths_with_surviving_directories` → `directory_is_in_the_way`，卡内证据 133/133 → 135/135；㊲（Codex R4 提出、git 实测判定有意一致）落点上的**被忽略**文件被直接替换（`git merge` 同样如此；只有未跟踪且未忽略才拒绝），行为不改、文档写明「被忽略路径不受保护」并用 `merge_df_conflict_replaces_an_ignored_file_at_the_moved_name_like_git` 钉住；㉟（预审）登记既有缺口：`libra rm` 不接受未合并路径，因此「丢弃被移走的文件」目前只能靠 `--abort`（Git 用 `git rm foo~HEAD`），EN/zh 文档改为只承诺可行的解决路径，属 `rm` 命令面、另行立卡；Verification 计数 3/8→4/8、写集补 `tests/command/pull_test.rs`、modify/delete 分支锚点 `:4261`→`:4374`（以本地 `merge-ort.c` 副本核对）。

**Description:** 一侧 `foo` 为文件、另一侧 `foo/` 为目录（或反向）时，按 Git 语义（`merge-ort.c:4100-4198`）显式处理：目录保留原位，文件侧重命名为 `foo~<branch>` 写入结果。本卡唯一行为轴：D/F 冲突语义。

**Out of scope:** rename 导致的 D/F（rename 交互由 MG-06 承接）；工作区已有未跟踪 `foo` 挡路的情形（沿用既有 untracked-overwrite 拒绝框架，非本卡新增）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 拍平后 `foo` 与 `foo/bar` 为独立 key，无 D/F 检测 | `src/command/merge.rs` `fn merge_tree_items`（拍平引擎）与 `fn incremental_merge_walk` 的「一侧目录一侧文件」分支（增量引擎，MG-03 引入；两者都把二者当无关路径写进结果）——符号为准，计划成稿时的 `:3045` 已漂移 |
| Git 参照：`unique_path()` 改名 + `CONFLICT (file/directory)` | git@`3cb9185f6` `merge-ort.c:4100-4198` |

**Acceptance criteria:**

- [ ] file-vs-directory（双向）被检测为冲突，文件侧按 `~HEAD` / `~<branch>` 规则重命名落盘，人读输出含 `CONFLICT (file/directory)`。
- [ ] D/F 场景的 index stage 组合与 Git 对齐（用例注释注明出处，GC-MG-15）。
- [ ] `--abort` 完整恢复 D/F 冲突前的 index/worktree（含重命名文件清理）。
- [ ] `--dry-run` 对 D/F 冲突正确报告（exit 1 + JSON 含冲突类别）。
- [ ] 非 D/F 场景行为零变化（既有 `command::merge_test` 全绿）。

**判据规范（非计数正文，EX-01 门族，共 11 门）：**

- G1 ours 文件 vs theirs 目录被检测为冲突 — 承载 `merge_df_conflict`
- G2 ours 目录 vs theirs 文件被检测为冲突 — 承载 `merge_df_conflict`
- G3 文件侧按 `~HEAD` 重命名落盘 — 承载 `merge_df_conflict`
- G4 文件侧按 `~<branch>` 重命名落盘 — 承载 `merge_df_conflict`
- G5 人读输出含 `CONFLICT (file/directory)` — 承载 `merge_df_conflict`
- G6 stage 组合与 Git 对齐 — 承载 `merge_df_conflict`
- G7 `--abort` 恢复 index/worktree — 承载 `merge_df_conflict`
- G8 `--abort` 清理重命名文件 — 承载 `merge_df_conflict`
- G9 `--dry-run` exit 1 — 承载 `merge_dry_run`
- G10 `--dry-run` JSON 含冲突类别 — 承载 `merge_dry_run`
- G11 非 D/F 场景零变化 — 承载 `command::merge_test::` 回归

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_df_conflict`（(new) 门 G1-G8）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_dry_run`（--dry-run 既有用例 + (new) 门 G9-G10）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归；含 (new) `merge_crisscross_df_collision_inside_the_fold_moves_the_file_like_git`——折叠内部的 D/F，ER-02 ⑦）
- [ ] `source .env.test && cargo test --lib merge::df`（(new) `unique_df_path` 后缀规则、折叠标签、后处理只在目录存活时动作；Verification 由 3 条增为 4 条，粒度字段与审计表同步为 `VER=4/8`）

**Full-suite trigger:** `none`

**Dependencies:** MG-03（串行链前驱；基于增量遍历落地）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`（开工核对追加：`docs/commands/pull.md`、`docs/commands/zh-CN/pull.md`——pull 共用 merge 引擎，继承面须登记，ER-02 ⑥；Codex R1 追加：`tests/command/pull_test.rs`——pull 生产入口的 `--json` D/F 继承用例）

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=D/F 冲突语义; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=11/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-03; writeset=序列化于 MG-03; release=independent; split-from=N/A; exception=EX-01`

### Task MG-05: per-side rename 检测接入 merge

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** merge 对 base→ours 与 base→theirs 各执行一次 `rename_detect::match_pairs`（ADR-MG-03），将 rename 配对结果接入三路匹配：一侧 rename + 另一侧修改同一文件时，以（base 路径, 本侧新路径, 对侧路径）三元组做内容级合并，结果落在新路径。本卡唯一行为轴：rename 感知的合并决策（基础场景）。

**Out of scope:** rename/rename、rename/delete、rename/add 路径级冲突类型（由 MG-06 承接）；目录级 rename 推断（由 MG-07 承接）；rebase 的 rename 接入（由 MG-19 承接）；cherry-pick 的 rename 接入（DEFER-06）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| merge.rs 对 rename_detect 零引用（2026-09-03 grep 实证） | `src/command/merge.rs` 全文无 `rename_detect` 命中 |
| `rename_detect::match_pairs` 快照配对 API 与调用范式 | `src/command/rename_detect.rs:253`、`src/command/diff.rs:4104` |
| Git 参照：`detect_regular_renames` per-side + `process_renames` | git@`3cb9185f6` `merge-ort.c:3429`、`:2913` |

**Acceptance criteria:**

- [ ] 一侧 rename（纯改名）+ 另一侧修改内容：自动合并成功，结果在新路径。
- [ ] 一侧 rename + 另一侧修改且内容冲突：冲突呈现在新路径（marker 与 index stage 归属新路径，base stage 记录原路径内容，语义注释注明 git 出处）。
- [ ] rename 检测超预算时确定性降级为「无 rename」并输出 notice（不失败）。
- [ ] 双侧各自改名到不同路径时按「无 rename」确定性退化并报 notice（MG-06 升级为正式冲突）。
- [ ] 非 rename 场景行为零变化；`diff`/`status` 对 rename_detect 的既有消费面不回归。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 rename+修改自动合并成功 — 承载 `merge_rename`
- G2 合并结果落新路径 — 承载 `merge_rename`
- G3 rename+冲突 marker 落新路径文件 — 承载 `merge_rename`
- G4 index stage 归属新路径 — 承载 `merge_rename`
- G5 base stage 记录原路径内容 — 承载 `merge_rename`
- G6 预算超限确定性降级 notice — 承载 `merge::rename` 单测
- G7 双侧改名退化 notice — 承载 `merge_rename`
- G8 非 rename 场景零变化 — 承载 `command::merge_test::` 回归
- G9 diff 对 rename_detect 消费面不回归 — 承载 `rename_detect::` 单测回归
- G10 status 对 rename_detect 消费面不回归 — 承载 `rename_detect::` 单测回归

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_rename`（(new) 门 G1-G5、G7 + dry-run/squash/no-commit+continue/abort 组合——组合矩阵在本测试内逐场景断言）
- [ ] `source .env.test && cargo test --lib merge::rename`（(new) 门 G6 + 三元组重定向单测）
- [ ] `source .env.test && cargo test --lib rename_detect::`（门 G9-G10 复用面回归）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `T-1: GC-02 共享 helper（rename_detect 接口泛化影响 diff/status 消费面）`

**Dependencies:** MG-04（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`src/command/rename_detect.rs`（仅接口泛化，无算法改动）、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点 `src/command/`；2 生产文件：merge.rs、rename_detect.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=rename 感知合并; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=4/8; landing=1; prod-files=2; scope=M; deps=MG-04; writeset=序列化于 MG-04; release=independent; split-from=N/A; exception=EX-01`

### Task MG-06: rename 路径级冲突类型

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在 MG-05 的 per-side rename 之上，按 Git `handle_path_level_conflicts`（`merge-ort.c:2394`）实现 rename/rename(1to2)、rename/rename(2to1)、rename/add、rename/delete 四类路径级冲突。本卡唯一行为轴：rename 冲突语义。

**Out of scope:** 目录 rename 与路径级冲突的交互（由 MG-07 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `ConflictKind` 仅三种内容冲突 | `src/command/merge.rs:2752` |
| Git 参照：rename/rename(1to2)、rename/add、rename/delete 分类处理 | git@`3cb9185f6` `merge-ort.c:2913`（`process_renames`） |

**Acceptance criteria:**

- [ ] rename/rename(1to2)：两个新路径都保留并报 `CONFLICT (rename/rename)`。
- [ ] rename/rename(2to1)：目标路径内容级合并 + rename 冲突提示。
- [ ] rename/add：报 `CONFLICT (rename/add)`，按 Git 语义落盘两侧内容。
- [ ] rename/delete：报 `CONFLICT (rename/delete)`，保留新路径（含对侧修改）。
- [ ] 四类冲突的 index stage 组合与 Git 对齐（用例注释注明出处）。
- [ ] 人读提示文案与 Git 语义一致（用例断言关键行，注释注明 git 出处）。

**判据规范（非计数正文，EX-01 门族，共 13 门）：**

- G1 rename/rename(1to2) 检测 — 承载 `merge_rename_conflict`
- G2 rename/rename(1to2) 两个新路径保留 — 承载 `merge_rename_conflict`
- G3 rename/rename(2to1) 检测 — 承载 `merge_rename_conflict`
- G4 rename/rename(2to1) 目标路径内容级合并 — 承载 `merge_rename_conflict`
- G5 rename/add 检测并报告 — 承载 `merge_rename_conflict`
- G6 rename/add 两侧内容落盘 — 承载 `merge_rename_conflict`
- G7 rename/delete 检测并报告 — 承载 `merge_rename_conflict`
- G8 rename/delete 保留新路径（含对侧修改） — 承载 `merge_rename_conflict`
- G9 四类 stage 组合与 Git 对齐 — 承载 `merge::rename_conflict` 单测
- G10 人读文案与 Git 语义一致 — 承载 `merge_rename_conflict`
- G11 各类 `--continue` 收尾 — 承载 `merge_rename_conflict`
- G12 各类 `--abort` 完整恢复 — 承载 `merge_rename_conflict`
- G13 各类 `--dry-run` 报告类别 — 承载 `merge_rename_conflict`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_rename_conflict`（(new) 门 G1-G8、G10-G13——组合矩阵在本测试内逐场景断言）
- [ ] `source .env.test && cargo test --lib merge::rename_conflict`（(new) 门 G9 单测）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_rename`（MG-05 用例回归：MG-05 的双侧改名退化 notice 被正式冲突取代）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-05（per-side rename 检测 + 串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=rename 冲突语义; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=13/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-05; writeset=序列化于 MG-05; release=independent; split-from=N/A; exception=EX-01`

### Task MG-07: 目录级 rename 推断

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 Git 目录 rename 语义（`get_provisional_directory_renames` 等，`merge-ort.c:2467-2912`）：一侧删除目录且其中文件多数改名到同一目标目录时，推断目录整体改名。遵循 `merge.directoryRenames=true|conservative` 语义。本卡唯一行为轴：目录 rename 推断。

**Out of scope:** 目录 rename 与 rename/rename(1to2)/rename/rename(2to1) 叠加的其余角落案例（主干按 Git 参照实现；AC4 采样组合之外的未覆盖案例登记 **DEFER-07**，含重启条件与兼容说明）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| Libra 无任何目录 rename 推断（`ConflictKind` 无目录类，merge.rs 无目录 rename 逻辑） | `src/command/merge.rs:2752`（2026-09-03 grep 实证） |
| Git 参照：多数去向推断 + `apply_directory_rename_modifications` | git@`3cb9185f6` `merge-ort.c:2636`、`:2704` |

**Acceptance criteria:**

- [ ] 一侧 `old/` 整体改名 `new/`（多数文件配对）时推断目录 rename，对侧在 `old/` 下新增/修改的文件自动落位 `new/`。
- [ ] 推断不成多数或平局时确定性退化并报 notice。
- [ ] `merge.directoryRenames=true|false|conservative` 配置语义与 Git 对齐，非法值 fail-closed。
- [ ] 目录 rename 与 MG-06 路径级冲突的组合（如目标目录下 rename/add）按 Git 语义呈现。
- [ ] 非目录 rename 场景行为零变化（既有 `command::merge_test` 全绿）。

**判据规范（非计数正文，EX-01 门族，共 11 门）：**

- G1 多数配对时推断目录 rename — 承载 `merge_dir_rename`
- G2 对侧新增文件落位新目录 — 承载 `merge_dir_rename`
- G3 对侧修改文件落位新目录 — 承载 `merge_dir_rename`
- G4 平局/不成多数确定性退化 — 承载 `merge_dir_rename`
- G5 退化时输出 notice — 承载 `merge_dir_rename`
- G6 `merge.directoryRenames=true` 语义 — 承载 `merge_dir_rename`
- G7 `merge.directoryRenames=false` 语义 — 承载 `merge_dir_rename`
- G8 `merge.directoryRenames=conservative` 语义 — 承载 `merge_dir_rename`
- G9 非法配置值 fail-closed — 承载 `merge_dir_rename`
- G10 与 MG-06 路径级冲突组合呈现 — 承载 `merge_dir_rename`
- G11 非目录 rename 场景零变化 — 承载 `command::merge_test::` 回归

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_dir_rename`（(new) 门 G1-G10 端到端）
- [ ] `source .env.test && cargo test --lib merge::dir_rename`（(new) 多数推断单测）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-05（per-side rename 检测）、MG-06（路径级冲突分类 + 串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=目录 rename 推断; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=11/8@EX-01; VER=3/8; landing=1; prod-files=1; scope=M; deps=MG-05,MG-06; writeset=序列化于 MG-06; release=independent; split-from=N/A; exception=EX-01`

### Task MG-08: merge driver 分派（gitattributes + 内建 text/binary/union）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 Git `find_ll_merge_driver` 语义（`merge-ll.c:361-390`）实现内容合并的 driver 分派，**未命中一律回退 text 三路**（无论未命中的是属性名还是 `merge.default` 的值——Git 不做配置校验、不 fail-closed）。内建实现 text（现状 diffy 行级）、binary、union。分派经共享 helper 接入四个消费者：merge、merge-file、cherry-pick、revert（清单在此登记，不占 AC 计数）；merge-file 在仓库外运行时无 attributes 来源，恒按 text 默认（语义在卡内写明并入文档）。本卡唯一行为轴：内容合并的 driver 分派框架。

**Out of scope:** 外部 driver 命令（由 MG-09 承接）；`merge.<name>.recursive`（MG-09 登记）；text driver 的精化（MG-10）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 内容合并无属性分派，一律走 diffy（revert 无 favor 路径直接调 `diffy::merge_bytes`） | `src/command/merge.rs:2883`、`src/command/revert.rs:960-963` |
| 仓库已有 gitattributes 消费面（diff 的 `diff` 属性——外部 diff driver 选择，经 `attributes::diff_driver_for_path`）；通用属性读取入口 `attribute_state_for_path`（既有消费方 `check_attr.rs:177`） | `src/command/diff.rs:2728`、`:2734`；`src/utils/attributes.rs:267`、`src/command/check_attr.rs:177` |
| Git 参照：`find_ll_merge_driver` 四分支语义，未命中一律回退 text | git@`3cb9185f6` `merge-ll.c:361-390` |

**Acceptance criteria:**

- [ ] `merge` / `merge=text` 属性走 text driver；`-merge` / `merge=binary` 属性走 binary driver；`merge=union` 属性走 union driver。
- [ ] `merge=<unknown-name>` 恒回退 text 三路。
- [ ] 属性未设置时按 `merge.default` 分派；`merge.default` 未配置或其值未命中任何 driver 时回退 text（不 fail-closed）。
- [ ] 属性读取复用既有 gitattributes 引擎（GC-02），不自造解析。
- [ ] 四个消费者（清单见 Description）经共享 helper 继承分派，merge-file 仓库外恒 text 语义写明。
- [ ] 无属性场景行为零变化（merge / merge-file / cherry-pick / revert 既有测试全绿）。

**判据规范（非计数正文，EX-01 门族，共 15 门）：**

- G1 `merge`/`merge=text` → text driver — 承载 `merge_driver`
- G2 `-merge`/`merge=binary` → binary driver — 承载 `merge_driver`
- G3 `merge=union` → union driver — 承载 `merge_driver`
- G4 binary 不产冲突 marker — 承载 `merge_driver`
- G5 binary 无 favor 时整文件冲突 — 承载 `merge_driver`
- G6 union 冲突区域两侧行保留（ours 后 theirs） — 承载 `merge_driver`
- G7 `merge=<unknown-name>` 恒回退 text — 承载 `merge_driver`
- G8 属性未设置时按 `merge.default` 分派 — 承载 `merge_driver`
- G9 `merge.default` 未配置时回退 text — 承载 `merge_driver`
- G10 `merge.default` 值未命中任何 driver 时回退 text — 承载 `merge_driver`
- G11 属性读取复用既有 gitattributes 引擎 — 承载 `merge::driver` 单测
- G12 merge-file 继承分派（含仓库外恒 text） — 承载 `merge_file_driver`
- G13 cherry-pick 继承分派 — 承载 `cherry_pick_driver`
- G14 revert 继承分派 — 承载 `revert_driver`
- G15 无属性场景零变化（四消费者既有测试全绿） — 承载 `command::merge_test::` 回归 + T-1 全量

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_driver`（(new) 门 G1-G10 + 无属性零变化——矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib merge::driver`（(new) 门 G11 单测）
- [ ] `source .env.test && cargo test --test command_test command::merge_file_test::merge_file_driver`（(new) 门 G12）
- [ ] `source .env.test && cargo test --test command_test command::cherry_pick_test::cherry_pick_driver`（(new) 门 G13）
- [ ] `source .env.test && cargo test --test command_test command::revert_test::revert_driver`（(new) 门 G14）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归；三消费者模块回归由 T-1 全量覆盖）

**Full-suite trigger:** `T-1: GC-02 共享 helper（driver 分派被 merge/merge-file/cherry-pick/revert 共用）`

**Dependencies:** MG-07（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`src/command/merge_file.rs`、`src/command/cherry_pick.rs`、`src/command/revert.rs`（三消费者接线）、`tests/command/merge_test.rs`、`tests/command/merge_file_test.rs`、`tests/command/cherry_pick_test.rs`、`tests/command/revert_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/commands/merge-file.md`、`docs/commands/zh-CN/merge-file.md`、`docs/commands/cherry-pick.md`、`docs/commands/zh-CN/cherry-pick.md`、`docs/commands/revert.md`、`docs/commands/zh-CN/revert.md`、`docs/development/commands/merge.md`、`docs/development/commands/merge-file.md`、`docs/development/commands/cherry-pick.md`、`docs/development/commands/revert.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点 `src/command/`；4 生产文件：merge.rs、merge_file.rs、cherry_pick.rs、revert.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=driver 分派框架; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=15/8@EX-01; VER=6/8; landing=1; prod-files=4; scope=M; deps=MG-07; writeset=序列化于 MG-07; release=independent; split-from=N/A; exception=EX-01`

### Task MG-09: 外部 merge driver（`merge.<name>.driver`）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 实现 Git 外部 merge driver 语义（`merge-ll.c:191` `ll_ext_merge`）：`merge=<name>` 且配置 `merge.<name>.driver` 存在时，把 base/ours/theirs 写入临时文件，展开命令行占位符后经 shell 执行。**占位符安全契约与 Git 逐一对齐：** `%O %A %B` 为系统生成的临时文件绝对路径（不 quote，与 Git 相同）；`%P %S %X %Y` 按 Git `sq_quote_buf` 语义做单引号 shell-quote（`merge-ll.c:229-236`）。本卡唯一行为轴：外部 driver 子进程接入。

**Out of scope:** `merge.<name>.recursive`（Git 定义为内部共同祖先合并所用的低层 driver，非标签定制）——DEFER-08；driver 命令的 sandbox 化（信任模型见 Security 字段）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 无外部 driver 支持 | GAP-10 |
| Git 参照：占位符展开（含 sq_quote）+ 退出码语义 | git@`3cb9185f6` `merge-ll.c:191-277`、配置解析 `:280-345` |

**Acceptance criteria:**

- [ ] `%O %A %B %L` 展开与 Git 一致（`%L` = 冲突 marker 长度）。
- [ ] `%P %S %X %Y` 按 Git sq_quote 语义 shell-quote 后展开（含空格/引号/`$()` 的路径与标签回归用例）。
- [ ] 退出码 0 / 1..128 / >128 三态语义与 Git 一致；>128 或命令不可执行时报可操作错误且不产半成品 index。
- [ ] 临时文件写入 0700 权限的 worktree-local 目录，文件名不可预测，合并结束清理；信号中断路径不残留可读敏感内容。
- [ ] `%A` 文件在 driver 退出后被读回作为合并结果（空输出合法）。
- [ ] 与 `-X ours/theirs`、recursive 虚拟祖先（`%S/%X/%Y` 标签随 depth 变化）组合正确。

**判据规范（非计数正文，EX-01 门族，共 19 门）：**

- G1 `%O` 展开为 base 临时文件绝对路径 — 承载 `merge::ext_driver` 单测
- G2 `%A` 展开为 ours 临时文件绝对路径 — 承载 `merge::ext_driver` 单测
- G3 `%B` 展开为 theirs 临时文件绝对路径 — 承载 `merge::ext_driver` 单测
- G4 `%L` 展开为冲突 marker 长度 — 承载 `merge::ext_driver` 单测
- G5 `%P` sq_quote 展开（含空格/引号/`$()` 路径回归） — 承载 `merge_ext_driver`
- G6 `%S` sq_quote 展开 — 承载 `merge::ext_driver` 单测
- G7 `%X` sq_quote 展开 — 承载 `merge::ext_driver` 单测
- G8 `%Y` sq_quote 展开 — 承载 `merge::ext_driver` 单测
- G9 退出码 0 = 干净合并 — 承载 `merge_ext_driver`
- G10 退出码 1..128 = 冲突（采用 `%A` 为结果） — 承载 `merge_ext_driver`
- G11 退出码 >128 = 错误（fail-closed） — 承载 `merge_ext_driver`
- G12 命令不可执行时报可操作错误 — 承载 `merge_ext_driver`
- G13 错误路径不产半成品 index — 承载 `merge_ext_driver`
- G14 临时目录 0700 权限 — 承载 `merge::ext_driver` 单测
- G15 临时文件名不可预测 — 承载 `merge::ext_driver` 单测
- G16 合并结束临时文件清理 — 承载 `merge::ext_driver` 单测
- G17 `%A` 文件读回为合并结果（空输出合法） — 承载 `merge_ext_driver`
- G18 与 `-X ours/theirs` / recursive 标签（随 depth）组合正确 — 承载 `merge_ext_driver`
- G19 信号中断路径不残留可读敏感内容（清理或权限保证） — 承载 `merge_ext_driver`（中断注入场景）

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_ext_driver`（(new) 门 G5、G9-G13、G17-G19 端到端，用 POSIX sh 测试 driver 脚本——组合矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib merge::ext_driver`（(new) 门 G1-G4、G6-G8、G14-G16 单测）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）
- [ ] 手工证据（sanitized）：测试 driver 脚本清单与其退出码矩阵说明

**Full-suite trigger:** `none`

**Dependencies:** MG-02（recursive 标签语义）、MG-08（driver 分派框架 + 串行链前驱）

**Implementation write set:** `src/command/merge.rs`（外部 driver 的临时文件实现收敛于本卡：0700、系统生成不可预测名；既有 temp 设施仅只读复用、不修改——若开工 ER-02 核实确需扩展 `src/utils/atomic_write.rs` 共享 helper，按 ER-02 刷新本卡写集并将 Full-suite trigger 改为 T-1）、`tests/command/merge_test.rs`、`tests/fixtures/`（测试 driver 脚本）、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Security and privacy:** 外部 driver 是用户显式配置的信任边界（与 Git 同模型）：命令来自本地/global 配置，不来自被合并内容；`%P/%S/%X/%Y` 一律 sq_quote，临时路径系统生成；临时文件 0700 防本地嗅探；错误消息与日志截断脱敏（ER-11），不回显完整命令行。

**Estimated scope:** `M`（1 落点 `src/command/`；1 生产文件：merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=外部 driver 子进程; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=19/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-02,MG-08; writeset=序列化于 MG-08; release=independent; split-from=N/A; exception=EX-01`

### Task MG-10: 冲突精化 + zdiff3 + 行尾保持

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 Git `XDL_MERGE_ZEALOUS` 语义（`xdiff/xmerge.c:363`、`:467`）在 diffy 输出之上自建精化层；`merge.conflictStyle=zdiff3` 落地；冲突 marker 行尾跟随原文件；渲染层收敛为 merge/cherry-pick/revert 三消费者共享（revert 的无 favor 路径从直接调 `diffy::merge_bytes` 改走共享渲染层）。本卡唯一行为轴：冲突呈现质量。

**Out of scope:** 冲突块与 Git 字节级一致的承诺（ADR-MG-02：语义一致判据）；zdiff3 之外的新冲突风格。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 冲突块为 diffy 原始输出，无精化；行尾按平台 | `src/command/merge.rs:3256` |
| `conflict_style_from_config` 仅接受 merge/diff3 | `src/command/merge.rs:2117` |
| revert 无 favor 路径直接调 `diffy::merge_bytes`（不经共享渲染层） | `src/command/revert.rs:960-963` |
| Git 参照：refine/simplify/zdiff3 | git@`3cb9185f6` `xdiff/xmerge.c:334`、`:363`、`:467` |

**Acceptance criteria:**

- [ ] 冲突块含公共前缀/后缀行时缩小到真正差异行（refine）。
- [ ] 两侧做出相同变更的 hunks 化简为非冲突直接采纳（simplify）。
- [ ] `merge.conflictStyle=zdiff3` 输出 zdiff3 格式；非法值仍 fail-closed。
- [ ] 既有 `merge`/`diff3` 冲突格式不回归。
- [ ] 冲突 marker 行尾跟随原文件（CRLF 输入文件的全部 marker 行以 CRLF 结尾）。
- [ ] 精化不改变自动合并的成功/失败判定（只作用于呈现与化简）。
- [ ] 渲染层收敛为 merge/cherry-pick/revert 共享（revert 无 favor 路径改走共享层；既有钉住旧行为的测试按语义升级并登记修订历史）。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 refine 缩小冲突块 — 承载 `merge_conflict_refine`
- G2 simplify 化简相同变更 — 承载 `merge_conflict_refine`
- G3 zdiff3 输出格式 — 承载 `merge_conflict_refine`
- G4 既有 merge/diff3 格式不回归 — 承载 `command::merge_test::` 回归
- G5 非法 conflictStyle 值 fail-closed — 承载 `merge_conflict_refine`
- G6 CRLF 文件 marker 行尾保持 — 承载 `merge_conflict_refine`
- G7 精化不改变合并成败判定 — 承载 `merge::refine` 单测
- G8 cherry-pick 继承精化 — 承载 `command::cherry_pick_test::`
- G9 revert 继承精化 — 承载 `command::revert_test::`
- G10 revert 无 favor 路径改走共享渲染层 — 承载 `command::revert_test::`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_conflict_refine`（(new) 门 G1-G3、G5、G6 端到端）
- [ ] `source .env.test && cargo test --lib merge::refine`（(new) 门 G7 单测）
- [ ] `source .env.test && cargo test --test command_test command::cherry_pick_test::`（门 G8 继承回归）
- [ ] `source .env.test && cargo test --test command_test command::revert_test::`（门 G9-G10 继承回归）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `T-1: GC-02 共享渲染层（merge/cherry-pick/revert 共用）`

**Dependencies:** MG-09（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`src/command/cherry_pick.rs`（仅继承接入）、`src/command/revert.rs`（继承接入：无 favor 路径改走共享渲染层）、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/commands/cherry-pick.md`、`docs/commands/zh-CN/cherry-pick.md`、`docs/commands/revert.md`、`docs/commands/zh-CN/revert.md`（cherry-pick/revert 冲突渲染精化继承面）、`docs/development/commands/merge.md`、`docs/development/commands/cherry-pick.md`、`docs/development/commands/revert.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点 `src/command/`；3 生产文件：merge.rs、cherry_pick.rs、revert.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=冲突呈现质量; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=5/8; landing=1; prod-files=3; scope=M; deps=MG-09; writeset=序列化于 MG-09; release=independent; split-from=N/A; exception=EX-01`

### Task MG-11: `-X` whitespace 选项 + renormalize

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 实现 Git `-X ignore-space-change` / `ignore-all-space` / `ignore-space-at-eol` / `ignore-cr-at-eol`（`merge-ort.c:5551-5608` 透传 xdiff flags 的语义）与 `merge.renormalize` / `-X renormalize|no-renormalize`：归一化在内容合并输入侧进行，结果保持 ours 侧原始行尾；空白忽略通过归一化比较 + 原文回填实现（ADR-MG-02），复用 `src/command/diff.rs:5930-5960` 的既有空白归一化引擎。本卡唯一行为轴：合并输入归一化。

**Out of scope:** `-X diff-algorithm=*`（DEFER-02）；`-X find-renames[=<score>]` / `rename-threshold` flag 别名（rename 阈值配置已由 MG-05 的 renameLimit 面覆盖）——DEFER-09。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `-X` 仅 ours/theirs | `src/command/merge.rs:90` |
| diff 空白归一化引擎（`normalize_ignore_all_space` 等） | `src/command/diff.rs:5930-5960` |
| Git 参照：xdiff flags 透传 + renormalize | git@`3cb9185f6` `merge-ort.c:5551-5608`、`merge-ll.c:423-425` |

**Acceptance criteria:**

- [ ] `-X ignore-space-change`：仅空白数量差异的 hunks 不再冲突。
- [ ] `-X ignore-all-space`：比较时忽略全部空白，仅空白差异不冲突。
- [ ] `-X ignore-space-at-eol`：仅行尾空白差异不冲突。
- [ ] `-X ignore-cr-at-eol`：CRLF↔LF 行尾差异不冲突。
- [ ] `merge.renormalize=true` 或 `-X renormalize` 时按属性归一化输入再合并；`-X no-renormalize` 显式关闭。
- [ ] 归一化路径的落盘结果保持 ours 侧原始行尾（语义注释注明 git 出处）。
- [ ] 空白判定复用 diff 的既有空白引擎（merge 与 diff 对同一输入的空白判定对拍一致）。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 `-X ignore-space-change` 生效 — 承载 `merge_x_whitespace`
- G2 `-X ignore-all-space` 生效 — 承载 `merge_x_whitespace`
- G3 `-X ignore-space-at-eol` 生效 — 承载 `merge_x_whitespace`
- G4 `-X ignore-cr-at-eol` 生效 — 承载 `merge_x_whitespace`
- G5 `merge.renormalize=true` 归一化输入再合并 — 承载 `merge_x_whitespace`
- G6 `-X renormalize` 等价生效 — 承载 `merge_x_whitespace`
- G7 `-X no-renormalize` 显式关闭 — 承载 `merge_x_whitespace`
- G8 落盘结果保持 ours 侧原始行尾 — 承载 `merge_x_whitespace`
- G9 空白判定与 diff 引擎对拍一致 — 承载 `merge::whitespace` 单测
- G10 无新选项场景零变化 — 承载 `merge_x_whitespace` + `command::merge_test::` 回归

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_x_whitespace`（(new) 门 G1-G8、G10 + 相互组合——组合矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib merge::whitespace`（(new) 门 G9 单测 + 归一化/回填）
- [ ] `source .env.test && cargo test --test command_test command::diff_test::`（diff 空白引擎回归；模块名开工时刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `T-1: GC-02 共享 helper（diff 空白引擎导出给 merge 消费）`

**Dependencies:** MG-10（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`src/command/diff.rs`（空白引擎接口导出，无算法改动）、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点 `src/command/`；2 生产文件：merge.rs、diff.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=合并输入归一化; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=4/8; landing=1; prod-files=2; scope=M; deps=MG-10; writeset=序列化于 MG-10; release=independent; split-from=N/A; exception=EX-01`

### Task MG-12: octopus 多分支合并

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 支持 `libra merge <branch>...`（≥2 个分支）的 octopus 合并（ADR-MG-05）：求 octopus merge bases，逐 head 树级三方合并，全部自动可合并才生成多父提交；任一 head 产生内容冲突即整体原子失败。本卡唯一行为轴：多 head 合并。

**Out of scope:** octopus 冲突的人工解决（Git 同样不支持，永久非目标）；`merge-base --octopus` 命令面（「尚未排期，重启条件：脚本化工作流提出需求」）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 分支参数为单 `Option<String>` | `src/command/merge.rs:101` |
| Git 参照：`git-merge-octopus.sh` + `get_octopus_merge_bases` + 多父提交 | git@`3cb9185f6` `git-merge-octopus.sh`、`commit-reach.c:326`、`builtin/merge.c:1620-1634` |
| COMPATIBILITY 已登记 octopus deferred | `COMPATIBILITY.md:150` |

**Acceptance criteria:**

- [ ] 2 个及以上分支、树级无重叠变更：octopus 成功，生成 N+1 父合并提交（父序 = HEAD + 命令行序）。
- [ ] 任一 head 引入内容级冲突：整体原子失败（"Should not be doing an octopus" 语义对齐的可操作错误），index/worktree/HEAD 零变化。
- [ ] 冗余 head（可被其它 head 到达）按 Git `reduce_heads` 语义剔除。
- [ ] 全部 head 已是最新时 `Already up to date` 语义对齐。
- [ ] 单分支调用行为零变化（既有 `command::merge_test` 全绿）。
- [ ] octopus 路径禁止 fast-forward。
- [ ] octopus merge base 集合变体正确（含 criss-cross 输入）。
- [ ] `MergeState` 持久化多 target 向后兼容（GC-MG-14）。

**判据规范（非计数正文，EX-01 门族，共 13 门）：**

- G1 多分支 octopus 成功生成 N+1 父提交 — 承载 `merge_octopus`
- G2 父序 = HEAD + 命令行序 — 承载 `merge::octopus` 单测
- G3 内容冲突整体原子拒绝 — 承载 `merge_octopus`
- G4 拒绝时 index 零变化 — 承载 `merge_octopus`
- G5 拒绝时 worktree 零变化 — 承载 `merge_octopus`
- G6 拒绝时 HEAD 零变化 — 承载 `merge_octopus`
- G7 冗余 head 按 reduce_heads 剔除 — 承载 `merge_octopus`
- G8 全部最新时 Already up to date — 承载 `merge_octopus`
- G9 单分支调用行为零变化 — 承载 `command::merge_test::` 回归
- G10 octopus 禁止 fast-forward — 承载 `merge_octopus`
- G11 octopus merge base 变体正确 — 承载 `merge::octopus` 单测
- G12 criss-cross 输入的 octopus base 正确 — 承载 `merge::octopus` 单测
- G13 `MergeState` 多 target 持久化向后兼容 + `--abort` 恢复 — 承载 `merge_octopus`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_octopus`（(new) 门 G1、G3-G8、G10、G13 端到端——场景矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib merge::octopus`（(new) 门 G2、G11、G12 单测）
- [ ] `source .env.test && cargo test --test compat_help_examples_banner`（help 表面回归；target 名开工时刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `T-1: MergeArgs 位置参数 arity 变化属公开 CLI 表面（help/文档契约同步）`

**Dependencies:** MG-01（merge base octopus 变体）、MG-11（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`src/internal/merge_base.rs`（octopus 变体消费）、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（2 落点：`src/command/`、`src/internal/`；2 生产文件：merge.rs、internal/merge_base.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=多 head 合并; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=13/8@EX-01; VER=4/8; landing=2; prod-files=2; scope=M; deps=MG-01,MG-11; writeset=序列化于 MG-11; release=independent; split-from=N/A; exception=EX-01`

### Task MG-13: 策略框架（`-s` 别名 + 多策略回退）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** `-s/--strategy` 扩展为 `ort`（默认）、`recursive`（`ort` 别名）、`resolve`（降级：禁用 rename 检测与 recursive 虚拟祖先、单 base 三路）、`ours`（既有）；`-s` 可重复，按 Git 语义依次尝试，干净即止，全部有冲突时按 `evaluate_result` 口径选 best 重放。本卡唯一行为轴：策略选择与回退框架。

**Out of scope:** 外部 `git-merge-<name>` 策略命令发现（DEFER-06）；`-s subtree`（ADR-MG-01 永久非目标）；`-s octopus` 显式指定（多 head 自动路由已由 MG-12 覆盖）——DEFER-10。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `MergeStrategy` 仅 `Ours`，`-s recursive` 解析期拒绝 | `src/command/merge.rs:82` |
| Git 参照：`all_strategy` 表 + `try_merge_strategy` 回退循环 + `evaluate_result` | git@`3cb9185f6` `builtin/merge.c:101`、`:789`、`:1783-1822` |

**Acceptance criteria:**

- [ ] `-s ort` / `-s recursive` 接受且等价默认行为。
- [ ] `-s resolve` 走降级路径（rename 关闭、单 base、无 recursive 虚拟祖先）。
- [ ] rename 输入在 resolve 下确定性退化为 modify/delete+add 并有 notice。
- [ ] 未知策略名报可操作错误。
- [ ] 多 `-s`（如 `-s resolve -s ort`）依次尝试，首个干净策略生效并输出人读提示（"Trying merge strategy X..." 语义对齐）。
- [ ] 全部策略有冲突时按 Git `evaluate_result` 口径（冲突文件数 + 未解决条目数）选 best 重放（注释注明出处）。
- [ ] 默认策略路由不变（无 `-s` 时单 head=ort、多 head=octopus）。
- [ ] `--json` 新增策略字段为 additive（schema 演进说明入文档）。

**判据规范（非计数正文，EX-01 门族，共 14 门）：**

- G1 `-s ort` 接受 — 承载 `merge_strategy`
- G2 `-s recursive` 接受且等价 ort — 承载 `merge_strategy`
- G3 `-s resolve` 关闭 rename 检测 — 承载 `merge_strategy`
- G4 `-s resolve` 使用单 base — 承载 `merge::strategy` 单测
- G5 `-s resolve` 关闭 recursive 虚拟祖先 — 承载 `merge::strategy` 单测
- G6 resolve 下 rename 输入退化为 modify/delete+add — 承载 `merge_strategy`
- G7 resolve 退化时输出 notice — 承载 `merge_strategy`
- G8 未知策略名可操作错误 — 承载 `merge_strategy`
- G9 多 `-s` 依次尝试 — 承载 `merge_strategy`
- G10 首个干净策略生效并输出人读提示 — 承载 `merge_strategy`
- G11 全冲突时按 `evaluate_result` 口径选 best — 承载 `merge::strategy` 单测
- G12 默认单 head = ort — 承载 `merge_strategy`
- G13 默认多 head = octopus — 承载 `merge_strategy`
- G14 `--json` 策略字段 additive — 承载 `merge_strategy`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_strategy`（(new) 门 G1-G3、G6-G10、G12-G14 端到端——场景矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib merge::strategy`（(new) 门 G4、G5、G11 单测）
- [ ] `source .env.test && cargo test --test compat_help_examples_banner`（help 表面回归；target 名开工时刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-02（resolve 的「关闭 recursive」分界）、MG-05（resolve 的「关闭 rename」分界）、MG-12（多 head 默认路由 + 串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=策略选择框架; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=14/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-02,MG-05,MG-12; writeset=序列化于 MG-12; release=independent; split-from=N/A; exception=EX-01`

### Task MG-14: mergetool

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 新增 `libra mergetool` 命令：基于冲突 index 的 stage 1/2/3 提取 BASE/LOCAL/REMOTE 版本到临时文件，驱动外部合并工具（内建描述：vimdiff、nvimdiff、meld、vscode、opendiff；`merge.tool` / `mergetool.<tool>.cmd` 配置自定义——`.cmd` 按 Git 语义**经 shell 求值**并暴露 `$BASE/$LOCAL/$REMOTE/$MERGED` 变量，出处 git@pin `Documentation/config/mergetool.adoc:5`、`git-mergetool--lib.sh`；信任模型与 MG-09 外部 driver 一致：命令来自用户本地配置，临时文件路径仅经变量暴露、不拼接进命令字符串），工具按 Git 语义判定成功后写回并 `add`。本卡唯一行为轴：冲突人工解决的工具集成。

**Out of scope:** 全量 25+ 工具描述文件（DEFER-03）；`mergetool.keepBackup` 之外的全部 `mergetool.*` 配置（未实现的配置 fail-closed 提示）——DEFER-11；GUI 工具的平台路径探测细节（按 PATH 探测 + 显式配置优先）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `Commands` 枚举无 `Mergetool` 变体（2026-09-03 核对） | `src/cli.rs:613` 附近（`Commands::Merge` 挂载点） |
| 冲突 stage 1/2/3 已写入 index | `src/command/merge.rs:2156` |
| Git 参照：`git-mergetool.sh` + `git-mergetool--lib.sh` + `mergetools/` | git@`3cb9185f6` |

**Acceptance criteria:**

- [ ] 无冲突或无进行中的 merge 时给出可操作提示（exit 非 0，不启动工具）。
- [ ] 对每个冲突路径提取 stage1/2/3 到 0700 临时目录。
- [ ] `mergetool.<tool>.cmd` 经 shell 求值，`$BASE/$LOCAL/$REMOTE/$MERGED` 变量可用；临时文件路径仅经变量暴露，不拼接进命令字符串（Git 出处注释注明，GC-MG-15）。
- [ ] `mergetool.<tool>.path` 显式路径优先于 PATH 探测。
- [ ] 默认（`trustExitCode=false`）按 Git 语义判定：`$MERGED` 自工具启动后 mtime 变化即视为已解决；未变化时读取用户回答后按回答判定（出处 `git-mergetool--lib.sh:143`、`:334`）。
- [ ] mtime 未变且 stdin EOF/非交互 = 未解决（非零退出、不得 stage）。
- [ ] `trustExitCode=true` 时仅以退出码判定。
- [ ] 判定未解决时该路径保持未解决。
- [ ] `--tool=<name>` / `merge.tool` 选择工具；`--tool-help` 列出内建工具与可用性；未知工具名报可操作错误。
- [ ] `mergetool.keepBackup` 的 `.orig` 备份语义与 Git 对齐。
- [ ] symlink/mode 冲突与 modify/delete 冲突路径给出明确提示（不静默跳过）。

**判据规范（非计数正文，EX-01 门族，共 16 门）：**

- G1 无冲突时可操作提示（exit 非 0） — 承载 `mergetool_test::`
- G2 无进行中 merge 时可操作提示 — 承载 `mergetool_test::`
- G3 stage1/2/3 提取到临时目录 — 承载 `mergetool::` 单测
- G4 临时目录 0700 权限 — 承载 `mergetool::` 单测
- G5 cmd shell 求值 + 四变量可用（路径仅经变量暴露，不拼命令串；含 `cmd='tool "$LOCAL" "$REMOTE" "$MERGED"'` 既有配置形态回归） — 承载 `mergetool::` 单测 + `mergetool_test::`
- G6 `mergetool.<tool>.path` 优先于 PATH 探测 — 承载 `mergetool_test::`
- G7 默认（trustExitCode=false）按 `$MERGED` mtime 判定、未变化时读取用户回答（出处 `git-mergetool--lib.sh:143`、`:334`） — 承载 `mergetool_test::`
- G8 mtime 未变且 stdin EOF/非交互 = 未解决（非零退出、不得 stage） — 承载 `mergetool_test::`（非交互注入场景）
- G9 `trustExitCode=true` 仅以退出码判定 — 承载 `mergetool_test::`
- G10 解决后写回工作区并 stage 该路径 — 承载 `mergetool_test::`
- G11 判定未解决时保持未解决 — 承载 `mergetool_test::`
- G12 `--tool` / `merge.tool` 选择工具 — 承载 `mergetool_test::`
- G13 `--tool-help` 列出内建工具与可用性 — 承载 `mergetool_test::`
- G14 未知工具名可操作错误 — 承载 `mergetool_test::`
- G15 `mergetool.keepBackup` 的 `.orig` 语义 — 承载 `mergetool_test::`
- G16 symlink/mode 与 modify/delete 冲突明确提示 — 承载 `mergetool_test::`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::mergetool_test::`（(new) 模块：门 G1-G2、G6-G16 端到端——同卡在 `tests/command/mod.rs` 注册 `mod mergetool_test;`）
- [ ] `source .env.test && cargo test --lib mergetool::`（(new) 门 G3-G5 单测）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（merge 侧回归）
- [ ] `source .env.test && cargo test --test compat_command_docs_examples_section`（新命令文档契约；target 名开工时刷新）
- [ ] 手工证据（sanitized）：`--tool-help` 输出样例

**Full-suite trigger:** `T-1: 新增公开命令（src/cli.rs 注册 + COMPATIBILITY.md 新行）`

**Dependencies:** MG-13（串行链前驱）

**Implementation write set:** `src/command/mergetool.rs`（新）、`src/command/mod.rs`、`src/cli.rs`、`tests/command/mergetool_test.rs`（新模块）、`tests/command/mod.rs`（模块注册）、`tests/fixtures/`（假工具脚本）、`tests/INDEX.md`（若 `command_test` 行描述失真）、`docs/commands/mergetool.md`、`docs/commands/zh-CN/mergetool.md`、`docs/development/commands/mergetool.md`（新建）、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Docs and compatibility impact:** 新增命令文档面（默认覆盖登记项）：`docs/commands/mergetool.md` EN+zh 新建、`docs/development/commands/mergetool.md` 新建、`COMPATIBILITY.md` 新增 mergetool 行、`tests/command/mod.rs` 注册测试模块（`tests/INDEX.md` 的 `command_test` 行若描述失真则更新）。

**Security and privacy:** 工具命令仅来自用户本地配置与 PATH（信任模型与 MG-09 外部 driver 一致：配置是用户自有的信任边界）；`mergetool.<tool>.cmd` 按 Git 语义经 shell 求值，冲突文件内容派生的临时文件路径仅以 `$BASE/$LOCAL/$REMOTE/$MERGED` 变量暴露、不拼接进命令字符串；临时文件 0700；错误消息脱敏（ER-11）。

**Estimated scope:** `M`（2 落点：`src/command/`、`src/cli.rs` 按 src/ 根单文件计；3 生产文件：mergetool.rs、command/mod.rs、cli.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=mergetool 工具集成; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=16/8@EX-01; VER=5/8; landing=2; prod-files=3; scope=M; deps=MG-13; writeset=序列化于 MG-13; release=independent; split-from=N/A; exception=EX-01`

### Task MG-15: merge 提交签名（`-S/--gpg-sign`）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** merge 生成的合并提交接入既有 vault 签名设施：`-S/--gpg-sign` 显式开启，`--no-gpg-sign` 关闭；缺省时遵循 `commit.gpgSign` → `vault.signing` 既有优先级（与 commit 一致）。覆盖的提交路径：自动合并、`--no-commit --continue`、`-s ours`、octopus 多父提交（清单在此登记，不占 AC 计数）。本卡唯一行为轴：merge 提交签名。

**Out of scope:** 外部 GPG keyring / SSH 签名（vault-only，与 commit 现状一致）——DEFER-12；mergetag header（DEFER-04）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| merge 永不签名，`--no-gpg-sign` 为 no-op | `COMPATIBILITY.md:150` |
| commit 的签名优先级实现 | `src/command/commit.rs:215-224`、`src/command/history_config.rs:67-78` |
| cherry-pick 已有 `-S/--gpg-sign`（含 continue 保留签名用例） | `COMPATIBILITY.md:158`、`tests/command/cherry_pick_test.rs:2065`（`cherry_pick_continue_retains_gpg_sign`） |

**Acceptance criteria:**

- [ ] `-S` 时全部 merge 提交路径（清单见 Description）产出带 `gpgsig` 的提交。
- [ ] 签名失败（vault sealed 等）时报可操作错误且不产提交。
- [ ] 缺省优先级与 commit 完全一致：`--no-gpg-sign` > `commit.gpgSign` > `vault.signing` 默认。
- [ ] `--continue` 收尾的合并提交同样签名。
- [ ] 签名配置解析复用 commit 的既有 helper（GC-02）。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 自动合并路径产出 `gpgsig` 提交 — 承载 `merge_gpg_sign`
- G2 `--no-commit --continue` 路径产出 `gpgsig` 提交 — 承载 `merge_gpg_sign`
- G3 `-s ours` 路径产出 `gpgsig` 提交 — 承载 `merge_gpg_sign`
- G4 octopus 多父提交路径产出 `gpgsig` 提交 — 承载 `merge_gpg_sign`
- G5 签名失败时报可操作错误 — 承载 `merge_gpg_sign`
- G6 签名失败时不产提交 — 承载 `merge_gpg_sign`
- G7 `--no-gpg-sign` 显式关闭（最高优先） — 承载 `merge_gpg_sign`
- G8 `commit.gpgSign` 配置级联生效 — 承载 `merge_gpg_sign` + `compat_config_history_defaults`
- G9 `vault.signing` 默认兜底 — 承载 `merge_gpg_sign`
- G10 签名配置解析复用 commit 既有 helper — 承载 `merge::signing` 单测（GC-02 守卫）

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_gpg_sign`（(new) 门 G1-G7、G9 端到端——场景矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib merge::signing`（(new) 门 G10：签名配置解析复用 commit 既有 helper 的守卫单测）
- [ ] `source .env.test && cargo test --test compat_config_history_defaults`（配置级联回归；target 名开工时刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-12（octopus 提交路径纳入签名面）、MG-14（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs；一处公开接口变化：新增 `-S/--gpg-sign` flag）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=merge 提交签名; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-12,MG-14; writeset=序列化于 MG-14; release=independent; split-from=N/A; exception=EX-01`

### Task MG-16: `--signoff`

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** merge 支持 `--signoff`（Git 中 merge 的 signoff 无短选项，`-s` 已被 strategy 占用）：追加 `Signed-off-by: <user.name> <user.email>` trailer 到合并提交消息，复用 lore.md 1.9 既有 trailer 引擎。本卡唯一行为轴：signoff trailer。

**Out of scope:** `--signoff` 在其它命令的推广（非目标）；与编辑器/`--cleanup` 的组合顺序验收（由 MG-18 承接，本卡只保证与 `-m`/`--log` 组合正确）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| merge 无 `--signoff` | `src/command/merge.rs:99-265` |
| cherry-pick/revert 已有 `-s/--signoff` 与 trailer 复用范式 | `COMPATIBILITY.md:158`、`:172` |

**Acceptance criteria:**

- [ ] `--signoff` 在自动合并路径的消息末尾追加正确 trailer。
- [ ] `--signoff` 在 `--continue` 路径的消息末尾追加正确 trailer。
- [ ] 消息中已存在相同 signoff trailer 时不重复追加。
- [ ] 与 `-m` 组合时 trailer 在消息清理之后追加（顺序正确）。
- [ ] 与 `--log` 组合时 trailer 在消息清理之后追加（顺序正确）。

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_signoff`（(new) 追加/去重/组合端到端）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-15（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs；一处公开接口变化：新增 `--signoff` flag）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=signoff trailer; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=1; scope=M; deps=MG-15; writeset=序列化于 MG-15; release=independent; split-from=N/A; exception=N/A`

### Task MG-17: `--quit`

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** merge 支持 `--quit`（对齐 Git `remove_merge_branch_state` 语义）：只删除 merge 状态（merge-state.json 与相关投影），不动 index 与 worktree——冲突现场保持原样。本卡唯一行为轴：merge 状态机的「只清状态」出口。（本卡为原 MG-17 主轴；AUTO_MERGE 与 rerere flag 轴已由 MG-21、MG-22 拆出。）

**Out of scope:** `AUTO_MERGE` 投影及其清理（由 MG-21 承接，本卡不涉及）；rerere autoupdate（由 MG-22 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| merge 状态机仅有 `--continue`/`--abort`/`--restart`，无 `--quit` | `src/command/merge.rs:99-265`（MergeArgs） |
| Git 参照：`--quit` → `remove_merge_branch_state()` | git@`3cb9185f6` `builtin/merge.c:1417-1470`、`branch.c:863` |

**Acceptance criteria:**

- [ ] `--quit` 删除 merge-state.json（AUTO_MERGE 投影的清理由 MG-21 承接，本卡不涉及）。
- [ ] `--quit` 后 index 保持冲突现场原样（stage 1/2/3 不变）。
- [ ] `--quit` 后 worktree 保持冲突现场原样（marker 文件不变）。
- [ ] 无进行中 merge 时 `--quit` 的报错语义与 Git 对齐（可操作提示）。
- [ ] `--quit` 与其它 merge flag 的互斥组合在 clap 层拒绝（与 Git 语义对齐）。

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_quit`（(new) 冲突现场保留 / 无 merge 报错 / 互斥拒绝端到端）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `T-1: 新增公开 flag 且改变状态机清理行为（保守判定：--quit 非累加式 flag，删除 merge 状态与投影，故保守跑全量；MG-13/15/16/18 的累加式新 flag 由各自 focused 判据覆盖，不适用本口径）`

**Dependencies:** MG-16（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs；一处公开接口变化：新增 `--quit` flag）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=--quit 状态出口; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=1; scope=M; deps=MG-16; writeset=序列化于 MG-16; release=independent; split-from=N/A（本卡为原 MG-17 主轴）; exception=N/A`

### Task MG-18: 消息面 parity（`--edit`/`-F`/`--into-name`/`--cleanup`）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** merge 消息处理对齐 Git：`-e/--edit` 在提交前打开编辑器（复用 revert 的编辑器级联设施），默认保持「不自动开编辑器」；`-F/--file <file>` 从文件读取消息；`--into-name <branch>` 定制 "Merge ... into <name>"；`--cleanup=<strip|whitespace|verbatim|scissors|default>` 消息清理（五模式清单在此登记，不占 AC 计数）。本卡唯一行为轴：合并提交消息处理。

**Out of scope:** 默认改为开编辑器（保持 Libra 现状，Git 兼容注记入文档）——DEFER-13；`merge.suppressDest` 等 fmt_merge_msg 衍生配置——DEFER-14。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `--no-edit` 为 no-op，无 `-F`/`--into-name`/`--cleanup` | `src/command/merge.rs:99-265`、`COMPATIBILITY.md:150` |
| revert 已有 `--edit` + `--cleanup` 编辑器与清理复用范式 | `COMPATIBILITY.md:172` |

**Acceptance criteria:**

- [ ] `-e/--edit` 打开编辑器编辑合并消息（含冲突 `--continue` 路径）；编辑结果为空消息时 abort（语义与 revert/commit 既有行为一致）。
- [ ] `-F <file>` 读取消息（与 `-m` 互斥语义对齐 Git）。
- [ ] `--into-name <branch>` 替换自动消息的目标分支名。
- [ ] `--cleanup` 各模式（清单见 Description）复用既有实现（GC-02）。
- [ ] `--signoff`（MG-16）在 cleanup 之后追加（组合顺序与 Git 对齐）。
- [ ] 消息解析结果持久化进 `MergeState` 并跨 `--continue` 重放（GC-MG-14 向后兼容）。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 `--edit` 打开编辑器 — 承载 `merge_message_options`
- G2 冲突 `--continue` 路径可 `--edit` — 承载 `merge_message_options`
- G3 空消息 abort — 承载 `merge_message_options`
- G4 `-F <file>` 读取消息 — 承载 `merge_message_options`
- G5 `-F` 与 `-m` 互斥拒绝 — 承载 `merge_message_options`
- G6 `--into-name` 替换目标分支名 — 承载 `merge_message_options`
- G7 `--cleanup` 各模式复用既有实现 — 承载 `merge_message_options`
- G8 `--signoff` 在 cleanup 后追加 — 承载 `merge_message_options`
- G9 消息持久化进 `MergeState` — 承载 `merge_continue`
- G10 消息跨 `--continue` 重放 — 承载 `merge_continue`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_message_options`（(new) 门 G1-G8 端到端，编辑器用 `GIT_EDITOR` 脚本桩——组合矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_continue`（门 G9-G10 + (new) -F 组合）
- [ ] `source .env.test && cargo test --test compat_help_examples_banner`（help 表面回归；target 名开工时刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `none`

**Dependencies:** MG-16（`--signoff` 组合顺序）、MG-17（串行链前驱）

**Implementation write set:** `src/command/merge.rs`、`tests/command/merge_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/development/commands/merge.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=合并消息处理; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-16,MG-17; writeset=序列化于 MG-17; release=independent; split-from=N/A; exception=EX-01`

### Task MG-21: `AUTO_MERGE` pseudo-ref 投影与 tree-ish 消费路径

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 扩展 pseudo-ref 单一事实源（`src/internal/pseudo_ref.rs`，当前 `pub const ALL: [PseudoRef; 6]`）新增 `AUTO_MERGE`。**生命周期（与投影架构自洽）：** `AUTO_MERGE` 仅在 merge 进行中可观察——冲突落盘时指向自动合并结果树（含冲突 marker 的树），结果树 OID 持久化进 `MergeState`（GC-MG-14 可选字段）并纳入 GC root；`--continue`/`--abort`/`--quit` 收尾时随 merge state 清理幂等删除。干净自动合并不留常驻投影（Git 的写后即清理瞬时行为仅作源码注释对齐 `merge-ort.c:4960`）。**消费路径（ADR-MG-06）：** pseudo-ref 解析以**调用方 opt-in** 接入共享对象解析 seam `util::resolve_object_spec_typed`（`src/utils/util.rs:2124`）——只有 rev-parse、cat-file、diff 三个表面启用 AUTO_MERGE 解析；未 opt-in 的调用方（update-ref 及其余全部现有调用方）行为零变化、继续拒绝 pseudo-ref 名称。diff 侧在 `get_target_commit`（`mod.rs:555`，只产 commit）之外为「解析到 tree 的 pseudo-ref」补 tree-ish 分支。**compat 契约重构：** `tests/compat/pseudo_ref_surface.rs` 的名称提取从 `_HEAD` 后缀启发式改为显式集合，断言拆为 resolve 组（AUTO_MERGE 在冲突 merge 中可解析）与 reject 组（既有六 ref 继续拒绝）。（自 MG-17 拆出，G-09。）

**Out of scope:** `merge-tree --write-tree` 命令（DEFER-01）；AUTO_MERGE 在 rebase 的投影（非本卡）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `pub enum PseudoRef` 六变体、`pub const ALL: [PseudoRef; 6]`，无 AUTO_MERGE | `src/internal/pseudo_ref.rs:46`、`:70` |
| compat 契约：全部 DECLARED ref 须在 rev-parse 不可解析；名称提取用 `_HEAD` 后缀启发式（AUTO_MERGE 不匹配） | `tests/compat/pseudo_ref_surface.rs:38`、`:113`、`:135` |
| 共享对象解析 seam 与其调用方（含 update-ref 的 `<oldvalue>` CAS，不做 commit 类型检查） | `src/utils/util.rs:2124`、`src/command/rev_parse.rs:639`、`src/command/cat_file.rs:890`、`src/command/update_ref.rs:452`（resolver 调用 `:461`） |
| diff 目标解析只产 commit | `src/command/mod.rs:555`（`get_target_commit`） |
| merge state pseudo-ref 投影出口 | `src/command/merge.rs:380`（`merge_state_for_pseudo_refs`） |
| Git 参照：`merge_switch_to_result()` 写 AUTO_MERGE ref（指向 tree），完成清理时删除 | git@`3cb9185f6` `merge-ort.c:4960`、`branch.c:863` |

**Acceptance criteria:**

- [ ] 冲突落盘时 `AUTO_MERGE` 指向自动合并结果树（仅 merge 进行中可观察）；结果树 OID 持久化进 `MergeState`（serde 可选字段，GC-MG-14）并纳入 GC root。
- [ ] pseudo-ref 解析以调用方 opt-in 接入共享 seam（ADR-MG-06）；未 opt-in 调用方行为零变化（update-ref 对 pseudo-ref 名称继续拒绝，有具名回归）。
- [ ] `rev-parse AUTO_MERGE` 输出 tree id；`cat-file -t AUTO_MERGE` 返回 `tree`；无进行中合并时给出清晰拒绝。
- [ ] `diff AUTO_MERGE` 可用（tree vs worktree/index 的语义在卡内写明并与 Git `git diff AUTO_MERGE` 行为注释对照）。
- [ ] compat pseudo-ref surface 契约重构（显式名称集合 + resolve/reject 分组）且测试通过；文档同步。

**判据规范（非计数正文，EX-01 门族，共 14 门）：**

- G1 冲突落盘时 AUTO_MERGE 指向结果树 — 承载 `merge_auto_merge_ref`
- G2 仅 merge 进行中可观察（clean merge 无常驻投影） — 承载 `merge_auto_merge_ref`
- G3 结果树 OID 持久化进 `MergeState`（serde 可选） — 承载 `merge_auto_merge_ref`
- G4 结果树纳入 GC root（故障注入） — 承载 `merge_auto_merge_ref`
- G5 seam opt-in 接线（三表面经同一 resolver） — 承载 `pseudo_ref::` 单测
- G6 未 opt-in 调用方零变化：update-ref 对 pseudo-ref 继续拒绝 — 承载 update-ref 具名回归（模块名开工时刷新）
- G7 `rev-parse AUTO_MERGE` 输出 tree id — 承载 `merge_auto_merge_ref`
- G8 `cat-file -t AUTO_MERGE` 返回 `tree` — 承载 `merge_auto_merge_ref`
- G9 无进行中合并时清晰拒绝 — 承载 `merge_auto_merge_ref`
- G10 `diff AUTO_MERGE` 可用 — 承载 `merge_auto_merge_ref`
- G11 compat 名称提取改显式集合 — 承载 `compat_pseudo_ref_surface`
- G12 compat resolve/reject 分组（AUTO_MERGE 可解析 + 六 ref 拒绝） — 承载 `compat_pseudo_ref_surface`
- G13 `--continue`/`--abort`/`--quit` 幂等清除 — 承载 `merge_auto_merge_ref`
- G14 三命令文档契约一致：`rev-parse`/`cat-file`/`diff` 的 EN+zh 文档写明 AUTO_MERGE 为唯一可解析 pseudo-ref（仅冲突 merge 中）且既有六 ref 继续拒绝 — 承载 `compat_pseudo_ref_surface`（同文件新增 (new) 文档契约断言：六份文档（EN+zh × 3 命令）含 AUTO_MERGE 例外声明与六 ref 拒绝声明）

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_auto_merge_ref`（(new) 门 G1-G4、G7-G10、G13 端到端——场景矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --lib pseudo_ref::`（门 G5 单测 + pseudo_ref 回归 + (new) AUTO_MERGE 变体）
- [ ] `source .env.test && cargo test --test command_test command::update_ref_test::`（门 G6：update-ref 对 pseudo-ref 拒绝回归；模块名开工时刷新）
- [ ] `source .env.test && cargo test --test compat_pseudo_ref_surface`（(new) 门 G11-G12、G14：compat 契约更新后通过，含六份文档 AUTO_MERGE 例外声明断言；target 名开工时刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `T-1: GC-02 单一事实源扩展（pseudo-ref 接入共享对象解析 seam）+ 公开 pseudo-ref 表面变化`

**Dependencies:** MG-18（串行链前驱）

**Implementation write set:** `src/internal/pseudo_ref.rs`、`src/command/merge.rs`、`src/utils/util.rs`（resolver seam opt-in，`:2124`）、`src/command/rev_parse.rs`、`src/command/cat_file.rs`、`src/command/mod.rs`、`src/command/diff.rs`（tree-ish 分支）、`tests/compat/pseudo_ref_surface.rs`、`tests/command/merge_test.rs`、`tests/command/update_ref_test.rs`（拒绝回归，模块名开工时刷新）、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/commands/rev-parse.md`、`docs/commands/zh-CN/rev-parse.md`、`docs/commands/cat-file.md`、`docs/commands/zh-CN/cat-file.md`、`docs/commands/diff.md`、`docs/commands/zh-CN/diff.md`（三命令的 AUTO_MERGE 例外与六 ref 拒绝声明）、`docs/development/commands/merge.md`、`docs/development/commands/rev-parse.md`、`docs/development/commands/cat-file.md`、`docs/development/commands/diff.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（3 落点：`src/internal/`、`src/command/`、`src/utils/`；7 生产文件：pseudo_ref.rs、merge.rs、util.rs、rev_parse.rs、cat_file.rs、mod.rs、diff.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=AUTO_MERGE 投影与 tree-ish 消费; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=14/8@EX-01; VER=5/8; landing=3; prod-files=7; scope=M; deps=MG-18; writeset=序列化于 MG-18; release=independent; split-from=MG-17; exception=EX-01`

### Task MG-22: rerere autoupdate 三态覆盖（inherit/on/off）与持久化

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 将 rerere autoupdate 的判定从 `flag || config` 二态（`rerere.rs:126-158`，无法表达显式关闭）升级为三态 override：clap 层 `Option<bool>` 式 last-wins 解析（`--rerere-autoupdate` = on、`--no-rerere-autoupdate` = off、缺省 = inherit 配置）；override 持久化到各 sequencer 状态（merge-state.json、cherry-pick opts、rebase aux state，serde 默认值向后兼容），使冲突 + 新进程 `--continue` 后的后续提交保持同一 override；共享接口改造接入三个消费者：merge、rebase、cherry-pick（清单在此登记，不占 AC 计数）。（自 MG-17 拆出，G-09。）

**Out of scope:** rerere 引擎的规范化升级（由 MG-20 承接）；revert 的 flag 推广（非本卡）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `auto_update(bool)` 为 `flag \|\| config`，无法表达显式关闭 | `src/command/rerere.rs:126-158` |
| merge 的 `--no-rerere-autoupdate` 为 accepted no-op | `COMPATIBILITY.md:150` |
| cherry-pick 已把 `rerere_autoupdate` 布尔持久化进 opts 并 round-trip（三态改造的现状锚点） | `src/command/cherry_pick.rs:286-348` |
| rebase 的可恢复辅助状态 sidecar（`RebaseAuxState`，serde default 模式可复用） | `src/command/rebase.rs:74` 附近（开工时刷新精确行） |

**Acceptance criteria:**

- [ ] clap 层三态解析：`--rerere-autoupdate` / `--no-rerere-autoupdate` / 缺省 → on / off / inherit，同现 last-wins。
- [ ] on 当次强制 stage 回放 resolution（覆盖 `rerere.autoUpdate=false`）；off 当次强制不 stage（覆盖 `rerere.autoUpdate=true`，从 no-op 改为真实关闭）。
- [ ] override 持久化到 sequencer 状态：冲突后由**新进程**执行 `--continue` 的后续提交保持同一 override。
- [ ] 缺少新字段的旧版状态 JSON 可读（serde 默认值，GC-MG-14），三个状态面各有读回用例。
- [ ] rerere 未启用（`rerere.enabled` 未设置）时三态均为 no-op。

**判据规范（非计数正文，EX-01 门族，共 11 门）：**

- G1 clap 三态解析（on/off/inherit） — 承载 `merge_rerere_autoupdate`
- G2 两 flag 同现 last-wins — 承载 `merge_rerere_autoupdate`
- G3 on 覆盖 `rerere.autoUpdate=false` — 承载 `merge_rerere_autoupdate`
- G4 off 覆盖 `rerere.autoUpdate=true` — 承载 `merge_rerere_autoupdate`
- G5 merge：新进程 `--continue` 保持 override — 承载 `merge_rerere_autoupdate`
- G6 cherry-pick：多提交序列新进程续作保持 override — 承载 `cherry_pick_rerere_autoupdate`
- G7 rebase：多提交 replay 新进程续作保持 override — 承载 `rebase_rerere_autoupdate`
- G8 merge-state 旧 JSON 缺字段读回 — 承载 `merge_rerere_autoupdate`
- G9 cherry-pick opts 旧 JSON 缺字段读回 — 承载 `cherry_pick_rerere_autoupdate`
- G10 rebase aux 旧 JSON 缺字段读回 — 承载 `rebase_rerere_autoupdate`
- G11 rerere 未启用时三态均 no-op — 承载 `merge_rerere_autoupdate`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_rerere_autoupdate`（(new) 门 G1-G5、G8、G11——矩阵在本测试内逐格断言）
- [ ] `source .env.test && cargo test --test command_test command::cherry_pick_test::cherry_pick_rerere_autoupdate`（(new) 门 G6、G9）
- [ ] `source .env.test && cargo test --test command_test command::rebase_test::rebase_rerere_autoupdate`（(new) 门 G7、G10）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（回归）

**Full-suite trigger:** `T-1: GC-02 共享 helper（rerere autoupdate 接口被 merge/rebase/cherry-pick 共用）`

**Dependencies:** MG-21（串行链前驱）

**Implementation write set:** `src/command/rerere.rs`、`src/command/merge.rs`、`src/command/rebase.rs`、`src/command/cherry_pick.rs`（三消费者 flag 与持久化接线）、`tests/command/merge_test.rs`、`tests/command/cherry_pick_test.rs`、`tests/command/rebase_test.rs`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/commands/rebase.md`、`docs/commands/zh-CN/rebase.md`、`docs/commands/cherry-pick.md`、`docs/commands/zh-CN/cherry-pick.md`、`docs/development/commands/merge.md`、`docs/development/commands/rebase.md`、`docs/development/commands/cherry-pick.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点 `src/command/`；4 生产文件：rerere.rs、merge.rs、rebase.rs、cherry_pick.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=rerere autoupdate 三态覆盖; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=11/8@EX-01; VER=4/8; landing=1; prod-files=4; scope=M; deps=MG-21; writeset=序列化于 MG-21; release=independent; split-from=MG-17; exception=EX-01`

### Task MG-19: rebase 收敛到统一三路引擎

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 删除 rebase 的平行三路实现（`rebase.rs:4649` `resolve_three_way`、`:4756`），rebase 的 replay 改经 merge 的统一引擎（含 rename、driver、recursive、精化全部能力）；rebase 特有语义（stage 角色对调、`--autostash`、sequencer 状态）保持不变。本卡唯一行为轴：合并引擎单一事实源（GC-02）。

**Out of scope:** rebase 新能力（`--rebase-merges` 等，非目标）；cherry-pick/revert 收敛（它们已复用 merge 内容合并，本卡只做核对不扩 scope）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| rebase 平行实现不复用 merge（含内联单测 `resolve_three_way_merges_and_conflicts`） | `src/command/rebase.rs:4649`、`:4756`、`:4379` |
| merge 引擎经 MG-02/MG-05/MG-08/MG-10 后具备 superset 能力 | 本计划 MG-02、MG-05、MG-08、MG-10 |

**Acceptance criteria:**

- [ ] `rebase.rs` 的 `resolve_three_way` 删除（零命中守卫断言）。
- [ ] `rebase.rs` 的 `replay_commit_with_conflict_detection` 删除（零命中守卫断言）。
- [ ] rebase replay 改经 merge 的统一引擎（路由唯一，无平行三路实现残留）。
- [ ] rename 能力在 rebase 生效（(new) 用例）。
- [ ] driver 能力在 rebase 生效（(new) 用例）。
- [ ] recursive 能力在 rebase 生效（多 base 场景 replay，(new) 用例）。
- [ ] 精化能力在 rebase 生效（(new) 用例）。
- [ ] 既有 `command::rebase_test` 全绿。
- [ ] rebase 的 `--ours`/`--theirs` stage 角色对调语义在统一引擎下保持（参照 `COMPATIBILITY.md:129` 角色说明）。
- [ ] `docs/commands/rebase.md` EN+zh 同步统一引擎带来的用户可见行为说明。

（过程性义务，不计入 AC 门族：语义升级处逐用例登记修订历史（G-09），开发文档 `docs/development/commands/rebase.md` 收敛说明同步——随 Docs 默认与 G-09 执行。）

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 `resolve_three_way` 零命中 — 承载 Verification 第 ③ 条 `rg` 守卫命令（按判定口径模板，`>1` 失败分支可验证）
- G2 `replay_commit_with_conflict_detection` 零命中 — 承载 Verification 第 ④ 条 `rg` 守卫命令（按判定口径模板，`>1` 失败分支可验证）
- G3 rebase replay 经统一引擎路由 — 承载 `command::rebase_test::`（全量回归）
- G4 rename 能力生效 — 承载 `rebase_rename`（(new)，`command::rebase_test::` 内）
- G5 driver 能力生效 — 承载 `rebase_driver`（(new)，`command::rebase_test::` 内）
- G6 recursive 能力生效 — 承载 `rebase_recursive`（(new)，`command::rebase_test::` 内）
- G7 精化能力生效 — 承载 `rebase_refine`（(new)，`command::rebase_test::` 内）
- G8 既有用例全绿 — 承载 `command::rebase_test::`（全量回归）
- G9 stage 角色对调语义保持 — 承载 `command::rebase_test::`（既有角色用例）
- G10 rebase.md EN+zh 文档契约 — 承载 `compat_command_docs_examples_section`（文档契约；target 名开工时刷新）

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::rebase_test::`（全量回归，含 (new) `rebase_driver`/`rebase_recursive`/`rebase_refine`）
- [ ] `source .env.test && cargo test --test command_test command::rebase_test::rebase_rename`（(new) rename 用例）
- [ ] 门 G1 零命中守卫：`if rg -n "resolve_three_way" src/command/rebase.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi`
- [ ] 门 G2 零命中守卫：`if rg -n "replay_commit_with_conflict_detection" src/command/rebase.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi`
- [ ] `source .env.test && cargo test --test compat_command_docs_examples_section`（(new) 门 G10 文档契约；target 名开工时刷新）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::`（merge 侧回归）

**Full-suite trigger:** `T-1: GC-02 单一事实源收敛（merge/rebase 共享引擎）`

**Dependencies:** MG-02、MG-05、MG-08、MG-10（统一引擎能力前置）、MG-22（串行链前驱）

**Implementation write set:** `src/command/rebase.rs`、`src/command/merge.rs`（引擎接口导出）、`tests/command/rebase_test.rs`、`docs/commands/rebase.md`、`docs/commands/zh-CN/rebase.md`、`docs/development/commands/rebase.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点 `src/command/`；2 生产文件：rebase.rs、merge.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=引擎单一事实源; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=6/8; landing=1; prod-files=2; scope=M; deps=MG-02,MG-05,MG-08,MG-10,MG-22; writeset=序列化于 MG-22; release=independent; split-from=N/A; exception=EX-01`

### Task MG-20: rerere per-hunk 规范化

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** rerere 的冲突 ID 从「整文件字节级」升级为 Git 语义的规范化冲突文本（`rerere.c:340` `handle_conflict`）：扫描文件内冲突 marker，丢弃 diff3 base 段、两侧 hunk 按字典序排序后拼接，对其哈希得 conflict ID。本卡唯一行为轴：rerere 匹配规范化。

**Out of scope:** 命令面扩展（status/diff/forget/clear/gc 已存在，非目标）；存量 resolution 的迁移（新旧 ID 并存：旧 ID 未命中即按新 ID 重新记录，无迁移）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `conflict_id` 为全文件 SHA-256，无 per-hunk 规范化 | `src/command/rerere.rs:414-419`；`COMPATIBILITY.md:174` |
| Git 参照：`handle_conflict` 规范化 + `try_merge` 回放 | git@`3cb9185f6` `rerere.c:340`、`:598` |

**Acceptance criteria:**

- [ ] 冲突 ID 按 hunk 提取 + 排序 + 去 base 段规范化计算。
- [ ] ours/theirs 互换的同一冲突命中同一 ID。
- [ ] 回放路径（preimage→postimage 变更应用于当前冲突的三方合并）与 Git `try_merge` 语义对齐；无法干净回放时不写回（fail-safe）。
- [ ] 无 marker 的冲突文件按整文件哈希回退（兼容旧行为）。
- [ ] merge/rebase/cherry-pick 自动集成与 `--continue` 记录 postimage 路径行为不变（具名回归）。

**判据规范（非计数正文，EX-01 门族，共 10 门）：**

- G1 冲突 hunk 提取 — 承载 `rerere::normalize` 单测
- G2 两侧 hunk 字典序排序 — 承载 `rerere::normalize` 单测
- G3 diff3 base 段丢弃 — 承载 `rerere::normalize` 单测
- G4 ours/theirs 互换命中同一 ID — 承载 `rerere::normalize` 单测
- G5 回放与 Git `try_merge` 语义对齐 — 承载 `command::rerere_test::`
- G6 无法干净回放时不写回 — 承载 `command::rerere_test::`
- G7 无 marker 文件整文件哈希回退 — 承载 `rerere::normalize` 单测
- G8 merge 自动集成行为不变 — 承载 `merge_rerere`
- G9 rebase 自动集成行为不变 — 承载 `command::rebase_test::`（T-1 全量）
- G10 cherry-pick 自动集成与 `--continue` postimage 行为不变 — 承载 `command::cherry_pick_test::`

**Verification:**

- [ ] `source .env.test && cargo test --test command_test command::rerere_test::`（门 G5、G6 + 既有回归）
- [ ] `source .env.test && cargo test --lib rerere::normalize`（(new) 门 G1-G4、G7 单测）
- [ ] `source .env.test && cargo test --test command_test command::merge_test::merge_rerere`（门 G8 merge 集成回归）
- [ ] `source .env.test && cargo test --test command_test command::cherry_pick_test::`（门 G10 cherry-pick 集成回归）

**Full-suite trigger:** `T-1: GC-02 共享 helper（rerere 被 merge/rebase/cherry-pick sequencer 共用）`

**Dependencies:** MG-19（串行链前驱）

**Implementation write set:** `src/command/rerere.rs`、`tests/command/rerere_test.rs`、`docs/commands/rerere.md`、`docs/commands/zh-CN/rerere.md`、`docs/commands/merge.md`、`docs/commands/zh-CN/merge.md`、`docs/commands/rebase.md`、`docs/commands/zh-CN/rebase.md`、`docs/commands/cherry-pick.md`、`docs/commands/zh-CN/cherry-pick.md`（G8-G10 三消费者的 rerere 集成面说明）、`docs/development/commands/rerere.md`、`docs/development/commands/merge.md`、`docs/development/commands/rebase.md`、`docs/development/commands/cherry-pick.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Rollback mode:** `immutable-release`

**Estimated scope:** `M`（1 落点；1 生产文件：rerere.rs）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组适用计划级统一登记契约）

**Granularity:** `type=implementation; axis=rerere 规范化; recovery=前滚修复（immutable-release）; complete=yes; self-contained=yes; AC=10/8@EX-01; VER=4/8; landing=1; prod-files=1; scope=M; deps=MG-19; writeset=序列化于 MG-19; release=independent; split-from=N/A; exception=EX-01`

## 测试矩阵

本表登记「本计划最终必须被覆盖到什么」，不是「每张卡都要跑完整张表」。执行阶段按 ER-13 只跑与本卡相关的行；整张表由收口阶段的全量门统一覆盖。

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | merge base 染色/保守终止/skewed 日期/一致性对拍；recursive 折叠与 depth；剪枝对象计数；rename 三元组重定向与冲突分类；driver 分派；占位符展开（含 sq_quote）；精化层；空白归一化；octopus base；rerere 规范化；AUTO_MERGE 投影 | `cargo test --lib merge_base::` / `merge::<filter>` / `rename_detect::` / `rerere::normalize` / `mergetool::` / `pseudo_ref::` |
| 集成 | merge 全场景（ff/no-ff/squash/no-commit/continue/abort/dry-run/restart/autostash + rename/D-F/crisscross/octopus/driver/whitespace/签名/消息面/quit/AUTO_MERGE） | `cargo test --test command_test command::merge_test::` |
| 集成 | mergetool 假工具端到端 | `cargo test --test command_test command::mergetool_test::`（MG-14 新模块） |
| 兼容 | help/banner/文档契约、错误码同步、配置级联、pseudo-ref surface | `cargo test --test compat_help_examples_banner` / `compat_error_codes_doc_sync` / `compat_config_history_defaults` / `compat_command_docs_examples_section` / `compat_pseudo_ref_surface`（target 名开工时刷新） |
| 兼容 | rebase 收敛回归、cherry-pick/revert/merge-file/rerere/update-ref 继承回归 | `cargo test --test command_test command::rebase_test::` / `cherry_pick_test::` / `revert_test::` / `merge_file_test::` / `rerere_test::` / `update_ref_test::` |
| 迁移 | merge-state.json 与 sequencer 状态旧 schema 可读（GC-MG-14） | `cargo test --lib merge::state`（既有模式延续）+ MG-22 三状态面读回用例（new） |
| 安全 | gitlink fail-closed（两档语义）；外部 driver sq_quote 注入回归与临时文件权限；mergetool 工具解析 | `cargo test --test command_test command::merge_test::merge_gitlink` / `merge_ext_driver` / `mergetool_test::` |
| 性能 | merge base ≥10^4 提交；树合并 ≥10^5 文件 1% 变化 | `cargo test --lib merge_base::bench_merge_base_scaling` / `merge::tree`（卡内断言） |
| live/gated | N/A（本计划不触外部服务） | N/A |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| MG-00 | `merge.rs:2666`、`rebase.rs:3842` vs `merge.rs:3495` | `src/command/{merge,rebase,cherry_pick}.rs`、`src/utils/error.rs` | `docs/error-codes.md`、merge/rebase/cherry-pick.md EN+zh、开发文档、COMPATIBILITY | `command::merge_test::merge_gitlink*`（new） |
| MG-01 | git `commit-reach.c:187`；`merge_base.rs:72` | `src/internal/merge_base.rs` | merge-base.md EN+zh、开发文档 | `command::merge_base_test::` + `merge_base::*`（new） |
| MG-02 | git `merge-ort.c:5313` | `src/command/merge.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_crisscross*`（new） |
| MG-03 | git `merge-ort.c:1259`；`merge.rs` `fn commit_tree_split`（收口时 `:3181`） | `src/command/merge.rs` | 开发文档 | `merge::tree*`（new） |
| MG-04 | git `merge-ort.c:4100-4198`（`unique_path` `:923`） | `src/command/merge.rs`（`resolve_df_conflicts` / `conflict_placements` / `relocate_virtual_df_files`） | merge.md EN+zh、pull.md EN+zh（继承）、COMPATIBILITY（merge + pull 行）、`docs/development/commands/merge.md` | `command::merge_test::merge_df_conflict*`、`merge_dry_run_reports_*`、`merge_crisscross_df_collision_inside_the_fold_moves_the_file_like_git`、`--lib merge::df`（new） |
| MG-05 | git `merge-ort.c:3429`；`rename_detect.rs:253` | `src/command/merge.rs`、`rename_detect.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_rename*`（new） |
| MG-06 | git `merge-ort.c:2394`、`:2913` | `src/command/merge.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_rename_conflict*`（new） |
| MG-07 | git `merge-ort.c:2467-2912` | `src/command/merge.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_dir_rename*`（new） |
| MG-08 | git `merge-ll.c:361-390`；`revert.rs:960-963` | `src/command/{merge,merge_file,cherry_pick,revert}.rs` | merge/merge-file/cherry-pick/revert.md EN+zh、对应开发文档、COMPATIBILITY | `command::merge_test::merge_driver*` + 三消费者具名（new） |
| MG-09 | git `merge-ll.c:191-277` | `src/command/merge.rs` | merge.md EN+zh、开发文档、COMPATIBILITY | `command::merge_test::merge_ext_driver*`（new） |
| MG-10 | git `xdiff/xmerge.c:334/363/467`；`revert.rs:960-963` | `src/command/{merge,cherry_pick,revert}.rs` | merge/cherry-pick/revert.md EN+zh、对应开发文档、COMPATIBILITY | `command::merge_test::merge_conflict_refine*`（new） |
| MG-11 | git `merge-ort.c:5551-5608`；`diff.rs:5930-5960` | `src/command/merge.rs`、`diff.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_x_whitespace*`（new） |
| MG-12 | git `git-merge-octopus.sh`、`commit-reach.c:326` | `src/command/merge.rs`、`src/internal/merge_base.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_octopus*`（new） |
| MG-13 | git `builtin/merge.c:101`、`:789`、`:1783` | `src/command/merge.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_strategy*`（new） |
| MG-14 | git `git-mergetool.sh` | `src/command/mergetool.rs`、`src/cli.rs` | mergetool.md EN+zh（新建）、开发文档（新建）、COMPATIBILITY、tests/INDEX.md（若失真） | `command::mergetool_test::`（new 模块） |
| MG-15 | `commit.rs:215-224`；git `-S` | `src/command/merge.rs` | merge.md EN+zh、开发文档、COMPATIBILITY | `command::merge_test::merge_gpg_sign*`（new） |
| MG-16 | git `--signoff`；`COMPATIBILITY.md:158` | `src/command/merge.rs` | merge.md EN+zh、开发文档、COMPATIBILITY | `command::merge_test::merge_signoff*`（new） |
| MG-17 | git `builtin/merge.c:1417-1470`、`branch.c:863` | `src/command/merge.rs` | merge.md EN+zh、COMPATIBILITY | `command::merge_test::merge_quit*`（new） |
| MG-18 | git `--edit`/`-F`/`--into-name`/`--cleanup`；`COMPATIBILITY.md:172` | `src/command/merge.rs` | merge.md EN+zh、开发文档、COMPATIBILITY | `command::merge_test::merge_message_options*`（new） |
| MG-21 | git `merge-ort.c:4960`；`pseudo_ref.rs:46`、`:70`；`util.rs:2124`、`update_ref.rs:452/:461`、`pseudo_ref_surface.rs:38/:113/:135` | `src/internal/pseudo_ref.rs`、`src/utils/util.rs`、`src/command/{merge,rev_parse,cat_file,mod,diff}.rs` | merge/rev-parse/cat-file/diff.md EN+zh、对应开发文档、COMPATIBILITY、compat 契约重构 | `command::merge_test::merge_auto_merge_ref*`（new） |
| MG-22 | `rerere.rs:126-158`；`cherry_pick.rs:286-348`；`COMPATIBILITY.md:158` | `src/command/{rerere,merge,rebase,cherry_pick}.rs` | merge/rebase/cherry-pick.md EN+zh、对应开发文档、COMPATIBILITY | `command::merge_test::merge_rerere_autoupdate*` + 两消费者具名（new） |
| MG-19 | `rebase.rs:4649`、`:4756` | `src/command/rebase.rs`、`merge.rs` | rebase.md EN+zh、开发文档、COMPATIBILITY | `command::rebase_test::` 全量 + `rebase_rename*`（new） |
| MG-20 | git `rerere.c:340`、`:598`；`rerere.rs:414-419` | `src/command/rerere.rs` | rerere/merge/rebase/cherry-pick.md EN+zh、对应开发文档、COMPATIBILITY | `rerere::normalize*`（new） |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | Phase 0 完成：gitlink fail-closed 上线 | MG-00 `complete` + `gh release` 证据 | 前滚修复并发布新 patch（immutable-release） |
| M1 | Phase 1 完成：rename/criss-cross/D-F/增量合并与 Git 语义一致 | MG-01~MG-07 全部 `complete`；`merge_rename*`/`merge_crisscross*` 绿 | 前滚修复并发布新 patch |
| M2 | Phase 2 完成：driver/精化/whitespace 对齐 | MG-08~MG-11 `complete` | 前滚修复并发布新 patch |
| M3 | Phase 3 完成：octopus/策略/mergetool/签名/状态面/消息面 | MG-12~MG-18、MG-21、MG-22 `complete`；COMPATIBILITY merge 行缺口收敛 | 前滚修复并发布新 patch |
| M4 | 收口：rebase 收敛 + rerere 规范化 + 全量门全绿 + Codex PASS | MG-19/MG-20 `complete`；全量收口门记录 | 前滚修复（ER-13 收口口径） |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| recursive 虚拟祖先写入对象库后、合并完成前崩溃 | 虚拟 tree/commit 成为 loose object | 由 merge state 的 target/base 在 `--restart`/`gc` 时重算或回收（一次性对象，ADR-MG-04） | gc 回收后 `--restart` 无法重算恢复；半成品 merge state 被当作干净 |
| 外部 driver 执行中信号中断 | 0700 临时目录残留 | 下次 merge 启动时清理或权限隔离 | 残留文件含敏感内容且权限宽松；index 半成品 |
| octopus 中途某 head 冲突 | 无（原子拒绝） | 整体失败，用户改用逐分支 merge | 部分 head 已落 index/worktree |
| AUTO_MERGE 投影写入后崩溃 | 悬空 pseudo-ref（结果树 OID 在 MergeState 中，GC root 覆盖） | 状态清理路径（`--continue/--abort/--quit`）幂等删除 | GC 误删指向 tree；ref 永久残留；clean merge 留下常驻投影 |
| mergetool 工具非零退出 | `.orig` 备份（keepBackup 语义） | 路径保持未解决，用户重跑 | 未解决路径被静默 stage |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| rename 接入改变大量既有合并行为 | 高：回归面大 | 非 rename 场景零变化判据 + 全量回归 + T-1 全量门 | MG-05 |
| diffy 内核与 xdiff 的精化结果差异 | 中：冲突呈现与 Git 不逐字节一致 | ADR-MG-02 语义一致判据 + 用例注释出处 | MG-10 |
| merge.rs 单文件持续膨胀（已 3987 行） | 中：可维护性 | 卡内新增逻辑按内聚函数组织；整体拆分模块登记 DEFER-05 | 全部算法卡 |
| 外部 driver/mergetool 子进程安全面 | 中：命令注入/敏感残留 | sq_quote 契约 + argv 传递 + 0700 临时目录 + 脱敏 + 注入回归用例 | MG-09、MG-14 |
| octopus 原子性破坏既有单 head 路径 | 中 | 单分支零变化判据 + 原子性用例 | MG-12 |
| rebase 收敛引入行为漂移 | 高：rebase 是高频命令 | rebase_test 全量回归 + stage 角色钉住 + 语义升级逐条登记 | MG-19 |
| merge base 新算法结果与旧实现分歧 | 高：所有下游命令受影响 | 新旧对拍一致性测试 + T-1 全量门 | MG-01 |
| pseudo-ref 接入共享 seam 波及其余调用方 | 中：update-ref 等拒绝面被破坏 | ADR-MG-06 调用方 opt-in + 未 opt-in 拒绝回归 | MG-21 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| merge base（MG-01 后） | 单次双染色遍历，frontier 有界；无 generation 依赖的提前退出 | 每次 merge 一次 | ≥10^4 提交合成历史优于旧 BFS（实测断言）；提前退出随 commit-graph reader 另行登记 | `merge_base::bench_merge_base_scaling` |
| 树合并（MG-03 后） | O(变化路径) 对象读取 | 每次 merge 一次 | ≥10^5 文件 1% 变化：读取数随变化集而非全树 | `merge::tree` benchmark |
| rename 检测（MG-05 后） | exact O(n) + inexact 受预算截断 | 每次 merge 两次（双侧） | `RenameDetectConfig` 预算内；超限确定性降级 | `merge::rename` 预算单测 |
| recursive 折叠（MG-02 后） | O(base 数) 次递归合并 | criss-cross 场景 | depth 上限 + 显式错误 | `merge::recursive` 单测 |
| 外部 driver（MG-09 后） | 每冲突文件一次子进程 | 冲突文件数 | 无并发 fanout（串行）；临时文件有界 | `merge_ext_driver` |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md` merge 行（:150）更新为「主路径与 Git 对齐；剩余差异 = 永久非目标（subtree/submodule/gitlink）+ DEFER-01~14 登记项」（**不**宣称仅三项例外）；merge-base（:151）、rerere（:174）行同步；新增 mergetool 行。
- [ ] `docs/commands/merge.md` 已同步（rename/driver/octopus/策略/签名/消息/quit/AUTO_MERGE 各节）。
- [ ] `docs/commands/zh-CN/merge.md` 已同步。
- [ ] `docs/commands/mergetool.md` + zh-CN 已新建（MG-14）。
- [ ] `docs/commands/merge-file.md` / `cherry-pick.md` / `revert.md` / `rebase.md` / `rerere.md` EN+zh 的继承行为说明已同步（MG-00、MG-08、MG-10、MG-19、MG-20、MG-22）。
- [ ] `docs/commands/rev-parse.md` / `cat-file.md` / `diff.md` EN+zh 的 AUTO_MERGE 例外与六 ref 拒绝声明已同步（MG-21）。
- [ ] 各卡触达的 `docs/development/commands/*.md` 已同步：merge（多数卡）、rebase（MG-00/MG-19/MG-20/MG-22）、cherry-pick/revert（MG-00/MG-08/MG-10/MG-20）、merge-file（MG-08）、rerere（MG-20）、rev-parse/cat-file/diff（MG-21）、mergetool（MG-14 新建）。
- [ ] `docs/error-codes.md` 已同步（MG-00 及后续新增码）。
- [ ] `tests/INDEX.md` 已核对：`command_test` 行描述未失真，或已同步（MG-14 新模块）。
- [ ] `Cargo.toml` `[[test]]`：N/A（本计划不新增独立 `--test` target；mergetool 测试为 `command_test` 内新模块）。若执行中确需新增 compat 文件，再按 GC-06 注册并同步 `tests/compat/README.md`。
- [ ] `plan-long.md` 日期计划索引或 LR 状态已同步，或说明 `N/A`。

## Codex review log

Result 只允许 `PASS` 或 `FAIL`。`FAIL` 必须列出 P0/P1 条目并在下一轮复审关闭；P2 可由具名责任人书面接受为 residual risk，但不改变本轮 `FAIL` 记录（ER-05）。除 Codex 外，Claude Code 的独立评审轮次同样登记于本表（Round 列标注评审者）。Evidence 列以**归档标识** `libra-plan-review/<reviewer>-merge-plan-r<N>.txt` 登记评审输出；归档持久化于计划外目录（绝对路径不入本文档，ER-11；系统 /tmp 重启即清，不作为证据位置）。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R1 | 计划全文 | FAIL | 11 项 P1（逐项→修复映射：① 目标与 DEFER 矛盾→成功定义/COMPATIBILITY 措辞修订；② 串行窗口未落依赖边+漏 6 逻辑边+MG-16↔MG-18 循环→串行链+逻辑边落地、MG-16→MG-18；③ AC 未按谓词计数→全卡重写；④ 假回滚→immutable-release；⑤ D 组未登记→计划级 D 契约；⑥ cargo 双 filter→单 filter；⑦ MG-01 generation 无 reader→移除该 AC；⑧ MG-08 `merge.defaultDriver`/回退错误→`merge.default`+text 回退；⑨ MG-09 未 sq_quote→契约修正；⑩ 继承面 Verification 缺消费者+T-1 误标→补消费者回归+T-1 修正；⑪ MG-17 三轴混合+pseudo-ref 锚点错误→拆 MG-17/21/22） | P2（review log `待审` 措辞/Current evidence 锚点）已修；「测试矩阵标题重复」经 grep 核对不成立 | `codex exec`（codex-cli 0.152.0，read-only）输出存档 `libra-plan-review/codex-merge-plan-r1.txt` |
| R2 | 计划全文（R1 修订后） | FAIL | 8 项 P1（逐项→修复映射：① `--test merge_test` 等 target 不存在→全部 Verification 改 `command_test` 模块路径；② AC 仍压缩谓词→二次重写；③ MG-08 `merge.default` 语义→仅属性未设置时生效、`merge=<unknown>` 恒回退 text；④ D 组 homebrew best-effort 假阳性→硬判据限 build-and-upload/upload-install-scripts/CDN；⑤ MG-00 与 MG-03 剪枝矛盾+写集缺消费者→ADR-MG-01 两档语义+写集扩三消费者；⑥ MG-21 tree 无消费路径→增 tree-ish 设计；⑦ MG-22 无法表达显式 false→三态 override+T-1；⑧ MG-08 消费者无接线→写集加 cherry_pick.rs/revert.rs） | P2（pseudo_ref 锚点→`:46`/`:70`；R1 log 编号→1:1 映射）已修 | `codex exec`（codex-cli 0.152.0，read-only）输出存档 `libra-plan-review/codex-merge-plan-r2.txt` |
| R3 | 计划全文（R2 修订后） | FAIL | 7 项 P1（逐项→修复映射：① MG-08 非法 `merge.default` fail-closed 与 Git 相反→改「未命中回退 text」；② MG-21 tree-ish 无闭环→接入共享对象解析 seam；③ MG-22 override 无跨冲突持久化→持久化到三状态面+续作用例+文档；④ MG-08 三消费者缺具名测试与文档→补具名 driver 测试+EN+zh 文档+merge-file 仓库外恒 text；⑤ AC 谓词计数→三次重写；⑥ D 组误排 `request-stable-manifest`→加回硬判据；⑦ `gh release create --verify-tag` 顺序→ER-08 明确 tag 推送顺序） | 无 P2 | `codex exec`（codex-cli 0.152.0，read-only）输出存档 `libra-plan-review/codex-merge-plan-r3.txt` |
| R4 | 计划全文（R3 修订后） | FAIL | 5 项 P1（逐项→修复映射：① MG-21 seam 写集不具名+prod-files 错误→具名 `src/utils/util.rs:2124`、prod-files 7、landing 3；② MG-08 merge-file 无具名测试→`command::merge_file_test::merge_file_driver`（new）；③ MG-22 无新进程/旧状态验收→三具名测试补「新进程 continue + 旧 JSON 缺字段读回」；④ 全卡 AC 仍按行数计→三次原子化重写（枚举下沉 Description/Verification，审计表分子同步）；⑤ MG-21 的 clean merge AUTO_MERGE 生命周期与投影架构矛盾→修正为「仅 merge 进行中可观察」，结果树 OID 持久化进 `MergeState` 作 GC root） | 无 P2 | `codex exec`（codex-cli 0.152.0，read-only）输出存档 `libra-plan-review/codex-merge-plan-r4.txt` |
| R5 | 计划全文（R4 修订后） | FAIL | 4 项 P1（逐项→修复映射：① AC 仍超 G-03 上限且未用豁免通道→16 张门族卡登记 EX-01（模板 G-03 门族型验收白名单），每卡新增「判据规范」门族块逐门挂具名测试命令，分子如实写 `n/8@EX-01`；② MG-21 公共边界未闭合（compat `_HEAD` 启发式+全量拒绝断言冲突、update-ref 共享 resolver 会接受 tree OID）→ADR-MG-06 调用方 opt-in + compat 重构（显式名称集合、resolve/reject 分组）+ update-ref 拒绝回归；③ prod-files 与具名写集不符→全卡改为真实计数（MG-09 具名 `src/utils/atomic_write.rs`、MG-11 具名 `src/command/diff.rs`）；④ 缺 `## 文档职责` 标题→已补） | 无 P2 | `codex exec`（codex-cli 0.152.0，read-only）输出存档 `libra-plan-review/codex-merge-plan-r5.txt` |
| R6 | 计划全文（R5 修订后） | FAIL | 2 项 P1（逐项→修复映射：① EX-01 准入不全（Approver 非具名 + MG-07/08/09 门数不实）→Approver 改具名 Eli Ma <eli@patch.sh>（libra 配置身份），MG-07 补 G11（→11 门）、MG-08 补 G15（→15 门）、MG-09 补 G19（→19 门），卡内分子/审计表/waiver 同步；② MG-21 新增 rev-parse/cat-file/diff 三公开表面未带文档→写集补三命令 EN+zh 文档，门族补 G14 文档契约（→14 门）） | 无 P2 | `codex exec`（codex-cli 0.152.0，read-only）输出存档 `libra-plan-review/codex-merge-plan-r6.txt` |
| R7 | 计划全文（R6 修订后） | FAIL | 4 项 P1（逐项→修复映射：① MG-21 G14 无可执行文档承载→改由 `compat_pseudo_ref_surface` 同文件 (new) 文档契约断言承载+写集补三命令开发文档；② 非 EX-01 卡 AC 仍压缩→MG-03/MG-15 登记入 EX-01（门族卡 18 张，各 10 门），MG-15/16/17 scope S→M；③ Docs 默认与 MG-19 写集矛盾→默认改「按本卡公开表面」+MG-19 补 rebase EN+zh 文档与文档 AC；④ MG-08 锚点缺 file:line→补 `attributes.rs:267`）+ 1 P2（Approver 身份断言与 git config 不一致→删除配置身份断言） | P2 已修（同左⑤） | `codex exec`（codex-cli 0.152.0，read-only）输出存档 `libra-plan-review/codex-merge-plan-r7.txt` |
| R8 | 计划全文（R7 修订后） | FAIL | 4 项 P1（逐项→修复映射：① EX-01 waiver Review round=R5 对 MG-03/MG-15 不实→改「R5、R7（MG-03/MG-15 于 R7 补登）」；② MG-15 G10 无可执行承载→Verification 增 `cargo test --lib merge::signing`（new），VER 3→4 同步 Granularity 与审计表，MG-00 G7 补「（T-1 全量）」承载标注；③ R7 的 Docs 默认未一致落卡→MG-00/MG-08/MG-10/MG-14/MG-15/MG-16/MG-18/MG-20/MG-22 写集补消费者 EN+zh 与对应开发文档，MG-14 补开发文档新建，收口清单扩容；④ 非 EX-01 卡 AC 压缩→MG-01（4→6）、MG-02（7→8）、MG-16（3→5）、MG-17（3→5）、MG-19（6→8）原子化重写并同步 Granularity 与审计表） | 4 项 P2 已修：① MG-09 Verification 补 G19、MG-21 补 G14 (new)；② `待复审` 违反模板 Result 取值→本轮双评审 FAIL 记录在案；③ MG-03 拍平旧路径生命周期写明（保留承载对拍门，下线随 DEFER-05）；④ 交叉表同步（评审结论表 16→18、默认覆盖表删除已按默认覆盖的 MG-08/MG-22 行、追溯表 MG-00/08/10/14/15/16/18/19/20/21/22 文档列补齐、收口清单补 rev-parse/cat-file/diff 与开发文档行）+ 锚点刷新（diff.rs:4089→:4104、status.rs 补 :2459、pseudo_ref_surface :38 语义与 :135 拒绝断言、cherry_pick_test :2049→:2065）+ review log 证据迁移至持久目录 `libra-plan-review/`（CLAUDE.md 证据卫生要求，/tmp 重启即清） | Claude Code（claude 2.1.259，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r8.txt` |
| R9 | 计划全文（R8 修订后） | PASS | none（Codex：P0/P1/P2 全 none；Claude Code：P0/P1 none，1 项 P2） | Claude Code P2（MG-21 Current evidence `pseudo_ref_surface.rs:131`→`:135`，与事实基线锚点刷新对齐）已修；R7/R8 全部 P1/P2 关闭情况经双评审逐项核实 | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r9.txt` |
| R10 | 计划全文（R9 修订后；Codex 1 项 P1，Claude Code 0 P1 + 2 项 P2） | FAIL | 1 项 P1（Codex）：ADR-MG-04「虚拟 base 不写入 merge-state.json」与 MG-02「GC root 覆盖不误删」矛盾——`merge_state_gc_oids`（merge.rs:367）只收集语义 OID 字段，未持久化的虚拟祖先不可达，保留模型不可执行→采用「一次性对象 + 重算恢复」契约：ADR-MG-04 Consequences 改写（GC 回收为预期行为，恢复路径仅为 `--restart` 重算）、MG-02 Description/AC7/Verification 矩阵与 maintenance_test 注释同步、故障恢复矩阵 recursive 行同步 | 2 项 P2 已修（Claude Code）：① 事实基线 revert.rs `:956-963`→`:960-963`（与 MG-08/MG-10 证据行对齐）；② `_HEAD` 启发式锚点 `:109`→`:113`（事实基线、GAP-21、MG-21 证据、追溯表四处） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r10.txt` |
| R11 | 计划全文（R10 修订后；Codex 2 项 P1 + 1 项 P2，Claude Code 0 P1 + 2 项 P2） | FAIL | 2 项 P1（Codex，已修）：① ADR-MG-04 声称 `--continue` 重算虚拟祖先不可执行（`run_merge_continue` 只加载 state/index 收尾，merge.rs:2291-2356）→恢复路径改为仅 `--restart` 重算，ADR 写明 `--continue` 不重算；② MG-07 排除的目录 rename × rename/rename 角落案例未在 DEFER 登记、违反成功定义→登记 DEFER-07（非目标 bullet + DEFER 表 + 成功定义/COMPATIBILITY 口径改 DEFER-01~07 + MG-07 Out of scope 引用） | 已修：Codex P2（R10 记录 AC5→AC7 更正）；Claude Code P2①（MG-14 卡内补 `Docs and compatibility impact` 字段行）②（非目标 bullet 补 DEFER-05） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r11.txt` |
| R12 | 计划全文（R11 修订后；Codex 3 项 P1 + 1 项 P2，Claude Code 0 P1 + 2 项 P2） | FAIL | 3 项 P1（已修）：① MG-20 写集缺 G8-G10 三消费者的 EN+zh 与开发文档→补 merge/rebase/cherry-pick 六份用户文档 + 三份开发文档，收口清单与追溯表同步（R8 行对 MG-20 的概括至此完全成立）；② MG-02 写集含共享 `src/internal/merge_base.rs`（仅消费）但 trigger=none，违反模板 T-1 客观命中规则→该文件移出写集（本卡仅消费 MG-01 已就绪的多 base 接口，不修改），Estimated scope/Granularity/审计表 landing 2→1、prod-files 2→1；③ MG-09 写集含共享 `src/utils/atomic_write.rs`（复用/扩展）但 trigger=none，同规则→临时文件实现收敛于 merge.rs（0700、不可预测名），共享 helper 仅只读复用并移出写集，写集注明「若开工确需扩展共享 helper 则按 ER-02 刷新并改 T-1」，landing/prod-files 2→1 同步 | 已修：Codex P2（R10/R11 行 Result 复合值→FAIL，评审者分歧移至 Scope 列）；Claude Code P2①（事实基线 merge 行转述删「rename」并注明由 GAP-06 独立证据支撑）②（非目标 DEFER-06 bullet 补「cherry-pick 的 rename 接入」） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r12.txt` |
| R13 | 计划全文（R12 修订后；Codex 2 项 P1，Claude Code 0 P0/P1/P2 全 none） | FAIL | 2 项 P1（已修）：① 非 EX-01 卡 AC 仍压缩多谓词（模板 G-03「且」连接须分列）：MG-01 染色/日期队列/提前退出压为一项→拆 3（4→8 门，仍 ≤8）；MG-02 一次性对象/gc 回收/`--restart` 恢复压为一项→拆 3（8→10 门，超限）；MG-19 引擎的 rename/driver/recursive/精化生效压为一项→拆 4（8→10 门，超限）→MG-02/MG-19 登记 EX-01 门族（20 张卡），各新增「判据规范」块逐门挂具名测试命令，MG-19 增 (new) `rebase_driver`/`rebase_recursive`/`rebase_refine` 用例与 `compat_command_docs_examples_section` 门 G10（VER 4→5），修订历史登记移为卡内过程性义务（不计 AC）；② MG-09 追溯表仍列 `src/utils/atomic_write.rs` 与 R12 移出写集矛盾→追溯表落点删除该文件并补开发文档 | 无 P2 | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r13.txt` |
| R14 | 计划全文（R13 修订后；Codex 2 项 P1，Claude Code 0 P1 + 2 项 P2） | FAIL | 2 项 P1（已修）：① MG-14 把 `mergetool.<tool>.cmd` 宣称为 argv 传递/禁 shell，与 Git pin 语义（shell 求值 + `$BASE/$LOCAL/$REMOTE/$MERGED` 变量，`Documentation/config/mergetool.adoc:5`、`git-mergetool--lib.sh:171`）矛盾且会破坏既有 `cmd='tool "$LOCAL" ...'` 配置、漏 `trustExitCode`/`.path`→按 Git 信任模型重写 MG-14（Description/AC 7→9/门族 13→15：G5 shell 求值+变量、G6 `.path`、G7 marker 判定、G9 trustExitCode=true，Security 字段同步信任边界），AC 13→15@EX-01；② MG-19 G1/G2 零命中守卫无可复制执行命令→Verification 拆为两条完整 `rg` 守卫命令（含 `>1` 失败分支），VER 5→6 同步 Granularity 与审计表 | 2 项 P2 已修（Claude Code）：① MG-08 Current evidence 的 gitattributes 消费面示例失实（diff.rs 只消费 `diff` 属性，无 text/eol）→改 `diff_driver_for_path`（diff.rs:2728/:2734）+ `attribute_state_for_path`（check_attr.rs:177）；② release.yml `set -euo pipefail` 锚点 `:726-730`→`:732`（事实基线与 D 组契约两处） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r14.txt` |
| R15 | 计划全文（R14 修订后；Codex 2 项 P1，Claude Code 0 P1 + 1 项 P2） | FAIL | 2 项 P1（已修）：① MG-14 `trustExitCode=false` 语义写成「按 marker 判定」仍与 Git 不符（默认实为 `$MERGED` mtime 变化判定 + 未变化时询问，`git-mergetool--lib.sh:143`/`:334`）→AC5/G7/G9 改写为「默认 mtime + 未变更询问；trustExitCode=true 仅看退出码」；② 成功定义「仅余 DEFER-01~07」与卡内未登记的 Git 表面矛盾→登记 DEFER-08（`merge.<name>.recursive` 低层 driver，修正 MG-09 对其的误述）、DEFER-09（`-X find-renames`/`rename-threshold`）、DEFER-10（显式 `-s octopus`）、DEFER-11（`mergetool.*` 其余配置），非目标 bullet、DEFER 表、成功定义与收口口径改 DEFER-01~11 | 1 项 P2 已修（Claude Code）：MG-17 的 T-1「保守判定」补区别理由（--quit 非累加式、删除状态与投影；MG-13/15/16/18 累加式 flag 不适用该口径） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r15.txt` |
| R16 | 计划全文（R15 修订后；Codex 4 项 P1 + 1 项 P2，Claude Code 0 P1 + 1 项 P2） | FAIL | 4 项 P1（已修）：① MG-01「committer-date 排序 + 提前退出」不可靠——Git 早退以 generation 拓扑序为前提（`commit-reach.c:194` 注释），本仓库无 commit-graph reader→删除提前退出承诺，改为保守全量染色终止，性能判据改写（单次双染色遍历替代两次遍历+交集运算），generation 依赖的提前退出随 commit-graph reader 另行登记（Out of scope/性能摘要同步）；② MG-01 五个调用面压缩为一个 AC→逐调用面拆分并入 EX-01 门族（11 门，含 skewed 日期回归与 diff/rebase/log 具名回归，VER 4→7），门族卡 20→21 张（waiver/粒度规则/完成判据/评审结论同步）；③ MG-14 非交互分支未闭合→AC 拆出「mtime 未变且 stdin EOF/非交互 = 未解决（非零退出、不得 stage）」，门族 15→16（G8 承载非交互注入场景），AC 9→11；④ 成功定义「仅余 DEFER-01~11」仍漏 MG-15 外部 GPG/SSH 签名、MG-18 默认不开编辑器与 `merge.suppressDest`→登记 DEFER-12/13/14（非目标 bullet + DEFER 表 + Out of scope 引用 + 口径改 DEFER-01~14） | 已修：Codex P2（MG-14 文档锚点 `config/mergetool.adoc`→`Documentation/config/mergetool.adoc:5`，R14 行同步）；Claude Code P2（`update_ref.rs:401` 落在 `resolve_new_value`（:421 有 commit 类型检查，与主张相反）→四处引用改 `:452`（`resolve_old_value` 起始）+ `:461`（resolver 调用点）） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r16.txt` |
| R17 | 计划全文（R16 修订后；Codex 4 项 P1，Claude Code 0 P1 + 2 项 P2） | FAIL | 4 项 P1（已修）：① MG-17 的 AC1 要求删除 AUTO_MERGE 投影与「Out of scope 由 MG-21 承接」矛盾（MG-17 在串行链上先于 MG-21）→AC1 改「AUTO_MERGE 投影的清理由 MG-21 承接，本卡不涉及」，Out of scope 同步；② MG-01 把 `log A...B` 列为调用面失实——log.rs:846 为独立可达集交集实现、未消费 internal::merge_base 且不在写集→从 Description/AC/门族（11→10 门）/Verification/T-1 理由中移除 log，注明其收敛随后续 log 计划登记；③ D 组判据 ① 不可复现（`gh run watch` 无 run ID、未逐 job 查询）→改写为「`gh run list --json databaseId,headSha` 取 run 并核对 headSha → `gh run watch "<run_id>" --exit-status` → `gh run view "<run_id>" --json jobs` 要求三硬判据 job conclusion=success」；④ review log Evidence 含绝对私有路径违反 ER-11→全文改为归档标识 `libra-plan-review/<reviewer>-merge-plan-r<N>.txt`（绝对路径不入文档，preamble 写明） | 2 项 P2 已修（Claude Code）：① 事实基线 MergeArgs 锚点补 `:101`（`branch` 字段，struct 声明 :99）；② rebase.rs 锚点精确化（:4649 定义、:4756 replay 定义、:4823 调用点） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）+ Codex（codex-cli 0.152.0，read-only）双评审，输出存档 `libra-plan-review/{claude,codex}-merge-plan-r17.txt` |
| R18 | 计划全文（R17 修订后；Claude Code 终审） | PASS | none（Claude Code 零条目：R8~R17 全部 P1/P2 逐项闭环核实、串行链+逻辑边 DAG 与各卡 Dependencies 一致、21 张 EX-01 门族卡的门数/AC 分子/审计表分子/waiver 清单一致、43 处 file:line 锚点源码级核对全部相符） | Codex R18 因服务不可用（chatgpt.com backend 连续 404，三次重试均失败）**经委托人 Eli Ma 2026-09-03 指示取消**，未出结论；ER-05 的 Codex 最终 PASS 留待服务恢复后补做（遗留项，恢复后按同一规则基准复审 R17 修订后全文） | Claude Code（claude 2.1.259 --model sonnet，read-only 权限）输出存档 `libra-plan-review/claude-merge-plan-r18.txt`；Codex 失败输出存档 `libra-plan-review/codex-merge-plan-r18.txt`（仅 404 日志） |
| MG-00 R1–R10 | Task MG-00 实现（代码 + 测试 + 文档），逐轮复审 | PASS（R10） | R1 FAIL 8 项 P1（三消费者的引擎内 gate 晚于首次写入 → 各加前置 gate；rebase 冲突路径与 `reset --hard` 把 gitlink 当 blob 加载；六份命令文档缺 `LBR-UNSUPPORTED-001` 行；新错误变体缺 Display/stable-code pin；用例只断言错误码未断言「未写入」）。R2 FAIL 4 项 P1（`pull --rebase` 绕过 gate；rebase 前置 gate 对 fast-forward/空列表/非首父**假拒绝**；cherry-pick 前置 gate 忽略 `--ff`/`-m`；写集漏 `pull.rs`）。R3 FAIL 7 项 P1（`pull --rebase --autostash` 仍先写后判；rebase 未镜像 already-up-to-date 短路；merge 前置 gate 抢在 unrelated-histories 与 `--ff-only` 拒绝之前；cherry-pick `--ff` 模拟未更新 ours 侧；`reset --hard` 对 gitlink 目录 `remove_file`；checkout 豁免前提经实测证伪 → 删豁免并补 `pull --rebase` 端到端用例）。R4 FAIL 6 项 P1（`untracked_overwrite_path` 全量过滤 gitlink 会放行删除同名未跟踪文件；fast-forward 先动 ref 后 restore；rebase 前置 gate 未按 `--no-keep-empty` 过滤；cherry-pick root commit 缺空树 base；`EmptyCommit` 优先级被抢；status 把所有 metadata 错误当「未 materialize」）。R5 FAIL 3 项 P1、R6 FAIL 6 项 P1、R7 FAIL 2 项 P1、R8 FAIL 1 项 P1 + 1 P2、R9 2 项 P1 + 2 项 P2（1 修 3 拒）——逐轮收敛到 restore/reset 在 `160000` 路径上的两个方向：只有索引记为普通 tracked blob（可从对象库取回）或工作树那一格是目录时才允许改动，否则 `SubmodulePathNotOwned`；`--overlay` 只抑制「source 缺失即删除」。 | R9 的 3 项（`restore` 写入已检出 submodule **之下**、`reset --hard` 空占位清理与递归写入、`--ours/--theirs` 缺失 stage 删除）经逐条评审判定属 submodule 内容管理、先于本卡存在且未被本卡变得可达，登记为 `_compatibility.md` D24「已登记残留」并在卡内 `Out of scope` 引用；R8 P2（计划文本沿用已删除的标识符）已修 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg00-r1..r10.log` |
| MG-04 R11 | Task MG-04 实现（R10 修订后复审） | **PASS** | 无 P0/P1/P2：门族 G1–G11、ER-02 精化项、Git 对拍各形态、两条 walk、pull 继承面、文档与 R10 的符号链接/被忽略祖先回归均由证据覆盖 | 无 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r11.log` |
| MG-04 R10 | Task MG-04 实现（R9 修订后复审） | FAIL（R10，1 项 P1，已修）| R9 的祖先清理只加在文件写入分支，`write_workdir_symlink` 仍直接 `create_dir_all`——被忽略的普通文件挡路时写 tracked `foo/link` 以 `Not a directory` 失败（拍平引擎此时 HEAD 已移动）→ 符号链接分支同样先 `clear_ancestor_files`；`merge_replaces_an_ignored_file_standing_where_a_directory_must_go` 扩展为 theirs 同时新增 `foo/bar.txt` 与符号链接 `foo/link`，双 walk 断言两者都写出且 `status` 干净 | 无 P2 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r10.log` |
| MG-04 R9 | Task MG-04 实现（R8 修订后复审） | FAIL（R9，1 项 P1，已修）| 路径上被**忽略**的普通文件（未跟踪检查按 `IgnorePolicy::Respect` 跳过它）挡住合并要创建的目录时，`write_workdir_file` 的 `create_dir_all` 直接失败——而拍平引擎此时 commit 已写、HEAD 已移动，留下历史/索引/工作树不一致；`git merge` 实测把该文件**替换成目录**（`create mode 100644 foo/bar.txt`）→ 新增 `clear_ancestor_files`（写前逐级清掉路径上的普通文件；符号链接分量仍由 `refuse_symlink_components` 拒绝，未跟踪未忽略分量仍由 `ensure_no_untracked_conflicts` 拒绝），(new) `merge_replaces_an_ignored_file_standing_where_a_directory_must_go` 双 walk 断言合并完成、`foo` 成目录、`status` 干净 | 无 P2 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r9.log` |
| MG-04 R8 | Task MG-04 实现（R7 修订后复审） | FAIL（R8，1 项 P1 + 1 项 P2，已修）| ① 目录**不挡路**时（base 已有该路径的空 tree）文件留在结果里，但增量 walk 采纳的空子树条目（`TreeItemMode::Tree`）仍在 map 中——结果树会在同一名字下同时出现 blob `foo` 与目录 `foo/`（拍平引擎靠 `retain` 剥离标记，两引擎因此还分叉）→ `resolve_df_conflicts` 在「不挡路且文件存活」分支按前缀清掉其下条目（此时它们**必然**不含文件，正是「不挡路」的定义），折叠的 `relocate_virtual_df_files` 同样处理；(new) `merge_keeps_only_the_file_when_an_empty_subtree_is_not_in_the_way`（双 walk：base `foo/bar`（空）→ theirs 换成 `foo/baz`（空），ours 在 `foo` 放文件；断言 `ls-tree -r -t` 下 `foo` 只有一条 100644 条目）；② P2：卡内 ER-02 ㉙ 与开发文档仍引用已被 `write_workdir_entry` 取代的 `write_moved_conflict_file` → 两处锚点更新 | 1 项 P2 已修 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r8.log` |
| MG-04 R7 | Task MG-04 实现（R6 修订后复审） | FAIL（R7，2 项 P1，已修）| R5 引入的「写前 unlink」使写入变成**新建**文件，于是两项属性不再从磁盘继承：① 已跟踪的**符号链接**条目（`write_workdir_file` 被合并结果与 `reset_workdir_tracked_only` 的检出共用）被写成含目标文本的普通文件——非 D/F 路径的新回归；② 已跟踪的 `100755` 文件重写后退化为 umask 默认权限，干净合并/abort 后脚本失去可执行位。修法：抽出 `write_workdir_entry(workdir, path, mode, content)` 作为**唯一**工作树写入口（`write_workdir_symlink` 原始字节建链接、`apply_file_mode` 显式 `0755`/`0644`），三处调用点（冲突路径已合并文件、D/F 落点、reset 检出）全部改用，reset 用 `index_mode_to_tree_item_mode` 取模式；(new) `merge_preserves_symlinks_and_executable_bits_when_it_writes`（双 walk：磁盘上先 chmod 0600 与保留链接，合并后断言 `0755` 与真符号链接、索引仍是 `100755`/`120000`）。副产物：**MG-03 登记的检出写入器残留（不应用可执行位、不重写符号链接目标）由此消除** | 无 P2 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r7.log` |
| MG-04 R6 | Task MG-04 实现（R5 修订后复审） | FAIL（R6，1 项 P1，已修）| 本卡新增的拍平 flattener `flat_items_with_empty_dirs` 用 `TreeExt::load`（该 API 对缺失/损坏对象 **panic**），生产合并路径上损坏仓库会让 Libra 直接 abort 而非返回 `PullMergeError::TreeLoad` → 改用 `Tree::try_load` + 结构化错误，并把同样 panic 的 `commit_gitlink_entries`（经 `commit_tree_split` → `get_plain_items_with_mode`，实测拍平 walk 下 `fatal: CLI thread panicked`）一并改走该可失败路径；(new) `merge_flat_walk_reports_a_missing_nested_tree_instead_of_panicking` 断言 `LBR-REPO-002`、命名缺失 tree、HEAD 未动 | 无 P2 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r6.log` |
| MG-04 R5 | Task MG-04 实现（R4 修订后复审） | FAIL（R5，2 项 P1 + 2 项 P2，全部已修）| ① `--abort` 用 `full.is_file()` 判断是否清理移位文件——该调用**跟随**符号链接，悬空或指向目录的移位链接因此被永久遗留（恢复后的索引也不跟踪它）→ 改 `symlink_metadata`，(new) `merge_df_conflict_abort_removes_a_moved_dangling_symlink`（双 walk）；② 工作树写入对已存在普通文件原地 `fs::write` 截断同一 inode——`git merge` 实测是**替换目录项**（重写后 inode 改变、仓库外硬链接别名保留旧内容与旧权限）→ `clear_write_target` 增加 `remove_file`，(new) `merge_replaces_the_directory_entry_of_a_rewritten_file`（硬链接别名不受影响）；③ P2：rustdoc 与开发文档仍引用已删除的 `paths_with_surviving_directories` → 改 `directory_is_in_the_way`；④ P2：卡内 A 组证据 133/133 已过时 → 135/135（R4 两个用例）| 2 项 P2 已修（见左） | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r5.log` |
| MG-04 R4 | Task MG-04 实现（R3 + 本地预审修订后复审） | FAIL（R4，3 项 P1：2 项已修 + 1 项经 git 实测判定为有意一致）| ① 搬迁的符号链接目标要求 UTF-8（`str::from_utf8`），非 UTF-8 的合法链接在 merge state 与索引都已写出之后失败 → 改用 `OsStr::from_bytes` 按原始字节写出；② 落点上是**空目录**时符号链接分支对目录调 `remove_file` → `EISDIR`（同样在状态已写出之后）→ 抽出 `clear_write_target`（链接删链接、空目录走 `remove_empty_dir_tree`），两者由 (new) `merge_df_conflict_moves_a_non_utf8_symlink_over_an_empty_directory`（双 walk：非 UTF-8 目标 + 落点空目录）钉住；③ 「落点上被忽略的普通文件被静默覆盖」——`git merge` 实测**同样覆盖**（D/F 落点与普通新增文件两形态；只有未跟踪且未忽略才拒绝），判定为有意一致：行为不改，EN/zh 文档与 COMPATIBILITY 写明「被忽略路径不受保护」，(new) `merge_df_conflict_replaces_an_ignored_file_at_the_moved_name_like_git` 钉住 | 无 P2 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r4.log` |
| MG-04 预审（本地，非 Codex） | Task MG-04 实现（R3 修订后）——6 lens（git 对拍 / 工作树安全 / 双引擎一致 / 契约 / 文档卡 / 测试与复杂度）× 每条发现 3 个 refuter 的对抗式复核 | FAIL（7 项确认，全部已修）| ① **判据纠正（最重要）**：R1 结论「空 tree 也挡路」只在 base 于该路径**无条目**时成立——git 实测 base 有文件 `foo`、ours 修改、theirs 换成 `foo/{bar:空}` 时是普通 `CONFLICT (modify/delete): foo`（不搬迁）；抽出 `directory_is_in_the_way`（文件条目 / 空 tree + `base_present` / 携带子树读回调），`DfCandidate.base_present` 两引擎各自提供，虚拟祖先保留空目录标记，R1 的用例改为双形态双 walk；② 搬迁文件按原类型落盘（`write_moved_conflict_file`：真符号链接 / 可执行位），此前符号链接变成含目标文本的普通文件；③ 公告移到冲突状态写成之后（预检可能拒绝，git 拒绝时不打印任何 CONFLICT 行）；④ 存在性判断改 `symlink_metadata`：**悬空**已跟踪符号链接此前不被删除，随后写其下路径 `File exists`，拍平引擎已移动 HEAD → HEAD/索引/工作树不一致；⑤ 目录→文件接管改为写前预检（`directory_content_outside`，gitlink 豁免），被忽略的构建产物不再让合并中途失败；⑥ 冲突按路径排序后再算落点，消息/`conflicted_paths`/`Would conflict in:` 与 git 排序一致；⑦ 登记 `libra rm` 不接受未合并路径（「丢弃移位文件」只能 `--abort`），EN/zh 文档改为只承诺可行路径 | 无 P2 | Claude Code 本地 workflow（6 lens × 3 refuter，read-only 子代理；journal `subagents/workflows/wf_21ef7b4c-4f9/journal.jsonl`）|
| MG-04 R3 | Task MG-04 实现（R2 六项修订后复审） | FAIL（R3，6 项 P1 + 5 项 P2，全部已修）| R3 FAIL 6 项 P1：① 冲突路径的删除循环并入 base/ours/theirs 历史路径，会删掉重新出现的未跟踪文件、且可能穿过被忽略的符号链接祖先删外部文件——删除集改为「当前索引跟踪 ∧ 结果不含」，删除路径的祖先一并预检，`merge_df_conflict_deleted_input_path_still_occupies_its_name` 加未跟踪 `foo~HEAD` 存活断言 + (new) `merge_refuses_to_unlink_a_tracked_file_through_an_ignored_symlinked_directory`（双 walk）；② 拍平引擎的符号链接预检在 hook 之后失效、HEAD 先动——两处 hook 复检点重跑 `refuse_symlink_traversal`，(new) `merge_refuses_a_hook_planted_ignored_symlink_before_head_moves`（双 walk × pre-merge-commit / commit-msg）；③ 普通冲突的回退 stage 仍可能拿到空目录标记（base 空 `foo` + 两侧各自新增 `foo`）——回退查找过滤 `Tree`，(new) `merge_add_add_conflict_over_an_empty_base_directory_has_no_stage_1`（plumbing 构造，双 walk）；④ 预检把 D/F 必须替换的**已跟踪**符号链接也拒绝（ours 链接 `foo` vs theirs `foo/bar`）——预检放行本次合并要删除的已跟踪链接祖先（删除先于写入），(new) `merge_df_conflict_tracked_symlink_gives_way_to_a_directory`（双 walk，`120000 … 2 foo~HEAD`）；⑤ 折叠搬迁的占用集只看 `merged.keys()`——传入三侧 map 用同一 `df_occupied_names`，交叉合并用例植入并删除双标签名再由 x' 重建；⑥ 后处理逐候选扫描冲突列表为二次方——一次建 modify/delete 槽位表，(new) `resolve_df_conflicts_scales_linearly_with_the_candidate_count`（4000 候选）。5 项 P2 已修：增量 `files_changed` 搬迁修正 + 双 walk `--dry-run` 摘要对拍；`count_item_map_changes` 标记视为不存在 + (new) `merge_empty_directory_turning_into_a_file_counts_one_change`；写集补 `pull_test.rs`；VER 3/8→4/8；锚点 `:4261`→`:4374` | 5 项 P2 已修（见左） | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r3.log` |
| MG-04 R2 | Task MG-04 实现（R1 五项修订后复审） | FAIL（R2，6 项 P1，全部已修）| R2 FAIL 6 项 P1：① 增量引擎在 `expand_adopted_subtrees` 之后才算 placements，空子树标记已丢、修改文件 vs 空子树的 modify/delete 不搬迁——落点决策整体提前到 `resolve_df_conflicts`（结果仍含全部目录条目时），modify/delete 在此变成 `FileDirectory { modify_delete: true }`，每条引擎只算一次 placements 并随 `MergeConflictInput::placements` 下传，CLI `merge_df_conflict_edited_file_vs_empty_subtree_relocates_on_both_walks`（plumbing 构造空 tree，双 walk）；② 占用集漏掉输入里已被删除的路径（Git `opt->priv->paths` 含全部输入；实测仅 base 有过的 `foo~HEAD` ⇒ `foo~HEAD_0`）——`df_occupied_names`（拍平：三侧 map）/ `incremental_df_occupancy`（增量：仅在有 D/F 搬迁时经缓存源枚举三侧全部路径含目录），单测 + CLI `merge_df_conflict_deleted_input_path_still_occupies_its_name`（双 walk）；③ 拍平候选可能把 base 的空目录标记当 `base_file` 送进 stage 1——候选 `base_file` 过滤 `Tree`，stage 只取 `FileDirectory` 自带条目；④ 工作树写入跟随符号链接（`create_dir_all`/`fs::write`），被忽略的 `foo -> /外部` 可把写入导出仓库、`remove_empty_dir_tree` 可能穿链接递归——`refuse_symlink_components`（逐级 `symlink_metadata`）写前预检（冲突路径 `paths_to_write` 与 `reset_workdir_tracked_only`）、目标处符号链接删链接后写、`remove_empty_dir_tree` 拒绝链接，CLI `merge_refuses_to_write_through_an_ignored_symlinked_directory`（双 walk，HEAD/索引/外部目录不变）+ `merge_df_conflict_replaces_an_ignored_symlink_at_the_moved_name`；⑤ `-X ours/theirs` 绕过 D/F 的 modify/delete 搬迁、`-X theirs` 静默丢掉修改过的文件——候选不论解析结果都登记，后处理对「文件侧改过 + 目录存活」在 `-X` 删除后重新升起冲突（git 实测 `-X` 下仍是 D/F + modify/delete），单测 + CLI `merge_df_conflict_strategy_option_keeps_the_modify_delete_conflict`；无目录的普通 modify/delete 的 `-X` 裁决差异登记为先于本卡；⑥ pull 文档承诺的提示行在 JSON/machine 下被抑制——EN/zh pull.md 与 COMPATIBILITY pull 行改写为「人读输出」，383 行不变 | 无 P2 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r2.log` |
| MG-04 R1 | Task MG-04 实现（`merge.rs` D/F 冲突：两引擎共用后处理 + 落点搬迁 + 折叠内部搬迁 + `--dry-run` `conflict_kinds` + EN/zh/COMPATIBILITY/pull/开发文档），逐轮复审 | FAIL（R1，5 项 P1，全部已修）| R1 FAIL 5 项 P1：① `unique_df_path` 的占用集只含精确路径，已跟踪的 `foo~HEAD/bar` 不阻止 `foo~HEAD`（Git 选 `foo~HEAD_0`）——改 `occupied_names`（路径 + 全部祖先目录），git 实测一致，`placements_treat_an_occupied_ancestor_directory_as_taken` + CLI `merge_df_conflict_occupied_name_and_nested_directory_restart_and_abort`；② 修改过的文件 vs 目录只打印 file/directory 行、缺 Git 的 modify/delete 行，且写入 Libra 标记与「Version HEAD … left in tree」自相矛盾——`announce_df_conflicts` 双向补打 modify/delete 行，搬迁冲突一律按 `moved_file_content` 原样写落点，用例断言两行与原样内容；③ 目录下只有空 tree 时两引擎分歧（`tree_leaves`/共享 flattener 丢空子树，增量原样采纳）而 Git 仍报 D/F（人工空 tree 实测）——拍平引擎与折叠改用 `commit_tree_split_for_merge` / `flat_items_with_empty_dirs` 保留空子树标记，`sides_without_empty_dir_beside_file` + D/F 后 `retain` 剥离，`count_item_map_changes` 忽略标记，`merge::tree::an_empty_directory_in_the_way_raises_the_df_conflict_on_both_walks`；④ `--json` 的真实合并/pull 把 CONFLICT 行混进 stdout（`info_println!` 只看 `quiet`）——`announce_df_conflicts` 在 `output.is_json()` 下直接返回，`merge_df_conflict_json_keeps_stdout_machine_clean` + `pull_test::test_pull_df_conflict_json_keeps_stdout_clean`；⑤ 嵌套目录（`foo/a/bar.txt`）下 `--abort` 留下空 `foo/a`，恢复文件 `foo` 时 `Directory not empty`——`prune_empty_parents`（删文件后逐级清空目录）+ `remove_empty_dir_tree`（只含空目录的树才接管），`--restart`/`--abort` 嵌套用例 | 无 P2 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg04-r1.log` |
| MG-03 R1–R12 | Task MG-03 实现（`merge.rs` 增量树合并 + `merge::tree` 单测 + 端到端 + 开发文档），逐轮复审 | PASS（R12） | R1 FAIL 6 项 P1：① `preflight_merge_gitlinks` 仍经 `commit_gitlink_entries` 拍平三树，性能预算失效——单/无 base 改用只读 `incremental_gitlink_gate`；② 多个快捷采纳臂（`b==t`/`o==t`/单侧新增/单侧删除）绕过隐藏 gitlink 裁决——裁决整体改为**独立只读 gate 先行**，规则与拍平路径逐点等价（三侧齐全且相同才 pass-through，否则裁决；三侧一致目录不打开，其它目录在每个持有侧打开），覆盖全部臂；③ 自动合并 blob 可能先于裁决落盘——gate 先于 walk，被拒合并零对象写入；④ 未跟踪祖先碰撞漏判（只检 `starts_with(dir)`）——改 `worktree::paths_conflict` 对称判定，补端到端用例 `merge_tree_walk_refuses_untracked_ancestor_of_an_adopted_subtree`（HEAD/索引/工作树/未跟踪文件均不变）；⑤ 生产代码 `expect("root")`——直接构造；⑥ G1/G2/G5 只在辅助入口测——新增生产证据 seam `LIBRA_TEST_MERGE_TREE_STATS`（记录默认合并所走路径与 tree 读取数），端到端 `merge_tree_walk_default_is_incremental_and_reads_only_changed_paths` 断言默认路径为 `incremental` 且读取数 ≤ 根 + 差异路径深度 × 侧数。R2 FAIL 1 项 P1：hook 之后的两次未跟踪碰撞复检沿用 hook 之前算出的叶子清单，hook 在采纳子树下新建的文件漏判且 HEAD 先于 reset 移动——抽出 `adopted_aware_write_paths` 在合并前 / `pre-merge-commit` 后 / 消息 hook 后**按当时未跟踪集合重算**，补 `merge_tree_walk_rechecks_hook_created_files_under_adopted_subtrees`（拒绝、HEAD 不动、hook 文件保留）。R3 FAIL 1 项 P1：按 id 原样采纳的子树若含缺失/损坏的嵌套 tree，拍平路径在读树阶段即失败，新路径却要到 HEAD 更新之后的 reset 才发现——新增 `verify_adopted_subtrees_readable` 在第一次写入前读遍采纳子树下的全部 tree（经缓存源，只读 tree），补缺失嵌套 tree 端到端用例。R4 FAIL 3 项 P1：① 该校验递归打开所有携带子树，读取量退回仓库规模，且 stats seam 在其之前上报——**撤回校验**，改以「walk 未打开的 tree 必已被 HEAD 引用」不变量论证（gate 递归进入三侧不全相同的每个目录；未打开处所有在场侧 id 相同）并以 `unopened_trees_are_heads_own` 钉住；缺失嵌套 tree 用例改述为「新增子树由 gate 完整枚举，写前失败」（`merge_tree_walk_refuses_an_added_subtree_with_a_missing_nested_tree`）；② 自动合并 blob 可能先于校验落盘——校验撤回后不再成立（gate 先行仍保证 gitlink 拒绝先于落盘）；③ 根 tree 的 `refs/replace` 口径与拍平路径不一致——根 id 经 `replace::resolve`、嵌套 tree 用 `load_object_raw`，逐点对齐拍平路径（根经 `load_object`、嵌套经 `Tree::load`）。 R5 FAIL 2 项 P1：① walk 未打开而 HEAD 自有的共享 tree 若缺失，仍会在 commit+HEAD 写入之后的检出才发现——把检出（本就读遍结果树）提前到 commit/HEAD 之前，任何缺失对象在持久写入前失败，补 `merge_tree_walk_missing_shared_tree_fails_before_head_moves`（删共享嵌套 tree，合并失败，HEAD/索引/工作树/状态均不变）；② 冲突路径的 stage-2/3 用未经 `replace::resolve` 的根——改用 walk 同一批解析后的根，补 `merge_tree_walk_conflict_stages_follow_the_replaced_root_on_both_walks`（`restore --theirs` 得到被替换内容且两路径一致）。 R6 零 P0/P1（仅 1 项 P2）。R7 FAIL 1 项 P1：G9/G10 的 CLI 承载 `merge_gitlink*` 只有根级 `vendor`，gate 直接访问到，未覆盖「埋在被剪枝/采纳子树内部」的形态——补 `merge_gitlink_nested_changed_pointer_inside_a_pruned_subtree_is_refused`（`deps/vendor/lib` 在 ours==base 的子树内，theirs 改指针→`LBR-UNSUPPORTED-001` 命名嵌套路径、HEAD/索引/对象/工作树/状态零改动）与 `merge_gitlink_nested_identical_pointer_inside_a_pruned_subtree_passes_through`（三侧相同→合并成功、`cat-file -p HEAD:deps/vendor` 仍含 `160000`，stats seam 证明 `deps/` 未打开）。 R8 FAIL 1 项 P1：`--dry-run` 不打开携带的 tree，缺失的共享 tree 会让预演报成功而真实合并失败——预演增加只读探测 `probe_carried_trees_readable`（经缓存源读携带子树的全部 tree），结论与真实合并一致，代价等于拍平预演本就读的那批 tree，补 `merge_tree_walk_dry_run_matches_the_real_verdict_on_a_missing_shared_tree`。 R9 FAIL 2 项 P1：① 预演探测用 raw 视角而检出用 `load_object`（应用嵌套 `refs/replace`），悬空的嵌套 replace 会让预演与真实结论相反——探测改用检出视角的独立源 `as_checkout_sees_them()`，补 `merge_tree_walk_dry_run_follows_nested_replacements_like_the_checkout`；② G6–G8 无 CLI 承载——补 mode-only / symlink / 新增可执行三个双 walk 用例（theirs 用 plumbing 构造，porcelain `add` 不识别裸 chmod；断言 tree 与索引 mode、双 walk 工作树一致）；构造时发现 merge 检出写入器不应用可执行位 / 不重写符号链接目标（tree/索引正确，两 walk 相同，`checkout` 正确）——判定为本卡外既有残留，登记于开发文档与 Out of scope。 R10 FAIL 1 项 P1：G6–G8 CLI 承载的共享 helper 用了 `std::os::unix::fs::symlink` 却未加 `#[cfg(unix)]`（只有调用者加了），Windows 的 `command_test` 编译会失败——helper 补 `#[cfg(unix)]`。 R11 FAIL 1 项 P1：预演一律以检出视角探测，但有冲突的真实合并不检出、经 raw 源写冲突状态——「别处冲突 + 悬空嵌套 replace」时预演报错而真实合并成功；预演改为按结论选视角（无冲突→检出视角，有冲突→walk 的 raw 源），补 `merge_tree_walk_conflicted_dry_run_matches_the_real_conflict_path_under_a_dangling_replace`（预演 exit 1 报冲突，真实合并写出冲突状态）。 R12 PASS（零 P0/P1/P2）。 | R10 一项 P2 已修：mode-only 与新增可执行两个用例的 G6/G8 标注对调（卡内 G6=mode、G8=executable）。R9 两项 P2 已修：seam 计读取尝试（失败亦计）并写明失败退出不写 stats；Performance budget 改为可度量的界 O(改动路径 × 深度 × 侧数) × 2。R8 两项 P2 已修：① stats seam 改在引擎每个出口、最后一个读源消费者之后写出；② 未跟踪碰撞检查只展开**结果引入的**子树（`adopted_from_theirs`），ours 本有的子树不因其下的未跟踪文件被打开（stats 端到端用例加未跟踪文件仍守界）。R7 一项 P2 已修：「任何持久写入前」收窄为「HEAD/索引/工作树/merge-state 写入前」，写明此前落盘的仅是无引用、可回收的内容寻址对象（与拍平路径同时点相同）。R6 一项 P2 已修：`commit_tree_split` 行号锚点随本卡编辑再次漂移（`:3177`→`:3181`）——三处改为**符号优先**引用并注明收口时行号。R5 两项 P2 已修：stats seam 改为进程内所有 `ObjectStoreTrees` 的总读取（preflight + 引擎两遍），端到端界改为 2×；冲突路径展开**全部**携带子树、O(全树) 如实写入 rustdoc 与开发文档。R4 两项 P2 已修：`--dry-run` 与真实合并读同一批 tree、结论一致（写入文档）；`merge.rs:2666` 锚点三处刷新为 `commit_tree_split` `:3177`。R3 一项 P2 已修：`ObjectStoreTrees` 改用 `load_object_raw`，与拍平路径的 `Tree::load` 同样不应用 `refs/replace`，两路径 tree 视图一致（是否尊重 replace 属另一条轴）。R2 两项 P2 已修：开发文档引用的读取计数用例名与界（`…_with_only_the_gates_reads`，3 + 8）；rustdoc 链接 `pruned_tree_diff`→`pruned_subtree_diff`。R1 两项 P2 已修：① 原样采纳的子树内空 tree 得以保留（拍平路径从不重建空 tree）——G3 精度定为「逐叶判定相同」，对拍用例加入含空目录的采纳子树并注明为 Git 同义的已知差异；② 可见/隐藏裁决路径分别排序——gate 统一收集后全局取最小路径。 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg03-r1..r12.log` |
| MG-02 R1–R7 | Task MG-02 实现（`merge.rs` 递归虚拟祖先 + 单测/集成测试 + 文档），逐轮复审 | PASS（R7） | R1 FAIL 4 项 P1：① 无 base 的 add/add 二进制/非 blob 冲突回退成 `ours`，会吞掉外层 add/add 冲突——按 `merge-ll.c` `ll_binary_merge`（steal `orig`，无 orig 即空缓冲）与 `merge-ort.c` `handle_content_merge`（`call_depth` 下取 `o`，无 `o` 即路径不存在）重写：二进制取 base 内容/空 blob、符号链接与混合类型取 base 条目/不存在；② 二进制判据用 UTF-8 有效性，与 Git `buffer_is_binary`（前 8000 字节含 NUL）双向不符——改为 Git 判据，并把 `relabel_conflict_markers` 从 `from_utf8_lossy` 改为逐字节（否则「Git 判为文本但非 UTF-8」的内容会被改写成 U+FFFD）；③ G1–G5 只测辅助函数、不测生产接线——补生产入口 `fold_merge_bases` 的深度上限用例、三 merge base 端到端（两步折叠 + `--restart` 一致）、`diff3` 下嵌套/外层标记宽度端到端；④ 文档与实现不一致——EN+zh 补「多 base 提前解析 `merge.conflictStyle`」与二进制/符号链接/mode 规则，`COMPATIBILITY.md` 与卡内 Description 同步四项 Git 差异。R2 FAIL 3 项 P1：① `--dry-run` 仍会回收陈旧 autostash sidecar 并执行 finalize（写入）——`dry_run` 下跳过两者，补 `merge_crisscross_dry_run_leaves_a_stale_autostash_sidecar_untouched`（写集内既有缺口，卡内 ER-02 登记）；② 第四项 Git 差异（合成 commit 的 parents 拓扑）未进用户文档与 `COMPATIBILITY.md`——EN+zh 与 merge 行补齐；③ pull 文档仍只写「single-head three-way」——`docs/commands/pull.md` EN+zh 的说明与错误表、`COMPATIBILITY.md` pull 行补继承说明（写集 ER-02 偏离登记）。R3 FAIL 2 项 P1：① 宽交叉历史下 `merge_bases_of_folded` 逐对 `is_ancestor` 且每次新建图源，工作量随 base 数平方增长且无上界——单已折叠 base 时直接返回 `merge_bases`（零过滤）、多部分时只在不同部分间做支配过滤、新增 `MAX_VIRTUAL_ANCESTOR_BASES = 32` 宽度上限（`VirtualAncestorTooWide`→`LBR-UNSUPPORTED-001`，加载前拒绝）并以生产入口单测钉住（33 拒 / 32 进入加载）；② pull 文档 autostash 措辞「即使失败也会应用」与 merge 路径「冲突时持有（`kept`）直到 `--continue`/`--abort`」矛盾（既有文本，因 pull 文档已入本卡写集而修）——EN+zh 说明与参数表区分 rebase 恢复与 merge 持有。R4 FAIL 3 项 P1：① `src/cli.rs` 分发层仓库 preflight（schema 升级/object-index 修复/`maintenance.lock`）先于 `--dry-run`——**判定为本卡外既有分发层行为并登记残留**（所有命令共有、先于本卡、`cli.rs` 为 T-1），用户文档把预演契约收窄到 merge 自有表面；② 宽度上限晚于 `merge_base_commits` 的逐 base 加载与 gitlink preflight 的 tree 加载——改为拿到裸 id 即检查（`will_fold` 门控，`-s ours` 不受限），并补端到端 33-base 用例（拒绝、零写入、`-s ours` 放行）；③ `merge_bases_of_folded` 在调用方宽度检查之前无界收集候选并两两走图——改为入口检查已折叠数、收集中逐个检查候选数、超限即拒绝且不做任何 `is_ancestor`，补单测。R5 FAIL 2 项 P1：① 宽度上限抢在 `--ff-only` 的 NonFastForward 判定之前（分叉的 `--ff-only` 本应报 `LBR-CONFLICT-002`）——抽出共享判定 `merge_options_will_fold`（默认策略且非 `--ff-only`），preflight 与引擎同用，33-base 用例补 `--ff-only` 断言；② pull 文档只写深度上限——EN+zh 说明与错误表、`COMPATIBILITY.md` pull 行补 32-base 宽度拒绝。R6 FAIL 1 项 P1：折叠内二进制判据缺 Git `ll_xdl_merge` 的尺寸分支（输入 > `MAX_XDIFF_SIZE` 1023 MiB 同走二进制）——补 `MAX_XDIFF_SIZE` 常量与尺寸谓词，单测钉边界（按长度断言，不分配 GiB），EN+zh/COMPATIBILITY/开发文档同步。 | R6 两项 P2 已修：`COMPATIBILITY.md` merge 行补 `--ff-only` 宽度豁免；单 base 冲突路径的 `merge.conflictStyle` 注释改为说明多 base 已提前解析。R7 PASS（零 P0/P1）；其 1 项 P2（计划内 `merge_base_commits` 锚点因后续编辑漂移）已修：三处刷新为 `merge.rs:2829`。R1 两项 P2 已修：③（R3）事实基线/GAP-03/Current evidence 的 `merge.rs:2537` 锚点刷新为 `merge_base_commits` 现址并注明前身；① gc 用例改为按 object id 断言「本次合并新增的对象被回收」且「`--restart` 把同一批 id 重算回来」（合成 commit 必新增；其 tree 常因内容寻址与既有 merge 的 tree 相同而不新增，用例据此断言非空而非固定条数）；② `compat_command_docs_examples_section` 已补跑并记入 A 组证据 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg02-r1..r7.log` |
| MG-01 R1–R4 | Task MG-01 实现（`internal/merge_base.rs` 全量改写 + 内联单测 + 文档），逐轮复审 | PASS（R4） | R1 FAIL 5 项 P1：① `≤3×trunk` 读取上界不成立（反例：一侧 tip 日期高于整条主干、另一侧低于全部，主干被重放两次）；② AC10「无全历史内存驻留」比算法实际提供的更强（commit 一旦访问就必须留在 paint map）；③ G2 未被真正承载（无用例观察优先队列顺序或抽干）；④ 文档与代码矛盾（rebase 早已迁到本模块）；⑤ 「`rebase_test` 覆盖 `am`」不成立（无 `am_test` 模块）。R2 FAIL 2 项 P1：① 上界应按**边**而非 commit 数（每次出队要读每个 parent）；② `remove_redundant` 对候选集仍是「每候选一次全祖先遍历」，候选多时二次。R3 FAIL 2 项 P1：① `pop_dates` 是生产路径上的 O(pops) 历史，自相矛盾地推翻驻留结论；② 上界对「自反单 commit」图不成立，且 benchmark 测的是 `merge_bases_with`（含极大化）而非染色本身。修复：上界改写为 `3 × (commits + edges)` 并加 500 路 octopus fan-out 与自反退化两组测量；`remove_redundant` 改为**一次多源遍历**；测试专用字段 `#[cfg(test)]` 门控；同 tip 只 seed 一次；AC10/G10 按实情改为**对比性**判据并实测；`am` 调用面改挂真实承载 `compat_mail_am_basic`。 | R4 两项 P2 已修：`COMPATIBILITY.md` merge-base 行不再宣称「consolidation 完成」（`log A...B` 仍独立）；卡内锚点按最终代码刷新 | `codex exec`（codex-cli 0.153.0，gpt-5.6-luna xhigh，read-only）逐轮输出存档 `libra-plan-review/codex-mg01-r1..r4.log` |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-01 | `merge-tree --write-tree` 命令 | 独立命令表面，非 `git merge` 能力；AUTO_MERGE（MG-21）先提供结果树消费面 | 脚本化「不碰工作区合并」需求出现 | 后续日期计划 |
| DEFER-02 | `-X diff-algorithm=<patience\|histogram\|minimal>` | diffy 内核单一算法（ADR-MG-02） | 自研 xdiff 等价物或 diffy 支持多算法 | ADR-MG-02 Revisit |
| DEFER-03 | mergetool 全量 25+ 工具描述 | 边际价值低；`mergetool.<tool>.cmd` 已提供任意工具接入 | 用户提出具体工具需求 | MG-14 的 `merge.tool` 配置面 |
| DEFER-04 | 签名 tag 合并的 `mergetag` header 与 throwaway tag 强制 no-ff | 依赖外部 GPG 互操作（vault-only 现状下价值有限） | vault 支持外部 keyring 或 Git 签名 tag 工作流需求 | 后续日期计划 |
| DEFER-05 | merge.rs 模块拆分（预计超 5000 行后的 `src/internal/merge/` 归位） | 纯结构重构无行为轴价值，且与各卡写集冲突 | 本计划收口后作为独立重构计划 | 后续日期计划 |
| DEFER-06 | cherry-pick 的 rename 接入；外部 `git-merge-<name>` 策略命令发现 | cherry-pick 已复用内容合并（rename 接入面独立）；外部策略发现是独立机制面 | 用户提出对应场景需求 | 后续日期计划 |
| DEFER-07 | 目录 rename 推断与 rename/rename(1to2)/rename/rename(2to1) 叠加的其余角落案例（MG-07 AC4 采样组合之外） | 主干语义已按 Git 参照对齐；逐案实现与验证成本高、场景罕见 | 用户提出对应场景需求，或收到与 Git 结果分歧的报告 | 后续日期计划（登记于 MG-07 Out of scope） |
| DEFER-08 | `merge.<name>.recursive`（Git 定义为内部共同祖先合并所用的低层 driver，非标签定制） | 仅影响 recursive 虚拟祖先 + 自定义 driver 组合的内部行为 | 用户提出 recursive + 外部 driver 组合需求 | 后续日期计划（登记于 MG-09 Out of scope） |
| DEFER-09 | `-X find-renames[=<score>]` / `rename-threshold` flag 别名 | rename 阈值已由 `merge.renameLimit` 配置面覆盖，flag 仅按次调阈值的便利别名 | 用户提出按次调阈值需求 | 后续日期计划（登记于 MG-11 Out of scope） |
| DEFER-10 | 显式 `-s octopus` 策略名 | 多 head 自动路由至 octopus 已由 MG-12 覆盖，显式指定为冗余表面 | 脚本化工作流需要显式指定策略名 | 后续日期计划（登记于 MG-13 Out of scope） |
| DEFER-11 | `mergetool.keepBackup` 之外的 `mergetool.*` 配置面（hideResolved/gpgSign/quiet/writeToTemp 等） | 边际价值低；未实现配置按 fail-closed 提示（MG-14 既有语义） | 用户提出具体配置需求 | 后续日期计划（登记于 MG-14 Out of scope） |
| DEFER-12 | 外部 GPG keyring / SSH 签名（merge `-S` 签名路径仅 vault，与 commit 现状一致） | 依赖外部 GPG 互操作（vault-only 现状） | vault 支持外部 keyring 或 Git 签名工作流需求（与 DEFER-04 同源） | 后续日期计划（登记于 MG-15 Out of scope） |
| DEFER-13 | merge 默认打开编辑器（Git 默认 `--edit`；Libra 保持「不自动开编辑器」现状） | 有意差异，兼容注记入 COMPATIBILITY 与命令文档 | 用户提出默认开编辑器需求 | 后续日期计划（登记于 MG-18 Out of scope） |
| DEFER-14 | `merge.suppressDest` 等 fmt_merge_msg 衍生配置 | 消息模板衍生配置面，现状不消费 fmt-merge-msg 模板 | 用户提出需求 | 后续日期计划（登记于 MG-18 Out of scope） |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [ ] 所有任务卡满足粒度规则 `G-*`：无未登记的 L 例外、无 XL 卡、无碎片卡、无未登记的合并发布例外、实现写集冲突均已消解；「任务卡粒度审计表」已填齐（21 张门族卡的 AC 分子均带 `@EX-01` 且 EX-01 在 waiver 表有效）。
- [ ] 所有非延后任务的 acceptance criteria 已满足，且 `Lifecycle=done` 且 `Acceptance=complete`（ER-04）；无 `blocked` 卡残留；全部卡取得 D 组统一登记契约的绿色证据。
- [ ] 所有任务的 Verification 命令已运行并记录结果。
- [ ] **计划完成门 / 全量收口门（ER-13）**：全部任务卡完成后运行一次 —— `cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all` 全绿；本计划不改动 `web/`、`worker/`。
- [ ] **全量收口门暴露的 Bug 已全部处理（ER-13）**：定位到本计划的失败已前滚修复并重跑全量至全绿；既有失败附基线复现证据并登记 `FIX-*`/`DEFER-*`。
- [ ] 「兼容与文档收口」全部勾项完成或注明 N/A。
- [ ] 每张卡的 `Rollback mode`（immutable-release：前滚修复 + 用户降级指引）已被实际验证或记录为不可验证的原因。
- [ ] Codex review 最终结论为 `PASS`，P0/P1 全部关闭；仅 P2 residual risk 允许保留且有具名接受人。
- [ ] 每张会发版卡已 `patch + 1`、版本面五处一致、`Cargo.lock` 仅由工具链刷新、提交/推送/tag 推送完成且 `gh release create v<version>` 证据取得（ER-08）。
- [ ] 「修订历史」已记录成稿后的全部规范性变更（G-09）。
- [ ] `plan-long.md` 相关状态或日期计划索引已同步，或明确 `N/A`。
