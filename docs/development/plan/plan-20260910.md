# 数据库迁移作用域与全局配置兼容计划（2026-09-10）

> **最终收尾用户覆盖（2026-09-15 CST）:** 用户最新明确要求「进行收尾工作，无需等待 release 的信息」。本次直接执行本机最终验收、最终审查与 signed+DCO 文档提交/推送，不再把 v0.22.28 Release/Advanced CodeQL 的返回、manifest/Homebrew/R2 后置信息或继续轮询作为收尾前置；本地 watch 已停止，远端任务不取消、不重跑。此指令覆盖下文 R44/R45 为保守解释新增的「D 全绿后再全量」时序，以及本次 MIG-R03/计划完成判据中等待 D 的条件；不豁免源码、测试、版本、签名/DCO、push 或已完成的 gh 创建要求。最终默认全量现在执行，继续继承原双 env 的 build16/test16、既有600s/二例1200s配置与原有忽略集合，不追加 feature/Web/release build。D 信息未取得时单列最后已知 run 链接与 `remote-pending / not verified`，不得假定成功；本机收尾过程中若已存在真实终态，可直接核对并更新事实，但不为它等待或重启流程。此前各卡已取得的 D、历史失败与审查证据保持不变。

> **当前执行状态（2026-09-15 CST）:** MIG-00..MIG-06、MIG-R01/MIG-R02/MIG-R03十卡实现与各自验收均完成；v0.22.28已由gh发布，Release/Advanced CodeQL及manifest/Homebrew/四平台下载证据全部取得。按用户指令直接收尾、未等待远端流程，最终本机全量 `51e1b567-8528-4577-9947-cdf15960873a` 亦10046/10046通过、无失败/重试（2062.399s，10 slow，3原有ignored），build16/test16与冻结source/metadata保持。最终事实及审查结果由本次docs-only闭包提交保存，不新增任务卡或版本。下文逐轮“待审/运行中/尚未执行”均为相应时点的历史记录，以最终事实闭包及此段当前状态为准，不改写原失败、旧参数或当时的remote-pending。

> **测试标准迁移（2026-09-12，模板 v2.5）:** 本计划尚未执行。所有未执行任务卡、ER-13 全量门及完成判据中的全量收口门统一运行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`。既有 focused 命令与历史运行记录不变；本计划中旧的 `cargo test --all` 表述在上述范围内失效，仅可用于诊断，不能作为通过证据。

> **本计划测试执行覆盖（2026-09-14，用户指令）:** 当前 checkout 是 Git repository，而全量构建/测试包含只能在 Libra repository 运行的命令；本计划执行期间不触发 `cargo build --all*`、`cargo test --all`、`cargo nextest run --all` 或 `cargo clippy --all-targets --all-features`。每卡仅运行其具名 focused target、`cargo +nightly fmt --all --check`、静态契约守卫及不依赖当前 repository metadata 的必要检查；长时间无输出且超过预估时限的测试必须终止并分析，不得无限等待。每次 patch bump 仍必须执行 `gh release create`，release notes 依据该卡实际修改总结，D-MIG-STD 远端发布证据仍须取得。

> **全量收口恢复（2026-09-14，后续用户指令）:** 用户明确要求“在健康发布的同时，启动全量测试”，从本次收口起覆盖上一条对全量 Rust 测试的禁止：在当前 Git checkout 执行 `source .env.test && source .env.live-test && LIBRA_SKIP_WEB_BUILD=1 CARGO_INCREMENTAL=0 cargo nextest run --all --no-fail-fast --retries 2 --build-jobs 4 --test-threads 4`，不增加 all-features、ignored/live feature targets 或网站全量构建，也不转换仓库。复用仓库 nextest serial groups，并通过仓库外 tool config 为单例设置每 120s 慢测试提示、600s 终止、10s graceful shutdown；编译阶段独立监测 CPU/进度，不按测试静默时限误杀正常编译。既有卡内 focused-only 记录保持历史事实，最终判据须补本次真实全量结果；失败、重试与 Git/Libra 环境限制必须如实记录并分析，不以跳过代替通过。发布与 Advanced CodeQL 继续异步跟踪，不等待其完成才启动测试。此授权不等于已经取得 CI runner 上的 100 MiB 性能证据。

> **后续测试参数覆盖 / R39（2026-09-14，用户最新指令）:** 用户要求“后面测试请采用 .env.test 里面设定的参数”。从此后新启动的测试起，source 原绝对路径 `.env.test`，需要完整验收环境时仍 source `.env.live-test`；不再用命令行 `--build-jobs 4`、`--test-threads 4` 或环境赋值 `CARGO_BUILD_JOBS=4` 覆盖用户配置。只读核对当前 `.env.test` 的 `CARGO_BUILD_JOBS=28`、`LIBRA_SKIP_WEB_BUILD=1`，source 两个文件后这两个值不变；未配置 nextest 测试槽数，因此不能把 build jobs 误称为 test slots，未指定的槽数交给 runner 的环境／配置默认值。现有 serial groups、确需单线程的专用 provider profile、隔离 target/global/system fixture 路径、禁止网站全量与 all-feature 扩张、单例超时监控和失败如实记账均保持。正在执行的 `76064f34-b105-429b-b2dd-74606390ddab` 不重启，仍以其启动时的 4 slots / 4 build jobs 完成，历史命令和实测证据不回写为 28。此用户覆盖优先于下文 R35/R38 对未来调用的固定 4 配置；不改变受测源码、全量用例集合或完成门。Codex 自审核对参数与范围，独立审查结果另记。

**R39 审查闭包:** Codex 参数/范围自审 PASS，Claude Code 独立只读审查最终字面 `VERDICT: PASS`，P0/P1 none（`r39-env-parameters-review.log`）。其 P2 执行提示记录为：上述“从此后新启动”同样覆盖顶部旧的固定 4/4 命令；后续仍须显式传入既有仓库外 `--tool-config-file libra-full-suite:/mnt/gitmono/tmp/libra-full-suite.tGFCUS/nextest-timeouts.toml`，不能把仅 source 环境误称为已接入 600s 超时；保留先前 `CARGO_INCREMENTAL=0` 稳定性设置不等于覆盖一个当前未设置的 `.env.test` 参数。若后续用户文件新增测试/编译参数，按新用户配置读取，不把本次检查到的值永久硬编码。更高并行度导致的资源争用或超时必须保留并分析，不能静默降并行度或将失败改称通过；现有 live run 与其源码/计数证据均未改变。

**R39 用户配置更新实测:** 用户随后在原 `.env.test:12` 设置 `export NEXTEST_TEST_THREADS=16`，`:7` 仍为 `export CARGO_BUILD_JOBS=28`。只读 `bash -n` 通过；新 shell source 后两者均已 export 到子进程，随后 source `.env.live-test` 仍分别为 16/28，未被覆盖；`RUST_TEST_THREADS` 未设置。本次仅核验，未修改用户环境文件、未启动或重启测试。此事实更新取代上段“未配置测试槽”的旧检查结果，不回写正在执行的 4 槽历史。

**用户并行参数再次更新（2026-09-15 CST）:** 用户明确要求“把cargo build并行参数调整为16，重新测试”。已仅将原 `/mnt/gitmono/libra/.env.test:7` 的 `export CARGO_BUILD_JOBS=28` 改为16，`:12` 的 `NEXTEST_TEST_THREADS=16` 保持；`bash -n`通过，source两个原绝对env文件后实际build16/test16/skip-web1。按R39既有“继承用户最新env”规则执行，不另加CLI线程覆盖；下文build28均为旧轮次启动事实，不回写。仅中断已核实属于本任务、尚在旧28参数编译阶段的focused进程组126801（SIGINT、退出130、0 tests），其子进程已全部退出；不是测试失败或通过。重新从完整预编译开始写独立 `r03-j16-*` 证据；仍要求原有完整默认集合与二例MCP1200s仓库配置，最终全绿之前不发布本卡。

> **MCP 单例超时覆盖 / R40（2026-09-14，用户最新指令）:** 用户要求“把这两个测试用例的超时时间设为 20 分钟”。仅对 `e2e_mcp_flow::test_e2e_mcp_flow` 与 `e2e_mcp_flow::test_web_only_sigterm_releases_ports`，新调用使用 task-owned `/mnt/gitmono/tmp/libra-full-suite.tGFCUS/nextest-timeouts-mcp20m.toml`：原 default 为 `period=120s / terminate-after=5 / grace-period=10s`，新增精确 binary+两个 exact test 的 override 改为 `terminate-after=10`，即单次 1200s（终止后仍最多 10s graceful shutdown）。其它用例仍为 600s，超时仍判失败；不增加重试、不更改用例内容、源码、仓库生成的 serial 配置或完成门。新参数文件已由 nextest 的 metadata-only list 实际解析，filter 恰好匹配这两个案例且 total=2；此为配置验证，不是运行测试。该新调用实际继承 `.env.test` 的 build jobs=28/test slots=16，未以 CLI 覆盖。

**R40 运行中边界:** 原 `nextest-timeouts.toml` 不覆盖，保留当前 `76064f34-b105-429b-b2dd-74606390ddab` 启动配置的原始证据；nextest 已加载的 600s 不会热更新。本轮已到 10043/10044，其中 `test_e2e_mcp_flow` 实际 431.994s 首次通过，另一例仍运行；不为改参数丢弃已经完成的全量，也不声称当前过程已获得 1200s。新文件取代 R39 对未来调用的旧 tool-config 路径；后续任何复验都显式使用新文件并 source 最新用户环境，原始失败/超时/重试如实保留，完整全量终态仍须全绿，不以两个 focused 结果拼接替代全量。本项仅调整用户指定的本计划测试执行参数，无产品/测试源码或版本面变化；Codex 已核对 filter、120×10 与作用域，独立审查结果另记。

**R40 审查与旧运行事件:** Codex 自审及 Claude Code 独立只读复核均 PASS，最终字面 `VERDICT: PASS`、P0/P1 none（`r40-mcp-timeouts-review.log`），确认精确二例、1200s/600s 分界、10s grace、无 retry 或 serial 改写、不得将局部复测拼作全量。随后旧运行最后一例 `test_web_only_sigterm_releases_ports` 在其原 600s 时限实际 `TRY 1 TMT [600.019s]`；当时它先等待另一例的 Cargo build 锁，再执行自身编译，已观察到活跃 rustc，不能改写为已知死锁。nextest 依该运行既有 retries2 进入第二次尝试，仍是原 600s 设置；本轮全量保持 10043/10044 已完成、末例待终态。原 timeout 和前面的 push 首次失败记录均保留，终态之后再分析结果文件，不提前声称全绿或已热载入新配置。

**模板版本:** `v2.4` 起草，按 `v2.5/v2.6` 增量迁移；用户执行覆盖见顶部注记。

> **异步发布覆盖（2026-09-14，用户指令）:** 每卡仍须提交、推送、patch +1 并调用 `gh release create`；不等待 GitHub 发布流程即可开始下一张卡。实现与发版命令继续串行，D 组 workflow 链接和实际状态独立记录；运行中不得写成成功。此覆盖优先于 ER-MIG-03、发布窗口和任务依赖中要求前卡 D 组完成后才开工的等待条件。

> **执行基线刷新（2026-09-14）:** `7df326fa3` / v0.22.20 的 Repository manifest 已包含 `2026090801`。下文 2026-09-08 的 `2026090601` 与旧 binary 现场描述保留为历史证据，不代表当前树；固定旧 binary 的实际最高版本须由 DEP-MIG-01 重新核验。doctor 的 supported/unsupported 必须按运行 build 的对应 role manifest 计算，禁止硬编码 `2026090801` 永远 unsupported；是否支持该 ID 不等于 legacy repair 已有 attestation，DEP-MIG-02、fingerprint、oracle、确认与备份门均不豁免。

> **测试标准迁移（2026-09-12）:** 本计划尚未执行的所有任务卡及计划收口门采用模板 v2.5：全量测试一律运行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`。既有 focused 命令保持不变；`cargo test --all` 仅限诊断，不构成通过证据。

## 文档职责

本文规划将 Libra 的 SQLite migration 从「任意连接均使用 `builtin_migrations()`」改为按数据库角色选择 migration registry/ledger，并先阻断 global/system config 的污染路径。`2026090801` 已知是 origin/main 的 forward-only convergence barrier，但当前 `0.22.16` build 的 manifest 最高仅为 `2026090601`，且用户 DB 没有唯一 writer PID 归因；因此它是 **known producer, unsupported by this build, ineligible for automatic repair**。只有取得该 producer revision、attestation 和旧/新 binary oracle 后，才允许进入受确认 repair。目标是：仓库专属 schema 演进不能再阻断全局配置读取或远端同步；真正的配置 schema 不兼容仍然 fail-closed；任何历史库修复均为显式、可备份、可审计的前滚操作。

本文只规划任务，不宣称实现完成。落地时每个任务必须先刷新源码、测试、模板和用户文档锚点，再按任务卡验收。

### 适用范围

- `src/internal/db.rs`、`src/internal/db/migration.rs` 的连接、schema compatibility、migration registry 与 ledger 选择。
- `src/internal/config.rs`、`src/utils/client_storage.rs`、`src/cli.rs`、`src/command/config.rs` 的 global/system config 连接路径、诊断与受确认的历史库修复。
- `sql/migrations/`、`tests/db_migration_test.rs`、`tests/compat/global_config_schema_future_test.rs` 以及共享测试环境隔离。
- `docs/development/plan/plan-template.md` 的永久迁移准则，以及本计划实际触及的用户/开发文档、兼容矩阵、错误码和网站文档。

### 非目标

- 不读取、导出、覆盖、删除或手工编辑真实 `~/.libra/config.db` 的配置值或凭据；开发与验证一律使用隔离 fixture。
- 不把 `0.22.16` 的二进制反向修改为支持未知新格式；当前 `2026090801` 个案只能安装其 producer 所匹配的新版二进制，或在用户已持有的已验证备份上执行灾难恢复。
- 不把 Derived database、publish D1 或 repository data-model 的无关演进重构为本计划的一部分。
- 不在没有已验证旧二进制 oracle、备份与原子事务的情况下承诺修复任意历史 global config DB；不满足条件的个案停在 `DEFER-MIG-01`。

### 成功定义

- 每项 runtime migration 都有显式 `DatabaseRole` allowlist；Repository、GlobalConfig/SystemConfig、Derived 的 schema compatibility 和 migration receipt 不再共享「最高 builtin version」语义。
- repo-only migration 打开或升级 repository DB 时，不改变隔离 global-config DB 的 migration receipt 或 config/config_kv 语义。
- `pull`、`push`、`fetch`、`clone`、`cloud` 只会在 **配置角色** 的真正 future schema 时以 `LBR-CONFIG-001` fail-closed；无关 repository receipt 不再造成误阻断。
- 对观测到的 `2026090801`，doctor 分别报告已知 convergence producer、由运行 build 的 role manifest 计算的支持状态，以及独立的 repair eligibility；没有完整 attestation 时保持 ineligible。它不属于自动 repair 承诺。只有 producer revision 已证明且显式确认的候选库才允许前滚 repair，失败保持原库不变。
- 模板的 GC-13 成为后续所有 schema/connection/fixture 计划的强制准则；本计划的任务、测试、文档、发布与 Claude Code 最终 `VERDICT: PASS` 证据齐全后才可完成。

## 事实基线

> 以下锚点在 2026-09-08 核对。真实用户 global DB 只读取 schema receipt；未读取任何 config 值或 secret。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 连接入口 | `establish_connection()` 在打开任何路径后调用无角色参数的 `ensure_database_schema_is_current()` | `src/internal/db.rs:101-118` |
| schema 比较 | 只比较 `current_builtin_schema_version_readonly()` 与 `latest_builtin_schema_version()` | `src/internal/db.rs:481-510` |
| migration 执行 | upgrade 先补多个 repository shape，再执行全部 `run_builtin_migrations()` | `src/internal/db.rs:771-808` |
| bootstrap 写入 | `create_database()` 在 fresh DB 上先跑完整 bootstrap，再调用通用 upgrade | `src/internal/db.rs:824-859` |
| config 写入入口 | `libra config --global/--system` 以通用 `create_database()` 与 `establish_connection()` 写入 scoped config DB | `src/command/config.rs:88-125,186-191` |
| migration registry | `builtin_migrations()` 是唯一 registry，最高版本为 `2026090601` | `src/internal/db/migration.rs:828-1417,1619-1641,1859-1867` |
| 全局配置路径 | `LIBRA_CONFIG_GLOBAL_DB` 优先，否则回退 `~/.libra/config.db` | `src/internal/config.rs:1250-1262` |
| 系统配置路径 | `LIBRA_CONFIG_SYSTEM_DB` 优先，否则回退 `/etc/libra/config.db` | `src/internal/config.rs:1264-1269` |
| 配置读取 | strict cascade 将 global path 作为一个 SQLite scope 查询 | `src/internal/config.rs:1371-1391,1808-1823` |
| 同步阻断策略 | remote/cloud 命令在 global config schema future 且可能读取 global storage config 时返回 `LBR-CONFIG-001` | `src/cli.rs:2085-2168`; `src/utils/error.rs:365` |
| 既有兼容测试 | `compat_global_config_schema_future` 在独立 HOME/`LIBRA_CONFIG_GLOBAL_DB` 下覆盖 remote fail-closed、offline 与 JSON 诊断 | `tests/compat/global_config_schema_future_test.rs:23-115,252-319,507-544` |
| 测试环境风险 | module tests 仍有直接 set/remove `LIBRA_CONFIG_GLOBAL_DB` 的模式；`ScopedEnvVar` 已能恢复先前值 | `src/internal/config.rs:2687-2811`; `src/utils/test.rs:158-196` |
| 现象 | 当前 `/Users/eli/.libra/config.db` 的只读 receipt 查询显示最高 `2026090801`，而安装的 `libra 0.22.16` 只支持 `2026090601`；其 convergence producer 已知，具体 writer PID 仍是 PARTIAL attribution | `docs/development/plan/plan-20260903.md:359,404-407`; 2026-09-08 本地只读 `schema_versions` 查询；`libra status` 警告 |
| 文档基线 | 现有 compat 描述把任何 future global config schema 视为 remote/cloud fail-closed 条件 | `tests/compat/README.md:83`; `tests/INDEX.md:67` |
| 规则模板 | 模板 v2.4 的 GC-13 要求 role-scoped ledger、fixture isolation 和显式历史库 repair | `docs/development/plan/plan-template.md:5-9,27-31,179` |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-MIG-01 | `builtin_migrations()` 和单个 `schema_versions` 最高值被所有连接共用 | repo-only receipt 可把 global config 判为未来 schema，并阻断同步 | `src/internal/db.rs:481-510,771-808` | MIG-02 |
| GAP-MIG-02 | global/system command 的 creation 与 write connection 仍使用 generic DB API | fresh config DB 可被 bootstrap、top-up 与 repo receipt 污染 | `src/command/config.rs:88-125,186-191`; `src/internal/db.rs:771-808,824-849` | MIG-03 |
| GAP-MIG-03 | 当前 `LBR-CONFIG-001` guard 无法区分真正 Config future schema 与 repo-only receipt | 安全策略正确但误阻断合法远端工作流 | `src/cli.rs:2085-2168` | MIG-04 |
| GAP-MIG-04 | 历史 build manifest 为 `2026090601`，当前树已支持 `2026090801`；历史 user DB writer PID 仍未唯一归因，见顶部基线刷新 | 支持该 ID 仍不能替代 producer attestation 与 repair 资格核验 | `docs/development/plan/plan-20260903.md:359,404-407`; `src/internal/db/migration.rs:1859-1867`（历史锚点） | MIG-05、DEP-MIG-02 |
| GAP-MIG-05 | 直接环境变量改写的 module test 易在 panic/early-return 后泄露 global-config 路由 | 测试可意外回退真实 HOME 的 config DB | `src/internal/config.rs:2687-2811`; `src/utils/test.rs:158-196` | MIG-01 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-20260903.md` | 对 0801 convergence semantics、fixture upgrade 与 user DB PARTIAL writer attribution 的已核对历史记录 | 作为只读事实来源引用 `:359,404-407`；不修改、回滚或吸收其任务 |
| `plan-20260908.md` / `plan-20260909.md` | 并行日期计划，工作树可能重叠 | 仅在 ER-01 中保留既有脏状态；禁止写入它们 |
| `plan-long.md` | 没有已批准的长期 migration-role 路线图 | N/A；本计划不更新长期计划 |
| `docs/development/plan/plan-template.md` | 全项目计划治理契约 | MIG-00 固化 GC-13，并由本文按 v2.4 消费 |
| `docs/development/internal/database-migration-scope.md` | 本计划新增的实施设计与 role map | MIG-00 产出并由 MIG-02..06 消费 |

## 评审结论与修订记录

计划成稿前已完成基于当前源码的自审；Claude Code 审查结果必须写入下方的独立日志，且最终必须为字面 `VERDICT: PASS`。

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | 高优先级：真实用户全局库被无关 receipt 推进可阻断同步 | 采用 role-scoped registry，而非教用户手工改数据库 |
| 可行性 | 现有 registry、compat guard、config command path 和隔离 fixture 均有明确落点 | 按测试隔离→registry/bootstrap→写入路由→policy→diagnosis→attested repair 的 DAG 实施 |
| 任务卡粒度 | 7 张卡各自只处理治理、隔离、registry、写入路由、policy、只读诊断或确认式修复一个恢复轴 | 生产卡均为 M；门族 waiver 仅用于不可拆的 doctor/repair fixture 清单 |
| 依赖与顺序 | 无环；MIG-00/01 先于生产变更，MIG-06 最后 | 见实施顺序和 DEP 表 |
| 完整性 | 包含 migration、旧/新兼容、故障注入、docs、release、远端证据 | 每卡写明 migration/rollback 和 ER-06a 判定 |
| 安全性 | 全局 config 可能含凭据；误放行和误写都不可接受 | 只隔离 fixture、secret-free 输出、默认只读 doctor、确认+备份 repair |
| 功能正确性 | 关键风险是 ledger 角色混淆及 legacy repair 错删 receipt | 独立 ledger、manifest allowlist、old-binary oracle、原子事务 |
| 接口兼容 | `LBR-CONFIG-001` 的真实 future-schema fail-closed 契约必须保留 | MIG-04 保留稳定码/JSON 字段，并扩展判定 fixture |
| 数据流与控制流 | bootstrap、top-up、连接自动迁移和 repair 都是数据写入点 | MIG-02 allowlist 单一入口；MIG-03 覆盖 config writer；MIG-06 backup/transaction/fail-closed |
| 性能与容量 | schema checks 不能变成 config hot-path 无界扫描 | ledger 按 role indexed lookup；MIG-06 测试给出 query-count/规模断言 |
| 可靠性与容错 | backup、锁、崩溃窗口和环境恢复均是关键路径 | MIG-01/MIG-06 明确恢复矩阵与故障注入 |
| 可维护性 | registry 和 role policy 若分散会再次漂移 | ADR-MIG-01 规定单一 manifest 与单一 role-aware DB API |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-15 | 用户要求直接收尾，无需等待 release 信息 | 本机最终验收/审查/文档提交不等待远端 D；保留 remote-pending 与真实状态，不取消任务、不宣称成功 | 无新增卡或版本变更 | 顶部用户覆盖、MIG-R03/计划收尾时序与完成判据；R46 独立复核 |
| 2026-09-15 | R44 最终依赖/修订登记审计 | 将已有 MIG-R01 与 MIG-R03 卡内前置显式同步到 DAG，补齐 R30 的修订历史表行；发布窗口一行列出全部实现卡 | 无新增任务卡或行为变更 | 实施顺序、发布窗口、修订历史；原 C/D 与异步执行边界不变 |
| 2026-09-14 | 用户明确要求两项MCP1200s固定到仓库，并重申不等gh即可下一卡 | 追加独立runner配置/生成器/同轴守卫/索引卡；继承最新env参数，重新完整验收、patch+1与异步D | R40执行参数 → MIG-R03（非拆分生产迁移卡） | 发布窗口、卡片、粒度、测试/追溯/风险/里程碑与完成判据；R41独立审查 |
| 2026-09-14 | 固定源码全量暴露 Repository-context 测试失败（R30，原登记见已决议设计决策） | 追加独立测试期上下文恢复卡 MIG-R02；复用隔离 fixture 与 role/ledger manifest，独立 patch/C/D，不扩大生产行为 | MIG-06 后计划全量收口 → MIG-R02 | 实施顺序、发布窗口、卡片/粒度、测试/追溯/风险/里程碑与完成判据；R30 独立 PASS |
| 2026-09-14 | MIG-04 source/binary/website preflight | 固定 old binary/source 实测 max0101；网站 Git cf 已确认；具体化 mutation-only transactional sentinel、bounded known-receipt classifier、五 production 文件和当前 0801 支持事实 | 无卡拆分，原 14 AC/12 gates、4 落点与 M scope | MIG-04、粒度表、DEP 证据、R15 review input；用户不全量/不等 D 覆盖保持 |
| 2026-09-14 | MIG-03 开工 source refresh 与 R14 PASS | 覆盖 strict/exact/prefix/subsection/vault/storage 全部 config reader；四 production 文件，五具名门；集中 literal-path readonly API、保留 legacy fence 并重检 cache；无 policy/barrier 提前发布 | 无卡拆分，MIG-03 routing 轴不变 | MIG-03 写集/门/内部 role-map；R14 P2 处置见卡内 |
| 2026-09-14 | v0.22.20 Windows release 失败：worktree move 在 non-Unix 引用 Unix-only libc，E0433 | 插入独立恢复卡 MIG-R01，仅修正跨平台错误种类判断；保留未提交的 MIG-02 写集，MIG-R01 使用 v0.22.21，MIG-02 随后 v0.22.22；不改已发布 tag，不等待 D | MIG-01 D recovery → MIG-R01 | 发布窗口、粒度表、恢复卡、MIG-01 D evidence；无迁移行为轴扩张 |
| 2026-09-14 | Claude Code 执行期 R10 PASS 的三项 P2 建议 | 刷新 current-tree `2026090801` 基线，doctor 支持判定改为运行 build manifest 驱动而非硬编码旧 build；保留独立 attestation 门；就地标注异步 D 等待覆盖；old-reader oracle 归属修正为 MIG-04/MIG-06 | 无卡拆分 | 顶部基线刷新、成功定义、发布窗口、MIG-05/MIG-06、测试矩阵、DEFER-MIG-01 |
| 2026-09-14 | 用户要求其他提交一并推送，并明确无需等待 gh 发布流程即可继续下一卡 | 已将 `852774d16` 与 MIG-01 一并推送、创建 v0.22.20；后续卡在前卡 C 组已完成后开工，D 组异步记录，不虚报成功 | 无卡拆分 | 顶部异步发布覆盖、MIG-01 C/D 证据、MIG-02 开工 |
| 2026-09-14 | 用户要求提升 timeout 并完成一次测试 | MIG-01 的大型 Rust focused targets 统一使用 1800s 上限；先前中断留下的增量产物出现 linker missing-symbols 后，改以 `CARGO_INCREMENTAL=0` 重新生成，首个 fixture target 已完整通过 | 无卡拆分 | MIG-01 Verification / Execution evidence |
| 2026-09-14 | 用户执行补充：禁用本计划全量构建/测试；每次 bump 均以内容化 notes 执行 `gh` release | 本计划暂停 ER-13 全量门与 all-target/all-feature Clippy，只跑有界 focused/static 门；release 与 D 组不豁免 | 无卡拆分 | 顶部执行覆盖、各卡 Full-suite trigger、发布窗口、完成判据 |
| 2026-09-14 | 用户执行指令：每卡分别提交/推送，所有发布版本 patch +1 | MIG-00 保持 no-release 但单独提交/推送；MIG-01..06 逐卡独立发布并统一 patch +1，MIG-04 的 forward-only 数据兼容语义不再绑定 minor 版本号 | 无卡拆分 | ADR-MIG-02、MIG-00/MIG-04、发布窗口、完成判据 |
| 2026-09-12 | 用户指令：全量测试标准迁移 | 尚未执行任务卡、ER-13 全量门与完成判据收口门改用模板 v2.5 的 `cargo nextest` 命令；focused 命令不变，`cargo test --all` 仅作诊断 | 无卡拆分 | 顶部迁移注记、ER-13、完成判据 |
| 2026-09-10 | 初稿 / 当前源码核对 | 新建 role-scoped migration 与 global-config repair 计划；模板升至 v2.4 并加入 GC-13 | N/A → MIG-00..MIG-06 | 全文、依赖表、测试矩阵、里程碑、风险表 |
| 2026-09-10 | Claude Code R1 `VERDICT: FAIL` | 更正 GC-13 锚点、将 DAG 与依赖收敛为单链、声明单一发布者、按 G-03 逐谓词重写 AC，并将只读 doctor 从 mutating repair 拆为 MIG-04/MIG-05 | MIG-04 → MIG-04, MIG-05 | 实施顺序、DEP、默认表、粒度审计、测试/追溯/里程碑/风险/完成判据、review log |
| 2026-09-10 | Claude Code R2 `VERDICT: FAIL` | 纳入 config command 实际写入、bootstrap/top-up、system override、producer attribution 与一致性备份；拆分写入路由、policy、doctor、repair；当前 `2026090801` 改为 ineligible；补齐 D 组、依赖字段、拆卡闭环与精确 gate | MIG-03 → MIG-03,MIG-04；MIG-04 → MIG-05；MIG-05 → MIG-06 | ADR、GC、DAG、DEP、所有任务卡、测试/追溯/里程碑/风险/review log |
| 2026-09-10 | Claude Code R3 `VERDICT: FAIL` | 采用 0801 已知 convergence 事实；把 storage policy callsite 移入 MIG-04；增加 unsupported receipt、legacy-reader barrier、old-reader zero-write、config bootstrap、doctor absent/future 与 migration fault gates | MIG-02..MIG-06 保持编号，修订既有行为边界 | 事实基线、ADR、DEP、MIG-02..06、测试/性能/review log |
| 2026-09-10 | Claude Code R4 `VERDICT: FAIL` | 将 barrier write、recognition、cascade、old-reader proof 收敛到 MIG-04；MIG-04 重分类为 forward-only migration，补齐 config docs、DEP blocking 与 oracle target registration | MIG-04 only | MIG-02..06、DEP、粒度/审查记录 |
| 2026-09-10 | Claude Code R5 `VERDICT: FAIL` | 明确 barrier 的 migration 类型与 minor/forward-only 兼容窗口；补齐 barrier 写入 owner、事务幂等与 read-path zero-write gates；修正 MIG-01/MIG-02 粒度依赖与审计计数；补齐 config 文档追溯和 DEP-MIG-01 对 MIG-04 的阻断/延后闭环 | MIG-04 gate block、ADR-MIG-02、MIG-02/MIG-04 audit、DEFER-MIG-01 | MIG-02..MIG-04、完成判据、追溯表、审查记录 |
| 2026-09-10 | Claude Code R6 `VERDICT: FAIL` | 要求 MIG-04 将 barrier owner/callsite 落到具体函数并增加 single-writer gate；在卡内写明 DEP-MIG-01 缺失时的 blocked/DEFER 判据；明确整体 compat 重跑不计入 VER 分子 | `write_configuration_barrier(...)`、MIG-04 Blocking condition、门族计数说明 | MIG-04、粒度审计、审查记录 |
| 2026-09-10 | Claude Code R7 `VERDICT: FAIL` | 要求 MIG-06 也具备 DEP-MIG-01/02/03 缺失时的卡内 blocked/DEFER 自足判据；指出 bootstrap 与稳定错误码两处源码锚点偏移 | MIG-06 Blocking condition、bootstrap 锚点 `824-859`、error 锚点 `error.rs:365`；同时补旧 reader 命令 `.env.test` 前缀 | MIG-06、事实基线、审查记录 |
| 2026-09-10 | Claude Code R8 `VERDICT: FAIL` | 要求 MIG-04、MIG-05 对 `DEP-MIG-03`（后端 `cf` 分支/网站文档）分别写出卡内 blocked/停止判据，避免依赖表外推导致文档或发布绕过 | 已在 MIG-04/MIG-05 增加 `Blocking condition`，明确 `Lifecycle=blocked`、`Acceptance` 非 complete、暂停网站写入/发布；R9 待复审 | MIG-04、MIG-05、审查记录 |
| 2026-09-10 | Claude Code R9 `VERDICT: PASS` | 完整复核 R8 修复、所有历史 P0/P1、迁移/备份/旧 reader/文档与 G-03 门族计数；确认无未关闭 P0/P1 | 计划达到 Claude PASS 审查门；仅保留执行阶段的实现与 C/D 证据工作 | 全计划、模板、相关源码/测试/文档锚点 |

## 已决议设计决策

**2026-09-14 R30 规范性修订登记（已 PASS，详见卡内）:** 追加 MIG-R02，承接固定源码全量的 Repository-context 测试失败；同步 DAG、粒度表、最终发布窗口、追溯与里程碑及完成判据。仅测试期作用域/fixture，不新增生产行为或 waiver；C 前完整全量不豁免。独立审查须明确判断 G-01 是否成立，未 PASS 前不改其代码写集。

### ADR-MIG-01: migration registry、receipt 与兼容检查按数据库角色分离

- **Status:** Accepted
- **Context:** 当前 `establish_connection()`、schema inspector 和 upgrade runner 对任意 DB 路径使用一个 builtin registry/最高版本。全局配置数据库只需要配置相关表，却可被 repository operation/worktree migration receipt 推进并被旧客户端拒绝。
- **Decision:** 引入显式 `DatabaseRole::{Repository, GlobalConfig, SystemConfig, Derived}`，由一个 role-aware schema manifest 为每项 migration、fresh bootstrap DDL 与 non-migration idempotent top-up 声明 allowlist。`Repository` 继续使用现有 repository receipt；`GlobalConfig` 与 `SystemConfig` 使用独立的 configuration receipt/registry，只登记 configuration schema 实际需要的 DDL；`Derived` 不得经过 runtime schema writer。新 role-aware open、inspect、upgrade 和 create API 必须显式接收 role；兼容期保留的 generic wrapper 固定为 `Repository`，且不得被 config callsite 调用。禁止根据路径字符串猜测 role。每一 role 的 latest/current 比较只使用自己的 registry/ledger。
- **Alternatives considered:** 保留单一 registry、只让 CLI 忽略 future receipt——拒绝：会将真正不兼容的 config schema 静默用于远端凭据/存储配置。仅向旧 binary 添加例外——拒绝：不能修正新连接继续错误写入的问题。以路径前缀自动猜 role——拒绝：`LIBRA_CONFIG_GLOBAL_DB`/system override 与 test fixture 使路径不是可靠语义。
- **Consequences:** 新 migration、bootstrap 或 top-up 增加时必须标注 role allowlist，并同时覆盖每个受影响 role。当前 generic receipt 不会被新 global-config reader 当作 configuration compatibility 的唯一事实；历史 conversion 由 ADR-MIG-02 的显式 repair 处理。
- **Revisit when:** `SystemConfig` 获得与 `GlobalConfig` 不同的 schema、或引入新的持久化 database role 时。

### ADR-MIG-02: 历史 global-config receipt 只经确认式前滚 repair 处置

- **Status:** Accepted
- **Context:** 已观察到 global config DB 的 generic receipt 高于安装 binary。`2026090801` 的 origin/main convergence semantics 已知，但 user DB 的唯一 writer PID 未建立归因；该 receipt 还高于当前 build 最大 `2026090601`。直接删 receipt、覆盖 SQLite 文件或自动 downgrade 都可能损失未知状态，且会破坏 fail-closed 安全边界。
- **Decision:** 提供 `libra config doctor --global-schema` 的只读诊断，输出 receipt、known-convergence / unsupported-by-build disposition 与非敏感 file metadata。只有 `--repair --confirm <canonical-config-db-path>` 可执行 repair，且只有 DEP-MIG-02 提供完整 source/binary attestation 的 receipt 才可能 eligible。repair 必须在目标锁定后重做资格核验，校验 canonical target，用 role manifest 判定每个 receipt，以 SQLite backup API 或等价 SQLite-consistent 方法建立可重开验证的 backup，并在单个 SQLite transaction 中初始化 config-specific ledger 和转换允许 receipt；unattested receipt、表 fingerprint 不符、备份/锁/事务失败都不写库并给出稳定、secret-free 诊断。repair 的资格必须同时由固定旧 binary/source oracle 证明，不能用“看起来无关”代替。
- **Compatibility window:** configuration-owned legacy-reader barrier is a compatibility migration: MIG-04 uses the user-mandated `patch + 1` release increment plus `forward-only`, and its release gate must prove the pinned old reader rejects the barrier before any write while the same-build Config-role reader accepts it. Version numbering does not authorize downgrade or rollback.
- **Alternatives considered:** 打开 config DB 时自动修复——拒绝：用户全局凭据库不应由普通读取隐式改写。让用户用 sqlite 手改 schema_versions——拒绝：不可审计且易数据损失。永久保留 generic receipt 并要求所有旧版本升级——拒绝：不能阻断在 configuration schema 未变化时的可验证兼容窗口。
- **Consequences:** `2026090801` 在完整 attestation 到手前保持阻断并进入 `DEFER-MIG-01`；本计划不把它写成可 repair。repair 只前滚到清晰的双-ledger 模型。文档必须明确备份位置、恢复步骤、不可自动 repair 的情形和安装 producer-matched binary 的升级路径。
- **Revisit when:** 固定 0.22.16 oracle 无法获得或发现 config DB 中有额外 role-coupled state 时；此时停止 repair 实施，不放宽 allowlist。

## 全局工程约束

本计划继承 `plan-template.md` v2.4 的 GC-01..GC-13 全文，尤其是 GC-07（fail-closed）、GC-08（原子性与恢复）、GC-09（隔离与资源生命周期）、GC-12（精确暂存）和 GC-13（role-scoped migration/ledger、global config fixture isolation、禁止手改 receipt）。以下为本计划附加约束。

- **GC-MIG-01 隔离优先:** 任何测试、review 命令和临时脚本必须显式指向同一 tempdir 下的 `LIBRA_CONFIG_GLOBAL_DB` 与 `LIBRA_CONFIG_SYSTEM_DB`；不得仅依赖 `LIBRA_HOME`，因为 global/system config 均有环境回退。测试结束必须恢复先前环境，并以 sentinel/canary 证明真实用户 global 与 system config 路径未被打开或写入。
- **GC-MIG-02 迁移单一事实源:** `DatabaseRole`、migration/bootstrap/top-up allowlist、role ledger 名称、latest-version 计算和 compatibility classifier 必须来自同一 manifest/API；CLI/config/client-storage 不得复制 version comparison。
- **GC-MIG-03 legacy repair 安全:** repair 默认只读；mutating path 必须要求 canonical-path confirmation、目标锁下 eligibility recheck、SQLite-consistent 且可重开验证的 backup、事务原子性、receipt/table-shape allowlist 和旧 binary oracle。禁止 secret 输出、自动执行、手写 SQL 建议或无备份回滚。
- **GC-MIG-04 兼容界限:** 只有真正属于 `GlobalConfig`/`SystemConfig` 的 future schema 才可阻断远端配置读取。`--offline`/local-only 的既有降级语义、稳定码 `LBR-CONFIG-001` 和 secret-free JSON/human diagnostics 必须保留并测试。

## 执行检查必备需求（强制）

本计划继承模板 v2.4 的 ER-01..ER-13 全文。以下补充要求与模板并列生效。

- **ER-MIG-01 双重计划审查:** 本文每一轮规范性修改后必须由 Codex 自审和 Claude Code 独立审查。Claude Code 的最终输出必须含字面 `VERDICT: PASS`；任何 P0/P1 关闭后重审，不以 silence、局部测试或“看起来合理”替代 PASS。
- **ER-MIG-02 migration 预检:** 每张 migration/implementation 卡开工前均须用隔离 Repository、GlobalConfig、SystemConfig fixture 构造当前/旧/future schema 矩阵；不得通过打开真实 user DB 取得“基线”。
- **ER-MIG-03 release 次序:** MIG-01 → MIG-02 → MIG-03 → MIG-04 → MIG-05 → MIG-06 严格串行实施和发布。每张 implementation/migration 卡依 ER-08 单独 patch/release；后续卡的基线必须包含此前已发布 card。
- **ER-MIG-04 远端证据:** 每个发布卡必须登记并取得 `D-MIG-STD` 的全部 post-push 证据；任一失败只允许前滚。

## 实施顺序

- `MIG-00 -> MIG-01`
- `MIG-01 -> MIG-R01`（MIG-01 D failure 的独立前滚恢复卡）
- `MIG-01 -> MIG-02`
- `MIG-02 -> MIG-03`
- `MIG-03 -> MIG-04`
- `DEP-MIG-01 -> MIG-04`
- `DEP-MIG-03 -> MIG-04`
- `MIG-04 -> MIG-05`
- `DEP-MIG-03 -> MIG-05`
- `MIG-05 -> MIG-06`
- `MIG-01 -> MIG-R02`（复用隔离 fixture，修复验收上下文；独立 patch 发布）
- `MIG-02 -> MIG-R02`（复用已发布的 role/ledger manifest；发布窗口排在 MIG-06 之后）
- `MIG-R02 -> MIG-R03`（以 MIG-R02 C 为开工前提；D 异步取证，不阻塞下一卡）
- `DEP-MIG-01 -> MIG-06`
- `DEP-MIG-02 -> MIG-06`
- `DEP-MIG-03 -> MIG-06`

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-MIG-01 | incoming | 上游 release | 可复现的 Libra `0.22.16` binary 或 tag-pinned source oracle | Libra release maintainer | `LIBRA_TEST_OLD_BINARY` 与 `LIBRA_TEST_OLD_BINARY_SHA256` 由 release pin 提供；`old_reader_oracle_test` 必须以 `required-features=test-upgrade` 和 `#[ignore]` 注册，只有 C-group 命令设置 `LIBRA_ENABLE_OLD_READER_ORACLE=1` 后才执行 | 2026-09-08 已安装 binary 输出 `0.22.16`；开工依 ER-02 重新 pin hash | 缺失时 C-group old-reader gate 不得跳过；MIG-04/MIG-06 不得 complete，登记 `DEFER-MIG-01` |
| DEP-MIG-02 | incoming | 本仓库 origin/main provenance | receipt `2026090801` 的 convergence source revision、migration manifest 与 producer binary hash | Libra repository release maintainer | manifest 将 `2026090801` 映射到具体 migration/DDL；source revision 与 binary hash 可复验；不含 config 值 | `plan-20260903.md:359,404-407` 已定位 forward-only barrier；开工重新 pin source/hash | 未取得完整 attestation 即当前个案恒为 ineligible；MIG-06 `blocked`，用户只能安装 producer-matched binary 或使用既有验证备份 |
| DEP-MIG-03 | incoming | 相邻文档仓库 | `../libra-backend` 的唯一 VCS 类型与 `cf` 分支 | libra-backend maintainer | 状态命令证明只存在 `.libra` 或 `.git` 一种元数据；当前 branch=`cf` | ER-06a 指定的开工检查输出与日期 | 无法确认或切换 `cf` 时停止受影响卡的文档写入、发布和 `Acceptance=complete`，不绕过 |

### 发布分组与并发窗口

**发布者:** `/root` 主代理；只有用户书面移交后，才可在本节和修订历史中更名。发布者负责所有 C 组的 bump、构建、安装、精准暂存、提交、branch/tag push、`gh release create`，以及 D 组证据；同一时刻只运行一张卡的发布窗口。

本计划不采用家族卡或批量发布。MIG-01..MIG-06 均为独立发布切片，但按 ER-MIG-03 串行；它们的实现写集或兼容语义存在前后依赖，不得并发。MIG-00 是 no-release 文档卡，继承 MIG-01 的最终 C/D 覆盖。

**并发声明:** `none`；MIG-01..MIG-06 的 I 写集存在前后依赖，且 R 写集由唯一发布者锁定。MIG-00 只在 MIG-01 发布窗口中被精准暂存。

**发布窗口顺序:** `MIG-01 -> MIG-R01 -> MIG-02 -> MIG-03 -> MIG-04 -> MIG-05 -> MIG-06 -> MIG-R02 -> MIG-R03`；MIG-R01 是 v0.22.20 D 失败后的独立恢复卡；MIG-02 已开始的写集在其发布时保留未提交。MIG-R02/R03 是随后用户验收/固定配置请求追加的窗口，详见下两段。依顶部异步发布覆盖，前卡取得 C evidence 后可开启后续窗口，D evidence 异步跟踪，未成功前保持 `remote-pending`。

**全量恢复窗口补充（R30 PASS）:** 在 MIG-06 已完成 C、D 与 CI 性能门后，追加 `MIG-R02` 处理固定源码全量暴露的测试上下文错误。其 C 步仍独立 patch +1、提交、推送、内容化 `gh release create`，不得合并到已发布 .26 或跳过全量；发布窗口保持单一发布者串行。当前基线全量的源码/target 冻结直至该进程结束；可在另一个 task-owned worktree 准备修复，不能再次令测试 binary 与被扫描源码来自不同状态。修复后的完整全量必须在最终 bump 状态全绿后才能推送本卡，不用旧绿灯、过滤或改成 ignore 代替。

**用户追加固定配置窗口（R41 待审）:** MIG-R02 C 全量、signed+DCO commit `0ef805fa12c2a61b7e9c31ceb7e227fb9c9a5e50`、main push 与内容化 v0.22.27 Release 已完成。用户明确要求将 R40 的二例 20 分钟配置写入仓库，故在其后追加独立 `MIG-R03`，发布序为 `MIG-R02 -> MIG-R03`；MIG-R02 的 D 异步验收不阻塞新卡开工，但仍须取得。新卡只持久化 runner 超时策略及同轴守卫，独立 patch +1 / signed+DCO / push / gh，不把配置源码变化追记到 .27 的全量证据。既有两阶段完整 nextest 方法允许本卡使用，但必须重新完整预编译并核对本卡自己的 version/source/metadata/listing；无过滤运行全部实际 non-ignored cases，保留原有 ignored 集合，不能复用 .27 的旧源码 hash 或旧全量结果。新调用 source 用户最新双 env（当前 build28/test16），不强制 4/4；MCP repo override 为1200s，其它仍可由 task-owned tool config 保持600s，仓库串行分组不变。

**D-MIG-STD（每张 independent 卡均适用）:** branch push `libra push origin main` 后观察 `.github/workflows/codeql.yml` 的 job `analyze`（event=`push`，ref=`main`，结论=success）；`gh release create v<version>` 触发 tag push 后观察 `.github/workflows/release.yml` 的 jobs `build-and-upload`、`upload-install-scripts`、`update-homebrew-tap`、`verify-homebrew-formula`、`request-stable-manifest`（event=`push`，ref=`v<version>`，结论=success），并记录对应 Cloudflare upload 与 `download.libra.tools` artifact 可下载。每项 run URL、tag、下载 URL 与检查日期进入该卡 D 证据；失败前滚。

### Phase 0: 治理基线和安全隔离

- MIG-00：固化 GC-13 与 role contract。
- MIG-01：封闭 global/system config fixture 的环境恢复和真实路径防护，并承载 MIG-00 docs 的首个 release。

**Checkpoint P0:** 模板、内部 role map 和共享 fixture 已就位；fixture 能证明 panic/early-return 后恢复环境，且未打开真实 global config 路径。

### Phase 1: 角色化 migration 基础

- MIG-02：实现 role-scoped registry、ledger、bootstrap/top-up、inspect/open/upgrade 单一入口。

**Checkpoint P1:** isolated repository migration 不推进 isolated GlobalConfig/SystemConfig receipt；bootstrap/upgrade/future compatibility 按角色可重复验证。

### Phase 2: 配置使用路径与兼容策略

- MIG-03：把 global/system config 的 create/open/read/inspect writer path 接到 Config role。
- MIG-04：以 Config role 实施 remote/cloud future-schema policy 与同卡文档同步。

**Checkpoint P2:** config writer 不再执行 Repository bootstrap/top-up；true Config future schema 仍安全阻断；Repository-only receipt 不再阻断 remote command；稳定错误和 secret redaction 都通过 compatibility target。

### Phase 3: 只读诊断和历史库的确认式修复

- MIG-05：交付只读 global-schema doctor、producer/mtime 诊断和无写入证明。
- MIG-06：仅对 DEP-MIG-02 attested receipt 交付一致性备份、确认式 atomic repair 和旧 binary oracle。

**Checkpoint P3:** doctor 默认不改 schema/config 数据，按运行 build manifest 报告 `2026090801` 的 known Repository convergence/support 状态，未 attested 仍 ineligible；只有 attested eligible fixture repair 后可被新 reader 使用，固定旧 reader 必须 fail-closed without write；任何 unattested/损坏/备份失败路径不改主库，且最终 release evidence 已收集。

## 任务卡

### 任务卡粒度规则（强制）

以下任务均按模板 G-01..G-11 自审：每卡只有一个可独立恢复的行为轴；测试与所改行为同卡；实现写集不并发相交；不存在 L/XL。MIG-06 的门族 waiver 按模板白名单完整登记。ER-04 强制门不计入下文 AC/Verification 分子。

#### 字段全局默认与例外

- **Release boundary 默认:** `independent`（仅 MIG-01..MIG-06；MIG-00 为 `no-release`）。
- **Task type 默认:** `implementation`。
- **Rollback mode 默认:** `forward-only`（涉及 role ledger 或历史 DB 的卡）；MIG-00/MIG-01 明确覆盖。
- **Migration and rollback 默认:** `N/A`；migration 卡必须另写 up/forward/recovery。
- **Security and privacy 默认:** 继承 GC-07、GC-11、GC-13、GC-MIG-01..04。
- **Performance budget 默认:** 继承 GC-10；schema checks 不得引入无界表扫描。

**默认覆盖：**

| 任务 | 偏离字段 | 取值与理由 |
|---|---|---|
| MIG-00 | Task type / Release boundary / Rollback mode | `docs` / `no-release` / `revert`；仅治理文档 |
| MIG-01 | Rollback mode / Migration and rollback | `revert` / N/A；共享测试隔离不修改持久化格式 |
| MIG-02 | Task type | `migration`；引入 role-specific ledger，必须前滚 |
| MIG-04 | Task type / Rollback mode / Version increment | `migration` / `forward-only` / `patch`；依 2026-09-14 用户指令统一 patch +1；configuration-owned barrier 仍禁止 downgrade/rollback |
| MIG-05 | Rollback mode / Migration and rollback | `revert` / N/A；只读 doctor 不改变数据库 |
| MIG-06 | Task type / Rollback mode | `migration` / `forward-only`；可能转换 attested global DB receipt，禁止自动 down |

**规则 waiver（`EX-*`）：**

| 例外 ID | 任务 | 豁免项 | 理由与补偿措施 | Approver | Review round | 证据 | 有效期 |
|---|---|---|---|---|---|---|---|
| EX-MIG-01 | MIG-05 | G-03 条目上限（门族型验收） | 同一只读 diagnostic axis 需要逐一覆盖 command、role、receipt、provenance、mtime、zero-write、backup absence、redaction 与 docs；拆卡会让同一 public diagnostic contract 分裂。补偿：下方「门族判据规范」逐项列出具名可复制命令；任一失败整卡不达标。 | Eli（本计划请求者） | Claude R9 PASS | 本计划 `MIG-05` 门族规范与 Claude verdict | 仅本计划 |
| EX-MIG-02 | MIG-06 | G-03 条目上限（门族型验收） | 同一不可拆的 atomic repair recovery axis 需要逐一覆盖 confirmation、attestation、锁下 recheck、backup、transaction、old/new reader 与 failure fixtures；拆卡会让同一 mutation 与恢复契约分离。补偿：下方「门族判据规范」逐条列出具名可复制命令；任一失败整卡不达标。 | Eli（本计划请求者） | Claude R9 PASS | 本计划 `MIG-06` 门族规范与 Claude verdict | 仅本计划 |
| EX-MIG-03 | MIG-04 | G-03 条目上限（门族型验收） | 同一 configuration compatibility axis 的 remote preflight、cascade、legacy barrier and old-reader fixtures cannot be released separately without a fail-open/fail-closed window；15 AC / 12 named gates。 | Eli（本计划请求者） | Claude R9 / R15 PASS | MIG-04 gate specification and Claude verdict | 仅本计划 |

#### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| MIG-00 | docs | migration-governance contract | revert docs | yes | yes | 5/20 | 4/20 | 2/0 | S | none | no-overlap | no-release | N/A | N/A |
| MIG-01 | implementation | global/system config test sandbox | revert helper/tests | yes | yes | 8/8 | 5/8 | 2/2 | M | MIG-00 | serialized before MIG-02 | independent | N/A | N/A |
| MIG-R01 | implementation | portable cross-device move error classification | forward-fix release compilation | yes | yes | 3/8 | 2/8 | 1/1 | S | MIG-01 D failure | separate from retained MIG-02 files | independent | release recovery | N/A |
| MIG-02 | migration | role-scoped schema manifest and ledger | forward-only; preserve generic receipt | yes | yes | 8/8 | 6/8 | 4/4 | M | MIG-01 | serialized before MIG-03 | independent | N/A | N/A |
| MIG-03 | implementation | config writer role routing | revert routing | yes | yes | 8/8 | 5/8 | 4/4 | M | MIG-02 | serialized before MIG-04 | independent | N/A | N/A |
| MIG-04 | migration | configuration compatibility barrier | forward-only | yes | yes | 15/8@EX-MIG-03 | 12/8@EX-MIG-03 | 4/5 | M | MIG-03,DEP-MIG-01,DEP-MIG-03 | serialized before MIG-05 | independent | split from MIG-03 | EX-MIG-03 |
| MIG-05 | implementation | read-only global-schema doctor | revert command/docs | yes | yes | 12/8@EX-MIG-01 | 12/8@EX-MIG-01 | 3/3 | M | MIG-04,DEP-MIG-03 | serialized before MIG-06 | independent | split from MIG-04 | EX-MIG-01 |
| MIG-06 | migration | attested legacy global-receipt repair | forward-only; verified backup restoration | yes | yes | 16/8@EX-MIG-02 | 16/8@EX-MIG-02 | 4/6 | M | MIG-05,DEP-MIG-01,DEP-MIG-02,DEP-MIG-03 | final serial card | independent | split from MIG-05 | EX-MIG-02 |
| MIG-R02 | implementation | explicit Repository context in acceptance tests | revert test-only context changes before release; forward-fix afterward | yes | yes | 6/8 | 7/8 | 3/3 (conservative src-path count; cfg(test)-only) | M | MIG-01,MIG-02 | isolated from frozen baseline; final serial recovery | independent | full-suite recovery | N/A |
| MIG-R03 | implementation | persistent bounded MCP test timeout | revert runner policy before release; forward-fix afterward | yes | yes | 8/8 | 5/8 | 2/2 (conservative infrastructure count) | S | MIG-R02 C | serial after MIG-R02 | independent | explicit user configuration request | N/A |

### Task MIG-00: 固化数据库迁移作用域准则与实施 role map

**Task type:** `docs`

**Lifecycle / Acceptance:** `done` / `complete`（本卡 A/B 与独立 docs commit 已完成；v0.22.20 原失败保留，由 MIG-R01 / v0.22.21 的成功 C/D 前滚覆盖，见 MIG-R01 D closure）

**Description:** 固化 GC-13（若基线缺失则写入模板；当前计划起草 diff 已含该规则），并在内部设计文档中冻结数据库角色、ledger、allowlist、fixture isolation 和历史 repair 的词汇/边界。本卡唯一行为轴是使后续计划和实现使用同一 migration 治理契约。

**Out of scope:**

- 不改 runtime migration runner（由 MIG-02 承接）。
- 不改任何 config 路由、future-schema policy 或用户命令（由 MIG-03/MIG-04/MIG-05/MIG-06 承接）。
- 不读取或修复真实 global config DB（永久非目标；仅隔离 fixture）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 模板当前 v2.6 保留 v2.4 引入的 GC-13；role-map design document 尚未在开工基线中存在 | `docs/development/plan/plan-template.md:7,33,182`; 2026-09-14 开工前 `test ! -e docs/development/internal/database-migration-scope.md` |
| generic runner 的完整 registry 是当前唯一事实源 | `src/internal/db/migration.rs:828-1417,1619-1641` |

**Acceptance criteria:**

- [x] `plan-template.md` 的版本说明和 GC-13 要求每项 role allowlist。
- [x] GC-13 覆盖 migration、bootstrap、top-up、ledger、compatibility 与 global/system fixture isolation。
- [x] `docs/development/internal/database-migration-scope.md` 定义四个 DatabaseRole、ledger、owner 与 migration author checklist。
- [x] 设计文档链接 ADR-MIG-01/02，并说明 unattested receipt 的 ineligible 边界。
- [x] 无任何用户命令或配置值行为在本卡改变；ER-06a 判定为 N/A，因为本卡只冻结内部开发契约，未修改命令、输出、配置或网站表面。

**Verification:**

- [x] `test -f docs/development/internal/database-migration-scope.md && rg -n "GC-13|DatabaseRole|GlobalConfig|SystemConfig|Derived|receipt" docs/development/plan/plan-template.md docs/development/internal/database-migration-scope.md`
- [x] `rg -n "ADR-MIG-01|ADR-MIG-02|MIG-00" docs/development/plan/plan-20260910.md docs/development/internal/database-migration-scope.md`
- [x] VCS `status --short --branch`；相对 ER-01 clean 快照的增量仅为本卡 role-map、计划状态与用户指定的发布版本口径修订。

**Full-suite trigger:** `N/A`（no-release docs card）。

**Dependencies:** `无`

**Deliverables:** `docs/development/plan/plan-template.md`、`docs/development/internal/database-migration-scope.md`。

**Implementation write set:** `docs/development/plan/plan-template.md`、`docs/development/internal/database-migration-scope.md`。

**Release write set:** `N/A`

**Files likely touched:** 同 Deliverables。

**Docs and compatibility impact:** `N/A`：仅开发计划治理和内部设计，未改变用户可见命令、参数、输出、错误或配置；不写 `docs/commands/**` 或 `../libra-backend`。

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** 文档不得记录真实 config 路径以外的用户标识、配置值、token、备份内容或 schema dump。

**Performance budget:** `N/A`

**Estimated scope:** `S`

**Version increment:** `N/A`

**Release boundary:** `no-release`

**C/D coverage from:** `MIG-01`（MIG-01 的精确暂存必须包含已冻结的本卡 Deliverables，并在其 D 证据中覆盖）。

**Granularity:** `type=docs; axis=migration-governance contract; recovery=revert docs to the prior template/design wording; complete=yes; self-contained=yes; AC=5/20; VER=4/20; landing=2; prod-files=0; scope=S; deps=none; writeset=no-overlap; release=no-release; split-from=N/A; exception=N/A`

**Execution evidence (2026-09-14):** ER-01 发现当前 checkout 实际为 `main...origin/main` 的 clean Git repository（存在 `.git`，不存在计划所假定的 `.libra`）；因此只读状态、精确暂存、签名+DCO 提交与非强制推送使用当前唯一可用的 Git CLI，不初始化或转换仓库。ER-02 刷新确认模板已升级到 v2.6 且仍完整保留 GC-13，内部 role-map 文件不存在；ER-03 复核后行为轴、5 项 AC、4 项 VER 与 S scope 不变。用户在本轮要求每卡分别提交/推送并要求发布版本均为 patch +1；MIG-00 仍依计划为 no-release，独立提交/推送但不制造无实现内容的 release，后续 release 卡逐卡 patch +1。卡片特有的 structure/link/status 门、`git diff --check`、`cargo +nightly fmt --all --check` 与 `cargo clippy --all-targets --all-features -- -D warnings` 均通过；Clippy 仅报告第三方 `proc-macro-error2` 的 future-incompatibility 提示，无 lint 失败。当前源码自审 `VERDICT: PASS`：设计文件完整覆盖 GC-13 角色/ledger/writer/fixture/repair 边界，未发现 P0/P1/P2；本卡没有 runtime、用户配置值或公开命令行为变化，ER-06a=N/A。C/D coverage 仍按计划由 MIG-01 承接。

### Task MIG-01: 封闭 global/system config migration fixture 的环境和文件系统边界

**Task type:** `implementation`

**Lifecycle / Acceptance:** `done` / `complete`（MIG-R01 / v0.22.21 的 D 前滚覆盖已取得；不改写 v0.22.20 原失败事实）

**Description:** 为会打开/创建 global 或 system config DB 的 Rust unit、integration 和 compat 测试建立可恢复的 sandbox helper，并迁移当前直接修改 override 的相关测试。唯一行为轴是防止测试在成功、失败、panic 或并发边界后回退到真实用户或系统配置路径。

**Out of scope:**

- 不改变 runtime 数据库角色或 migration 语义（由 MIG-02 承接）。
- 不改生产 global/system config 路由（由 MIG-03 承接）。
- 不把全仓所有环境变量测试作无关重写；只迁移触及 global-config DB 的用例和共享 helper。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `ScopedEnvVar` 在开工基线只保存并 Drop 恢复单个变量，缺少组合 sandbox | `src/utils/test.rs:158-196`（2026-09-14 刷新） |
| config module/command tests 在开工基线直接 set/remove global/system DB override | `src/internal/config.rs:2971-3347`; `tests/command/config_test.rs:35-1685`（2026-09-14 刷新） |
| compat/command child fixture 在开工基线使用 isolated HOME/XDG/global DB，但未统一设置 system DB override | `tests/compat/global_config_schema_future_test.rs:23-74`; `tests/command/mod.rs:65-88`（2026-09-14 刷新） |

**Acceptance criteria:**

- [x] scoped guard 设置 `LIBRA_CONFIG_GLOBAL_DB` 到 tempdir。
- [x] scoped guard 设置 `LIBRA_CONFIG_SYSTEM_DB` 到 tempdir。
- [x] scoped guard 设置 HOME 到 tempdir。
- [x] scoped guard 设置 USERPROFILE 到 tempdir。
- [x] scoped guard 设置 XDG_CONFIG_HOME 到 tempdir。
- [x] scoped guard 在 Drop 后恢复 caller 的原值。
- [x] global-config fixture 只在 tempdir 创建 SQLite 文件。
- [x] system-config fixture 只在 tempdir 创建 SQLite 文件。

**Verification:**

- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test db_migration_test config_db_fixture_ -- --test-threads=1`（new test prefix；从超时的 monolithic lib-test link 收敛到既有 focused target）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test compat_global_config_schema_future -- --test-threads=1`。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test compat_all_production_unwrap_guard -- --test-threads=1`。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test command_test command::config_test::config_scope_uses_isolated_db -- --exact --test-threads=1`（new）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test compat_serial_registry -- --test-threads=1`（shared serial registry / nextest group guard）。

**Full-suite trigger:** 原判定 `T-4: 触及 src/utils/test.rs 与 tests/command/mod.rs 的共享测试基础设施`；依 2026-09-14 用户执行覆盖不运行全量，以本卡 focused target + serial registry 静态守卫替代并显式记录残余风险。

**Dependencies:** `MIG-00（GC-13 与 role/fixture contract）`

**Deliverables:** `N/A`

**Implementation write set:** `src/utils/test.rs`、`src/internal/config.rs`（仅其 test module）、`tests/db_migration_test.rs`、`tests/compat/global_config_schema_future_test.rs`、`tests/command/config_test.rs`、`tests/command/mod.rs`（仅 child-process system config override 与 fixture re-export）、`tests/INDEX.md`、必要时 `tests/SERIAL_REGISTRY.tsv` 与生成的 `.config/nextest.toml`。MIG-00 已依 2026-09-14 用户指令单独提交/推送，不重复暂存其 Deliverables。

**Release write set:** `ER-08 版本面五处`、工具链生成的 `Cargo.lock`、release artifact。

**Files likely touched:** 同 Implementation write set。

**Docs and compatibility impact:** `N/A`：测试隔离实现不改变用户命令、参数、输出、错误或配置；不写 `docs/commands/**` 或 `../libra-backend`。

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** helper 禁止接受真实 HOME 路径作为 fixture 目标；测试日志和 assertion 不得输出 ambient config 值或 secret。

**Performance budget:** fixture setup/teardown 仅 tempdir 和常量个环境变量；不得在全量测试中扫描用户目录。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**D post-push:** `D-MIG-STD`；本卡 D 证据还要列出 MIG-00 Deliverables 随本 release 的 commit/tag。

**Granularity:** `type=implementation; axis=global/system config test sandbox; recovery=revert helper and migrated tests to the prior isolated-fixture pattern; complete=yes; self-contained=yes; AC=8/8; VER=5/8; landing=2; prod-files=2; scope=M; deps=MIG-00; writeset=serialized before MIG-02; release=independent; split-from=N/A; exception=N/A`

**Execution evidence (2026-09-14):** ER-01 基线为 `main...origin/main` clean；MIG-00 commit `4d6feac8b` 已推送。开工后共享工作树另有独立 commit `852774d16`（仅 `plan-20260907.md`/`plan-20260913.md`），本卡不修改或暂存其文件，推送前必须确认该 commit 已由其 owner 交付。ER-02 刷新确认 `ScopedEnvVar` 只管理单变量、config module/command tests 仍有直接 global/system override，compat/command child fixture 未一律设置 system override。ER-03 在同一 test-sandbox 行为轴内把 `tests/command/mod.rs` 的 child system override 纳入写集；scope 仍为 M，无 production runtime 行为变化。实现新增拥有自身 tempdir 的五变量 `ConfigDbFixture`，迁移 config unit/command/compat child fixtures，并增加 Drop、early-return、panic、SQLite 路径与 command 双 scope 回归。初次 monolithic lib target 被 SIGKILL；300s focused 尝试仍停在活跃 codegen，用户随后要求提高时限。1800s 重跑先在链接阶段暴露被中断增量快取的 missing-symbols，改用 `CARGO_INCREMENTAL=0` 后 fixture 3/3、global-schema compat 12/12、production unwrap 1/1、精确 command 1/1、serial registry 18/18 均通过；serial 首轮发现的 4 个 lane 顺序漂移已修正后重跑全绿。bump 后以 `libra v0.22.20` 再次完成 fixture 3/3、上述 compat 31/31（含 version surface 2/2）及精确 command 1/1；`cargo +nightly fmt --all --check`、`bash -n install.sh`、`git diff --check` 通过。依用户执行覆盖未运行全量 build/test/clippy，本卡残余风险是共享 helper 未获全套件覆盖；远端 release 构建与 D 组仍待 C 组触发。Codex review `VERDICT: PASS`：未发现 P0/P1/P2，测试路径均 fail-closed 到 fixture root，production runtime 行为未改变，ER-06a=N/A。

**C / D publication evidence (2026-09-14):** 用户明确授权把其他未推送提交一并推送，解除上述父提交协调点。`git push origin 7df326fa3:refs/heads/main` 成功，随后 `git ls-remote` 证实 main 为 `7df326fa33c6708a2d0c1dc015d05843316bee2e`，其中包含 `852774d16` 与 MIG-00 的 `4d6feac8b`。MIG-01 commit 含 SSH `gpgsig` 与 DCO trailer。`gh release create v0.22.20 --target 7df326fa33c6708a2d0c1dc015d05843316bee2e --notes-file ...` 已成功，发布说明按 v0.22.19..v0.22.20 实际 diff 总结；[Release](https://github.com/libra-tools/libra/releases/tag/v0.22.20) 与远端 tag 均已核实。发布构建 [run 34805627444](https://github.com/libra-tools/libra/actions/runs/34805627444)（push / v0.22.20）及 [CodeQL run 34805577647](https://github.com/libra-tools/libra/actions/runs/34805577647)（push / main）仍在运行，D 组尚未完成。计数更正：post-bump 四个 compat targets 共 **33/33**（1 + 12 + 18 + 2），加 fixture 3/3 与 command 1/1，共 **37** 个指定用例通过。关闭增量后成功只能支持“增量产物或增量编译问题”的诊断，未证明由先前中断直接造成。

### Task MIG-R01: 恢复 Windows release 的跨磁盘错误分类编译

**Task type:** `implementation`

**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 修复 v0.22.20 release 的 Windows 编译错误，使用标准库 `ErrorKind::CrossesDevices` 识别跨磁盘 rename；不改变 copy/remove/rollback 实现，不把该恢复行为混入 MIG-02。

**Current evidence:** [release run 34805627444](https://github.com/libra-tools/libra/actions/runs/34805627444) 的 Windows Build Binary 在 `src/command/worktree.rs:5256` 报 `E0433 libc`；Cargo.toml 仅在 `cfg(unix)` 声明 libc。Linux amd64/arm64 成功，macOS 被 fail-fast 取消，后续 upload/install/stable manifest jobs skipped；v0.22.20 D 不得记为完成。

**Out of scope:** 不重构跨磁盘搬移、删除或回滚算法；不修改 DB role、配置策略、依赖或 CI workflow；MIG-02 文件不暂存进本卡。

**Acceptance criteria:**

- [x] production error 判定不再引用 Unix-only libc。
- [x] Unix 与 Windows 的标准错误类别映射有具名回归。
- [x] 既有 cross-filesystem move 测试在两个不同 device 的 fixture 上执行，而非同 device 跳过。

**Verification:**

- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test command_test command::worktree_test::test_worktree_move_cross_device_error_is_portable -- --exact --test-threads=1`。
- [x] `source .env.test && TMPDIR=/mnt/gitmono/libra/target/mig-r01-fixtures.DNZWwT CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test command_test command::worktree_test::test_worktree_move_across_filesystems_rolls_back_when_supported -- --exact --test-threads=1`；已创建 task-owned 临时目录；`/mnt/gitmono/libra/target` device=`66308`，`/tmp` device=`57`。可按同一环境合并为 `worktree_test::test_worktree_move_` filter，一并复验 9 项搬移案例；输出须包含两项完整测试名，不接受 0 tests。

**Full-suite trigger:** `T-2 release recovery`；依用户覆盖只跑两项指定测试、fmt、限定 `--lib --test command_test` clippy 与版本守卫。

**Dependencies:** `MIG-01 D failure`。

**Deliverables:** `N/A`。

**Implementation write set:** `src/command/worktree.rs`、`tests/command/worktree_test.rs`；限定 command_test clippy 发现的 MIG-01 遗留 `tests/command/config_test.rs` 三处 `&&Path` needless borrow，仅移除多余 `&`，无行为轴扩张；本计划恢复证据。

**Files likely touched:** 同 Implementation write set。

**Release write set:** ER-08 五处版本面与工具链生成的 `Cargo.lock`；独立 patch +1，v0.22.21。

**Docs and compatibility impact:** `N/A`：仅恢复已声明跨平台表面的编译，命令、参数、输出和搬移语义不变；release notes 明确 Windows 构建修复，EN/zh/网站无需行为文档变更。

**Rollback mode:** `revert`。

**Migration and rollback:** 发布前 revert；发布后仅 forward-fix，不删除或重写 v0.22.20；无 schema 变更。

**Security and privacy:** 仅 fixture，不触及用户 worktree。

**Performance budget:** 一个标准库错误种类比较，无新增 I/O。

**Estimated scope:** `S`。

**Version increment:** `patch`。

**Release boundary:** `independent`。

**C/D coverage from:** `self`，其 D 成功也为 MIG-00/MIG-01 提供 forward-fix 覆盖，原失败 run 保留。

**D post-push:** `D-MIG-STD`，Windows 构建需真实远端证据，不由 Linux 冒充。

**Granularity:** `type=implementation; axis=portable cross-device error classification; recovery=revert before release and forward-fix after release; complete=yes; self-contained=yes; AC=3/8; VER=2/8; landing=1; prod-files=1; scope=S; deps=MIG-01 D failure; writeset=separate from retained MIG-02 files; release=independent; split-from=release recovery; exception=N/A`

**Execution evidence (2026-09-14, pending final gates):** 首次 v0.22.21 grouped move filter 实际执行 9/9，含 portable error guard 与异设备 move，编译 3m25s、测试 3.71s。限定 `cargo clippy --lib --test command_test -- -D warnings` 发现 MIG-01 fixture 的三处 needless borrow，已按机械 lint 修正，重跑待记录。远端新增 merge `db12567e6`（含 `5652e0592 docs(plan): record competitor audit`）仅修改 `plan-long.md`，与本地写集不重叠；已 `git merge --ff-only origin/main` 保留其变更。当前工作树同时包含未提交 MIG-02；本卡精准暂存排除所有 MIG-02 production/test/registry/index 文件，release 不声称包含 role-aware schema 实现。

**Final local evidence (2026-09-14):** post-lint `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo clippy --lib --test command_test -- -D warnings` 通过（1m09s）；随后以上述异设备 TMPDIR 重跑 grouped move filter **9/9**（编译 3m29s、执行 7.43s），两个指定门完整名称均出现且为 `ok`。同一 `command_test-6990d2da4360a586` 已编译执行档再以 `--exact` 同时运行 `command::config_test::{test_config_get_cascaded_global_read_failure_returns_error,test_config_set_read_failure_does_not_silently_skip_existing_state_check,resolve_user_identity_sources_tolerates_corrupt_global_db}`，**3/3** 通过（0.99s）。`cargo test --test compat_version_surface_sync --test compat_all_production_unwrap_guard -- --test-threads=1`（同 source/CARGO_INCREMENTAL/1800s 前缀）**3/3** 通过，Cargo.lock 工具链仅更新自身版本为 0.22.21。`cargo +nightly fmt --all --check`、`bash -n install.sh`、`git diff --check` 通过。合计 **15 个**不同 focused cases；未运行全量或本地 Windows 构建，Windows Build Binary 仍归 D 组。Codex implementation review `VERDICT: PASS`：一行跨平台判断保持其它 rename error 原样传播，3 处 test borrow 仅机械清理，未发现未关闭 P0/P1；计划 R13 独立 `VERDICT: PASS`。C/D 尚待提交、推送与 gh release。

**C / D publication evidence (2026-09-14):** signed+DCO commit `1760860e9b6e4a769a1bc530a92bc89e159ba343` 已推送，`git ls-remote` 证明 main 与 v0.22.21 tag 同指该 SHA；`gh release create v0.22.21 --target <该 SHA> --notes-file ...` 成功，[Release](https://github.com/libra-tools/libra/releases/tag/v0.22.21) notes 概括 Windows portability fix、三处 fixture lint 与实际携带的 competitor roadmap 文档。实际提交只含三个 command/test 文件、六个版本文件与本计划，MIG-02 runtime/test/index/registry 未进入提交。D [release run 34808390077](https://github.com/libra-tools/libra/actions/runs/34808390077) 与 [CodeQL run 34808378678](https://github.com/libra-tools/libra/actions/runs/34808378678) 均仍运行中，尤其 Windows Build Binary 尚无成功证据；依用户指令已恢复 MIG-02，不等待 workflow，MIG-R01/MIG-00/MIG-01 均不宣称 D complete。

**D closure (2026-09-14):** v0.22.21 [Release run](https://github.com/libra-tools/libra/actions/runs/34808390077) 与 main [CodeQL run](https://github.com/libra-tools/libra/actions/runs/34808378678) 均 completed/success。四个 build-and-upload、upload-install-scripts、update-homebrew-tap、verify-homebrew-formula、request-stable-manifest 均 success；Windows job `103864674889` 在 05:41:48 UTC 完成 release build，05:42 上传 `.exe` 与 SHA256；Homebrew job `103871800710` 实际安装 `/opt/homebrew/Cellar/libra/0.22.21`，无实际 warning/skip。stable manifest job `103871771600` 返回 HTTP 200，`control_revision=13`、`version=0.22.21`。公开 artifact HEAD 四项均 HTTP 200：[Linux amd64](https://download.libra.tools/libra/releases/v0.22.21/libra-linux-amd64)、[Linux arm64](https://download.libra.tools/libra/releases/v0.22.21/libra-linux-arm64)、[macOS arm64](https://download.libra.tools/libra/releases/v0.22.21/libra-darwin-arm64)、[Windows amd64](https://download.libra.tools/libra/releases/v0.22.21/libra-windows-amd64.exe)。`git merge-base --is-ancestor` 确认 MIG-00 `4d6feac8b` 与 MIG-01 `7df326fa3` 都在 `1760860e9` release 历史内；据本卡已批准的 C/D carryforward，MIG-00/MIG-01/MIG-R01 收口。未把用户豁免的全量门记为通过。

### Task MIG-02: 引入 role-scoped migration registry 与独立 schema ledger

**Task type:** `migration`

**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 在单一 DB API 中实现 `DatabaseRole`、allowlisted schema manifest 和 role-specific current/latest/upgrade/create runner；Repository 继续走 repository ledger，GlobalConfig/SystemConfig 走 configuration ledger，Derived 不得运行 runtime schema writer。兼容期 generic wrapper 固定委派 Repository，直到 MIG-03 完成所有 config writer route。唯一行为轴是消除 schema writer 的跨角色污染。

**Out of scope:**

- 不把现有 config 调用点接到新 role（由 MIG-03 承接）。
- 不移除已有 global DB 的 legacy generic receipt（由 MIG-06 承接）。
- 不在本卡新增用户可见 doctor 或 repair command（由 MIG-05/MIG-06 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| generic inspect 读取 builtin latest/current 且返回 future/upgrade 状态 | `src/internal/db.rs:465-543` |
| upgrade hard-code 补 repository tables 后跑全部 builtin migrations | `src/internal/db.rs:771-808` |
| builtin registry 与 runner helper 没有 role 参数 | `src/internal/db/migration.rs:1619-1641,1760-1788` |

**Acceptance criteria:**

- [x] 新 role-aware schema open API 接受 `DatabaseRole`。
- [x] 新 role-aware schema inspect API 接受 `DatabaseRole`。
- [x] 新 role-aware schema upgrade API 接受 `DatabaseRole`。
- [x] 新 role-aware schema create API 接受 `DatabaseRole`。
- [x] 唯一 manifest 为每项 migration 声明 role allowlist。
- [x] 唯一 manifest 为每个 bootstrap DDL 声明 role allowlist。
- [x] 唯一 manifest 为每个 idempotent top-up 声明 role allowlist。
- [x] generic compatibility wrapper 固定委派 `Repository` role。

**Verification:**

- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test db_migration_test role_scope::database_role_manifest_is_total_and_disjoint -- --exact --test-threads=1`（new；纯 manifest 断言复用 integration target，避免 monolithic lib-test link）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test db_migration_test role_scope::role_scoped_schema_writer_keeps_ledgers_disjoint -- --exact --test-threads=1`（new；absent/old/current/future 与 Derived zero-write）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test db_migration_test role_scope::global_and_system_bootstrap_create_no_repository_shape -- --exact --test-threads=1`（new）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test db_migration_test role_scope::repository_only_upgrade_preserves_config_receipt_and_values -- --exact --test-threads=1`（new；包括 1,000-row config fixture 的 no-value-read 断言）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test db_migration_test role_scope::role_schema_up_fault_rolls_back_ledger -- --exact --test-threads=1`（new fault injection）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test db_migration_test role_scope::configuration_ledger_has_independent_version_namespace -- --exact --test-threads=1`（new）。

**Full-suite trigger:** `T-1: src/internal/db.rs、schema compatibility 和 migration registry 是跨切面 shared source of truth。` 依用户覆盖不运行全量；上述六门可合并为 `cargo test --test db_migration_test role_scope:: -- --test-threads=1`，并复验该具名 migration target 与相关 compat guards。

**Dependencies:** `MIG-01（隔离 global/system fixture）`

**Database migration contract:** `roles=Repository,GlobalConfig,SystemConfig,Derived`；migration/bootstrap/top-up allowlist 全部来自一个 manifest；Repository ledger 与 configuration ledger 是独立 version namespace，允许相同 numeric migration ID 分别出现；legacy-reader barrier 的 write/read contract 属于 MIG-04，禁止在本卡或 MIG-03 单独发布。

**Deliverables:** `N/A`

**Implementation write set:** `src/internal/db.rs`、`src/internal/db/migration.rs`、`src/internal/db/schema.rs`（role/ledger/manifest 的单一实现）、`sql/migrations/` 或被 manifest 引用的 role-specific DDL、`tests/db_migration_test.rs` 及其 `tests/db_migration/role_scope.rs` 子模块、`tests/INDEX.md` 与相关 serial registry 行。

**Release write set:** `ER-08 版本面五处`、工具链生成的 `Cargo.lock`、release artifact。

**Files likely touched:** 同 Implementation write set；若新建 compat target，必须同卡更新 `Cargo.toml`、`tests/compat/README.md`、`tests/INDEX.md`。

**Docs and compatibility impact:** `N/A`：本卡只建立内部 role-aware schema API，尚未改变用户命令、错误、配置或远端行为；MIG-03/MIG-04/MIG-05/MIG-06 承担相关 EN/zh/网站文档同步。

**Rollback mode:** `forward-only`

**Migration and rollback:** up 创建/初始化 configuration ledger；bootstrap/top-up 也必须按 role manifest 选择。不得在本卡删除 legacy generic receipt 或通过 config callsite 写用户库。若 up 失败，事务回滚且所有 ledger 保持原样；已含 configuration ledger 的库不执行 destructive down，恢复仅依受验证备份或前滚修复。

**Security and privacy:** 角色选择仅来自调用方显式参数；不得通过 user-controllable path 猜 role。diagnostic 只包含角色、版本和 DB path，不含 config 值。

**Performance budget:** 每次 inspect/open 至多对所选 ledger 执行常量次 indexed version 查询；不遍历 migration SQL 或 config rows；测试至少以 1,000 条 config_kv fixture 证明 version inspection 不读取配置内容。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**D post-push:** `D-MIG-STD`。

**Granularity:** `type=migration; axis=role-scoped schema manifest and ledger; recovery=forward-only by retaining legacy receipt and repairing only through a later explicit path; complete=yes; self-contained=yes; AC=8/8; VER=6/8; landing=4; prod-files=4; scope=M; deps=MIG-01; writeset=serialized before MIG-03; release=independent; split-from=N/A; exception=N/A`

**Execution preflight (2026-09-14):** 基线 `7df326fa3` 已在 main 与 v0.22.20；仅本计划的异步发布证据为当前脏状态。用户已覆盖前卡 D 组等待条件。源码刷新：generic inspect/upgrade=`src/internal/db.rs:480-565,729-778`，bootstrap/top-up=`src/internal/db.rs:566-714`，registry/runner=`src/internal/db/migration.rs:1186-1803,2001-2020,2140`。当前 repository latest 已为 `2026090801`，不是计划成稿时的 `2026090601`；实现以当前树为准并保留原 migration 身份。ER-03 复核保持单一 schema writer 隔离行为轴，8 个 AC、6 个具名门、M scope、4 个 production 文件以内。新增 schema 子模块用于集中 role allowlist、ledger 和 bootstrap/top-up 分派；现有 config 调用点留给 MIG-03。仅使用 1800s 有界 focused targets、fmt 与静态检查。

**Execution / final local evidence (2026-09-14):** MIG-R01 C 完成后以 `1760860e9` / v0.22.21 恢复，最终缓存与并发重检实现的六门再次 **6/6**（1.29s）。`schema.rs` 集中四角色、ledger、migration/bootstrap/top-up allowlist，Repository 保留原 receipt；配置使用独立 ledger，单事务包含写锁后 version recheck、DDL 与 receipt。manifest/latest 通过 OnceLock 仅验证一次；1,000-row fixture 的坏视图证明 inspect 不读取配置值；两个独立 pool 的 concurrent upgrade 合计仅写一次 receipt。角色 fixture 还覆盖 Derived/不存在路径 zero-write、future bytes 不变、existing-create 不覆盖、DDL 与 receipt 注入失败回滚。未触碰现有 config 调用点。

版本面已同步 **0.22.22**，Cargo.lock 仅由 Cargo 刷新 package version。post-bump `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo clippy --lib --test db_migration_test -- -D warnings` 通过（52.43s）；同前缀的 `cargo test --test db_migration_test --test compat_global_config_schema_future --test compat_all_production_unwrap_guard --test compat_serial_registry --test compat_version_surface_sync -- --test-threads=1` 全绿：migration **90/90**（含六个具名 role 门）、global-schema **12/12**、unwrap **1/1**、serial **18/18**、version **2/2**，共 **123 个**不同用例。fmt、installer shell syntax、diff whitespace 通过；nextest generator 已运行，生成文件无额外 diff。Codex implementation review `VERDICT: PASS`：角色拒绝先于文件创建，配置事务失败不留下新 ledger，角色版本空间与 Repository wrapper 均有回归，未发现未关闭 P0/P1。计划 R11 `VERDICT: PASS` 已覆盖本卡契约，R12/R13 仅为独立恢复卡增量。未执行全量、旧 binary oracle（属 MIG-04/MIG-06）或本地 release build；后续 multi-platform D 仍须真实取证。本卡此时仅 locally-accepted，待提交/推送/gh release。

**C / D publication evidence (2026-09-14):** signed+DCO commit `ba129f0ea52abbe962df98fe66176aab2b2528ae` 已推送，main 与 v0.22.22 tag 已核对指向同一 SHA；`gh release create` 成功，[v0.22.22](https://github.com/libra-tools/libra/releases/tag/v0.22.22) 使用内容化 notes 说明 role manifest、独立 ledger、事务与 123 个 focused cases，并明确本卡尚未接入 config callers。D [Release 34809210423](https://github.com/libra-tools/libra/actions/runs/34809210423) 与 [CodeQL 34809196524](https://github.com/libra-tools/libra/actions/runs/34809196524) 发布时均 in_progress；依用户覆盖直接进入 MIG-03，不把运行中记成成功。

**D closure (2026-09-14，MIG-04 执行期间补记):** 上述 Release 与 Advanced CodeQL (`codeql.yml`) 均 success；八个 Release jobs 全部成功（四平台、installers、Homebrew 更新及实际安装验证、signed manifest）。Homebrew verify job `103874003422` 在 06:00:17 UTC 实际安装 `/opt/homebrew/Cellar/libra/0.22.22`，无实际 warning/skip；manifest job `103873968733` 返回 HTTP 200、`control_revision=14`、version `0.22.22`、SHA256 `3f4b7d18220ed2056f72e1354126a5774e3795935eedd4cba276e543bf7bd4fa`。四个 v0.22.22 平台 R2 artifact URL 均 HEAD 200。本卡 A/B/C/D 齐备，升级为 complete；不要求后续卡等待其发布。

### Task MIG-03: 将 global/system config writer 路由接入 Config role

**Task type:** `implementation`

**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 令 global/system config 的 create、write connection 与 cascade reader 使用 MIG-02 的 Config role API；尤其覆盖 `libra config --global/--system` 的实际创建/写入入口。唯一行为轴是消除 config writer 对 Repository generic wrapper 的依赖，使 bootstrap、top-up 与 receipt 不再污染 config DB。

**Out of scope:**

- 不修改 global DB 的 legacy receipt 或提供 repair command（由 MIG-06 承接）。
- 不改变 Repository migration 的 schema format（由 MIG-02 承接）。
- 不改变 remote/cloud future-schema 结果或文档（由 MIG-04 承接）。
- 本卡从原路由/策略卡拆出 MIG-04；MIG-04 独立承担公开 policy 与同卡文档。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| global config path 通过 override 或 HOME 回退得到 | `src/internal/config.rs:1250-1262` |
| config command 在 missing scoped DB 调用 generic create | `src/command/config.rs:88-125` |
| config command 写入前调用 generic establish connection | `src/command/config.rs:186-191` |
| config cascade 的 read-only connection 不迁移，但 incompatible query 会降级为 unusable | `src/internal/config.rs:1808-1823` |

**Acceptance criteria:**

- [x] global scoped create 使用 `GlobalConfig` role。
- [x] global scoped write connection 使用 `GlobalConfig` role。
- [x] global cascade reader 使用 `GlobalConfig` role。
- [x] fresh Config-role bootstrap 只写 configuration-owned schema state。
- [x] system scoped create 使用 `SystemConfig` role。
- [x] system scoped write connection 使用 `SystemConfig` role。
- [x] system cascade reader 使用 `SystemConfig` role。
- [x] config callsite source guard 零命中 generic schema wrapper。

**Verification:**

- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test command_test command::config_test::global_config_create_uses_global_role -- --exact --test-threads=1`（new）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test command_test command::config_test::system_config_create_uses_system_role -- --exact --test-threads=1`（new）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test compat_global_config_schema_future config_cascade_uses_explicit_role -- --exact --test-threads=1`（new）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test compat_global_config_schema_future config_role_bootstrap_writes_configuration_state_only -- --exact --test-threads=1`（new）。
- [x] `source .env.test && CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s cargo test --test compat_global_config_schema_future config_callsites_do_not_use_generic_schema_wrapper -- --exact --test-threads=1`（new）。

**Full-suite trigger:** `T-1: src/internal/config.rs、config command writer 和 shared schema helper 变化。` 依用户覆盖仅运行具名 focused targets、限定 `cargo clippy --lib --test command_test --test compat_global_config_schema_future --test db_migration_test -- -D warnings`、fmt 与静态守卫，不运行全量。

**Dependencies:** `MIG-02（role-aware API/manifest/ledger）`

**Deliverables:** `N/A`

**Implementation write set:** `src/command/config.rs`、`src/internal/config.rs`、`src/internal/db/schema.rs`（集中 literal-path 只读连接与既有 legacy fence 的兼容接线）、`src/utils/client_storage.rs`（仅 scoped credential reader；不改 future preflight classifier）、`tests/command/config_test.rs`、`tests/compat/global_config_schema_future_test.rs`、`tests/INDEX.md`、相关 serial registry/generator、内部 role-map 文档。

**Release write set:** `ER-08 版本面五处`、工具链生成的 `Cargo.lock`、release artifact。

**Files likely touched:** 同 Implementation write set。

**Docs and compatibility impact:** EN/zh/网站为 `N/A`：仅落实 MIG-00 内部 role-map 已声明的 configuration-only writer 与非迁移读取契约，不新增命令、参数或输出。严格读取保留 missing/corrupt/future 的既有结果，兼容 legacy config 表；best-effort fresh reader 的既有 policy 保持不变。内部 `database-migration-scope.md` 同卡记录只读 API 与过渡 fence。MIG-04 承担公开 remote/cloud future policy 与相关三处文档同步。

**Rollback mode:** `revert`

**Migration and rollback:** MIG-02 的 manifest/ledger 不在本卡重写；首个 scoped config write 可以按 MIG-02 manifest 创建 configuration ledger，这是配置 schema 的显式前滚，不得创建 Repository shape。legacy-reader barrier 仅由 MIG-04 与其 classifier 同卡写入。路由回退只恢复原代码，不转换既有 receipt。发布后若发现漏路由，一律新 patch 前滚。

**Security and privacy:** 角色来自 scoped caller 而非 user path；任何无法判定 role 的 config writer 不得回退 Repository generic wrapper。

**Performance budget:** global/system writer 每 invocation 仅走一次 role-specific schema path；cascade reader 不在 key lookup 内运行 DDL。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**D post-push:** `D-MIG-STD`。

**Granularity:** `type=implementation; axis=config writer role routing; recovery=revert routing before release or forward-fix routing gaps after release; complete=yes; self-contained=yes; AC=8/8; VER=5/8; landing=4; prod-files=4; scope=M; deps=MIG-02; writeset=serialized before MIG-04; release=independent; split-from=N/A; exception=N/A`

**Execution preflight / routing refinement (2026-09-14):** 基线 `ba129f0ea` / v0.22.22 clean，MIG-02 C 已完成，D 异步运行。以该版本刚编译的 `db_migration_test-1bd14c2a88a0e111` 复验 `role_scope::role_scoped_schema_writer_keeps_ledgers_disjoint` 的 Repository/Global/System absent-old-current-future 与 Derived 矩阵；全部数据库均为 ConfigDbFixture-owned。源码锚点：command `ConfigScope::ensure_config_exists` 与 `ScopedConfig::get_or_create_cached_connection`（含 cache hit）；internal config 的 exact/case-insensitive/prefix/subsection/vault/global/system 读取仍使用 repository path cache；`client_storage::read_config_env_value` 的 global 分支仍使用 migrating generic connection。ER-03 复核仍为同一 config routing 轴，8 AC/5 gates、4 production files、M scope；纯逻辑/矩阵门移入现有 integration target，避免 monolithic lib-test。

**Compatibility-preserving routing contract:** configuration writer 接线使用集中 DB policy helper，保留旧 generic repository ledger 的 future fence（preflight 与 migration write-lock 下都检查），且只执行 Config-role DDL；cached connection 每次取得均验证 schema，不因 cache hit 绕过 future 拒绝。MIG-02 的 role-only API/manifest 语义保持不变，过渡配置 policy 以其 role classifier 组合实现，不复制数值比较；MIG-04 再原子替换 legacy policy 与 barrier。配置只读连接使用 literal SQLite filename、read-only/no-create 与无迁移打开；strict cascade 使用相同 role classifier 与旧 legacy fence，fresh best-effort reader 保留既有错误隔离规则。legacy config-only/absent DB、真正配置 future、旧 generic future、cached future、scope precedence、保值/无 DDL 与 source guard 均归入上述五门及既有 compat target；不读真实用户库。

**R14 review disposition / gate mapping:** 两个 command role 门均在同一进程先取 cache、注入 Config future/legacy Repository future、再取连接并断言拒写与原值不变。source guard 使用语法树只检查 `ensure_config_exists`、`get_or_create_cached_connection`、internal config 的 exact/case-insensitive/prefix/subsection/fresh helper 与 storage `read_config_env_value`；显式 Repository-only 路径及尚属 MIG-04 的 public preflight 不作整文件误报。错误上下文可以新增 role 标签，稳定码/JSON policy 不变；内部 role-map 同步。R14 P2-3 的「helper 已在 HEAD」归因不采纳：`git show ba129f0ea:src/internal/db/schema.rs` 无 `SchemaPolicy`/readonly/configuration policy helper，审查读取期间的新增 helper 属本卡工作树；此轮为计划审查，不作为冻结实现快照的证据。

**Final local evidence (2026-09-14):** 全部五个具名门已在 grouped target 中执行并为 `ok`。post-bump **0.22.23** scoped `cargo clippy --lib --test command_test --test compat_global_config_schema_future --test db_migration_test -- -D warnings` 通过（1m04s）；随后同 `source .env.test` / `CARGO_INCREMENTAL=0` / 1800s 前缀以六个具名 `--test` 执行 `cargo test ... --no-run`（3m23s，无全量），得到 `command_test-de55519b639b33c9`、`compat_global_config_schema_future-40ad9420d8ba930a`、`db_migration_test-c3d772d706a08986`、serial/unwrap/version 三个守卫。用这些刚构建的同版本 libtest 执行档与相同环境/timeout、`--test-threads=1` 实际运行：config module **82/82**（59.17s），global-schema **15/15**（27.96s），migration **90/90**（58.43s），serial **18/18**（49.22s），unwrap **1/1**，version **2/2**；另以 `--exact` 运行 `command::{merge_test::merge_ext_driver_reads_a_global_driver_for_the_configured_default,mergetool_test::mergetool_rejects_deferred_configuration_instead_of_ignoring_it}` **2/2**（13.34s），合计 **210** 个不同用例。第一轮编译发现的 io error 转换与两个测试 import 路径已修正后重跑；无残留失败。fmt、installer shell syntax、diff whitespace 与 nextest regeneration 均通过，Cargo.lock 仅同步 package version。

Codex implementation review `VERDICT: PASS`：显式 scope mapping、只读/no-create/literal path、两种 future cache fence、独立配置 DDL 与 migration under-lock recheck 均已核对；legacy-only/empty/absent/corrupt/true-future、scope precedence、凭证读取、URI metacharacter filename 与 byte-stable reads 有回归。MIG-02 的 role-only matrix 90-target 重跑无回归，remote/cloud 既有 12-case policy 全部保留。本卡没有 legacy receipt repair/barrier，未读真实配置值，未运行全量。R14 独立计划审查 PASS 与四个 P2 处置已记录；此时待 C 提交/推送/gh release，D 不等待。

**C / D publication evidence (2026-09-14):** signed+DCO commit `29597a9323a45913b667f00d6f3e18440ffa1a82` 已推送，`git ls-remote` 确认 main 与 v0.22.23 tag 同指该 SHA；[v0.22.23 Release](https://github.com/libra-tools/libra/releases/tag/v0.22.23) 已由 `gh release create --notes-file` 创建，notes 按实际 role routing/readonly/cache/future fence 与 210 个 focused cases 总结。D [Release 34811586920](https://github.com/libra-tools/libra/actions/runs/34811586920)、[codeql.yml / CodeQL Advanced 34811570526](https://github.com/libra-tools/libra/actions/runs/34811570526) 发布时 in_progress；另有 Code Quality `34811570657`，不与 D 指定的 codeql.yml 混淆。依用户指令不等待，进入 MIG-04。

**D closure (2026-09-14，MIG-04 工作期间补记):** Release 与指定 Advanced CodeQL 均 success。八个 Release jobs 全部成功，包含 Windows job `103873857039` 与其余三平台。Homebrew verify `103881808109` 在 06:39:20 UTC 实际安装 `/opt/homebrew/Cellar/libra/0.22.23`，日志中的 warning/skip 文本仅为脚本定义，没有实际跳过。signed-manifest job `103881754472` 在 06:38:57 UTC 返回 HTTP 200、`control_revision=15`、version `0.22.23`、payload digest `ab4729007b853f440090ec99d5ea9d7d5ff64ef7f369726282f711cc7ff0f0ba`。四个 v0.22.23 平台 R2 artifact URL 均 HEAD 200。本卡 A/B/C/D 全部齐备，标记 complete；未等待发布而中断后续卡。

### Task MIG-04: 收敛 Config future-schema remote/cloud policy

**Task type:** `migration`

**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 让 remote/cloud preflight 只依据 MIG-03 的 `GlobalConfig`/`SystemConfig` compatibility classifier 作出 future-schema 阻断判定，并同步这个公开恢复边界的文档。唯一行为轴是保留真实 Config future 的 fail-closed，同时放行只含 Repository-only receipt 的配置库。

**Out of scope:**

- 不重写 MIG-03 的 scope/path create/open 路由；只为显式 mutation 接入本卡唯一 barrier writer 与原子事务。
- 不创建 doctor 或 repair command（由 MIG-05/MIG-06 承接）。
- 不把 offline/local-only 变成 remote sync 的通用 bypass。
- 本卡自 MIG-03 拆出；使用已发布路由，集中替换过渡 compatibility policy，不复制新的开库分支。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| remote/cloud commands 在 future global config schema 条件下可能返回 `LBR-CONFIG-001` | `src/cli.rs:2230-2245,2293-2323`（v0.22.23 基线） |
| current generic schema inspector 被 client storage 使用 | `src/utils/client_storage.rs:2394-2417` |
| compat target 已覆盖 future、offline、JSON 路径 | `tests/compat/global_config_schema_future_test.rs:23-115,252-319,507-544` |

**Acceptance criteria:**

- [x] Repository-only receipt fixture 允许 remote preflight。
- [x] Config-role future fixture 返回 `LBR-CONFIG-001`。
- [x] no-role-manifest receipt fixture 返回 `LBR-CONFIG-001`。
- [x] configuration-owned legacy-reader barrier fixture 不阻断同 build remote preflight。
- [x] Repository-only receipt fixture 保持 global config values readable。
- [x] barrier write is idempotent within one transaction.
- [x] `src/internal/db.rs::write_configuration_barrier(...)` is the sole barrier writer; no other callsite writes barrier state directly.
- [x] global/system mutation callsites invoke the sole barrier writer.
- [x] barrier is written only by explicit global/system config mutation.
- [x] cascade and remote preflight never write barrier.
- [x] human diagnostic 不输出 config value。
- [x] JSON diagnostic 不输出 config value。
- [x] offline fixture 保持既有允许条件。
- [x] repo-local storage-satisfied fixture 保持既有允许条件。
- [x] ER-06a 文档集记录当前 known-unsupported receipt 的 upgrade-only 边界。

**Verification:**

**门族判据规范（非计数正文，全部命令均必须通过）:**

以下末尾的整体 compat target 重跑只作回归防护，不计入 `VER=12` 分子；其余每条具名命令各计一门。

- [x] `source .env.test && cargo test --test compat_global_config_schema_future repository_only_receipt_does_not_block_remote_commands -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_and_system_future_schema_fail_closed -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_future_diagnostics_are_redacted -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future offline_and_local_storage_semantics_are_preserved -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future unregistered_legacy_receipt_fails_closed -- --exact --test-threads=1`（new；按当前 manifest，不再把已支持的 0801 硬编码为 unsupported）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future configuration_barrier_does_not_block_same_build -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future repository_receipt_keeps_global_config_values_readable -- --exact --test-threads=1`（new）。
- [x] `source .env.test && LIBRA_ENABLE_OLD_READER_ORACLE=1 cargo test --features test-upgrade --test old_reader_oracle_test barrier_config_blocks_02216_without_write -- --ignored --exact --test-threads=1`（new；DEP-MIG-01 L2 oracle gate）。
- [x] `source .env.test && cargo test --test db_migration_test configuration_barrier_is_idempotent_and_atomic -- --exact --test-threads=1`（new fault injection）。
- [x] `source .env.test && cargo test --test db_migration_test configuration_barrier_has_single_writer_callsite -- --exact --test-threads=1`（new；asserts only `write_configuration_barrier` mutates barrier state）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future read_paths_never_write_configuration_barrier -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_policy_docs_are_synchronized -- --exact --test-threads=1`（new；asserts the listed docs exist and contain the policy anchor）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future -- --test-threads=1`。

**Full-suite trigger:** `T-1: src/cli.rs、stable error contract、compat fixture 与 public command docs 改变。` 用户覆盖继续适用：以上命令均使用 `CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s`；不运行全量、all-target/all-feature clippy、网站全量 build/preview。只跑具名 targets、限定 clippy、fmt、网站 docs manifest 与相关 docs unit tests；不把豁免门记录为通过。

**Dependencies:** `MIG-03（config writer role routing）`、`DEP-MIG-01（old reader oracle）`、`DEP-MIG-03（website cf branch）`

**Blocking condition:** `DEP-MIG-01` 不可复验或其 old-reader oracle 不可执行时，本卡 `Lifecycle=blocked`、`Acceptance` 保持空值，登记 `DEFER-MIG-01`，不得发布或宣告 `complete`；`DEP-MIG-03` 无法确认或无法切换 `cf` 分支时，同样保持 `Lifecycle=blocked`、`Acceptance` 非 `complete`，暂停网站文档写入与发布，遵循依赖表停止策略。依赖证据恢复后必须重跑本卡全部门族。

**Database migration contract:** `role=GlobalConfig,SystemConfig`；allowlist=configuration-owned legacy-reader barrier only；ledger=configuration ledger plus recognized barrier state；same build must recognize barrier as non-future; DEP-MIG-01 old reader must reject barrier DB before write; barrier write fault rolls back atomically.

**Deliverables:** `N/A`

**Implementation write set:** 五个 production 文件：`src/cli.rs`（scope-aware preflight/diagnostic）、`src/utils/client_storage.rs`（只读 Global/System classifier 接线）、`src/internal/db/schema.rs`（唯一 manifest 的 barrier 定义及集中配置 classifier）、`src/internal/db.rs::write_configuration_barrier(...)`（sole barrier writer）、`src/command/config.rs`（显式 mutation 的 transaction/barrier 调用）；`src/internal/config.rs` 复用 MIG-03 的 `check_configuration_schema`，无需新增 routing。附属测试/文档：`tests/compat/global_config_schema_future_test.rs`、`tests/command/config_test.rs`、`tests/db_migration_test.rs` 及对应 barrier 子模块、`tests/old_reader_oracle_test.rs`、`Cargo.toml`（`required-features=["test-upgrade"]` target）、`tests/INDEX.md`、`tests/compat/README.md`、必要 serial registry/generator、内部 role-map、`docs/commands/config.md`、`docs/commands/zh-CN/config.md`、`docs/commands/{pull,push,fetch,clone,cloud}.md` 及 zh-CN、`docs/error-codes.md`、`COMPATIBILITY.md`；网站限定 `../libra-backend/apps/tanstack-app/content/docs/commands/{config,pull,push,fetch,clone,cloud}.en.md`（当前网站仅有 EN 内容，不虚构已有 zh 页面）。

**Release write set:** `ER-08 版本面五处`、工具链生成的 `Cargo.lock`、release artifact。

**Files likely touched:** 同 Implementation write set。

**Docs and compatibility impact:** 修改 `config`、`pull`、`push`、`fetch`、`clone`、`cloud` 的 EN/zh 命令文档、错误码与 compatibility matrix；经 DEP-MIG-03 确认后同步网站现有六个 EN 页面。说明 configuration-owned legacy barrier 令旧 binary 写入前拒绝；当前 manifest 已支持 Repository 0801，允许它参与 legacy config 读取不代表任何自动 repair 资格。未注册/当前 build 不支持的 receipt 保持 fail-closed、upgrade-only，无自动 repair；禁止手工 SQLite 和把 offline 当成未确认 remote sync bypass。

**Rollback mode:** `forward-only`

**Migration and rollback:** barrier write is configuration-owned schema migration and must be idempotent/transactional; no down migration. A released barrier is never reverted; repair is forward-only and old binaries must upgrade or fail closed.

**Security and privacy:** unknown schema 默认阻断；human/JSON 输出只含版本、role 和 stable error，不含 config/vault value、token、SQL 或 backup 内容。

**Performance budget:** 每个 remote/cloud preflight 仅一次 Config-role read-only inspect；不枚举 config row。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**D post-push:** `D-MIG-STD`。

**Granularity:** `type=migration; axis=configuration compatibility barrier; recovery=forward-only; complete=yes; self-contained=yes; AC=15/8@EX-MIG-03; VER=12/8@EX-MIG-03; landing=4; prod-files=5; scope=M; deps=MIG-03,DEP-MIG-01,DEP-MIG-03; writeset=serialized before MIG-05; release=independent; split-from=MIG-03; exception=EX-MIG-03`

**Execution preflight (2026-09-14):** 基线 `29597a932` / v0.22.23 clean，C 已完成，D 异步。DEP-MIG-01 binary `/tmp/libra-migration-oracles.YnzG5q/libra-v0.22.16-linux-amd64` SHA256=`d04f6221e4a8aa77f118e287fa2eba7e45a0b3520e2e9f10f2a1a81b7c0a6f55`，与成功 release run `34021189383` 的 digest artifact `9985738483` 一致，source pin=`2ebd409045b5114707986b66a7a6b42b5a4a4b7e`。在 `env -i`、五项隔离配置路径与 `LIBRA_TEST=1` 的 `/tmp/libra-mig04-oracle-probe.EmRtcY` 下 `--version` 返回 `libra 0.22.16`，`init --vault false` 创建 fixture repo 后只读 `MAX(schema_versions.version)` 为 **2026090101**，与 pinned source 一致；不是历史记录中的 0601。未触碰真实用户库。DEP-MIG-03 已 clone 并核对 `/mnt/gitmono/libra-backend`：仅 `.git`、无 `.libra`，clean `cf...origin/cf`，HEAD=`dc03ce1dd80b6187c17decde51b013544961e394`；根及 app AGENTS 已完整读取。六个网站 EN command 页面存在，无 zh 内容文件；后续每次写前重检分支/状态。

**Concrete barrier / classification contract (R15 review input):** 单一 schema manifest 登记 configuration-owned、explicit-mutation-only 的 legacy-reader barrier，使用保留 sentinel **i64::MAX** 与固定名称 `configuration_legacy_reader_barrier`；它不是 Repository migration，也不进入自动 bootstrap/upgrader 列表。`configuration_schema_versions` 继续保留真实 configuration_base receipt；barrier 的兼容状态是 legacy `schema_versions` 中的追加 sentinel，只有配置 ledger 同时含当前 manifest 的 base receipt、且 version/name 都精确匹配时才承认为本 build 支持的 configuration barrier。不得覆盖或删除既有 legacy receipt。unknown/non-manifest/name-mismatched/重复 receipt（含低于 MAX 的未知项）保持 unsupported，不能仅比较最大值后忽略；metadata 查询按 manifest 条目数设有界 LIMIT，不读 config row。MIG-02 role-only API 保持自己的 ledger 语义，生产 configuration policy 集中组合 own-role 与 legacy metadata 分类。

`write_configuration_barrier(&DatabaseTransaction, role)` 为唯一实际 barrier writer；自身在任何 schema read 前取得 SQLite writer lock、锁下重检配置兼容性与 base receipt，再幂等追加 sentinel。scoped set/add/unset/unset-all/import 与 section remove/rename 经统一 mutation transaction 调用；barrier 和该次配置数据写入共享提交/回滚，失败不得单独留下 barrier。constructor/get/list、strict/fresh cascade、remote preflight 不调用该 writer。新旧 binary oracle 必须覆盖 Global/System，旧版 scoped mutation 在 barrier 上拒绝且 bytes 不变；新 build 接受同一 barrier。真正 Config future 与未知 legacy receipt 都返回 `LBR-CONFIG-001`；Global scope 保留 process/repo-local storage-satisfied 与 offline 分支，System future 对需要系统配置默认值的 remote/cloud 命令保持 fail-closed，不能由仅满足 Global storage 的 env 证据豁免。human/JSON 保留已有稳定字段，新增 scope/ledger/reason 时不含未信任 receipt 名称或 config 值。

**R15 disposition:** 独立 `VERDICT: PASS`，无 P0/P1。四个 P2 已关闭：刷新 CLI 基线行号；拆开 sole-writer 与 mutation-callsite 两个独立谓词，AC 如实记为 15；同步 EX-MIG-03 review reference；mutation 入口明确复用 `db::begin_write_transaction`，barrier writer 使用相同 `ACQUIRE_WRITE_LOCK_SQL` 自行保障首语句取锁，不引入新锁协议。网站 `gen-docs-manifest.ts` 的 `CONTENT_LANG=en` 将 EN 内容镜像到 zh-CN URL，六个 EN 页面是实际单一来源。baseline docs generator 248 records，五个适用的 parser/tokenizer unit cases 通过；四个旧双语/固定页面数量假设的无关 cases 未运行，不记为通过。

**Execution audit (2026-09-14):** 初轮 config 82/82、migration 92/92 通过，compat 23/24；旧 MIG-03 fixture 的 strict Global-future 预期须按本卡公开 policy 更新为「跳过该 Global 值并保留合法 System fallback」，remote/cloud 仍由 preflight 阻断，fresh best-effort 的既有行为不变。自查补充：真正 own-role future 优先于其他损坏 metadata，不能被降级为 System 可忽略 I/O；NULL/非 text name 按不支持 receipt 处理；非 table ledger 不冒充 absent。实际 CLI trigger failure 与 section validation failure 证明 marker 随数据回滚。补充 Claude Code implementation review 尝试受 600s timeout 停止（exit 124），未返回 verdict、不计 PASS；已通过的 R15 独立计划审查不由此替代。

**Old-reader / website evidence:** post-bump v0.22.24 `cargo clippy --features test-upgrade --lib --test old_reader_oracle_test -- -D warnings` 通过（53.68s），单一 oracle target 编译 2m38s，显式 opt-in / hash-pinned `barrier_config_blocks_02216_without_write` **1/1** 通过（0.21s）。该门逐一覆盖 Global/System，旧 v0.22.16 set/unset 拒绝且 DB bytes 不变，新 binary get/set 成功。网站最终 `gen:docs` 生成 248 docs + 248 search records，五个相关 unit cases 通过，四个无关旧假设 cases 不运行；另用生成模块验证六页面 × EN/zh-CN 镜像共 **12** 个 locale 页面均含 policy anchors。无网站 full build/typecheck/preview、无真实配置 DB 读写。默认 feature 最终门结果与 C/D 另记，尚未以此预先宣告本卡 complete。

**Final A/B evidence (2026-09-14):** v0.22.24 冻结 production 写集后，最终默认 feature 定向验证通过：`command::config_test::` **82/82**（42.40s），两个 merge/mergetool global-config 回归 **2/2**（2.78s + 2.68s），`db_migration_test` **92/92**（41.11s），`compat_global_config_schema_future` **24/24**（最终 18.85s），serial registry **18/18**（49.45s）、production unwrap **1/1**、version faces **2/2**、error-code docs **2/2**；加上述固定旧 reader oracle **1/1**，合计 **224** 个不同 Rust cases。最后一次 compat 重跑只修正 trigger failure 的错误上下文断言（CLI 输出 `failed to insert config_kv`，不直接暴露底层 trigger 字串），没有改变 production；真实 CLI 故障回滚及 section validation 回滚现均实际通过。全部 12 个具名门已在对应目标中执行。最终同环境 scoped `cargo clippy --lib --test command_test --test compat_global_config_schema_future --test db_migration_test --test compat_error_codes_doc_sync -- -D warnings` **通过**（1m03s），fmt/check、installer bash syntax、diff whitespace、nextest generator 均通过；上游 `proc-macro-error2` 的既有 Cargo future-incompatibility notice 不属于新增 clippy lint。全量及 all-target/all-feature 门按用户覆盖未运行，不记绿。

Codex source/implementation 自审 `VERDICT: PASS`：role-only API 保持独立；单一 manifest 的配置屏障不进入自动迁移；唯一 writer 在读取前取锁、锁下重检、共享 caller transaction；known/lower-unknown/duplicate/name/NULL/fake/unpaired/future 与损坏 metadata 优先级、Global/System 旁路和 human/JSON redaction 均有回归。R15 是独立计划 PASS；600s 未返回 verdict 的补充实现审查仍不计 PASS。网站六页 signed+DCO commit **85c524715cfa571c8bc376e8f138f4c0c30b9127** 已在已核对的 Git `cf` 分支生成，待与主卡一起推送。本卡 A/B 就绪；C 提交/推送/gh release 及 D 异步结果随后记账，未预先宣告 complete。

**C / D publication evidence (2026-09-14):** signed+DCO commit **9ef1712845ba37a348a76a5f71e14aa5a25d1ea0** 已推送，`git ls-remote` 确认 main 与 v0.22.24 tag 同指该 SHA；`gh release create --target <exact-sha> --notes-file` 已创建 [v0.22.24](https://github.com/libra-tools/libra/releases/tag/v0.22.24)，notes 按实际 policy/barrier/transaction/diagnostic/docs 与 224 cases 总结。网站 signed+DCO commit **85c524715cfa571c8bc376e8f138f4c0c30b9127** 已推送，`refs/heads/cf` 精确匹配，工作树 clean。D [Release 34815984720](https://github.com/libra-tools/libra/actions/runs/34815984720) 和 [Advanced CodeQL 34815920980](https://github.com/libra-tools/libra/actions/runs/34815920980) 仍 in_progress；Code Quality `34815920918` 已 success，但不代替 Advanced。依用户指令直接进入 MIG-05，不等待、不把 D pending 记为成功。

**D closure (2026-09-14，MIG-05 工作期间异步补记):** Release `34815984720` 与 Advanced CodeQL `34815920980` 均 completed/success；八个 Release jobs 全部成功。Homebrew verify `103896380730` 在 07:43:19 UTC 实际安装 `/opt/homebrew/Cellar/libra/0.22.24`，脚本定义中的 skip 文本未实际执行。manifest job `103896310588` 在 07:42:48 UTC 返回 HTTP 200，revision `16`、version `0.22.24`、payload digest `c9b4b9d855f86de2de8703cf9f0aeb64d25ff6acd0188d9a658cf1d37f1b7b0d`。四个 [Linux amd64](https://download.libra.tools/libra/releases/v0.22.24/libra-linux-amd64)、[Linux arm64](https://download.libra.tools/libra/releases/v0.22.24/libra-linux-arm64)、[macOS arm64](https://download.libra.tools/libra/releases/v0.22.24/libra-darwin-arm64)、[Windows amd64](https://download.libra.tools/libra/releases/v0.22.24/libra-windows-amd64.exe) artifact 均 HEAD 200。本卡 A/B/C/D 齐备，标记 complete；没有为 D 暂停后卡工作。

### Task MIG-05: 提供只读 global-config schema doctor

**Task type:** `implementation`

**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 实现 `libra config doctor --global-schema` 的 read-only Config-role diagnosis。它输出 receipt、producer disposition 和 file mtime；对 `2026090801` 报告 known convergence，支持状态由当前 role manifest 决定，缺少 attestation 时仍 ineligible。它不读取 config value、不创建 backup、不写数据库。唯一行为轴是 secret-free、zero-write 的诊断表面。

**Out of scope:**

- 不接受或执行 `--repair`（由 MIG-06 承接）。
- 不对 legacy generic receipt、config table 或 vault 值做写入。
- 不改变 MIG-04 已交付的 remote/cloud future-schema policy。
- 本卡自 MIG-04 拆出；不把诊断表面与 policy rewrite 合并。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| config command 已有 scoped subcommands，但没有 global schema doctor | `src/command/config.rs:287-514` |
| historical build maximum was below observed receipt; current Repository manifest contains that ID | 历史 `src/internal/db/migration.rs:1859-1867`; 2026-09-08 receipt=`2026090801`；2026-09-14 基线刷新与 MIG-02 preflight |
| existing compat fixture 可在 tempdir 构造 nonsecret future receipt | `tests/compat/global_config_schema_future_test.rs:23-115` |

**Acceptance criteria:**

- [x] `libra config doctor --global-schema` exists.
- [x] doctor selects the `GlobalConfig` role.
- [x] doctor reports the observed receipt value.
- [x] doctor reports producer disposition.
- [x] doctor reports file mtime.
- [x] doctor leaves the database fingerprint unchanged.
- [x] doctor creates no backup artifact.
- [x] absent target remains absent after doctor.
- [x] future-schema target is diagnosed without schema write.
- [x] doctor documents its known-unsupported/ineligible result.
- [x] human output redacts config values.
- [x] JSON output redacts config values.

**门族判据规范（非计数正文，全部命令均必须通过）:**

- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_command_exists -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_uses_global_role -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_reports_receipt -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_reports_producer_disposition -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_reports_mtime -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_preserves_db_fingerprint -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_creates_no_backup -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_absent_target_stays_absent -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_diagnoses_future_without_schema_write -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_docs_describe_known_unsupported -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_human_redacts_values -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future config_doctor_json_redacts_values -- --exact --test-threads=1`（new）。

**Full-suite trigger:** `T-1: public config command、stable diagnostic contract、compat fixture and command docs change。` 用户覆盖继续适用：只跑本卡具名 targets、scoped clippy、fmt、网站 docs manifest 与相关 docs unit checks；不运行本地全量、all-target/all-feature lint、网站 full build/typecheck/preview。测试均加 `CARGO_INCREMENTAL=0 timeout --signal=INT --kill-after=30s 1800s`；出现超出合理预估的无输出测试要中止并分析，不能记作通过。

**Dependencies:** `MIG-04（Config future-schema policy）`、`DEP-MIG-03（website cf branch）`

**Blocking condition:** `DEP-MIG-03` 无法确认 `../libra-backend` 的唯一 VCS 类型、`cf` 分支或目标网站文件时，本卡 `Lifecycle=blocked`、`Acceptance` 保持空值，暂停网站文档写入与发布并遵循依赖表停止策略，不得宣告 `complete`；确认恢复后必须重跑本卡全部门族。

**Deliverables:** `N/A`

**Implementation write set:** 三个 production 文件：`src/command/config.rs`（Doctor args、解析、只读 predicate 与 dispatch）、`src/command/config/doctor.rs`（新建，纯 metadata 诊断与展示）、`src/cli.rs`（诊断专用 no-repository / ReadOnly / no-auto-upgrade 接线）；复用现有 `db::schema` 只读 opener、role manifest 与 `inspect_configuration_schema`，不复制 future/unknown 判定。附属 `tests/compat/global_config_schema_future_test.rs`、必要的 config parser tests、`docs/commands/config.md`、`docs/commands/zh-CN/config.md`、`docs/development/internal/database-migration-scope.md`、`docs/error-codes.md`、`COMPATIBILITY.md`、受确认的 `../libra-backend` config page 与测试索引。

**Release write set:** `ER-08 版本面五处`、工具链生成的 `Cargo.lock`、release artifact。

**Files likely touched:** 同 Implementation write set；新 test target 若存在则同卡注册 Cargo/test index。

**Docs and compatibility impact:** 新增只读 `libra config doctor --global-schema`；同卡更新 config EN/zh、error code/compatibility 表和确认后的 `cf` 网站页。文档必须说明当前 known-unsupported receipt 不能 repair，禁止手工 SQLite，且不把 offline 描述为 remote-sync bypass。

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`；doctor 不得调用 migration runner、写 receipt、创建 backup 或改 config table。

**Security and privacy:** 输出仅含 classification、receipt、producer disposition、mtime 和受控路径信息；任何无法检查的状态保守标为 ineligible。

**Performance budget:** 仅 role-specific read-only ledger/metadata 查询；不枚举 config rows，不做文件 copy。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**D post-push:** `D-MIG-STD`。

**Granularity:** `type=implementation; axis=read-only global-schema doctor; recovery=revert command/docs to the prior no-doctor interface; complete=yes; self-contained=yes; AC=12/8@EX-MIG-01; VER=12/8@EX-MIG-01; landing=3; prod-files=3; scope=M; deps=MIG-04,DEP-MIG-03; writeset=serialized before MIG-06; release=independent; split-from=MIG-04; exception=EX-MIG-01`

**Execution preflight / R17 review input (2026-09-14):** MIG-04 C 已完成，基线 `9ef1712845ba37a348a76a5f71e14aa5a25d1ea0` / v0.22.24，D 异步。网站 Git `cf` clean、HEAD 与 origin/cf 同为 `85c524715cfa571c8bc376e8f138f4c0c30b9127`，config EN page 已确认；现有 generator 将 EN 镜像至 zh-CN。原四 production 文件是预算，本次具体落点为三个；12 AC / 12 gates 不变。`src/cli.rs` 的全局 schema guard、repo schema/hash-kind preflight、operation classification、startup recovery 及 auto-upgrade hook 均在 handler 前，必须逐层排除纯 schema doctor，否则 handler 自身只读仍会产生旁路写入。只为该精确请求开例外（包括需要被拒绝的 Doctor flag 组合），不改变其他命令或 MIG-04 remote policy。

**Concrete diagnostic contract (R17):** 仅支持 `config doctor --global-schema`；缺 flag、`--local`/`--system`、mutating/typed/positional conflict，以及 `--repair`/`--confirm` 均在读取前以现有 CLI usage code 拒绝。冗余 `--global` 可接受。固定 GlobalConfig scope，路径来源为 `LIBRA_CONFIG_GLOBAL_DB` 或 HOME 默认；不读取 System/Repository DB 或 config/vault value。absent 保持 absent；已有目标经 canonical path/metadata 检查、专用 literal readonly/no-create opener、SQLite 只读 snapshot transaction 取得角色化兼容结果与两 ledger 的 observed/latest maximum，禁止调用 ScopedConfig writer/cache、migration、barrier、backup、checkpoint 或 journal-mode change。再次观察主库及 WAL 元数据，变化时保守报告 `changed_during_inspection`、不作 producer 归因。不可读/损坏目标报告 `unreadable` / ineligible，不伪称健康。

doctor 的 versioned JSON data 包含 action、scope、path source、configured/canonical path、exists、size、UTC mtime、configuration/legacy ledger 的 observed/latest version、compatibility classification、受控 producer disposition、repair eligibility 和行动提示；版本使用字符串保留 i64::MAX 精度。只展示 manifest 中可验证的名称或受控类别，不输出任意 receipt name/SQL/配置值。`2026090801` 的支持与 known-convergence 判断来自当前 manifest 和集中 classifier，不证明历史 writer PID/binary。MIG-05 一律 `repair_eligible=false`，无 repair authority；未来/不支持的 schema 是成功的诊断结果，不等于健康，非法调用仍失败。human 与 JSON 共用一份 metadata report。

只读保证针对持久化 schema/config 状态、稳定目标的主 DB/WAL 内容及 modification time、无 backup/application artifact；不声称操作系统 access time 或 SQLite `-shm` 协调状态恒定。使用标准 SQLite read-only snapshot，不通过 `immutable` 跳过 live DB 的锁/变化检测：[SQLite URI contract](https://www.sqlite.org/uri.html)、[WAL read-only semantics](https://www.sqlite.org/wal.html)。先验证目标是普通文件并仅读取固定 100-byte SQLite header；empty/short/bad-magic 均在 SQLite open 前报告 `unreadable`。WAL-mode 若缺 WAL/SHM，或 sidecar 为非普通文件／symlink，必须在 SQLite open 前报告 `unreadable` / ineligible，不创建 sidecar。两 sidecar 均为现存普通文件时才打开正常只读 snapshot，前后检查主 DB/WAL identity、size、mtime。该 fence 并非 filesystem lock：不能保证排除外部删除／rotation 竞态下 SQLite 协调文件的创建；文档必须如实说明，不作 repair 资格或全文件系统零变化承诺。

测试需覆盖旧/当前/future/unknown/absent/corrupt、WAL fixture 的主 DB/WAL bytes+mtime、仓库/系统 canary、无备份、无值查询的 schema-only fixture、非法组合零写入，以及 default CLI 的 operation/upgrade/preflight 排除。`config_doctor_preserves_db_fingerprint` 同时覆盖现存 sidecar 的 live WAL 和 clean-close 缺 sidecar 时的 refusal/no-create；`config_doctor_creates_no_backup` 检查文件 inventory；`config_doctor_docs_describe_known_unsupported` 固定 missing-sidecar/race 文档边界。receipt/producer/mtime 使用 rollback-journal 或现存 sidecar 的可打开 fixture。保持原 12 AC/12 gates，不新增第 13 门。此处不引入数据 repair，独立计划审查通过且 P1 关闭后才写 production。

**R17 review disposition:** 原 300s read-only source investigation 超时（exit 124），不计 PASS；保留上下文后以禁用 tools 的 continuation 返回字面 `VERDICT: PASS`，但明确列出三项 P1 条件：把 header/sidecar refinement 写入计划、纳入原门族、确保可打开 fixture 与清楚 docs。上述文本与 preliminary tests/docs 已逐项落实；另明确 empty/short/bad-magic 为 unreadable。source opener 已复核没有 journal-mode pragma。外部 CLI 使用已配置 `glm-5-3-flash`，不宣称 Anthropic 模型。R18 只审三项闭包，未通过前不写 production。

**R18 closure:** continuation 返回字面 `VERDICT: PASS`，三项 R17 P1 全部关闭，剩余 P0/P1 为零；先前已核实的源码支持计划。runtime fingerprint、future flags 与外部 rotation 限制作为实现检查保留，不冒充实现已通过。收到此 verdict 后才开始三个 production 文件的实现。

**Implementation review / initial verification:** R19 bounded Claude Code source review 返回 `VERDICT: PASS`（既有模型 `glm-5-3-flash`）；指出异常 WAL/SHM 的 fixture 缺口与 changed report 残留旧 issue。补充已纳入原门族：missing SHM、directory/symlink WAL/SHM refusal；changed 清除旧 issue；header 仅接受 1/1 或现存 sidecar 的 2/2（复核发现 R19 对原 OR 条件的文字描述不精确，现已明确），unit 同时覆盖 malformed/mixed header；epoch unit 使用跨平台可表示的整秒。三 production 文件写集未扩大，无新 gate。初轮 v0.22.25：command::config_test 82/82（63.00s），compat 36/36（27.36s，包含 doctor 12/12），serial 18、unwrap 1、version 2、error-doc 2、config unit 7 通过。最后补充后的 frozen-source 重编译/回归/scoped clippy 正在执行，未提前记为通过。

网站 docs manifest/search 生成各 248 records，相关 unit 5 pass / 4 未运行（旧固定页面数量/双语来源假设），EN 与 zh-CN 生成路由均验证 doctor/repair_eligible/WAL/SHM/changed 标记。网站 signed+DCO commit `8c71df8359916b0ffeaa951f66916ff640175c73` 已创建，等待与主卡一起推送；无网站全量 build/typecheck/preview。

**Final A/B evidence (v0.22.25, 2026-09-14):** frozen-source 的具名 `--no-run` 编译成功（4m13s），随后运行其产出的明确 test binaries，全部使用 1800s timeout 和 `--test-threads=1`。149 个 distinct Rust cases 通过：config command 82（36.47s）、configuration compat 36（25.67s，含 12 个 doctor gates 和最后补充的 sidecar/flags）、config unit 8（五 parser + 三 doctor）、serial registry 18（49.41s）、production unwrap 1、error docs 2、version 2；doctor 12 个具名 gate 另以同一已编译 target 的 `--exact` 逐个复验。最终 scoped clippy 覆盖 `--lib` 与上述六 integration targets，`-D warnings` 通过（5m16s 含等待 compile lock；实际检查约 1m03s）。fmt、install.sh syntax、nextest regeneration、diff-check 均通过。既有 `proc-macro-error2` dependency future-incompat notice 不属于新增 clippy warning。未运行本地全量、all-feature/all-target lint 或网站全量构建，未出现超时测试。Codex final-source 自审 PASS；R19 独立 PASS 与随后两条建议的明确关闭均如上记录。C/D 待实际发布结果，不提前记 complete。

**C / D evidence:** MIG-05 signed+DCO implementation commit `614e5084ef25b122ab5129e745344f5a44b8f255`；首次 push 因并行远端 docs 更新被拒绝。已检查上游 `2306b9c3e` / merge `c5a005ef5` 只修改 `docs/development/plan/plan-long.md`（94 行竞品比较文档），无 runtime 重叠，以 signed+DCO merge `592277092ea08bc76641450d80ef5e2315f5b707` 保留双方历史，未 force。merge 相对已测 implementation 只有该文档差异。main 已推送，`git ls-remote` 证明 main 与 v0.22.25 同指 `592277092`；`gh release create --target <exact-sha> --notes-file /tmp/libra-mig05-release.IYuOTT/release-notes.md` 创建 [v0.22.25](https://github.com/libra-tools/libra/releases/tag/v0.22.25)，notes 总结实际 doctor、安全边界、验证及并行文档更新。网站 `8c71df8359916b0ffeaa951f66916ff640175c73` 已推送并精确确认 cf，工作树 clean。D [Release 34820389933](https://github.com/libra-tools/libra/actions/runs/34820389933) 与 [Advanced CodeQL 34820370020](https://github.com/libra-tools/libra/actions/runs/34820370020) in_progress，不能以 Code Quality `34820369758` 代替；按用户指令不等待，继续 MIG-06。

**D closure（MIG-06 工作期间异步记录）:** Release `34820389933` 与 Advanced CodeQL `34820370020` 均 completed/success，八个 Release jobs 全成功。Linux arm64 `103900419808`、Linux amd64 `103900419939`、Windows `103900420022`、macOS `103900420047`、Homebrew update `103912048728`、installers `103912049484`、manifest `103912049555`、Homebrew verify `103912100177`。manifest 于 08:42:41 UTC 返回 HTTP 200：control revision `17`、version `0.22.25`、payload digest `f45ec769a26431504c5ad7f21e27ccbf196049d7ba63483b930b63e776cc6a25`。Homebrew 于 08:42:55 UTC 实际安装 `/opt/homebrew/Cellar/libra/0.22.25`（4 files / 96 MB）；skip 仅出现在脚本定义，未实际执行。四个 v0.22.25 R2 artifacts（Linux amd64/arm64、darwin arm64、Windows amd64）HEAD 均 200。本卡 A/B/C/D 齐备，标记 complete；没有等待 D 才推进 MIG-06。

### Task MIG-06: 提供 attested global-config legacy receipt repair

**Task type:** `migration`

**Lifecycle / Acceptance:** `done` / `complete`（本卡A/B/C/D与CI100MiB性能门均已取得；MIG-R02完整10044-case全绿补齐计划级复验，后续用户追加配置卡不改写本卡结果）

**Description:** 在 MIG-05 的只读诊断基础上，仅对 DEP-MIG-02 已 attested 的 legacy receipt 执行 `libra config doctor --global-schema --repair --confirm <canonical-config-db-path>`。当前 `2026090801` 在 attestation 缺失时必须保持 ineligible；本卡不得把它当作可 repair 的示例。唯一行为轴是 confirmation-protected、SQLite-consistent、atomic 的 receipt conversion。

**Out of scope:**

- 不修改 MIG-05 的 read-only classification 或默认 doctor 行为。
- 不对 unknown receipt、unattested receipt、未知表形状、缺失 oracle 或未确认 target 写入；由 `DEFER-MIG-01` 承接。
- 不改变 global config 的业务键、加密形式、vault 值或 Repository DB receipt。
- 本卡自 MIG-05 拆出；不把读诊断与 mutating recovery 混在一个 release。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| generic migration receipt lives in `schema_versions` | `src/internal/db/migration.rs:1619-1641,1760-1788` |
| config command is the scoped command extension point | `src/command/config.rs:287-514` |
| source membership is now known, but producer binary attestation remains a separate repair prerequisite | 2026-09-14 基线刷新；DEP-MIG-02 必须重新 pin source/binary hash 与 fingerprint |
| SQLite WAL mode is not current storage behavior | `src/internal/db.rs:146-148` |

**Acceptance criteria:**

- [x] repair requires `--repair`.
- [x] repair requires canonical `--confirm` path.
- [x] repair holds the target lock.
- [x] repair rechecks DEP-MIG-02 attestation under that lock.
- [x] repair rechecks manifest receipt membership under that lock.
- [x] repair rechecks config fingerprint under that lock.
- [x] ineligible fixture leaves main DB unchanged.
- [x] repair uses a SQLite-consistent backup mechanism.
- [x] backup reopens as SQLite.
- [x] backup row set equals source row set.
- [x] repair transaction commits atomically.
- [x] repair writes only configuration-owned metadata: initialize the configuration ledger/base and append the existing configuration-owned legacy-reader barrier; preserve all original legacy receipts and business rows.
- [x] DEP-MIG-01 old reader rejects post-repair fixture without writes.
- [x] new Config-role reader accepts post-repair fixture.
- [x] repair failure emits stable secret-free diagnostic.
- [x] repair/recovery docs state the attestation boundary.

**门族判据规范（非计数正文，全部命令均必须通过）:**

- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_requires_repair_flag -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_requires_canonical_confirm -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_holds_target_lock -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --features test-upgrade --test compat_global_config_schema_future global_schema_repair_rechecks_attestation_under_lock -- --exact --test-threads=1`（new；DEP-MIG-02；R20/R21 approved fault hook）。
- [x] `source .env.test && cargo test --features test-upgrade --test compat_global_config_schema_future global_schema_repair_rechecks_receipt_manifest_under_lock -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --features test-upgrade --test compat_global_config_schema_future global_schema_repair_rechecks_fingerprint_under_lock -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_ineligible_keeps_main_db -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_uses_sqlite_consistent_backup -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_backup_reopens -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_backup_row_set_matches_source -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --features test-upgrade --test db_migration_test global_schema_repair_transaction_is_atomic -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test db_migration_test global_schema_repair_writes_configuration_ledger_only -- --exact --test-threads=1`（new）。
- [x] `source .env.test && LIBRA_ENABLE_OLD_READER_ORACLE=1 cargo test --features test-upgrade --test old_reader_oracle_test repaired_config_role_blocks_02216_without_write -- --ignored --exact --test-threads=1`（new；L2 oracle gate；DEP-MIG-01 supplies the two required environment values）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_new_reader_accepts_fixture -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_failure_is_secret_free -- --exact --test-threads=1`（new）。
- [x] `source .env.test && cargo test --test compat_global_config_schema_future global_schema_repair_docs_describe_attestation_boundary -- --exact --test-threads=1`（new）。

**Full-suite trigger:** `T-1: runtime schema mutation、public config command、stable diagnostic contract、shared compatibility fixture and recovery docs change。`

**Dependencies:** `MIG-05（read-only diagnosis）`、`DEP-MIG-01（old reader oracle）`、`DEP-MIG-02（receipt producer attestation）`、`DEP-MIG-03（website cf branch）`

**Blocking condition:** `DEP-MIG-01` 不可复验或 `DEP-MIG-02` attestation/fingerprint 不完整时，本卡 `Lifecycle=blocked`、`Acceptance` 保持空值，登记 `DEFER-MIG-01`，不得发布、repair 或宣告 `complete`；`DEP-MIG-03` 无法确认时，遵循依赖表的停止策略，暂停受影响文档/发布写入并保持 `Acceptance` 非 `complete`。依赖恢复后必须重跑本卡全部门族。

**Database migration contract:** `role=GlobalConfig`；allowlist 仅为 configuration ledger initialization 与 DEP-MIG-02 attested receipt conversion；ledger=`configuration`；old reader must fail closed before write because of the configuration-owned legacy barrier; new reader must accept; `2026090801` is known but excluded until DEP-MIG-02 supplies complete attestation.

**Deliverables:** `N/A`

**Implementation write set:** 六个 production 文件：`src/command/config.rs`、`src/command/config/doctor.rs`（提示及安全文件检查复用）、`src/command/config/repair.rs`（new）、`src/cli.rs`、`src/internal/db.rs`、`src/internal/db/schema.rs`；以及 `tests/compat/global_config_schema_future_test.rs`、`tests/db_migration_test.rs` / `tests/db_migration/`、`tests/old_reader_oracle_test.rs`、隔离 attestation fixture/元数据、`docs/commands/config.md`、`docs/commands/zh-CN/config.md`、`docs/error-codes.md`、`COMPATIBILITY.md`、测试索引和确认后的 `../libra-backend` 网站文档。不运行 Repository migration 或新增业务 SQL。

**Release write set:** `ER-08 版本面五处`、工具链生成的 `Cargo.lock`、release artifact。

**Files likely touched:** 同 Implementation write set。

**Docs and compatibility impact:** 扩展 doctor workflow 为 attestation-gated `--repair --confirm <path>`；同卡更新 config EN/zh、error/compat、网站 recovery 内容。文档必须说明 SQLite-consistent backup、explicit disaster restore、unattested producer 不 repair、producer-matched upgrade；禁止手工 SQLite 与未确认 offline remote sync。

**Rollback mode:** `forward-only`

**Migration and rollback:** backup successful and re-opened before mutation; transaction changes only attested configuration ledger state; no down migration. Disaster recovery restores only a verified SQLite-consistent backup after preserving the repaired DB for forensics. Unknown receipt never migrates.

**Security and privacy:** no application-level config/vault value enumeration or logging; SQLite alone copies logical pages for the backup. Exact canonical confirmation, filesystem identity fences, private paths, advisory repair serialization and SQLite writer locking are separate checks, not a claim that path strings alone prevent replacement. Observed busy/locking/symlink/identity/attestation changes fail closed; eligibility is rechecked under lock. See the execution refinement below for the explicit supported boundary.

**Performance budget:** inspect only indexed receipt and fixed schema metadata; backup is O(DB bytes) via SQLite-consistent mechanism; a 100 MiB fixture must complete backup plus re-open validation within 10 seconds on the documented CI runner and prove no write transaction is held during copy.

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**D post-push:** `D-MIG-STD`。

**Granularity:** `type=migration; axis=attested legacy global-config receipt repair; recovery=forward-only with verified SQLite-consistent backup restoration; complete=yes; self-contained=yes; AC=16/8@EX-MIG-02; VER=16/8@EX-MIG-02; landing=4; prod-files=6; scope=M; deps=MIG-05,DEP-MIG-01,DEP-MIG-02,DEP-MIG-03; writeset=final serial card; release=independent; split-from=MIG-05; exception=EX-MIG-02`

**Execution refinement / R20 review input (2026-09-14，生产实现前):**

1. **可复验的格式 attestation，而非历史 writer 归因。** Linux amd64 v0.22.19 release `34707811885` / artifact `10302058644` 对应 source `b94bfe12f2ec2f039b88ddb5c6f8871787d60f17`，binary SHA-256 `03447eb983178433425b5afffba351edae4044e7ded5b9e35c956dddb2bb68a6`，size `116642608`。在 `/tmp/libra-mig06-attestation.ptgT14` 用 `env -i`、隔离 HOME/global/system 路由运行该 hash-verified binary 的 `config set --global test.attestation fixture`，得到 60 receipts、max `2026090801`、217 个非 SQLite 内部 schema objects（81 tables / 115 indexes / 21 triggers）。按 `(type,name)` 排序的 `json_group_array(json_array(type,name,tbl_name,sql))` UTF-8 无换行 SHA-256 为 `d8ce4bf5399389e30dffd0325f6754c3a2052880932b67286577d8b6e0e21f05`；按 `(version,name)` 排序的 receipt JSON SHA-256 为 `d0078ce68596fe6c4e9f1d5c8e9db36181ca1fceac0c1cca3776bdd5e007a443`。实现必须核验等价规范化字节，不能只检查 MAX。该证据只注册精确 producer-format cohort，不证明任意用户 DB 曾由此 binary/PID 写入；默认 doctor 的 unattested/repair_eligible=false 契约不变。
2. **GlobalConfig 与 Repository 的区别不能靠 schema hash。** 隔离 current `libra init --vault false` 产生的 Repository DB 具有同一 schema hash，但 `reference` 有 3 行。因此除 manifest + 完整 schema/receipt 指纹外，还须排除 Repository storage 位置，并对所有非配置业务表作有界 `EXISTS` 检查，必须为空。只允许 config/config_kv 自身数据，以及 producer SQL 明确创建的 bootstrap metadata：`metadata_kv` 中唯一 `repository / empty target / stash.reflog.generation / text / 1` seed、`worktree_registry_capability` 的 2/3 两个版本。只返回布尔资格，不返回未知行值。任何其他数据、trigger/schema/receipt 差异、缺失/多余对象或不完整 config ledger 都拒绝；不把原本的 Repository DB 转为 GlobalConfig。已经具备合法 configuration base + barrier 的目标仅报告无需修复，不再次备份/写入。
3. **确认与路径范围。** 仅 `doctor --global-schema --repair --confirm <exact canonical path>` 可请求写入；`--confirm` 单独使用、路径非绝对/非规范/不匹配、与 get/set/list 等组合皆在写入前拒绝。所有 doctor 请求继续跳过 Repo preflight、startup recovery、全局/system schema guard 和 auto-upgrade；repair 与纯 doctor 的 mutation classification 分开，Config 自有 operation boundary 不创建 Repository operation。首个保守实现的 mutating repair 限 Unix 普通本地文件：owner 为当前 uid、single link、目标和直接父目录不可由 group/other 写入；拒绝符号链接和不安全祖先（root/current-owner 的不可他写祖先及安全 sticky temp root 除外）。非 Unix 或无法证明此权限边界的文件在任何 repair 写入前 fail closed，doctor/配置读写功能不受影响；EN/zh/网站必须明确此限制，不宣称所有 release platforms 都支持 mutating repair。
4. **锁不等于备份快照。** 初次只读资格核验后，在同一可信父目录建立 0600、no-follow、single-link、owner-checked 的固定 repair lock file，用 `try_lock` 非阻塞持有到结束，不 unlink 锁文件。重新核对 canonical target、文件/父目录 identity 和资格，然后通过不创建文件、不运行 migration 的专用连接打开；显式单连接池、无 idle/max-lifetime reap、不与其它调用共享，所有 SQL 错误立即终止，禁止重新连接后继续比较 `PRAGMA data_version`。SQLite 原生并发锁负责非协作 writer；独立 lock 只串行化 repair。文件/目录 identity 在备份前、SQLite 写锁后及 commit 前再次核验；不承诺抵抗同 uid/root 恶意进程绕过权限/替换路径，要求停止外部文件替换工具。实际观察到的替换必须 rollback/refuse。
5. **先一致性备份，再短事务。** 在同一专用物理连接读取 `PRAGMA data_version`，无 write transaction 地执行绑定绝对目标路径的 `VACUUM INTO ?`。备份放在目标同目录新建的 0700 随机 recovery directory，0600 create-new 文件，禁止覆盖/链接；使用 FULL durability、fsync 文件与父目录，重新 readonly 打开并检查 integrity 结果（只接受 ok，不输出任意 SQLite 错误文本）及同一 attested schema。复制失败不得开始 repair；已经创建的备份保留且在结果中清楚标记 verified/unverified，不自动删除证据。之后 `begin_write_transaction` 取得 SQLite 写锁，再比较同一 connection 的 data_version，重验当前 manifest、attestation、schema/receipt/角色资格、路径 identity；有其它 commit 即停止，不把旧快照当作当前数据备份。只有通过后才在此事务初始化 configuration ledger/base，再调用现有唯一 `write_configuration_barrier`，commit。保留原 60 receipts、所有业务行和 encryption flags，不运行 Repository migration、不改 journal mode、不 checkpoint、不使用 immutable。备份只由 SQLite 复制逻辑内容；Rust 不枚举配置值。rollback 保持逻辑行集和 receipts 不变；不得把 SQLite 协调文件/访问时间的不变误写成跨进程文件系统原子性保证。
6. **可执行故障门与性能。** 16 个门名与 AC 保留；`global_schema_repair_writes_configuration_ledger_only` 的 ledger-only 指 configuration-owned 元数据，包括物理位于 legacy ledger 的 MIG-04 barrier。锁下 recheck/backup-before-write/transaction-failure 门可用 `test-upgrade` + `LIBRA_TEST=1` 双门控的限时 checkpoint/fault hooks，release 构建不得包含这些 hooks；相关具名命令在执行时加 `--features test-upgrade`，default suite 不运行需要 hook 的 cases。通过独立进程在 checkpoint 修改 receipt/schema/普通 KV/路径、占用 repair lock、注入备份和事务中断，验证拒绝/rollback/保留备份/secret-free。无 hook 的确认、资格、备份、new-reader 和 docs gates 仍默认运行。fixture 含 secret canary、加密标记、legacy config 和 sqlite_sequence；测试允许读取其自建 synthetic 行并精确比较 backup 与 pre-repair row sets。100 MiB fixture 的 backup+reopen 实测记录平台/磁盘和耗时；CI 预算 10s，另证复制阶段没有写事务。旧 v0.22.16 oracle hash 保持 `d04f6221e4a8aa77f118e287fa2eba7e45a0b3520e2e9f10f2a1a81b7c0a6f55`，必须实际执行而非静态替代。

7. **R20 后的安全细化（R21 复审输入）。** Item 3 的拒绝时机明确为「在任何 repair side effect 之前，包括 lock file、recovery directory 和 backup 创建之前」；不是仅在主库事务之前。除非 SQLite 自身协调并发外，不允许先操作不安全路径再检查权限。补充注册包含 SQLite 内部对象的完整 schema 指纹：293 objects（82 tables / 190 indexes / 21 triggers），相同规范化 JSON SHA-256 `aaf969a014d1690f58cc76d2df0d71a55cb871cf6f70bfcc9bd1c6ec3f48714e`，producer fixture 与 SQLite-consistent probe backup 完全一致；实现按全部对象有界匹配，不能用含 wildcard `_` 的 LIKE 前缀排除来漏掉用户对象，也不能忽略被修改的 sqlite_sequence 等内部结构。单连接池之外，添加仅在该物理连接内存在的 TEMP nonce guard，比较 data_version 时同时校验 nonce；池发生透明替换即因 TEMP guard 缺失/不匹配拒绝，不依赖「max_connections=1 应当没重连」的假设。该 TEMP 表不改变 main DB。需要证明「复制中无写事务」的 test-upgrade gate 可用 SQLx 的安全 `lock_handle().set_progress_handler(...)` 在 SQLite VM 复制期间设置限时 checkpoint，另一个连接必须能 BEGIN IMMEDIATE/ROLLBACK；callback 不操作其 SQLite connection，超时返回 false 中断，不向 release 编译。R21 仅复审这些更强约束与 R20 P2 closure，不增加行为轴或 AC 数量。

**R20 evidence:** Claude Code 在 8 次 Read/Grep 后返回字面 `VERDICT: PASS`、P0 none / P1 none；唯一 P2 是平台/权限拒绝必须先于任何 backup side effect，已由 item 7 明确关闭。其结果误称 `--global-schema` 尚不存在：该 flag 已在 MIG-05 的 `src/command/config.rs` 定义，由 `src/cli.rs` 转派；尚未新增的是 `--repair/--confirm`，这里纠正，不把错误描述作为证据。实际 reviewer 模型为既有配置 `glm-5-3-flash`，非 Anthropic 模型声明。Codex 自审认可格式 attestation 不等于历史 writer 归因、保守 Unix 支持、无配置值枚举和 sole barrier writer；追加 TEMP physical-connection proof 与完整 schema-object 指纹后，R21 待复审，生产实现仍未开始。

**Pre-implementation SQLite protocol probes（非 Rust gate）:** 在隔离 `protocol-probe.db` 上，同一连接的 data_version 在 TEMP nonce + VACUUM INTO 后保持 1，独立连接更新 synthetic config 后，主连接取得 no-op writer lock 时变为 2，TEMP nonce 仍存在，随后 rollback。另在 `/mnt/gitmono/libra/target/mig06-backup-probe.6YPKxU` 的 NVMe 文件系统，Linux `7.2.3-arch1-3 x86_64` / SQLite CLI 3.53.4 对 25,931 × 4096 bytes（101.29 MiB）fixture 执行 FULL VACUUM INTO + readonly quick_check/reopen，0.144s、ok、101 synthetic config rows。`/usr/bin/time` 不存在的首次计时命令退出 127，没有运行备份；改用 Bash time 的上述成功结果。该证据不替代 bundled SQLite/SQLx 的正式 Rust tests、CI 预算或旧 binary oracle。

**R21 closure:** Claude Code 单次 Read 后返回 `VERDICT: PASS`，P0 none / P1 none，确认 R20 P2 已关闭、完整 schema 指纹、TEMP nonce 的物理连接证明与受限 progress-handler 故障门一致；同时明确纠正 R20 的 flag 定义位置错误。Codex 自审 PASS；两轮只批准计划，不认证尚未编写的实现。现在开始 MIG-06 production/test 写集；用户禁止本地全量构建/测试的覆盖不变。MIG-05 Advanced CodeQL `34820370020` 已 completed/success，Release `34820389933` 仍在运行，继续异步记录。

**Implementation review / failure closure（2026-09-14）:** 首轮 feature-gated repair filter 实际 15 pass / 1 fail：`global_schema_repair_backup_reopens` 发现 tempfile 默认目录权限不是私有 0700；已显式使用 Builder permissions(0700)，并补充开始源库事务前 fsync recovery directory 在其父目录中的新目录项。另补 FIFO 非阻塞拒绝、保留 unverified 中断备份、不复用旧备份、WAL committed rows 与 unsupported-platform zero-side-effect gates。R22 独立实现审查返回 `VERDICT: FAIL`，唯一 P1 为 schema/receipt 文本截断可能造成指纹盲区；producer 实测最大 SQL 2153 bytes，因此 4096 长尾假设本身不命中当前 cohort，但 SQLite text substr 的 NUL 截断是实际盲区。现改用 BLOB 按 byte 读取上限 + 1，超长直接拒绝，并保留 NUL 后缀参与完整 digest，不过滤任何 schema/receipt 行；新回归覆盖两类元数据的超长/NUL、main bytes 不变及无 lock/backup side effects。R23 两次 Read 后返回 `VERDICT: PASS`，P0/P1 none，确认修正不需重 pin 且关闭 R22；实际 reviewer 均为既有 `glm-5-3-flash`。这些 verdict 是 source review，不替代待完成的 .26 post-bump tests。

**Build incident（2026-09-14）:** .26 多个具名 target 的并行链接中，clang/mold 被 signal killed，Cargo 退出 101，尚未开始该轮测试。kernel journal、systemd-oomd/earlyoom 与当前 cgroup hierarchy 的 memory.events 均无 OOM 证据，不能宣称已查明为 OOM。未删除缓存或终止其它仓库的进程；保留 1800s 上限、`CARGO_INCREMENTAL=0`，以 `CARGO_BUILD_JOBS=1` 重跑同一具名写集。健康 CPU-active 编译不当作挂起测试。

该单工作重跑在 16m09s 已完成 production lib 与 `command_test-34f58f9ba82ecf43`，剩余 lib-test 的单核 codegen 刚运行 1m22s。为避免 jobserver 限制继续把大型编译串成单核长等待，核对 Cargo PID 的 cwd 后主动 SIGINT 仅本任务 Cargo（exit 130，非测试失败/超时）；已完成产物保留，未删缓存。后续改为逐一具名 target，并给单目标 `CARGO_BUILD_JOBS=4`，避免原先多目标同时链接的 fan-out，同时让 rustc 的后端工作并行。

**ER-06a closure input:** 已有 `docs/development/commands/config.md`，因此本卡同步其中的角色/doctor/repair 设计而非记 N/A；依 `_general.md` 同步 integration-test-plan coverage matrix 与 scenario YAML 的 Cargo-only 注记，不新增 runner scenario 或扩大 production 轴。`compat_matrix_alignment` 纳入限定静态门。网站 `cf` 的修复说明已通过 gen:docs（248 docs / 248 search）、五个选定 Vitest cases 与 en/zh-CN generated content/TOC 检查；未运行全站 build/typecheck/preview。签名+DCO commit `0f87fccddd89a4f9f99c6962e717f5addd6fcd4f` 已推送 cf。首次检查错误地要求 generator 不生成的 HTML anchor id，后改为实际 heading/TOC/content 契约后通过；不宣称不存在的 anchor 已获验证。

**R24 source/docs closure:** 六次 Read/Grep 后独立审查返回 `VERDICT: PASS`，P0/P1 none；确认新增 after_backup/after_ledger 路径替换测试的 replacement-byte / original-rowset / verified-backup 断言以及 ER-06a Cargo-only 文档同步在既有范围内。其摘要把 after_ledger 称为 ledger+barrier 阶段不精确：该 checkpoint 位于 barrier 调用之前；after_barrier 的独立原子事务门另行覆盖。integration matrix 行在 reviewer 的有界输出被省略，因此用实际注册 target/文档链接检查与待执行 `compat_matrix_alignment` 补证，不把未读内容写成已审。Codex 当前 source 自审 PASS；两者都不认证尚未结束的 .26 测试、Clippy 或 D 组。

**R25 portability closure:** Codex 对本地 libc 0.2.189 类型定义的只读核对发现 Apple `mode_t=u16` / `S_ISVTX:mode_t`，而 `MetadataExt::mode()` 为 u32，原混合位运算会导致 macOS 编译失败。改用等值的 Unix sticky bit `0o1000`（推导为 u32），并将 AsRawFd import 限 Linux/macOS，补入现有静态回归门；Linux 权限语义与所有 format pins 不变。R25 两次 Read/Grep 后返回 `VERDICT: PASS`，承认前轮遗漏，P0/P1 none。未进行本地 Apple 构建，不把源码核对冒充远端 macOS 成功。此前 .26 lib config 9/9（5m08s 编译、0.30s 执行）、compat 57/57（4m01s 编译、39.28s 执行；100 MiB 211.733868ms）、db migration 94/94（27.77s 执行）已通过；portable bit 修改后的具名复验与 Clippy 待收口。单目标执行中仍有一次 production library 重新生成，未宣称前次全部缓存持续复用；没有清理缓存或接触其它仓库的 Cargo。

**Repeated-build diagnosis / execution adjustment:** Cargo fingerprint INFO 精确指出 `build.rs:27` 监看的 `web/tailwind.config.ts` 不存在，导致 custom-build 每次 dirty，继而使依赖它的库与测试重复生成。这是既有构建失效问题，不归本卡 repair 行为轴；未创建伪配置或修改 build.rs。结束仅在等待 build lock 的重复 Cargo 队列（exit 130，无运行中用例被杀），将剩余具名 targets 合并一次 `--no-run`，并以明确输出的 test-binary 路径、`CARGO_MANIFEST_DIR=/mnt/gitmono/libra`、1800s timeout / 单测试线程直接执行；不运行全量。错误的 `move_test` 子串曾额外匹配 remove_test 19/19，不能算作 MIG-R01 的搬移证据；已改为完整 `worktree_test::test_worktree_move_` 九门，device 66308 与 57 保证跨设备案例实际执行。首次直接执行未提供 Cargo 的 runtime manifest 环境，虽通过但出现 helper fallback 提示，补全环境后复验。

**Final A/B / plan-focused closure (v0.22.26, 2026-09-14):** 16 AC 与 16 具名门全部有实际执行证据，按各完整 target / 前缀分组复验，没有 0-test 替代。R25 修正后：test-upgrade configuration compat 57/57（41.04s）；db migration 全 target 94/94（69.93s，包含三项 MIG-01 fixture、MIG-02 role 矩阵、MIG-04 barrier 与 MIG-06 两项事务门）；config command 82/82（30.00s）；MIG-R01 精确 move 前缀 9/9（9.29s、跨 device 未跳过）；hash-pinned old-reader oracle 2/2（2.64s，实际 .19 producer 生成 cohort，再证明 .16 拒绝写入）；serial registry 18/18、production unwrap 1/1、版本面 2/2、error docs 2/2、docs matrix 9/9。matrix 首次 8 pass / 1 fail 是新增表格中中文逗号紧贴 feature token，令扫描器读成 `test-upgrade` 后连带下一 target；仅补分隔空白后原守卫 9/9，不放宽校验。另在无 test-upgrade 的默认编译（4m19s）上完成 compat 50/50（84.96s）及 ledger-only 1/1（1.32s），证明 release-disabled hooks 的常规路径；默认与 feature cases 不重复计作 distinct。lib config 的 9/9 如前述在 Linux 等值 portability 修改前完成，未冒充修正后的 Apple 执行。

最终 scoped Clippy 两组均 `-D warnings` 通过：test-upgrade 的 `--lib` + compat/db/oracle/command/serial/unwrap/version/error/matrix 九个明确 integration targets（3m08s，含等待 build lock），以及默认 `--lib --test compat_global_config_schema_future --test db_migration_test`（59.55s）。fmt check、install.sh syntax、diff check、nextest generator 同步均通过；只有既有第三方 proc-macro-error2 future-incompat notice。未运行任何本地全量 build/test/all-target-all-feature lint 或网站全量 build/typecheck/preview，未读取真实 global/system 配置值。285 个 distinct 具名 Rust cases 的分组证据如上，额外误匹配 remove 19 cases 不用于验收计数。Codex final source review PASS；独立 R25 最终字面 `VERDICT: PASS`，R22 与 portability 问题均关闭。C/D 仍需实际提交、Release 与远端结果，不因本地绿灯提前 complete。

网站 minimum-version 补充签名+DCO commit `6f6d80123add52a744c480ac9c7410c8acbe71b1` 已推送并精确匹配 cf（主体修复说明为 `0f87fccddd89a4f9f99c6962e717f5addd6fcd4f`）。更新后 gen:docs 仍为 248 docs/search；en/zh-CN 均含 v0.22.26 最低版本；选定 Vitest 5 pass / 4 未选中，不声称全九项通过。MIG-06 是本计划最后一张实现卡；创建 Release 后继续最后的证据审计，D 结果异步记录。

**C complete / D asynchronous handoff（2026-09-14）:** signed+DCO implementation commit `b60005021afd90bcb720d329b5b776fec4c55e7d` 已推送 main；`gh release create v0.22.26 --target <exact-sha> --notes-file /tmp/libra-mig06-release.N0kbCW/release-notes.md` 于 10:06:47 UTC 创建非 draft [v0.22.26 Release](https://github.com/libra-tools/libra/releases/tag/v0.22.26)。内容化 notes 总结确认式修复、精确 cohort、Unix/权限限制、SQLite 备份/事务、旧 reader 保护与实际限定验证，不宣称任意 legacy DB 可 repair。`git ls-remote` 证明 main 与 tag 当时均精确指向 b60005021；两个仓库工作树 clean。实际 D [Release 34831494280](https://github.com/libra-tools/libra/actions/runs/34831494280) 与 [CodeQL Advanced 34831470993](https://github.com/libra-tools/libra/actions/runs/34831470993) 均为 push / b60005021、in_progress；已成功的 Code Quality `34831470788` 不能替代 Advanced。尚未取得 .26 的多平台构建、manifest、Homebrew 与 R2 下载成功证据，因此本卡与整个 goal 不提前标 complete。不等待该 workflow 才推进工作；实现卡已全部交付，剩余仅 D 异步验收和最终证据收口。本段以无版本变化的 docs-only 证据提交保存，不新增任务卡或第二个 release。

**全量收口启动 / R26（2026-09-14 10:25:35 UTC）:** 依用户最新指令，在 `2140f250c52cfa6b472dc49255dc29115caed2cd` 基线上启动顶部 nextest 命令；实际还传入 `--tool-config-file libra-full-suite:/mnt/gitmono/tmp/libra-full-suite.tGFCUS/nextest-timeouts.toml --no-pager --color never --failure-output never`。临时 config 不覆盖仓库 serial groups，仅设置所有单个测试的 120s slow / 600s terminate / 10s grace；失败输出不直接转交终端，后续通过私有结果日志分析，不隐藏失败状态。原始运行日志在 `/mnt/gitmono/tmp/libra-full-suite.tGFCUS/full-suite.log`，本机 nextest 为 0.9.144（不是 CI pin 的 0.9.143，未改 CI 或安装工具）；编译仍在执行，尚无全量 pass/fail 结果。Codex bounded review PASS；Claude Code R26 只读新覆盖段落与临时 config 后返回字面 `VERDICT: PASS`，P0/P1 none，提示需确认配置实接线与不掩盖 Git/Libra 环境失败，均由上述精确参数和最终失败分析约束承接。它不认证未执行结果，也不补足 CI 性能证据。Release 的 Linux amd64/arm64 已成功，macOS、Windows 与 Advanced CodeQL 仍运行，未等待 D 才启动本次全量。

**CI 性能取证执行细化（R27 待独立审查）:** `global_schema_repair_100_mib_backup_budget` 的原有 CI runner / 10s 门不变，本机 100 MiB 耗时不能替代 CI 证据。使用从 v0.22.26 implementation commit `b60005021afd90bcb720d329b5b776fec4c55e7d` 派生的独立 worktree 与 `codex/mig06-ci-benchmark` 验证分支；仅在该分支将默认分支已存在的 `live-compat.yml` 手动入口替换为单个 `ubuntu-latest` benchmark job，并添加专用验证脚本。该分支改动不合并 main、不改产品/原测试/版本面/Release、不触发 scheduled/live AI/cloud 或任何 Cargo/web 构建，属于同一 MIG-06 既有性能门的可复现验证工件，不是新产品任务卡；签名+DCO 提交并推送该精确分支，再以 `gh workflow run live-compat.yml --ref codex/mig06-ci-benchmark` 启动。工作流只授予 `contents: read`，checkout 不持久化凭据，不引用 secrets / OIDC / write token，使用独立 concurrency group 与有界 job/子进程超时。下载与执行固定 v0.22.19 producer（既有 hash pin）和实际 v0.22.26 Linux amd64 binary（hash `6d6599a2babab22a14bbe0a001f47f98d4205ae13f3e55142b845d0fb8b6ccb1`，来源为 Release `34831494280` / artifact `10343240668`，116926712 bytes），必须先验证 SHA-256。所有 CLI 子进程清空继承环境后只注入固定 PATH、隔离 HOME/XDG/global/system 路径与 LIBRA_TEST，不使用真实配置。实际 producer 生成 cohort 后加入与原 Rust 门相同的 100×1 MiB synthetic zeroblob；量测新版 CLI 的 backup+reopen+repair 全程（严格包含预算要求的区间），并核验 retained backup 的 quick_check、内容/receipt 保留与私有权限；复制阶段无写事务仍由已执行的原故障门证明，不用墙钟推断锁语义。只上传小型 metrics JSON（版本/hash、耗时/预算、runner OS/arch/CPU/文件系统、验证断言结果），不上传 DB、配置值或继承环境。收口记录精确分支 commit、workflow run/job 与真实耗时；任何失败保留并分析，不通过扩大 10s 预算、跳过或把本地运行冒充 CI 关闭该门。

**R27/R28 验证分支记录:** R27 计划审查与 R28 脚本/工作流审查均返回字面 `VERDICT: PASS`，P0/P1 none；原 10s 预算、版本/hash 与数据隔离不变。R27 提示的收口矩阵已同步，验证分支明确保留供复验、不合并 main。signed+DCO commit `96ad847b2d7b367691e310915fbedf95bc188a4a` 已推送并精确确认 `codex/mig06-ci-benchmark`，实际 dispatch [34834775944](https://github.com/libra-tools/libra/actions/runs/34834775944) / job `103945994362` 取得 CI 运行但失败：安全自测 3/3 通过，binary 下载阶段 `HTTPError`，没有进入性能量测；小型失败 metrics 已保留。脚本早期本地发现的版本输出 `libra 0.22.19`（无 v）与 Python Path.parent 属性误用已在该 commit 前修正，之后本地实际 pinned binaries 完整通过（0.162746s，tmpfs，ci=false），未冒充 CI。后续只读网络诊断发现相同公开 URL 的 urllib HEAD 为 403、正常 curl HEAD 为 200，具体服务端拒绝规则未确认；脚本改用仓库 installer 已使用的 curl，禁用 curlrc、不继承环境/凭据、不伪造 User-Agent、不跟随重定向，保留 TLS-only、size/hash 与时限约束。两份 binary 从零下载后的本地完整 gate 为 0.159680s / ci=false，CI 复验与该改动复审仍待取得。R28 第三次 Read 恰遇下述共享 worktree 被切换，读到了旧矩阵；不把该次读取当成新矩阵确认，已从 stash 恢复的正确矩阵需最终复核。

**全量运行中断与隔离恢复（2026-09-14）:** 首次 nextest `789e0be8-2915-4bba-b454-de3b61404253` 的编译 11m22s，计划运行 10040 tests / 230 binaries；但共享 checkout 的 reflog 证明 10:35:20 UTC 被其它工作从 main 切到 `feat/m4-m5-ol10-15`，10:37:53 又 merge 成 `f63cbf9fe7df3a521ccf252d7385b74f61779117`。切换发生在编译完成前，运行时读源码的守卫已混入另一分支；因此主动向本次 nextest PID `3327354` 发 SIGINT，非测试超时、未终止任何其它工作。退出 100，执行阶段 323.286s，6065/10040 完成（6045 pass、20 fail、3 ignored），3975 未完成；此混合源码运行不能用作完整验收。20 个候选失败包括 17 个 merge 无 ambient Libra fixture 的用例，以及 GC file/config surface/mutable table 三个登记守卫；逐项必须在固定源码上再归因，不以共享分支上恰有修复的源码冒充旧 binary 已通过。

原工作区的 `stash@{0}` / `d868972f5d9356fb300c53b6c5190269b1ab76c6` 仅保存本计划 WIP；已只读取得该文件并通过 patch 恢复到从 `2140f250c` 创建的独立 `codex/mig-final-validation` worktree `/mnt/gitmono/tmp/libra-mig-final.hKyvQI`，不 pop/drop stash、不切换/修改共享 feature 分支、不合并其 PR。独立 target 是原缓存的普通文件副本而非 hardlink，后续 Cargo 不写共享 target。第二次全量使用该固定 worktree 与独立 `--target-dir`，从原路径 source 两个测试环境文件（不输出内容），另将 global/system DB override 指向该 worktree 内 task-owned `test-scopes`，保留 4 build jobs / 4 test threads、原仓库 serial groups 与 600s 单例时限；运行日志为 `/mnt/gitmono/tmp/libra-full-suite.tGFCUS/isolated-full-suite.log`。第二次已启动，待实际完整结果，不把重启当成通过。本计划与 goal 在完整全量、CI 性能及 Release D 收口前继续保持未完成。

**D closure / v0.22.26（2026-09-14）:** Release `34831494280` 与 Advanced CodeQL `34831470993` 均 completed/success，精确 source 仍为 `b60005021`。四平台 build/upload（Linux amd64 `103935626897`、Linux arm64 `103935627157`、macOS `103935627065`、Windows `103935627071`），Homebrew update `103946855517`、installers `103946856007`、manifest `103946856108` 与 Homebrew verify `103946927482` 八项均成功。manifest 于 10:49:22 UTC 返回 HTTP 200，control revision `18`、version `0.22.26`、payload digest `f936d1cc0c41603e2599a5b88ce1887b0839c67bf3f4f3652eebfbbaa4602548`；Homebrew 于 10:49:43 实际安装 `/opt/homebrew/Cellar/libra/0.22.26`（4 files / 96.1 MB），warning/skip 仅出现在脚本定义而非实际分支。四个 v0.22.26 R2 artifact HEAD 均 200。D 已闭合，但 CI 性能及用户新要求的真实全量未闭合，不能把整个计划/goal 标完成。

**R29 / 后续验证进度:** R29 对 curl 前滚修正及隔离 worktree 内已恢复的计划矩阵返回字面 `VERDICT: PASS`，P0/P1 none；其 partial-download-cache P2 不影响 CI 每次 fresh tempdir 的路径，显式缓存诊断保持 hash fail-closed，不自动复用不完整 binary。signed+DCO commit `b660a078812bf22028e40f69fec6bdd449ece4ac` 已推送验证分支，实际重新 dispatch [34835661978](https://github.com/libra-tools/libra/actions/runs/34835661978)，待真实结果。固定 `2140f250c` 源码全库检索进一步确认既无 `operation-v2-repository.lock`，也无 `file_history.json`，首次对应两个登记失败由编译后扫描另一 feature 分支引入的新名字造成；不为这两项混合证据修改主线无关代码。其余 17 个 merge fixture 候选与 configuration ledger 登记缺口仍须以隔离全量结果闭环。

**CI 性能门闭环（2026-09-14）:** 上述 `34835661978` 已 completed/success，event=`workflow_dispatch`、branch=`codex/mig06-ci-benchmark`、head=`b660a078812bf22028e40f69fec6bdd449ece4ac`，job=`103948771543`，所有步骤均成功。已实际下载 `mig06-released-binary-metrics` 并核对：`ci=true` / `passed=true`，Ubuntu 24 image `20260907.300.1`、Linux `6.17.0-1022-azure`、glibc 2.39、x86_64、4 CPUs，文件系统 stat 类型 `ext2/ext3`；fixture 106221568 bytes（含 100 MiB payload），CLI-inclusive backup+reopen+repair 实测 **0.734816586s < 10s**，`backup_and_preservation_verified=true`。实际执行 binary pins 与 Release/producer 既定 hash、size 完全一致，release source=`b60005021`。原失败 run 保留，未扩大预算、未跳过或把本机计时改写成 CI 计时；独立验证分支保留但不合并主线。至此 .26 的 Release D 与原 CI 性能门均已取得，尚待隔离的完整全量与其候选失败前滚收口。

### Task MIG-R02: 消除验收代码中的隐式 Repository 上下文假定

**Task type:** `implementation`

**Lifecycle / Acceptance:** `done` / `complete`（完整全量、signed+DCO/push/gh及v0.22.27 D均已取得；重复Release run的immutable-object失败如实保留）

**Description:** 让 role separation 后的验收代码显式声明其 Repository 上下文，修复当前 Git checkout 下的 18 个确定性单元测试失败。本卡只改测试期的作用域判据与 fixture，不改变配置迁移、修复流程或 merge 算法。

**Current evidence:** 固定 `2140f250c` runtime + `b68af3e8c` docs 的隔离全量 `0c0f133e-a64d-45f4-a229-94e4586e03e1` 已重现 17 个 `command::merge` 用例缺失 ambient Libra repository 与 `internal::mutable_state_ownership::tests::mutable_state_scope_registration_guard` 一项失败。`src/internal/mutable_state_ownership.rs:1-21` 定义的是 Repository state inventory，但 `:1149-1161` 对全源码 DDL 无角色区分；configuration ledger 已由 `src/internal/db/schema.rs:45-54` / `:267-276` 的 role/ledger manifest 归属配置数据库，不应补成 Repository state，也不应伪称 migration-only scratch。`src/command/merge.rs:14509-14513`、`:15764-15775`、`:16715-16725`、`:17677-17687` 的测试/测试 helper 仅锁住 ambient CWD，没有创建自己的 repository；实际 panic 在 `src/utils/util.rs:1033`，这是调用方未满足生产 API 前置条件，不是允许放宽 working_dir 的理由。全量仍在继续，后续新失败须另行归因，不得无界纳入本卡。

**Behavior boundary / G-01 review:** 单一恢复动作是撤销本卡的测试上下文适配，回到原测试定义；生产配置与 merge 行为不变。三个源码文件落点都属于验收代码把配置/工作目录视为隐含 Repository 的假定：源码表扫描须承认已存在的 Configuration role manifest，内容合并测试须持有独立 Repository fixture。独立审查必须确认这仍是一个有界的测试上下文行为轴；若 G-01 不通过，先拆分/重新决策，不得以“全量变绿”强行打包不同生产行为。

**Out of scope:** 不改生产 `StateOwner` 枚举或新增公开 ownership API；不改变生产 schema manifest、SQL、migration/barrier、`working_dir`、属性解析、merge 冲突/rename 算法或命令 surface；不转换任何工作 checkout、不更改其它 feature 分支；不导入原混合分支的 `operation-v2-repository.lock` / `file_history.json` 变更，不把测试改为 ignore。

**Acceptance criteria:**

- [x] 配置 ledger 只通过现有 Configuration role/ledger manifest 的精确名称在测试判据中识别，不错误登记为 Repository 或 transient state；未知持久表仍 fail-closed。
- [x] 新增真实隔离 GlobalConfig/SystemConfig 数据库验证，证明配置 ledger 存在且归属正确；Repository materialization 守卫仍禁止配置专属 ledger 泄漏，原双向/唯一性/存在性检查保留。
- [x] 17 个已失败 merge 用例及其共享测试 helper 使用自有临时 Repository 与 global/system 配置隔离，不依赖外层 checkout 的类型或 `.gitattributes`，原行为断言不削弱。
- [x] 新 fixture 的 CWD/环境/hash-kind 在正常返回及 panic 时恢复，临时目录删除前先恢复 CWD；CWD 锁覆盖环境恢复，避免引入并发窗口，使用既有 ConfigDbFixture/ChangeDirGuard，不改共享 helper 实现。
- [x] 受影响的单元测试显式使用 cwd/env/hash_kind 串行键；`tests/SERIAL_REGISTRY.tsv` 与生成的 `.config/nextest.toml` 按现有 classifier 的实际扫描范围验证同步，未被扫描的 src 单元测试不得制造 dangling row；不得放宽 classifier，相关 source-scan 与 serial 守卫通过。
- [x] 最终 patch-bumped 状态的相关门与完整默认 nextest 全绿，真实版本/签名/DCO/push/gh/D 证据齐全；内部 fixture/role-map 文档同步，用户命令文档 N/A 的理由明确。

**Verification:** 以下命令在实施 worktree 执行；先从原路径 source `.env.test` 和 `.env.live-test`，不输出其内容；`LIBRA_SKIP_WEB_BUILD=1 CARGO_INCREMENTAL=0`，独立 target。全量仍使用顶部已验证的临时 nextest timeout config；CLI 命令的等价绝对 env-file 路径与 target 路径须记录。

- [x] `source .env.test && source .env.live-test && cargo nextest run --lib -E 'test(internal::mutable_state_ownership::tests::)' --no-fail-fast --retries 2 --test-threads 1`；须含新增 `configuration_ledger_scope_is_explicit` 与 `configuration_materialization_is_not_repository_state`（new），不接受 0 tests。
- [x] `source .env.test && source .env.live-test && cargo nextest run --lib -E 'test(command::merge::)' --no-fail-fast --retries 2 --test-threads 1`；须覆盖原 17 个失败案例与新 fixture 的正常/panic 恢复案例（new），不能只测一个代表例。
- [x] `source .env.test && source .env.live-test && cargo nextest run --test db_migration_test --no-fail-fast --retries 2 --test-threads 1`，验证已有角色矩阵未退化。
- [x] `source .env.test && source .env.live-test && cargo nextest run --test compat_serial_registry --no-fail-fast --retries 2 --test-threads 1`。
- [x] `source .env.test && source .env.live-test && cargo nextest run --test compat_all_production_unwrap_guard --no-fail-fast --retries 2 --test-threads 1`。
- [x] `source .env.test && source .env.live-test && cargo nextest run --test compat_version_surface_sync --no-fail-fast --retries 2 --test-threads 1`。
- [x] `source .env.test && source .env.live-test && cargo nextest run --test compat_matrix_alignment --no-fail-fast --retries 2 --test-threads 1`。

**C quality gates（所有推送卡的既有门，不计入本卡 VER）:** `cargo +nightly fmt --all --check`、`git diff --check`；按现有登记规则运行 `sh tests/NEXTEST_GROUPS.sh`，其内容由上述 serial guard 校验；`source .env.test && source .env.live-test && cargo clippy --lib --test db_migration_test --test compat_serial_registry --test compat_all_production_unwrap_guard --test compat_version_surface_sync --test compat_matrix_alignment -- -D warnings`（依用户覆盖保持 scoped Clippy，不升级到 all-target/all-feature）。

**Full-suite trigger:** `T-1`：触及作用域判据及生成的测试分组配置；`T-5`：用户已明确恢复全量收口。本卡 C 组推送前必须在最终 bump 状态实际运行载入两个测试环境文件的 `cargo nextest run --all --no-fail-fast --retries 2` 至全绿；原 10 分钟单例限时、失败分析及不增加 all-features/ignored/live feature targets 的边界不变。

**Dependencies:** `MIG-01`（已发布的隔离 fixture）、`MIG-02`（已发布的 role/ledger manifest）；二者均 complete。发布窗口排在 MIG-06 的已完成 C 之后，但不把“计划级全量已全绿”设为本修复卡的开工依赖。

**Deliverables:** `N/A`。

**Implementation write set / Files likely touched:** `src/internal/mutable_state_ownership.rs`（仅 cfg(test) 判据/测试及其契约注释）、`src/command/merge.rs`（仅 cfg(test) fixture/测试接线）、`src/command/merge_rename_content_test.rs`（既有 cfg(test) 子模块的相同 fixture/serial 接线）、`tests/SERIAL_REGISTRY.tsv` 与生成的 `.config/nextest.toml`（验证同步；仅 classifier 实际覆盖变更时才产生 diff）、`docs/development/internal/database-migration-scope.md` 的 fixture/ownership 验证说明、本计划。复用既有 test targets，无 Cargo test 注册/索引条目新增；如命名索引说明需同步，只改对应既有行。

**Release write set / Version increment / Release boundary:** ER-08 六个实际版本文件（Cargo.toml、Cargo.lock、install.sh、install.ps1、web/package.json、worker/package.json）；工具链刷新 lock，独立 `patch +1`（当前 .26，实施前重读实际版本），`independent`，不能提前批量 bump。

**Docs and compatibility impact:** 无命令/flag/output/schema/稳定错误码变更，EN/zh 命令文档、COMPATIBILITY、error docs 与网站为 N/A；不新增公开 Rust API。仅内部测试边界说明与基于实际修改的 release notes。Version faces 按 ER-08 同步。

**Rollback mode / Migration and rollback:** `revert`（发布前撤销本卡测试上下文适配）；已发布后只 forward-fix。无真实 DB、迁移或生产 merge 语义变更。

**Security and privacy:** 只使用 task-owned repository 与 ConfigDbFixture；不访问真实 global/system 配置值、不使用原 checkout 作为 Libra fixture，测试 stdout/报告不得泄漏 env 文件内容。

**Performance budget:** 无生产 hot-path 成本；新增 fixture setup 有界，仅本地临时数据库与常量配置，不做网络扫描或 unbounded retry；同一测试仍受既有 600s timeout，focused merge 前缀实际耗时须记录。

**Estimated scope:** `M`（保守按 3 个 src 文件/落点计，均为测试期代码；serial、docs 和版本面为随附同步集）。

**C/D coverage from / D post-push:** `self` / `D-MIG-STD`；完成本卡后才允许以完整全量把本计划收口，D 运行中不得标 `complete`。

**Granularity:** `type=implementation; axis=explicit Repository context in acceptance tests; recovery=revert test-only context adaptation before release and forward-fix afterward; complete=yes; self-contained=yes; AC=6/8; VER=7/8; landing=3; prod-files=3 (conservative cfg(test)-only src count); scope=M; deps=MIG-01,MIG-02; writeset=isolated from frozen baseline then final serial recovery; release=independent; split-from=full-suite recovery; exception=N/A`

**R30 closure:** Codex 自审 PASS；Claude Code 已读取完整卡、DAG/粒度/完成判据、模板 G-01..G-10 / ER-13 与三个 merge helper 和 scope guard 源码，返回字面 `VERDICT: PASS`，P0/P1 none，明确判断为一个有界的测试上下文修正轴（AC6、VER7、M）。第四个 helper `apply_for_test` 的锚点已由 Codex 直接核对；全量以 T-5 明确用户授权为确定依据，T-1 为保守附加归类；实现必须从 `SchemaLedger::Configuration.table_name()` 取名，不复制 literal。现可在独立实施 worktree 开始测试期写集；当前基线测试的 runtime/test/config/version 文件与 target 不改。若后续全量暴露其它根因，不以本卡“测试修复”为名无界扩写。

**R31 开工锚点补正（待审）:** 直接核对发现 `merge_rename_content_test` 是 `src/command/merge.rs:14402-14403` 挂接的独立测试文件，原卡漏列其 `merge` helper 与直接 outer merge 测试；三条 tree helper 均可能读取 attributes。补齐该同型测试接线文件，保守计数改为 3 src 文件/3 落点，仍为单一测试上下文轴、AC6/VER7、scope M，不新增生产写集。`setup_with_new_libra_in` 调用 init 会设置全局 hash-kind，因此 fixture 必须保存并在正常/panic 时恢复它，串行键覆盖 cwd/env/hash_kind（classifier 如判 global，使用现有全资源键），CWD 锁覆盖全部状态恢复。使用已存在配置 fixture、测试期局部 RAII guard；所有调用新增 fixture 的测试均加显式串行注解；登记范围以 R32 的现有 classifier 实际扫描边界为准，不只修当前失败的 17 项。独立实施 worktree 为 `/mnt/gitmono/tmp/libra-mig-r02.MW2s7B`（分支 `codex/mig-r02-recovery`，基线 b68af3e8c）；原固定全量不改源码/target，R31 PASS 前不写 Rust/serial/config。

**R31 closure / 开工预检:** Codex 自审与 Claude Code 独立复审均 PASS，最终字面 `VERDICT: PASS`，P0/P1 none；已逐项核对三个文件、init hash-kind 副作用和 G-01..G-10，未认证未来代码/测试。开工前以固定基线 binary 单线程直接运行 `role_scope::role_scoped_schema_writer_keeps_ledgers_disjoint`，1/1 pass（7.90s），包含三种持久角色的 absent/old/current/future 与 Derived 拒绝写入矩阵，配置路径为 task-owned override，未读真实配置。这是预检，不冒充 bump 后的 nextest 门。remote main 仍精确 b68af3e8c、v0.22.27 尚不存在，六版本面当前同为 0.22.26。

**R32 serial 扫描边界补正（待审）:** 实际运行 classifier 对这些 src 单元测试无输出，源码 `tests/SERIAL_CLASSIFY.sh:582-624,3689` 明确只遍历 `tests/`，不是对 src 判 `none/global`；`tests/compat/serial_registry.rs:101-141` 会拒绝未扫描测试的 dangling rows。故本卡全部 src fixture 调用仍受 `serial(cwd,env,hash_kind)`（可保守扩为现有全资源键）和 CWD RAII lock 保护，不新增无效 TSV row，不修改 classifier；生成器已重跑，209 test + 7 binary filters 与基线字节一致。AC5/写集如实改为按实际覆盖范围验证同步，并非跳过 serial guard（完整 18 cases 仍须执行）；T-5 full-suite、AC6/VER7、M3/3 和独立 release 不变。这是修正扫描范围假定，不扩产品行为或增加 waiver。R32 PASS 前暂停 serial/config 后续写入，已批准的 Rust 实现及有界 check 可继续。

**R32 closure / implementation verification start:** Codex 自审与 Claude Code bounded review 均 PASS，字面 `VERDICT: PASS`，P0/P1 none；直接证实 src 不进入 classifier 输入，禁止伪造登记列是遵守原双向守卫而非豁免。实际 generator 输出仍为 209 test filters + 7 binary filters，TSV/config diff 为零。五版本面已 patch +1 至 0.22.27，`cargo check --lib --offline --target-dir <isolated-target>` 通过（1m22s），工具链仅更新 Cargo.lock 的 libra 版本。现将 7 个原 focused 门合并一次 nextest 编译/运行：`--lib` 与卡内五个 integration targets，表达式选取两个完整 lib 前缀及五个完整 binary，保留 retries2/single-thread/600s tool config；不把未执行测试计为通过。原固定全量继续，未改其源码或 target。

**R33 / 第一轮 focused 证据:** 独立 implementation review 字面 `VERDICT: PASS`，P0/P1 none，确认 33 个原测试 fixture 调用与 2 个恢复测试全部带串行注解，配置 ledger 来自现有 manifest 且原 source/materialization 双向守卫未放宽。第一轮 bumped nextest `489200d6-8355-4f6b-a406-9880ca36b6d5` 编译 9m02s、执行 343.106s，236/236 pass（6 binaries，4824 未选择），包括原 18 个失败与新增 4 项测试，无失败/重试。fmt、diff、installer shell syntax 均通过；这不是最终全量。R33 P2 的 symlink temp-root 比较差异与 Codex 自查发现的 process-wide DB cache 生命周期已在同一 AC4 fixture 边界继续补强：先拥有 RAII 对象再初始化，canonical root，runtime 尚存时 evict/close repo pool 并清除 object-storage cache，正常/panic 测试额外保留 pool clone 断言已关闭；LIBRA_TEST 改用既有常量。数据库与 merge 生产代码仍无改动；后续必须重跑这 236 项，不能用本轮绿灯替代修正后的证据。

**R34 closure / 最终态预编译:** 独立 follow-up review 字面 `VERDICT: PASS`，P0/P1 none；直接核对先构造 owner 再 setup、panic 展开时 runtime enter guard 已释放、cache key 与生产 discovery 的 canonical `.libra` 路径一致、先 close pool 再 drop runtime/tempdir。Codex 另将测试比较两端均 canonicalize，涵盖 Windows verbatim prefix，而非只规范化预期路径；未改其它断言。scoped Clippy 第一轮 `-D warnings` 已过（1m10s，最终路径断言规范化前），后续最终态再验一次。当前以 `cargo nextest list --all --build-jobs 4 --list-type binaries-only --message-format json --target-dir <isolated-target>` 预编译完整默认集合，产出 task-owned `r02-binaries.json`；没有启动第二套并行全量，原基线继续完整运行。当前 tracked src/tests/sql/build/config/六版本面组合 SHA-256（按 `git ls-files -z ... | xargs -0 sha256sum | sha256sum`）为 `5fd064e6c62cff39236c3717dd07b6e7867678f1bd2a5b3ff09a3836635993d7`，预编译至测试期间冻结该集合；后续实际测试计数/退出状态才是验收，不把 metadata 或预编译当作测试通过。

**R35 nextest 两阶段执行登记（待审，仅 MIG-R02）:** 实际完整预编译已成功（11m09s），binary metadata 含 230 binaries、cargo metadata 唯一 package 为本 worktree 的 libra 0.22.27；无 filter 的实际 listing 共 10047 cases（10044 non-ignored、3 原有 ignored），预编译前后 tracked source 组合 hash 均为上段值。nextest 0.9.144 实际拒绝 `--all` 与 `--binaries-metadata` 同传：第一次相关门调用退出 2、没有运行测试，已保留参数错误，不计入通过；移除编译期 `--all` 后，相关门仍精确选择 236 cases（完整 metadata 的其它 9811 项仅本次 focused 未执行）。为避免缺失 web tailwind 输入导致 Cargo 再次重复编译，允许本卡最终全量以官方两阶段复用执行：① 已执行的 `cargo nextest list --all --build-jobs 4 --list-type binaries-only --message-format json --target-dir <isolated-target>`；② 从原绝对路径 source 两个 env 文件、设置相同 skip-web/private-config/CARGO_MANIFEST_DIR 后，执行 `cargo nextest run --binaries-metadata /mnt/gitmono/tmp/libra-full-suite.tGFCUS/r02-binaries.json --cargo-metadata /mnt/gitmono/tmp/libra-full-suite.tGFCUS/r02-cargo-metadata.json --no-fail-fast --retries 2 --test-threads 4 --tool-config-file libra-full-suite:/mnt/gitmono/tmp/libra-full-suite.tGFCUS/nextest-timeouts.toml --no-pager --color never --failure-output never`。最终命令禁止任何 `-E` / test-name / binary filter，必须实际 Starting 10044 tests / 230 binaries、只跳过原有 3 ignored，终态全部通过，失败/重试如实记录。运行前后再次核对源码 hash 与 metadata package/count；任一不匹配即禁止复用，恢复顶部直接 `run --all` 编译+执行，不减用例、不放宽时限、不使用旧 binary 或分组绿灯替代全量。此处仅改变编译复用入口，不豁免 ER-13 或 C 全量；其它卡历史命令不改。原始固定基线全量结束前不启动这次最终全量；R35 独立 PASS 前不启用此替代入口。

**最终 A/B focused 与 scoped C 静态门（v0.22.27）:** 复用本次完整预编译的实际运行 `e7522251-2a64-416c-985f-9a074bfb9136` 已退出 0：236/236 pass、执行 316.251s，无失败/重试；9811 skipped 是这一次 focused 的未选集合（含原有 ignored），不得计作全量。细分为 merge lib 108、ownership lib 5、db migration 默认 93、serial registry 18、unwrap 1、version surfaces 2、matrix alignment 9；每个门非 0 tests。新增 pool-closed 正常/panic 两例实际通过，原基线 18 个失败全部在最终态通过。最终 scoped Clippy `-D warnings` 通过（1m01s），fmt check、diff check、shell syntax 与 generator 零漂移通过。整个受测 src/tests/sql/build/config/版本面组合 hash 仍为 `5fd064e6c62cff39236c3717dd07b6e7867678f1bd2a5b3ff09a3836635993d7`。metadata 指向唯一 owned worktree/target，所有 230 binary 路径均在其中；完整无过滤 listing 为 10047、3 ignored、10044 non-ignored，non-ignored unmatched=0。binary metadata hash=`0731dfc5efafae56f2ef77078e3f4ff327f09c50c388ad1d2ee5e3c17a35171f`，cargo metadata hash=`c0d9480db10adeb1c19d1f6698d0b18cfb2e4677f4f69e42f91679820b05c43a`，listing hash=`1c5f6bddbf9010c7d7e35f7c4015283886fd232be8a845a8a0bd54a603ae1e5a`。这批 A/B/C 静态绿灯不替代 C 完整全量、提交/gh 或 D；本卡与计划仍未完成。

**R35 closure:** Codex 自审与 Claude Code 独立规范审查均 PASS，最终字面 `VERDICT: PASS`，P0/P1 none。它直接读取了 CLI 冲突错误、已运行的官方复用日志、R35 条款与 ER-13，确认 `--all` 在完整构建阶段选择、无 filter 的复用执行覆盖同一完整非 ignored 集合，不是减弱验收；必须满足既定前后 hash/package/count 检查及不匹配时的直接 `run --all` 回退。其结论不认证尚未执行的最终全量。现可在原始全量结束、失败集合核对完成后，按 R35 精确命令启动 10044-case 最终全量；开始/退出结果另记。

**固定基线完整全量终态（2026-09-14）:** `0c0f133e-a64d-45f4-a229-94e4586e03e1` 已完整退出 100：230 binaries、10040 executed，10014 passed（14 slow、1 flaky）、26 failed、3 原有 ignored；编译 12m52s、测试执行 7845.436s。26 项为本卡 17 merge + 1 ownership、R37 的 7 live-ledger anchor、1 umask 前提；没有 TIMEOUT/ABORT。`command::pull_test::test_pull_ff_only_diverged_remote_rejects_without_changing_head_or_worktree` 第二次通过（9.175s），如实保留为 flaky，不算首次通过。最后两个 MCP 案例内嵌 `cargo build --bin libra`，首个编译、第二个等 Cargo 锁；实际均通过（289.486s / 533.099s），CPU/子进程持续推进，没有误杀健康编译。首次混合源码无效运行与此固定基线运行仍独立保留，不合并计数。

**R38 最终完整验收启动（2026-09-14）:** 新完整预编译已退出 0（10m49s）。重新生成的 `r02-r38-binaries.json` / `r02-r38-cargo-metadata.json` / `r02-r38-all-tests.json` hashes 分别为 `0731dfc5efafae56f2ef77078e3f4ff327f09c50c388ad1d2ee5e3c17a35171f` / `c0d9480db10adeb1c19d1f6698d0b18cfb2e4677f4f69e42f91679820b05c43a` / `1c5f6bddbf9010c7d7e35f7c4015283886fd232be8a845a8a0bd54a603ae1e5a`；路径/测试清单未变所以这些 hash 与旧 metadata 相同，但本轮完整编译和受测 source hash `cde8a659dde307a16b0735e6d1ff5266d38fc175df43d21945a0b75c359f7a60` 是独立新证据。已核实唯一 package libra 0.22.27、workspace/target 均为 `/mnt/gitmono/tmp/libra-mig-r02.MW2s7B`、230 个实际 binary、10047 total、10044 non-ignored、3 ignored 与旧清单逐项相同、non-ignored unmatched=0。原始基线退出后，实际按 R35/R38 无 filter metadata 命令启动 `76064f34-b105-429b-b2dd-74606390ddab`，Starting 10044 tests / 230 binaries、3 skipped；原绝对路径 source 双 env，`umask 022`，`LIBRA_SKIP_WEB_BUILD=1 CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=4`，显式 owned `CARGO_TARGET_DIR` / `CARGO_MANIFEST_DIR` 与 private config override，4 test slots、retries2、600s tool config。完整日志为 task-owned `r02-final-full-suite.log`；当前仍 running，不认证全绿或 C/D 完成。

**MIG-R02 完整 C 测试终态（2026-09-14）:** `76064f34-b105-429b-b2dd-74606390ddab` 已退出 0，测试执行 **7141.513s**，230 binaries、**10044/10044 passed（13 slow、2 flaky）**、3 原有 ignored；没有增加 filter、忽略项或 live/all-feature 范围。逐一按 binary+test identity 对照原固定基线第三次失败清单，26/26 已在本轮通过；其中 ledger 全 target 43/43、umask 案例 3.332s。运行后源码组合 hash 仍为 `cde8a659dde307a16b0735e6d1ff5266d38fc175df43d21945a0b75c359f7a60`，三个 metadata hashes 与启动时逐项相同；结果 XML 的 tests=10044/failures=0/errors=0，已保留私有 `r02-final-junit.xml`（SHA-256 `6f795b06be97f22b851cd4a2a246680027c8549284b792622d49041906eed864`）。post-run fmt check、generator 零漂移、install.sh syntax 与 diff check 通过。

**两项重试分析:** push quiet-force 案例首次 4.229s 失败，JUnit 将失败定位到 `tests/command/push_test.rs:93` 的 fixture `config user.email` 子进程非成功，stderr 为空；尚未进入所测 force-push 行为，记录没有提供子进程 exit/signal，不能编造更深根因。原 nextest 第二次 10.678s 通过；另在同一最终 binary 上做三次有界单例诊断均通过（10.32/10.78/10.58s），未修改源码，不宣称已消除偶发性。一次初始诊断因误用不存在的 binary 路径退出 127，未执行测试，单独保留而不计入复验。MCP 首例 431.994s 通过；`test_web_only_sigterm_releases_ports` 首次在旧 600s 限时实际 timeout 600.019s，JUnit 确认曾等待 build-directory lock，进程观察证实之后在活跃 rustc 编译；第二次 355.310s 通过。保持这两项 flaky 与旧 timeout 事实，不把 retries2 的全绿写成首次全绿。用户后续要求将二例 20 分钟配置写入仓库，作为独立后续配置卡执行，不混入本卡已经完成验收的源码/版本写集；因此本轮全绿不能替代后续配置卡的最终验收。本卡提交/gh/D 仍待取得，计划与 goal 未完成。

**R36/R38 文档证据提交:** 独立 signed+DCO commit `38ab8d34402039f30aca5161a2aefc7550daa5a9` 已推送 main，精准写集为原定 19 个 docs/evidence files（83 insertions/83 deletions），无版本变化。该提交先于 MIG-R02 implementation commit，保证 v0.22.27 的父链含有本次全量实际读取并通过的 live ledger/registry 修正；没有把这 19 个文件混入本卡 11-file implementation 写集，也没有改写任何历史 release。

**MIG-R02 C 发布交接:** 文档父提交 `38ab8d344` 之后，11-file implementation commit `0ef805fa12c2a61b7e9c31ceb7e227fb9c9a5e50` 已 SSH signed+DCO 提交并推送 main。已用实际修改/验证摘要执行 `gh release create v0.22.27 --target 0ef805fa12c2a61b7e9c31ceb7e227fb9c9a5e50 --notes-file /mnt/gitmono/tmp/libra-full-suite.tGFCUS/r02-release-notes.md`，非 draft Release 为 https://github.com/libra-tools/libra/releases/tag/v0.22.27；D 的 Release / Advanced CodeQL 尚在运行，未标成功。按用户要求不等待 D 才启动下一卡。

**MIG-R02 D 异步事件（2026-09-15 CST）:** 同一tag/commit出现两个独立push-triggered Release runs（34863017149、34863027428，均attempt1），实际只调用一次gh release create；重复触发来源尚未确认。前者arm64上传在2026-09-14T15:49:05Z明确报 `object libra/releases/v0.22.27/libra-linux-arm64 already exists (immutable release prefix)`，触发matrix fail-fast取消其它三平台，下游四job skipped；该失败保留，不重跑或覆盖immutable物件。后者Linux两平台已成功、macOS/Windows仍运行，待取得真实后续D。Advanced CodeQL 34862946862已success，exact head为0ef805fa；不能将这一项绿灯或另一个run的部分成功写成全部D成功。

**MIG-R02 D closure / v0.22.27（2026-09-15 CST）:** Release `34863027428` 已completed/success，四平台build/upload（104039813635/104039813841/104039813887/104039813986）、Homebrew update 104055015654、installers 104055016577、manifest 104055016507、Homebrew verify 104055083950八job全部success；与已成功的Advanced CodeQL 34862946862共同覆盖exact head `0ef805fa12c2a61b7e9c31ceb7e227fb9c9a5e50`。manifest于2026-09-14T16:16:23Z实际HTTP200，control revision19/version0.22.27/payload digest `0eaae3aea180c054bb3ed01038af1489260b0cbc0763c0be3c4a124195b9b0ff`；Homebrew于16:16:52Z实际安装 `/opt/homebrew/Cellar/libra/0.22.27`（4 files/96.1MB），不是warning/skip分支。四个v0.22.27 R2 artifact HEAD均200。重复失败run 34863017149不改写、不重跑；本卡C/D已完成，但追加MIG-R03仍须自身全量与release，不提前完成整个goal。

### Task MIG-R03: 将两项 MCP 测试的二十分钟上限固化到仓库

**Task type:** `implementation`

**Lifecycle / Acceptance:** `done` / `complete`（实现、A/B/C、signed+DCO/push/tag/gh均已完成；D在本机最终测试期间自行完成并取得实际证据，未为其等待，详见最终D事实）

**Remote D status:** `completed / success`；Release `34870861067` 与 Advanced CodeQL `34870771544` 均exact head `8fca47ebf19fe46e1bd7f3a5acbf52859179a1a1`。仅停止过本地watch，未取消/重启远端任务；本机最终测试期间的一次有界查询已直接取得成功终态及manifest/Homebrew/R2实证，不是假定成功。

**Description / single axis:** 将已获用户明确授权的两项 MCP nextest 单例 1200s 上限，从本次执行的 tool config 持久化到仓库可再生的 runner 配置；不改变 MCP 服务、测试行为或全局并行度。

**R41/R42 精确持久化契约:** 生成器输出下面一个确定的 timeout-only override，不写不存在的 `timeout = "1200s"` 字段，也不是两条按函数字典序分开的配置。120s慢提示和10s grace一同保存在仓库里，不依赖外部文件为二例补齐。guard须断言整个timeout-bearing override集合恰好是这一项、filter精确匹配两个给定identity、slow-timeout字段恰好为下列值；默认profile及其它override不得新增timeout/retry/on-timeout=pass设置。外部tool文件只给其它测试600s默认，不携带二例特例。

```toml
[[profile.default.overrides]]
filter = 'binary(=e2e_mcp_flow) & (test(=test_e2e_mcp_flow) | test(=test_web_only_sigterm_releases_ports))'
slow-timeout = { period = "120s", terminate-after = 10, grace-period = "10s" }
```

nextest官方per-test规则按“测试T与设置S”选择首个匹配且配置了S的override，不是匹配一条就停止读取所有其它设置。因此将来同一测试合法增加external成员时，timeout-only条目与group-only条目可以组合；不得为了不存在的属性冲突拒绝合法的registry成员。真正的重复/缺失/未知external成员仍由结构解析与原双向成员集合守卫拒绝。脚本摘要209/7从registry生成的F/B中间行计数，不从所有filter行计数。旧前缀误计的证据只针对本卡实际采用的 `filter = 'binary(=` 形式，不声称所有 `test(=...)` 形式都会被旧扫描器误计。

**R41 review / R42 closure pending:** 初次独立调查480s超时退出124，不计PASS；同会话禁用工具收口输出字面 `VERDICT: PASS` 但附3项P1义务，故不能据此宣布全部问题关闭。精确集合断言与摘要计数义务已纳入上述合同；其中“持久化timeout标量/120s与grace仅存在tool配置/必须拒绝未来合法属性重叠”的前提与已实际解析的R40表格及nextest按属性override语义不符，现明确更正，不盲目实施错误字段或新增限制。R42须复核上述关闭，P0/P1 none与字面PASS后才写配置/生成器/guard源码。

**R42 closure（2026-09-14）:** 独立 Claude Code CLI follow-up 已退出0、字面 `VERDICT: PASS`，明确 No remaining P0 or P1 findings。直接核对上述精确契约与R40实际配置后，确认整个时间策略集合断言、120s/1200s/10s持久化、F/B摘要来源、按属性合法重叠及外部配置不掩盖的关闭。允许进入实现，不认证尚未发生的全量、gh或D。开工只读复核main仍为 `0ef805fa12c2a61b7e9c31ceb7e227fb9c9a5e50`，v0.22.28 tag不存在；按独立patch+1实施。

**R43 implementation review / 静态门（2026-09-15 CST）:** 生成器、生成配置、结构成员提取、两项新守卫与索引已实现，六版本面已由工具链联动到0.22.28（Cargo.lock只有root package version变化）。Codex自审及独立Claude Code CLI源码审查均PASS，字面 `VERDICT: PASS`，P0/P1 none；审查明确不认证未来测试/gh/D。`sh -n`、生成器byte equality、209/7摘要、fmt check、diff check和install.sh syntax均通过；限定 `cargo clippy --test compat_serial_registry --test e2e_mcp_flow --test compat_version_surface_sync -- -D warnings` 已退出0（54.57s），只有既有第三方proc-macro-error2 future-incompat notice。当前以原绝对路径source双env、build28/test16、skip-web和owned target/config scopes预编译完整默认集合。编译前source组合hash为 `9fe7ca97f17b86ca4a5a2f2eb1b4bd5f90736db107762dae67c7018543033f06`；metadata唯一package为owned worktree的libra0.22.28。实际20项guard、二例MCP和最终完整全量尚未执行，不把源码审查/预编译计作测试通过。

**R03 首次预编译异常 / 有界诊断:** 首次nextest list退出101，日志只有cargo `test --no-run ... --all` 子程序exited，没有Rust错误或明确exit/signal；binary metadata为0 bytes，没有运行任何测试，不计通过。退出前rustc持续活跃，观察时可用内存55–68GiB；kernel与systemd-oomd近期日志未提供OOM证据，不能据此认定内存耗尽、死锁或某个外部发送者。余下子进程随后均退出，owned进程组已空，源码hash仍为上述值。保留原log与空metadata，按完全相同build28/test16范围重新nextest list，仅增加verbose诊断，写独立 `r03-retry-*` 证据；不降低并行度、不跳过target或把重新编译当成测试重试通过。

**R03 第二次预编译与诊断继续:** `r03-retry-full-compile.log` 显示 `compat_previous_branch_shortcut` 的rustc明确 `signal: 9, SIGKILL: kill`，`code_tool_acl_test` 的clang报告链接器 `Killed`，最终cargo/nextest退出101；仍未执行测试。自身及user.slice各级cgroup的memory.events均oom=0/oom_kill=0，memory.high/max无限制；kernel、systemd-oomd及已有audit日志无对应终止证据。不能把SIGKILL发送者或内存根因当成已确认，也未修改系统守护程序。已非阻塞询问用户是否存在其它编译清理任务，并以原env build28/test16启动本卡serial/version两目标诊断，完整全量仍必须另取得，不由focused结果代替；源码与时限策略不变。

**R03 build16/test16完整预编译与focused终态:** 用户调整参数后，完整 `nextest list --all` 预编译成功（4m54s），source前后hash均 `9fe7ca97f17b86ca4a5a2f2eb1b4bd5f90736db107762dae67c7018543033f06`。新metadata唯一package为owned worktree的libra0.22.28，230个binary路径全部在owned target且可执行；无过滤listing实际10049 total/10046 non-ignored/3原有ignored、non-ignored unmatched=0。与.27逐一比较binary+test+ignored身份，删除0，仅新增本卡两项serial guard；MCP filter实际恰好命中两项指定非ignored案例。`r03-j16-binaries.json`、`r03-j16-cargo-metadata.json`、`r03-j16-all-tests.json` SHA-256依次为 `fd40f3efe9f48411cd005d9295e79871a5f063e8c6f91f16e13b7a363abb6218`、`02e6e59b5aa4f0bd4e4cb0975c870b70a5e1d3cb8a62aab3c5f17cf89ba8df9d`、`1ea689506406a9177336ab39294111c3194f4b09a15cc1f297be0163fdc53c5b`。复用此次metadata的focused run `773dd288-2863-479f-aa81-aeaf8f79e1d8` 已22/22通过、24.603s、无retry：serial target20、version target2；228未选binaries不计作全量。本次仍仅传通用600s外部tool config，1200s来自仓库override。接续同一冻结source的无filter完整10046-case执行，结果未取得前不认证C或发布.28。

**R03 最终完整执行启动:** `390fc1aa-a22a-4bb3-96e2-63a9fe27a5b5` 实际Starting10046 tests/230 binaries/3原有skipped，无任何测试filter。运行前sourcehash不变，实际runner环境核对为CARGO_BUILD_JOBS=16、NEXTEST_TEST_THREADS=16、skip-web1与owned target/global/system路径；通用外部600s配置不含MCP特例。日志 `r03-j16-full-suite.log`，当前running，不将阶段性PASS数、22-case focused或.27结果当作本次终态。

**R03 最终完整C测试终态（2026-09-15 CST）:** `390fc1aa-a22a-4bb3-96e2-63a9fe27a5b5` 已退出0，**10046/10046 passed、3 slow、0失败/重试、3原有skipped**，230 binaries、执行1446.661s。两项MCP首次分别245.575s和483.697s通过；第二项先等同一Cargo锁，再完成活跃编译/端口释放断言，两项子Cargo实际继承build16/test16/skip-web1/incremental0，不是热改旧轮次。完整运行后source组合hash仍为 `9fe7ca97f17b86ca4a5a2f2eb1b4bd5f90736db107762dae67c7018543033f06`，三个metadata hash与启动时相同。JUnit根tests=10046/failures=0/errors=0/time1446.661，无failure/error/rerun/flaky节点；已复制并逐字节校验私有 `r03-final-junit.xml`，SHA-256 `7f460923356ff4fc7b6edb5b3a3b750921bbe3b5b445d65b4aa853b74e8ffb47`。post-run fmt、diff、shell syntax、generator byte equality均通过；推送前只读确认main仍0ef805fa、v0.22.28 tag不存在。先前28-job编译SIGKILL及按用户要求中断的focused运行独立保留，不能因本次全绿编造它们的根因；本卡代码及全部C测试已满足，signed+DCO/push/gh和D随后按实际结果记录。

**R03 C提交/gh交接:** 11-file implementation commit `8fca47ebf19fe46e1bd7f3a5acbf52859179a1a1` 已SSH signed+DCO并推送main，签名头与Signed-off-by均核对；private test-scopes及原checkout忽略的 `.env.test` 未暂存/提交。只对精确SHA单次push轻量tag v0.22.28，remote tag已核对，然后执行 `gh release create v0.22.28 --verify-tag --target 8fca47ebf19fe46e1bd7f3a5acbf52859179a1a1 --notes-file /mnt/gitmono/tmp/libra-full-suite.tGFCUS/r03-release-notes.md`，不让发布命令重新创建tag。非draft [v0.22.28 Release](https://github.com/libra-tools/libra/releases/tag/v0.22.28) 于2026-09-14T16:48:14Z发布，notes概括精确二例20分钟、生成器/结构guard/文档及真实10046-case结果，不宣称MCP服务功能变化。当前仅观察到一条Release run [34870861067](https://github.com/libra-tools/libra/actions/runs/34870861067)，与main的Advanced CodeQL [34870771544](https://github.com/libra-tools/libra/actions/runs/34870771544) 均exact head8fca47ebf、in_progress；Code Quality34870770981已success，不替代Advanced。依用户指令异步推进后续验收，不把gh创建成功写成D成功。本卡仍remote-pending，整个goal尚未完成。

**Current evidence:** `tests/NEXTEST_GROUPS.sh:72-97` 的 `emit()` 是 `.config/nextest.toml` 的生成入口，直接改生成结果会被 `tests/compat/serial_registry.rs:1832-1851` 的 byte equality 守卫拒绝。该守卫 `:1893-1904` 当前按全文 `filter = 'binary(=` 前缀计数组成员，会把 timeout-only override 误当成 external member，因此必须按已解析的 TOML table 的 `test-group` 字段区分，不靠换引号或加括号避开扫描。现有 external 集合为209 fn/7 binary，`tests/INDEX.md:129` 的208为待同卡刷新旧说明。`tests/e2e_mcp_flow.rs` 二例均内嵌 cargo build；.27 全量首例431.994s通过，末例先等build锁后在旧600s timeout，重试355.310s通过。R40 新 tool config 已实际解析并精确选择二例，不代表仓库已持久化。

**Acceptance criteria:**

- [x] `e2e_mcp_flow::test_e2e_mcp_flow` 的仓库 nextest 单次终止上限为1200s。
- [x] `e2e_mcp_flow::test_web_only_sigterm_releases_ports` 的仓库 nextest 单次终止上限为1200s。
- [x] 两例的慢测试提示周期均保持120s。
- [x] 两例的退出宽限均保持10s。
- [x] 任何其它测试的 timeout 设置不因本卡改变。
- [x] 重新生成 `.config/nextest.toml` 后与已提交文件逐字节相同。
- [x] external 分组的真实成员集合仍精确等于 registry 推导的209 fn/7 binary。
- [x] 测试索引说明两项固定超时与生成器的归属。

**Verification（5 gates）:** 所有 Cargo/nextest 调用先 source `/mnt/gitmono/libra/.env.test` 和 `/mnt/gitmono/libra/.env.live-test`，使用 owned target/config fixture、`LIBRA_SKIP_WEB_BUILD=1 CARGO_INCREMENTAL=0`；build/test并行度继承文件，不覆写为4。下面命令可在本 owned worktree 运行，必要时加绝对 `--target-dir` 与既有 tool config，不能加 ignored/live feature。

本卡自足执行前缀（从 `/mnt/gitmono/tmp/libra-mig-r02.MW2s7B` 运行）：

```bash
set -o pipefail
source /mnt/gitmono/libra/.env.test
source /mnt/gitmono/libra/.env.live-test
umask 022
export LIBRA_SKIP_WEB_BUILD=1
export CARGO_INCREMENTAL="${CARGO_INCREMENTAL:-0}"
export CARGO_MANIFEST_DIR=/mnt/gitmono/tmp/libra-mig-r02.MW2s7B
export CARGO_TARGET_DIR=/mnt/gitmono/tmp/libra-mig-r02.MW2s7B/target
export LIBRA_CONFIG_GLOBAL_DB=/mnt/gitmono/tmp/libra-mig-r02.MW2s7B/test-scopes/global.db
export LIBRA_CONFIG_SYSTEM_DB=/mnt/gitmono/tmp/libra-mig-r02.MW2s7B/test-scopes/system.db
```

各 nextest `run` 加 `--tool-config-file libra-full-suite:/mnt/gitmono/tmp/libra-full-suite.tGFCUS/nextest-timeouts.toml --no-pager --color never --failure-output never`：这里只保留通用600s默认，二例1200s必须来自仓库的per-test override；本卡不传R40的 `nextest-timeouts-mcp20m.toml`，避免外部二例配置掩盖仓库规则缺失。该固定仓库请求覆盖R40仅针对本卡未来命令的外部二例file路径，旧文件/旧运行证据保留不改。使用新metadata复用时仍执行相同前缀/参数，不传build/test线程覆盖；实际两个MCP结果可由完整全量提供。

- [x] `sh -n tests/NEXTEST_GROUPS.sh`。
- [x] `sh tests/NEXTEST_GROUPS.sh --stdout | cmp - .config/nextest.toml`（shell启用pipefail）。
- [x] `cargo nextest run --test compat_serial_registry --no-fail-fast --retries 2`；包含新增 `nextest_mcp_tests_have_twenty_minute_timeout`、`nextest_group_membership_ignores_timeout_only_overrides`，完整target不为0；保留原classifier/缺失/悬空/成员变异断言。
- [x] `cargo nextest list --test e2e_mcp_flow -E 'test(=test_e2e_mcp_flow) | test(=test_web_only_sigterm_releases_ports)' --message-format json`；实际恰好两个非ignored匹配，不以字符串相似代替 filter 解析。
- [x] `cargo nextest run --test e2e_mcp_flow --no-fail-fast --retries 2`；真实两例通过，固定repo配置生效；禁止用仅list代替运行。

**C quality gates / Full-suite trigger:** T-1（仓库runner配置/生成器）、T-5（用户要求）；最终 bump 状态必须完整默认 nextest 全绿。可用上述已登记两阶段方法先 `cargo nextest list --all --list-type binaries-only --message-format json` 得到本卡新metadata，再无任何filter地复用运行；前后 source hash、package/version、所有binary路径、完整listing/ignored清单须一致，失败时不用focused绿灯替代全量。推送前 fmt check、diff check、`cargo clippy --test compat_serial_registry --test e2e_mcp_flow --test compat_version_surface_sync -- -D warnings`、version守卫均须通过；全量与新二例执行可共享同一次真实运行证据，但不重复计数。继续不运行本地 all-target/all-feature Clippy、网站全量或本地 release build/install。

**Dependencies / Blocking:** `MIG-R02` 的 C 全量/版本提交/push/gh 已完成才可开始，本卡不等待其 D；任何未知 main/tag 漂移、真实配置路径回退或scope扩张必须先分析，不能覆盖其它分支。独立R41字面PASS前不写本卡生成器/配置/守卫源码。

**Implementation write set:** `tests/NEXTEST_GROUPS.sh`、`.config/nextest.toml`、`tests/compat/serial_registry.rs`、`tests/INDEX.md`、本计划。`tests/SERIAL_REGISTRY.tsv`、classifier、MCP两例测试体、production src、SQL、CI workflow均不变。成员提取使用现有 `toml` dependency 的结构解析，unknown external filter 或非字符串字段须明确失败，不能静默跳过；新增synthetic timeout-only/成员变异 counterexample，既有209/7与双向集合断言保留。

**Release write set / Version increment / Release boundary:** ER-08六版本面，Cargo.lock由工具链更新；当前v0.22.27，实际开工复核后独立patch+1（预期v0.22.28），`independent`，signed+DCO/push/内容化gh与D-MIG-STD均必需。

**Docs / compatibility:** 更新 `tests/INDEX.md` 的现有两目标说明及运行配置提示，无新target注册。无产品CLI、schema、公开API或稳定错误码变化，用户EN/zh命令文档、COMPATIBILITY、网站为N/A；release notes说明固定MCP时限、生成器/守卫和实际测试，不夸大为MCP功能变更。

**Rollback mode / Migration / Security / Performance:** `revert`（发布前一起撤销生成器、配置、守卫/文档）；发布后仅forward-fix。无数据迁移、真实配置值读取或远端测试副作用；沿用隔离fixture。二例1200s每次/10s grace与现有有界重试，其他测试timeout与serial不变，不改变用户build28/test16。无production hot-path成本。

**Deliverables:** `N/A`；实现随独立release交付。

**C/D coverage from / D post-push:** `self` / `D-MIG-STD`。

**Estimated scope / Granularity:** `S; type=implementation; axis=persistent bounded MCP test timeout; recovery=revert policy before release, forward-fix afterward; complete=yes; self-contained=yes; AC=8/8; VER=5/8; landing=2; prod-files=2 (conservative generator/config count); deps=MIG-R02 C; writeset=serial; release=independent; split-from=explicit user configuration request; exception=N/A`。

## 测试矩阵

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | role manifest、bootstrap/top-up allowlist、ScopedEnvVar global/system recovery | MIG-01 specified `db_migration_test config_db_fixture_` target；MIG-02 specified focused unit gate |
| 集成 | Repository/GlobalConfig/SystemConfig/Derived fresh/old/future open；global/system writer；read-only doctor；attested repair | MIG-02/MIG-03/MIG-05/MIG-06 specified `db_migration_test` and compat gates |
| 兼容 | true Config future blocks remote/cloud；repo-only receipt does not；human/JSON redaction；offline/local semantics | MIG-04 specified compat gates |
| 迁移 | role ledger idempotence、bootstrap topology、old reader oracle、atomic repair、unknown receipt refusal | MIG-02 role/ledger gates；MIG-04/MIG-06 old-reader and repair gates |
| 安全 | global/system override canary、canonical confirmation、lock recheck、backup validity、symlink/TOCTOU、unknown zero-write | MIG-01/MIG-05/MIG-06 gate specifications |
| 性能 | role inspect constant indexed queries；SQLite-consistent 100 MiB backup duration | MIG-02/MIG-06 focused benchmark/assertion |
| runner配置 | MCP二例1200s、generator byte equality、external成员结构解析与反例 | MIG-R03五门与最终bump后完整默认nextest |
| 计划收口 | 所有任务完成后的最新用户覆盖门 | fmt、静态契约守卫、各卡 focused targets；另依 2026-09-14 后续授权执行载入两个测试环境文件的默认全量 nextest，不增加 all-features/ignored/live feature targets、网站全量构建或 all-target-all-feature Clippy |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| MIG-00 | v2.4 GC-13 plus absent role-map doc | template, internal role-map doc | N/A，内部治理 | document existence/link checks |
| MIG-01 | direct env mutation plus system fallback | `src/utils/test.rs`, config test module | N/A，非用户面 | global/system sandbox gates |
| MIG-02 | generic inspect/upgrade/create/bootstrap/top-up | `src/internal/db.rs`, `migration.rs`, role DDL | developer migration design | role matrix plus old reader gate |
| MIG-03 | config command actual writer plus cascade/inspector | command/config/client-storage | N/A，internal routing | global/system writer gates |
| MIG-04 | CLI future guard and current compat fixture | CLI/client-storage | config + remote/cloud EN+zh+web, errors, compatibility | Config policy gates |
| MIG-05 | known Repository convergence receipt with unattested writer; unsafe manual repair risk | config doctor command | config EN+zh+web, errors, compatibility | read-only/provenance/redaction gates |
| MIG-06 | attested receipt conversion risk | config command plus DB repair API | repair/recovery EN+zh+web, errors, compatibility | atomic/backup/old-reader gates |
| MIG-R02 | 固定源码全量的 18 个 Repository-context 假定失败 | cfg(test) scope discrimination 与 isolated merge fixtures | 内部 fixture/role-map；用户 surface N/A | ownership/merge/role/serial guards，最终 bump 后完整 nextest |
| MIG-R03 | 用户要求R40二例1200s成为固定仓库配置 | NEXTEST_GROUPS生成器、nextest配置、serial守卫 | tests/INDEX与配置注释；产品/网站N/A | generator、完整serial target、精确MCP listing/运行与最终全量 |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | GC-13 和 role-map 已审核；仅治理 docs 变更 | MIG-00 structure/link evidence；Claude plan PASS | `revert` 文档；不改变 runtime |
| M1 | sandbox prevents real global/system fallback and restores env | MIG-01 release + full-suite/D evidence | revert helper/test change；任何 canary failure停止后续卡 |
| M2 | all roles select schema manifest/ledger/bootstrap/top-up correctly | MIG-02 role matrix, old-reader test, release evidence | forward-fix; preserve generic receipt |
| M3 | config writer never invokes Repository generic wrapper | MIG-03 writer gates, release evidence | revert routing; do not rewrite DB |
| M4 | true Config future safe and repo-only receipt nonblocking | MIG-04 compat/docs/release evidence | forward-fix; never weaken true-future fail-closed |
| M5 | doctor classifies observed receipt as ineligible without writes | MIG-05 gate family, release D evidence | revert doctor command/docs; no DB recovery required |
| M6 | repair only changes attested fixture atomically | MIG-06 old-reader, backup/failure gates, release D evidence | verified SQLite-consistent backup restoration only by explicit disaster procedure; otherwise forward-fix |
| M7 | 验收代码不再依赖隐式 Repository 上下文，完整全量通过 | MIG-R02 focused、最终 nextest、独立 patch C/D | 发布前撤销测试适配；发布后 forward-fix |
| M8 | 仓库固定MCP1200s与serial成员守卫同步，全量通过 | MIG-R03 runner/guard/MCP/full、独立patch C/D | 发布前整体撤销policy；发布后forward-fix |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| fixture setup 后、test panic 前 | tempdir 及已恢复/Drop-managed env | Drop guard + tempdir cleanup；canary asserts no real global or system path access | leaked override 导致后续 test 回落真实 config DB |
| role ledger up 前/中 transaction | 无，或未提交 temporary SQLite state | transaction rollback；reopen verifies old receipt | partial ledger/partial role receipt |
| doctor read-only | 无 DB/file write | compare DB fingerprint and backup directory before/after | doctor triggers migration、backup 或 config value readout |
| SQLite-consistent backup 前/中 | 主库不动；未验证 artifact 不可标成功 | delete/mark failed artifact; require fresh backup retry | byte copy of a journal-inconsistent DB is accepted as backup |
| confirmed repair transaction | re-opened backup，主库旧或新的一致态 | rollback/restore verified SQLite-consistent backup via documented explicit procedure | unknown receipt 删除、generic future 误放行、secret 泄露 |
| remote release D failure | 已发布 prior release 或 new tag/artifact | 新 patch 前滚并重跑 D evidence | 回滚 tag/artifact 或声称未验证发布已完成 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 将真实 Config future 当作 repo-only receipt 忽略 | 高：可能错误使用不兼容凭据/存储配置 | manifest allowlist、true Config future compat fixture、GC-MIG-04 | MIG-02/MIG-04 |
| config bootstrap/top-up 再次污染 config DB | 高：fresh install 重现 blocking receipt | role-scoped create/upgrade/top-up plus global/system writer gates | MIG-02/MIG-03 |
| 未归因 writer 在 repair 后重新写 receipt | 高：恢复后再次阻断 | writer routing prevention；doctor provenance/mtime；repair lock recheck | MIG-03/MIG-05/MIG-06 |
| unknown receipt repair 删除 state | 高：破坏 user DB / old-binary safety assumption | DEP-MIG-02 attestation、fingerprint、SQLite-consistent backup、unknown zero-write | MIG-06 |
| 测试环境泄露至真实 HOME or system path | 高：改写个人 config/credentials | shared sandbox, serial(env), env restore canary | MIG-01 |
| role API 被 new callsite bypass | 中高：问题复发 | Repository-only compatibility wrapper plus config source guard | MIG-02/MIG-03 |
| repair command path or symlink misuse | 高：修复非预期文件 | canonical confirmation、TOCTOU test、fail-closed | MIG-06 |
| 文档不同步导致用户误用 offline/manual SQLite | 中：误导恢复、数据风险 | same-card EN/zh/web/error-code docs and review | MIG-04/MIG-05/MIG-06 |
| timeout-only filter 被误判external成员或扩大到其它测试 | 中：串行守卫误报/漏报或时限无界扩张 | TOML结构解析test-group、原209/7双向集合不变、精确二例filter/计时断言、真实全量 | MIG-R03 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| role schema inspect | O(1) indexed max/read in selected ledger | O(commands) | 每 invocation 至多一次；不扫描 config rows | MIG-02 query-count/1,000-row fixture |
| config lookup | 常量次 read-only queries | O(keys) | 不在 key lookup 中运行 DDL/migration | MIG-03 focused fixture |
| doctor classification | O(receipt count) only | O(migrations) | receipt 数量受 manifest 有界；zero config value enumeration | MIG-05 fixture |
| backup/repair | O(DB file bytes) SQLite-consistent backup + O(receipts) transaction | explicit operator-only | 100 MiB fixture <=10s on documented CI runner；backup copy does not hold write transaction | MIG-06 benchmark/assertion |

## 兼容与文档收口

**R36 / ER-06a 遗留开发说明收口（待审）:** 最终逐文件核对发现 `docs/development/commands/{pull,push,fetch,clone,cloud}.md` 仍在 P0-12 段把 CLI preflight 描述为只调用 `inspect_global_config_schema_future`；当前 `src/cli.rs::enforce_global_config_schema_policy` 已调用 `inspect_configuration_schema_issues`，按 GlobalConfig/SystemConfig role 判定，不能把这五处记为 N/A。依本计划原有 ER-06a 文档义务，限定补正这五个既有段落并链接已有 config design/role-map/用户文档；不新增产品行为、不改生产源码、测试、版本面或 MIG-R02 的实现写集。这是 MIG-04 已发布行为的开发文档遗漏前滚与计划级文档收口，不新设 implementation 卡，不再 bump，最终以独立 signed+DCO docs-only commit 随计划事实闭包推送；不改写原 release tag 或其 D 记录。最终提交须通过 diff/fmt、五个链接目标存在性与旧 helper 描述清除检查；已有用户 EN/zh、compat/error/index 与 website 证据保留。R35 冻结的源文件集合和最终全量入口、计数、600s 上限均不变；文档补正不冒充最终全量通过。Codex 自审认定为原文档收口义务，独立 PASS 前不写这五个文件。

**R36 closure:** Codex 与 Claude Code 独立规范审查均 PASS，字面 `VERDICT: PASS`，P0/P1 none。五处现已说明双作用域、System 不受 Global credential bypass 豁免、role manifest 与未知 receipt 拒绝、offline 边界；保留原 diagnostic/redaction 与回归 target，链接已有设计/用户文档，并明确旧 Global helper 仍用于 cascade，未声称该函数被删除。实际提交与最终文档静态检查待收口；不认证尚未完成的全量。

**R37 / 全量后段归因与证据锚点收口（R38 缩窄复审待通过）:** 原始固定全量后段又出现 7 个 `compat_ledger_schema` 失败与 1 个 `compat_mail_am_basic::empty_file_umask_and_symlink_sections_apply` 失败，不能继续写成只有 18 项。单项重现证实前七项共享同一根因：MIG-04..06 在 `COMPATIBILITY.md` 命令表之前增加说明，原 `update-index` anchor `:206` 已移动到 `:242`（相关命令表行统一 +36），而既有 ledger/registry 的数字引用未同步。限定按实际命令／surface 原文校验后，只前滚更新 `docs/development/gap/surface-registry.tsv`、`tests/compat-ledger/t4/` 中 12 个含该引用的真实 TOML与工具生成的 `SURFACES.lock` 的证据位置；不改 scenario ID/category/status/reason/owner/review_date、测试映射、negative fixtures、schema validator 或 generator 逻辑，不改 `COMPATIBILITY.md` 来迁就旧数字。此为既有兼容文档及证据同步遗漏，与 R36 一并独立 docs/evidence-only commit 收口，不扩大 MIG-R02 的生产／测试代码行为轴、不新增 patch。必须实际运行完整 `compat_ledger_schema` 43 cases、重新生成 lock 并验证零漂移、核对非 anchor 字段字节不变；不得删除 ledger 行或弱化断言。变更会打破 R35 的旧 source hash，因此不得直接复用旧冻结证明：完成修正后重新执行完整 `nextest list --all` 预编译，重取 source/metadata hashes 与无过滤 230 binaries / 10047 total / 3 ignored / 10044 non-ignored listing，再据 R35 同等门运行最终全量。

**R37 / umask 执行环境补正:** `/proc/3901449/status` 实测原全量父进程为 `0077`；新 shell source 两个既有 env 文件前后均为 `0022`，故不是 env-file 要求。相同基线 binary 的该 mail 用例在 task-owned fixture 下 `umask 077` 失败（100600、3.12s），`umask 022` 通过（3.40s），直接对应测试 `:1678-1690` 明定的 022/002 前提。原始失败保留，不改生产文件权限算法或测试断言；最终 coordinator 在 source 两个文件后明确 `umask 022`，保留私有 task-owned 日志／配置目录、配置 override 及 secret-free 输出。此补正仅恢复普通测试 shell 的既有 umask，不改用户全局设置，不使用 ignore/filter 规避。若完整基线继续发现新根因则另行分析；R37 独立 PASS 前不改上述 ledger/registry 文件。

**R37 review / R38 冻结快照边界补正（待审）:** R37 独立审查已返回字面 `VERDICT: PASS`、P0/P1 none，并提醒不可对 snapshot 盲目 +36；Codex 随后读取 `t4/PRECHECK.sh:277-310` / `CT302_GATE.sh:100-142`，确认 `DIRECT_SNAPSHOT.tsv` 是 CT3-06 所有的已提交冻结消费输入，不是当前 live ledger mirror。R37 将其作为可更新文档的范围假定错误，现从写集明确移除，原文件与其它 frozen inputs/历史计划一字不改；独立复审完成前全部 ledger/registry 仍未修改。允许集缩为 14 files（12 live TOMLs、1 live surface registry、1 generated lock）。Codex 已逐项比较 v0.22.19 与当前 `COMPATIBILITY.md` 的所有候选引用原文，live anchors 在 +36 位置字节一致；snapshot 中更旧的数字不作为当前命令证据，不重写其历史归属。R38 只复审这个更严格的所有权边界，R37 43-case/full precompile/hash/count/umask 约束原样保留；最终运行另显式 `CARGO_BUILD_JOBS=4`，避免子测试的内嵌 Cargo 调用继承 env 文件中的更高并行度。

**R38 closure / live evidence 修正:** 独立复审及同会话 marker 补正最终返回字面 `VERDICT: PASS`，P0/P1 none；确认 CT3-06 frozen snapshot 只读、14-file live 写集与原验收门保持完整。实际已仅更新 12 TOML 与 registry 的 59 个数字引用，并由既有 `SURFACES.gen` 生成 lock 的 19 个引用；14 files 去除 anchor 数字后的每一个字节均与 HEAD 一致，generator 再运行零漂移，`DIRECT_SNAPSHOT.tsv` 无 diff。新的 src/tests/sql/build/config/六版本面组合 hash 为 `cde8a659dde307a16b0735e6d1ff5266d38fc175df43d21945a0b75c359f7a60`。旧 R35 metadata/log 保留；接下来重新完整预编译并写入独立 `r02-r38-*` metadata/log，不用旧 hash 冒充新证据。R36/R38 的 docs/evidence files 在最终独立文档闭包 commit 提交，不进入 MIG-R02 的 11-file implementation commit。

- [x] `COMPATIBILITY.md`：MIG-04/MIG-05/MIG-06 的 config policy/doctor/repair surface 已同步，或记录 N/A。
- [x] `docs/commands/config.md` + `zh-CN/config.md`：MIG-04/MIG-05/MIG-06 已同步。
- [x] `docs/commands/{pull,push,fetch,clone,cloud}.md` + zh-CN：MIG-04 的 true-future/upgrade guidance 已同步。
- [x] `docs/development/commands/`：现场判定是否存在 config/remote behavior design 文档并同步，或记录 N/A。
- [x] `docs/error-codes.md`：`LBR-CONFIG-001` contract 及 repair-related stable code（如新增）已同步。
- [x] `tests/INDEX.md`、`Cargo.toml` `[[test]]`、`tests/compat/README.md`：新增/描述变更 target 已同步，或记录 N/A。
- [x] `../libra-backend`：每次写入前已记录单一 VCS 类型、`cf` 分支和对应网站文件；阻塞时不绕过。
- [x] `plan-long.md`：N/A，本计划不改变长期路线图。

**文档逐文件核对（2026-09-14）:** 已读 config EN/zh、五组 remote/cloud EN/zh、error/compat 现有 role/doctor/repair 章节；config 开发设计 `:18-19` 已描述唯一 barrier writer、确认/备份/事务与隔离，五个 remote 开发段落由 R36 补齐。INDEX 中 compat/global、db migration、old-reader 与 Cargo 显式 target、compat README `global_config_schema_future_test.rs` 行一致，无新 target 遗漏。网站本次只读复核仍为唯一 `.git`、无 `.libra`、clean `cf`，HEAD 与远端 cf 均为 `6f6d80123add52a744c480ac9c7410c8acbe71b1`（其前序 `0f87fcc...` 的 repair 文档及当前 minimum-version 更正）；原生成 248 docs/search 与 focused Vitest 证据见 MIG-06，不运行新网站全量。MIG-R02 没有新用户命令行为，网站不需追加内容。R38 当前完整 ledger binary 43/43 通过（0.53s），fmt/diff/五组链接存在性与 generated lock 零漂移均通过；19 个 docs/evidence 工作树修改仍待独立 signed+DCO 提交，以上勾选只记录内容同步，不表示发布或全量已完成。

## Codex review log

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R1 | `plan-20260910.md`、`plan-template.md` | FAIL | 与 Claude R1 一致：GC-13 锚点、DAG、发布者、G-03 计数；doctor/repair 拆分 | N/A | 2026-09-10 当前源码/模板复核 |
| R2 | `plan-20260910.md`、`plan-template.md` | FAIL | 与 Claude R2 一致：实际 writer/bootstraps、producer attribution、backup、system isolation、D group、dependency/waiver closure；已改为 MIG-03..06 与 GC-13 v2.4 contract | P2 逐项纳入本轮结构修订 | Claude Code R2 output；R3 待复审 |
| R3 | `plan-20260910.md`、`plan-template.md` | FAIL | 0801 known convergence fact；MIG-03/MIG-04 policy window；old-reader and bootstrap barriers；doctor opening semantics；oracle gating；fault injection | P0/P1 已纳入 R4 修订；P2 记录为后续 review input | Claude Code R3 output；R4 待复审 |
| R4 | `plan-20260910.md`、`plan-template.md` | FAIL | barrier ownership、migration contract、forward-only/versioning、DEP blocking、R4 log | P0/P1 已纳入 R5 修订 | Claude Code R4 output；R5 待复审 |
| R5 | `plan-20260910.md`、`plan-template.md` | FAIL | barrier owner/callsite 未落到具体函数；MIG-04 卡内缺 DEP-MIG-01 blocked/DEFER 判据；整体 compat 重跑计数口径未说明 | 已补 `write_configuration_barrier(...)` 唯一 writer、single-writer gate、卡内 blocking condition、`VER=12` 计数说明 | Claude Code R5 output；R6 待复审 |
| R6 | `plan-20260910.md`、`plan-template.md` | FAIL | 同上三项仍需落到具体 symbol、卡内 blocked 判据和明确计数口径 | 已补 concrete writer symbol、single-writer gate、Blocking condition 与 blanket rerun 计数说明；R7 待复审 | Claude Code R6 output；R7 待复审 |
| R7 | `plan-20260910.md`、`plan-template.md` | FAIL | MIG-06 缺卡内自足的 DEP-MIG-01/02/03 blocked/DEFER 判据；P2 bootstrap/error 源码锚点偏移 | 已补 MIG-06 Blocking condition、锚点修正和旧 reader 命令环境前缀；R8 待复审 | Claude Code R7 output；R8 待复审 |
| R8 | `plan-20260910.md`、`plan-template.md` | FAIL | MIG-04 与 MIG-05 缺卡内自足的 DEP-MIG-03 blocked/停止判据 | 已补两卡 `Blocking condition`，明确后端 `cf` 不可确认时暂停网站写入/发布；R9 待复审 | Claude Code R8 output；R9 待复审 |
| R9 | `plan-20260910.md`、`plan-template.md` | PASS | 未发现未关闭 P0/P1；R8 的 MIG-04/MIG-05/MIG-06 blocking、锚点、计数与文档闭环均通过 | N/A | Claude Code R9 output；2026-09-10 |
| R10 | MIG-02 contract、2026-09-14 用户执行覆盖 | PASS | 单一 role/ledger writer 轴、8 AC/6 gates、隔离 fixture、事务回滚、Repository wrapper 与 MIG-03 边界一致；无 P0/P1 | 三项 plan P2 已按修订历史关闭，复审待 R11 | 2026-09-14 Codex source/plan 自审与 Claude Code R10 output |
| R11 | R10 P2 closure 与运行 build manifest 基线 | PASS | R10 三项均关闭；无 P0/P1；支持判定不替代 repair 资格，MIG-02 边界保持不变 | template header 与 GAP-MIG-04 的两项标签漂移已就地修正 | 2026-09-14 Codex 自审与 Claude Code R11 output |
| R12 | MIG-R01 独立恢复卡、发布次序与 D carryforward | PASS | 一行 portability fix、独立写集、3 AC/2 gates 与 S scope；无 P0/P1 | Windows Build Binary 单独记证；source guard 改为忽略空白；不把 waived full-suite 记为通过 | 2026-09-14 Codex plan/source 自审与 Claude Code R12 output |
| R13 | MIG-R01 lint-only fixture 清理与远端 docs 快转 | PASS | 三处 `&&Path` → `&Path`，无 production 行为扩张；暂存范围已核对，MIG-02 implementation 保留未提交 | 无新增 P2；最终门结果记录后才提交 | 2026-09-14 Codex diff 自审与 Claude Code R13 output |
| R14 | MIG-03 routing refinement、四 production 文件/五门与用户覆盖 | PASS | 无未关闭 P0/P1；角色专用 API、过渡 fence 与 MIG-04 policy/barrier 边界一致 | cache/source-guard/错误上下文建议已纳入卡内；HEAD 归因用 git show 更正 | 2026-09-14 Codex source/plan 自审与 Claude Code R14 输出 |
| R15 | MIG-04 concrete barrier / classification contract | PASS | 无 P0/P1；唯一 writer、共享事务、未知 receipt 有界判定、old-reader 与网站依赖闭包一致 | 刷新行号，15 AC 如实拆分，EX review 与既有锁原语同步 | 2026-09-14 Codex plan/source 自审与 Claude Code R15 |

## Claude Code review log

MIG-05 执行期：Codex source/plan 自审 PASS；R17 条件与 R18 closure 详见卡内，三项 P1 已关闭。

Claude Code 审查提示必须要求 source-grounded、high-recall review，且最终响应显式含 `VERDICT: PASS` 或 `VERDICT: FAIL`。每个 FAIL 的 P0/P1 必须在计划内修正并重新审查。

R17（MIG-05 plan/source）300s 调查超时不计 PASS；同会话禁用工具继续后返回 `VERDICT: PASS` 但附三项 P1 条件。R18 对已写入计划的三项闭包返回 `VERDICT: PASS`、remaining P0/P1 none；两轮由 Claude Code CLI 的既有配置模型 `glm-5-3-flash` 完成，不宣称 Anthropic 模型，也不替代最终实现审查。

| Round | Scope | Verdict | Findings / disposition | Evidence |
|---|---|---|---|---|
| R1 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P1-1 GC-13 anchor；P1-2 DAG；P1-3 发布者；P1-4 G-03 计数。全部在 2026-09-10 修订关闭，R2 待复审 | Claude Code read-only review，2026-09-10 |
| R2 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P0 actual config writer/bootstraps；P1 role contract、current `2026090801` ineligible、backup/system isolation/template/D group/MIG-00 baseline；P2 dependency/split/test-command closure。全部纳入 2026-09-10 R3 修订，R3 待复审 | Claude Code read-only review，2026-09-10 |
| R3 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P0 0801 known convergence evidence and policy window；P1 legacy-reader barrier、config bootstrap, unattested receipt, doctor read-only open, oracle layer, fault injection；P2 audit/reuse/performance/window/anchor details。P0/P1 已纳入 2026-09-10 R4 修订，R4 待复审 | Claude Code read-only review，2026-09-10 |
| R4 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P1 barrier migration ownership, forward-only versioning, DEP blocking and review-history closure; P2 gate block/audit/prefix notes. P1 已纳入 2026-09-10 R5 修订，R5 待复审 | Claude Code read-only review，2026-09-10 |
| R5 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P1 owner/callsite lacked concrete symbol and single-writer gate; P1 MIG-04 lacked self-contained DEP-MIG-01 blocked/DEFER rule; P2 blanket compat rerun was not explicitly excluded from `VER` count. 已补唯一 writer symbol/gate、blocking condition、count note；R6 待复审 | Claude Code read-only review，2026-09-10 |
| R6 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P1 barrier owner/callsite still lacked concrete symbol and single-writer gate; P1 MIG-04 still lacked self-contained DEP-MIG-01 blocked/DEFER rule; P2 blanket compat rerun count note. 已补 `write_configuration_barrier(...)`、single-writer gate、Blocking condition、`VER=12` count note；R7 待复审 | Claude Code read-only review，2026-09-10 |
| R7 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P1 MIG-06 lacked self-contained blocked/DEFER rule for DEP-MIG-01/02/03; P2 bootstrap/error source anchors were imprecise. 已补 MIG-06 Blocking condition、`824-859`/`error.rs:365` anchors and `.env.test` oracle prefix；R8 待复审 | Claude Code read-only review，2026-09-10 |
| R8 | `plan-20260910.md`、`plan-template.md` | `VERDICT: FAIL` | P1 MIG-04/MIG-05 lacked self-contained DEP-MIG-03 blocked/stop rule. 已补两卡 `Blocking condition` 与网站写入/发布停止策略；R9 待复审 | Claude Code read-only review，2026-09-10 |
| R9 | `plan-20260910.md`、`plan-template.md` | `VERDICT: PASS` | 完整只读复核确认 R8 修复无回归；未发现未关闭 P0/P1，MIG-04/05/06 依赖阻断、门族计数、迁移安全和 ER-06a 文档闭环均通过 | Claude Code read-only review，2026-09-10 |
| R10 | MIG-02 contract、2026-09-14 用户执行覆盖 | `VERDICT: PASS` | 无 P0/P1；P2 为过时 current-build 前提、未就地标注的旧等待句、old-reader oracle 归属；三项已修正，R11 复审待记录。本轮仅审计划，不替代实现审查 | Claude Code read-only review（Read/Grep/Glob），2026-09-14；输出含字面 verdict |
| R11 | R10 P2 closure 与当前 manifest 事实 | `VERDICT: PASS` | 三项 R10 P2 均确认关闭；无 P0/P1；新增两个非规范性标签建议已修正。本轮不包含其结束时新登记的 MIG-R01，恢复卡另审 R12 | Claude Code read-only review，2026-09-14；输出含字面 verdict |
| R12 | MIG-R01 独立 release recovery | `VERDICT: PASS` | 无 P0/P1；P2 为 Windows job 取证须明确、source-text guard 的空白脆弱性、全量豁免记账。前后两项纳入 D/收口记录，中项以 whitespace-normalized guard 修正；不扩大 copy/delete 行为轴 | Claude Code read-only review，2026-09-14；输出含字面 verdict |
| R13 | MIG-R01 三处测试 lint-only 清理与写集闭包 | `VERDICT: PASS` | 无阻断项；仅要求提交前记录 post-lint scoped clippy、测试与版本门的实际结果。远端 merge 的 docs-only 事实由 Codex 的 `git diff HEAD..origin/main` 与 ff 输出补证 | Claude Code read-only review，2026-09-14；输出含字面 verdict |
| R14 | MIG-03 explicit-role routing 计划 | `VERDICT: PASS` | 无 P0/P1；P2 为 cache gate 归属、guard 扫描范围、工作树/HEAD 漂移归因、错误上下文边界。前三项澄清/更正见卡内，role-map 同步；不替代最终实现审查 | Claude Code Read/Grep/Glob 只读审查，2026-09-14；输出含字面 verdict |
| R15 | MIG-04 sentinel、classification 与 mutation transaction 计划 | `VERDICT: PASS` | 无 P0/P1；P2 为过时 CLI 行号、bundled AC 实为 15、EX review reference、应复用现有 writer-lock 原语；已逐项关闭，不改变行为轴 | Claude Code Read/Grep/Glob 只读审查，2026-09-14；生产实现前返回字面 verdict |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-MIG-01 | 对 `2026090801` 或任何无法取得 producer attestation、fixed old-reader oracle、allowlist 或 fingerprint 的真实 global DB 做 mutating repair；同时暂停依赖该 oracle 的 MIG-04 legacy-reader barrier gate | manifest 已支持某 ID 不足以证明 producer/data safety 或旧 reader 的 fail-closed 窗口；证据不完整仍 fail-closed | DEP-MIG-01/02 都可复验后恢复执行；若已正式延后则以新日期计划承接；在此之前 MIG-04/MIG-06 不得宣告 complete | 依赖恢复前保留 prevention、doctor 和 upgrade guidance；正式延后项由新日期计划承接 |
| DEFER-MIG-02 | Derived/publish D1 schema 的全面 role framework 迁移 | 与本次 config contamination 不是同一持久化边界 | Derived DB 开始复用 runtime migration API | 独立设计/计划 |

## 最终事实核对（2026-09-15 CST）

**R48 最终事实复核 / 交付边界:** 独立复核退出0、字面 `VERDICT: PASS`，无P0/P1；确认最终全量与D实证、用户不等待指令和历史失败保留一致。其P2已通过明确标注“启动时记录（历史）”和“R03 C证据”关闭，两次10046-case运行各自的JUnit hash与结果不混用。最终Codex与独立review门均PASS。本次闭包仅提交本计划文档，保持v0.22.28六版本面及全部受测源码不变；签名/DCO、精准单文件暂存与远端ref须以本提交的实际Git对象/推送结果验证，不另建release或修改既有tag。

**最终本机全量终态 / Codex审计:** `51e1b567-8528-4577-9947-cdf15960873a` 已退出0，10046/10046 passed、0失败/重试、10 slow、3原有ignored，实际2062.399s。两项MCP首次234.862s和493.401s通过；等待构建锁及活跃编译过程已实际观察，两子Cargo均继承build16/test16/skip-web1/incremental0，nextest及上述两项MCP/子Cargo已退出。其余8项slow是merge/rename案例，120.348–329.897s，均在600s上限内通过，无timeout。JUnit实际10046个唯一testcase，failure/error/rerun/flaky/skipped节点均0（3原有ignored未进入JUnit），timestamp为2026-09-15T01:05:18.239+08:00；逐字节复制保存的私有 `final-closeout-j16-junit.xml` SHA-256为 `d92303ee1219e16f5b3962499f38d4ea06dda1ef4941b1076b2058547d86a4a9`。运行后source组合hash仍 `9fe7ca97f17b86ca4a5a2f2eb1b4bd5f90736db107762dae67c7018543033f06`，两个metadata与listing的三个pin均与启动时相同。post-run fmt、diff check、install/generator shell syntax和generator byte equality通过；scoped Clippy 56.76s的受测源码未变。网站再次只读核对为clean Git cf，本地/远端仍 `6f6d80123add52a744c480ac9c7410c8acbe71b1`。Codex按十卡AC/具名门、专门oracle/性能证据、文档/版本/签名/DCO/tag/gh及各D逐项审计为PASS；最终写集仅本计划文件，不含private fixtures/logs、忽略的env、版本面或任何产品/测试/网站源码。最后的文档提交仍须精确暂存、signed+DCO及非强制推送，不能以本段代替实际Git结果。

**v0.22.28 最终D事实（收尾期间直接取得，未等待流程）:** 对两个既有run各执行一次15s有界查询，均立即返回completed/success、exact SHA `8fca47ebf19fe46e1bd7f3a5acbf52859179a1a1`。Release八job全部success：四平台build/upload `104066096997` / `104066097251` / `104066097287` / `104066097295`、Homebrew update `104080604166`、installers `104080605234`、manifest `104080605292`、Homebrew verify `104080659419`；Advanced两个job也均success。实际manifest于2026-09-14T17:31:11Z返回HTTP200，control_revision=20/version0.22.28/payload_digest=`9a481159a818c1162bf1adeb0cf894dd1772bdb3d96be2954db9df50b250ca35`。Homebrew于17:31:26Z实际安装 `/opt/homebrew/Cellar/libra/0.22.28`（4 files/96.1MB），不是脚本定义或warning/skip。四个v0.22.28公开R2 artifact（linux-amd64、linux-arm64、darwin-arm64、windows-amd64.exe）HEAD全部HTTP200。私有日志 `r03-final-manifest.log` / `r03-final-homebrew.log` 留作取证；其后的本机最终全量仍待独立终态，不把D成功替代测试通过。上文R46/R47及早期remote-pending是当时事实，现据实际结果更新，不恢复等待要求。

**R47 最终计划/源码范围复核:** 独立 Claude Code CLI 退出0、字面 `VERDICT: PASS`，P0/P1 none。复核当前用户覆盖、MIG-R03 本机done与独立remote-pending、DAG/发布序/修订登记、冻结source/metadata/listing与完成判据，确认没有新增源码/行为、门或版本，不将未完成的最终全量及未来文档提交记为通过。MIG-R02最后AC的漏勾已依据其实际10044-case JUnit、签名/DCO/tag/push、成功D及内部fixture/role-map文档补齐；十卡AC实际计数5/8/3/8/8/15/12/16/6/8逐一匹配声明，均已勾选。此复核是计划/源码范围门，不冒充测试终态或远端D验证；最终本机结果与提交另据真实输出关闭。

**R46 用户收尾覆盖审查:** Codex 核对最新用户指令的优先级与范围，Claude Code 独立复核退出0、字面 `VERDICT: PASS`、P0/P1 none；确认只改变远端等待/后置证据收集的阻塞性，不弱化源码、测试、迁移安全、签名/DCO或已发布版本要求。最终交付可按用户覆盖完成，但必须保留 v0.22.28 remote-pending/not verified，不能称为健康发布。两个本地 gh watch 均按用户要求 SIGINT 退出130，远端任务未取消；不是远端工作流失败。

**最终本机验收启动时记录（历史）:** 在原双 env 的 build16/test16、skip-web1/incremental0 与 owned target/global/system scopes 下，fmt check 通过，限定三target Clippy `-D warnings` 通过（56.76s；仅既有第三方 future-incompat notice）。源文件组合 hash 仍为 `9fe7ca97f17b86ca4a5a2f2eb1b4bd5f90736db107762dae67c7018543033f06`。重新无过滤 nextest list 得到230个可执行owned binaries、10049 total/10046 non-ignored/3原有ignored、unmatched=0；全部binary+test+ignored/filter身份与成功C运行相同，listing SHA-256也同为 `1ea689506406a9177336ab39294111c3194f4b09a15cc1f297be0163fdc53c5b`，两个原metadata pin逐一核对相同、唯一package为owned libra0.22.28。实际全量 run `51e1b567-8528-4577-9947-cdf15960873a` 于2026-09-14T17:05:19Z前后启动，Starting10046/230/3 skipped，无filter、原retries2及通用600s/仓库二例1200s；日志 `final-closeout-j16-full-suite.log`。启动时终态尚未取得；实际终态另见本节“最终本机全量终态”，不与C记录混用。

**R44 / R45 文档闭包:** Codex 核对发现 MIG-R01 与 MIG-R03 的卡内前置未显式列入总 DAG，已补齐原有依赖，不新增行为或前置条件。R44 独立审查为 `VERDICT: FAIL`，唯一 P1 是 R30/MIG-R02 的规范性新增仅登记于设计决策段、遗漏修订历史表；P2 是发布顺序一行未含后续恢复/配置卡。现补齐原日期/原因/受影响引用及完整发布顺序；R45 独立复审退出0、字面 `VERDICT: PASS`、P0/P1 none，确认两项关闭。原失败/旧参数/历史证据不改写。

**实证核对（R44时点，针对R03 C证据）:** 远端 v0.22.20..v0.22.28 九个 tag 的精确 SHA 均为当前 HEAD 的祖先，提交对象均有签名头与 DCO；main 与 v0.22.28 均为 `8fca47ebf19fe46e1bd7f3a5acbf52859179a1a1`。该时点源文件组合与三份 metadata、R03 C的10046-case JUnit 的 SHA-256 均与其C终态记录相同；最终收口另有独立JUnit `d92303ee1219e16f5b3962499f38d4ea06dda1ef4941b1076b2058547d86a4a9`，不能混用两轮结果，JUnit 实际10046个唯一 testcase、0 failure/error/retry nodes。默认具名门与 JUnit 逐项映射无缺项；feature/oracle 门仍由 MIG-04/MIG-06 已记录的实际专门运行证明，不冒充默认全量包含 ignored 门。v0.22.16/v0.22.19 oracle 文件重新计算的 SHA-256 与固定 pins 相同。CI benchmark run `34835661978` 仍为 exact `b660a078812bf22028e40f69fec6bdd449ece4ac` / success；metrics 为 ci=true、passed=true、106221568 bytes、0.734816586s、backup_and_preservation_verified=true。网站仍 clean Git `cf`，本地/远端同为 `6f6d80123add52a744c480ac9c7410c8acbe71b1`。生成器 byte equality 与 diff check 通过；没有追加生产、测试或网站源码变化。

**R44/R45 时点的最终门（历史，后由顶部用户指令覆盖等待时序）:** 当时 v0.22.28 Release/Advanced CodeQL 仍在运行，未标 D success。R44 认为同一最终源码的 C 全量可兼任收口证据；Codex 原计划按 ER-13 字面在 D 后复跑，R45 认可该保守执行选择。用户随后明确不等待 release 信息进行收尾，因此现立即执行该次最终默认全量，所有 source/metadata/无过滤集合、build16/test16、隔离、600s/二例1200s 与禁止扩张的要求不变。既有 C 全绿不撤销，未来验收不计通过；仅远端 D 等待不再阻塞本次交付，实际状态单列，不冒充成功。

## 完成判据

- [x] 所有任务卡满足 G-01..G-11，粒度审计表与任务字段一致；仅 EX-MIG-01/02/03 为已登记且 Claude PASS 的门族例外；无并发写集冲突、无 L/XL 卡。
- [x] MIG-00..MIG-06 与恢复/配置卡 MIG-R01/MIG-R02/MIG-R03 的实现、适用本机验收与提交/推送均完成；MIG-R03 的远端 D 在本机收尾期间自行完成并直接取得实证，未等待/重跑流程。若 DEP-MIG-01 未满足，MIG-04 与 MIG-06 必须 `blocked` 并按 DEFER-MIG-01 正式移交新日期计划；若 DEP-MIG-02 未满足，MIG-06 必须 `blocked`，计划不得虚报完成。
- [x] 每卡的 focused、migration、security、性能和文档 Verification 证据已记录；ER-04 A/B/C 适用门全部通过，既有 D 证据保留，MIG-R03 D亦已根据实际成功终态及后置实证闭合。
- [x] 依顶部最新用户“全量收口恢复”指令，最终收口除 fmt、计划静态契约守卫与每卡具名 focused target 外，还须取得载入 `.env.test` / `.env.live-test` 的全量默认 nextest 真实通过证据；任何失败均分析、前滚修复并重跑全绿，不用 Git/Libra 环境跳过冒充通过。此前 focused-only 历史证据保留；本次仍不增加 all-features/ignored/live feature targets、网站全量构建或 all-target-all-feature Clippy。
- [x] true Config future 仍 fail-closed，repo-only receipt 无误阻断，unknown/failure repair path zero-write，current `2026090801` 在无 attestation 时明确 ineligible，且任何测试均未访问真实 global/system config path。
- [x] 每张发布卡已按用户指令执行 `patch + 1`，完成版本面同步、工具链刷新 `Cargo.lock`、精准暂存、签名+DCO 提交、推送、tag 与 gh release 创建；既有 D 组证据保留，MIG-R03 D在本机收尾期间也已直接取得。本地 full build/install 依用户覆盖不执行，不把运行中状态冒充成功；已知失败仍只前滚。
- [x] 用户/开发/compat/error-code/test-index/website 文档均按 ER-06a 同卡收口，或有可核实的 N/A。
- [x] Codex 最终 review 为 `PASS`；Claude Code 最终输出含字面 `VERDICT: PASS`，所有 P0/P1 关闭。
