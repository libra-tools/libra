# Issue #484：`add` ignored 路径退出状态与 `--dry-run --ignore-missing` 语义修复计划（2026-09-14）

> 模板版本：v2.6；继承 [plan-template.md](../plan-template.md) 的 GC-01..13、ER-01..13、G-01..11。
> 来源：[libra-tools/libra#484](https://github.com/libra-tools/libra/issues/484)（含 2026-09-14 两条 TC-0004/TC-0005 更新评论）。
> 状态：设计计划；全部任务卡 `pending`，尚未进行 Codex review（见「Codex review log」）。

## 文档职责

本文解决 issue #484：`libra add --dry-run --ignore-missing track-this ignored-file` 在 `ignored-file` 不存在、但被 ignore 规则匹配时 exit 0，并只打印「did not match any files and was skipped」。Git 则把该路径列入 ignored 报告并以非零状态结束（`t/t3700-add.sh` 的 TC-0004/TC-0005）。目标是让 Libra 在「报告 ignored 路径」时的退出状态与 `--ignore-missing` 的判定语义都与 Git 对齐。

核对时发现，Git 的非零退出并非 `--ignore-missing` 专属：只要 ignored 报告非空，`add` 就在处理完其它路径后以 1 结束（`builtin/add.c:346-357`）。Libra 目前只在「除 ignored 外什么都没暂存」时才失败（`check_ignored_only_error`），因此混合形态 `add other.txt top.log` 同样 exit 0，上游 `t3700:138-145` 同样会失败。只修 `--ignore-missing` 分支无法让 TC-0004 通过，本计划因此拆为两个行为轴。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `libra add` 显式 pathspec 命中 ignored 路径时的退出状态（dry-run 与真实暂存），以及人读 ignored 报告块。
- `libra add --dry-run --ignore-missing`：用 ignore 规则分类未匹配任何文件的 pathspec。
- `--json` / `--machine`、`--quiet`、`--exit-code-on-warning` 在上述形态下的契约。
- 同步：`docs/commands/add.md` EN + zh-CN、`docs/development/commands/add.md`、`docs/error-codes.md`（`LBR-ADD-001` 退出码列）、`COMPATIBILITY.md` 的 `add` 行、`../libra-backend` 网站文档。

### 非目标

- Git advice 系统与 `advice.addIgnoredFile` 配置（DEFER-01）。
- ignored 报告中路径的拼写：Git 输出仓库相对路径（`sub/local.tmp`），Libra 输出原始 pathspec（`local.tmp`）；本计划保持 Libra 现状（DEFER-02）。
- `add -u --ignore-missing <missing>`：Git 为 128 错误，Libra 为跳过（DEFER-03）。
- `--ignore-errors` 下失败路径的退出状态：Git 为 1，Libra 为 0（DEFER-04）。
- 「仅 ignored」形态的退出码统一：Git 为 1，Libra 保持既有稳定契约 `LBR-ADD-001`/128（ADR-IA-02，不是延后项）。
- issue 描述的分歧 2（`--ignore-missing` 下不报告可添加文件）：核对日已不复现（0.22.25 输出 `add: track-this`），只在 IA-02 用回归测试钉住。

### 成功定义

- 移植 TC-0004：`add --dry-run --ignore-missing track-this ignored-file` 以非零状态（1）结束，index 不变。
- 移植 TC-0005：stdout 报告 `track-this` 的 add 动作；stderr 的 ignored 报告列出 `ignored-file`，并说明它被配置的 ignore 规则匹配。
- 移植 `t3700:138-145`：`add <可添加> <ignored>` 暂存可添加路径，不暂存 ignored 路径，以非零状态结束。
- 目录、`.`、`-A`、`-u`、`-f` 形态的退出状态不变（exit 0）；内部调用 `run_add` 的命令（`rerere`、`am`）行为不变。
- 两张卡各自完成 C 组发布（`patch + 1` + `gh` 触发 `release.yml`）并取得 D 组证据，然后全量收口门全绿。满足这些之后，本计划才可标记完成。

## 事实基线

> 核对日 2026-09-14。行号在开工当天刷新。核对时 `libra status --short --branch` 为 `## main...origin/main`，另有未跟踪的 `docs/development/plan/issues/483.md`（另一份未开工计划）；`Cargo.toml` 版本 `0.22.26`，本机安装的 `libra` 为 `0.22.25`。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 复现（TC-0004） | `.libraignore` 含 `ignored-file`，文件不存在：`libra add --dry-run --ignore-missing track-this ignored-file` → exit 0；stdout `add: track-this` / `(dry run, no files were staged)`；stderr `warning: pathspec 'ignored-file' did not match any files and was skipped (--ignore-missing)` | 2026-09-14 scratch 实测（`libra 0.22.25`） |
| 复现（混合 ignored） | `top.log` 存在并被 `*.log` 忽略：`libra add other.txt top.log` → exit 0，`other.txt` 已暂存；`libra add -n other.txt top.log` → exit 0；stderr 均为 ignored warning 块 | 同上 |
| 仅 ignored | `libra add top.log` / `add -n local.tmp` → `LBR-ADD-001`，exit 128，JSON error envelope | 同上；`tests/command/add_json_test.rs:344-358` 钉住该契约 |
| 不受影响的形态 | `add -n dir`（含 ignored 文件）、`add -n .`、`add -n -A`、`add -n -u` → exit 0，不报告 ignored（与 Git 一致） | 同上（与 git 2.54 并排实测） |
| 其它 flag | `--exit-code-on-warning add -n other.txt top.log` → exit 9（`LBR-WARN-001`）；`--json add -n track-this real.log` → `ok: true`，`data.ignored:["real.log"]`，exit 0；`--json --dry-run --ignore-missing track-this ignored-file` → `data.missing:["ignored-file"]`，`data.ignored: []` | 同上 |
| ignore 源 | Libra 读取 `.libraignore`、`.gitignore`、`info/exclude`、`core.excludesFile`；实测 `.gitignore` 中的条目被报告为 ignored | `docs/commands/add.md:329-345`；同上实测 |
| 缺失路径的 ignore 判定能力 | `libra check-ignore -v x.log build/missing sub/local.tmp` 对不存在的路径正确报告命中规则（含子目录 ignore 文件、`!` 否定、目录模式） | 同上实测；`src/command/check_ignore.rs:140-200` |
| 代码：参数 | `--ignore-missing` 注释为「silently skip pathspecs that match no file」，`requires = "dry_run"` | `src/command/add.rs:132-135` |
| 代码：分类 | `validate_pathspecs` 中，未匹配 selectable 候选但匹配 ignored 候选的 → `ignored`；两者都不匹配时，`ignore_missing` 为真 → `missing`，否则 `PathspecNotMatched`。缺失路径从不做 ignore 判定；函数签名没有 `force` | `src/command/add.rs:1185-1233`（分支 `:1210-1227`） |
| 代码：pathspec API | `unmatched_positive_specs` 只返回原始 spec 字符串；没有「每个 spec 的规范化仓库相对路径」访问器 | `src/utils/pathspec/mod.rs:159` |
| 代码：ignore 判定 | `should_ignore(path, IgnorePolicy::Respect, index)`（含 layer 排除、tracked 可见）；`path_matches_ignore_pattern(path, workdir)` 为原始模式匹配 | `src/utils/ignore.rs:226`、`:323` |
| 代码：退出决策 | `execute_safe` 先 `run_add`，再 `render_add_output`，然后在 ignored/failed/missing 非空时 `record_warning()`；只有 `run_add` 内的 `check_ignored_only_error`（`is_empty()` 时）会失败 | `src/command/add.rs:414-452`（`:442`、`:444-447`）；`:594`、`:704`、`:830`、`:960-982` |
| 代码：人读块 | ignored 块输出 `warning: the following paths are ignored by configured ignore rules:`、路径、`Hint: use -f …`，以及误导性的 `Hint: use 'libra restore --staged <file>' to unstage if needed`（ignored 路径从未被暂存） | `src/command/add.rs:1131-1156` |
| 内部调用方 | `am` 以 `force: true` 调用 `run_add`；`rerere` 以 `force: false` 暂存单个已解决路径 | `src/command/am.rs:959-975`；`src/command/rerere.rs:268-285` |
| 非零 + 完整输出先例 | `check-ignore` 先渲染（含 JSON）再 `CliError::silent_exit(1)`；`status --exit-code` 同样 | `src/command/check_ignore.rs:163-171`；`src/command/status.rs:4522-4523`；`src/utils/error.rs:774` |
| 错误码与文档漂移 | `AddNothingStaged`（`LBR-ADD-001`）运行时退出码为 128（特判为 Fatal）；`docs/error-codes.md` 表格却写 129 | `src/utils/error.rs:382`、`:486-498`；`docs/error-codes.md:115` |
| 测试 | 既有 `--ignore-missing` 用例（均使用不被 ignore 的 `nope.txt`）；串行登记 | `tests/command/add_test.rs:1253`、`:1327`、`:1355`；`tests/SERIAL_REGISTRY.tsv:199-201` |
| 文档 | EN/zh `--ignore-missing` 节把语义写成「skip … instead of failing」；人读输出样例的 hint 文案与实际不符；网站页仍有过时文案 `warning: all specified paths are ignored by .libraignore` | `docs/commands/add.md:165-174`、`:214-224`；`docs/commands/zh-CN/add.md:130-137`、`:181-187`；`docs/development/commands/add.md:67`；`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md:150-158`、`:203-204`；`COMPATIBILITY.md:182` |
| 网站仓库 | `../libra-backend` 为 Git 仓库，分支 `cf`（`## cf...origin/cf`） | 2026-09-14 `git -C ../libra-backend status --short --branch` |
| 测试环境 | `.env.test` 存在；`.env.live-test` 不存在，仓库无 example | 2026-09-14 `ls -a` |
| 外部参照（语义） | issue pin git@`1630431f326e15fcde608827b5ff38422528eb59`（本地副本 `/Volumes/Data/competition/git/git`）：`builtin/add.c:234-235`（ignored 报告标题）、`:279`（`--ignore-missing` 帮助：「check if - even missing - files are ignored in dry run」）、`:346-357`（`add_files`：ignored 非空 → 打印并 `exit_status = 1`，之后仍添加其余路径）、`:518-519`（要求 `--dry-run`）、`:622-650`（未匹配 pathspec：GLOB/ICASE magic 或文件不存在时，`ignore_missing` 下 `is_excluded` 命中才 `dir_add_ignored`，否则静默；非 `ignore_missing` 时 die）；`t/t3700-add.sh:124-145`（ignored 形态 `test_must_fail`，含「add others」）、`:424-441`（TC-0004 / TC-0005 的 expect.out、expect.err 与断言） | 核对日 2026-09-14 |
| 外部参照（实跑） | `/usr/bin/git` 2.54.0：TC-0004 exit 1，stdout `add 'track-this'`，stderr 标题 + 路径 + 两行 hint；缺失且不被 ignore → exit 0 静默；`-f` → exit 0 静默；`advice.addIgnoredFile=false` → 仍 exit 1，只省略 hint；`--ignore-errors` 不改变 exit 1；多个缺失 ignored 路径逐行列出，不被 ignore 的缺失路径不列；`!keep.log` 否定 → 0；`build/missing`（目录模式，目录不存在）→ 1；目录存在时额外列出 `build`；子目录 ignore 文件的 `sub/local.tmp` → 1，根目录 `local.tmp` → 0；`:(literal)x.log`、`:(icase)X.LOG` → 1；`ign*`、`:(glob)ign*` → 0；非 dry-run `add track-this ignored-file`（存在）→ exit 1 且 `track-this` 已暂存；`add -n dir`、`.`、`-A`、`-u` → 0；`add -u --ignore-missing track-this x.log` → 128 | 2026-09-14 scratch 实测 |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | 混合 ignored 形态（有可添加路径 + 显式 ignored 路径）exit 0，dry-run 与真实暂存均如此 | 依赖失败的脚本或 CI 预检静默通过；`t3700:138-145` 失败 | 事实基线「复现（混合 ignored）」；`src/command/add.rs:960-982` 只在 `is_empty()` 时失败 | IA-01 |
| GAP-02 | `--ignore-missing` 下缺失 pathspec 不做 ignore 判定，一律当普通未匹配跳过 | TC-0004 exit 0；TC-0005 stderr 无法区分「被 ignore 的缺失路径」与「任意未匹配」 | `src/command/add.rs:1218-1221` | IA-02 |
| GAP-03 | 人读 ignored 块含 `restore --staged` 提示，而 ignored 路径从未暂存 | 误导用户 | `src/command/add.rs:1131-1156` | IA-01 |
| GAP-04 | `docs/error-codes.md:115` 把 `LBR-ADD-001` 的退出码写成 129，实际为 128 | 机器消费者按文档分支会出错 | `src/utils/error.rs:486-498`；实测 exit 128 | IA-01 |
| GAP-05 | 命令文档把 `--ignore-missing` 写成「跳过并成功」，人读样例与网站文案过时 | 文档与 Git 语义、实际输出不符 | 事实基线「文档」 | IA-01（输出与退出状态）、IA-02（`--ignore-missing` 语义） |
| GAP-06 | 无「每个 pathspec 的规范化仓库相对路径」访问器 | IA-02 无法对原始 spec 做 ignore 判定（子目录前缀、magic） | `src/utils/pathspec/mod.rs:159` | IA-02（ADR-IA-03） |
| GAP-07 | `.env.live-test` 本机缺失 | v2.6 全量门无法执行 | 事实基线「测试环境」 | DEP-IA-01 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | 无对应 LR/SB/UP 编号；issue 计划不属于日期计划 | N/A，不触碰「日期计划索引」 |
| [`issues/483.md`](483.md) | 其 CO-03 修改 `add` 预览的 operation 分类，写集与本计划相交：`tests/command/add_test.rs`、`docs/commands/add.md`（EN/zh/dev/网站）、`COMPATIBILITY.md` | DEP-IA-02：两份计划的相交卡不得并发实施；由单一发布者串行发布；后开工的卡按 GC-01 刷新锚点 |
| [`plan-20260708.md`](../plan-20260708.md) | P1-01 引入 `add` 使用的共享 pathspec engine | IA-02 只新增只读访问器，不改变匹配语义 |
| `docs/development/commands/_compatibility.md` | 兼容分级与拒绝/延后登记的事实源 | 两张卡按其规则更新 `add` 子面描述 |
| `genedna/libra-testcases` | 外部用例 TC-0004 / TC-0005 | DEP-IA-04（outgoing） |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | issue 建议「任何 pathspec 未匹配就非零」与 Git 不符：Git 只在缺失路径被 ignore 时非零，其它缺失路径静默跳过 | 以 git@pin 源码与 git 2.54 实跑为准（ADR-IA-01）；在 #484 回复时说明（DEP-IA-04） |
| 可行性 | 所需原语均已存在：ignore 判定支持不存在的路径，`silent_exit` 有先例 | 只新增 pathspec 访问器（ADR-IA-03） |
| 任务卡粒度 | 「退出状态」与「缺失路径分类」是两个可独立恢复的轴 | 拆为 IA-01（退出状态）→ IA-02（分类），各自发布 |
| 依赖与顺序 | IA-02 的 TC-0004 断言依赖 IA-01 的退出状态；与 issues/483 CO-03 写集相交 | IA-01 → IA-02；DEP-IA-02 跨计划互斥 |
| 完整性 | `docs/error-codes.md` 漂移、网站过时文案、既有 `--ignore-missing` 测试需保持 | 列入 IA-01/IA-02 的 AC 与 Verification |
| 安全性 | 退出决策若放进 `run_add`，会让 `rerere`/`am` 在含 ignored 路径时中断 | ADR-IA-02：只在 `execute_safe` 渲染之后决定退出码 |
| 功能正确性 | ignore 判定必须覆盖否定、目录模式、子目录 ignore 文件、`.gitignore` 源、magic pathspec | M-MISS 矩阵逐行实跑 git 对照 |
| 接口兼容 | 退出码 0 → 1 属于公开行为变化；JSON 结构不变；仅 ignored 的 `LBR-ADD-001`/128 保持 | ADR-IA-02、ADR-IA-04；文档四处登记差异 |
| 数据流与控制流 | 真实暂存时 index 写入必须先于非零退出完成 | M-EXIT E1 断言 index 结果；决策点位于写入与渲染之后 |
| 性能与容量 | 每个未匹配 pathspec 做一次 ignore 判定，O(未匹配 spec 数) | 无新增扫描 |
| 可靠性与容错 | 非零退出不得跳过 `VCS_EVENT_POST_ADD` 历史事件分发 | IA-01 AC 覆盖事件分发顺序 |
| 可维护性 | ignore 判定复用 `utils::ignore`，不复制规则 | ADR-IA-03 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-14 | 初稿自审 + 与 git 2.54 并排实测 | 发现混合 ignored 形态退出状态才是 Git 非零的来源，拆出 IA-01；issue 建议的「任何未匹配即非零」按 Git 实际语义修正 | 草案单卡 → IA-01、IA-02 | 实施顺序、审计表、测试矩阵、追溯表、风险表 |

## 已决议设计决策

实现时若需偏离本节，必须先修改计划并说明原因，不得在代码中静默改语义。

### ADR-IA-01: Git 语义参照与判据矩阵

- **Status:** Accepted
- **Context:** issue 评论明确「措辞不必逐字节一致」，但退出状态、stdout 报告、stderr ignored 识别与 index 状态必须一致；issue 正文的建议与 Git 实际行为有出入。
- **Decision:** 以 git@`1630431f32` 源码（事实基线逐行锚定）与 `/usr/bin/git` 2.54 实跑为准。开工时每一行都要在相同 fixture 上用 git 复跑，并把 sanitized 对照写入 `LIBRA_TEST_SCRATCH_DIR`。自动化测试不调用系统 git。下列矩阵是 AC 引用的**判据规范**，每行必须有对应测试。

  通用 fixture：`.libraignore` 含 `*.log`、`!keep.log`、`build/`、`ignored-file`；`sub/.libraignore` 含 `local.tmp`；`.gitignore` 含 `gi.txt`；已跟踪 `track-this` 在工作区被修改；未跟踪 `other.txt`；目录 `dir/` 含未跟踪 `new.txt` 与被忽略的 `inner.log`。

  **M-EXIT（ignored 报告的退出状态，IA-01）** — 另有存在且被忽略的 `top.log`、`sub/local.tmp`

  | 行 | 调用 | Git 2.54 | Libra 目标 |
  |---|---|---|---|
  | E1 | `add other.txt top.log` | 1；`other.txt` 已暂存 | exit 1；`other.txt` 已暂存；`top.log` 未暂存 |
  | E2 | `add -n other.txt top.log` | 1；stdout `add 'other.txt'` | exit 1；stdout 报告 `other.txt`；index 不变 |
  | E3 | `add top.log`、`add -n top.log` | 1 | 保持 `LBR-ADD-001`，exit 128（ADR-IA-02） |
  | E4 | `add -n dir` | 0；不报告 `inner.log` | exit 0；不报告 |
  | E5 | `add -n .` | 0 | exit 0 |
  | E6 | `add -n -A`、`add -n -u` | 0 | exit 0 |
  | E7 | `add -f -n other.txt top.log` | 0 | exit 0；无 ignored 块 |
  | E8 | `add --ignore-errors other.txt top.log` | 1 | exit 1；`other.txt` 已暂存 |
  | E9 | `--json add -n other.txt top.log` | — | stdout 为既有 data envelope（`added`/`modified` 含 `other.txt`，`ignored:["top.log"]`）；exit 1；stderr 无人读块 |
  | E10 | `--exit-code-on-warning add -n other.txt top.log` | — | exit 1（优先于 9） |
  | E11 | 在 `sub/` 下运行 `add -n local.tmp ../other.txt` | 1 | exit 1；ignored 块列出该路径（拼写见 DEFER-02） |

  **M-OUT（人读 ignored 块，IA-01）**

  | 行 | 条件 | Libra 目标 |
  |---|---|---|
  | O1 | 混合形态 | stderr：标题行 `warning: the following paths are ignored by configured ignore rules:`、每行一个路径、`Hint: use -f if you really want to add them.` |
  | O2 | 任意 ignored 块 | 不再输出 `Hint: use 'libra restore --staged <file>' to unstage if needed` |
  | O3 | `--quiet` | stdout 抑制；stderr ignored 块保留；exit 1 |
  | O4 | 真实暂存 | `VCS_EVENT_POST_ADD` 事件照常分发（`wrote_index()` 为真时），之后才返回非零 |

  **M-MISS（`--dry-run --ignore-missing` 的缺失路径分类，IA-02）** — 下列路径均不存在

  | 行 | 调用（均带 `-n --ignore-missing`） | Git 2.54 | Libra 目标 |
  |---|---|---|---|
  | M1 | `track-this ignored-file`（TC-0004/0005） | 1；stdout `add 'track-this'`；stderr 列出 `ignored-file` | exit 1；stdout 报告 `track-this`；stderr ignored 块列出 `ignored-file`，且无 `did not match any files` 行；index 不变 |
  | M2 | `track-this x.log` | 1 | exit 1 |
  | M3 | `track-this keep.log`（`!keep.log`） | 0，静默 | exit 0；保留 Libra 跳过 warning |
  | M4 | `track-this build/missing`（`build/` 不存在） | 1 | exit 1 |
  | M5 | `track-this build/missing`（`build/` 存在且为空） | 1；另列 `build` | exit 1；至少列出 `build/missing`（额外的 `build` 行不要求，登记于 ADR-IA-04） |
  | M6 | `track-this sub/local.tmp` | 1 | exit 1 |
  | M7 | `track-this local.tmp`（根目录，子目录规则不适用） | 0 | exit 0；跳过 warning |
  | M8 | `track-this gi.txt`（`.gitignore` 源） | 1（`.gitignore`） | exit 1 |
  | M9 | `track-this ':(literal)x.log'` | 1 | exit 1 |
  | M10 | `track-this ':(icase)X.LOG'` | 1（大小写不敏感视图） | exit 1；仅在 `core.ignorecase` 生效时断言 |
  | M11 | `track-this 'ign*'`、`track-this ':(glob)ign*'` | 0 | exit 0；跳过 warning |
  | M12 | `-f track-this x.log` | 0，静默 | exit 0；跳过 warning；不做 ignore 判定 |
  | M13 | `track-this x.log y.log nope` | 1；列出 `x.log`、`y.log` | exit 1；ignored 块含 `x.log`、`y.log`；`nope` 仍输出跳过 warning |
  | M14 | 在 `sub/` 下运行 `local.tmp ../x.log`（无可添加路径） | 1 | 走「仅 ignored」契约：`LBR-ADD-001`，exit 128（ADR-IA-02） |
  | M15 | `--json track-this ignored-file` | — | `data.ignored` 含 `ignored-file`；`data.missing` 不含；exit 1 |

- **Alternatives considered:** 采纳 issue 正文的「任何未匹配 pathspec 即非零」——与 Git 源码 `builtin/add.c:640-646` 和实跑结果冲突，拒绝。录制 git 输出做逐字节比对——issue 评论明确不要求措辞一致，且 Libra 文件名为 `.libraignore`，拒绝。
- **Consequences:** 矩阵行变化时必须同批修改 ADR 与测试，并登记修订历史。
- **Revisit when:** 外部用例仓库升级 Git 参照版本，或 `builtin/add.c` 的 ignored 处理发生变化。

### ADR-IA-02: 退出状态模型

- **Status:** Accepted
- **Context:** Git 在 ignored 报告非空时以 1 结束，但仍完成其余路径（`builtin/add.c:346-357`）。Libra 的稳定错误码表只有 0/9/128/129 这几类粗粒度退出码，但已有「完整渲染后 `silent_exit(1)`」的先例（`check_ignore.rs:163-171`、`status.rs:4522-4523`）。
- **Decision:**
  1. `run_add` 的返回值与错误语义不变。退出决策只在 `execute_safe` 中做：完成 `render_add_output`、warning 记录与 `VCS_EVENT_POST_ADD` 分发之后，若 `result.ignored` 非空，就返回 `CliError::silent_exit(1)`。
  2. 「仅 ignored」形态仍由 `check_ignored_only_error` 返回 `LBR-ADD-001`，exit 128，保持 `add_json_test.rs:344-358` 的契约。
  3. `--json` / `--machine` 下 stdout 为既有 data envelope（结构不变），退出码为 1。
  4. exit 1 优先于 `--exit-code-on-warning` 的 9。「不被 ignore 的缺失 pathspec」仍只是 warning（exit 0，或在 `--exit-code-on-warning` 下为 9）。
  5. `-f` 时 ignored 列表为空，不触发。
- **Alternatives considered:** 为混合形态新增 128 类稳定错误码——error envelope 会丢失 `added`/`modified` 数据，且偏离 Git 的 1，拒绝。把决策放进 `run_add`——`rerere` 等内部调用方会在含 ignored 路径时中断，拒绝。把「仅 ignored」一并改为 exit 1——会破坏已发布的 `LBR-ADD-001` 契约，而上游测试只要求非零，拒绝。
- **Consequences:** 混合形态是一次公开行为变化（0 → 1），必须在 EN/zh 文档的退出状态说明、`COMPATIBILITY.md` 的 `add` 行与 dev 文档中写明。JSON 消费者应以 `data.ignored` 非空或退出码为判据。
- **Revisit when:** Libra 引入统一的「部分成功」退出码或 envelope 字段。

### ADR-IA-03: `--ignore-missing` 缺失路径的 ignore 判定

- **Status:** Accepted
- **Context:** GAP-02、GAP-06。Git 对 GLOB/ICASE magic 或不存在的路径，用 pathspec 的 match 字符串（已去除 magic、已加前缀）调用 `is_excluded`，命中才列入 ignored（`builtin/add.c:622-650`）。
- **Decision:**
  1. 在 `src/utils/pathspec/mod.rs` 新增只读访问器，按原始 spec 返回规范化后的仓库相对 match 路径（去除 magic、应用当前目录前缀、保留 icase 属性）；不改变任何匹配语义。
  2. `validate_pathspecs` 增加 `force` 参数。对「selectable 与 ignored 候选都不匹配」的 spec，在 `ignore_missing && !force` 时，用 `utils::ignore::should_ignore(path, IgnorePolicy::Respect, index)` 判定该 match 路径：命中 → 进入 `ignored`（沿用原始 spec 拼写）；未命中 → 进入 `missing`（保留既有 warning 与 JSON `missing`）。
  3. 含通配符但不带 magic 的 spec（如 `ign*`），按 Git 行为把字面字符串交给判定，结果以 M-MISS M11 的实跑为准。
- **Alternatives considered:** 在 `add.rs` 内另写一套 ignore 匹配——违反 GC-02，拒绝。直接调用 `check-ignore` 的 `classify_path`——它是命令私有函数，且带输出结构，拒绝。
- **Consequences:** `src/utils/pathspec/` 新增公共访问器，是共享 helper（IA-02 命中 T-1）。
- **Revisit when:** DEFER-02（路径拼写对齐 Git）被排期。

### ADR-IA-04: 有意差异

- **Status:** Accepted
- **Context:** issue 评论允许 Libra 措辞、额外 hint、错误码与 JSON 不同；以下差异仍需让用户知情。
- **Decision:** 下列差异同时写入 EN 文档、zh-CN 文档、`COMPATIBILITY.md` 的 `add` 行与 `docs/development/commands/add.md`：
  1. 「仅 ignored」形态为 `LBR-ADD-001`，exit 128（Git 为 1）。
  2. ignored 块标题与 hint 为 Libra 措辞，无 Git advice 行，且不支持 `advice.addIgnoredFile`。
  3. ignored 路径使用原始 pathspec 拼写（Git 为仓库相对路径）。
  4. 不被 ignore 的缺失 pathspec 仍输出 Libra 跳过 warning（Git 静默）。
  5. 目录模式下已存在的被忽略父目录不额外列出（M-MISS M5）。
- **Alternatives considered:** 不登记——违反 GC-03，拒绝。
- **Consequences:** 移植用例断言退出状态、stdout 报告、stderr 是否识别路径与 index 状态，不断言措辞。
- **Revisit when:** DEFER-01/02 被排期。

## 全局工程约束

继承模板 GC-01..GC-13（GC-13 为 N/A，理由：不触及 SQLite schema 或连接建构）。计划级补充：

- **GC-IA-01 生产入口判据:** 所有退出状态与输出类 AC，必须由子进程 `run_libra_command(args, cwd)` 测试证明；只调用内部 helper 的测试不算判据。
- **GC-IA-02 串行登记:** 新增集成测试优先使用 `init_repo_via_cli` + `run_libra_command` 的 cwd 参数，不使用 `ChangeDirGuard`。确需 `#[serial(cwd)]` 的，必须同步 `tests/SERIAL_REGISTRY.tsv`，重跑 `sh tests/NEXTEST_GROUPS.sh`，并通过 `compat_serial_registry`。
- **GC-IA-03 证据目录:** git 对照证据写入 `LIBRA_TEST_SCRATCH_DIR`（持久介质），脱敏且不含私有绝对路径（ER-11）。

## 执行检查必备需求（强制）

完整继承模板 ER-01..ER-13。计划级补充：

- **ER-IA-01:** 命中 ER-13 全量触发条件的 C④ 与计划收口门，必须在 DEP-IA-01 满足后执行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`；不得以 `cargo test --all` 替代。
- **ER-IA-02:** 每张卡开工时的 ER-02 核对，必须对本卡矩阵的全部行重跑 git 2.54 与 Libra 并排实测，结果附在执行记录中；与本文事实基线不一致时，先修订计划。

## 实施顺序

- `DEP-IA-01 -> IA-01`（仅约束 C④ 与收口门）
- `DEP-IA-02 -> IA-01`（与 issues/483 相交卡互斥）
- `IA-01 -> IA-02`

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-IA-01 | incoming | 环境前置 | 仓库根 `.env.live-test` | 发布者 / 仓库维护者 | 文件存在，且 `source .env.test && source .env.live-test` 返回 0 | 2026-09-14 本机缺失 | 未满足时，命中 T-* 的卡停在 `locally-accepted`，收口门 `blocked`；不得降级为 `cargo test --all` |
| DEP-IA-02 | incoming | 跨计划写集互斥 | [`issues/483.md`](483.md) 的 CO-03（`tests/command/add_test.rs`、`docs/commands/add.md` 系列、`COMPATIBILITY.md`） | 单一发布者 | 开工时 CO-03 未处于 `in-progress`，或已 `done`；两份计划的发布不交错 | 2026-09-14 两份计划均为 `pending` | 冲突时后开工者等待；不得合并两张卡的提交；锚点按 GC-01 刷新 |
| DEP-IA-03 | incoming | 外部仓库 | `../libra-backend`（网站文档） | docs owner | ER-06a：Git 仓库且位于 `cf` 分支，写入前重核 | 2026-09-14 `## cf...origin/cf` | 分支或脏状态阻塞时该卡 `blocked`，不得绕过 |
| DEP-IA-04 | outgoing | 外部用例 | `genedna/libra-testcases` 的 TC-0004、TC-0005 | 该仓库维护者 | IA-02 发布版本号，以及 M-MISS M1 的移植测试证据 | issue #484 评论（2026-09-14） | 本计划不修改外部仓库；在 #484 回复发布版本、证据，以及对 issue 正文「任何未匹配即非零」的更正。该回复属于对外动作，发出前须经用户确认 |

### 发布分组与并发窗口

无 `REL-*`：两张卡均为 `independent`，各自 `patch + 1` 并用 `gh` 发布。

**并发声明:** 全串行。IA-01 与 IA-02 的实现写集相交（`src/command/add.rs`、`tests/command/add_test.rs`、add 文档）；与 issues/483 CO-03 相交部分见 DEP-IA-02。

**发布者:** 执行本计划的主 Agent（单一发布者）负责 C 组 bump、构建、安装、`libra add`/`libra commit -s`/`libra push origin main`、`gh release create`，并跟踪 D 组证据；与 issues/483 共用同一串行发布队列。

**发布窗口顺序:** IA-01 → IA-02；同一时刻只允许一张卡（含 issues/483 的卡）处于「已 bump 未完成推送 / 未触发 release」状态。版本号在进入窗口时从 `Cargo.toml` 读取（核对日基线 `0.22.26`）。

### Phase 0: 基线冻结

**目标:** 计划通过 Codex review；前置依赖状态已记录。

**进入条件:**

- 本计划成稿。

**退出条件:**

- 「Codex review log」有一轮 `PASS`。
- DEP-IA-01、DEP-IA-02 的状态已记录。

### Phase 1: ignored 报告退出状态

**目标:** IA-01 发布。

**进入条件:**

- Phase 0 退出。

**退出条件:**

- IA-01 为 `done/complete`。

### Phase 2: `--ignore-missing` 分类

**目标:** IA-02 发布，关闭 TC-0004/TC-0005。

**进入条件:**

- IA-01 为 `done/complete`。

**退出条件:**

- IA-02 为 `done/complete`，且全量收口门全绿。

## 任务卡

### 字段全局默认与例外

- **Release boundary 默认:** `independent`。
- **Task type 默认:** `implementation`。
- **Rollback mode 默认:** `revert`（源码单提交 revert；已发布 artifact 按 ER-08 发新 patch 前滚）。
- **Migration and rollback 默认:** N/A（无 schema、存储格式或数据迁移）。
- **Security and privacy 默认:** 继承 GC-07、GC-11；证据按 ER-11 脱敏。
- **Performance budget 默认:** 继承 GC-10；每个未匹配 pathspec 至多一次 ignore 判定，无新增目录扫描。
- **Version increment 默认:** `patch`。
- **C/D coverage from 默认:** `self`。
- **D 组默认:** `.github/workflows/release.yml`，由 `gh release create "v${VERSION}"`（`push` `v*` tag）触发；判据为 run 全部 job 成功，且 `download.libra.tools` 能取到对应版本（`gh run list --workflow=release.yml` / `gh run watch` 取证）。
- **Lifecycle / Acceptance 默认:** `pending` / 空。

**默认覆盖:** 无。

**规则 waiver:** 无 `EX-*`。

### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| IA-01 | implementation | 显式 ignored 路径的退出状态与人读块 | revert 单提交 | yes | yes | 8/8 | 7/8 | 1/1 | M | DEP-IA-01, DEP-IA-02 | 序列化于首位（跨计划见 DEP-IA-02） | independent | N/A | N/A |
| IA-02 | implementation | `--ignore-missing` 缺失路径的 ignore 分类 | revert 单提交 | yes | yes | 8/8 | 6/8 | 2/2 | M | IA-01 | 序列化于 IA-01 | independent | IA-01 | N/A |

### Task IA-01: 显式 ignored 路径使 `add` 以非零状态结束

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-IA-02，当显式 pathspec 命中 ignored 路径、且还有其它路径被暂存或报告时，`add`（dry-run 与真实暂存）在完成暂存、渲染与事件分发后以 exit 1 结束。同时修正人读 ignored 块的误导性 hint 与 `LBR-ADD-001` 退出码文档。唯一行为轴是「ignored 报告的退出状态」。

**Out of scope:** `--ignore-missing` 的缺失路径分类（由 IA-02 承接）；「仅 ignored」退出码改为 1（永久非目标，理由见 ADR-IA-02）；`--ignore-errors` 失败路径的退出状态（DEFER-04）；ignored 路径拼写（DEFER-02）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 混合形态 exit 0（dry-run 与真实暂存） | 事实基线「复现（混合 ignored）」 |
| 只有 `is_empty()` 时才失败 | `src/command/add.rs:960-982` |
| 退出决策与事件分发位置 | `src/command/add.rs:414-452` |
| 误导性 hint | `src/command/add.rs:1131-1156` |
| 渲染后 `silent_exit(1)` 先例 | `src/command/check_ignore.rs:163-171` |
| `LBR-ADD-001` 退出码文档漂移 | `src/utils/error.rs:486-498`；`docs/error-codes.md:115` |
| Git 语义 | git@1630431f32 `builtin/add.c:346-357`；`t/t3700-add.sh:138-145` |

**Acceptance criteria:**

- [ ] 混合形态通过 ADR-IA-01 的 M-EXIT E1–E2。
- [ ] 「仅 ignored」形态通过 M-EXIT E3（`LBR-ADD-001`/128，`add_json_test::json_ignored_only_returns_error` 不变）。
- [ ] 不受影响的形态通过 M-EXIT E4–E7。
- [ ] `--ignore-errors`、`--json`、`--exit-code-on-warning` 与子目录形态通过 M-EXIT E8–E11。
- [ ] 人读 ignored 块通过 ADR-IA-01 的 M-OUT O1–O4。
- [ ] `run_add` 的返回值不变：`rerere` 自动暂存与 `am` 暂存行为不因本卡改变。
- [ ] `docs/error-codes.md` 中 `LBR-ADD-001` 的退出码列更正为 128，与 `src/utils/error.rs:486-498` 一致。
- [ ] ER-06a 同步：`docs/commands/add.md` 与 `docs/commands/zh-CN/add.md`（`-n`、`--ignore-errors` 旁的退出状态说明、人读输出样例改为实际文案、新增 exit 1 说明与 ADR-IA-04 差异 1–3）、`docs/development/commands/add.md`、`COMPATIBILITY.md:182` 原行追加、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`（含修正 `:203-204` 的过时文案）。

**Verification:**

- [ ] `source .env.test && cargo test --lib command::add`（new：退出决策表驱动单测）
- [ ] `source .env.test && cargo test --test command_test add_test`（new：`test_add_ignored_with_others_stages_others_and_fails`、`test_add_dry_run_ignored_with_others_fails`、`test_add_ignored_report_unaffected_forms`、`test_add_ignored_report_flags_matrix`、`test_add_ignored_block_hints_and_quiet`）
- [ ] `source .env.test && cargo test --test command_test add_json_test`（new：`json_mixed_ignored_emits_data_and_exits_one`）
- [ ] `source .env.test && cargo test --test command_test rerere_test`
- [ ] `source .env.test && cargo test --test compat_error_codes_doc_sync`
- [ ] `source .env.test && cargo test --test compat_ledger_schema`
- [ ] 手工证据：M-EXIT、M-OUT 各行的 git 2.54 与 Libra 并排实测（存 `LIBRA_TEST_SCRATCH_DIR`）

**Full-suite trigger:** `T-1: 修改 docs/error-codes.md 的稳定错误码条目`

**Dependencies:** `DEP-IA-01`（仅约束 C④）、`DEP-IA-02`

**Deliverables:** N/A

**Implementation write set:** `src/command/add.rs`、`tests/command/add_test.rs`、`tests/command/add_json_test.rs`、`docs/commands/add.md`、`docs/commands/zh-CN/add.md`、`docs/development/commands/add.md`、`docs/error-codes.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set；若 GC-IA-02 需要串行 lane，则追加 `tests/SERIAL_REGISTRY.tsv` 与生成的 `.config/nextest.toml`

**Docs and compatibility impact:** `add` 命令页 EN + zh-CN + 网站页的退出状态与人读输出；`docs/error-codes.md` 退出码更正；`COMPATIBILITY.md` 的 `add` 行追加「显式 ignored 路径 → exit 1（仅 ignored 仍为 `LBR-ADD-001`/128）」。后端写入前记录 Git 仓库类型与 `cf` 分支证据（DEP-IA-03）。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 非零退出发生在 index 写入与事件分发之后，不产生半写 index；内部调用方不受影响（AC 第 6 条）。

**Performance budget:** Inherited

**Estimated scope:** `M`（单一生产文件，但退出状态是公开行为变化）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=显式 ignored 路径的退出状态与人读块; recovery=revert 单提交，混合形态恢复 exit 0; complete=yes; self-contained=yes; AC=8/8; VER=7/8; landing=1; prod-files=1; scope=M; deps=DEP-IA-01,DEP-IA-02; writeset=序列化于首位; release=independent; split-from=N/A; exception=N/A`

### Task IA-02: `--dry-run --ignore-missing` 按 ignore 规则分类缺失 pathspec

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-IA-03，`--dry-run --ignore-missing` 对未匹配任何文件的 pathspec 执行 ignore 判定：命中 ignore 规则的进入 ignored 报告（借助 IA-01 以 exit 1 结束），未命中的保持跳过 warning。移植 TC-0004/TC-0005。唯一行为轴是「缺失路径的 ignore 分类」。

**Out of scope:** `-u --ignore-missing` 的 Git 128 语义（DEFER-03）；`advice.addIgnoredFile`（DEFER-01）；ignored 路径拼写（DEFER-02）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 缺失路径不做 ignore 判定 | `src/command/add.rs:1218-1221` |
| `validate_pathspecs` 无 `force` 参数 | `src/command/add.rs:1185-1192` |
| pathspec 只返回原始 spec | `src/utils/pathspec/mod.rs:159` |
| ignore 判定支持不存在的路径 | `src/utils/ignore.rs:226`；`libra check-ignore -v` 实测 |
| 既有 `--ignore-missing` 测试（非 ignored 的缺失路径） | `tests/command/add_test.rs:1253`、`:1327`、`:1355` |
| Git 语义与上游用例 | git@1630431f32 `builtin/add.c:622-650`；`t/t3700-add.sh:424-441` |

**Acceptance criteria:**

- [ ] TC-0004：M-MISS M1 的退出状态为 1，且 index 不变。
- [ ] TC-0005：M-MISS M1 的 stdout 报告 `track-this` 的 add 动作；stderr ignored 块列出 `ignored-file`，且不再出现 `did not match any files and was skipped` 行。
- [ ] ignore 规则来源与模式形态通过 M-MISS M2–M11。
- [ ] `-f` 与混合缺失形态通过 M-MISS M12–M13。
- [ ] 「仅缺失且 ignored」形态通过 M-MISS M14（`LBR-ADD-001`/128）。
- [ ] `--json` 通过 M-MISS M15。
- [ ] 不被 ignore 的缺失 pathspec 保持既有契约：`test_add_ignore_missing_dry_run_skips`、`test_add_ignore_missing_json_exposes_skipped`、`test_add_ignore_missing_triggers_warning_exit` 不修改断言即通过。
- [ ] ER-06a 同步：EN/zh `--ignore-missing` 节改为 Git 语义（检查缺失路径是否被 ignore；被 ignore 则报告并以 1 结束；未被 ignore 则跳过并 warning），写入 ADR-IA-04 差异 4–5；`docs/development/commands/add.md:67` 行、`COMPATIBILITY.md:182` 原行中「skip non-existent pathspecs」的描述更正、网站 `add.en.md:150-158`；`src/command/add.rs:132-133` 的参数帮助文案同步。

**Verification:**

- [ ] `source .env.test && cargo test --lib utils::pathspec`（new：规范化 match 路径访问器，覆盖子目录前缀、`:(literal)`、`:(icase)`、`:(glob)`、`:(top)`）
- [ ] `source .env.test && cargo test --lib command::add`（new：`validate_pathspecs` 在 `ignore_missing`/`force` 组合下的分类单测）
- [ ] `source .env.test && cargo test --test command_test add_test::test_add_dry_run_ignore_missing_ignored_path_tc0004`（new）
- [ ] `source .env.test && cargo test --test command_test add_test`（含 new `test_add_dry_run_ignore_missing_output_tc0005`、`test_add_ignore_missing_rule_matrix`、`test_add_ignore_missing_force_and_mixed`、`test_add_ignore_missing_only_ignored_is_add_001`，以及三个既有 `--ignore-missing` 用例）
- [ ] `source .env.test && cargo test --test command_test add_json_test`（new：`json_ignore_missing_ignored_path_in_ignored_bucket`）
- [ ] 手工证据：M-MISS 各行的 git 2.54 与 Libra 并排实测（存 `LIBRA_TEST_SCRATCH_DIR`）

**Full-suite trigger:** `T-1: src/utils/pathspec/ 新增共享 pathspec 访问器（GC-02 单一事实源）`

**Dependencies:** `IA-01`（消费其「ignored 报告非空 → exit 1」的退出决策）

**Deliverables:** N/A

**Implementation write set:** `src/utils/pathspec/mod.rs`、`src/command/add.rs`、`tests/command/add_test.rs`、`tests/command/add_json_test.rs`、`docs/commands/add.md`、`docs/commands/zh-CN/add.md`、`docs/development/commands/add.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** `add` 命令页 EN + zh-CN + 网站页的 `--ignore-missing` 语义；`COMPATIBILITY.md` 的 `add` 行更正；`--help` 参数文案；错误码 N/A（不新增）。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** ignore 判定只读取仓库内 ignore 源；越出工作树的 pathspec 仍由既有 `PathOutsideRepo` 在判定前拒绝。

**Performance budget:** 每个未匹配 pathspec 一次 `should_ignore`，O(未匹配 spec 数 × 路径深度)。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=--ignore-missing 缺失路径的 ignore 分类; recovery=revert 单提交，缺失路径恢复一律跳过; complete=yes; self-contained=yes; AC=8/8; VER=6/8; landing=2; prod-files=2; scope=M; deps=IA-01; writeset=序列化于 IA-01; release=independent; split-from=IA-01; exception=N/A`

## 测试矩阵

执行阶段按 ER-13 只跑与本卡相关的行；整张表由收口阶段的全量门统一覆盖。

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | 退出决策表；pathspec 规范化访问器；`validate_pathspecs` 分类 | `source .env.test && cargo test --lib command::add`；`--lib utils::pathspec` |
| 集成 | M-EXIT、M-OUT、M-MISS 全部行；TC-0004、TC-0005；`t3700:138-145` 等价用例；既有 `--ignore-missing` 用例 | `source .env.test && cargo test --test command_test add_test`；`add_json_test`；`rerere_test` |
| 兼容 | 错误码文档同步；`COMPATIBILITY.md` 行锚 | `--test compat_error_codes_doc_sync`；`--test compat_ledger_schema`；需要串行 lane 时加 `--test compat_serial_registry` |
| 迁移 | N/A（无 schema 或存储格式变化） | N/A |
| 安全 | 越出工作树的 pathspec 仍被拒；非零退出不产生半写 index | M-EXIT E1；既有 `PathOutsideRepo` 用例（`add_test`） |
| 性能 | 无新增扫描 | 代码 review 核对（ER-05） |
| Git 对照（手工） | 全部矩阵行与 git 2.54 实跑一致 | 证据写入 `LIBRA_TEST_SCRATCH_DIR`（ER-IA-02） |
| 全量 | 收口门 | `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`（DEP-IA-01） |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| IA-01 | issue #484 分歧 1；`builtin/add.c:346-357`；`t3700:138-145`；2026-09-14 实测 | `src/command/add.rs:414-452`、`:1128-1156` | add 命令页 EN/zh/网站、`docs/error-codes.md:115`、`COMPATIBILITY.md:182` | `add_test::test_add_ignored_with_others_stages_others_and_fails`、`add_json_test::json_mixed_ignored_emits_data_and_exits_one` |
| IA-02 | issue #484 分歧 1/3 与 TC-0004/0005 评论；`builtin/add.c:622-650`；`t3700:424-441` | `src/command/add.rs:1185-1233`、`src/utils/pathspec/mod.rs` | add 命令页 `--ignore-missing` 节、`COMPATIBILITY.md:182`、`--help` | `add_test::test_add_dry_run_ignore_missing_ignored_path_tc0004`、`add_test::test_add_dry_run_ignore_missing_output_tc0005` |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | 计划 Codex review PASS；DEP-IA-01/02 状态已记录 | review log | N/A |
| M1 | IA-01 `done/complete`（`t3700:138-145` 等价用例通过） | patch 版本、`release.yml` run、git 对照证据 | revert 源码 + 新 patch 前滚 |
| M2 | IA-02 `done/complete`（TC-0004/0005 通过）；全量收口门全绿 | 同上 + 全量 nextest 结果 | 同上 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| 真实暂存中途失败（index 保存前） | 无 index 变化 | 按既有 `IndexSave` 错误返回 | 返回 exit 1 却未写入可添加路径 |
| 渲染完成后返回 exit 1 | index 已写入、事件已分发 | 无需恢复（预期结果） | 跳过 `VCS_EVENT_POST_ADD` |
| IA-02 判定 ignore 源读取失败 | 无 | 按既有 ignore 读取错误路径处理，并在 ER-02 中确认其错误码 | 静默当作不被 ignore 而 exit 0 |
| 发布步 `gh release create` 失败 | 已推送的 `main` 提交 | 按 ER-09 记录原因，下一窗口补 tag/release | blind retry；只推 `main` 就宣称已发布 |
| D 组 `release.yml` 失败 | 已存在的 tag | 前滚修复，发新 patch | 回退已推送提交或删除已发布 artifact |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 现有脚本或 Agent 流程依赖混合 ignored 形态 exit 0 | 中：升级后流程在 `add a ignored` 处中断 | ADR-IA-02 登记为公开行为变化；文档与 release note 说明；`-f` 或移除 ignored pathspec 可恢复 | IA-01 |
| 退出决策误入 `run_add`，导致 `rerere`/`am` 中断 | 高：冲突解决流程失败 | ADR-IA-02；AC 第 6 条；`rerere_test` | IA-01 |
| 目录、`.`、`-A` 形态被误判为报告 ignored，导致大面积非零 | 高：日常 `add .` 失败 | M-EXIT E4–E6 回归测试 | IA-01 |
| 否定模式、目录模式、子目录 ignore 文件的判定与 Git 不一致 | 中：parity 声明失实 | M-MISS 逐行实跑对照；复用 `utils::ignore` | IA-02 |
| pathspec 访问器改变共享匹配语义 | 高：影响所有使用 pathspec 的命令 | ADR-IA-03 限定为只读访问器；IA-02 命中 T-1 跑全量 | IA-02 |
| 与 issues/483 CO-03 并发修改同一测试和文档文件 | 中：冲突与漏同步 | DEP-IA-02 串行互斥 | IA-01、IA-02 |
| `.env.live-test` 缺失阻塞全量门 | 中：卡停在 `locally-accepted` | DEP-IA-01，禁止降级 | 全部 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| IA-01 退出决策 | O(1) | O(1) | 无新增 I/O | 单测 |
| IA-02 缺失路径 ignore 判定 | 一次 `should_ignore` | O(未匹配 spec 数 × 路径深度) | 无目录遍历 | 代码 review |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md` 的 `add` 行（第 182 行）已在原物理行内更新，不改变文件行数。
- [ ] `docs/commands/add.md` 已同步。
- [ ] `docs/commands/zh-CN/add.md` 已同步。
- [ ] `docs/development/commands/add.md` 已同步。
- [ ] `docs/error-codes.md`：`LBR-ADD-001` 退出码列已更正（IA-01）；不新增错误码。
- [ ] `tests/INDEX.md`：N/A（只在既有 `command_test` 模块内新增用例，索引行描述未失真）。
- [ ] `Cargo.toml` `[[test]]`：N/A（无新 target）。
- [ ] `../libra-backend` 网站 `commands/add.en.md` 已在已验证的 `cf` 分支同步（DEP-IA-03）。
- [ ] `plan-long.md`：N/A（issue 计划，不进日期计划索引，无 LR 编号）。

## Codex review log

本计划尚未进行 Codex review。按 ER-05，Phase 0 必须至少有一轮 `PASS` 后才能开工。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-01 | Git advice 文案与 `advice.addIgnoredFile` 配置 | Libra 尚无 advice 配置体系；issue 评论不要求措辞一致 | 引入 advice 配置面 | 待立计划 |
| DEFER-02 | ignored 报告路径改为仓库相对拼写（Git `sub/local.tmp`） | 会改变 JSON `ignored` 的既有内容 | 外部用例断言路径拼写，或 JSON schema 版本升级 | 待立 issue |
| DEFER-03 | `add -u --ignore-missing <missing>` 对齐 Git 的 128 错误 | 与 ignore 判定无关的独立分歧 | 上游用例移植需要 | 待立 issue |
| DEFER-04 | `--ignore-errors` 下失败路径以 exit 1 结束（Git 行为） | 独立的部分失败语义，需单独评估 Agent 调用方 | 上游用例移植需要 | 待立 issue |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [ ] 所有任务卡满足 `G-*`：无 L/XL 卡、无未登记合并发布，实现写集冲突已按串行消解（含 DEP-IA-02），「任务卡粒度审计表」已填齐。
- [ ] IA-01、IA-02 的 AC 全部满足，且 `Lifecycle=done` **且** `Acceptance=complete`（含 D 组 `release.yml` 证据）。
- [ ] 所有 Verification 命令已运行并记录结果；ADR-IA-01 每个矩阵行都有对应测试与 git 2.54 对照证据。
- [ ] **全量收口门（ER-13）**：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2` 全绿（DEP-IA-01 已满足）。
- [ ] **全量暴露的 Bug 已全部处理（ER-13）**：本计划引起的失败已前滚修复并重跑全量至全绿；既有失败附计划基线 commit 上同样失败的复现证据，并登记 `FIX-*` 或 `DEFER-*`。
- [ ] docs、compat、error-code 同步完成（见「兼容与文档收口」）。
- [ ] 每张卡的 `Rollback mode` 已验证或记录不可验证的原因。
- [ ] Codex review 最终为 `PASS`，P0/P1 全部关闭。
- [ ] 每张卡已 `patch + 1`、版本面五处一致、`Cargo.lock` 仅由工具链刷新，并已用 `gh release create v<version>` 触发 `release.yml` 且取得 Cloudflare 发布证据（ER-08）。
- [ ] 「修订历史」已记录成稿后的全部规范性变更。
- [ ] DEP-IA-04：经用户确认后，已在 #484 回复发布版本、TC-0004/0005 证据与语义更正（或记录用户决定不回复）。
