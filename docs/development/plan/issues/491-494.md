# Issue #491–#494：`add` 更新语义、未合并条目、`--resolved` 与字面 pathspec 修复计划（2026-09-15）

> 模板版本：v2.6；继承 [plan-template.md](../plan-template.md) 的 GC-01..13、ER-01..13、G-01..11。
> 来源（均来自 `genedna/libra-testcases`，对应 git@`1630431f32` 上游测试）：
> - [#491](https://github.com/libra-tools/libra/issues/491)：TC-0022，`add -u` 带未跟踪 pathspec 应整体失败（`t/t2200-add-update.sh:67`）。
> - [#492](https://github.com/libra-tools/libra/issues/492)：TC-0024，全局 `--literal-pathspecs`（`t/t2200-add-update.sh:100`）。
> - [#493](https://github.com/libra-tools/libra/issues/493)：TC-0028，解决冲突期间 `add -u` 的输出与未合并条目（`t/t2200-add-update.sh:203`）。
> - [#494](https://github.com/libra-tools/libra/issues/494)：TC-0032，`add --resolved` 与 `-u`/`-A` 的冲突诊断（`t/t2207-add-resolved.sh:101`）。
>
> 状态：设计计划；全部任务卡 `pending`，尚未进行 Codex review（见「Codex review log」）。

## 文档职责

四个 issue 都落在 `libra add` 与共享 pathspec 引擎上，合并为一份计划，便于在 `src/command/add.rs` 上排定串行顺序。本计划在 libra 0.22.28 与 git 2.54.0 上并排复测，并以 git@`1630431f32` 源码补足 git 2.54 尚未提供的 `add --resolved`。

复测结论：

- **#491 确认。** `add -u baz top`（`baz` 未跟踪）在 Libra 中 exit 0 并暂存 `top`；Git exit 128、报 `pathspec 'baz' did not match any file(s) known to git`、索引不变。根因是 `validate_pathspecs` 把工作树新文件也算作可匹配候选。
- **#492 确认。** Libra 不认识全局 `--literal-pathspecs`，也忽略 `GIT_LITERAL_PATHSPECS`；引擎之外还有 `log/show/checkout/reset/stash/archive` 自带路径匹配。
- **#493 确认且比 issue 描述更严重。** 冲突合并后 `add -u` 完全跳过未合并条目（`conflict.txt` 仍为 `UU`），只报告 `bystander.txt` 被删除。进一步实测发现：显式 `add c.txt`、`add -A`、`add .` 会写入 stage 0，却**保留 stage 1/2/3**，使索引同时存在 stage 0 与冲突 stage（Git 不允许），`status` 反而显示为 `M`。另外 Git 的 `add` 默认不输出，而 Libra 文档化地输出摘要行。
- **#494 确认。** Libra 在参数解析阶段拒绝 `--resolved`（129）。git 2.54 同样没有该参数，git@`1630431f32` 与 `t2207` 定义了完整语义。

本计划交付 6 张卡：AU-01（#491）、AU-02 与 AU-03（#493）、AU-04 与 AU-05（#492）、AU-06（#494），目标是完全解决四个 issue 描述的问题。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `add -u`（以及 `--ignore-errors`、`--dry-run` 组合）对 pathspec 的「已知于索引」校验与原子失败。
- `add` 全部模式（显式路径、`-A`、`.`、`-u`）对未合并条目的暂存：写入 stage 0 时删除 stage 1–3；工作树缺失时删除全部 stage；`-u`/`-A` 候选集包含只有冲突 stage 的路径。
- `add` 默认人读输出与 Git 的静默行为对齐方式（ADR-AU-05）。
- 全局 `--literal-pathspecs` / `--no-literal-pathspecs` 与 `GIT_LITERAL_PATHSPECS`：共享 pathspec 引擎，以及 `log`、`show`、`checkout`、`reset`、`stash`、`archive` 的自带路径匹配。
- `add --resolved [<pathspec>...]`：只暂存未合并路径、冲突标记检查、删除处理、与 `-u`/`-A` 的互斥诊断。
- 同步：`docs/commands/{add,README,...}.md` EN + zh-CN、`docs/development/commands/`、`COMPATIBILITY.md` 相关行、`../libra-backend` 网站文档。

### 非目标

- `--glob-pathspecs`、`--noglob-pathspecs`、`--icase-pathspecs` 全局参数（DEFER-01）。
- `add -n` 预览文本与 Git 的 `add 'x'` / `remove 'x'` 格式对齐（`t2200:145`，DEFER-02）；`add -v` 的 Git 格式（DEFER-03）。
- `conflict-marker-size` 属性（DEFER-04）。
- `update-index --index-info`（`t2200:165` 需要，由 [`issues/479.md`](479.md) UI-01 承接）与 `diff-files --name-only/--name-status`（`t2200:47-60`、`:95`、`:100`、`:203` 的后续断言需要，由 479 DF-01 承接）。
- `count-objects`（`t2200:145` 的对象计数断言，由 [`issues/483.md`](483.md) 承接）。

### 成功定义

- 判据矩阵 M-UPD、M-UNM、M-OUT、M-LIT、M-LITX、M-RES 全部行由子进程集成测试证明，并附 git 2.54（`--resolved` 为 git@`1630431f32` 源码与 `t2207`）对照。
- 上游用例移植通过：`t2200:67-75`（#491）、`:87-110`（#492，含 `diff-files` 断言在 DEP-AU-01 满足后补跑）、`:203-239`（#493）、`t2207:10-106`（#494 全部 7 个用例）。
- 任何 `add` 调用之后，索引中不存在同一路径同时带 stage 0 与冲突 stage 的状态。
- 6 张卡各自完成 C 组发布并取得 D 组证据，然后全量收口门全绿。#491–#494 在全部卡片实现后才关闭（DEP-AU-07）。

## 事实基线

> 核对日 2026-09-15。行号在开工当天刷新。
> - 仓库：`libra status --short --branch` 为 `## main...origin/main`（HEAD `60133c3`），工作区干净；`Cargo.toml` 与本机安装的 `libra` 均为 `0.22.28`，`v0.22.28..HEAD` 无 `src/` 变更，实测结果代表当前源码。
> - Git 对照：`/usr/bin/git` 2.54.0；`add --resolved` 以 git@`1630431f326e15fcde608827b5ff38422528eb59`（本地副本 `/Volumes/Data/competition/git/git`）源码与 `t2207` 为准。该副本工作树 HEAD 为 `47ce805`，所有 Git 行号一律经 `/usr/bin/git -C /Volumes/Data/competition/git/git show 1630431f326e15fcde608827b5ff38422528eb59:<path>` 读取。
> - 实测脚本与输出：`/Volumes/Data/tmp/issue491-494/{cmp,cmp2}.sh` 与 `report{,2}.txt`（隔离 `HOME`、`GIT_CONFIG_NOSYSTEM=1`、`LIBRA_CONFIG_GLOBAL_DB`）。

### issue 断言与复测（2026-09-15）

| Issue | 断言 | git 2.54 | libra 0.22.28 | 结论 |
|---|---|---|---|---|
| #491 | `add -u baz top`（`baz` 未跟踪、`top` 已修改）应非零、报告 `baz`、索引不变 | exit 128，`error: pathspec 'baz' did not match any file(s) known to git`，`diff --cached --name-only` 为空 | exit 0，`add 'top' (modified)`，`top` 已暂存 | 确认（AU-01） |
| #491 | 变体 | `add -u baz` 128 同文案；`add -u 'b*' top` 128 `fatal: pathspec 'b*' did not match any files`；`add -u -n baz top` 先输出 `add 'top'` 后 128；`add -u --ignore-errors baz top` exit 0；`add -u nothere` 128 `fatal: ... did not match any files` | `add -u baz`、`-n`、`--ignore-errors`、`'b*'` 均 exit 0（`nothing to add` 或暂存）；`add -u nothere` 129 `LBR-CLI-003` | 确认 |
| #492 | `libra --literal-pathspecs add -u` 应按字面解释并执行 | 子目录内 `--literal-pathspecs add -u` exit 0；`--literal-pathspecs diff --name-only -- '*.txt'` 只列 `*.txt`，普通模式列 `*.txt` 与 `x.txt`；`GIT_LITERAL_PATHSPECS=1` 等价；`--literal-pathspecs ls-files -- ':(glob)*.txt'` 无输出（magic 失效）；`status`、`grep`、`log` 同样生效；`--literal-pathspecs --no-literal-pathspecs` 恢复 glob；`add --literal-pathspecs`（放在子命令后）129 `unknown option` | 全部 129 `unexpected argument '--literal-pathspecs'`；`GIT_LITERAL_PATHSPECS=1` 被忽略 | 确认（AU-04、AU-05） |
| #493 | 冲突合并后删除 `bystander.txt`，`add -u` 应无 stdout、解决未合并条目 | `add -u` 无输出；`ls-files -u` 为空；`ls-files bystander.txt conflict.txt` 只列 `conflict.txt`；`status --short` 为 `D  bystander.txt`、`M  conflict.txt` | stdout `add 'bystander.txt' (removed)`；`conflict.txt` 的 stage 1/2/3 保留；`status --short` 为 `D  bystander.txt`、`UU conflict.txt` | 确认（AU-02、AU-03） |
| #494 | `add --resolved -u` / `-A` 应报告 `cannot be used together` | git 2.54：129 `unknown option 'resolved'`（尚无该参数）；git@`1630431f32`：`die_for_incompatible_opt3` → `options '-u/--update' and '--resolved' cannot be used together` | 129 `unexpected argument '--resolved'` | 确认（AU-06） |

### 未合并条目暂存真值（2026-09-15，`report2.txt`）

场景：`c.txt` 合并冲突后写入 `resolved`，`k.txt` 另有未暂存修改。

| 调用 | git 2.54 | libra 0.22.28 |
|---|---|---|
| `add c.txt` | `c.txt` 只剩 stage 0 | stage 0 与 stage 1/2/3 **同时存在**；输出 `add 'c.txt' (new file)`；`status` 显示 `M  c.txt` |
| `add -A`、`add .` | 同上，`k.txt` 一并暂存 | 同左（stage 1/2/3 残留），并报告 `2 new` |
| `add -u`、`add -u c.txt` | `c.txt` 只剩 stage 0 | 完全跳过 `c.txt`（`add -u c.txt` 输出 `nothing to add`），仍为 `UU` |
| `add -u -n` | 输出 `add 'c.txt'`、`add 'k.txt'`，索引不变 | 只预览 `k.txt` |
| 冲突标记仍在时 `add -u` | 把含标记内容写入 stage 0（`-u` 不检查标记） | `nothing to add`，保持 `UU` |

根因：`add` 与 `status` 的变更计算只遍历 stage 0 条目（`Index::tracked_files` 调用 `tracked_entries(0)`），`Index::add` 以 `(name, stage)` 为键，写入 stage 0 不会移除冲突 stage。

### 源码与参照

| 类别 | 当前事实 | 证据 |
|---|---|---|
| `add` 参数 | `AddArgs`：`pathspec`、`-A/--all`（`:80`）、`-u/--update`（`:85`）、`-v`（`:97`）、`--ignore-errors`（`:110`）、`--ignore-missing`（`:135`）；无 `--resolved` | `src/command/add.rs:71-135` |
| `add` 主流程 | `run_add` 在空 pathspec 时要求 `-A/-u/--refresh/--renormalize`；变更由 `status::changes_to_be_staged_split_*` 计算；`validate_pathspecs` 后按模式挑选文件，`-u` 时排除新文件；dry-run 分类；逐文件 `stage_a_file` | `src/command/add.rs:480`、`:519`、`:535-545`、`:553-560`、`:600-611`、`:672-700`、`:1346` |
| pathspec 校验 | 可匹配候选 = 可见变更（含新文件）∪ 已跟踪文件；未匹配 → `PathspecNotMatched`（`fatal` + `LBR-CLI-003`，129）；`--ignore-missing` 例外 | `src/command/add.rs:1185-1233`、`:161-162`、`:214` |
| 输出 | `render_add_output`（`--quiet` 抑制）→ `render_normal`：单文件 `add 'x' (kind)`，多文件 `add N files (...)`，`-v` 先逐文件列出；dry-run `add: x` + 页脚；文档把默认摘要列为人读输出契约 | `src/command/add.rs:999-1128`；`docs/commands/add.md:191-212` |
| 变更计算只看 stage 0 | `changes_to_be_staged_split_with_index` 遍历 `index.tracked_files()` | `src/command/status.rs:6658-6667` |
| 索引 API | `update`/`add` 以 `(name, stage)` 插入；`remove(name, stage)`；`tracked_files` = `tracked_entries(0)` | `~/.cargo/registry/.../git-internal-0.8.6/src/internal/index.rs:470-487`、`:531-536` |
| pathspec 引擎 | `Magic{top,exclude,icase,literal,glob}`；`from_workdir*`；`Pathspec::parse` 在 `magic.literal` 或无通配符时用前缀匹配；`parse_magic` 解析 `:(...)`、`:/`、`:!` | `src/utils/pathspec/mod.rs:61-68`、`:71-94`、`:241-270`、`:296-347` |
| 引擎使用方 | `add`、`clean`、`diff`、`grep`、`ls_files`、`remove`、`restore`、`status`、`status_probe` | `rg -l 'pathspec::\|PathspecSet' src` |
| 引擎之外的路径匹配 | `log` 位置参数拆分；`reset` 自带匹配；`stash push` 路径匹配；`archive` 路径过滤；`checkout`、`show` 路径参数 | `src/command/log.rs:217`、`:926-950`；`src/command/reset.rs:580`、`:612`、`:619`、`:698`；`src/command/stash.rs:659`、`:707`；`src/command/archive.rs:293`、`:319`、`:323` |
| 全局参数 | `Cli` 以 `global = true` 声明 `--json/--machine/--no-pager/--color/--no-color/-q/--exit-code-on-warning/--progress/--sync-data/--offline/--max-connections`；`apply_global_runtime_flags` 每次调用都重设进程级状态；`prepare_cli_invocation_state` 读取环境默认 | `src/cli.rs:241-330`、`:2196-2238`、`:2474-2480` |
| TTY 判定先例 | 分页器仅在 stdout 为终端且非测试环境时启用 | `src/utils/pager.rs:182-187` |
| 冲突标记检测 | `diff --check` 的行首标记判断；rerere 的标记检测单测 | `src/command/diff.rs:5015-5020`；`src/command/rerere.rs:796` |
| 相关候选 | plan-20260903 MG-06 已登记 `FIX-MG06-CAND-01`：`libra add -A` 跳过未合并条目，交后续卡 | `docs/development/plan/plan-20260903.md:1393` |
| 文档 | 全局参数表 | `docs/commands/README.md:24-31`；`docs/commands/zh-CN/README.md:20-27`；`../libra-backend/apps/tanstack-app/content/docs/commands/index.en.mdx:15-20` |
| 兼容矩阵行 | add `:182`、rm `:187`、restore `:190`、status `:191`、clean `:192`、stash `:193`、ls-files `:195`、log `:196`、show `:199`、reset `:216`、diff `:227`、grep `:233`、archive `:261`、checkout `:271`；文件 517 行 | `COMPATIBILITY.md` |
| 测试环境 | `.env.live-test` 存在（执行前重核）；`../libra-backend` 为 Git 仓库、分支 `cf` | 2026-09-15 |

外部参照锚点（git@`1630431f32`）：

- **`add`：** `builtin/add.c:37`（`take_worktree_changes`）、`:39`（`add_resolved`）、`:268`、`:270`（`--resolved`）、`:272`、`:278`；`:392-452`（`add_resolved_files`：收集未合并路径、检查标记、缺失删除/存在暂存）；`:509-516`（`-u` 与 `-A`、`--resolved` 互斥）；`:527-529`（`--resolved` 不要求 pathspec）；`:635-647`（非 glob 路径不存在 → `did not match any files`）；`:664-676`（`add_resolved_files` / `add_files_to_cache`；`-u` 且非 `--ignore-errors` 时 `report_path_error` → `exit(128)`）。
- **`dir.c:637-673`**（`report_path_error`：`did not match any file(s) known to git`）；**`read-cache.c:639`**（`remove_file_from_index_with_flags`）、`:823`（`add_file_to_index`）、`:3904`（`fix_unmerged_status`）、`:3987-4024`（`update_callback`）、`:4026`（`add_files_to_cache`）。
- **`merge-ll.c:503-526`**（`has_conflict_markers`：按标记长度逐行判断，遇二进制内容停止）；**`parse-options.c:1538-1566`**（`options '%s' and '%s' cannot be used together` 与三项形式）。
- **全局 pathspec：** `git.c:282-301`；`environment.h:38`；`pathspec.c:257-300`（`get_literal_global` 等）；`Documentation/git.adoc:197-216`。
- **上游测试：** `t/t2200-add-update.sh:19-36`（setup）、`:38-65`、`:67-75`（#491）、`:87-110`（#492）、`:145-163`、`:165-201`、`:203-239`（#493）、`:241-245`；`t/t2207-add-resolved.sh:10-31`、`:33-44`、`:46-57`、`:59-75`、`:77-86`、`:88-99`、`:101-106`（#494）。

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | `add -u` 把未跟踪文件当作可匹配 pathspec，不报错且部分暂存 | 脚本无法发现无效路径（#491） | `add.rs:1185-1233`；复测 #491 | AU-01 |
| GAP-02 | 暂存未合并路径时保留冲突 stage；`-u` 跳过未合并路径 | 索引出现 stage 0 与冲突 stage 并存，冲突无法经 `add -u` 解决（#493） | 未合并条目暂存真值；`status.rs:6665`；`index.rs:474-477` | AU-02 |
| GAP-03 | `add` 默认向 stdout 输出摘要，Git 默认静默 | `t2200:203` 的 `test_must_be_empty out` 失败（#493） | `add.rs:1076-1128`；`add.md:191-212` | AU-03 |
| GAP-04 | 无全局 `--literal-pathspecs` / `GIT_LITERAL_PATHSPECS` | Git 脚本的全局写法直接失败（#492） | `cli.rs:241-330`；`pathspec/mod.rs:241-270` | AU-04 |
| GAP-05 | 引擎之外的命令自带路径匹配，不受全局字面模式影响 | `log -- '*.c'` 等在字面模式下语义不一致（#492） | `log.rs:926-950`；`reset.rs:580-698`；`stash.rs:659`；`archive.rs:293-323` | AU-05 |
| GAP-06 | 无 `add --resolved` | 无法用 Git 接口暂存已解决冲突，也无法得到互斥诊断（#494） | `add.rs:71-135`；`builtin/add.c:392-452` | AU-06 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| [`issues/484.md`](484.md) | IA-01/IA-02 修改 `validate_pathspecs` 的 ignored 与 missing 分类及渲染后退出 | DEP-AU-02：AU-01 与其串行，候选集改动与 IA-02 的缺失分类合并验证 |
| [`issues/489.md`](489.md)、[`issues/483.md`](483.md)、[`issues/490.md`](490.md) | CH-01（`apply_chmod`）、CO-03（dry-run 零对象写入）、SW-03/SW-06（`add.rs` 标志保留与稀疏诊断）同改 `add.rs` | DEP-AU-02 串行 |
| [`issues/477.md`](477.md) | HF-16..HF-19 为 `add` 新增 patch mode；HF-01..HF-03 处理未合并索引上的 cherry-pick/revert；#495 为其子 issue | DEP-AU-02/DEP-AU-03：`add.rs` 串行；AU-02 修复「用 `add` 解决冲突」，是 477 M-UNMERGED 与 #495 流程中「解决后 `libra add`」提示成立的前提 |
| [`issues/479.md`](479.md) | DF-01 提供 `diff-files --name-only/--name-status`；UI-01 提供 `update-index --index-info` | DEP-AU-01：`t2200` 相关断言在其完成后补跑 |
| [`issues/488.md`](488.md) | GR-01/GR-02 修改 `grep` 的 pathspec 作用域 | DEP-AU-04：AU-04 修改共享引擎，与其串行 |
| [`plan-20260903.md`](../plan-20260903.md) | MG-06 登记的 `FIX-MG06-CAND-01`（`add -A` 跳过未合并条目） | AU-02 承接并关闭该候选，在其修订记录中注明 |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | #493 的表象是输出，但复测暴露了索引 stage 并存的数据正确性缺陷，必须先于输出修复 | AU-02 先于 AU-03 |
| 可行性 | 引擎已有字面匹配分支；`add` 已有逐文件暂存与删除路径；冲突标记检测有现成逻辑 | 复用 `Pathspec::parse` 的前缀分支与 `stage_a_file` |
| 任务卡粒度 | 四个 issue 覆盖六个行为轴：`-u` 校验、未合并暂存、默认输出、全局字面模式（引擎）、全局字面模式（引擎外）、`--resolved` | 6 张卡 |
| 依赖与顺序 | `add.rs` 被多份 issue 计划同时修改 | DEP-AU-02 串行；本计划内 AU-01 → AU-02 → AU-03 → AU-06，AU-04 → AU-05 |
| 完整性 | `--ignore-errors`、`-n`、glob、缺失路径、二进制与符号链接冲突都需覆盖 | 判据矩阵列出 |
| 安全性 | 失败路径不得部分写入索引 | GC-AU-02 |
| 功能正确性 | git 2.54 无 `--resolved` | ADR-AU-08 以 pinned 源码与 `t2207` 为准 |
| 接口兼容 | 默认输出与 `branch`/`commit` 等其它命令的输出风格有关联 | ADR-AU-05 给出方案与备选，需用户评审 |
| 可维护性 | 引擎外命令各自匹配 | AU-05 逐命令审计并接入全局开关 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-15 | 初稿自审 + git 2.54 两轮复测 + git@`1630431f32` 源码核对 | 发现 #493 的 stage 并存缺陷并拆出 AU-02；`--resolved` 以 pinned 源码为准 | 无拆卡 | 全文 |
| 2026-09-15 | 协调者指出 Git 锚点读自工作树 HEAD `47ce805` 而非 pinned 提交 | 经 `git show 1630431f32:<path>` 重读全部 Git 锚点：`builtin/add.c` 行号整体修正，`merge-ll.c` 函数范围修正为 `:503-526`；其余文件在两个提交间行号一致 | 无拆卡 | 事实基线、ADR-AU-03/AU-08、AU-01/AU-06、追溯表 |

## 已决议设计决策

实现时若需偏离本节，必须先修改计划并说明原因，不得在代码中静默改语义。

### ADR-AU-01: Git 参照与判据矩阵

- **Status:** Accepted
- **Context:** 四个 issue 均来自上游 t-suite 移植；`--resolved` 在 git 2.54 中不存在。
- **Decision:** 使用事实基线与下列矩阵。开工时对本卡矩阵每行用 `/usr/bin/git` 与构建产物并排实跑（`--resolved` 行对照 pinned 源码与 `t2207`），脱敏证据写入 `LIBRA_TEST_SCRATCH_DIR`。自动化测试不调用系统 git。标注「以 git 实跑为准」的行在实现前写回本 ADR。

  **M-UPD（AU-01，#491）**

  | 行 | 调用（`t2200` setup 后 `reset --hard`，`baz` 未跟踪，`top` 已修改） | 期望 |
  |---|---|---|
  | U1 | `add -u baz top`（`t2200:67-75`） | 非零；stderr 指出 `baz` 不是索引已知路径；`diff --cached --name-only` 为空；索引字节不变 |
  | U2 | `add -u baz`；`add -u top baz` | 同 U1 |
  | U3 | `add -u 'b*' top`（glob 在工作树与索引中都无匹配） | 非零；`pathspec 'b*' did not match any files`；索引不变 |
  | U4 | `add -u nothere` | 非零（沿用 `LBR-CLI-003`）；索引不变（`t2200:241-245`） |
  | U5 | `add -u -n baz top` | 非零；零写入；预览行为按 ADR-AU-03 第 3 条 |
  | U6 | `add -u --ignore-errors baz top` | exit 0；`top` 已暂存，`baz` 未出现（以 git 实跑为准登记 stderr） |
  | U7 | `add -A baz top`；`add baz top` | exit 0；两者均暂存（回归） |
  | U8 | `add -u dir1 dir2`、子目录内 `add -u sub2`、`add -u :/top`（`t2200:38-65`、`:87-93`） | 与 Git 相同的暂存结果（回归） |
  | U9 | `--json` 下 U1 | error envelope，`error_code` 为 `LBR-CLI-003`，stdout 无 data |

  **M-UNM（AU-02，#493）**

  | 行 | 场景（冲突合并后） | 期望（与 git 2.54 一致） |
  |---|---|---|
  | N1 | 解决 `c.txt` 后 `add c.txt` | `ls-files -s c.txt` 只有 stage 0；`status` 为 `M` |
  | N2 | `add -A`；`add .` | 同 N1；其它改动照常暂存 |
  | N3 | `add -u`；`add -u c.txt` | 同 N1（未合并路径进入 `-u` 候选集） |
  | N4 | 冲突标记仍在时 `add -u` | 含标记内容写入 stage 0（`-u` 不检查标记） |
  | N5 | `t2200:203-239`：删除 `bystander.txt` 后 `add -u` | `ls-files -u` 为空；`ls-files bystander.txt conflict.txt` 只列 `conflict.txt`；无 rename 配对 |
  | N6 | 冲突路径在工作树中被删除后 `add -u` / `add -A` | 全部 stage 删除 |
  | N7 | `add -u -n` | 预览包含未合并路径；索引与冲突 stage 不变 |
  | N8 | 任意 `add` 调用后 | 不存在同一路径同时带 stage 0 与 stage 1–3（单元与集成断言） |
  | N9 | `add` 的 `--json` 结果 | 解决的未合并路径计入 `modified`（工作树缺失时计入 `removed`），字段结构不变 |
  | N10 | `t2200:165-201`（`add -u` 解决 path1..path6 各类 stage 组合） | 通过（依赖 DEP-AU-01 的 `--index-info`） |

  **M-OUT（AU-03，#493）**

  | 行 | 场景 | 期望 |
  |---|---|---|
  | O1 | stdout 非终端时 `add a`、`add -u`、N5 的 `add -u` | stdout 为空（`t2200:228` 的 `test_must_be_empty out`） |
  | O2 | stdout 为终端时同 O1 | 输出现有摘要行（`add 'a' (new file)` 等） |
  | O3 | `-v` | 无论是否终端，都输出现有逐文件列表与摘要 |
  | O4 | `--dry-run` | 无论是否终端，都输出现有预览与页脚 |
  | O5 | `--json` / `--machine` | 与改动前逐字节一致 |
  | O6 | 忽略路径警告、`--ignore-missing` 警告 | 仍写 stderr，不受终端判定影响 |
  | O7 | `--quiet` | 行为不变 |

  **M-LIT（AU-04，#492，共享引擎）**

  | 行 | 调用 | 期望（与 git 2.54 一致） |
  |---|---|---|
  | L1 | `t2200:100-110`：子目录内 `--literal-pathspecs add -u` 再普通 `add -u` | 两次都从仓库根更新；exit 0 |
  | L2 | `--literal-pathspecs diff --name-only -- '*.txt'` | 只列字面文件 `*.txt` |
  | L3 | `GIT_LITERAL_PATHSPECS=1 libra diff --name-only -- '*.txt'` | 同 L2 |
  | L4 | `--literal-pathspecs add -- '*.txt'` | 只暂存 `*.txt` |
  | L5 | `--literal-pathspecs ls-files -- ':(glob)*.txt'` | 无输出（magic 被当作字面） |
  | L6 | `--literal-pathspecs status --short -- '*.txt'`；`grep -n s2 -- '*.txt'`；`clean -n -- '*.txt'`；`restore --staged -- '*.txt'`；`rm --cached -- '*.txt'` | 只作用于字面路径 |
  | L7 | `--literal-pathspecs --no-literal-pathspecs ...`；`GIT_LITERAL_PATHSPECS=1` 加 `--no-literal-pathspecs` | 后者生效，恢复通配 |
  | L8 | `add --literal-pathspecs -u`（放在子命令后） | 按 ADR-AU-06 第 2 条 |
  | L9 | 未设置时 | 全部引擎使用方行为与改动前一致 |
  | L10 | `GIT_LITERAL_PATHSPECS=yes/0/invalid` | 按 ADR-AU-06 第 3 条 |

  **M-LITX（AU-05，#492，引擎之外）**

  | 行 | 调用（字面模式开启） | 期望（与 git 2.54 一致） |
  |---|---|---|
  | X1 | `log --oneline -- '*.txt'` | 只匹配字面路径 |
  | X2 | `show --stat HEAD -- '*.txt'` | 同上 |
  | X3 | `checkout -- '*.txt'`；`restore`（若走引擎则由 L6 覆盖） | 同上 |
  | X4 | `reset -- '*.txt'` | 同上 |
  | X5 | `stash push -- '*.txt'` | 同上 |
  | X6 | `archive HEAD -- '*.txt'` | 以 git 实跑为准登记 |
  | X7 | 各命令未开启字面模式时 | 与改动前一致 |

  **M-RES（AU-06，#494）**

  | 行 | 场景（`t2207` setup：三文件冲突） | 期望（git@`1630431f32` / `t2207`） |
  |---|---|---|
  | R1 | `t2207:33-44`：只解决 file1 后 `add --resolved` | 失败；stderr 含 `the following paths still have conflict markers:` 与 `file2.txt`、`file3.txt`；三个文件的冲突 stage 均保留（零写入） |
  | R2 | `t2207:46-57`：全部解决后 `add --resolved` | exit 0；`ls-files -u` 为空；三个文件各有 stage 0 |
  | R3 | `t2207:59-75`：另有未冲突的本地修改 | 该文件不被暂存 |
  | R4 | `t2207:77-86`：冲突文件被删除 | 从索引删除 |
  | R5 | `t2207:88-99`：`add --resolved file1.txt` | 只处理 pathspec 内的未合并路径 |
  | R6 | `t2207:101-106`：`add --resolved -u`、`add --resolved -A` | 失败；stderr 含 `cannot be used together` |
  | R7 | 无未合并条目时 `add --resolved` | exit 0，零写入 |
  | R8 | `--dry-run --resolved` | 预览将解决的路径；零写入；标记检查同样生效 |
  | R9 | 二进制冲突文件、符号链接冲突 | 二进制在遇到二进制内容处停止检查；非普通文件不检查标记（与 `builtin/add.c:418-426` 一致） |
  | R10 | `--resolved -p`；`--json --resolved` | 前者用法错误（ADR-AU-08 第 5 条）；后者输出与普通 `add` 相同结构 |

- **Alternatives considered:** 仅修复 issue 列出的单条命令——#493 的 stage 并存与 #492 的引擎外命令会遗留，拒绝。
- **Consequences:** 矩阵行变化时同批修改 ADR、测试与修订历史。
- **Revisit when:** 上游 `t2200`/`t2207` 变化。

### ADR-AU-02: 退出码与稳定错误码

- **Status:** Accepted
- **Context:** Git 对 #491、`--resolved` 冲突标记与互斥分别 `exit(128)` / `die`；[`issues/479.md`](479.md) ADR-PL-02 规定 Libra 保留「用法与无效目标 129、运行期 128」。
- **Decision:**
  1. #491 的「pathspec 不是索引已知路径」沿用 `PathspecNotMatched` 的 `LBR-CLI-003`（129），文案区分两种情况（ADR-AU-03 第 2 条）。
  2. `--resolved` 与 `-u`/`-A` 互斥为 `LBR-CLI-002`（129），文案使用 Git 原文。
  3. `--resolved` 发现冲突标记为 `LBR-CONFLICT-001`（128）。
  4. 不新增稳定错误码；与 Git 128 的差异在 `COMPATIBILITY.md:182` 登记。
- **Alternatives considered:** 对齐 Git 的 128——与 479/477 的退出码决策冲突，而上游测试只要求非零，拒绝。
- **Consequences:** 脚本通过 `Error-Code` 与 JSON `error_code` 区分。
- **Revisit when:** 稳定错误码表进行 major 调整。

### ADR-AU-03: `add -u` 的 pathspec 校验（#491）

- **Status:** Accepted
- **Context:** GAP-01；git `builtin/add.c:635-676`、`dir.c:637-673`。
- **Decision:**
  1. `-u` 模式（含 `--renormalize` 以外的 `-u` 调用）的可匹配候选集为索引已知路径（任意 stage）与已跟踪但工作树删除的路径；工作树中的未跟踪新文件不再算作匹配。
  2. 未匹配时报错：pathspec 在工作树与索引中都无匹配 → `pathspec '<p>' did not match any files`（现有文案）；只匹配到未跟踪文件 → `pathspec '<p>' did not match any file(s) known to the index`。
  3. 校验在任何暂存与预览输出之前完成；`-n` 下同样先失败、零输出（与 Git「先预览后报错」不同，登记为有意差异，换取原子性）。
  4. `--ignore-errors` 下跳过该校验，只暂存能匹配的路径（与 Git 一致）；stderr 行为以 M-UPD U6 实跑为准。
  5. 同一 pathspec 重复出现且其中一个已匹配时不报错（`dir.c:650-667` 的去重规则）。
- **Alternatives considered:** 暂存可匹配路径后再报错——违反 #491 验收 3「索引字节不变」，拒绝。
- **Consequences:** 改动集中在 `validate_pathspecs` 的候选集，与 484 IA-02 串行（DEP-AU-02）。
- **Revisit when:** 无。

### ADR-AU-04: 未合并条目的暂存（#493）

- **Status:** Accepted
- **Context:** GAP-02；git `read-cache.c:3904`、`:3987-4024`、`:4026`；未合并条目暂存真值。
- **Decision:**
  1. **索引不变量：** 任一路径写入 stage 0 时，同一事务内删除该路径的 stage 1–3；删除一个路径时删除全部 stage。实现为 `add` 暂存路径的单一 helper，`stage_a_file` 与删除分支都经过它。
  2. **候选集：** `add` 的变更计算把「只有冲突 stage 的路径」纳入：工作树存在 → 视为待暂存修改；工作树缺失 → 视为删除。`-u`、`-A`、`.` 与显式 pathspec 共用该候选集。
  3. **标记检查：** 普通 `add` 不检查冲突标记（Git 一致）；只有 `--resolved`（ADR-AU-08）检查。
  4. **不做 rename 配对：** `add` 的候选计算不进行 rename 检测（与 `t2200:203` 的意图一致）。
  5. **报告分类：** 解决的未合并路径在人读摘要与 JSON 中计入 `modified`，工作树缺失时计入 `removed`；不再报告为 `new file`。
  6. **范围：** 只修改 `add` 使用的变更计算入口；`status` 与 `commit -a` 共享的计算函数若需改动，按 GC-AU-03 保持其对未合并路径的既有显示（`UU`）不变。
- **Alternatives considered:** 在 git-internal 的 `Index::add` 中自动删除冲突 stage——改变所有下游写入方（merge 需要写入冲突 stage），拒绝。
- **Consequences:** 承接 plan-20260903 的 `FIX-MG06-CAND-01`；AU-02 命中 T-1（共享变更计算）。
- **Revisit when:** 无。

### ADR-AU-05: `add` 默认人读输出（#493）

- **Status:** Accepted（需用户评审）
- **Context:**
  - Git 的 `add` 除 `-v`、`-n` 外不向 stdout 输出；`t2200:228` 断言 stdout 为空。
  - Libra 把默认摘要行写入 stdout，并在 `docs/commands/add.md:191-212` 文档化；Agent 与脚本应使用 `--json`/`--machine`。
  - Libra 已有「stdout 为终端才启用」的先例（`src/utils/pager.rs:182-187`）。
- **Decision:**
  1. 默认模式下，只有 stdout 为终端时才输出摘要行；stdout 被重定向或为管道时不输出（与 Git 一致）。
  2. `-v`、`--dry-run` 属于用户显式请求的输出，任何情况下都输出现有内容。
  3. `--json`、`--machine`、`--quiet`、stderr 警告不受影响。
  4. 判定集中在 `render_add_output`，并提供测试可控的判定开关（与 `LIBRA_TEST` 分页器处理一致），不在各分支重复检查终端。
- **Alternatives considered:**
  - 始终静默，摘要只在 `-v` 下输出——完全对齐 Git，但去掉了交互用户已习惯的确认信息；作为备选，若用户评审选择它，只需把第 1 条改为「始终不输出」。
  - 保持现状并把 `t2200:203` 登记为有意差异——无法满足 #493 验收 1，拒绝。
- **Consequences:** 依赖默认摘要的既有集成测试需改为断言非终端静默或改用 `-v`/`--json`（ER-AU-02 统计并同批更新）；`add.md` 的 Human Output 小节改写。
- **Revisit when:** 其它命令（如 `rm`、`mv`）也需要统一「非终端静默」策略时。

### ADR-AU-06: 全局 `--literal-pathspecs`（#492，共享引擎）

- **Status:** Accepted
- **Context:** GAP-04；git `git.c:282-289`、`pathspec.c:257-300`、`Documentation/git.adoc:197-200`。
- **Decision:**
  1. **参数：** 在 `Cli` 新增 `--literal-pathspecs` 与 `--no-literal-pathspecs`，后者覆盖前者；`apply_global_runtime_flags` 每次调用都设置进程级字面模式状态（未指定时由环境变量决定），避免复用进程继承旧状态。
  2. **位置：** 与 Libra 其它全局参数一致使用 `global = true`，放在子命令之后也接受（Git 在子命令后报 129），登记为有意差异。
  3. **环境变量：** 读取 `GIT_LITERAL_PATHSPECS`，`1/true/yes/on` 为开启，`0/false/no/off` 与未设置为关闭；无法识别的值按关闭处理并输出一次 warning。命令行参数优先于环境变量。
  4. **引擎语义：** 字面模式开启时 `Pathspec::parse` 不解析 magic（`:(...)`、`:/`、`:!`、`:^` 均按字面路径），不做通配，使用前缀匹配；`--pathspec-from-file` 读入的路径同样适用。
  5. **范围：** 共享引擎的全部使用方自动生效（M-LIT L6）；引擎之外的命令由 AU-05 处理。
- **Alternatives considered:** 只为 `add` 实现——#492 验收 1 要求全局语义，且 Git 的 `log`/`status`/`grep` 同样生效，拒绝。在子命令之后报错以对齐 Git——与 Libra 全局参数惯例不一致，拒绝。
- **Consequences:** AU-04 命中 T-1（`src/cli.rs` 全局参数与共享引擎）。
- **Revisit when:** DEFER-01 排期。

### ADR-AU-07: 引擎之外的路径匹配（#492）

- **Status:** Accepted
- **Context:** GAP-05；`log`、`show`、`checkout`、`reset`、`stash`、`archive` 各自处理路径参数。
- **Decision:**
  1. 开工时逐命令登记其匹配方式（前缀、glob、引擎）。
  2. 已支持通配或 magic 的命令在字面模式开启时改为字面匹配，优先改为调用共享引擎；只做前缀匹配的命令记录为「天然字面」，补测试证明结果与 Git 一致。
  3. 不改变这些命令在字面模式关闭时的现有行为（M-LITX X7）。
- **Alternatives considered:** 一次性把六个命令迁移到共享引擎——超出 #492 范围且风险高，拒绝。
- **Consequences:** 审计结果写入 AU-05 执行记录与各命令开发文档。
- **Revisit when:** 命令迁移到共享引擎时。

### ADR-AU-08: `add --resolved`（#494）

- **Status:** Accepted
- **Context:** GAP-06；git@`1630431f32` `builtin/add.c:270`、`:392-452`、`:509-516`、`:527-529`、`merge-ll.c:503-526`、`parse-options.c:1538-1566`；`t2207`。git 2.54 尚无该参数。
- **Decision:**
  1. **参数：** 新增 `--resolved`；不要求 pathspec，给出 pathspec 时只处理匹配的路径。
  2. **收集：** 取索引中带冲突 stage 的路径（按 pathspec 过滤、去重）；没有则 exit 0 且零写入。
  3. **标记检查：** 对工作树中存在的普通文件逐行检查冲突标记（行首连续 7 个 `<`、`=`、`>` 或 `|`，其后为空白或行尾），遇到二进制内容停止；任一文件仍含标记时，列出全部这类路径并以 `LBR-CONFLICT-001` 失败，零写入。
  4. **暂存：** 通过 ADR-AU-04 的 helper：工作树缺失 → 删除全部 stage；存在 → 写入 stage 0 并删除冲突 stage。
  5. **互斥：** 与 `-u`、`-A` 同用时报 `options '-u/--update' and '--resolved' cannot be used together`（三项同用时使用 Git 的三项文案），`LBR-CLI-002`；与 `-p`/`--patch`（477 HF-16..HF-19 提供）同用时同样报用法错误（Git 中 patch 分支先执行、`--resolved` 被忽略，登记为有意差异）。
  6. **组合：** `--dry-run` 预览并执行标记检查；输出与 JSON 结构沿用 `add`（ADR-AU-05）。
- **Alternatives considered:** 登记为有意不支持——#494 验收 1 允许，但 pinned Git 已实现且语义清晰，拒绝。
- **Consequences:** `add.md` 与 `COMPATIBILITY.md:182` 新增说明；`conflict-marker-size` 属性延后（DEFER-04）。
- **Revisit when:** DEFER-04 排期。

## 全局工程约束

继承模板 GC-01..GC-13（GC-13 为 N/A：不触及 SQLite schema）。计划级补充：

- **GC-AU-01 并排判据:** 每张卡开工时对本卡矩阵用 `/usr/bin/git` 与构建产物并排实跑，`--resolved` 行对照 git@`1630431f32` 源码与 `t2207`；脱敏证据写入 `LIBRA_TEST_SCRATCH_DIR`（持久介质）；自动化测试不调用系统 git。
- **GC-AU-02 失败零写入:** 所有失败路径（M-UPD U1–U5、M-RES R1、R6、R10）断言索引字节不变。
- **GC-AU-03 共享计算不回归:** AU-02 若修改 `status` 与 `commit -a` 共享的变更计算，必须证明 `status` 对未合并路径的 `UU` 显示与 `commit -a` 的拒绝行为不变。
- **GC-AU-04 行锚不变:** `COMPATIBILITY.md` 只在原物理行追加，文件保持 517 行；`compat_ledger_schema` 必须通过。
- **GC-AU-05 串行登记:** 新测试优先不使用 `ChangeDirGuard`；需要 `#[serial(cwd)]` 或 `#[serial(env)]`（`GIT_LITERAL_PATHSPECS`）时同步 `tests/SERIAL_REGISTRY.tsv` 并重跑 `sh tests/NEXTEST_GROUPS.sh`。

## 执行检查必备需求（强制）

完整继承模板 ER-01..ER-13。计划级补充：

- **ER-AU-01:** 命中 ER-13 全量触发条件的 C④ 与计划收口门，必须在 DEP-AU-05 重核后执行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`；不得以 `cargo test --all` 替代。
- **ER-AU-02:** AU-03 开工时先统计依赖 `add` 默认摘要输出的既有测试与文档示例，列入执行记录并同批更新；若用户评审选择 ADR-AU-05 的备选方案，先修订 ADR 再实现。
- **ER-AU-03:** AU-02 开工时确认 `status`/`commit -a` 是否共用被修改的计算入口；共用时补 GC-AU-03 回归。
- **ER-AU-04:** 标注「以 git 实跑为准」的矩阵行，在实现前把实测期望写回 ADR-AU-01。

## 实施顺序

- `DEP-AU-02 -> AU-01`
- `AU-01 -> AU-02`
- `DEP-AU-03 -> AU-02`
- `AU-02 -> AU-03`
- `AU-03 -> AU-06`
- `DEP-AU-04 -> AU-04`
- `AU-04 -> AU-05`
- `DEP-AU-05 -> AU-02`（仅约束 C④ 与收口门；其它命中 T-* 的卡同理）

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-AU-01 | incoming | 跨计划能力 | [`issues/479.md`](479.md) DF-01（`diff-files --name-only/--name-status`）与 UI-01（`update-index --index-info`） | 单一发布者 | 两卡为 `done/complete` | 2026-09-15 均为 `pending`；复测中 `diff-files --name-only` 为 129 | 未满足时本计划的矩阵断言改用 `diff --name-only` / `status --short` 等价证明，并在其完成后补跑 `t2200` 原始断言；不得宣称上游用例完整移植 |
| DEP-AU-02 | incoming | 跨计划写集互斥 | `add.rs`：[`issues/484.md`](484.md) IA-01/IA-02、[`issues/489.md`](489.md) CH-01、[`issues/483.md`](483.md) CO-03、[`issues/490.md`](490.md) SW-03/SW-06、[`issues/477.md`](477.md) HF-16..HF-19 | 单一发布者 | 相交卡不处于 `in-progress`；发布不交错 | 2026-09-15 均为 `pending` | 后开工者等待并按 GC-01 刷新锚点；IA-02 已落地时 AU-01 在其缺失分类之上修改候选集 |
| DEP-AU-03 | incoming | 跨计划语义协调 | [`issues/477.md`](477.md) HF-01..HF-03（未合并索引上的 cherry-pick/revert）与子 issue #495；[`plan-20260903.md`](../plan-20260903.md) `FIX-MG06-CAND-01` | 单一发布者 / plan-20260903 执行者 | 语义不冲突：AU-02 使「解决后 `libra add`」可靠；plan-20260903 修订记录标注该候选由 AU-02 承接 | `plan-20260903.md:1393` | 冲突时以本计划 ADR-AU-04 为准并同步对方计划 |
| DEP-AU-04 | incoming | 跨计划写集互斥 | [`issues/488.md`](488.md) GR-01/GR-02（`grep` pathspec）；其它 issue 计划的 `COMPATIBILITY.md` 写集 | 单一发布者 | 相交卡不处于 `in-progress` | 2026-09-15 均为 `pending` | AU-04 与其串行，按 GC-AU-04 追加 |
| DEP-AU-05 | incoming | 环境前置 | 仓库根 `.env.live-test` | 发布者 / 仓库维护者 | 文件存在，且 `source .env.test && source .env.live-test` 返回 0 | 2026-09-15 存在；执行前重核 | 失效时命中 T-* 的卡停在 `locally-accepted`，收口门 `blocked`；不得降级 |
| DEP-AU-06 | incoming | 外部仓库 | `../libra-backend` 网站文档 | docs owner | ER-06a：Git 仓库、`cf` 分支，写入前重核 | 2026-09-15 核对 | 阻塞时该卡 `blocked` |
| DEP-AU-07 | outgoing | issue 回复 / 外部用例 | libra-tools/libra#491、#492、#493、#494；`genedna/libra-testcases` TC-0022、TC-0024、TC-0028、TC-0032 | 用户 / 用例仓库维护者 | 复测结论、各卡发布版本、上游用例移植证据；#491–#494 只在本计划全部卡片 `done/complete` 后关闭 | issue #491–#494 | 对外动作，回复与关闭前须经用户确认 |

### 发布分组与并发窗口

无 `REL-*`：6 张卡均为 `independent`，各自 `patch + 1` 并用 `gh` 发布。

**并发声明:**
- AU-01、AU-02、AU-03、AU-06 共享 `add.rs`，串行。
- AU-04（`src/cli.rs`、`src/utils/pathspec/mod.rs`）与 AU-01..AU-03 的实现写集不相交，可并发实现；AU-05 与 AU-04 串行。
- 所有卡共享 `COMPATIBILITY.md`，发布串行。

**发布者:** 执行本计划的主 Agent（单一发布者），与其它 issue 计划共用同一串行发布队列。

**发布窗口顺序:** AU-01 → AU-02 → AU-03 → AU-04 → AU-05 → AU-06。同一时刻只允许一张卡处于「已 bump 未完成推送 / 未触发 release」状态。版本号在进入窗口时从 `Cargo.toml` 读取（核对日基线 `0.22.28`）。

### Phase 0: 基线冻结

**目标:** 计划通过 Codex review；ADR-AU-05 经用户评审；DEP 状态确认。

**进入条件:**

- 本计划成稿。

**退出条件:**

- 「Codex review log」有一轮 `PASS`；ADR-AU-05 的方案已确认；DEP-AU-01..06 状态已记录。

### Phase 1: `add` 更新语义与未合并条目

**目标:** AU-01、AU-02、AU-03 发布。

**进入条件:**

- Phase 0 退出；DEP-AU-02、DEP-AU-03 满足。

**退出条件:**

- 三张卡为 `done/complete`；M-UPD、M-UNM、M-OUT 全绿。

### Phase 2: 字面 pathspec

**目标:** AU-04、AU-05 发布。

**进入条件:**

- Phase 0 退出；DEP-AU-04 满足。

**退出条件:**

- 两张卡为 `done/complete`；M-LIT、M-LITX 全绿。

### Phase 3: `add --resolved`

**目标:** AU-06 发布。

**进入条件:**

- AU-03 为 `done/complete`。

**退出条件:**

- AU-06 为 `done/complete`；`t2207` 全部移植用例通过；全量收口门全绿。

## 任务卡

### 字段全局默认与例外

- **Release boundary 默认:** `independent`。
- **Task type 默认:** `implementation`。
- **Rollback mode 默认:** `revert`（源码单提交 revert；已发布 artifact 按 ER-08 发新 patch 前滚）。
- **Migration and rollback 默认:** N/A（不改 SQLite schema；索引格式不变）。
- **Security and privacy 默认:** 继承 GC-07、GC-11；失败路径零写入（GC-AU-02）。
- **Version increment 默认:** `patch`。
- **C/D coverage from 默认:** `self`。
- **D 组默认:** `.github/workflows/release.yml`，由 `gh release create "v${VERSION}"`（`push` `v*` tag）触发；判据为 run 全部 job 成功，且 `download.libra.tools` 能取到对应版本。
- **Lifecycle / Acceptance 默认:** `pending` / 空。

**默认覆盖:** 无。各卡字段均取默认值或在卡内写明。

**规则 waiver:** 无 `EX-*`。

### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AU-01 | implementation | `add -u` pathspec 的索引已知校验 | revert 单提交 | yes | yes | 6/8 | 3/8 | 1/1 | S | DEP-AU-02 | 序列化首位 | independent | N/A | N/A |
| AU-02 | implementation | `add` 暂存未合并条目 | revert 单提交 | yes | yes | 7/8 | 5/8 | 1/2 | M | AU-01, DEP-AU-03, DEP-AU-05 | 序列化于 AU-01 | independent | N/A | N/A |
| AU-03 | implementation | `add` 默认人读输出 | revert 单提交 | yes | yes | 6/8 | 3/8 | 1/1 | S | AU-02 | 序列化于 AU-02 | independent | N/A | N/A |
| AU-04 | implementation | 全局字面 pathspec（共享引擎） | revert 单提交 | yes | yes | 7/8 | 5/8 | 2/2 | M | DEP-AU-04, DEP-AU-05 | 与 AU-01..AU-03 不相交 | independent | N/A | N/A |
| AU-05 | implementation | 全局字面 pathspec（引擎之外） | revert 单提交 | yes | yes | 7/8 | 5/8 | 1/6 | M | AU-04 | 序列化于 AU-04 | independent | N/A | N/A |
| AU-06 | implementation | `add --resolved` | revert 单提交 | yes | yes | 7/8 | 4/8 | 1/1 | M | AU-03 | 序列化于 AU-03 | independent | N/A | N/A |

### Task AU-01: `add -u` pathspec 必须是索引已知路径（#491）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-AU-03，把 `add -u` 的 pathspec 可匹配候选限定为索引已知路径与已跟踪的删除，未匹配时在任何暂存与预览之前失败，`--ignore-errors` 下跳过校验。唯一行为轴是「`add -u` pathspec 的索引已知校验」。来源 issue：#491。

**Out of scope:** 未合并路径进入候选集（由 AU-02 承接）；预览文本格式（DEFER-02）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 候选集包含工作树新文件 | `src/command/add.rs:1185-1233` |
| `-u` 在挑选阶段才排除新文件 | `src/command/add.rs:600-611` |
| 实测 | 事实基线 #491 两行 |
| Git 语义 | git@1630431f32 `builtin/add.c:635-676`；`dir.c:637-673`；`t2200:67-75`、`:241-245` |

**Acceptance criteria:**

- [ ] 判据矩阵 M-UPD U1–U2（未跟踪 pathspec 整体失败、索引不变）。
- [ ] glob 与缺失路径通过 M-UPD U3–U4。
- [ ] 预览与 `--ignore-errors` 通过 M-UPD U5–U6。
- [ ] 回归通过 M-UPD U7–U8。
- [ ] 结构化输出通过 M-UPD U9。
- [ ] ER-06a 同步：`docs/commands/add.md` 与 zh-CN 的 `-u` 说明与错误表、`COMPATIBILITY.md:182` 原行追加（含 ADR-AU-02/03 的有意差异）、网站 `add.en.md`。

**Verification:**

- [ ] `source .env.test && cargo test --lib command::add`（new：候选集与文案分类单测）
- [ ] `source .env.test && cargo test --test command_test add_test`（new：`test_add_update_untracked_pathspec_fails_atomically_matrix`，含 `t2200:67-75`、`:241-245` 移植）
- [ ] `source .env.test && cargo test --test compat_ledger_schema`

**Full-suite trigger:** `none`

**Dependencies:** `DEP-AU-02`

**Deliverables:** N/A

**Implementation write set:** `src/command/add.rs`、`tests/command/add_test.rs`、`docs/commands/add.md`、`docs/commands/zh-CN/add.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** add 命令页 EN + zh-CN + 网站页；`COMPATIBILITY.md:182` 追加；错误码 N/A（沿用 `LBR-CLI-003`）。后端写入前记录仓库类型与 `cf` 分支证据（DEP-AU-06）。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 修复部分暂存导致的静默数据差异。

**Performance budget:** 候选集改为索引路径，成本不高于现状。

**Estimated scope:** `S`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=add -u pathspec 的索引已知校验; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=DEP-AU-02; writeset=序列化首位; release=independent; split-from=N/A; exception=N/A`

### Task AU-02: `add` 正确暂存未合并条目（#493）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-AU-04，引入「暂存路径时删除冲突 stage」的单一 helper，并让 `add` 的候选集包含只有冲突 stage 的路径，使显式路径、`-A`、`.`、`-u` 都能解决冲突且不留下 stage 并存。唯一行为轴是「`add` 暂存未合并条目」。来源 issue：#493；承接 `FIX-MG06-CAND-01`。

**Out of scope:** 默认输出（由 AU-03 承接）；`--resolved`（由 AU-06 承接）；`update-index` 的冲突处理（479 UI-01）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 变更计算只遍历 stage 0 | `src/command/status.rs:6658-6667` |
| 索引按 `(name, stage)` 写入 | `git-internal-0.8.6/src/internal/index.rs:470-487`、`:531-536` |
| 暂存与删除入口 | `src/command/add.rs:655-700`、`:1346` |
| 实测 stage 并存与 `-u` 跳过 | 事实基线「未合并条目暂存真值」；#493 行 |
| Git 语义 | git@1630431f32 `read-cache.c:639`、`:823`、`:3904`、`:3987-4024`、`:4026`；`t2200:165-201`、`:203-239` |

**Acceptance criteria:**

- [ ] ER-AU-03 判定已记录（共享计算入口与回归范围）。
- [ ] 判据矩阵 M-UNM N1–N3（显式、`-A`、`.`、`-u` 解决冲突）。
- [ ] 标记不检查与上游用例通过 M-UNM N4–N5。
- [ ] 删除与预览通过 M-UNM N6–N7。
- [ ] 索引不变量通过 M-UNM N8。
- [ ] 报告分类通过 M-UNM N9；`t2200:165-201` 在 DEP-AU-01 满足后通过 N10（未满足时按其失败策略登记）。
- [ ] ER-06a 同步：`docs/commands/add.md` 与 zh-CN 说明未合并条目的暂存行为、`docs/development/commands/add.md`、`COMPATIBILITY.md:182` 原行追加、网站 `add.en.md`；`plan-20260903.md` 修订记录注明 `FIX-MG06-CAND-01` 由 AU-02 承接。

**Verification:**

- [ ] `source .env.test && cargo test --lib command::add`（new：冲突 stage 清理 helper 与候选集单测）
- [ ] `source .env.test && cargo test --test command_test add_test`（new：`test_add_resolves_unmerged_entries_matrix`、`test_t2200_add_u_avoids_rename_pairing_on_unmerged_paths`）
- [ ] `source .env.test && cargo test --test command_test status_test`（回归：`UU` 显示）
- [ ] `source .env.test && cargo test --test command_test commit_test`（回归：未解决冲突时 `commit -a` 拒绝）
- [ ] `source .env.test && cargo test --test command_test merge_test`（回归：冲突后 `add` + `merge --continue`）

**Full-suite trigger:** `T-1: 修改被 status 与 commit -a 共享的工作树变更计算入口`

**Dependencies:** `AU-01`（`validate_pathspecs` 写集串行）、`DEP-AU-03`、`DEP-AU-05`（仅约束 C④）

**Deliverables:** N/A

**Implementation write set:** `src/command/add.rs`、`src/command/status.rs`、`tests/command/add_test.rs`、`tests/command/status_test.rs`、`tests/command/commit_test.rs`、`tests/command/merge_test.rs`、`docs/commands/add.md`、`docs/commands/zh-CN/add.md`、`docs/development/commands/add.md`、`docs/development/plan/plan-20260903.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** add 命令页 EN + zh-CN + 网站页；开发文档；`COMPATIBILITY.md:182` 追加；plan-20260903 修订记录；错误码 N/A。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 修复索引 stage 并存的数据正确性缺陷。

**Performance budget:** 额外一次冲突 stage 扫描 O(索引条目)。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=add 暂存未合并条目; recovery=revert 单提交; complete=yes; self-contained=yes; AC=7/8; VER=5/8; landing=1; prod-files=2; scope=M; deps=AU-01,DEP-AU-03,DEP-AU-05; writeset=序列化于 AU-01; release=independent; split-from=N/A; exception=N/A`

### Task AU-03: `add` 默认人读输出与 Git 的静默对齐（#493）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-AU-05，stdout 非终端时 `add` 默认模式不输出摘要；终端、`-v`、`--dry-run`、结构化输出与 stderr 警告保持现状。唯一行为轴是「`add` 默认人读输出」。来源 issue：#493。

**Out of scope:** 预览与 `-v` 的 Git 文本格式（DEFER-02、DEFER-03）；其它命令的输出策略。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 输出入口与摘要格式 | `src/command/add.rs:999-1128` |
| 文档化的默认摘要 | `docs/commands/add.md:191-212` |
| 终端判定先例 | `src/utils/pager.rs:182-187` |
| Git 语义 | `t2200:228`；复测 A0（Git `add`/`add -u` 无输出，`-v` 输出 `add 'a'`） |

**Acceptance criteria:**

- [ ] ER-AU-02 统计与 ADR-AU-05 方案确认已记录。
- [ ] 判据矩阵 M-OUT O1（非终端静默，含 `t2200:203-239` 的输出断言）。
- [ ] 终端摘要通过 M-OUT O2。
- [ ] 显式输出通过 M-OUT O3–O4。
- [ ] 结构化、警告与 `--quiet` 通过 M-OUT O5–O7。
- [ ] ER-06a 同步：`docs/commands/add.md` 与 zh-CN 改写 Human Output 小节、`COMPATIBILITY.md:182` 原行追加、网站 `add.en.md`；依赖默认摘要的既有测试同批更新。

**Verification:**

- [ ] `source .env.test && cargo test --lib command::add`（new：输出判定单测）
- [ ] `source .env.test && cargo test --test command_test add_test`（new：`test_add_default_output_silent_when_not_terminal_matrix`；既有输出断言更新）
- [ ] `source .env.test && cargo test --test command_test add_json_test`（回归）

**Full-suite trigger:** `none`

**Dependencies:** `AU-02`（`add.rs` 写集串行；N5 输出断言依赖 AU-02）

**Deliverables:** N/A

**Implementation write set:** `src/command/add.rs`、`tests/command/add_test.rs`、`tests/command/add_json_test.rs`、`docs/commands/add.md`、`docs/commands/zh-CN/add.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set；ER-AU-02 统计出的其它测试文件

**Docs and compatibility impact:** add 命令页 EN + zh-CN + 网站页；`COMPATIBILITY.md:182` 追加；错误码 N/A。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** Inherited

**Performance budget:** 一次终端判定。

**Estimated scope:** `S`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=add 默认人读输出; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=AU-02; writeset=序列化于 AU-02; release=independent; split-from=N/A; exception=N/A`

### Task AU-04: 全局 `--literal-pathspecs`（共享引擎，#492）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-AU-06，新增全局 `--literal-pathspecs` / `--no-literal-pathspecs`，读取 `GIT_LITERAL_PATHSPECS`，每次调用设置进程级字面模式，并让共享 pathspec 引擎在字面模式下不解析 magic、不做通配。唯一行为轴是「全局字面 pathspec（共享引擎）」。来源 issue：#492。

**Out of scope:** 引擎之外的命令（由 AU-05 承接）；`--glob/--noglob/--icase-pathspecs`（DEFER-01）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 全局参数与运行期设置 | `src/cli.rs:241-330`、`:2196-2238`、`:2474-2480` |
| 引擎的 magic 与字面分支 | `src/utils/pathspec/mod.rs:61-68`、`:241-270`、`:296-347` |
| 引擎使用方 | `add`、`clean`、`diff`、`grep`、`ls_files`、`remove`、`restore`、`status`、`status_probe` |
| 实测 | 事实基线 #492 行 |
| Git 语义 | git@1630431f32 `git.c:282-289`；`pathspec.c:257-300`；`Documentation/git.adoc:197-200`；`t2200:100-110` |

**Acceptance criteria:**

- [ ] 判据矩阵 M-LIT L1（`t2200:100-110` 的两次 `add -u` 均执行；`diff-files` 断言按 DEP-AU-01 处理）。
- [ ] 字面匹配通过 M-LIT L2–L4。
- [ ] magic 失效通过 M-LIT L5。
- [ ] 其它引擎使用方通过 M-LIT L6。
- [ ] 开关优先级、参数位置与环境变量取值通过 M-LIT L7、L8、L10。
- [ ] 未开启时回归通过 M-LIT L9。
- [ ] ER-06a 同步：`docs/commands/README.md` 与 zh-CN 全局参数表新增两项、`docs/commands/add.md` 与 zh-CN pathspec 小节注明、`COMPATIBILITY.md:182`、`:191`、`:195`、`:227`、`:233` 原行追加（含 ADR-AU-06 第 2 条有意差异）、网站 `index.en.mdx` 与 `add.en.md`。

**Verification:**

- [ ] `source .env.test && cargo test --lib utils::pathspec`（new：字面模式解析与匹配单测）
- [ ] `source .env.test && cargo test --lib cli::tests`（new：参数优先级、环境变量与每次调用重置单测）
- [ ] `source .env.test && cargo test --test command_test add_test`（new：`test_literal_pathspecs_global_add_matrix`，含 `t2200:100-110`）
- [ ] `source .env.test && cargo test --test command_test ls_files_test`（new：`test_literal_pathspecs_disables_magic`）
- [ ] `source .env.test && cargo test --test compat_ledger_schema`

**Full-suite trigger:** `T-1: 修改 src/cli.rs 全局参数与共享 pathspec 引擎`

**Dependencies:** `DEP-AU-04`、`DEP-AU-05`（仅约束 C④）

**Deliverables:** N/A

**Implementation write set:** `src/cli.rs`、`src/utils/pathspec/mod.rs`、`tests/command/add_test.rs`、`tests/command/ls_files_test.rs`、`docs/commands/README.md`、`docs/commands/zh-CN/README.md`、`docs/commands/add.md`、`docs/commands/zh-CN/add.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/index.en.mdx`、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set；需要 `#[serial(env)]` 时加 `tests/SERIAL_REGISTRY.tsv` 与 `.config/nextest.toml`

**Docs and compatibility impact:** 全局参数表 EN + zh-CN + 网站页；add 文档；`COMPATIBILITY.md` 五行追加；错误码 N/A。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 字面模式不放宽路径校验（仓库外路径与 `.libra` 仍拒绝）。

**Performance budget:** 字面模式下跳过正则编译，成本不高于现状。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=全局字面 pathspec（共享引擎）; recovery=revert 单提交; complete=yes; self-contained=yes; AC=7/8; VER=5/8; landing=2; prod-files=2; scope=M; deps=DEP-AU-04,DEP-AU-05; writeset=与 AU-01..AU-03 不相交; release=independent; split-from=N/A; exception=N/A`

### Task AU-05: 引擎之外命令的字面 pathspec（#492）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-AU-07，审计并改造 `log`、`show`、`checkout`、`reset`、`stash`、`archive` 的路径参数匹配，使其在全局字面模式开启时按字面路径匹配，关闭时行为不变。唯一行为轴是「全局字面 pathspec（引擎之外）」。来源 issue：#492。

**Out of scope:** 把这些命令整体迁移到共享引擎（按 ADR-AU-07 第 2 条择优）；其它全局 pathspec 参数（DEFER-01）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 自带路径匹配 | `src/command/log.rs:217`、`:926-950`；`src/command/reset.rs:580`、`:612`、`:619`、`:698`；`src/command/stash.rs:659`、`:707`；`src/command/archive.rs:293`、`:319`、`:323`；`checkout.rs`、`show.rs` 路径参数 |
| Git 语义 | 事实基线 #492 行（`log` 在字面模式下只匹配字面路径） |

**Acceptance criteria:**

- [ ] 逐命令匹配方式审计已记录（ADR-AU-07 第 1 条）。
- [ ] 判据矩阵 M-LITX X1（`log`）。
- [ ] `show` 通过 M-LITX X2。
- [ ] `checkout` 通过 M-LITX X3。
- [ ] `reset` 与 `stash push` 通过 M-LITX X4–X5。
- [ ] `archive` 与关闭时回归通过 M-LITX X6–X7。
- [ ] ER-06a 同步：`docs/development/commands/{log,reset,stash,archive}.md` 记录审计结论、`COMPATIBILITY.md:193`、`:196`、`:199`、`:216`、`:261`、`:271` 原行追加；用户命令文档只在行为有变化的命令页注明（EN + zh-CN + 网站）。

**Verification:**

- [ ] `source .env.test && cargo test --test command_test log_test`（new：`test_log_literal_pathspecs_global`）
- [ ] `source .env.test && cargo test --test command_test reset_test`（new：`test_reset_literal_pathspecs_global`）
- [ ] `source .env.test && cargo test --test command_test stash_test`（new：`test_stash_push_literal_pathspecs_global`）
- [ ] `source .env.test && cargo test --test command_test archive_test`（new：`test_archive_literal_pathspecs_global`）
- [ ] `source .env.test && cargo test --test command_test checkout_test`（new：`test_checkout_literal_pathspecs_global`；`show_test` 同名用例一并加入该模块或 `show_test`）

**Full-suite trigger:** `none`（若改造落到被多个命令共享的路径匹配 helper，改为 `T-1` 并登记）

**Dependencies:** `AU-04`（消费全局字面模式状态）

**Deliverables:** N/A

**Implementation write set:** `src/command/log.rs`、`src/command/show.rs`、`src/command/checkout.rs`、`src/command/reset.rs`、`src/command/stash.rs`、`src/command/archive.rs`、`tests/command/log_test.rs`、`tests/command/show_test.rs`、`tests/command/checkout_test.rs`、`tests/command/reset_test.rs`、`tests/command/stash_test.rs`、`tests/command/archive_test.rs`、`docs/development/commands/{log,reset,stash,archive}.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set；有行为变化的命令页 `docs/commands/<cmd>.md`、`docs/commands/zh-CN/<cmd>.md` 与网站页

**Docs and compatibility impact:** 开发文档审计结论；`COMPATIBILITY.md` 六行追加；有行为变化的用户命令页；错误码 N/A。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** `archive` 的路径越界校验不因字面模式放宽。

**Performance budget:** 与现有匹配同量级。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=全局字面 pathspec（引擎之外）; recovery=revert 单提交; complete=yes; self-contained=yes; AC=7/8; VER=5/8; landing=1; prod-files=6; scope=M; deps=AU-04; writeset=序列化于 AU-04; release=independent; split-from=N/A; exception=N/A`

### Task AU-06: `add --resolved`（#494）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-AU-08，为 `add` 新增 `--resolved`：只暂存未合并路径（可按 pathspec 过滤），仍含冲突标记时整体拒绝并列出路径，工作树缺失时删除条目，与 `-u`/`-A`/`-p` 互斥。唯一行为轴是「`add --resolved`」。来源 issue：#494。

**Out of scope:** `conflict-marker-size` 属性（DEFER-04）；`update-index` 与 `rerere` 的标记检测。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `AddArgs` 无 `--resolved` | `src/command/add.rs:71-135` |
| 可复用的冲突 stage 清理 helper | AU-02（ADR-AU-04 第 1 条） |
| 现有标记检测参考 | `src/command/diff.rs:5015-5020`；`src/command/rerere.rs:796` |
| 实测 | 事实基线 #494 行（git 2.54 与 Libra 均 129） |
| Git 语义 | git@1630431f32 `builtin/add.c:270`、`:392-452`、`:509-516`、`:527-529`；`merge-ll.c:503-526`；`parse-options.c:1538-1566`；`t2207:10-106` |

**Acceptance criteria:**

- [ ] 判据矩阵 M-RES R1（残留标记整体拒绝、零写入）。
- [ ] 全部解决通过 M-RES R2。
- [ ] 未冲突修改与删除通过 M-RES R3–R4。
- [ ] pathspec 通过 M-RES R5。
- [ ] 互斥诊断通过 M-RES R6。
- [ ] 空集、预览、二进制与符号链接、`-p`/`--json` 组合通过 M-RES R7–R10。
- [ ] ER-06a 同步：`docs/commands/add.md` 与 zh-CN 新增 `--resolved` 参数行与错误表、`ADD_EXAMPLES`、`docs/development/commands/add.md`、`COMPATIBILITY.md:182` 原行追加、网站 `add.en.md`。

**Verification:**

- [ ] `source .env.test && cargo test --lib command::add`（new：标记检测与路径收集单测）
- [ ] `source .env.test && cargo test --test command_test add_test`（new：`test_t2207_add_resolved_matrix`，覆盖 `t2207:33-106` 全部用例）
- [ ] `source .env.test && cargo test --test compat_help_examples_banner`
- [ ] `source .env.test && cargo test --test compat_ledger_schema`

**Full-suite trigger:** `none`

**Dependencies:** `AU-03`（`add.rs` 写集串行；复用 AU-02 helper 与 AU-03 输出判定）

**Deliverables:** N/A

**Implementation write set:** `src/command/add.rs`、`tests/command/add_test.rs`、`docs/commands/add.md`、`docs/commands/zh-CN/add.md`、`docs/development/commands/add.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/add.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** add 命令页 EN + zh-CN + 网站页；帮助示例；开发文档；`COMPATIBILITY.md:182` 追加；错误码 N/A（复用 `LBR-CLI-002`、`LBR-CONFLICT-001`）。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 残留标记时零写入，避免把未解决冲突当作已解决提交。

**Performance budget:** 每个未合并普通文件逐行扫描一次，遇二进制内容停止。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=add --resolved; recovery=revert 单提交; complete=yes; self-contained=yes; AC=7/8; VER=4/8; landing=1; prod-files=1; scope=M; deps=AU-03; writeset=序列化于 AU-03; release=independent; split-from=N/A; exception=N/A`

## 测试矩阵

执行阶段按 ER-13 只跑与本卡相关的行；整张表由收口阶段的全量门统一覆盖。

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | `-u` 候选集与文案；冲突 stage 清理 helper；输出判定；字面模式解析与全局开关；标记检测 | `source .env.test && cargo test --lib command::add`；`--lib utils::pathspec`；`--lib cli::tests` |
| 集成 | M-UPD、M-UNM、M-OUT、M-LIT、M-LITX、M-RES（含 `t2200:67-75`、`:100-110`、`:203-239`、`:241-245` 与 `t2207:33-106` 移植） | `source .env.test && cargo test --test command_test` 下 `add_test`、`add_json_test`、`status_test`、`commit_test`、`merge_test`、`ls_files_test`、`log_test`、`show_test`、`checkout_test`、`reset_test`、`stash_test`、`archive_test` |
| 兼容 | `COMPATIBILITY.md` 行锚；帮助示例 | `--test compat_ledger_schema`；`--test compat_help_examples_banner` |
| 迁移 | 不改 schema 与索引格式 | N/A |
| 安全 | 失败路径零写入；stage 并存不变量；字面模式不放宽路径校验 | M-UPD U1–U5；M-UNM N8；M-RES R1、R6；M-LIT L6 |
| 性能 | 冲突 stage 扫描与标记检测 | 代码 review（ER-05） |
| 全量 | 收口门 | `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`（DEP-AU-05） |

## 追溯表

| 任务 | 来源 issue | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|---|
| AU-01 | #491 | TC-0022；`t2200:67-75`；git `builtin/add.c:664-676` | `src/command/add.rs:1185-1233` | add 文档、`COMPATIBILITY.md:182` | `add_test::test_add_update_untracked_pathspec_fails_atomically_matrix` |
| AU-02 | #493 | TC-0028；`t2200:203-239`；git `read-cache.c:3987-4024` | `src/command/add.rs:1346`；`src/command/status.rs:6658-6667` | add 文档、`COMPATIBILITY.md:182`、plan-20260903 修订记录 | `add_test::test_add_resolves_unmerged_entries_matrix` |
| AU-03 | #493 | TC-0028；`t2200:228` | `src/command/add.rs:999-1128` | add 文档 Human Output | `add_test::test_add_default_output_silent_when_not_terminal_matrix` |
| AU-04 | #492 | TC-0024；`t2200:100-110`；git `git.c:282-289` | `src/cli.rs:241-330`；`src/utils/pathspec/mod.rs:241-270` | 全局参数表、add 文档、`COMPATIBILITY.md` 五行 | `add_test::test_literal_pathspecs_global_add_matrix` |
| AU-05 | #492 | TC-0024；git `pathspec.c:257-300` | `src/command/{log,show,checkout,reset,stash,archive}.rs` | 开发文档、`COMPATIBILITY.md` 六行 | `log_test::test_log_literal_pathspecs_global` |
| AU-06 | #494 | TC-0032；`t2207:10-106`；git `builtin/add.c:392-452` | `src/command/add.rs:71-135` | add 文档、`COMPATIBILITY.md:182` | `add_test::test_t2207_add_resolved_matrix` |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | 计划 Codex review PASS；ADR-AU-05 经用户确认；DEP 状态确认 | review log；DEP 记录 | N/A |
| M1 | AU-01、AU-02、AU-03 `done/complete` | patch 版本、`release.yml` run | revert 源码 + 新 patch 前滚 |
| M2 | AU-04、AU-05 `done/complete` | 同上 | 同上 |
| M3 | AU-06 `done/complete`；全量收口门全绿；DEP-AU-01 满足后补跑的上游断言通过 | 同上 | 同上 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| `add -u` 校验失败 | 索引原样 | 返回 `LBR-CLI-003` | 部分暂存 |
| 暂存未合并路径时索引写入失败 | 原索引（原子替换） | 沿用既有索引保存错误 | stage 0 与冲突 stage 并存 |
| `--resolved` 发现残留标记 | 索引原样 | 列出路径并返回 `LBR-CONFLICT-001` | 只暂存部分路径 |
| `GIT_LITERAL_PATHSPECS` 取值无法识别 | 字面模式关闭 | 输出一次 warning | 静默开启或报错中断 |
| 发布步 `gh release create` 失败 | 已推送的 `main` 提交 | 按 ER-09 记录，下一窗口补 tag/release | blind retry；只推 `main` 就宣称已发布 |
| D 组 `release.yml` 失败 | 已存在的 tag | 前滚修复，发新 patch | 回退已推送提交或删除已发布 artifact |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 默认输出改为非终端静默，影响依赖摘要的脚本与测试 | 中 | ADR-AU-05 需用户评审；ER-AU-02 统计并同批更新；`--json` 不变 | AU-03 |
| 修改共享变更计算影响 `status`/`commit -a` | 高 | GC-AU-03 回归；T-1 全量 | AU-02 |
| 与 484/489/483/490/477 并发修改 `add.rs` | 中 | DEP-AU-02 串行 | AU-01、AU-02、AU-03、AU-06 |
| 字面模式遗漏引擎之外的命令 | 中 | AU-05 逐命令审计并登记 | AU-05 |
| git 2.54 无 `--resolved`，判据只能来自源码与 `t2207` | 低 | ADR-AU-08 逐条引用源码行；开工时若能构建 pinned Git 则补实跑 | AU-06 |
| `diff-files --name-only` 与 `--index-info` 缺失导致上游用例无法完整移植 | 中 | DEP-AU-01 失败策略：等价断言 + 补跑 | AU-02、AU-04 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| `add -u` 候选集 | 索引路径集合 | O(索引条目) | 不高于现状 | M-UPD |
| 冲突 stage 清理 | 每个暂存路径一次查找 | O(暂存路径) | 可忽略 | M-UNM |
| 字面模式解析 | 跳过 magic 与正则 | O(pathspec 数) | 不高于现状 | M-LIT |
| `--resolved` 标记检测 | 每个未合并普通文件逐行扫描 | O(冲突文件大小) | 遇二进制停止 | M-RES |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md` 相关行（`:182`、`:191`、`:193`、`:195`、`:196`、`:199`、`:216`、`:227`、`:233`、`:261`、`:271`）已在原物理行内更新，文件仍为 517 行，`compat_ledger_schema` 通过。
- [ ] `docs/commands/{add,README}.md` 与有行为变化的命令页已同步。
- [ ] 上述页面的 `docs/commands/zh-CN/` 版本已同步。
- [ ] `docs/development/commands/{add,log,reset,stash,archive}.md` 已同步；`plan-20260903.md` 修订记录已注明 `FIX-MG06-CAND-01` 由 AU-02 承接。
- [ ] `docs/error-codes.md`：N/A（复用 `LBR-CLI-002`、`LBR-CLI-003`、`LBR-CONFLICT-001`，不新增错误码）。
- [ ] `tests/INDEX.md` 与 `Cargo.toml` `[[test]]`：N/A（新增用例均落在既有 `command_test` 模块）。
- [ ] `../libra-backend` 网站对应页（`index.en.mdx`、`add.en.md` 及行为有变化的命令页）已在已验证的 `cf` 分支同步（DEP-AU-06）。

## Codex review log

本计划尚未进行 Codex review。按 ER-05，Phase 0 必须至少有一轮 `PASS` 后才能开工。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-01 | `--glob-pathspecs`、`--noglob-pathspecs`、`--icase-pathspecs` 全局参数 | 不在 #492 描述范围内；复用 AU-04 的全局状态即可扩展 | 用例移植需要 | 待立 issue |
| DEFER-02 | `add -n` 预览文本对齐 Git 的 `add 'x'` / `remove 'x'`（`t2200:145-163`） | 预览是 Libra 文档化格式，四个 issue 未涉及 | 用例移植需要 | 待立 issue |
| DEFER-03 | `add -v` 输出对齐 Git 的 `add 'x'` 格式 | 同上 | 用例移植需要 | 待立 issue |
| DEFER-04 | `conflict-marker-size` 属性影响 `--resolved` 的标记长度 | Libra 尚无该属性解析 | 属性支持落地 | 待立 issue |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [ ] 所有任务卡满足 `G-*`：无 L/XL 卡、无未登记合并发布，实现写集冲突已消解（含 DEP-AU-02、DEP-AU-04），「任务卡粒度审计表」已填齐。
- [ ] ADR-AU-05 的方案已经用户评审确认并记录。
- [ ] AU-01..AU-06 的 AC 全部满足，且 `Lifecycle=done` **且** `Acceptance=complete`（含 D 组 `release.yml` 证据）。
- [ ] 所有 Verification 命令已运行并记录结果；每个判据矩阵行都有对应测试与 git 2.54（或 pinned 源码）对照。
- [ ] 上游用例移植通过：`t2200:67-75`、`:100-110`、`:203-239`、`:241-245` 与 `t2207:33-106`；依赖 DEP-AU-01 的原始断言已在其满足后补跑。
- [ ] **全量收口门（ER-13）**：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2` 全绿（DEP-AU-05 已重核）。
- [ ] **全量暴露的 Bug 已全部处理（ER-13）**：本计划引起的失败已前滚修复并重跑全量至全绿；既有失败附计划基线 commit 上同样失败的复现证据，并登记 `FIX-*` 或 `DEFER-*`。
- [ ] docs、compat 同步完成（见「兼容与文档收口」）。
- [ ] 每张卡的 `Rollback mode` 已验证或记录不可验证的原因。
- [ ] Codex review 最终为 `PASS`，P0/P1 全部关闭。
- [ ] 每张卡已 `patch + 1`、版本面五处一致、`Cargo.lock` 仅由工具链刷新，并已用 `gh release create v<version>` 触发 `release.yml` 且取得 Cloudflare 发布证据（ER-08）。
- [ ] 「修订历史」已记录成稿后的全部规范性变更。
- [ ] DEP-AU-07：经用户确认后，已在 #491–#494 回复复测结论、各卡发布版本与移植证据，并在本计划全部卡片完成后关闭四个 issue（或记录用户决定不回复、不关闭）。
