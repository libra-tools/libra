# Operation Log、Working Copy 快照与稳定 Change ID 实施计划（2026-08-22）

本计划依据 `docs/development/plan/plan-template.md`（模板版本 `v2`）编写，覆盖 `docs/development/internal/operation-log-working-copy-change-id.md`（下文简称「设计文档」）从阶段 0 到阶段 8 的全部实施内容。

## 本次执行窗口（2026-09-05）

本窗口在同一 `feat/scheam-update` 分支联合落地 OL-02、OL-03、OL-04：完成 v2 SQLite schema、canonical view/facet codec、`OperationStoreV2`、journal 与 op-head CAS。为保持现有 active `operation_wrapper` 可用，v1 表迁移到明确的 `legacy_*` 命名空间；不进行 v1/v2 双写，不伪造 v2 view/journal/head/genealogy/AI link。legacy 表在 OL-09/OL-15 完成运行时切换及删除验收前保留。

本窗口版本冻结：`Cargo.toml`、`web/package.json`、`worker/package.json`、`install.sh`、`install.ps1` 五个版本面均保持 `0.22.14`；不 bump、不打 tag、不生成 release artifact，只执行代码、测试、签名 commit 与 feature branch push。OL-00 继续遵守 sidecar-only：不写 `change-id` commit header，不重写 Git commit，不改变 Commit OID。

## 文档职责

本文解决「一次 CLI 操作修改了哪些仓库状态、未提交状态能否统一撤销、commit 经 rewrite 后如何继续识别同一项逻辑工作」三个能力缺口，目标是交付设计文档定义的三项能力：全命令 Operation Log、Working Copy Snapshot 与 Undo/Redo、稳定 Change ID 与 Rewrite Genealogy，并最终以 v2 替换 v1 operation 实现。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 规范性 ID 与术语

计划正文引用规范条款时一律用具体 ID，不用「上一节」「前面那条」等会随条款增删失效的范围表述。

| 前缀 | 含义 | 定义位置 |
|---|---|---|
| `ER-*` | 执行检查必备需求（开工、验收、发布、证据） | 「执行检查必备需求」 |
| `GC-*` | 全局工程约束（对全部任务生效） | 「全局工程约束」 |
| `G-*` | 任务卡粒度规则 | 「任务卡粒度规则」 |
| `ADR-*` | 已决议设计决策 | 「已决议设计决策」 |
| `GAP-*` | 事实基线缺口 | 「当前缺口」 |
| `DEP-*` | 依赖登记项（含跨计划与外部前置） | 「依赖登记表」 |
| `REL-*` | 发布分组（含家族卡窗口） | 「发布分组与并发窗口」 |
| `EX-*` | 白名单内的规则 waiver（需具名审批） | 「字段全局默认与例外」 |
| `FIX-*` | 执行期发现的越界修复卡（ER-10） | 对应 Phase 末尾 |
| `DEFER-*` | 延后项 | 「非目标与延后项」 |
| `M-*` | 里程碑 | 「里程碑验收与回滚」 |

### 适用范围

- 包含的命令/模块：全部可修改仓库状态的 `libra <subcommand>`（mutation census）、`libra op` 子命令族（restore/undo/redo/revert/doctor）、Agent tool mutation gateway（`src/internal/ai/tools/*`、`src/internal/ai/libra_vcs.rs`）。
- 包含的存储、schema、协议或 UI 表面：SQLite operation v2 schema（最终替换 v1 `operation/operation_parent/operation_view/operation_view_ref/operation_view_workspace`；OL-02～OL-04 窗口先迁移为 `legacy_*`）、`RepoViewV2` / `WorkspaceSnapshotV2` canonical manifest（写入 `ClientStorage` Git ODB）、`change_identity/change_revision/change_predecessor/ai_operation_link` 表、`libra op` 机器接口、Web Operation/Change 图。
- 包含的测试、文档、兼容矩阵或发布动作：每阶段验证 target（`operation_dag`、`workspace_snapshot_roundtrip` 等，见「测试矩阵」）、`tests/INDEX.md` 与 `Cargo.toml [[test]]` 注册、`docs/commands/op.md` EN+zh、`COMPATIBILITY.md`、`docs/error-codes.md`、版本面五处 bump、v1 移除。

### 非目标

- 不设计用户、Agent 或命令权限模型；默认调用者拥有执行操作所需的最大权限（设计文档第 1 节范围假设）。「最大权限」只移除 authorization/approval/sandbox 设计，不移除 ref lock、CAS、worktree lease、冲突检测与 destructive 确认。
- 不更换现有 Git 后端：不把 Libra 迁移成 jj 的 repository model，保留 Git refs/index/worktree/远端协议（设计文档第 1 节「为什么借鉴，而不取代现有 Git 后端」）。
- 不为 v1 operation 维护长期兼容层或双写 adapter；本窗口仅保留 `legacy_*` staging 命名空间以保护 active v1 runtime，最终删除仍依赖 OL-09/OL-15 的运行时切换与验收（设计文档 §5.1）。
- 不实现 SQLite projection、stat cache 等派生状态的旧值恢复；派生状态一律按事实源重建（设计文档第 3 节）。
- 不承诺本地 Undo 撤回 push/网络发布等外部系统副作用，只记录 receipt（设计文档第 3 节）。
- 不把 Working Copy 自动变成 Git commit；自动 snapshot 不产生 Commit OID 或 Change ID（设计文档第 2 节）。
- Web Operation/Change 图只做只读投影，不提供 Web 写入口。

### 成功定义

- 用户或系统行为变化：任何经 Libra CLI 的持久修改都形成完整可恢复的 Operation；`libra op undo/redo/restore/revert` 可安全恢复单 worktree 最近或指定 Operation；`libra commit`/amend/rebase 后同一逻辑 change 的 Change ID 稳定；Undo commit 不改变已有 Commit OID。
- 机器接口或数据状态变化：SQLite v2 schema 落地；`RepoViewV2`/`WorkspaceSnapshotV2` manifest 可序列化、校验、闭包可枚举；op-head CAS 与多父 DAG 可发布；`libra op` 新子命令输出 JSON/machine receipt；Operation/Change Web 图可查询。
- 文档、测试、发布证据：每阶段 Gate 的指定 test target 全绿；`tests/INDEX.md` 注册新增 target；`docs/commands/op.md` EN+zh、`COMPATIBILITY.md`、`docs/error-codes.md` 同步；v1 代码 `rg` 零命中守卫属于 OL-15；版本面五处按发布卡 bump 并发布，本窗口不 bump。
- 何时可标记计划完成：满足「完成判据」全部条目，Codex review 最终结论为 PASS，所有非延后任务 `Lifecycle=done` 且 `Acceptance=complete`。

## 事实基线

> 所有行号和源码锚点必须在开工当天刷新。过期锚点只能作为历史线索。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 代码入口 | `enum Commands` 穷举命令分类；`command_preflight`、`command_scope`、`command_name` 三处 dispatch match 是 v2 middleware 接入点 | `src/cli.rs:336`、`src/cli.rs:1474`、`src/cli.rs:1681`、`src/cli.rs:2123` |
| 数据/状态 | v1 operation schema：`operation/operation_parent/operation_view/operation_view_ref/operation_view_workspace` 表内嵌于 `OPERATION_SCHEMA_SQL` | `src/internal/db.rs:557-606` |
| 数据/状态 | v1 OperationService 持久化入口 | `src/internal/operation.rs:1018`（`persist_operation_graph`） |
| 数据/状态 | v1 operation wrapper：`ParentSelectionMode::SingleLatestSuccess`、`OperationMeta`、`with_operation_log`（v2 最终删除） | `src/internal/operation_wrapper.rs:46`、`:60-61`、`:405` |
| 用户命令 | `libra op` 子命令族（当前为 v1 查看/restore 路径） | `src/command/op.rs` |
| 用户命令 | `libra status` 的 bounded I/O worker 是 `worktree_io` 抽取来源 | `src/command/status_io_worker.rs` |
| 用户命令 | `WorktreeScope` 是 RequestScope pinning 现状基础 | `src/internal/worktree_scope.rs` |
| 机器输出 | `--json`/`--machine` 输出契约与稳定 `LBR-*` 错误码（新子命令按 GC-04 扩展） | `src/utils/error.rs`、`docs/error-codes.md` |
| 错误码 | 稳定错误码枚举 | `src/utils/error.rs` |
| 文档 | 设计文档（本次迁移后落点） | `docs/development/internal/operation-log-working-copy-change-id.md` |
| 文档 | 本计划模板 | `docs/development/plan/plan-template.md` |
| 测试 | 集成测试索引与 `[[test]]` 注册约定；新增 target 必须注册 | `tests/INDEX.md`、`Cargo.toml` |
| 外部参照 | Jujutsu 参照（pin 见「依赖登记表」DEP-01） | `plan-long.md:91`（`Jujutsu 6e98d068b`，开工当天刷新） |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | v1 operation 只记录命令文本与 HEAD/refs 快照，不恢复 index/worktree/sequencer | 无法统一撤销未提交状态 | `src/internal/operation_wrapper.rs:405`、`src/internal/db.rs:557-606` | OL-02/OL-03/OL-04/OL-06/OL-07 |
| GAP-02 | 无统一 CLI mutation 边界；mutation census 只到 `command_scope` 粒度 | 未知/新增 mutation 无法 fail closed | `src/cli.rs:1681` | OL-08/OL-09 |
| GAP-03 | 无稳定逻辑身份；commit 只有易变 OID | amend/rebase 后 Intent 等 AI 对象无法锚定同一 change | `plan-long.md:180`（LR-03） | CH-01/CH-02/CH-03/CH-04 |
| GAP-04 | 无 crash-safe undo/redo/restore 与恢复引擎 | 崩溃窗口可能产生半吊子状态 | `src/internal/operation.rs:1018` | OL-10/OL-11/OL-12 |
| GAP-05 | 无多 worktree Operation heads/reconcile 与 Web 展示 | 并发 CLI 修改无法收敛展示 | 设计文档 §2「多 CLI 进程」 | OL-13/OL-14 |
| GAP-06 | v1 代码/表/命令路径将在 v2 验证后被移除 | 不维护双写，需一次性替换 | 设计文档 §5.1 | OL-15 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | LR-02（Op log）与 LR-03（Change ID）为 P0「已验证」项，需推进为实施 | 本计划是 LR-02/LR-03 的可执行切片；执行期按 GC-05 同步 LR 状态 |
| `plan-YYYYMMDD.md`（其它日期计划） | 无直接依赖；本计划与 W5 系列（已合入 main）无写集交集 | 不触碰其它计划任务卡 |
| `docs/development/internal/operation-log-working-copy-change-id.md` | 设计文档为事实源与契约 | 本计划逐卡落实其第 2/3/5 节要求；执行中发现设计偏差必须先修设计文档再开工（GC-01/ADR 规则） |
| `docs/development/plan/plan-template.md` | 模板为规范 | 本计划整份符合 v2 模板 |

## 评审结论与修订记录

计划成稿前必须从以下维度做一次自审；如果有阻断项，先修计划再开工。

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | 目标值得做：LR-02/LR-03 是 plan-long P0 已验证项，三项能力是 AI 版本控制核心 | 无 |
| 可行性 | 任务可拆、可交付：按设计文档 §5.4 的 9 阶段拆成 20 张卡，每卡单一行为轴 | 无 |
| 任务卡粒度 | 无 L/XL 卡、无碎片卡、无未登记合并发布；OL-00 为 spike，其余 S/M | 按 G-03/G-04 逐卡核对计数 |
| 依赖与顺序 | DAG 无环；先快照后 Undo、先单 worktree 后并发、先冻结事实源后 Web | 依赖边见「实施顺序」 |
| 完整性 | 测试/文档/迁移/回滚齐全：每张卡带 AC/Verification/写集，新增 target 同卡注册 | 无 |
| 安全性 | 无新权限模型（设计文档范围假设）；数据一致性保护保留；OperationMetaV2 只存 redacted ID | 无 |
| 功能正确性 | 状态机（Running→Success/Failed/Partial/Aborted）、CAS、journal phase、undo 追加式语义均在卡内覆盖 | 无 |
| 接口兼容 | v1 op 命令路径在 OL-15 移除（minor bump）；新子命令按 GC-04 稳定错误码与机器输出 | 无 |
| 数据流与控制流 | 事务边界、幂等键、崩溃窗口、回滚策略按 GC-08/GC-09 逐卡声明 | 无 |
| 性能与容量 | worktree_io bounded I/O、status 零回归 benchmark、Web 图有界 | 无 |
| 可靠性与容错 | journal 前滚/回退、doctor、crash matrix、故障恢复矩阵 | 无 |
| 可维护性 | 单一事实源：schema/状态机/输出结构不复制；v2 直接替换 v1 | 无 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-08-22 | 自审 | 计划成稿 | — | — |
| 2026-08-22 | 自审 R1 | 按 G-03 计数口径重计各卡 AC/VER（ER-04 强制门不计入），与「任务卡粒度审计表」及各卡 `Granularity` 行对齐；补充「规范性 ID 与术语」表 | — | 任务卡粒度审计表、各卡 Granularity |
| 2026-08-25 | OL-00 spike | 真实 Git 验证 header 可互操作但不冻结为 Libra 写入协议；ADR-OL-04 冻结为 sidecar-only，已有 header 仅作导入兼容信息；补充 spike 结论文档并更新 CH-01/CH-03 承接约束 | OL-00 → CH-01/CH-03 | 设计文档 §2、§5.4；DEP-02；Gate-0/Gate-5 |
| 2026-08-25 | CI Rust prerequisites（用户授权） | 修复未被 OL-00 原始写集覆盖的 clippy 与 rustdoc 基线问题，使 PR Rust CI 可通过；同步扩大本卡 allowlist 与验证项 | OL-00 | `src/internal/ai/agent_bridge/methods.rs:112`、`src/internal/ai/agent_bridge/vcs.rs:803`；base.yml Rust clippy/doc job |

## 已决议设计决策

实现时若需偏离本节，必须先修改计划并说明原因，不得在代码中静默改语义。

### ADR-OL-01: v2 最终替换 v1，不维护长期兼容层

- **Status:** Accepted
- **Context:** 设计文档 §5.1 的最终目标是不维护 v1 长期兼容层；但 OL-02～OL-04 必须先完成 schema、codec、store 的安全基础，并保持 active `operation_wrapper` 可用。
- **Decision:** v2 是最终事实源，禁止 v1/v2 双写和永久 adapter。OL-02～OL-04 使用独立 `legacy_*` staging 命名空间承接现有 v1 读写；OL-09/OL-15 完成所有运行时切换、验证与删除后，移除 legacy 表和 v1 model/service。
- **Alternatives considered:** 直接删除 v1 表（拒绝：会在基础窗口切断 active operation logging）；双写适配（拒绝：引入两个事实源和一致性风险）；长期兼容 adapter（拒绝：无用户价值且增加维护面）。
- **Consequences:** 本窗口保留明确的 legacy 存储边界，但不将旧行伪造为 v2 view/journal/head/genealogy/AI link；OL-02/OL-15 的 `Version increment` 仍为 `minor`，并同步 `COMPATIBILITY.md`。
- **Revisit when:** 若未来出现必须与 v1 仓库数据长期互操作的场景。

### ADR-OL-01b: OL-02～OL-04 legacy staging 与删除条件

- **Status:** Accepted for the 2026-09-05 execution window
- **Context:** migration runner 从 bootstrap v1 逐步迁移到 v2；active `operation_wrapper`、现有 DAO 与 maintenance/object-root 读取仍需要原有 operation 语义。v1 行中的 view、workspace、历史关系不足以无损构造 v2 manifest 或 operation genealogy。
- **Decision:** 在单事务内 copy-first 创建 staging 表，校验行数和关键字段后，将 v1 表原子更名为 `legacy_operation*`；再创建八张 v2 canonical 表。新运行时只写 v2，现有 v1 logging 在本窗口继续只读写 legacy 表，不启用双写。无法无损映射的字段只保留在 legacy 表，不填充伪造 v2 行。
- **Deletion condition:** 仅当 OL-09/OL-15 已完成 mutation runtime cutover、active logging smoke、legacy 读写零命中守卫、对象 root/maintenance 收口、备份/删除演练与兼容验收后，才允许单独提交删除 `legacy_*` 表、legacy model/service 及其测试 fixture；在此之前禁止删除或清空 legacy 数据。
- **Failure and rollback:** copy、关键字段验证、DDL 或 canonical schema 创建任一步失败，migration version claim、staging、source rename 与数据修改全部回滚；该 migration forward-only，不提供含义不明的 down migration。

### ADR-OL-02: 命令前先 snapshot 的统一 mutation 边界

- **Status:** Accepted
- **Context:** jj 几乎所有命令在读取仓库后、执行逻辑前先 snapshot（设计文档 §2「Libra 准备如何借鉴」、§5.3.1）。Libra 需要所有可修改 Working Copy 的 CLI 命令与 Agent tool 都经过统一入口。
- **Decision:** `middleware::run_with_operation` 在固定 `PinnedRequestScope` 下先做外部变化检测（磁盘 vs last post-view），有变化先发布 pure `ExternalSnapshot` operation，再执行业务闭包并发布 `Command` operation；`pre_view_oid` 规则按设计文档 §5.3.1。
- **Alternatives considered:** 逐命令手动 snapshot（拒绝：无法穷举 fail closed）；不做 pre-snapshot 直接执行（拒绝：丢失 CLI 外修改归因）。
- **Consequences:** 所有 mutation surface 必须经 `classify_command` 穷举分类，未知类 fail closed（OL-08/OL-09）。
- **Revisit when:** 出现无法在入口预分类的新修改入口时。

### ADR-OL-03: Change ID 使用 128-bit CSPRNG 随机 ID，而非 UUIDv4 或内容哈希

- **Status:** Accepted
- **Context:** 设计文档 §2「Libra 新建逻辑变更：使用随机 ID」。UUIDv4 的 version/variant 位使随机有效位约 122 bit 并附带不需要的格式语义；内容哈希会随 amend/rebase/message/tree 变化而破坏逻辑身份稳定。
- **Decision:** `ChangeId([u8; 16])`，production 用 `ring::rand::SystemRandom` 一次填充 16 字节；存储层保存原始字节，CLI 用 `change` 前缀 hex 与唯一短前缀展示；本地写入发现冲突重新生成，导入发现碰撞显式报告。
- **Alternatives considered:** UUIDv4（拒绝：有效随机位 122 bit + 多余语义）；内容哈希（拒绝：rewrite 后不稳定）。
- **Consequences:** 需要 sidecar/投影持久化 Change ID（`change_identity/change_revision` 表）；legacy import 用 synthetic ID（ADR-OL-04）。
- **Revisit when:** 需要与外部系统交换 Change ID 标准格式时。

### ADR-OL-04: legacy Git commit 用确定性 synthetic Change ID

- **Status:** Accepted
- **Context:** 导入没有 change-id header 的既有 Git commit 时，需要可复现、跨机器一致的 Change ID（设计文档 §2）。
- **Decision:** `synthetic_change_id = first_16_bytes(SHA-256("libra-change-id-v1\0" || git_object_format || commit_oid_bytes))`，域分隔字符串 + object format 防跨协议复用，支持 SHA-1/SHA-256。
- **Alternatives considered:** 随机 ID 导入（拒绝：同一 commit 重复导入得到不同 ID）；取 commit ID 末尾字节（jj 做法，拒绝：与 Libra 域分隔约定不一致）。
- **Consequences:** 该 commit 首次 rewrite 后继承 synthetic ID 并写入 Libra sidecar；Libra 新建或 rewrite 不写 `change-id` commit header。已有合法 header 可以作为导入兼容信息读取并标记 `origin=header`，没有 header 时使用 synthetic ID。OL-00 证据与限制见 `docs/development/internal/change-id-header-spike.md`。
- **Revisit when:** Git 生态对 change-id header 的兼容行为变化时。

### ADR-OL-05: Working Copy 用 WorkspaceSnapshotV2 manifest，而非 jj working-copy commit

- **Status:** Accepted
- **Context:** 设计文档 §2「Libra 准备如何借鉴」：自动 snapshot 不移动 Git HEAD、不创建用户可见 commit。
- **Decision:** 每个 worktree 的完整状态由 `WorkspaceSnapshotV2` manifest 表达：`head`、`index_tree_oid`（semantic）+ `raw_index_blob_oid`（byte-exact raw index）、`working_copy_tree_oid`、`untracked_manifest_oid`、`sparse/sequencer` facet、`worktree_generation`、`capture_policy`、`completeness`、`facet_restore_policies`。内容寻址对象写入 `ClientStorage`，SQLite 只存协调状态与投影。
- **Alternatives considered:** 引入 working-copy commit（拒绝：改变 Git HEAD 语义、破坏 Git 兼容）；只存 v1 式 HEAD/refs 快照（拒绝：无法恢复 index/worktree/sequencer）。
- **Consequences:** raw index 需要 byte-exact 保存以恢复 intent-to-add/skip-worktree/stat 等位；扫描 I/O 必须走 `WorktreeIo` bounded executor（OL-06/OL-07）。
- **Revisit when:** Git index 格式或 Libra index 语义发生大版本变化时。

### ADR-OL-06: op-head CAS + 多父 reconcile 表达并发

- **Status:** Accepted
- **Context:** 多 CLI 进程、多 worktree 的 append-only Operation DAG 需要防止「最后写入者覆盖前一个结果」（设计文档 §2）。
- **Decision:** `operation_head` 表按 `(repo_id, scope_key)` 保存 head 集合；发布用 `cas_update_op_heads(old_heads, new_heads)` 校验旧 head 与期望一致，不一致时保留两个 head 并报告并发；多父 `reconcile` operation 再收敛 head 集合。
- **Alternatives considered:** 最后写入者胜（拒绝：丢失并发事实）；单 head 全局锁（拒绝：破坏多 worktree 并行）。
- **Consequences:** 需要 worktree lease/generation 与固定锁顺序保护底层写；`OpHeadsView` 参与 workspace pointer 的 stale/sibling 判定（OL-05/OL-13）。
- **Revisit when:** 需要仓库级发布锁以支持并发发布时（当前按 ER-12 全串行）。

### ADR-OL-07: Undo 为追加式显式状态变换，不修改历史 Operation

- **Status:** Accepted
- **Context:** 设计文档第 3 节：Undo 不设固定次数，恢复深度由被保留且完整的 Operation 历史决定；Undo commit 不修改/删除 commit 对象。
- **Decision:** undo/redo/revert/restore 都创建新的 Operation 节点；`OperationV2` 带 `pre_view_oid`、`post_view_oid` 与 `restores_op_id/reverts_op_id`，使 undo 是显式状态变换而非事后 diff 推断；恢复路径按 facet 的 `RestorePolicy`（AutoRestore/Rebuild/NeverRestore）执行，逐 facet 幂等。
- **Alternatives considered:** jj 式「undo 也是普通 transaction 靠 view diff 推断」（拒绝：Libra 需要显式 from→to 变换与 receipt）；内存内撤销（拒绝：崩溃窗口不可恢复）。
- **Consequences:** 需要 `operation_journal` phase 记录与 doctor 前滚/回退；Undo 深度受 retention/GC 约束（OL-10/OL-11/OL-12）。
- **Revisit when:** GC 保留策略或 retention 管理员策略变化时。

### ADR-OL-08: OperationMetaV2 只保存 redacted 因果 ID

- **Status:** Accepted
- **Context:** 设计文档 §5.2.3：`OperationMetaV2` 的 causal 字段只保存 redacted ID 与 config provenance digest，不保存 prompt/transcript/secret/lease token。
- **Decision:** `ai_operation_link` 表保存 `session_id/run_id/tool_invocation_id/intent_id/repo_id/worktree_id/workspace_id/lease_generation/config_provenance_digest/redaction_version`；Operation 与 Change 通过稳定 ID 关联 AI 对象，不直接绑定易变 Commit OID。
- **Alternatives considered:** 全量保存因果上下文（拒绝：泄露 secret/PII、违反 ER-11 证据卫生）。
- **Consequences:** 需要 `redaction_version` 字段与脱敏写入测试；AI FileHistoryStore 只读/迁移兼容（CH-04）。
- **Revisit when:** AI 因果对象 schema 演进时。

## 全局工程约束

以下约束对本文所有任务生效。任务条目不再逐条重复，违反任一项即视为任务未完成。

- **GC-01 现状核实前置:** 每个任务开工前重新核对计划、相关开发文档、用户文档、当前代码和测试。如果已实现，则任务改为补测试、补文档、更新状态或关闭，不重复实现。
- **GC-02 单一事实源:** 状态机、schema、输出结构、权限策略、配置解析和共享 helper 必须有单一事实源。禁止 CLI、Web、Agent adapter 或测试 fixture 各自复制逻辑。
- **GC-03 Git 互操作与 Libra 扩展边界:** Git 兼容表面必须说明与 Git 的一致点和有意差异；Libra-only 表面必须说明替代工作流、用户影响和机器接口。
- **GC-04 输出与错误契约:** 用户可见错误使用稳定 `LBR-*` 码并同步 `docs/error-codes.md`。`--json`、`--machine`、退出码和人读输出必须分别验收。
- **GC-05 文档同步:** 命令或公开行为变化必须同步 `docs/commands/<cmd>.md`、`docs/commands/zh-CN/<cmd>.md`、相关 `docs/development/commands/*.md`、`COMPATIBILITY.md` 和测试索引。
- **GC-06 测试索引:** 新增或重命名 `--test` target 必须更新 `tests/INDEX.md`；`tests/compat/*` 新文件还必须在 `Cargo.toml` 注册 `[[test]]` 并更新 `tests/compat/README.md`。
- **GC-07 安全默认值:** 未满足认证、授权、路径归属、schema 版本、对象闭包、sandbox 或 secret redaction 前置时默认 fail-closed。任何 fail-open 必须有显式用户选择、日志和测试。
- **GC-08 原子性与恢复:** 修改 HEAD、refs、index、worktree registry、SQLite、D1/R2、对象库、Agent session/checkpoint 或发布状态时，必须定义事务边界、幂等键、崩溃窗口和回滚/前滚策略。
- **GC-09 并发与资源生命周期:** 锁顺序、CAS、lease、队列、子进程、连接池、临时目录和 watcher 必须有释放/恢复语义；测试不得依赖未隔离的全局状态。
- **GC-10 性能预算:** `status`、`diff`、`add`、`commit`、fetch/push、Agent hot path 和 Web/SSE 热路径不得引入无界扫描、无界内存或 N+1 网络/DB 调用。需要时写出数据规模和断言。
- **GC-11 生产 panic 禁止:** 生产路径不得新增裸 `unwrap()`、`expect()`、`panic!()`；必须用 `Result`、`anyhow::Context` 或领域错误返回可操作信息。
- **GC-12 精确暂存:** 提交前只 `libra add <相关路径>`，不得使用 `commit -a`。发现无关脏状态时保留并报告，不得清理、重置或混入提交。

## 执行检查必备需求（强制）

任一要求未满足，对应任务不得标记完成。条目使用稳定 ID，正文引用时用 ID 而不是序号，便于后续插入条目而不破坏交叉引用。

1. **ER-01 开工前安全检查:** 运行 `libra status --short --branch`（`--branch` 才会输出 `## <branch>...<upstream>` 行；不带它只有文件状态），确认当前分支与计划指定分支一致、工作区脏状态、目标文件是否已有无关改动。若目标文件已有未确认用户改动，先报告并避免覆盖。
2. **ER-02 先核对后实现:** 刷新本任务相关源码锚点、文档锚点、测试 target 和外部参照 revision，再决定实现、补测、补文档、关闭或降级。
3. **ER-03 粒度门禁:** 开工前按粒度规则 `G-*` 逐条复核本任务卡，并逐字段核对该卡的 `Granularity` 摘要行。若核对后发现范围已扩大（新增行为轴、AC/Verification 超限、scope 升到 L、写集与其它在跑任务重叠），先修改计划拆卡再开工，不得在实现中静默扩张任务范围。
4. **ER-04 每卡验收门:** 门由 **A 表面 focused 门**（按实际改动的表面）+ **B 类型门**（按 `Task type`）+ **C 发布收口门**（覆盖要求对所有非延后卡生效，执行归属见下）+ **D 远端后置门**（有不可本地复现的 CI 语义时）四组组成，**所有适用行累加**，全部通过才算验收。权威口径分层：`CLAUDE.md`「Quality Acceptance Criteria」的三门（`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all`）是**任何会推送的改动**的完成契约，模板不得削弱；任务卡指定的 focused 用例是在此之上的**附加**门，用来证明本卡行为，不是三门的替代品。`web/`、`worker/` 的命令以各自 `package.json` 的 `scripts` 为权威（本表按当前 scripts 列出，scripts 变更时以 `package.json` 为准并同批更新本表）。权威口径变更时必须同批同步 `CLAUDE.md`、`AGENTS.md`，以及**采用本模板当前版本的计划**（存量计划按「模板版本与迁移政策」处理，不因本条被追认为违规）。

   **门不计入条目上限：** 本条列出的门是全局强制门，**不计入**任务卡 `Verification` 的 G-03 条目计数；`Verification` 只登记本卡特有的判据（指定用例、新增守卫、手工证据）。

   **两个正交状态字段（都在任务卡登记）:**
   - `Lifecycle`（执行生命周期）：`pending` | `in-progress` | `blocked`（ER-10 的越界故障置此值）| `done`。
   - `Acceptance`（验收状态）：空 | `locally-accepted` | `remote-pending`（仅当本卡有适用的 D 组远端后置门）| `complete`。与 `CLAUDE.md` 的完成契约对齐 —— `CLAUDE.md` 规定任何 change 只有三门全绿才算 done，因此：
     - `locally-accepted` = 本卡适用的 **A 组 + B 组**门已过，但本卡的 **C 组覆盖**（自行执行或从承接卡继承，见下）尚未取得。此状态下**不得**对外报告「完成 / done」。
     - `remote-pending` = A/B 已过且 C 组覆盖已取得（含一次三门全绿的运行，其被测树状态包含本卡最终变更），但本卡适用或继承的 **D 组**远端后置门尚未全绿。此状态同样**不得**报告完成。
     - `complete` = 本卡的 **A + B** 已过、**C 组覆盖**已取得、且适用或继承的 **D 组**已全绿。无 D 时 C 覆盖到手即可 `complete`；有 D（含继承的 D）时必须先经 `remote-pending`。
     - 唯一状态转移路径：A/B 通过 → `locally-accepted` → ER-05 review PASS → 取得 C 组覆盖 →（无 D：`complete`；有 D：`remote-pending` → D 全绿 → `complete`）。
   - 两者独立取值：`blocked` 卡的 `Acceptance` 可以已是 `locally-accepted` 甚至 `complete`（例如变更已被三门覆盖，但仍卡在外部前置）。`Lifecycle=done` 必须以 `Acceptance=complete` 为前提；`blocked` 必须先回到 `in-progress` 并完成剩余动作才能进入 `done`，**不允许**从 `blocked` 直接标 `done`。计划完成门另要求所有非延后任务都到 `done`（见「完成判据」）。`Granularity` 里的 `complete=yes` 是 G-02 的结构完整性判据，与本字段无关，不可混用。

   **C 组覆盖与执行归属（每张非延后卡都必须取得 C 覆盖，但不都自己执行）:**
   - **独立发布卡（`Release boundary = independent`）与发布点卡（`release` / `family release point`）**：自行执行完整 C 组门。
   - **`family child`**：不 bump、不构建、不推送，**继承**其家族唯一发布点的 C 覆盖——前提是该发布点的三门运行其被测树状态包含本子卡的最终变更；同时继承该发布点适用的 D 组。
   - **`no-release` 卡（`docs` / `audit` / `spike` / `handoff`）**：**继承**任务卡显式声明的承载发布点（或计划收口点）的 C 覆盖与其 D 组；该承载点必须在卡内写明 ID，不得留空。
   - **`batch-release child`（任意 `Task type`，含 `implementation`）**：用于「逐卡提交并**推送**、全计划收口时统一发一次版本」的批量发布组（G-07a）。它**自行执行** C 组中的三门、提交与 `push`（因为它会推送，`CLAUDE.md` 的完成契约不可继承），**不** bump 版本面、**不**构建/安装、**不**打 tag、**不**产 artifact；版本面/artifact 这一段与 **D-02** 继承自其批量发布组的唯一发布点，**D-01 不继承**（每次推送各自触发，须自行取证）。其 `Acceptance` 在发布点完成前停在 `locally-accepted`。必须在卡内写明发布点 ID，并在「发布分组与并发窗口」登记该组。
   - 继承 D 组的卡同样要经过 `remote-pending`，直到被继承的 D 组证据全绿。
   - **不存在「用零命中守卫替代三门」的通道**——零命中守卫只用于证明这类卡未改代码，不改变其在取得 C 覆盖前仍是 `locally-accepted` 的事实。

   门分四组，全部适用者累加：**A 表面 focused 门**（按实际改动的表面，每个表面唯一命中一行，命中几行加几行）+ **B 类型门**（按 `Task type` 取一行）+ **C 发布收口门**（由会推送的卡执行，其覆盖可被 `family child` / `no-release` 卡继承）+ **D 远端后置门**（只在存在不可本地复现的 CI 语义时适用）。A/B/C 是本地可完成的门；D 只能在推送之后取得证据，因此**不阻塞** `locally-accepted` 与 ER-05 的 review 顺序。

   **A 表面 focused 门**（覆盖全部合法生产表面；`family child` 复用同一映射）：

   | 实际改动的表面 | focused 门 |
   |---|---|
   | Rust 内部单元逻辑（测试写在 `src/**` 的 `#[cfg(test)]` 里） | `source .env.test && cargo test --lib <filter>` |
   | Rust 集成 / CLI 行为（测试在 `tests/**`） | `source .env.test && cargo test --test <target> [<filter>]` |
   | Rust 二进制入口（测试挂在 bin target 上） | `source .env.test && cargo test --bin <name> [<filter>]` |
   | `web/` | `pnpm --dir web lint`、`pnpm --dir web test`（可加 `-- <file>` 收窄）、`pnpm --dir web build` |
   | `worker/`，**不触及** Cloudflare 运行时绑定（纯逻辑 / UI） | `pnpm --dir worker lint`、`pnpm --dir worker test`、`pnpm --dir worker build` |
   | `worker/`，**触及** D1 / R2 / Cloudflare 运行时绑定 | 上一行全部 + `pnpm --dir worker test:miniflare`（判据：改动涉及绑定、查询或运行时行为即必须加） |
   | `sql/migrations/**`（运行时迁移） | forward 幂等重放、`*_down.sql`（若有）回滚、旧二进制读新库的兼容判定，各自有指定用例 |
   | `sql/sqlite_*.sql`（bootstrap） | 新建库（bootstrap 路径）与既有库（迁移路径）最终 schema 一致的断言用例 |
   | `sql/publish/**`（Worker D1） | 迁移在 D1 上 forward 可应用 + `pnpm --dir worker test:miniflare` 覆盖受影响查询 |
   | `install.sh` / 打包脚本 | `bash -n install.sh` + 关键路径 dry-run 或安装冒烟证据 |
   | 仓库配置与 CI（`Cargo.toml` 非版本行、`rustfmt.toml`、`.github/workflows/**`） | 受影响 CI job 的本地等价命令，按下文「仓库配置与 CI 展开规则」现场提取；不可本地复现的部分归 D 组 |
   | 只改文档 / 索引（无代码、无配置） | 无表面 focused 门，只走 B 组的结构与链接门 |

   **Rust 行的修饰规则（不是第四行）:** feature gate 与 env/线程约束是上面三条 Rust 行的**修饰条件**，不构成独立表面：先按测试归属唯一选中 `--lib` / `--test` / `--bin` 中的一行，再把该 target 实际需要的 `--features`、env 变量与线程约束**并入同一条命令**，例如 `source .env.test && LIBRA_ENABLE_TEST_PROVIDER=1 cargo test --features test-provider --test code_ui_scenarios -- --test-threads=1`（`source .env.test &&` 前缀不可省略）。不得另跑一条不带 feature 的裸命令充当「集成行」，也不得留未替换占位符。

   **不得**为了凑一条 Rust 命令而制造与本卡无关的用例；也不得因为「C 组已有全量测试」就跳过 A 组某个表面的行。

   **仓库配置与 CI 展开规则:** 受影响 job 必须**从本卡实际改动的那个 workflow 文件现场提取**（不限于 `base.yml`；仓库当前还有 `release.yml`、`codeql.yml`、`live-compat.yml` 及 nightly workflows）。模板不保存 job→命令的映射快照（必然漂移）。两步：

   ① 列出 job，且必须限定在 `jobs:` 节点内（裸 `rg "^  [a-z...]:"` 会把 `on:` 下的 `push`/`schedule`、`permissions:` 下的 `contents`、`concurrency:` 下的 `group` 误报为 job）：

   ```bash
   awk '/^jobs:/{in_jobs=1; next} in_jobs && /^[^[:space:]]/{exit} in_jobs && /^  [A-Za-z0-9_-]+:/{print FNR ":" $0}' .github/workflows/<file>.yml
   ```

   ② 读取受影响 job 的**完整定义**——`permissions`、`strategy` / `matrix`（含 `fail-fast`）、job 与 step 级 `env`、`if`、`shell`、`run`、`uses`、`with`——再据此写判据。只看 `run:` 不够：`codeql.yml` 当前**没有任何 `run:` 步骤**，其执行步骤主要由 action 定义并同时受上述完整 job/step 字段约束；`release.yml` 也有大量语义性 `uses:` 步骤。分流：
   - **可本地复现**的步骤 → 抄成命令进 A 组，保留其 `env`、shell、`--features`、`--test-threads` 等全部约束。**依赖 GitHub checkout 的 Git 命令不得照抄**（本仓库无 `.git`，见 ER-07）：写出保持同一判据的 Libra-native 等价实现，例如 `web-check` 的 `web/out` 漂移检查用 `libra status --porcelain=v1 web/out`（输出非空即失败）替代 `git status --porcelain -- web/out`。
   - **不可本地复现**的 action-only 语义（CodeQL 扫描、artifact 上传、Homebrew tap 更新等）→ 不得凭空造命令，归入下文 **D 组远端后置门**。

   参考：`base.yml` 在 2026-07-28 的 job 集合为 `format` / `clippy` / `web-check` / `redundancy` / `test` / `network-remotes`（仅核对日快照，判定一律以现场读取的 workflow 文件为准）。

   **B 类型门**（按 `Task type` 取一行）：

   | Task type | 类型门 |
   |---|---|
   | `implementation` / `migration` / `removal` | 无额外类型门（由 A 组 + C 组构成完整验收；`family child` 自跑 A 组 + fmt/clippy，C 覆盖继承自家族唯一发布点） |
   | `docs` / `audit` / `handoff` | 结构与链接门：本卡产物文件存在且章节完整、内部链接与 `file:line` 锚点可解析、`libra status --short --branch` 无越界改动；涉及命令文档时另加 EN/zh 双份存在性检查。这三类**必须保持 no-code / no-config**：一旦发现需要改动代码或配置，不得「就地升级门」，必须先按 ER-03 把卡重分类为 `implementation` / `migration` / `removal`，同步 `Release boundary`、`Version increment`、`Release write set`，重跑粒度门后再执行 |
   | `spike` | 产物门：结论文档或 ADR 已落盘、go/no-go 已判定、承接卡已登记；**allowlist diff 门**——`libra status --short --branch` 的全部变更必须落在本卡 `Deliverables` 声明的产物内，且生产表面零改动（至少覆盖 `src/**`、`web/src/**`、`worker/src/**`、`sql/**`、`build.rs`、`install.sh`、`Cargo.toml`/`Cargo.lock`、CI 与仓库配置），用「Verification 判定口径」的退出码模板逐条守卫 |
   | `release` | 聚合守卫（本组引入的全部新守卫用例）+ release note / 兼容证据 |

   **C 发布收口门（由会推送的卡执行，顺序强制）:** ① 版本面五处 parity 预检（ER-08）→ ② 按 `Version increment` bump 五处 + 让工具链刷新 `Cargo.lock`（不手改）→ ③ 在**已 bump 的状态**上跑 `CLAUDE.md` 三门（fmt、clippy、`source .env.test && cargo test --all`）→ ④ `cargo build --release` → ⑤ 安装 → ⑥ `libra add <相关路径>` + `libra commit -s -m`（ER-07 的签名预检与提交后校验）→ ⑦ 推送并确认 branch ref：`libra push origin main` 成功且远端 ref 已更新 → ⑧ **条件步（仅当本计划要产出用户可获取的 release artifact；否则整步 `N/A`）**：创建并推送版本 tag `libra tag v<version>` + `libra push origin v<version>`，**并确认 tag 推送成功**（当前 `release.yml` 只由 `push` 的 `v*` tag 触发，**不**由 main branch push 触发——不推 tag 则 release 流水线根本不会运行）。

   三门必须覆盖 bump 后的最终状态——bump 与 `Cargo.lock` 刷新本身可能引入格式、lint 或编译回归，`cargo build --release` 不能替代 clippy 与全量测试。

   **C / D 边界（唯一口径）:** C 组**截止到已验证的 branch / tag 推送**；推送之后由远端流水线产生的一切证据——release artifact 上传与可获取性、Homebrew tap 更新、CodeQL 结果——全部归 **D 组**。任务卡必须为每个 D 组项登记：workflow 文件、job 名、**触发事件与 ref**（例如 `release.yml` = `push` `v*` tag）、以及判据。

   **D 远端后置门（post-push，仅在有不可本地复现的 CI 语义时适用）:** CodeQL 扫描、artifact 上传、Homebrew tap 更新这类步骤只能由 push / PR / tag 触发（例如 `codeql.yml` 由 push 与 PR 触发，`release.yml` 需要 tag），本地无法产出证据。因此：
   - D 组**不属于**本地验收，不阻塞 `locally-accepted`，也不改变 ER-05「先本地验收再 review」与 C 组「review 通过后才提交推送」的顺序。
   - 任务卡必须写明要看哪个 workflow 的哪个 job、**触发事件与 ref**（哪条命令触发的：`libra push origin main` 还是 `libra push origin v<version>`）、判据是什么，并在推送后取得结果。release artifact 的上传与可获取性、Homebrew tap 更新均属本组。
   - 只有当适用的 D 组门也全绿，该卡的 `Acceptance` 才能到 `complete`；此前停在新增的中间态 `remote-pending`。
   - D 组失败一律**前滚修复**（新提交 / 新版本），不得回退已推送提交或已发布 artifact；修复卡按 ER-10 的越界规则处理。

   「完成判据」的计划级门是最后一次总检查，不替代每张会推送的卡各自跑过的发布收口门。
5. **ER-05 Codex review 闭环:** 实现和本地验收完成后进行代码 review；review 问题修复后重跑相关验收，直到 review 明确给出 PASS。P0/P1 必须关闭，不得以「residual risk 已接受」替代 PASS（仅 P2 可由具名责任人书面接受）。
6. **ER-06 文档与兼容同步:** 涉及公开行为的任务必须同步用户文档 EN + zh、开发文档、兼容矩阵、错误码、help/examples、测试索引。
7. **ER-07 Libra-native 工作流与提交签名:** 本仓库使用 Libra 工作流：`libra status`、`libra add <相关路径>`、`libra commit -s -m "<scope>: <summary>"`、`libra push origin main`。不要把仓库当普通 Git 仓库处理。签名口径按 `AGENTS.md`「Commit & Pull Request Guidelines」执行 —— 提交需同时带 DCO 与 PGP 签名，并按仓库事实落地：`libra commit` **没有** `-S` 开关，`-s` 只添加 `Signed-off-by`；签名策略的优先级是 `--no-gpg-sign`（最高）> `commit.gpgSign`（Git 级联，`false` 直接关闭签名）> `vault.signing` 默认（为 `true` 且 vault unseal key 可用时签名），见 `src/command/commit.rs:215-224`、`src/command/history_config.rs:67-78`。因此：
   - 预检必须**先读 `commit.gpgSign`**（`libra config get commit.gpgSign`），未设置时再回退 `libra config get vault.signing`；`commit.gpgSign=false` 时不得当作「已启用签名」。
   - 每次提交后强制校验：`libra cat-file -p HEAD | rg -q '^gpgsig'`（注意 `libra log` 不支持 `--show-signature`，没有 `verify-commit` 子命令）。校验失败不得推送。
   - 只有在「字段全局默认与例外」的 waiver 表中存在 `豁免项 = ER-07 签名要求` 的 `EX-*`（含 Approver、Review round、证据、有效期）时，才允许以 sign-off-only 方式提交；「事实基线」最多链接该 `EX-*`，不构成独立授权路径。
   - **与 `AGENTS.md` 的已知漂移及优先级（必须按此执行）:** `AGENTS.md`「Commit & Pull Request Guidelines」示例写的是 `git commit -S -s`，「Workspace Notes」示例写的是 `libra commit -a -s`。两者在本仓库都不可照抄——本仓库没有 `.git`（必须用 `libra`），`libra commit` 没有 `-S`，而 `-a` 会把并发节点改动和带外删除一起提交（违反 GC-12 的精确暂存）。计划执行时**以 ER-07 + GC-12 为准**：`libra add <相关路径>` → `libra commit -s -m` →（按上两条做签名预检与提交后 `gpgsig` 校验）。该漂移应作为独立的文档修复项承接（更新 `AGENTS.md` 这两行），不得在计划执行中两套并行。
8. **ER-08 版本与发布:** 版本权威源是 `Cargo.toml` 的 `version`。发布前先做版本面 parity 预检：`Cargo.toml`、`web/package.json`、`worker/package.json`、`install.sh` 的 `DEFAULT_VERSION`、`install.ps1` 的 `$DefaultVersion`（后两者允许 `v` 前缀差异）必须一致；**不一致时先建立修复卡对齐，禁止直接 bump**。对齐后按任务卡的 `Version increment` 递增并同步全部五处，其余步骤与顺序按 ER-04 的「发布收口门」执行（bump 后必须重跑三门），并记录安装/发布证据。开工时须重新核对版本面文件数量是否仍为五处（2026-08-05 起为五处——`install.ps1` 由 `tests/compat/version_surface_sync.rs`（target `compat_version_surface_sync`）纳入强制相等；该守卫是版本面集合的权威）。
   - `Version increment` 取值：`patch`（默认）| `minor` | `major` | `N/A`。
   - 删除公开 surface、破坏兼容的 schema/协议变更必须用 `minor` 或 `major`，且递增级别由 ADR + 兼容窗口证据决定，不得用 patch 夹带；家族卡（G-08）的递增级别写在唯一发布点卡上，子卡为 `N/A`。
   - `docs` / `audit` / `spike` / `handoff` 卡为 `N/A`，但必须说明产物随哪次发布进入用户渠道。
9. **ER-09 push 失败策略:** 非 fast-forward 需要 pull/merge 后重新验收再推；认证、权限、网络或服务端失败不 blind retry，记录原因，待下一次修复/发布窗口处理。
10. **ER-10 内部服务错误（有界重试）:** AI provider、MCP、R2/D1、GitHub API、release/download 等错误不得直接把任务宣告完成。先分类：确定性错误（4xx 参数/权限、schema 不符、编译或配置缺陷）不重试，按范围决定归属——只有当修复落在**本卡行为轴内**，且先更新本卡 `Acceptance criteria` / `Verification` / `Implementation write set` / `Granularity` 后**重跑 ER-03 的全部 `G-*` 仍然通过**（含 G-10 写集不与在跑卡新冲突）时，才作为本卡修复项就地修；任一条不满足就新建修复卡 `FIX-*`、加一条 `FIX-* -> 当前卡` 的依赖边，并把当前卡置为 `blocked`，不得为了「顺手修完」突破粒度；暂时性错误（超时、5xx、限流、网络中断）按指数退避重试，并写明**最大尝试次数与总时间预算**（计划未另行规定时默认 ≤ 5 次、总计 ≤ 30 分钟）。超预算后把任务置为 `blocked` 并记录 sanitized 证据与升级对象，不得静默空转。发布类动作（push、release、安装）不自动重试，按 ER-09 处理。
11. **ER-11 证据卫生:** 验收证据不得保存 secret、API key、token、PII、未脱敏 transcript、绝对私有路径或原始 tool payload。需要留存时只写 sanitized summary。
12. **ER-12 并发边界与串行发布:** 并发只适用于**实现与 review 阶段**：只有 `Implementation write set` 不相交（G-10）的卡可以并发推进。**发布动作一律串行，且由单一发布者执行**：
    - 计划必须在「发布分组与并发窗口」声明发布者（哪个 Agent/人负责 C 组的 bump、构建、安装、提交、branch/tag 推送，以及推送后跟踪 D 组远端证据）。同一时刻只允许一个卡处于「已 bump 未完成推送」状态。
    - 进入发布前重新读取 `Cargo.toml` 权威版本（ER-08 的 parity 预检），按顺序做完整套发布动作后才轮到下一张卡。
    - **禁止多 Agent 并发发布。** 本仓库当前**没有**仓库级发布锁：`libra push origin main` 推送的是本地 `main` 的整个 ref tip，无法只发布一条协调记录，也无法在 push 之外提供 CAS 仲裁；靠纯文档约定实现的 lease 无法验证，属于未经实现验证的协议。若某计划确实需要并发发布，必须先用独立 ADR + 独立计划落地一个仓库级发布锁（含原子认领、fence 校验、超时回收、崩溃恢复与测试），并在本计划以 `DEFER-*` 登记；在该机制落地并通过验收之前，一律按本条串行执行。
    - 并发实现期间仍受 I–R 约束（G-10）：发布者持有发布窗口时，其它卡不得修改 `Release write set` 内的文件。

## 实施顺序

依赖边格式：`A -> B` 表示 A 必须先于 B。依赖图必须无环，且每条边都指向具体任务卡而不是整个 Phase（G-06）；任务卡拆分后本节必须同步更新。

- `OL-00 -> CH-01`（spike 的 header go/no-go 被 ChangeId 持久化消费）
- `OL-01 -> OL-06`（snapshotter 复用 `worktree_io`）
- `OL-02 -> OL-03`（v2 schema 是 codec/store 的落库前提）
- `OL-02 -> OL-04`（v2 schema 是 store 的前提）
- `OL-03 -> OL-04`（store 依赖 RepoViewV2 codec）
- `OL-04 -> OL-05`（pointer 依赖 op heads 读取）
- `OL-04 -> OL-08`（middleware 依赖 store）
- `OL-05 -> OL-06`（snapshotter 使用 pointer stale 检测）
- `OL-06 -> OL-07`（facet 捕获复用 snapshot 流程）
- `OL-06 -> OL-08`（middleware 的 external snapshot 依赖 snapshotter）
- `OL-07 -> OL-10`（restore 恢复 index/sequencer facet）
- `OL-08 -> OL-09`（CLI/Agent 接入消费 middleware）
- `OL-09 -> OL-10`（restore 子命令挂在已接入的 CLI 边界上）
- `OL-10 -> OL-11`（undo/redo 消费 RestoreEngine）
- `OL-10 -> OL-12`（doctor 复用 journal/restore 语义）
- `OL-11 -> OL-13`（多 worktree restore 依赖 undo 语义）
- `CH-01 -> CH-02`（投影/解析消费 ChangeId 类型）
- `CH-02 -> CH-03`（builder 写 change_revision 投影）
- `CH-03 -> CH-04`（genealogy 消费 builder 的 predecessor 记录）
- `OL-13 -> OL-14`（Web 图读多 head read model）
- `CH-04 -> OL-14`（Web 图展示 Change genealogy）
- `OL-12 -> OL-15`（移除 v1 前 doctor 已就绪）
- `OL-14 -> OL-15`（移除 v1 前 Web 投影已冻结事实源）

### 依赖登记表

本计划外的一切依赖关系（其它日期计划、外部服务、人工审批、上游 release），以及本计划向外移交的范围，都必须在此登记后才能被任务卡引用（G-06）。计划内任务之间的依赖直接写任务 ID，不进本表。

`direction` 区分方向：`incoming` = 本计划等待外部产物；`outgoing` = 本计划把范围移交给别的计划（此时 Owner 是接收方，「超时与失败策略」写接收方未接手时的回落处理）。

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | incoming | 外部参照 | Jujutsu 源码 pin（设计文档 §5.1/§5.5 引用的 `jj/lib/src/op_store.rs`、`transaction.rs`、`local_working_copy.rs`、`commit_builder.rs`、`backend.rs` 等） | 本计划执行者 | 开工时刷新外部参照 revision（plan-long 记录 `6e98d068b`），可用性判据：参照文件存在且能打开 | `plan-long.md:91`；设计文档 §5.1、§5.5；核对日期 2026-08-22 | 参照不可用时按 GC-01 以本仓库现有实现为事实基线，并在「修订历史」登记例外；不阻塞任务 |
| DEP-02 | incoming | 外部服务 | Git 兼容性验证（header 的真实 Git 读取、`git fsck`、push/clone 行为，以及 sidecar-only 对象闭包） | 本计划执行者 | OL-00 需要真实 Git 二进制可运行；判据：spike 测试中真实 Git 命令退出码；header 互操作证据不等于 Libra header 写入承诺 | OL-00（`commit_change_id_header_spike`） | Git 不可用时 spike 验收阻塞并等待环境恢复；sidecar-only 是设计决策，不替代真实 Git 证据 |

### 发布分组与并发窗口

默认每张卡独立发布（G-07）。本计划未登记合并发布 `REL-*` 组：OL-00 为 `no-release` spike；其余实现/迁移/移除卡各自是完整发布切片。v1 移除（OL-15）是单一可独立发布切片，按 `removal` 卡独立发布（minor bump），不构成 G-08 家族卡。

**本次联合执行窗口:** `REL-OL-02-04-foundation` 仅在 `feat/scheam-update` 内联合落地 OL-02、OL-03、OL-04。唯一发布动作是最终签名 commit 与该 feature branch 的 origin push；窗口内五个版本面保持 `0.22.14`，不创建 tag/release artifact，不推送 upstream。联合窗口不改变三张卡各自的行为轴、验收记录或后续依赖；OL-05～OL-09/OL-15 仍按原计划推进。

**并发声明:** 实现阶段可并发组（`Implementation write set` 互不相交，G-10）：组 A = {OL-01, OL-02, CH-01}（`worktree_io/`、`db.rs+sql+model`、`change/identity.rs` 互不相交）；组 B = {OL-03, CH-02}（`operation/view.rs+facet.rs` vs `change/store.rs+resolve.rs`）；组 C = {OL-05, CH-03}；其余卡按依赖边串行推进。任何并发组必须满足：I–I 不相交、发布窗口不重叠（ER-12）。

**发布者:** 每张独立发布卡的 C 组（bump/构建/安装/提交/branch 推送）由执行该卡的同一 Agent 执行；计划收口（OL-15）的发布与 D 组远端证据跟踪由计划收口负责人执行。同一时刻只允许一张卡处于「已 bump 未完成推送」状态。

**发布窗口顺序:** 按依赖边顺序执行：OL-01/OL-02/CH-01（组 A 内实现可并发，发布串行）→ OL-03/OL-04/CH-02 → OL-05/OL-06 → OL-07/OL-08 → OL-09 → OL-10 → OL-11/OL-12 → CH-03 → CH-04 → OL-13 → OL-14 → OL-15。当前 `REL-OL-02-04-foundation` 是已登记的联合窗口；每个后续发布点仍需重读 `Cargo.toml` 权威版本并做 ER-08 parity 预检。

### Phase 0: 基线冻结与设计消歧

**目标:** 冻结设计文档 §5 的结构体/函数定稿；完成 Change ID 写入路径 spike，并冻结 Libra sidecar-only 写入协议（已有 header 仅作导入兼容）。

**进入条件:**

- 本计划与设计文档（`docs/development/internal/operation-log-working-copy-change-id.md`）已合入；事实基线锚点刷新通过。

**退出条件:**

- OL-00 完成：`commit_change_id_header_spike` target 注册并运行；ADR-OL-04 的 sidecar-only 决策已记录；CH-01/CH-03 承接卡就绪。

### Phase 1: 持久化与 I/O 底座

**目标:** 落地 v2 SQLite schema、`worktree_io` 通用 bounded I/O、StateFacet/RepoViewV2 codec 与 OperationStoreV2（含 op-head CAS 与 journal）。

**进入条件:**

- Phase 0 退出条件满足。

**退出条件:**

- Gate-1 满足：对象格式、journal phase、I/O 协议冻结；`cargo test internal::operation::store` 与 `cargo test --test operation_dag` 全绿；status 零回归 benchmark 通过。

### Phase 2: 完整快照

**目标:** 实现 WorkspaceStatePointer stale 检测、WorkspaceSnapshotter 扫描捕获与 index/sequencer/sparse facet 适配器，使单 worktree 状态可完整恢复。

**进入条件:**

- Phase 1 退出条件满足。

**退出条件:**

- Gate-2 满足：facet restore policy、untracked/ignored/large-file 政策冻结；`workspace_snapshot_roundtrip` / `index_snapshot_roundtrip` / `sequencer_snapshot_roundtrip` 全绿。

### Phase 3: CLI + Agent 全修改记录

**目标:** 接入 `middleware::run_with_operation` 与 `classify_command`，使全部 CLI 命令与 Agent tool mutation 进入统一 Operation 模型。

**进入条件:**

- Phase 2 退出条件满足。

**退出条件:**

- Gate-3 满足：shadow mismatch、失败 operation、lease takeover 处理冻结；`operation_command_coverage` / `agent_shell_operation` 全绿；census zero-unclassified guard 通过。

### Phase 4: 可逆用户工作流

**目标:** 开放 crash-safe undo/redo/restore/revert 与 doctor 修复能力。

**进入条件:**

- Phase 3 退出条件满足。

**退出条件:**

- Gate-4 满足：crash/数据安全 review 通过、秒级 SLO 达标、机器接口冻结；`operation_restore_faults` / `op_undo_redo` 全绿；crash matrix 用例通过。

### Phase 5: 稳定逻辑身份

**目标:** 实现 ChangeId 128-bit 类型、随机/legacy synthetic 生成与 change 投影/解析。

**进入条件:**

- Phase 0 的 sidecar-only 写入决策与已有 header 导入语义已定；Phase 1 的 v2 schema 已含 change 表。

**退出条件:**

- Gate-5 满足：Change ID 格式、legacy synthetic、sidecar 投影与既有 header 导入语义一致；`cargo test internal::change::identity` 与 `change_id_resolution` 全绿。

### Phase 6: Rewrite 与 Agent 逻辑链接

**目标:** 通过 ChangeRevisionBuilder 唯一构建/重写入口接入 commit/amend/rebase/squash/split/duplicate，记录 typed predecessor 谱系并关联 AI 因果对象。

**进入条件:**

- Phase 5 退出条件满足。

**退出条件:**

- Gate-6 满足：relation 语义、hook/Git 兼容、AI FileHistoryStore 读取迁移完成；`change_genealogy_rebase` / `change_genealogy_squash_split` 全绿。

### Phase 7: 并发与 Web 展示

**目标:** 支持多 worktree Operation heads/reconcile；提供只读 Operation/Change Web 图。

**进入条件:**

- Phase 4 与 Phase 6 退出条件满足。

**退出条件:**

- Gate-7 满足：multihead fallback、redaction、bounded graph 冻结；`operation_restore_multi_worktree` 与 web graph tests 全绿。

### Phase 8: 移除 v1 与 GA

**目标:** 删除 v1 代码/表/命令路径，文档与测试收口，签名发布。

**进入条件:**

- Phase 7 退出条件满足；v1 与 v2 无并存需求。

**退出条件:**

- Gate-8 满足：`rg 'operation_wrapper|operation_view|restorable' src` 零命中；`docs/commands/op.md` EN+zh、`COMPATIBILITY.md`、`docs/error-codes.md` 同步；签名 release 完成。

## 任务卡

任务 ID 使用稳定前缀：`OL-*`（Operation Log / 快照 / Undo / 收口）与 `CH-*`（Change ID / genealogy）。编号被引用后不重排；拆分出的新卡在所属 Phase 末尾追加新编号，因此**编号顺序 ≠ 执行顺序**，执行以「实施顺序」的依赖边和各卡 `Dependencies` 为准。废弃的编号保留并标记替代关系。

### 任务卡粒度规则（强制）

粒度是任务卡质量的第一判据：卡过大则无法 review、无法回滚、无法交给单个 Agent 完成；卡过碎则实现、测试与文档脱节，且每片都要付一次发布成本。新增或修改任务卡时必须逐条满足下列 `G-*` 规则。任一条不满足即为「粒度不合格」，必须在开工前拆分或合并，并同步实施顺序、依赖登记表、发布分组、追溯表、测试矩阵、里程碑、风险表的任务归属和修订历史。

- **G-01 单一行为轴与恢复模式（上限）:** 一张卡只承担一个**可独立恢复**的行为轴——该卡失败或需要撤回时，存在**单一已声明的恢复动作**，执行后系统停在一个自洽状态，不留半吊子中间态。这里的「恢复」不等于「一次 `libra revert`」：不可逆变更同样合格，只要恢复路径是单一且已写明的。禁止把「协议变更 + 容量门 + 前端接入」「删 A + 删 B + 删 C」「新增能力 + 顺带重构既有实现」压进同一张卡。快速判据：`Description` 中出现两个以上并列的「并且 / 同时 / 顺带 / 以及」，先按「推荐拆分维度」拆。恢复形式必须在 `Rollback mode` 字段声明为四种模式之一：
  - `revert`：纯本地代码/文档变更，一次 revert 即完整撤销（默认）。
  - `forward-only`：已产生不可逆数据或迁移（SQLite/D1/R2/对象库），只能前滚修复；必须写出数据不变量、恢复验证命令和用户影响，**不得**为了凑「可 revert」而设计不安全的 down migration。
  - `compensating`：对外部服务已有副作用（发布、删除、远端写入），撤销靠补偿动作；必须写出补偿命令与幂等键。
  - `immutable-release`：已发布 artifact 不可撤回，只能发新版本；必须写出降级指引与兼容窗口。
- **G-02 完整可交付（下限）:** 一张卡必须是一个自洽的可验收增量。同一行为轴的实现、测试、文档、兼容矩阵与索引同步是**同一张卡**的验收内容，禁止拆成「实现卡 / 补测试卡 / 补文档卡」。只有当被拆出的部分本身就是独立可恢复的行为轴（G-01 意义上：有单一已声明的恢复动作——独立迁移、独立 deprecation 收口、独立性能门、独立 UI 切面、跨计划移交）时才允许单独成卡。
- **G-03 条目上限与计数口径:** 上限按 `Task type` 取值，计数按**独立判据**而非行数。ER-04 的强制门（fmt / clippy / 全量测试 / 表面门）**不计入**本条计数，`Verification` 只登记本卡特有的判据：

  | Task type | AC 上限 | Verification 上限 |
  |---|---|---|
  | `implementation` / `migration` / `removal` | 8 | 8 |
  | `spike` | 8 | 8 |
  | `docs` / `audit` / `handoff` | 20 | 20 |
  | `release` | 12 | 12（只计聚合守卫、release note、兼容证据等本卡特有项） |

  计数细则：
  - AC 按「独立 pass/fail 谓词」计。一条 checklist 内用「且 / 并且 / 以及 / 同时」连接的多个可分别失败的断言按多条计；嵌套子列表逐项计；表格行逐行计。
  - Verification 按「独立验证门」计。判据是「是否构成一次独立的通过/失败判定」：环境准备前缀（`source .env.test`、`LIBRA_*=1` 赋值、`cd`、`export`）与其后的命令合计为**一门**；一条命令中的多个 `--test` target 分别计；`&&` 串联两个都会独立判定的验收命令按两门计；手工证据按项计。
  - 超限视为多轴信号，必须拆卡，**不得**通过合并长句、塞进表格或改写成「等等」来规避。
  - 文档 / 审计 / 索引-only 卡的条目是清单项、不构成独立行为轴，故适用 20 条上限；这类卡仍受 G-01 约束，且必须在任务卡 `Deliverables` 字段登记产物范围（具体文件清单）——这是常规登记，不是例外，无需进 waiver 表。需要突破本表上限时，只能在 waiver 白名单登记 `EX-*`（具名审批），不得私自改写分母。
- **G-04 规模上限（可计数）:** `Estimated scope` 的开工态只允许 `S` 或 `M`。`L`/`XL` 只能作为「必须再拆」的中间标注，计划成稿后不得存在 L/XL 卡。计数**只统计行为实现落点与生产文件**，不统计「随附同步集」：
  - **计入**：承载本卡行为变更的生产代码落点与文件（`src/**`、`sql/migrations/**`、`web/src/**`、`worker/src/**` 等）。
  - **不计入（随附同步集）**：本卡自己的测试文件、按 GC-05/ER-06 强制同步的文档集（`docs/commands/<cmd>.md` EN+zh、`docs/development/**`、`COMPATIBILITY.md`、`docs/error-codes.md`）、测试索引（`tests/INDEX.md`、`tests/compat/README.md`、`Cargo.toml` 的 `[[test]]`）、以及 ER-08 的版本面五处。这些是每张卡的固定成本，不构成粒度信号；但仍要在写集字段中如实列出（文档/测试进 `Implementation write set`，版本面五处进 `Release write set`）。
  - **仓库根文件**（`Cargo.toml`、`install.sh`、`COMPATIBILITY.md` 等）按「单个文件」计，不各占一个落点；若某张卡的行为变更**就发生在**根文件本身（例如改 `build.rs` 逻辑），则该文件计为一个落点。
  - `S`：≤ 2 个行为落点、≤ 3 个生产文件，无 schema / 协议 / 公开接口变更。
  - `M`：≤ 4 个行为落点、≤ 12 个生产文件，最多一处公开行为或接口变化，仍是单一行为轴。
  - 超出 `M` 的计数即为 L：默认必须拆分。确实不可拆的机械变更（全仓重命名、批量删除、格式化）可在「字段全局默认与例外」的 waiver 白名单中登记 `EX-*`（需具名审批人与 review 轮次），写明为何不可拆、如何 review、如何恢复；此时该卡 `Estimated scope` 写 `L-exception:EX-<n>`，这是全文唯一允许出现 `L` 字样的形式，`XL` 永不允许。
  - 把 `src/`、`tests/`、`docs/` 或仓库根算作「一个落点」是规避行为，按「粒度反模式速查」的「落点注水」处理。
- **G-05 Agent 可独立执行:** 一张卡必须能在不阅读其它卡正文的前提下被执行：`Current evidence` 给出可核对的 `file:line` 锚点，`Acceptance criteria` 自洽可判定，`Verification` 是可直接复制执行的确切命令，`Dependencies` 只引用「依赖登记表」中的 `DEP-*` / 任务 ID。禁止「见上文」「同上一卡」式跨卡隐式约定；确属跨卡共享的约定要提升为全局工程约束或 ADR。
- **G-06 依赖闭合且无环:** 依赖必须有向无环。本计划内依赖直接引用任务 ID；跨计划与外部前置必须先在「依赖登记表」登记为 `DEP-*` 再引用，不得在卡内自由描述。互相等待、循环依赖、以及「等某个 Phase 整体完成」都是拆分错误——把依赖收敛到具体前置卡。「实施顺序」的依赖边与各卡 `Dependencies` 必须一致；不一致时以「实施顺序」为准并当场修正卡片。
- **G-07 发布切片对齐（按任务类型）:** 默认「一张卡 = 一个发布切片」（独立 review + ER-04 门 + 版本 + 提交 + 推送）。适用范围按 `Task type`（G-11）区分：`implementation` / `migration` / `removal` 必须走完整发布切片；`docs` / `audit` / `spike` / `handoff` 卡不 bump 版本、不产出 artifact，`Release boundary` 写 `no-release` 并说明其产物随哪次发布进入用户可见渠道；`release` 卡本身就是发布点（家族卡的唯一发布点必须是 `release` 卡，见 G-08）。任何「多卡合并发布」都是例外，必须在「发布分组与并发窗口」登记 `REL-*`：成员、唯一发布点、窗口期禁止插入的内容、失败时的逆序回滚顺序。例外必须先修订计划并通过 Codex review 才可开工，**不得**在开工时凭笔记临时合并。
- **G-07a 批量发布组（`batch-release child`）:** 当一组卡各自都是完整、可独立回滚的变更，但**逐卡发版对用户没有独立价值**（例如同一 wave 内的文档/账本/测试产物，或版本号推进会与并发计划频繁抢号）时，可登记为「批量发布组」：成员卡逐张 review、逐张通过全部适用的 A/B 门与三门、逐张提交并**推送**，但不 bump、不构建、不 tag、不产 artifact；全组共用一个唯一发布点卡（`Task type` 必须是 `release`），由它一次性完成版本面与 tag/artifact。与 G-08 家族卡的区别是**成员会推送**且各自可独立回滚（前滚补偿），因此不要求「变更不可分割」；与 G-07 默认的区别是把「发布」从每卡收敛到一次。必须在「发布分组与并发窗口」登记 `REL-*`（成员、唯一发布点、窗口规则、失败回滚顺序、理由），并在每张成员卡的 `Release boundary` 写 `batch-release child of REL-<n>`，**唯一发布点卡的 `Release boundary` 写 `batch-release point of REL-<n>`**（2026-08-06 补充：该取值属 G-07a 自身，不要写成 G-08 的 `family release point`——后者的前提是「成员不推送、变更不可分割」，与本形态互斥）。**成员卡的 `Acceptance` 在发布点完成前不得标为 `complete`，也不得对外报告完成。**
- **G-08 家族卡（不可分割变更的唯一出路）:** 当一次公开 surface 删除、或 schema 与 reader 必须同时上线这类变更确实无法切成可独立发布的切片时，用「家族卡」表达：拆成多张各自 review、各自通过全部适用 ER-04 门、各自本地提交的子卡，共用一个唯一发布点卡；**该发布点卡的 `Task type` 必须是 `release`**（不引入新行为，只做版本、构建、安装、聚合守卫与发布证据），以保证它在 ER-04 的 B 组中唯一命中 `release` 行。家族内子卡仍受除 G-07 外的全部 `G-*` 约束；子卡 `Release boundary` 写 `family child`，发布点卡写 `family release point`，家族边界与「不推送窗口」写进 `REL-*` 登记。
- **G-09 拆分协议:** 拆分已被引用的卡时，原编号保留给主轴，新子卡在所属 Phase 末尾追加新编号，不重排既有编号。原卡必须写明「拆出 `<ID>`、`<ID>`」，新卡写明「自 `<ID>` 拆出」，并同步实施顺序、依赖登记表、「发布分组与并发窗口」、追溯表、测试矩阵、里程碑、风险表，以及「修订历史」中的一行（日期、原因、原卡、新卡、受影响引用）。
- **G-10 写集与并发:** 写集分三类，每张卡必须声明前两类（第三类由 ER-12 统一定义，卡内不重复）：
  - **`Implementation write set`（I）**：承载本卡行为的代码、测试、文档文件。
  - **`Release write set`（R）**：ER-08 的版本面五处、`Cargo.lock`、release artifact。对所有发布卡相同；`family child` 与 `no-release` 卡写 `N/A`（它们不 bump、不构建、不推送）；`batch-release child` 同样写 `N/A`（它推送，但不 bump、不构建、不产 artifact——版本面归其批量发布组的唯一发布点）。
  - **协调写集（C）**：计划级的发布顺序与窗口记录（「发布分组与并发窗口」的发布者、发布顺序、`REL-*` 登记）。由 ER-12 的单一发布者串行维护，**不计入**任何卡的 I 或 R，也不参与并发判定。

  冲突规则：
  - **I–I 相交** → **禁止并发，无豁免通道**（G-10 不在 waiver 白名单内）：只有两个合法出路——补一条顺序依赖边，或把相交部分合并到唯一集成卡。
  - **I–R 相交**（某卡把 `Cargo.toml`、`install.sh` 等当行为落点，而它同时属于别的卡的 R）→ 在**已声明的串行发布窗口**内（ER-12），该窗口对 R 内文件是写锁：其它卡不得在此期间修改这些文件，必须等窗口结束或补顺序边。
  - **R–R 相交** → 由 ER-12 的串行发布窗口顺序化，不构成并发禁止条件。
  - `Files likely touched` 是估计值，并发判定以 `Implementation write set` 为准。
- **G-11 任务类型:** 每张卡必须声明 `Task type`，不同类型适用不同粒度口径：
  - `implementation`：默认类型，全部 `G-*` 条款全量适用。
  - `migration`：数据/schema 迁移，`Rollback mode` 通常为 `forward-only`，必须有 up/down 或前滚验证与故障注入用例。
  - `removal`：公开 surface 删除，通常进入家族卡（G-08），必须先有 deprecation 窗口证据。
  - `spike`：探索/验证，**不得**改动生产代码。必须写出待回答的问题、时间箱、产物（结论 + ADR 或缺口登记）、go/no-go 退出标准与后续承接卡；不适用 G-04 的文件计数，`Estimated scope` 按时间箱判定：`S` ≤ 0.5 人日、`M` ≤ 2 人日，超出即拆成多个问题或直接转 ADR / `implementation` 卡。
  - `audit` / `docs`：只读核对或文档收敛，按 G-03 的文档-only 口径执行。规模上限按**产物文件数或人日**判定（不适用 G-04 的生产文件计数）：`S` ≤ 5 个产物文件或 ≤ 0.5 人日；`M` ≤ 15 个产物文件或 ≤ 2 人日；超出即拆卡。随代码卡强制同步的文档仍按 G-04 的随附同步集处理，不计入这里。
  - `release`：发布点卡，不引入新行为，只做版本、构建、安装、聚合守卫与发布证据。
  - `handoff`：跨计划移交，默认 `no-release`。**移入**（本计划承接他人）在「依赖登记表」登记 `direction: incoming`；**移出**（本计划把范围交给别的计划）登记 `direction: outgoing`，并写明接收方计划、移交日期、本计划不再重做的部分，以及接收方未接手时的回落处理。

#### 推荐拆分维度

超限卡按下列维度之一切开；切完每片仍须独立满足 G-01（单一行为轴 + 已声明的恢复模式）。

| 维度 | 切法 | 典型结果 |
|---|---|---|
| 数据 / 状态轴 | schema 扩展 → 写入幂等与持久化 → 读取投影与恢复 | 3 张卡 |
| 协议轴 | 协议版本与协商 → 容量 / 背压与性能门 → 消费端接入 | 3 张卡 |
| 表面轴 | 后端 runtime → 机器接口（JSON / 事件 / 错误码） → 前端或 UI 组件 | 2–3 张卡 |
| 生命周期轴 | 新实现上线 → 默认切换 → deprecation shim → 物理删除 | 按发布窗口分卡 |
| 安全轴 | 身份与请求边界 → 文件 / 进程 posture → 敏感信息 redaction | 每轴一卡 |
| 清理轴 | 公开 surface 删除（家族卡） → 内部模块退场 → 依赖摘除 | 家族卡 + 普通卡 |

#### 粒度反模式速查

| 反模式 | 症状 | 处理 |
|---|---|---|
| 巨型卡 | `Estimated scope` = L；AC > 8；Description 含多个并列目标 | 按「推荐拆分维度」拆分（G-01/G-03/G-04） |
| 碎片卡 | 「补测试」「补文档」「改个字段名」单独成卡 | 合并回所属行为轴（G-02） |
| 多轴伪装 | 把多条 AC 合成一条长句、塞进表格或写「等等」以压到 8 条以内 | 按独立谓词还原计数后重新判定（G-03） |
| 落点注水 | 把 `src/` 或仓库根算作「一个落点」以保住 S/M | 按目录级落点重新计数（G-04） |
| 隐式依赖 | Description 写「按 X 卡的约定」而 X 卡未交付该约定 | 写进本卡，或提升为全局约束 / ADR（G-05） |
| 幽灵验收 | `Verification` 只写 `cargo test --all`，或零命中守卫不区分 `rg` 退出码 `1` 与 `>1` | 指定 target 与 test fn；按「Verification 判定口径」的退出码模板重写（G-05） |
| 悬空依赖 | `Dependencies` 写「Phase N 完成」或自由描述外部前置 | 收敛到具体前置卡 ID / `DEP-*`（G-06） |
| 假回滚 | 已发布或已迁移数据的卡仍写「一次 revert 撤销」 | 按实际选 `forward-only` / `compensating` / `immutable-release`（G-01） |
| 并发冲撞 | 两张无依赖的卡实现写集相交 | 只有两条出路：补顺序边，或合并到唯一集成卡（G-10 不可豁免）。版本面争用不算并发冲突，由 ER-12 的串行发布窗口处理 |
| 顺手合并 | 多张卡凭开工笔记合成一次发布 | 登记为 `REL-*` 家族卡，或拆回独立发布切片（G-07/G-08） |

#### 字段全局默认与例外

计划在本节声明字段的全局默认值后，任务卡中**取默认值的字段可以整行省略**，或写 `Inherited`；只有偏离默认的字段才在卡内展开并在下表登记。`Task type`、`Lifecycle / Acceptance`、`Rollback mode`、`Implementation write set`、`Version increment`、`C/D coverage from`、`Granularity` 摘要行是每卡必填，不可省略；`Release write set` 可写 `Inherited` 或 `N/A`；`Deliverables` 对 `docs` / `audit` / `spike` / `handoff` 卡必填。

- **Release boundary 默认:** 每张卡独立发布切片
- **Task type 默认:** implementation
- **Rollback mode 默认:** revert
- **Migration and rollback 默认:** N/A：无 schema 迁移
- **Security and privacy 默认:** 继承 GC-07、GC-11
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 按 GC-05 同步 EN + zh

**默认覆盖**（不是例外，只是取了非默认值，无需审批）：

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| OL-02 | `Rollback mode` | `forward-only`：SQLite v2 schema 直接替换 v1，无 down migration；一次性导入脚本 + 数据不变量与恢复验证命令 |
| OL-02 | `Version increment` | `minor`：破坏性 schema 替换（ADR-OL-01） |
| OL-15 | `Version increment` | `minor`：移除 v1 公开命令路径（ADR-OL-01 + 设计文档 §5.1 作为替换窗口证据） |
| OL-14 | `Docs and compatibility impact` | `web/` 改动按 `web/package.json` scripts 验收，另同步 `docs/development/commands/*.md` |

**规则 waiver（`EX-*`，需具名审批）**：可豁免的规则是**白名单**，只有下表三项；`G-01`、`G-02`、`G-05`、`G-06`、`G-07`、`G-08`、`G-09`、`G-10`、`G-11` **永不可豁免**（它们是可 review、可恢复、可并发的前提）。

| 可豁免项 | 允许的理由范围 |
|---|---|
| G-03 条目上限 | 清单型产物（文档 / 审计 / 索引）确实需要超过本类上限，且已写明产物文件清单 |
| G-03 条目上限（**门族型验收**，2026-08-06 扩充） | `implementation` 卡的验收本质是**同一恢复轴上的机械门族/fixture 清单**（净室、schema、绑定、投影、锁等），逐门计数必然超过本类上限，而按门拆卡会违反 G-01/G-02（同一行为轴的实现/测试/文档不得拆散）与「碎片卡」反模式。准入条件（缺一不可）：① 每个门都是**可复制执行的具名命令**，失败即整卡不达标；② 门族清单在卡内「判据规范（非计数正文）」块逐条枚举；③ 分子如实写作 `n/上限@EX-ID`，不得以合并长句掩盖；④ 门族增减时同批更新 waiver 行与粒度审计表 |
| G-04 规模上限（`L-exception`） | 不可拆的机械变更：全仓重命名、批量删除、格式化 |
| ER-07 签名要求 | 仓库策略层面的具名豁免（sign-off-only） |

| 例外 ID | 任务（或 `ALL/<作用域>`） | 豁免项 | 理由与补偿措施 | Approver | Review round | 证据 | 有效期 |
|---|---|---|---|---|---|---|---|
| EX-01 | ALL/operation-log-v2 | ER-07 签名要求 | 本仓库实际无 `libra` CLI 可执行、无 `.git` 时回退 `git commit -S -s`（AGENTS.md 口径）；补偿 = 每次提交校验 `gpgsig` 存在，且与 ER-07 的漂移登记在「修订历史」 | 仓库维护者 | R1 | `git config commit.gpgSign` + 提交后 `gpgsig` 校验 | 本计划内 |

#### 任务卡粒度审计表

计划成稿与每次规范性修订后填一次，逐卡汇总各卡 `Granularity` 行，便于机械核对与脚本校验。判定规则：任一列不达标即不得开工；`AC` / `VER` / `scope` 列超限时必须带 `@EX-ID` 或 `L-exception:EX-n`，且该 `EX-*` 必须同时满足：存在于 waiver 表、`任务` 列等于引用它的卡（或显式写 `ALL/<作用域>` 的计划级豁免）、`豁免项` 等于被超限的那条规则、理由落在白名单、且仍在有效期内。任一条不满足仍判为不达标。

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| OL-00 | spike | Change ID 写入路径 go/no-go | 结论可推翻，无生产改动 | yes | yes | 5/8 | 3/8 | 1 / 0 | M | DEP-02 | no-overlap | no-release | N/A | N/A |
| OL-01 | implementation | worktree_io 抽取 | revert 单提交 | yes | yes | 5/8 | 3/8 | 2 / 6 | M | none | no-overlap | independent | N/A | N/A |
| OL-02 | migration | v2 schema 替换 v1 | forward-only 前滚 + 导入脚本 | yes | yes | 6/8 | 3/8 | 3 / 5 | M | none | no-overlap | independent | N/A | N/A |
| OL-03 | implementation | RepoViewV2/StateFacet codec | revert 单提交 | yes | yes | 5/8 | 2/8 | 1 / 2 | S | OL-02 | 序列化于 OL-02 | independent | N/A | N/A |
| OL-04 | implementation | OperationStoreV2 + op-head CAS | revert 单提交（v2 表未发布前） | yes | yes | 6/8 | 2/8 | 1 / 2 | S | OL-02, OL-03 | 序列化于 OL-03 | independent | N/A | N/A |
| OL-05 | implementation | workspace pointer stale 检测 | revert 单提交 | yes | yes | 4/8 | 1/8 | 1 / 1 | S | OL-04 | 序列化于 OL-04 | independent | N/A | N/A |
| OL-06 | implementation | WorkspaceSnapshotter 捕获 | revert 单提交 | yes | yes | 6/8 | 2/8 | 1 / 2 | S | OL-01, OL-05 | 序列化于 OL-05 | independent | N/A | N/A |
| OL-07 | implementation | index/sequencer/sparse facet | revert 单提交 | yes | yes | 6/8 | 3/8 | 3 / 6 | M | OL-06 | 序列化于 OL-06 | independent | N/A | N/A |
| OL-08 | implementation | middleware run_with_operation | revert 单提交 | yes | yes | 6/8 | 1/8 | 1 / 2 | S | OL-04, OL-06 | 序列化于 OL-06 | independent | N/A | N/A |
| OL-09 | implementation | CLI/Agent mutation 接入 | revert 单提交 | yes | yes | 6/8 | 3/8 | 3 / 8 | M | OL-08 | 序列化于 OL-08 | independent | N/A | N/A |
| OL-10 | implementation | RestoreEngine 恢复 | revert 单提交 + journal 前滚 | yes | yes | 6/8 | 3/8 | 2 / 4 | M | OL-07, OL-09 | no-overlap | independent | N/A | N/A |
| OL-11 | implementation | undo/redo/revert 追加式 | revert 单提交 + 前滚 | yes | yes | 7/8 | 3/8 | 2 / 4 | M | OL-10 | 序列化于 OL-10 | independent | N/A | N/A |
| OL-12 | implementation | doctor 修复引擎 | revert 单提交 | yes | yes | 5/8 | 2/8 | 2 / 3 | S | OL-10 | 序列化于 OL-10 | independent | N/A | N/A |
| CH-01 | implementation | ChangeId 类型与生成 | revert 单提交 | yes | yes | 5/8 | 2/8 | 1 / 2 | S | OL-00, OL-02 | no-overlap | independent | N/A | N/A |
| CH-02 | implementation | change 投影与解析 | revert 单提交 | yes | yes | 5/8 | 2/8 | 1 / 2 | S | CH-01 | 序列化于 CH-01 | independent | N/A | N/A |
| CH-03 | implementation | ChangeRevisionBuilder 重写入口 | revert 单提交 + 前滚修复 | yes | yes | 6/8 | 3/8 | 3 / 8 | M | CH-02 | 序列化于 CH-02 | independent | N/A | N/A |
| CH-04 | implementation | genealogy 多边与 AI 链接 | revert 单提交 + 前滚修复 | yes | yes | 6/8 | 3/8 | 3 / 7 | M | CH-03 | 序列化于 CH-03 | independent | N/A | N/A |
| OL-13 | implementation | 多 worktree heads/reconcile | revert 单提交 + 前滚 | yes | yes | 5/8 | 3/8 | 2 / 4 | M | OL-11 | 序列化于 OL-11 | independent | N/A | N/A |
| OL-14 | implementation | Operation/Change Web 图 | revert 单提交 | yes | yes | 6/8 | 3/8 | 3 / 8 | M | OL-13, CH-04 | no-overlap | independent | N/A | N/A |
| OL-15 | removal | v1 代码/表/命令移除 | revert 单提交（发布后前滚） | yes | yes | 6/8 | 2/8 | 4 / 10 | M | OL-12, OL-14 | 序列化于 OL-14 | independent | N/A | N/A |

#### Verification 判定口径

- **零命中守卫必须区分「无命中」与「命令失败」。** `rg` 的退出码是 `0` = 有命中、`1` = 零命中、`>1` = 执行失败（非法正则、路径不存在、I/O 错误）。`! rg …` 和 `if rg …; then exit 1; fi` 都会把 `>1` 误判为通过，**不得**单独作为证据。裸 `rg …; rc=$?` 也不行——在 `set -e` 下零命中会让脚本在取 `$?` 前就退出。使用下列模板（已在 `bash -e` 与 `zsh -e` 下验证三条分支）：

  ```bash
  if rg -n "<pattern>" <paths>; then
    echo "FAIL: forbidden pattern found"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- 「只允许 allowlist 命中」类守卫必须逐条比对固定 allowlist，并在任务记录中附命中 diff。
- 仅用于定位符号的 `rg` 必须注明「锚点定位用，非判据」。
- 本任务新增的 test fn / 场景过滤必须标 `(new)`，并确保其所属 `--test` target 已存在，或在同卡内注册（`tests/compat/*` 还需 `Cargo.toml` `[[test]]`）并同步 `tests/INDEX.md`。
- `cargo test --all` 不能替代任务指定用例（ER-04）；反过来，指定用例也不能替代计划完成前的全量 L1 门（见「完成判据」）。

### Task OL-00: Change ID 写入路径 spike 与设计冻结

**Task type:** spike（G-11）

**Lifecycle / Acceptance:** done / complete

**Description:** 回答设计文档 §2 遗留的 go/no-go：用真实 Git 验证 commit header 与 sidecar-only 的对象/互操作行为，并冻结 Libra sidecar-only 写入协议。已有 header 只作为导入兼容信息，不能成为 Libra 的写入依赖。产出结论文档与 ADR 更新（ADR-OL-04），登记承接卡 CH-01/CH-03。唯一行为轴：Change ID 持久化路径的可行性判定。

**Out of scope:** 不实现 ChangeId 类型（CH-01 承接）；不实现 builder（CH-03 承接）；不改变 Libra 运行时行为。经用户授权，本卡额外包含两处与行为无关的 CI Rust 基线修复：`src/internal/ai/agent_bridge/methods.rs` 将恒定字符串的无效 `format!` 改为 `.to_string()`；`src/internal/ai/agent_bridge/vcs.rs` 将 rustdoc 无法解析的 `shutdown` 链接改为 `supervisor::shutdown`。

**Current evidence（开工前）:** 设计文档 §2「Change ID生成算法」与 §5.4 Gate-0；`src/internal/db.rs:557-606` 现有 v1 表无 change 表；开工前 `tests/INDEX.md` 无 `commit_change_id_header_spike` target（现已注册并验证）。

**Acceptance criteria:**

- [x] 新建 `tests/commit_change_id_header_spike.rs` target，同卡注册 `Cargo.toml [[test]]` 与 `tests/INDEX.md`；spike 用例验证「带 change-id header 的 commit 经真实 Git 读取/fsck 不报错」与「sidecar-only 方案对象闭包可枚举」两种路径的行为差异。
- [x] 结论文档 `docs/development/internal/change-id-header-spike.md` 落盘，写明 go/no-go、验证数据、风险与承接卡 ID。
- [x] ADR-OL-04 已按 spike 结论更新：Libra sidecar-only 写入；已有 header 仅作导入兼容信息。
- [x] OL-00 行为表面零改动：`git status --short --branch`（当前环境无 `libra` 二进制，使用等价 Git 现状检查）的全部变更落在本卡 `Deliverables` 产物内；生产文件变更仅为用户授权的两处无行为 CI Rust 基线修复。
- [x] 承接卡 CH-01/CH-03 已在计划中登记 sidecar-only 决策与已有 header 导入语义的消费点。

**Verification:**

- [x] `RUSTUP_TOOLCHAIN=stable LIBRA_SKIP_WEB_BUILD=1 CARGO_PROFILE_TEST_DEBUG=0 CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo test --test commit_change_id_header_spike`（真实 Git 是硬前置；Git 不可用时测试失败，不能记录 `N/A` 或用 sidecar-only 结论替代）
- [x] `cargo +nightly fmt --all --check` 通过；allowlist diff 守卫确认除 `src/internal/ai/agent_bridge/methods.rs` 与 `src/internal/ai/agent_bridge/vcs.rs` 的用户授权 CI Rust 基线修复、`Cargo.toml` 的 test target 注册、`tests/INDEX.md` 注册和 OL-00 文档/测试产物外，`src/**`、`web/src/**`、`worker/src/**`、`sql/**`、`build.rs`、`install.sh`、CI 与仓库配置均未改动。
- [x] `RUSTUP_TOOLCHAIN=stable LIBRA_SKIP_WEB_BUILD=1 cargo clippy --all-targets --all-features -- -D warnings`（base.yml Rust clippy job）通过。
- [x] `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links" RUSTUP_TOOLCHAIN=stable LIBRA_SKIP_WEB_BUILD=1 cargo doc --no-deps --all-features`（base.yml Rust doc job）通过。
- [x] 手工证据：`docs/development/internal/change-id-header-spike.md`、ADR-OL-04 更新和 CH-01/CH-03 承接引用均已落盘（sanitized）。

**Dependencies:** DEP-02（Git 兼容性验证可用性）

**Deliverables:** `docs/development/internal/operation-log-working-copy-change-id.md`（ADR-OL-04 决策修订）、`docs/development/internal/change-id-header-spike.md`、`tests/commit_change_id_header_spike.rs`（含 `Cargo.toml [[test]]` 与 `tests/INDEX.md` 行）、用户授权的 CI Rust 基线修复

**Implementation write set:** `tests/commit_change_id_header_spike.rs`、`Cargo.toml`（`[[test]]` 行）、`tests/INDEX.md`、`docs/development/internal/operation-log-working-copy-change-id.md`（ADR 修订）、`docs/development/internal/change-id-header-spike.md`、`src/internal/ai/agent_bridge/methods.rs`、`src/internal/ai/agent_bridge/vcs.rs`（仅 CI Rust 基线修复）

**Release write set:** N/A（no-release）

**Files likely touched:** `tests/commit_change_id_header_spike.rs`、`Cargo.toml`、`tests/INDEX.md`、`docs/development/internal/operation-log-working-copy-change-id.md`、`docs/development/internal/change-id-header-spike.md`、`src/internal/ai/agent_bridge/methods.rs`、`src/internal/ai/agent_bridge/vcs.rs`

**Docs and compatibility impact:** 仅内部设计文档与 spike 结论；无用户命令文档变化。

**Rollback mode:** revert（结论文档可推翻，无生产改动）

**Migration and rollback:** N/A

**Security and privacy:** N/A（spike 不接触真实用户数据；测试数据本地临时目录）

**Performance budget:** N/A

**Estimated scope:** M（时间箱 ≤ 2 人日；spike 按 G-11 不适用 G-04 文件计数）

**Version increment:** N/A（spike 产物随 CH-01 承载发布进入用户渠道）

**Release boundary:** no-release（产物为文档 + spike target，不 bump 版本、不产 artifact）

**C/D coverage from:** CH-01（spike 产物被 CH-01 消费，C 覆盖继承自 CH-01 的发布切片）

**Granularity:** type=spike; axis=Change ID 写入路径 go/no-go + CI Rust prerequisites; recovery=结论可推翻，基线修复可 revert; complete=yes; self-contained=yes; AC=5/8; VER=5/8; landing=3; prod-files=2; scope=M; deps=DEP-02; writeset=no-overlap; release=no-release; split-from=N/A; exception=N/A

### Task OL-01: 抽取通用 bounded 只读 I/O executor（worktree_io）

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete

**Description:** 从 `src/command/status_io_worker.rs` 抽取通用只读 I/O executor 到 `src/internal/worktree_io/`，协议带 root-relative capability 与 byte/frame 预算，helper 不持有 ODB/SQLite/refs 写权限；`status_io_worker.rs` 改为复用该 executor（行为零回归）。唯一行为轴：bounded 只读 I/O 基础设施。

**Out of scope:** 不改动 status 输出语义；不实现快照扫描（OL-06 承接）；不实现写路径。

**Current evidence:** `src/command/status_io_worker.rs` 现状为 status 专用 worker；设计文档 §5.1 `src/internal/worktree_io/` 模块落点与 §5.3.2「扫描 I/O 全部走 WorktreeIo」。

**OL-01 执行记录（2026-08-29，基于 `upstream/main...HEAD`）：**

- 基线 commit：`995fc1fa`（`upstream/main`）；当前实现分支：`codex/ol-01-worktree-io`。
- CI 对齐工具链：stable `1.98`、nightly `1.100`。
- 任务拆分与提交：

  | 提交 | 范围 |
  |---|---|
  | `7a98b01a` | 固定 worker 契约测试：并发/pending 上限、deadline、取消、错误传播与 checkpoint |
  | `814a385b` | 抽取 `internal::worktree_io` 只读协议、frame codec、路径编码与分离 capability |
  | `ea15cd6b` | 抽取 bounded executor、稳定队列、absolute deadline、取消及 helper 回收 |
  | `7173adaa` | status 适配与 capability seal 缓存性能修复；保持原有 deadline/partial/fail-closed 语义 |
  | `0efa4623` | stable 1.98 暴露的 `upstream/main` 既有测试 lint 兼容修复：两处 `collapsible_if` 与三个未使用 import，纯机械、无行为变化 |

- 安全边界：worktree 请求只携带密封 root capability 与严格 canonical relative path；协议和两端校验不执行文件系统探测；worktree 文件/目录读取仍经 `beneath::open_root`/no-follow 操作。object-store capability 独立、local-only 且不 hydration；helper 不具备 ODB/SQLite/refs 写入口。
- 兼容边界：`status_io_worker` 继续承载 handler、root session、deadline glue，以及既有 crate-internal `deadline_*`、hidden worker dispatch、数据类型与输出适配；未改变 CLI 输出。`change-id-header-spike.md` 不属于 OL-01 写集，OL-00 的 sidecar-only/header-import 结论不变。
- 性能 P1 修复：初次抽取发现 parent/helper 对同一 root 重复 seal；`7173adaa` 将 parent capability seal 限定到 status session，并让长寿命 helper 按 root key 复用 sealed capability（root 变化重新 seal），每次真实读仍重新经过 beneath 边界。
- 审查轮次：本地 benchmark audit 发现并关闭上述 seal 重复 P1；独立 `gpt-5.6-sol` `reasoning_effort=high` review 为下一轮（R1）pending，若产生 P0/P1 则由 Luna `reasoning_effort=xhigh` 修复并复审，不能将本地测试结果当作最终 review 结论。

**Acceptance criteria:**

- [x] `src/internal/worktree_io/` 提供 bounded 只读 I/O executor：root-relative capability、byte/frame 预算、超时与可取消性；helper 无 ODB/SQLite/refs 写权限。
- [x] `status_io_worker.rs` 复用 `worktree_io`，行为零回归（`status` 输出与既有测试一致）。
- [x] 新增单元测试覆盖预算超限、慢挂载超时、取消与错误传播（`cargo test --lib internal::worktree_io`）。
- [x] status 零回归 benchmark：大型工作区 `status` 耗时/内存不劣于抽取前基线（见下方记录）。
- [x] 文档同步：本卡、设计文档与 status 开发文档已记录边界；本卡未新增 Cargo test target，因此 `tests/INDEX.md` 不变。

**Verification:**

- [x] `LIBRA_SKIP_WEB_BUILD=1 CARGO_PROFILE_TEST_DEBUG=0 CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo test --lib internal::worktree_io`：13/13。
- [x] `LIBRA_SKIP_WEB_BUILD=1 CARGO_PROFILE_TEST_DEBUG=0 CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo test --lib status_io_worker`：10/10。
- [x] 12 个精确 `command_test` status/diff 回归各运行 1 次并通过：`tracked_unreadable_path_fails_closed_not_deleted`、`probe_budget_bounds_wide_directory`、`main_scan_mid_iteration_readdir_error_reports_partial`、`ioblocked_scan_does_not_replace_dirty_cache`、`probe_symlinked_escape_is_reported_not_followed`、`probe_toctou_directory_swap_is_blocked`、`object_read_budget_timeout_is_cancellable`、`json_scan_ioblocked_reports_partial_without_writing_cache`、`tracked_scan_io_timeout_is_reported_not_hung`、`probe_worker_killed_mid_stream_keeps_checkpoint`、`diff_rename_object_read_budget_timeout_is_cancellable`、`diff_rename_pairs_blob_larger_than_worker_frame_cap`。
- [x] `cargo +nightly fmt --all --check` 与 `git diff --check` 通过。
- [x] 手工/脚本证据：benchmark 基线对比（sanitized 数字，见下方）。
- [x] `LIBRA_SKIP_WEB_BUILD=1 CARGO_PROFILE_TEST_DEBUG=0 CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo test --test command_test status`：289 passed / 0 failed / 2781 filtered，耗时 287.67s。
- [x] `LIBRA_SKIP_WEB_BUILD=1 CARGO_PROFILE_TEST_DEBUG=0 CARGO_BUILD_JOBS=1 RUST_MIN_STACK=16777216 cargo test --test compat_serial_registry`：18/18。
- [x] `RUSTUP_TOOLCHAIN=stable LIBRA_SKIP_WEB_BUILD=1 cargo clippy --all-targets --all-features -- -D warnings`：通过。
- [x] 按用户要求以 focused 测试为准；已完成提交、push/PR，PR #460 已关联 issue #452。

说明：本轮未运行全量 `cargo test --all`，不在本轮用户要求范围；fmt 已通过，最终验收仍以独立 review 与发布收口为准。

**Benchmark 证据（同一 runner、`benchmark/run.sh`、release profile、5000 files、3 warmup、10 runs）：**

| 状态 | 基线 `995fc1fa` | OL-01 优化后 `7173adaa` | 变化 |
|---|---:|---:|---:|
| clean wall median | 198.605 ms | 202.626 ms | +2.0%（+4.021 ms） |
| clean RSS | 34,963,456 B | 35,078,144 B | +114,688 B（约 +0.3%） |
| dirty wall median | 203.079 ms | 202.620 ms | -0.2%（-0.459 ms） |
| dirty RSS | 35,299,328 B | 35,430,400 B | +131,072 B（约 +0.4%） |

以上未同时触发“wall 至少 +10% 且 +5 ms”或“RSS 至少 +10% 且 +4 MiB”门槛，故 benchmark 通过；独立 review 与发布收口仍未完成。

**Dependencies:** 无

**Implementation write set:** `src/internal/worktree_io/`、`src/command/status_io_worker.rs`、相关测试与索引

**Release write set:** Inherited

**Files likely touched:** `src/internal/worktree_io/{mod,executor,protocol}.rs`、`src/command/status_io_worker.rs`、`tests/**`、`tests/INDEX.md`

**Docs and compatibility impact:** 仅内部开发文档；无用户命令文档变化（行为零回归）。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：helper 无写权限即 fail-closed 边界。

**Performance budget:** 继承 GC-10；`status` 热路径不得引入无界扫描；benchmark 断言记录在卡内。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=worktree_io 抽取; recovery=revert 单提交; complete=yes; self-contained=yes; AC=5/8; VER=3/8; landing=2; prod-files=6; scope=M; deps=none; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A

### Task OL-02: SQLite v2 schema 替换 v1 operation 表

**Task type:** migration

**Lifecycle / Acceptance:** done / complete

**Description:** 按设计文档 §5.2.5 用 v2 表（`operation/operation_parent/operation_head/operation_journal/change_identity/change_revision/change_predecessor/ai_operation_link`）替换 v1 表的 canonical 角色，借助版本化 copy-first migration 将现有 v1 表迁移到 `legacy_operation*` 命名空间，保持 active v1 operation logging；同步完整 SeaORM model。旧行不伪造为 v2 view、journal、head、genealogy 或 AI link，无法无损映射的字段保留在 legacy 数据中。唯一行为轴：v2 持久化 schema 与安全 staging 落地。

**Out of scope:** 不实现 OperationStoreV2 读写逻辑（OL-04 承接）；不实现 codec（OL-03 承接）；不切换 CLI/Agent middleware（OL-05～OL-09）；不删除 legacy 表（OL-09/OL-15 承接）。

**Current evidence:** `src/internal/db.rs:557-606`（v1 `OPERATION_SCHEMA_SQL`）；`src/internal/model/` 现有 v1 operation model；设计文档 §5.2.5 SQL。

**Acceptance criteria:**

- [x] v2 八张表按设计文档 §5.2.5 定义落地；版本化 migration 在单事务中完成 v1 五张表到 `legacy_*` 的 copy-first、行数/关键字段校验、原子 rename 与 canonical v2 DDL。
- [x] SeaORM model 与 v2 八表及 legacy operation 表一一对应；`operation_head` 支持 `(repo_id, scope_key)` 多 head 行（CAS 用）。
- [x] 不做有损的一次性 v1→v2 业务导入；旧 operation/view/workspace 数据无损保留在 legacy 命名空间，v2 不生成伪造历史关系。
- [x] 新建库（bootstrap + migration 路径）与既有 v1 数据库（迁移路径）最终 schema 一致，且旧关键字段保留。
- [x] `operation_schema_v2` 覆盖 fresh、data-bearing copy、idempotence、rollback、reopen；active operation service/wrapper 回归通过。
- [x] 计划与设计文档记录 legacy staging、禁止双写和 OL-09/OL-15 删除条件；版本冻结窗口保持五个版本面 `0.22.14`。

**Verification:**

- [x] `cargo test --test operation_schema_v2 -- --nocapture`：fresh/data-bearing schema convergence、重复迁移、失败回滚与 reopen 用例通过。
- [x] `cargo test --test operation_service_test`：10/10；`cargo test --test operation_wrapper_test`：17/17，确认 active v1 logging 继续访问 legacy 表。
- [x] 手工审计：v2 migration 不生成 v2 历史 view/journal/head/genealogy/AI link；legacy 删除条件与 no-dual-write 约束已写入计划/设计文档。
- [x] 版本面 parity 预检保持 `0.22.14`；本窗口不执行 bump、tag 或 artifact 发布。

**Dependencies:** 无

**Implementation write set:** `src/internal/db.rs`、`sql/sqlite_*.sql` 或 `sql/migrations/**`、`src/internal/model/`、导入脚本、相关测试与索引

**Release write set:** Inherited

**Files likely touched:** `src/internal/db.rs`、`sql/**`、`src/internal/model/**`、`docs/COMPATIBILITY.md`、`docs/error-codes.md`

**Docs and compatibility impact:** `COMPATIBILITY.md` 记录存储格式 v2 替换；`docs/development/commands/*.md` 同步；无用户命令文档变化（命令表面不变）。

**Rollback mode:** forward-only（SQLite v2 schema 不可逆，前滚修复 + 导入脚本与数据不变量校验）

**Migration and rollback:** v1→v2 无 down migration；数据不变量：`operation` 主键与 `operation_parent` 外键一致、`change_revision` 主键唯一；恢复验证：导入脚本 + `doctor`（OL-12 上线前用脚本校验）。

**Security and privacy:** 继承 GC-07：导入脚本只处理审计 ID，不迁移 prompt/transcript/secret。

**Performance budget:** 继承 GC-10：`operation_head` 查询走 `(repo_id, scope_key)` 索引；无全表扫描热路径。

**Estimated scope:** M

**Version increment:** minor（破坏性 schema 替换，ADR-OL-01）

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=migration; axis=v2 schema 替换 v1; recovery=forward-only 前滚 + 导入脚本; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=3; prod-files=5; scope=M; deps=none; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A

### Task OL-03: StateFacet / RepoViewV2 / WorkspaceSnapshotV2 codec

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete

**Description:** 实现 `operation/facet.rs`（`StateFacet` trait、`FacetRegistry`、`RestorePolicy`、`FacetCapture`）与 `operation/view.rs`（`RepoViewV2`/`WorkspaceSnapshotV2` canonical serialization 与闭包校验），manifest 为版本化 canonical 格式（map key 排序、禁止浮点/隐式默认、hash 前 schema validation）。唯一行为轴：v2 view/facet 类型与 codec。

**Out of scope:** 不实现快照扫描（OL-06）；不实现持久化读写（OL-04）；不实现 facet 具体适配器（OL-07）。

**Current evidence:** 设计文档 §5.2.1/§5.2.2 结构体定义；`src/internal/db.rs:557-606`（v1 view 表将被替代）。

**Acceptance criteria:**

- [x] `StateFacet` trait 与 `FacetRegistry`（`FacetName -> Box<dyn StateFacet>`）实现；未注册 facet 的 capture 不能标 `fully_restorable`（fail closed）。
- [x] `RepoViewV2`/`WorkspaceSnapshotV2` 按设计文档字段实现 canonical codec；反序列化对未知 schema_version 报错。
- [x] 闭包校验：`roots()` 枚举全部引用对象；缺对象时校验失败。
- [x] 单元测试覆盖 canonical 序列化 roundtrip、schema 版本拒绝、闭包缺对象失败路径，以及 facet metadata canonical/registration policy。
- [x] `RestorePolicy` 三种取值（AutoRestore/Rebuild/NeverRestore）与设计文档一致，并由 registry 的完整性判定使用。

**Verification:**

- [x] `cargo test --lib internal::operation::view` 与 `cargo test --lib internal::operation::facet`：canonical/closure/registry 用例通过。
- [x] `cargo +nightly fmt --all --check` 与 clippy `-D warnings` 通过；本窗口无凭据依赖。
- [x] 版本面保持 `0.22.14`；不 bump、不打 tag、不生成 release artifact。

**Dependencies:** OL-02（v2 表/类型基线）

**Implementation write set:** `src/internal/operation/view.rs`、`src/internal/operation/facet.rs`、`src/internal/operation/mod.rs`（re-export）、相关单元测试

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/{view,facet,mod}.rs`

**Docs and compatibility impact:** 仅内部开发文档（模块注释）。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：codec 反序列化输入必须 schema 校验后才计算 hash。

**Performance budget:** 继承 GC-10：canonical 序列化无无界内存；manifest 大小有界。

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=RepoViewV2/StateFacet codec; recovery=revert 单提交; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=2; scope=S; deps=OL-02; writeset=序列化于 OL-02; release=independent; split-from=N/A; exception=N/A

### Task OL-04: OperationStoreV2、journal 与 op-head CAS

**Task type:** implementation

**Lifecycle / Acceptance:** done / complete

**Description:** 实现 `operation/store.rs`：`OperationV2` 类型、`OperationStoreV2`（`write_view_manifest`/`write_operation`/`cas_update_op_heads`/`load_view`/`append_journal`/`read_heads`）、journal phase 记录；新增 `operation_dag` 集成 target 验证多父 DAG、CAS 并发失败与 head 保留。唯一行为轴：v2 operation 持久化与发布 CAS。

**Out of scope:** 不实现 middleware（OL-08）；不实现 snapshotter（OL-06）；不实现多 head reconcile（OL-13）。

**Current evidence:** 设计文档 §5.2.3/§5.3.3；`src/internal/operation.rs:1018`（v1 `persist_operation_graph` 将被替换）；`src/internal/db.rs:557-606`。

**Acceptance criteria:**

- [x] `OperationV2` 字段（`pre_view_oid`/`post_view_oid`/`kind`/`status`/`restores_op_id`/`reverts_op_id`/`predecessor_map_oid` 等）与设计文档一致；`OperationMetaV2` 只存 redacted causal ID。
- [x] `cas_update_op_heads` 在旧 head 与期望一致时发布，不一致时失败并返回当前 heads；多 head 行保留并可查询。
- [x] journal 记录 phase（reserved/pre_view/mutation/post_view/publish），重启/重新读取可识别未完成 entry。
- [x] Store 单元测试与 `operation_dag` 覆盖 CAS 失败、多父 DAG、多 head 与 journal 持久化。
- [x] v1 `persist_operation_graph` 未被新代码调用；CLI/Agent middleware 接入明确留给 OL-05～OL-09。

**Verification:**

- [x] `cargo test --lib internal::operation::store` 与 `cargo test --test operation_dag`：通过多父、CAS 冲突/保持原 head、journal 读回。
- [x] `Cargo.toml [[test]]` 与 `tests/INDEX.md` 已注册 `operation_schema_v2`、`operation_dag`。
- [x] 版本面保持 `0.22.14`；本窗口只允许最终签名 feature-branch commit/push。

**Dependencies:** OL-02（v2 表）、OL-03（RepoViewV2 codec）

**Implementation write set:** `src/internal/operation/store.rs`、`src/internal/operation/mod.rs`、`tests/operation_dag.rs`、`Cargo.toml`（`[[test]]`）、`tests/INDEX.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/store.rs`、`src/internal/operation/mod.rs`、`tests/operation_dag.rs`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** 仅内部开发文档。

**Rollback mode:** revert（v2 表在 OL-02 已发布后，本卡代码可 revert；数据在 v2 表内）

**Migration and rollback:** N/A（OL-02 已处理 schema；本卡只写代码）

**Security and privacy:** 继承 GC-07/ER-11：`OperationMetaV2` 不得写入 prompt/transcript/secret。

**Performance budget:** 继承 GC-10：journal append 与 op-head 查询走索引；每 op 元数据有界。

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=OperationStoreV2 + op-head CAS; recovery=revert 单提交（v2 表未发布前）; complete=yes; self-contained=yes; AC=6/8; VER=2/8; landing=1; prod-files=2; scope=S; deps=OL-02,OL-03; writeset=序列化于 OL-03; release=independent; split-from=N/A; exception=N/A

### Task OL-05: WorkspaceStatePointer 与 stale/sibling 检测

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `operation/working_copy.rs`：`WorkspaceStatePointer`（`last_op_id`/`last_snapshot_oid`/`generation`）的 `load`/`save` 与 `staleness`（Fresh/Stale/Sibling）判定。唯一行为轴：worktree 状态指针与陈旧检测。

**Out of scope:** 不实现扫描（OL-06）；不实现 reconcile（OL-13）。

**Current evidence:** 设计文档 §5.3.2；`src/internal/worktree_scope.rs`（RequestScope pinning 现状）。

**Acceptance criteria:**

- [ ] `WorkspaceStatePointer::load/save` 读写 workspace 本地状态，字段与设计文档一致。
- [ ] `staleness` 依据 op heads 祖先关系与 generation 返回 Fresh/Stale/Sibling 三态；Stale 与 Sibling 的区分规则有测试。
- [ ] 单元测试覆盖 pointer 丢失、generation 落后、sibling 分叉（`cargo test --lib internal::operation::working_copy`）。
- [ ] 文档注释说明 pointer 与 `operation_head` 的关系。

**Verification:**

- [ ] `source .env.test && cargo test --lib internal::operation::working_copy`（含 `(new)` 用例）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-04（读 op heads）

**Implementation write set:** `src/internal/operation/working_copy.rs`、`src/internal/operation/mod.rs`、相关单元测试

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/working_copy.rs`、`src/internal/operation/mod.rs`

**Docs and compatibility impact:** 仅内部开发文档。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** N/A

**Performance budget:** 继承 GC-10：pointer 读写 O(1)。

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=workspace pointer stale 检测; recovery=revert 单提交; complete=yes; self-contained=yes; AC=4/8; VER=1/8; landing=1; prod-files=1; scope=S; deps=OL-04; writeset=序列化于 OL-04; release=independent; split-from=N/A; exception=N/A

### Task OL-06: WorkspaceSnapshotter 与 working copy 捕获

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `operation/snapshot.rs`：`WorkspaceSnapshotter::capture` 与 `scan_working_copy`——锁 worktree → 经 `WorktreeIo` 扫描 tracked/untracked → 变化文件写 Git blob/tree → 构建 `WorkspaceSnapshotV2` manifest → 发布 `ExternalSnapshot`/`Command` operation；无变化不发布。新增 `workspace_snapshot_roundtrip` target。唯一行为轴：单 worktree 工作区捕获为可恢复快照。

**Out of scope:** 不实现 index/sequencer/sparse facet 适配器（OL-07）；不实现 middleware 编排（OL-08）；不把快照变成 Git commit（ADR-OL-05）。

**Current evidence:** 设计文档 §2「Libra 准备如何借鉴」、§5.3.2；`src/command/status_io_worker.rs`（OL-01 抽取后成为 executor）。

**Acceptance criteria:**

- [ ] `capture` 流程：锁 → 扫描 → 变化则写 blob/tree/facets → 发布 pure snapshot op；无变化不发布 op。
- [ ] tracked 增量比较基于 Git index 与 stat 信息；untracked 按 capture policy 捕获、ignored 默认排除；文件持续变化或容量超限时标记 `Partial`，不发布为 fully-restorable。
- [ ] `WorkspaceSnapshotV2` manifest 写入 `ClientStorage`；`completeness`/`facet_restore_policies` 正确设置。
- [ ] `workspace_snapshot_roundtrip` target 注册（`Cargo.toml [[test]]` + `tests/INDEX.md`）并全绿：快照后修改文件再恢复，tracked/untracked 内容一致。
- [ ] 快照失败路径（扫描超时、对象写失败）返回 `SnapshotError` 并留可诊断状态。
- [ ] 文档注释说明「自动 snapshot 不产生 Commit OID/Change ID」边界。

**Verification:**

- [ ] `source .env.test && cargo test --test workspace_snapshot_roundtrip`（`(new)` target，同卡注册）
- [ ] `source .env.test && cargo test --lib internal::operation::snapshot`（含 `(new)` 用例）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-01（worktree_io）、OL-05（pointer stale 检测）

**Implementation write set:** `src/internal/operation/snapshot.rs`、`src/internal/operation/mod.rs`、`tests/workspace_snapshot_roundtrip.rs`、`Cargo.toml`（`[[test]]`）、`tests/INDEX.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/snapshot.rs`、`tests/workspace_snapshot_roundtrip.rs`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** 仅内部开发文档。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：untracked 捕获受 policy 限制，不越界读取。

**Performance budget:** 继承 GC-10：扫描 I/O 全部走 `WorktreeIo`（byte/frame 预算），无无界扫描。

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=WorkspaceSnapshotter 捕获; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=2/8; landing=1; prod-files=2; scope=S; deps=OL-01,OL-05; writeset=序列化于 OL-05; release=independent; split-from=N/A; exception=N/A

### Task OL-07: index / sequencer / sparse facet 适配器

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现设计文档 §5.2.1 的三个 StateFacet 适配器：raw index byte-exact 捕获（intent-to-add/skip-worktree/assume-unchanged/stat 位）、sequencer 状态捕获、sparse view 捕获；各 facet 接入 `FacetRegistry` 并参与 `Completeness` 判定。新增 `index_snapshot_roundtrip` 与 `sequencer_snapshot_roundtrip` target。唯一行为轴：index/sequencer/sparse 三类 facet 的捕获与恢复。

**Out of scope:** 不实现 restore 引擎（OL-10）；不实现工作区文件扫描（OL-06）。

**Current evidence:** 设计文档 §5.2.2（`raw_index_blob_oid`/`sequencer_facet_oid`/`sparse_facet_oid`）；`src/internal/sequencer/mod.rs` 现有 sequencer 状态。

**Acceptance criteria:**

- [ ] raw index facet 保存 byte-exact index 位；`index_snapshot_roundtrip` 验证恢复后 index 与原始字节一致（含 intent-to-add/skip-worktree 等位）。
- [ ] sequencer facet 捕获/恢复 sequencer 状态；`sequencer_snapshot_roundtrip` 验证 rebase/cherry-pick 中间态可恢复。
- [ ] sparse facet 捕获/恢复 sparse view 状态，纳入 capture policy。
- [ ] 三 facet 接入 `FacetRegistry`；任一 facet 捕获失败时快照标 `Partial` 且不得标 fully-restorable。
- [ ] 新增 target 注册（`Cargo.toml [[test]]` + `tests/INDEX.md`）并全绿。
- [ ] 文档注释说明 facet 的 `RestorePolicy` 归属。

**Verification:**

- [ ] `source .env.test && cargo test --test index_snapshot_roundtrip`（`(new)`）
- [ ] `source .env.test && cargo test --test sequencer_snapshot_roundtrip`（`(new)`）
- [ ] `source .env.test && cargo test --lib internal::operation::facet`（含 `(new)` 用例）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-06（snapshot 流程与 registry）

**Implementation write set:** `src/internal/operation/`（facet 适配器文件）、`src/internal/sequencer/`（如需暴露只读状态）、`tests/index_snapshot_roundtrip.rs`、`tests/sequencer_snapshot_roundtrip.rs`、`Cargo.toml`、`tests/INDEX.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/facets/**`（或 `operation/` 下适配器）、`src/internal/sequencer/mod.rs`、`tests/index_snapshot_roundtrip.rs`、`tests/sequencer_snapshot_roundtrip.rs`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** 仅内部开发文档。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：raw index 恢复只写本 worktree 受控路径。

**Performance budget:** 继承 GC-10：raw index 捕获有界（文件大小与 index 行数线性，无重复全量扫描）。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=index/sequencer/sparse facet; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=3; prod-files=6; scope=M; deps=OL-06; writeset=序列化于 OL-06; release=independent; split-from=N/A; exception=N/A

### Task OL-08: middleware run_with_operation 与 classify_command

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `operation/middleware.rs`：`MutationClass`（七类穷举）、`classify_command`、`run_with_operation`（pin RequestScope → 获取 worktree lease → stale 检测 → 外部变化先发 snapshot op → running op + journal reservation → 业务闭包 → post-view 捕获与闭包验证 → CAS 发布 + pointer 更新）；未知类 fail closed。唯一行为轴：统一 mutation 执行边界。

**Out of scope:** 不接入具体 CLI/Agent 调用点（OL-09）；不实现 RestoreEngine（OL-10）。

**Current evidence:** 设计文档 §5.3.1；`src/cli.rs:336`（`enum Commands`）、`:1681`（`command_scope`）。

**Acceptance criteria:**

- [ ] `MutationClass` 七类与 `classify_command` 穷举映射实现；未知/新增命令类 fail closed 拒绝执行。
- [ ] `run_with_operation` 实现 pre-view 规则：入口检测到外部变化时先发布 `ExternalSnapshot` op，其 `post_view_oid` 作为命令 op 的 `pre_view_oid`。
- [ ] running op 与 journal reservation 在业务闭包执行前落盘；失败 op 保留为可诊断状态（Running→Success/Failed/Partial/Aborted）。
- [ ] 发布路径 CAS + workspace pointer 原子推进；CAS 失败返回当前 heads 并重试或报并发。
- [ ] 单元/集成测试覆盖七类分类、未知类拒绝、external snapshot 前置、失败 op 状态（`cargo test --lib internal::operation::middleware`）。
- [ ] 文档注释说明 `InternalWorker` 分流（不产生 Operation）。

**Verification:**

- [ ] `source .env.test && cargo test --lib internal::operation::middleware`（含 `(new)` 用例）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-04（store）、OL-06（snapshotter）

**Implementation write set:** `src/internal/operation/middleware.rs`、`src/internal/operation/mod.rs`、相关单元测试

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/middleware.rs`、`src/internal/operation/mod.rs`

**Docs and compatibility impact:** 仅内部开发文档。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：未知 mutation fail closed。

**Performance budget:** 继承 GC-10：middleware 路径无额外全量扫描（复用 snapshot 结果）。

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=middleware run_with_operation; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=1/8; landing=1; prod-files=2; scope=S; deps=OL-04,OL-06; writeset=序列化于 OL-06; release=independent; split-from=N/A; exception=N/A

### Task OL-09: CLI 与 Agent tool mutation 接入

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 把 `classify_command` 接入 `src/cli.rs` 的 `enum Commands` 穷举与三处 dispatch（`:1474/:1681/:2123`），并把 Agent 侧 `src/internal/ai/tools/*` 与 `src/internal/ai/libra_vcs.rs` 的持久修改 tool call 前后接到 `run_with_operation`；新增 `operation_command_coverage` 与 `agent_shell_operation` target 与 census zero-unclassified guard。唯一行为轴：全 CLI/Agent mutation 进入统一 Operation 记录。

**Out of scope:** 不实现 undo/restore 命令（OL-10/OL-11）；不改变命令语义。

**Current evidence:** `src/cli.rs:336/1474/1681/2123`；`src/internal/ai/tools/*`、`src/internal/ai/libra_vcs.rs`；设计文档 §5.3.1「Agent 侧在 ai/tools/* 与 ai/libra_vcs.rs 的每个会产生持久修改的 tool call 前后调用同一入口」。

**Acceptance criteria:**

- [ ] `classify_command` 对全部 `Commands` 变体穷举分类，新增变体不分类无法编译（编译期 census guard）。
- [ ] Agent tool 持久修改调用点接入 `run_with_operation`；只读 tool 不产生 Operation。
- [ ] `operation_command_coverage` target 验证代表性命令的 Operation 记录（kind/status/pre/post view 存在）；target 注册并全绿。
- [ ] `agent_shell_operation` target 验证 Agent shell 产生的修改被识别为 `ExternalOrUnknown`/external snapshot 或正确归因；target 注册并全绿。
- [ ] census zero-unclassified guard（零命中守卫）通过：无命令停留在 Unknown 类。
- [ ] `docs/development/commands/*.md` 与 `tests/INDEX.md` 同步。

**Verification:**

- [ ] `source .env.test && cargo test --test operation_command_coverage`（`(new)` target，同卡注册）
- [ ] `source .env.test && cargo test --test agent_shell_operation`（`(new)` target，同卡注册）
- [ ] census zero-unclassified guard（按「Verification 判定口径」退出码模板，区分 `rg` 退出码 `1` 与 `>1`）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-08（middleware）

**Implementation write set:** `src/cli.rs`、`src/internal/ai/tools/*`、`src/internal/ai/libra_vcs.rs`、`tests/operation_command_coverage.rs`、`tests/agent_shell_operation.rs`、`Cargo.toml`、`tests/INDEX.md`

**Release write set:** Inherited

**Files likely touched:** `src/cli.rs`、`src/internal/ai/tools/**`、`src/internal/ai/libra_vcs.rs`、`tests/operation_command_coverage.rs`、`tests/agent_shell_operation.rs`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** 按 GC-05 同步 `docs/development/commands/*.md`；用户命令表面不变。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：Agent tool 未知 mutation fail closed；`ExternalOrUnknown` 无法验证时拒绝执行。

**Performance budget:** 继承 GC-10：middleware 复用快照结果，不新增热路径全量扫描。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=CLI/Agent mutation 接入; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=3; prod-files=8; scope=M; deps=OL-08; writeset=序列化于 OL-08; release=independent; split-from=N/A; exception=N/A

### Task OL-10: RestoreEngine 与 op restore 子命令

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `operation/restore.rs`：`RestoreEngine::restore`（按 `RestoreWhat` 选择 facet，先写 intent journal，按固定锁序恢复，逐 facet 幂等，失败可前滚到 target 或回退 pre-view，dry-run receipt）；新增 `libra op restore` 子命令与 `operation_restore_faults` target。唯一行为轴：可恢复状态的选择性恢复引擎。

**Out of scope:** 不实现 undo/redo/revert（OL-11）；不实现 doctor（OL-12）；repo-wide refs 恢复需显式确认的行为在 OL-10 内实现但默认拒绝。

**Current evidence:** 设计文档 §5.3.4；`src/command/op.rs`（现有 v1 op 命令路径，最终在 OL-15 替换）。

**Acceptance criteria:**

- [ ] `RestoreEngine::restore` 支持 facet 选择、固定锁序、幂等重放；每步可前滚到 target 或回退 pre-view。
- [ ] `op restore` 子命令（含 `--dry-run` 与 JSON/machine receipt）实现；repo-wide refs/其它 worktree 需要显式确认。
- [ ] 恢复后 `Completeness`/facet 状态一致；恢复失败不产生半吊子状态（journal 可回退）。
- [ ] `operation_restore_faults` target 覆盖：恢复中断、对象缺失、并发写冲突、dry-run receipt 内容；target 注册并全绿。
- [ ] `docs/commands/op.md` EN+zh、`docs/error-codes.md` 同步新子命令与新错误码。
- [ ] 机器接口冻结：receipt 字段与退出码有契约测试。

**Verification:**

- [ ] `source .env.test && cargo test --test operation_restore_faults`（`(new)` target，同卡注册）
- [ ] `source .env.test && cargo test --test command_test op restore`（回归过滤）
- [ ] 手工/脚本证据：dry-run receipt 的 JSON 结构与退出码（sanitized）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-07（index/sequencer/sparse facet 恢复能力）、OL-09（CLI 接入边界）

**Implementation write set:** `src/internal/operation/restore.rs`、`src/command/op.rs`、`tests/operation_restore_faults.rs`、`Cargo.toml`、`tests/INDEX.md`、`docs/commands/op.md`（EN+zh）、`docs/error-codes.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/restore.rs`、`src/command/op.rs`、`tests/operation_restore_faults.rs`、`docs/commands/op.md`、`docs/commands/zh-CN/op.md`、`docs/error-codes.md`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** `docs/commands/op.md` EN+zh 新增 `op restore`；`docs/error-codes.md` 新增错误码。

**Rollback mode:** revert（单提交回滚；恢复过程本身由 journal 前滚/回退保护）

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：repo-wide refs 与其它 worktree 恢复需显式确认，默认拒绝。

**Performance budget:** 继承 GC-10：恢复按 facet 幂等重放，无全量重写；receipt 有界。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=RestoreEngine 恢复; recovery=revert 单提交 + journal 前滚; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=2; prod-files=4; scope=M; deps=OL-07,OL-09; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A

### Task OL-11: undo / redo / revert 追加式语义与 op 子命令

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `operation/undo.rs`：`undo`（先 snapshot 当前磁盘，取 target op 的 `parent -> target` 状态差反向应用，写成新的 `Undo` operation）、`redo`（只重放与当前 lineage 可证明匹配的 undo，分叉/歧义返回稳定错误）、`revert`（把某 op 相对其父的逆向 delta 合并到当前 view）；新增 `op undo/redo/revert` 子命令与 `op_undo_redo` target + crash matrix。唯一行为轴：追加式撤销/重做/逆向合并。

**Out of scope:** 不实现选择性 restore（OL-10）；不实现 doctor（OL-12）；不修改/删除 commit 对象（Undo commit 只移动 HEAD 指针）。

**Current evidence:** 设计文档第 3 节与 §5.3.4；`src/command/op.rs`。

**Acceptance criteria:**

- [ ] `undo` 先 snapshot 再取反向状态差并发布 `Undo` op；`undo` 自身可再次被 undo；不修改/删除 commit 对象。
- [ ] `redo` 只重放可证明匹配的 undo；分叉/歧义返回稳定错误与候选，不猜测。
- [ ] `revert` 把逆向 delta 合并到当前 view；merge op 需要显式 parent。
- [ ] 所有命令支持 `--dry-run` 与 JSON/machine receipt；receipt 列出 facet、路径计数、ref diff、目标/新 op ID。
- [ ] `op_undo_redo` target 覆盖：undo commit 后 HEAD 回到 parent、commit 对象 OID 不变、redo 后 HEAD 回到原 revision、重新 commit 生成新 OID 且 Change ID 继承（CH-03 前先验证 OID 行为）；target 注册并全绿。
- [ ] crash matrix：在 undo 各 journal phase 注入崩溃，重启后恢复路径自洽（`operation_restore_faults` 或独立 crash 用例）。
- [ ] `docs/commands/op.md` EN+zh 与 `docs/error-codes.md` 同步。

**Verification:**

- [ ] `source .env.test && cargo test --test op_undo_redo`（`(new)` target，同卡注册）
- [ ] crash matrix 用例（`source .env.test && cargo test --test operation_restore_faults undo_crash` 或同卡注册用例）
- [ ] 手工/脚本证据：`undo commit` 后 `git rev-parse HEAD`/`libra log` 显示 parent，commit 对象仍存在（sanitized）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-10（RestoreEngine）

**Implementation write set:** `src/internal/operation/undo.rs`、`src/command/op.rs`、`tests/op_undo_redo.rs`、`Cargo.toml`、`tests/INDEX.md`、`docs/commands/op.md`（EN+zh）、`docs/error-codes.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/undo.rs`、`src/command/op.rs`、`tests/op_undo_redo.rs`、`docs/commands/op.md`、`docs/commands/zh-CN/op.md`、`docs/error-codes.md`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** `docs/commands/op.md` EN+zh 新增 `op undo/redo/revert`；`docs/error-codes.md` 同步。

**Rollback mode:** revert（单提交回滚；运行期由 journal 前滚保护）

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：revert 合并冲突时停止并要求显式选择，不覆盖后续工作。

**Performance budget:** 继承 GC-10：undo/redo 按 op 祖先链定向重放，无全历史扫描。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=undo/redo/revert 追加式; recovery=revert 单提交 + 前滚; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=2; prod-files=4; scope=M; deps=OL-10; writeset=序列化于 OL-10; release=independent; split-from=N/A; exception=N/A

### Task OL-12: doctor 修复引擎与 op doctor 子命令

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `operation/doctor.rs`：对象闭包校验、op heads 校验、journal 前滚/回退、workspace pointer 修复，参考 jj `debug operation`/`debug reindex` 思路；新增 `libra op doctor` 子命令。唯一行为轴：损坏 Operation 状态的诊断与修复。

**Out of scope:** 不实现 restore/undo 语义（OL-10/OL-11）；不处理 worktree registry 修复（归 worktree doctor 范围）。

**Current evidence:** 设计文档 §5.1（`operation/doctor.rs` 职责）；`src/command/op.rs`。

**Acceptance criteria:**

- [ ] doctor 检查对象闭包（pre/post view 引用对象可达）、op heads 一致性、journal 未完成 entry、workspace pointer 与 heads 关系。
- [ ] 修复动作：journal 前滚/回退、孤儿 head 报告、pointer 重建；每项修复可 dry-run。
- [ ] `op doctor` 子命令只读默认、`--fix` 显式修复，输出 JSON/machine receipt。
- [ ] 单元/集成测试覆盖：缺对象、坏 journal、pointer 落后等场景（`cargo test --test command_test op doctor` 或独立用例）。
- [ ] `docs/commands/op.md` EN+zh 与 `docs/error-codes.md` 同步。

**Verification:**

- [ ] `source .env.test && cargo test --test command_test op doctor`（含 `(new)` 用例）
- [ ] 手工证据：人为损坏 fixture 后 `op doctor --dry-run` 与 `--fix` 的 receipt 输出（sanitized）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-10（journal/restore 语义）

**Implementation write set:** `src/internal/operation/doctor.rs`、`src/command/op.rs`、相关测试、`docs/commands/op.md`（EN+zh）、`docs/error-codes.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/doctor.rs`、`src/command/op.rs`、`docs/commands/op.md`、`docs/commands/zh-CN/op.md`、`docs/error-codes.md`

**Docs and compatibility impact:** `docs/commands/op.md` EN+zh 新增 `op doctor`；`docs/error-codes.md` 同步。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：修复动作默认只读，`--fix` 需显式确认；receipt 不含 secret。

**Performance budget:** 继承 GC-10：闭包校验按引用遍历有界；journal 扫描按索引。

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=doctor 修复引擎; recovery=revert 单提交; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=2; prod-files=3; scope=S; deps=OL-10; writeset=序列化于 OL-10; release=independent; split-from=N/A; exception=N/A

### Task CH-01: ChangeId 类型、随机生成与 legacy synthetic

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `change/identity.rs`：`ChangeId([u8; 16])`、`generate`（production 用 `ring::rand::SystemRandom`）、`synthetic_for_commit`（SHA-256 域分隔前 16 字节）、`to_hex`/`short_prefix`；按 OL-00 结论把 canonical Change ID 接入 sidecar/投影，已有 header 仅作为可选导入来源，不预留 Libra header 写入点。唯一行为轴：ChangeId 类型与生成规则。

**Out of scope:** 不实现 change 表投影查询（CH-02）；不实现 builder（CH-03）；不实现前缀解析（CH-02）。

**Current evidence:** 设计文档 §2「Change ID生成算法」与 §5.2.4；`src/internal/db.rs:557-606`（change 表在 OL-02 落地）。

**Acceptance criteria:**

- [ ] `ChangeId` 为 128-bit opaque 类型，`generate` 使用 CSPRNG；不基于 commit 内容/message/时间/用户计算。
- [ ] `synthetic_for_commit` 实现 `first_16_bytes(SHA-256("libra-change-id-v1\0" || object_format || commit_oid_bytes))`；SHA-1/SHA-256 仓库行为一致。
- [ ] 本地写入冲突检测：随机 ID 被无关 change 占用时重新生成；导入碰撞显式报告，不自动合并。
- [ ] 单元测试覆盖随机性约束（不校验内容）、synthetic 确定性、碰撞处理（`cargo test --lib internal::change::identity`）。
- [ ] OL-00 spike 结论已消费：sidecar-only 写入决策和已有 header 导入语义在代码/ADR 中可追溯。

**Verification:**

- [ ] `source .env.test && cargo test --lib internal::change::identity`（含 `(new)` 用例）
- [ ] 手工证据：synthetic ID 对同一 commit 两次生成一致、不同 object format 不同（sanitized）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-00（spike 结论）、OL-02（change 表落地）

**Implementation write set:** `src/internal/change/identity.rs`、`src/internal/change/mod.rs`、相关单元测试

**Release write set:** Inherited

**Files likely touched:** `src/internal/change/identity.rs`、`src/internal/change/mod.rs`

**Docs and compatibility impact:** 仅内部开发文档（设计文档 ADR-OL-03/OL-04 已在计划层记录）。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07/GC-11：`generate` 用 `Result` 传播 CSPRNG 错误，不 `unwrap`。

**Performance budget:** N/A（O(1) 生成）

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=ChangeId 类型与生成; recovery=revert 单提交; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=2; scope=S; deps=OL-00,OL-02; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A

### Task CH-02: change 投影存储与 short-prefix 解析

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `change/store.rs`（`ChangeRevision` 投影：`change_identity/change_revision` 表的读写与查询）与 `change/resolve.rs`（`resolve_change_id_prefix`：Exact/Ambiguous/NotFound，human 短前缀可辨识、JSON/machine 输出完整 ID）。新增 `change_id_resolution` target。唯一行为轴：change 身份的投影查询与前缀解析。

**Out of scope:** 不实现 rewrite 写入（CH-03）；不实现 genealogy 多边查询（CH-04）。

**Current evidence:** 设计文档 §5.2.4/§5.3.5；`src/internal/db.rs:557-606`（change 表在 OL-02 落地）。

**Acceptance criteria:**

- [ ] `change_identity/change_revision` 表读写封装：`(change_id, commit_oid, created_op_id, revision_ordinal)` 投影可查询、可见性标记（visible/hidden）生效。
- [ ] `resolve_change_id_prefix` 返回 Exact/Ambiguous/NotFound 三态；歧义时给出候选列表与诊断。
- [ ] human 输出用可辨识短前缀；JSON/machine 输出完整 canonical ID。
- [ ] `change_id_resolution` target 注册（`Cargo.toml [[test]]` + `tests/INDEX.md`）并全绿：精确/歧义/不存在/短前缀长度边界。
- [ ] 单元测试覆盖投影读写与解析边界（`cargo test --lib internal::change`）。

**Verification:**

- [ ] `source .env.test && cargo test --test change_id_resolution`（`(new)` target，同卡注册）
- [ ] `source .env.test && cargo test --lib internal::change`（含 `(new)` 用例）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** CH-01（ChangeId 类型）

**Implementation write set:** `src/internal/change/store.rs`、`src/internal/change/resolve.rs`、`src/internal/change/mod.rs`、`tests/change_id_resolution.rs`、`Cargo.toml`、`tests/INDEX.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/change/{store,resolve,mod}.rs`、`tests/change_id_resolution.rs`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** 仅内部开发文档。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** N/A

**Performance budget:** 继承 GC-10：前缀解析用索引查询（`change_id` 前缀索引），无全表扫描。

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=change 投影与解析; recovery=revert 单提交; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=2; scope=S; deps=CH-01; writeset=序列化于 CH-01; release=independent; split-from=N/A; exception=N/A

### Task CH-03: ChangeRevisionBuilder 与 rewrite 调用点接入

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `change/builder.rs`：`ChangeRevisionBuilder`（`for_new_change`/`for_rewrite`/`set_change_id`/`generate_new_change_id`/`set_predecessors`/`build`），build 内写 commit 对象 → 插入 `change_revision` → 记录 `PredecessorEdge(kind)`；把 commit/amend/rebase/cherry-pick 调用点改为唯一入口（禁止调用点临时写 `change_predecessor`）；按 OL-00 决策写 sidecar/投影，不写 commit header。新增 `change_genealogy_rebase` target。唯一行为轴：commit 构建/重写统一入口与 Change ID 继承。

**Out of scope:** 不实现 squash/split/duplicate 多边与 AI 链接（CH-04）；不实现 undo（OL-11 已覆盖 OID 语义）。

**Current evidence:** 设计文档 §5.3.5（对照 jj `commit_builder.rs`）；`src/command/commit.rs`、`src/command/rebase.rs`、`src/command/cherry_pick.rs`。

**Acceptance criteria:**

- [ ] `ChangeRevisionBuilder` 实现：新 change 随机 ID、rewrite 继承、duplicate 新 ID、`build` 原子写 commit + `change_revision` + `PredecessorEdge`。
- [ ] commit/amend/rebase/cherry-pick 调用点全部改走 builder；`rg 'change_predecessor' src/command src/internal --glob '!**/change/**'` 无直接写入（按退出码模板）。
- [ ] amend/rebase 后新 revision 继承原 Change ID；`change_genealogy_rebase` target 验证 rebase 前后 `change_revision` 投影与 predecessor 边；target 注册并全绿。
- [ ] sidecar/投影写入按 OL-00 决策落地，且不注入 `change-id` commit header；写入后与真实 Git 互操作（`git fsck` 无错误）。
- [ ] 失败路径：builder 中途失败不留半写状态（事务/幂等键）。
- [ ] `docs/commands/commit.md`、`docs/commands/rebase.md`、`docs/commands/cherry_pick.md` EN+zh 与 `COMPATIBILITY.md` 同步（CH-03 需说明 sidecar-only 与已有 header 导入兼容语义）。

**Verification:**

- [ ] `source .env.test && cargo test --test change_genealogy_rebase`（`(new)` target，同卡注册）
- [ ] `source .env.test && cargo test --lib internal::change::builder`（含 `(new)` 用例）
- [ ] sidecar/projection 写入不注入 `change-id` header，并与真实 Git 互操作的手工证据：`git fsck` 与 `libra log` 输出（sanitized）；已有 header 导入读取由 CH-01 承接
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** CH-02（投影）

**Implementation write set:** `src/internal/change/builder.rs`、`src/internal/change/mod.rs`、`src/command/commit.rs`、`src/command/rebase.rs`、`src/command/cherry_pick.rs`、`tests/change_genealogy_rebase.rs`、`Cargo.toml`、`tests/INDEX.md`、`docs/commands/*.md`（EN+zh）、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/change/builder.rs`、`src/command/{commit,rebase,cherry_pick}.rs`、`tests/change_genealogy_rebase.rs`、`docs/commands/{commit,rebase,cherry_pick}.md`、`docs/commands/zh-CN/{commit,rebase,cherry_pick}.md`、`COMPATIBILITY.md`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** 按 GC-05 同步 commit/rebase/cherry_pick 命令文档 EN+zh；记录 sidecar-only 约束和已有 header 导入兼容语义。

**Rollback mode:** revert（单提交回滚；已写 sidecar/投影的仓库前滚修复）

**Migration and rollback:** N/A（sidecar 为新增投影；不写入 commit header，已有 header 仅作导入兼容）

**Security and privacy:** 继承 GC-07：ChangeId 不嵌入可推断内容的哈希。

**Performance budget:** 继承 GC-10：builder 每次构建 O(1) 对象写 + 索引插入；无全历史扫描。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=ChangeRevisionBuilder 重写入口; recovery=revert 单提交 + 前滚修复; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=3; prod-files=8; scope=M; deps=CH-02; writeset=序列化于 CH-02; release=independent; split-from=N/A; exception=N/A

### Task CH-04: genealogy 多边关系与 AI 因果链接

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 实现 `change/genealogy.rs`：`PredecessorEdge`（含 `RelationKind`：Amend/Rebase/CherryPick/Squash/Split/Duplicate/Import/ExternalReconcile）与 evolution 查询；squash/split/duplicate 调用点接入 builder；`ai_operation_link` 关联 Operation/Change 与 Intent/Run/Invocation；AI FileHistoryStore 读取迁移。新增 `change_genealogy_squash_split` target。唯一行为轴：typed predecessor 多边谱系与 AI 对象关联。

**Out of scope:** 不实现 rebase 主线（CH-03）；不实现 Web 图（OL-14）。

**Current evidence:** 设计文档 §5.2.4/§5.3.5/§5.5（`ai_operation_link` 表、FileHistoryStore 只读迁移）；`src/internal/ai/libra_vcs.rs`、AI `FileHistoryStore` 现状。

**Acceptance criteria:**

- [ ] `PredecessorEdge` 与 evolution 查询实现；squash/split/duplicate 产生多边 predecessor 且 relation kind 正确。
- [ ] squash/split/duplicate 调用点走 builder；split 不能让两个独立可见 change 共享同一 Change ID。
- [ ] `ai_operation_link` 关联写入/查询；Operation/Change 通过稳定 ID 关联 AI 对象，不绑定易变 Commit OID。
- [ ] AI FileHistoryStore 读取迁移：只读/迁移兼容，不再作为新 undo 实现（设计文档 §5.5）。
- [ ] `change_genealogy_squash_split` target 注册（`Cargo.toml [[test]]` + `tests/INDEX.md`）并全绿：squash 合并、split 独立 Change ID、duplicate 新 ID、多边查询。
- [ ] `docs/development/commands/*.md` 与 `COMPATIBILITY.md` 同步 relation 语义说明。

**Verification:**

- [ ] `source .env.test && cargo test --test change_genealogy_squash_split`（`(new)` target，同卡注册）
- [ ] `source .env.test && cargo test --lib internal::change::genealogy`（含 `(new)` 用例）
- [ ] AI 链接查询手工证据：`ai_operation_link` 按 intent_id 查询返回稳定 Change ID（sanitized）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** CH-03（builder）

**Implementation write set:** `src/internal/change/genealogy.rs`、`src/internal/change/mod.rs`、squash/split/duplicate 调用点、`src/internal/ai/libra_vcs.rs`、AI FileHistoryStore 迁移、`tests/change_genealogy_squash_split.rs`、`Cargo.toml`、`tests/INDEX.md`、`docs/development/commands/*.md`、`COMPATIBILITY.md`

**Release write set:** Inherited

**Files likely touched:** `src/internal/change/genealogy.rs`、`src/command/**`（squash/split/duplicate 相关）、`src/internal/ai/libra_vcs.rs`、`tests/change_genealogy_squash_split.rs`、`docs/COMPATIBILITY.md`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** `COMPATIBILITY.md` 记录 relation 语义与 AI 链接迁移说明。

**Rollback mode:** revert（单提交回滚；已迁移 FileHistoryStore 数据前滚修复）

**Migration and rollback:** FileHistoryStore 为只读迁移：旧读路径保持可读，新实现接管写入；数据不变量：`change_revision` 主键唯一、`change_predecessor` 主键唯一。

**Security and privacy:** 继承 ER-11：`ai_operation_link` 只存 redacted ID，不存 prompt/transcript/secret。

**Performance budget:** 继承 GC-10：genealogy 查询按 `(change_id, commit_oid)` 索引；无全谱系扫描。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=genealogy 多边与 AI 链接; recovery=revert 单提交 + 前滚修复; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=3; prod-files=7; scope=M; deps=CH-03; writeset=序列化于 CH-03; release=independent; split-from=N/A; exception=N/A

### Task OL-13: 多 worktree Operation heads 与 reconcile

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 在 `operation/store.rs` 实现多 head 发布与 `reconcile` operation：CAS 并发分叉保留两个 head，可证明无歧义时创建多父 reconcile operation 收敛 head 集合；冲突（同一 ref 不同目标）保留两 head 并报告，不猜测胜者。新增 `operation_restore_multi_worktree` target。唯一行为轴：多 worktree 并发 Operation 的 head 收敛。

**Out of scope:** 不实现 Web 图（OL-14）；不实现仓库级发布锁（ER-12 声明禁止并发发布，按 DEFER-01 登记）。

**Current evidence:** 设计文档 §2「多 CLI 进程、多 worktree 下的 append-only DAG」与 §5.3.3 CAS；`src/internal/worktree_scope.rs`。

**Acceptance criteria:**

- [ ] 并发分叉时 CAS 失败保留两个 head；reconcile 只合并可证明无歧义的状态（设计文档 §2 表格语义）。
- [ ] 同一 ref 不同目标的冲突保留两 head 并报告；不自动猜测胜者。
- [ ] worktree lease/generation 保护同一物理 worktree：旧写入者不得发布。
- [ ] `operation_restore_multi_worktree` target 注册并全绿：双 worktree 并发修改、reconcile 后 restore、冲突报告。
- [ ] `docs/development/commands/*.md` 与 `docs/commands/op.md` EN+zh 同步多 worktree 语义说明。

**Verification:**

- [ ] `source .env.test && cargo test --test operation_restore_multi_worktree`（`(new)` target，同卡注册）
- [ ] `source .env.test && cargo test --lib internal::operation::store reconcile`（含 `(new)` 用例）
- [ ] 手工证据：双 worktree 冲突场景的 `libra op` 输出（sanitized）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-11（undo 语义）

**Implementation write set:** `src/internal/operation/store.rs`（reconcile）、`src/internal/operation/`（如 reconcile 独立模块）、`tests/operation_restore_multi_worktree.rs`、`Cargo.toml`、`tests/INDEX.md`、`docs/commands/op.md`（EN+zh）

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation/store.rs`、`src/internal/operation/reconcile.rs`（如新增）、`tests/operation_restore_multi_worktree.rs`、`docs/commands/op.md`、`docs/commands/zh-CN/op.md`、`Cargo.toml`、`tests/INDEX.md`

**Docs and compatibility impact:** `docs/commands/op.md` EN+zh 同步多 worktree/reconcile 语义。

**Rollback mode:** revert（单提交回滚；已发布分叉 head 前滚收敛）

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07：reconcile 只合并可证明无歧义状态，冲突显式报告。

**Performance budget:** 继承 GC-10：reconcile 按 facet/ref 差异合并，无全量状态重算。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=多 worktree heads/reconcile; recovery=revert 单提交 + 前滚; complete=yes; self-contained=yes; AC=5/8; VER=3/8; landing=2; prod-files=4; scope=M; deps=OL-11; writeset=序列化于 OL-11; release=independent; split-from=N/A; exception=N/A

### Task OL-14: Operation/Change Web 只读图

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 基于冻结的后端事实源构建独立只读 read model，提供 Operation DAG 与 Change genealogy 的 Web 图：有界节点/深度、redaction（不展示 secret/PII）、multihead fallback；`web/` 前端 + 后端查询接口。新增 web graph tests。唯一行为轴：Operation/Change 的 Web 只读投影。

**Out of scope:** 不提供 Web 写入口；不做 SSE 实时推送（延后 DEFER-02）；不实现后端多 head reconcile（OL-13）。

**Current evidence:** 设计文档 §5.4 Phase 7「先冻结后端事实源，再构建 Web projection」；`web/` 为 Next.js 静态导出。

**Acceptance criteria:**

- [ ] 只读 read model 从 v2 operation/change 表投影；查询接口返回有界图（节点/深度上限）。
- [ ] Web 图渲染 Operation DAG 与 Change genealogy；multihead fallback 明确显示未收敛 head。
- [ ] redaction：图元数据不展示 prompt/transcript/secret/lease token（`ai_operation_link` 只展示 redacted ID）。
- [ ] web graph tests 覆盖：空仓库、单链、多 head、redaction 字段、有界分页。
- [ ] `pnpm --dir web lint`、`pnpm --dir web test`、`pnpm --dir web build` 全绿。
- [ ] `docs/development/commands/*.md` 与 `web/` 文档同步只读图说明。

**Verification:**

- [ ] `pnpm --dir web lint`、`pnpm --dir web test`、`pnpm --dir web build`（web 表面 focused 门）
- [ ] `source .env.test && cargo test --test command_test graph`（后端查询回归，如适用）
- [ ] 手工证据：浏览器/快照展示多 head 与 redaction（sanitized）
- [ ] 三门（C 组，独立发布卡自行执行）

**Dependencies:** OL-13（多 head read model）、CH-04（genealogy 查询）

**Implementation write set:** `web/src/**`（图组件与查询）、后端只读查询（`src/internal/` 或 `web/` API 层）、相关测试、`docs/development/commands/*.md`

**Release write set:** Inherited（含 `web/package.json` 版本面）

**Files likely touched:** `web/src/**`、`web/package.json`、`web/out`（构建产物，如入库）、`docs/development/commands/*.md`

**Docs and compatibility impact:** `web/` 按 `web/package.json` scripts 验收；同步 `docs/development/commands/*.md`。

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承 GC-07/ER-11：图接口必须 redact 因果元数据；默认不暴露 secret/lease token。

**Performance budget:** 继承 GC-10：图查询有界（节点/深度上限），无全库图遍历。

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=implementation; axis=Operation/Change Web 图; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=3; prod-files=8; scope=M; deps=OL-13,CH-04; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A

### Task OL-15: 移除 v1 operation 代码、表与命令路径

**Task type:** removal

**Lifecycle / Acceptance:** pending / 空

**Description:** 删除 v1 实现：`src/internal/operation_wrapper.rs`、`src/internal/operation.rs`（v1 `OperationService` 被替换部分）、v1 `operation_view*` 表与 model、v1 `op` 命令分支；`rg 'operation_wrapper|operation_view|restorable' src` 零命中；文档与兼容收口，签名发布。唯一行为轴：v1 实现整体退场（ADR-OL-01，替换窗口证据 = 设计文档 §5.1 + 本计划各阶段验收）。

**Out of scope:** 不删除 v2 代码；不维护 v1 兼容 adapter；不处理仓库级发布锁（DEFER-01）。

**Current evidence:** `src/internal/operation_wrapper.rs`（`:46/:60-61/:405`）、`src/internal/operation.rs:1018`、`src/internal/db.rs:557-606`（v1 表）、`src/command/op.rs`；设计文档 §5.4 Gate-8。

**Acceptance criteria:**

- [ ] v1 代码/表/命令路径删除完成；编译通过。
- [ ] `rg 'operation_wrapper|operation_view|restorable' src` 零命中（按「Verification 判定口径」退出码模板，区分 `1` 与 `>1`）。
- [ ] `docs/commands/op.md` EN+zh 重写为 v2 子命令面；`COMPATIBILITY.md` 记录 v1→v2 移除与替换窗口；`docs/error-codes.md` 清理 v1 专属码。
- [ ] 回归：`cargo test --all` 全绿（v1 相关测试删除或改写为 v2 语义）。
- [ ] `tests/INDEX.md` 与 `Cargo.toml [[test]]` 同步（删除 v1 专用 target 或改写）。
- [ ] 签名发布完成：版本面五处 bump minor，build/install/push/tag（按 ER-04 C 组）。

**Verification:**

- [ ] `rg 'operation_wrapper|operation_view|restorable' src` 零命中守卫（退出码模板）
- [ ] `source .env.test && cargo test --all`（三门之一，C 组覆盖）
- [ ] `source .env.test && cargo test --test compat_version_surface_sync`（版本面守卫）
- [ ] 三门 + 发布收口（ER-04 C 组：bump、build、install、commit、push、tag 判据）

**Dependencies:** OL-12（doctor 已就绪）、OL-14（Web 投影已冻结事实源）

**Implementation write set:** `src/internal/operation_wrapper.rs`（删除）、`src/internal/operation.rs`（v1 部分删除）、`src/internal/db.rs`（v1 表删除）、`src/internal/model/`（v1 model 删除）、`src/command/op.rs`（v1 分支删除）、`docs/commands/op.md`（EN+zh）、`COMPATIBILITY.md`、`docs/error-codes.md`、`tests/INDEX.md`、`Cargo.toml`

**Release write set:** Inherited

**Files likely touched:** `src/internal/operation_wrapper.rs`、`src/internal/operation.rs`、`src/internal/db.rs`、`src/internal/model/**`、`src/command/op.rs`、`docs/commands/op.md`、`docs/commands/zh-CN/op.md`、`COMPATIBILITY.md`、`docs/error-codes.md`、`tests/INDEX.md`、`Cargo.toml`、版本面五处

**Docs and compatibility impact:** `docs/commands/op.md` EN+zh 重写；`COMPATIBILITY.md` 记录 v1 移除；`docs/error-codes.md` 清理。

**Rollback mode:** revert（单提交回滚可恢复 v1 代码；已发布版本按 ER-09 前滚）

**Migration and rollback:** v1 表已在 OL-02 移除；本卡删除 v1 代码路径与文档。deprecation/替换窗口证据 = 设计文档 §5.1 + 本计划 Phase 1-7 验收记录。

**Security and privacy:** 继承 GC-07：删除路径不留旧 secret/redaction 逻辑残留。

**Performance budget:** N/A（净删除）

**Estimated scope:** M

**Version increment:** minor（公开命令路径移除，ADR-OL-01 + 替换窗口证据）

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** type=removal; axis=v1 代码/表/命令移除; recovery=revert 单提交（发布后前滚）; complete=yes; self-contained=yes; AC=6/8; VER=2/8; landing=4; prod-files=10; scope=M; deps=OL-12,OL-14; writeset=序列化于 OL-14; release=independent; split-from=N/A; exception=N/A

## 测试矩阵

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | ChangeId 生成/synthetic、facet/view codec、CAS、middleware 分类、pointer stale、projection 读写 | `cargo test --lib internal::operation::*`、`cargo test --lib internal::change::*` |
| 集成 | 单 worktree 快照 roundtrip、index/sequencer roundtrip、op undo/redo、restore 故障、command coverage、agent shell、multi-worktree reconcile、change genealogy | `cargo test --test workspace_snapshot_roundtrip`、`--test index_snapshot_roundtrip`、`--test sequencer_snapshot_roundtrip`、`--test op_undo_redo`、`--test operation_restore_faults`、`--test operation_command_coverage`、`--test agent_shell_operation`、`--test operation_restore_multi_worktree`、`--test change_genealogy_rebase`、`--test change_genealogy_squash_split`、`--test change_id_resolution` |
| 兼容 | Git 互操作（已有 change-id header 读取/导入兼容；Libra Change ID sidecar-only 写入；`git fsck`）、v1→v2 存储替换、版本面守卫 | `cargo test --test commit_change_id_header_spike`、`cargo test --test compat_version_surface_sync`、既有 `tests/compat/*` |
| 迁移 | v2 schema 新建库/既有库一致、v1 审计导入（如启用）、FileHistoryStore 只读迁移 | `cargo test --lib internal::operation::store`（schema 用例）、导入脚本 dry-run |
| 安全 | redaction（`ai_operation_link` 无 secret）、fail-closed（未知 mutation 拒绝）、repo-wide refs 显式确认 | `cargo test --test operation_command_coverage`（Unknown 类）、`--test operation_restore_faults`（确认路径） |
| 性能 | status 零回归、snapshot 扫描预算、Web 图有界 | benchmark（OL-01）、`cargo test --lib internal::operation::snapshot`、web graph tests |
| live/gated | 真实 LLM/Agent provider 下的 tool mutation 归因（如可用） | `--features test-live-ai` 的既有 AI target（Wave 4，仅当 env 可用） |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| OL-00 | 设计文档 §2/§5.4 Gate-0 | `tests/commit_change_id_header_spike.rs` | ADR-OL-04 修订 | `commit_change_id_header_spike` |
| OL-01 | 设计文档 §5.1/§5.3.2 | `src/internal/worktree_io/` | `docs/development/commands/*.md` | `cargo test --lib internal::worktree_io` |
| OL-02 | 设计文档 §5.2.5 | `src/internal/db.rs`、`sql/**`、`src/internal/model/` | `COMPATIBILITY.md`、`docs/error-codes.md` | schema 一致性用例、`compat_version_surface_sync` |
| OL-03 | 设计文档 §5.2.1/§5.2.2 | `src/internal/operation/{view,facet}.rs` | 模块注释 | `cargo test --lib internal::operation::view/facet` |
| OL-04 | 设计文档 §5.2.3/§5.3.3 | `src/internal/operation/store.rs` | 模块注释 | `cargo test --lib internal::operation::store`、`operation_dag` |
| OL-05 | 设计文档 §5.3.2 | `src/internal/operation/working_copy.rs` | 模块注释 | `cargo test --lib internal::operation::working_copy` |
| OL-06 | 设计文档 §2/§5.3.2 | `src/internal/operation/snapshot.rs` | 模块注释 | `workspace_snapshot_roundtrip` |
| OL-07 | 设计文档 §5.2.2 | `src/internal/operation/`、`src/internal/sequencer/` | 模块注释 | `index_snapshot_roundtrip`、`sequencer_snapshot_roundtrip` |
| OL-08 | 设计文档 §5.3.1 | `src/internal/operation/middleware.rs` | 模块注释 | `cargo test --lib internal::operation::middleware` |
| OL-09 | 设计文档 §5.3.1 | `src/cli.rs`、`src/internal/ai/tools/*`、`ai/libra_vcs.rs` | `docs/development/commands/*.md` | `operation_command_coverage`、`agent_shell_operation` |
| OL-10 | 设计文档 §5.3.4 | `src/internal/operation/restore.rs`、`src/command/op.rs` | `docs/commands/op.md` EN+zh、`docs/error-codes.md` | `operation_restore_faults` |
| OL-11 | 设计文档 第 3 节/§5.3.4 | `src/internal/operation/undo.rs`、`src/command/op.rs` | `docs/commands/op.md` EN+zh、`docs/error-codes.md` | `op_undo_redo`、crash matrix |
| OL-12 | 设计文档 §5.1 | `src/internal/operation/doctor.rs`、`src/command/op.rs` | `docs/commands/op.md` EN+zh | `command_test op doctor` |
| CH-01 | 设计文档 §2/§5.2.4 | `src/internal/change/identity.rs` | ADR-OL-03/OL-04 | `cargo test --lib internal::change::identity` |
| CH-02 | 设计文档 §5.2.4/§5.3.5 | `src/internal/change/{store,resolve}.rs` | 模块注释 | `change_id_resolution` |
| CH-03 | 设计文档 §5.3.5 | `src/internal/change/builder.rs`、`src/command/{commit,rebase,cherry_pick}.rs` | `docs/commands/*.md` EN+zh、`COMPATIBILITY.md` | `change_genealogy_rebase` |
| CH-04 | 设计文档 §5.2.4/§5.5 | `src/internal/change/genealogy.rs`、`ai/libra_vcs.rs` | `COMPATIBILITY.md` | `change_genealogy_squash_split` |
| OL-13 | 设计文档 §2/§5.3.3 | `src/internal/operation/store.rs` | `docs/commands/op.md` EN+zh | `operation_restore_multi_worktree` |
| OL-14 | 设计文档 §5.4 Phase 7 | `web/src/**` | `docs/development/commands/*.md` | web graph tests |
| OL-15 | 设计文档 §5.1/§5.4 Gate-8 | `src/internal/operation_wrapper.rs` 等删除 | `docs/commands/op.md` EN+zh、`COMPATIBILITY.md`、`docs/error-codes.md` | `rg` 零命中守卫、`compat_version_surface_sync` |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | 本计划 + 设计文档迁移合入 | commit / 本计划与设计文档路径 | N/A |
| M1 | Phase 1（OL-01..04）完成：v2 持久化底座可用 | `operation_dag`、store 测试、status 零回归 benchmark | 按卡 revert；schema 前滚（OL-02） |
| M2 | Phase 2（OL-05..07）完成：完整快照 | `workspace/index/sequencer_snapshot_roundtrip` | 按卡 revert |
| M3 | Phase 3（OL-08..09）完成：全修改记录 | `operation_command_coverage`、`agent_shell_operation`、census guard | 按卡 revert |
| M4 | Phase 4（OL-10..12）完成：undo/redo/restore/doctor | `op_undo_redo`、`operation_restore_faults`、crash matrix | journal 前滚/回退 + 按卡 revert |
| M5 | Phase 5-6（CH-01..04）完成：Change ID 与 genealogy | `change_id_resolution`、`change_genealogy_rebase/squash_split` | 前滚修复 + 按卡 revert |
| M6 | Phase 7（OL-13..14）完成：并发与 Web 图 | `operation_restore_multi_worktree`、web tests | 前滚收敛 + 按卡 revert |
| M7 | Phase 8（OL-15）完成：v1 移除与 GA | `rg` 零命中、签名 release | 已发布版本按 ER-09 前滚 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| snapshot 扫描中断/挂死 | worktree lease/临时对象 | bounded I/O 超时 + `Partial` 标记 + doctor | 发布与磁盘事实不一致的 post-view |
| op-head CAS 失败 | 两个并发 head | reconcile operation 收敛（可证明无歧义时） | 覆盖并发 head / 静默成功 |
| journal 写入后崩溃 | running op + journal reservation | doctor 前滚/回退 | 双写 / 静默成功 |
| undo/restore 恢复中断 | 部分 facet 恢复 | 幂等重放 + 前滚到 target 或回退 pre-view | 半吊子状态 / 数据丢失 |
| Web 投影读到旧 schema | 旧只读投影 | 重建投影（事实源冻结后） | 不兼容读取 / 写入口 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| v1 schema 替换导致旧仓库审计数据丢失 | 高：用户可追溯性受损 | 一次性导入脚本 + 备份指引 + `COMPATIBILITY.md` 说明 | OL-02、OL-15 |
| snapshot 扫描引入 status 热路径回归 | 高：日常命令变慢 | `worktree_io` bounded I/O + status 零回归 benchmark | OL-01、OL-06 |
| 并发多 head 冲突让用户困惑 | 中：信任受损 | 显式冲突报告 + CAS + doctor + 多 worktree 测试 | OL-13 |
| change-id header 写入改变 Commit OID 或在 rewrite 中丢失 | 高：外部工具兼容与逻辑身份错配 | OL-00 spike 记录 header 互操作证据但冻结 sidecar-only；`git fsck` 与对象闭包验证 | OL-00、CH-03 |
| undo/redo 崩溃窗口产生不一致状态 | 高：数据安全 | journal phase + doctor + crash matrix | OL-10、OL-11、OL-12 |
| AI 因果对象绑定 Change ID 迁移失败 | 中：Agent 工作流丢失因果链 | `ai_operation_link` 表 + FileHistoryStore 只读迁移 + 查询测试 | CH-04 |
| 移除 v1 公开命令引发兼容投诉 | 中：用户升级受阻 | minor bump + 替换窗口证据 + 文档收口 | OL-15 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| Operation 发布 | O(1) manifest 写（内容寻址）+ journal append | O(N) ops 持久化 | 每 op 元数据有界（redacted） | store/journal 测试 |
| snapshot 扫描 | O(changed files)，stat 增量 | O(W) 工作区文件（缓存收敛） | worktree_io byte/frame 预算 | status 零回归 benchmark |
| change 投影查询 | O(1) 按 `change_id` 索引 | O(R) revisions | 无全表扫描 | `change_id_resolution` |
| genealogy 查询 | O(depth) 按索引遍历 | O(E) predecessor 边 | 有界深度 | `change_genealogy_*` |
| Web 图查询 | O(bound) 节点/深度 | O(bound) 页大小 | 图节点/深度上限 | web graph tests |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md` 已同步，或说明 `N/A`。（执行项：OL-02/OL-15/CH-03/CH-04 同步 v1→v2 存储、sidecar-only 与已有 header 导入语义、relation 语义）
- [ ] `docs/commands/<cmd>.md` 已同步，或说明 `N/A`。（执行项：`op.md` 在 OL-10/OL-11/OL-12/OL-13/OL-15；`commit/rebase/cherry_pick.md` 在 CH-03）
- [ ] `docs/commands/zh-CN/<cmd>.md` 已同步，或说明 `N/A`。（同上，EN+zh 双份）
- [ ] `docs/development/commands/<cmd>.md` 已同步，或说明 `N/A`。（OL-01/OL-09/OL-13/OL-14）
- [ ] `docs/error-codes.md` 已同步，或说明 `N/A`。（OL-02/OL-10/OL-11/OL-12/OL-15）
- [ ] `tests/INDEX.md` 已同步，或说明 `N/A`。（每个新增 target 的同卡注册）
- [ ] `Cargo.toml` `[[test]]` 已同步，或说明 `N/A`。（同上）
- [ ] `plan-long.md` 日期计划索引或 LR 状态已同步，或说明 `N/A`。（执行期在 M1/M5 里程碑更新 LR-02/LR-03 状态；本计划文档本身不改 plan-long.md）

## Codex review log

Result 只允许 `PASS` 或 `FAIL`。`FAIL` 必须列出 P0/P1 条目并在下一轮复审关闭；P2 可由具名责任人书面接受为 residual risk，但不改变本轮 `FAIL` 记录（ER-05）。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R1 | 本计划全文（20 张任务卡 + 模板章节） | PASS | 无 | 无 | 与设计文档逐节核对 + plan-template.md v2 逐节核对（R1 修正见「修订历史」：AC/VER 计数对齐、补充规范性 ID 表）；无未关闭 P0/P1 |
| R2 | OL-00 spike 实现、真实 Git 测试、sidecar-only 决策、计划同步与 CI Rust prerequisite 修复 | PASS | 无 | 无 | 独立 reviewer 复核；stable 1.98.0 clippy/doc/test 与 nightly 1.100.0 fmt 均通过；无未关闭 P0/P1/P2 |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-01 | 仓库级发布锁（原子认领、fence 校验、超时回收、崩溃恢复） | ER-12：当前无仓库级发布锁，发布一律串行；并发发布需要独立 ADR + 独立计划 | 出现必须并发发布的场景时 | 独立 ADR + 独立计划 |
| DEFER-02 | Web 图 SSE 实时推送 | 只读投影先冻结事实源；实时推送增加复杂度 | M6 后按用户需求评估 | 独立 Web 计划 |
| DEFER-03 | 权限模型（authorization/approval/sandbox） | 设计文档第 1 节范围假设：默认最大权限 | 出现多用户/多租户需求时 | 独立安全计划 |
| DEFER-04 | v1 审计数据长期兼容读取 | 设计文档 §5.1：一次性导入脚本，不维护长期兼容层 | 出现必须长期互操作 v1 数据的场景 | 独立迁移计划 |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [ ] 所有任务卡满足粒度规则 `G-*`：无未登记的 L 例外、无 XL 卡、无碎片卡、无未登记的合并发布例外、实现写集冲突均已消解；「任务卡粒度审计表」已填齐。
- [ ] 所有非延后任务的 acceptance criteria 已满足，且 `Lifecycle=done` **且** `Acceptance=complete`（ER-04）。任何停在 `remote-pending` 的卡都必须先取得其 D 组远端后置门的绿色证据。任何仍为 `blocked` 的任务都必须先解除阻塞（`blocked` → `in-progress` → 完成剩余动作 → `done`）或按 `DEFER-*` 正式延后，不得带着 `blocked` 通过完成门。
- [ ] 所有任务的 Verification 命令已运行并记录结果。
- [ ] **计划完成门（区别于每卡 focused gate）**：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all` 全绿（L1 全量；L2/L3 未设置 env 时打印 skipped 可接受，失败不可接受）；若本计划改动 `web/`，`pnpm --dir web lint`、`pnpm --dir web test`、`pnpm --dir web build` 已通过；改动 `worker/` 时 `pnpm --dir worker lint`、`pnpm --dir worker test`、`pnpm --dir worker test:miniflare`、`pnpm --dir worker build` 已通过；改动 feature-gated 代码时，写明实际 feature/target/env 的命令已通过（不得留未替换占位符）。
- [ ] 必要的 docs/compat/error-code/test-index 更新已完成。
- [ ] 必要的 migration、rollback、failure-recovery 验证已完成；每张卡的 `Rollback mode` 都已被实际验证或记录为不可验证的原因。
- [ ] Codex review 最终结论为 `PASS`，P0/P1 全部关闭；仅 P2 residual risk 允许保留，且有具名接受人。
- [ ] 如有发布要求，版本面五处一致、构建、安装、提交、推送和发布证据已完成（ER-08）。
- [ ] 「修订历史」已记录成稿后的全部规范性变更（G-09）。
- [ ] `plan-long.md` 相关状态或日期计划索引已同步，或明确 `N/A`。
