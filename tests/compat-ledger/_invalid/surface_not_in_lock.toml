# the surface is absent from SURFACES.lock, so nothing adjudicates it.
#
# One deliberate defect, so the guard's refusal can be attributed to it.

[[scenario]]
id = "t4001-diff-format::shortstat-summarises-the-series"
category = "adapted"
command_status = "partial"
surface_compatibility = "git-compatible"
surface_evidence = "docs/commands/diff.md#description"
reason = "Fixture for the surface-lock half of the ledger guard."
owner = "libra-compat"
review_date = "2026-08-08"
upstream_revision = "dfb079967b9cbc99e533c21e65f674bb3f5e8b07"
upstream_file = "t4001-diff-format.sh"
libra_command = "diff"
libra_surface = "--shortstat"
