# `direct` claimed over a surface the LOCK records as deferred.
#
# One deliberate defect, so the guard's refusal can be attributed to it.

[[scenario]]
id = "t1400-update-ref::stdin-batch-updates"
category = "direct"
command_status = "partial"
surface_compatibility = "git-compatible"
surface_evidence = "docs/commands/update-ref.md#comparison-with-git"
reason = "Fixture for the surface-lock half of the ledger guard."
owner = "libra-compat"
review_date = "2026-08-08"
upstream_revision = "dfb079967b9cbc99e533c21e65f674bb3f5e8b07"
upstream_file = "t1400-update-ref.sh"
libra_command = "update-ref"
libra_surface = "--stdin"
