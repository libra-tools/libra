# Compatibility evidence for upstream t4005-diff-rename-2.sh, migrated in the t4 wave.
# One [[scenario]] per upstream test. The command and flags come from each
# scenario's BODY — what its assertion actually runs — and every flag is checked
# against the real CLI, not the documentation. A scenario that passes a flag Libra
# does not accept is blocked, however well documented the rest of it is.

[[scenario]]
id = "t4005-diff-rename-2::setup-reference-tree"
category = "direct"
command_status = "partial"
surface_compatibility = "git-compatible"
surface_evidence = "COMPATIBILITY.md:176"
reason = "hash-object is partial and every flag this scenario passes is accepted by Libra, with hash-object recorded git-compatible at the cited anchor (ADR-CT-06 rule 5)."
owner = "libra-compat"
review_date = "2026-08-08"
upstream_revision = "dfb079967b9cbc99e533c21e65f674bb3f5e8b07"
upstream_file = "t4005-diff-rename-2.sh"
libra_command = "hash-object"
libra_surface = "hash-object"
libra_tests = ["command::t4_port_test::t4_port_t4005_setup_reference_tree"]

[[scenario]]
id = "t4005-diff-rename-2::validate-output-from-rename-copy-detection-1"
category = "blocked"
command_status = "partial"
surface_compatibility = "absent"
surface_evidence = "docs/commands/diff.md#options"
reason = "Upstream needs diff-index -C, which Libra does not accept; the gap is exposure, not a design divergence (ADR-CT-06 rule 3)."
owner = "libra-compat"
review_date = "2026-08-08"
upstream_revision = "dfb079967b9cbc99e533c21e65f674bb3f5e8b07"
upstream_file = "t4005-diff-rename-2.sh"
libra_command = "diff-index"
libra_surface = "-C"
blocked_by = ["expose the diff-index porcelain flag surface (rename/copy detection, raw output)"]

[[scenario]]
id = "t4005-diff-rename-2::validate-output-from-rename-copy-detection-2"
category = "blocked"
command_status = "partial"
surface_compatibility = "absent"
surface_evidence = "docs/commands/diff.md#options"
reason = "Upstream needs diff-index -C, which Libra does not accept; the gap is exposure, not a design divergence (ADR-CT-06 rule 3)."
owner = "libra-compat"
review_date = "2026-08-08"
upstream_revision = "dfb079967b9cbc99e533c21e65f674bb3f5e8b07"
upstream_file = "t4005-diff-rename-2.sh"
libra_command = "diff-index"
libra_surface = "-C"
blocked_by = ["expose the diff-index porcelain flag surface (rename/copy detection, raw output)"]

[[scenario]]
id = "t4005-diff-rename-2::validate-output-from-rename-copy-detection-3"
category = "blocked"
command_status = "partial"
surface_compatibility = "absent"
surface_evidence = "docs/commands/diff.md#options"
reason = "Upstream needs diff-index -C, --find-copies-harder, which Libra does not accept; the gap is exposure, not a design divergence (ADR-CT-06 rule 3)."
owner = "libra-compat"
review_date = "2026-08-08"
upstream_revision = "dfb079967b9cbc99e533c21e65f674bb3f5e8b07"
upstream_file = "t4005-diff-rename-2.sh"
libra_command = "diff-index"
libra_surface = "-C"
blocked_by = ["expose the diff-index porcelain flag surface (rename/copy detection, raw output)"]
